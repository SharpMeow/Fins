/* Built by tools/build.mjs from src/post.mjs. Do not edit: change src/ and run npm run build. */
(() => {
  // src/layers/pace.js
  try {
    (function() {
      "use strict";
      var lastT = null;
      var answer = true;
      var prevT = null;
      var sinceP = 0;
      var monitorDt = 1 / 60;
      function cap() {
        try {
          var g = typeof gameState === "function" ? gameState() : typeof gameState === "object" ? gameState : null;
          var c = g ? +g.fpsCap : 0;
          return c > 0 ? c : 0;
        } catch (e) {
          return 0;
        }
      }
      function shouldPaint(t) {
        if (t === lastT) return answer;
        lastT = t;
        var dt = prevT == null ? 0 : (t - prevT) / 1e3;
        prevT = t;
        if (dt > 0 && dt < 0.25) monitorDt += (dt - monitorDt) * 0.05;
        var c = cap();
        if (!c) {
          sinceP = 0;
          answer = true;
          return answer;
        }
        sinceP += dt > 0 && dt < 0.25 ? dt : 0;
        if (sinceP < 1 / c - monitorDt / 2) {
          answer = false;
          return answer;
        }
        sinceP = 0;
        answer = true;
        return answer;
      }
      function seedWiki() {
        try {
          var wiki = typeof WIKI === "function" ? WIKI() : window.WIKI;
          if (!wiki || !wiki.push) return false;
          for (var i = 0; i < wiki.length; i++) if (wiki[i] && wiki[i].id === "k_pace") return true;
          wiki.push({
            id: "k_pace",
            sec: "Settings",
            t: "Frame rate",
            tags: "frame rate fps refresh hz vsync uncapped cap battery smooth settings monitor",
            w: "<p>Settings, Frame rate. <b>Auto</b> is the default: the shop paints every time your monitor refreshes, at 60, 144 or 240 Hz alike, with no cap. A number caps it, which saves power on a laptop. A cap only changes how often the picture is painted: the fish swim, the rain falls and the money comes in at exactly the same speed at any frame rate.</p>"
          });
          return true;
        } catch (e) {
          return false;
        }
      }
      (function seedSoon(tries) {
        if (!seedWiki() && tries > 0) setTimeout(seedSoon, 500, tries - 1);
      })(40);
      window.finsPace = {
        shouldPaint,
        cap,
        monitorHz: function() {
          return Math.round(1 / monitorDt);
        }
      };
    })();
  } catch (e) {
    console.error("Layer pace.js failed to start:", e);
  }

  // src/layers/bed.js
  try {
    (function() {
      "use strict";
      var patched = null;
      var NEAR = { s: 0.72, l: 0.86, h: 8 };
      var BACK = { s: 0.55, l: 0.62, h: 12 };
      var haze = null;
      var hazeDepth = -1;
      var depth = 0;
      var fixed = /* @__PURE__ */ new WeakMap();
      function floorY() {
        try {
          return typeof window.floorY === "number" ? window.floorY : NaN;
        } catch (e) {
          return NaN;
        }
      }
      function hexToHsl(s) {
        var m = /^#([0-9a-f]{6})$/i.exec(String(s || ""));
        if (!m) return null;
        var n = parseInt(m[1], 16);
        var r = (n >> 16 & 255) / 255, g = (n >> 8 & 255) / 255, b = (n & 255) / 255;
        var max = Math.max(r, g, b), min = Math.min(r, g, b), l = (max + min) / 2, h = 0, sat = 0;
        if (max !== min) {
          var d = max - min;
          sat = l > 0.5 ? d / (2 - max - min) : d / (max + min);
          h = max === r ? (g - b) / d + (g < b ? 6 : 0) : max === g ? (b - r) / d + 2 : (r - g) / d + 4;
          h *= 60;
        }
        return [h, sat * 100, l * 100];
      }
      function hslToRgb(h, s, l) {
        s /= 100;
        l /= 100;
        var a = s * Math.min(l, 1 - l);
        function f(n) {
          var k2 = (n + h / 30) % 12;
          return (l - a * Math.max(-1, Math.min(k2 - 3, Math.min(9 - k2, 1)))) * 255;
        }
        return [f(0), f(8), f(4)];
      }
      function paletteHue(fillStyle) {
        var hsl = hexToHsl(fillStyle);
        var c = hsl ? Math.max(0, Math.min(1, (hsl[2] - 16) / 10)) : 0.5;
        return 203 + c * 24.7;
      }
      function slate(src, hue) {
        var o = document.createElement("canvas");
        o.width = src.width;
        o.height = src.height;
        var g = o.getContext("2d", { willReadFrequently: true });
        g.drawImage(src, 0, 0);
        var im = g.getImageData(0, 0, o.width, o.height);
        var d = im.data;
        for (var i = 0; i < d.length; i += 4) {
          var shade = Math.max(0.05, d[i] / hue);
          var rgb = hslToRgb(hue, Math.min(100, d[i + 1] / shade), Math.min(100, d[i + 2] / shade));
          d[i] = rgb[0] * shade;
          d[i + 1] = rgb[1] * shade;
          d[i + 2] = rgb[2] * shade;
        }
        g.putImageData(im, 0, 0);
        return o;
      }
      function isRockTexture(ctx, img, a) {
        if (a.length !== 4 || !(img instanceof HTMLCanvasElement)) return false;
        if (Math.abs(ctx.globalAlpha - 0.85) > 1e-6) return false;
        var fy = floorY();
        return isFinite(fy) && Math.abs(a[1] + a[3] / 1.02 - (fy + 4)) < 0.5;
      }
      function toHsla(c) {
        var m = /^hsla?\(\s*(-?[\d.]+)\s*,\s*([\d.]+)%\s*,\s*([\d.]+)%\s*(?:,\s*([\d.]+)\s*)?\)$/i.exec(c);
        if (m) return [+m[1], +m[2], +m[3], m[4] == null ? 1 : +m[4]];
        m = /^rgba?\(\s*([\d.]+)\s*,\s*([\d.]+)\s*,\s*([\d.]+)\s*(?:,\s*([\d.]+)\s*)?\)$/i.exec(c);
        var rgbHex = null;
        if (m) rgbHex = "#" + [m[1], m[2], m[3]].map(function(v) {
          return ("0" + Math.round(+v).toString(16)).slice(-2);
        }).join("");
        else if (/^#[0-9a-f]{6}$/i.test(c)) rgbHex = c;
        if (!rgbHex) return null;
        var hsl = hexToHsl(rgbHex);
        return hsl ? [hsl[0], hsl[1], hsl[2], m && m[4] != null ? +m[4] : 1] : null;
      }
      function hazed(c, k2) {
        var p = toHsla(c);
        if (!p) return c;
        return "hsla(" + (p[0] + k2.h).toFixed(1) + "," + (p[1] * k2.s).toFixed(1) + "%," + (p[2] * k2.l).toFixed(1) + "%," + p[3] + ")";
      }
      (function() {
        var add = CanvasGradient.prototype.addColorStop;
        CanvasGradient.prototype.addColorStop = function(offset, color) {
          return add.call(this, offset, haze && typeof color === "string" ? hazed(color, haze) : color);
        };
      })();
      function patch(ctx) {
        var drawImage = ctx.drawImage;
        ctx.drawImage = function(img) {
          var a = Array.prototype.slice.call(arguments, 1);
          if (isRockTexture(this, img, a)) {
            try {
              var hue = paletteHue(this.fillStyle);
              var key = Math.round(hue);
              var byHue = fixed.get(img);
              if (!byHue) {
                byHue = {};
                fixed.set(img, byHue);
              }
              if (!byHue[key]) byHue[key] = slate(img, hue);
              return drawImage.call(this, byHue[key], a[0], a[1], a[2], a[3]);
            } catch (e) {
            }
          }
          return drawImage.apply(this, arguments);
        };
        var save = ctx.save;
        var restore = ctx.restore;
        var translate = ctx.translate;
        ctx.save = function() {
          depth++;
          return save.apply(this, arguments);
        };
        ctx.restore = function() {
          if (depth === hazeDepth) {
            haze = null;
            hazeDepth = -1;
          }
          depth--;
          return restore.apply(this, arguments);
        };
        ctx.translate = function(x, y) {
          var r = translate.apply(this, arguments);
          try {
            var fy = floorY();
            if (!haze && isFinite(fy) && Math.abs(y - (fy + 2)) < 0.01) {
              if (Math.abs(this.globalAlpha - 0.34) < 1e-6) haze = BACK;
              else if (Math.abs(this.globalAlpha - 1) < 1e-6) haze = NEAR;
              if (haze) hazeDepth = depth;
            }
          } catch (e) {
          }
          return r;
        };
        ["fillStyle", "strokeStyle"].forEach(function(prop) {
          var d = Object.getOwnPropertyDescriptor(CanvasRenderingContext2D.prototype, prop);
          if (!d || !d.set) return;
          Object.defineProperty(ctx, prop, {
            configurable: true,
            get: function() {
              return d.get.call(this);
            },
            set: function(v) {
              d.set.call(this, haze && typeof v === "string" ? hazed(v, haze) : v);
            }
          });
        });
        patched = ctx;
      }
      function tick() {
        try {
          var tank = document.getElementById("tank");
          var ctx = tank && tank.getContext && tank.getContext("2d");
          if (ctx && ctx !== patched) patch(ctx);
        } catch (e) {
        }
      }
      if (window.__onBeat) window.__onBeat(tick, 500);
      else
        (function loop() {
          tick();
          setTimeout(loop, 500);
        })();
    })();
  } catch (e) {
    console.error("Layer bed.js failed to start:", e);
  }

  // src/layers/nn.js
  try {
    (function() {
      "use strict";
      var HQ = { v: 4, ok: 1, fwdN: 0, learnN: 0, dirN: 0, lastErr: "" };
      window.__nnHQ = HQ;
      var EPS = 1e-8;
      var GELU_K = 0.7978845608028654;
      var HUBER = 0.18;
      var DIR_IN = 9;
      var DIR_H1 = 48;
      var DIR_H2 = 32;
      var DIR_OUT = 2;
      var DIR_ENS = 3;
      var DIR_BUF = 192;
      var DIR_BATCH = 8;
      var DIR_STEPS = 8;
      var CLIP_W = 8;
      function clamp(x, a, b) {
        return x < a ? a : x > b ? b : x === x ? x : 0;
      }
      function sig(x) {
        if (x > 12) return 1;
        if (x < -12) return 0;
        return 1 / (1 + Math.exp(-x));
      }
      function tanh(x) {
        if (x > 8) return 1;
        if (x < -8) return -1;
        var e = Math.exp(-2 * x);
        return (1 - e) / (1 + e);
      }
      function gelu(x) {
        return 0.5 * x * (1 + tanh(GELU_K * (x + 0.044715 * x * x * x)));
      }
      function geluGrad(x, y) {
        var u = GELU_K * (x + 0.044715 * x * x * x);
        var t = tanh(u);
        var dt = 1 - t * t;
        var du = GELU_K * (1 + 3 * 0.044715 * x * x);
        return 0.5 * (1 + t) + 0.5 * x * dt * du;
      }
      function nrand() {
        var u = 0, v = 0;
        while (!u) u = Math.random();
        while (!v) v = Math.random();
        return Math.sqrt(-2 * Math.log(u)) * Math.cos(6.283185307179586 * v);
      }
      function zeros(n) {
        var a = new Array(n);
        for (var i2 = 0; i2 < n; i2++) a[i2] = 0;
        return a;
      }
      function randn(n, s) {
        var a = new Array(n);
        for (var i2 = 0; i2 < n; i2++) a[i2] = nrand() * s;
        return a;
      }
      function copyA(a) {
        var n = a.length, o = new Array(n);
        for (var i2 = 0; i2 < n; i2++) o[i2] = a[i2];
        return o;
      }
      function lnInplace(a, n) {
        var m = 0, v = 0, i2;
        for (i2 = 0; i2 < n; i2++) m += a[i2];
        m /= n || 1;
        for (i2 = 0; i2 < n; i2++) {
          var d = a[i2] - m;
          v += d * d;
        }
        v = Math.sqrt(v / (n || 1) + EPS);
        for (i2 = 0; i2 < n; i2++) a[i2] = (a[i2] - m) / v;
        return v;
      }
      function huberGrad(pred, tgt, d) {
        var e = pred - tgt;
        var a = e < 0 ? -e : e;
        if (a <= d) return e;
        return d * (e < 0 ? -1 : 1);
      }
      function finite(x) {
        return typeof x === "number" && x === x && x !== Infinity && x !== -Infinity;
      }
      function newNet() {
        var s1 = 1 / Math.sqrt(DIR_IN);
        var s2 = 1 / Math.sqrt(DIR_H1);
        var s3 = 1 / Math.sqrt(DIR_H2);
        return {
          w1: randn(DIR_H1 * DIR_IN, s1),
          b1: zeros(DIR_H1),
          w2: randn(DIR_H2 * DIR_H1, s2),
          b2: zeros(DIR_H2),
          wS: randn(DIR_H2 * DIR_IN, s1 * 0.4),
          w3: randn(DIR_OUT * DIR_H2, s3),
          b3: zeros(DIR_OUT),
          m: null,
          v: null,
          t: 0
        };
      }
      function ensureAdam(net) {
        if (net.m && net.m.w1 && net.m.w1.length === net.w1.length) return;
        function zLike(src) {
          var o = {};
          for (var k2 in src) if (Array.isArray(src[k2])) o[k2] = zeros(src[k2].length);
          return o;
        }
        var w = { w1: net.w1, b1: net.b1, w2: net.w2, b2: net.b2, wS: net.wS, w3: net.w3, b3: net.b3 };
        net.m = zLike(w);
        net.v = zLike(w);
      }
      function fwdNet(net, x, drop) {
        var h1 = new Array(DIR_H1);
        var h2 = new Array(DIR_H2);
        var y = new Array(DIR_OUT);
        var i2, j2, s, pre;
        var pre1 = new Array(DIR_H1);
        for (i2 = 0; i2 < DIR_H1; i2++) {
          s = net.b1[i2];
          var row2 = i2 * DIR_IN;
          for (j2 = 0; j2 < DIR_IN; j2++) s += net.w1[row2 + j2] * x[j2];
          pre1[i2] = s;
          h1[i2] = gelu(s);
          if (drop && Math.random() < drop) h1[i2] = 0;
        }
        var pre2 = new Array(DIR_H2);
        for (i2 = 0; i2 < DIR_H2; i2++) {
          s = net.b2[i2];
          var r2 = i2 * DIR_H1;
          for (j2 = 0; j2 < DIR_H1; j2++) s += net.w2[r2 + j2] * h1[j2];
          var skip = 0;
          var rs = i2 * DIR_IN;
          for (j2 = 0; j2 < DIR_IN; j2++) skip += net.wS[rs + j2] * x[j2];
          pre = s + skip;
          pre2[i2] = pre;
          h2[i2] = gelu(pre);
        }
        for (i2 = 0; i2 < DIR_OUT; i2++) {
          s = net.b3[i2];
          var r3 = i2 * DIR_H2;
          for (j2 = 0; j2 < DIR_H2; j2++) s += net.w3[r3 + j2] * h2[j2];
          y[i2] = sig(s);
        }
        return { y, h1, h2, pre1, pre2 };
      }
      function adamW(net, g, lr, wd2) {
        ensureAdam(net);
        net.t = (net.t || 0) + 1;
        var b1 = 0.9, b2 = 0.999, t = net.t;
        var c1 = 1 - Math.pow(b1, t);
        var c2 = 1 - Math.pow(b2, t);
        var keys = ["w1", "b1", "w2", "b2", "wS", "w3", "b3"];
        for (var k2 = 0; k2 < keys.length; k2++) {
          var key = keys[k2];
          var w = net[key], m = net.m[key], v = net.v[key], gg = g[key];
          var decay = key.charAt(0) === "b" ? 0 : wd2;
          for (var i2 = 0; i2 < w.length; i2++) {
            var gi = gg[i2] || 0;
            if (!finite(gi)) gi = 0;
            gi = clamp(gi, -2, 2);
            m[i2] = b1 * m[i2] + (1 - b1) * gi;
            v[i2] = b2 * v[i2] + (1 - b2) * gi * gi;
            var mh = m[i2] / c1;
            var vh = v[i2] / c2;
            w[i2] = clamp(w[i2] - lr * (mh / (Math.sqrt(vh) + 1e-4) + decay * w[i2]), -CLIP_W, CLIP_W);
          }
        }
      }
      function bwdNet(net, x, cache, dY, lr, wd2) {
        var g = {
          w1: zeros(net.w1.length),
          b1: zeros(DIR_H1),
          w2: zeros(net.w2.length),
          b2: zeros(DIR_H2),
          wS: zeros(net.wS.length),
          w3: zeros(net.w3.length),
          b3: zeros(DIR_OUT)
        };
        var i2, j2;
        var dH2 = zeros(DIR_H2);
        for (i2 = 0; i2 < DIR_OUT; i2++) {
          var yi = cache.y[i2];
          var dy = dY[i2] * yi * (1 - yi);
          g.b3[i2] = dy;
          var r3 = i2 * DIR_H2;
          for (j2 = 0; j2 < DIR_H2; j2++) {
            g.w3[r3 + j2] = dy * cache.h2[j2];
            dH2[j2] += dy * net.w3[r3 + j2];
          }
        }
        var dH1 = zeros(DIR_H1);
        for (i2 = 0; i2 < DIR_H2; i2++) {
          var dh = dH2[i2] * geluGrad(cache.pre2[i2], cache.h2[i2]);
          g.b2[i2] = dh;
          var r2 = i2 * DIR_H1;
          for (j2 = 0; j2 < DIR_H1; j2++) {
            g.w2[r2 + j2] = dh * cache.h1[j2];
            dH1[j2] += dh * net.w2[r2 + j2];
          }
          var rs = i2 * DIR_IN;
          for (j2 = 0; j2 < DIR_IN; j2++) g.wS[rs + j2] = dh * x[j2];
        }
        for (i2 = 0; i2 < DIR_H1; i2++) {
          var d1 = dH1[i2] * geluGrad(cache.pre1[i2], cache.h1[i2]);
          g.b1[i2] = d1;
          var r1 = i2 * DIR_IN;
          for (j2 = 0; j2 < DIR_IN; j2++) g.w1[r1 + j2] = d1 * x[j2];
        }
        adamW(net, g, lr, wd2);
      }
      function feat(raw) {
        var x = new Array(DIR_IN);
        for (var i2 = 0; i2 < DIR_IN; i2++) x[i2] = finite(raw[i2]) ? clamp(raw[i2], -1.5, 1.5) : 0;
        return x;
      }
      function ensureDir(L) {
        if (!L) return null;
        var d = L.dnnHQ;
        if (!d || d.v !== HQ.v || !Array.isArray(d.ens) || d.ens.length !== DIR_ENS) {
          d = {
            v: HQ.v,
            ens: [newNet(), newNet(), newNet()],
            buf: [],
            seen: 0,
            err: 0.08,
            errBase: 0.08,
            conf: 0,
            lift: 0,
            want: 0.5,
            drive: 0.5,
            curve: null,
            pred: 0.5,
            calmPred: 0.5,
            disagree: 0,
            last: null
          };
          L.dnnHQ = d;
        } else {
          for (var i2 = 0; i2 < DIR_ENS; i2++) {
            var n = d.ens[i2];
            if (!n || !Array.isArray(n.w1) || n.w1.length !== DIR_H1 * DIR_IN) d.ens[i2] = newNet();
          }
          if (!Array.isArray(d.buf)) d.buf = [];
        }
        return d;
      }
      function ensFwd(d, x, drop) {
        var acc = [0, 0], sq = [0, 0];
        var caches = [];
        for (var i2 = 0; i2 < DIR_ENS; i2++) {
          var c = fwdNet(d.ens[i2], x, drop);
          caches.push(c);
          acc[0] += c.y[0];
          acc[1] += c.y[1];
          sq[0] += c.y[0] * c.y[0];
          sq[1] += c.y[1] * c.y[1];
        }
        var inv = 1 / DIR_ENS;
        var mean0 = acc[0] * inv, mean1 = acc[1] * inv;
        var var0 = Math.max(0, sq[0] * inv - mean0 * mean0);
        var var1 = Math.max(0, sq[1] * inv - mean1 * mean1);
        return {
          y: [mean0, mean1],
          std: [Math.sqrt(var0), Math.sqrt(var1)],
          caches
        };
      }
      function trainOne(d, x, skill, calm, lr) {
        var tgt = [clamp(skill, 0, 1), clamp(calm, 0, 1)];
        var err = 0;
        for (var i2 = 0; i2 < DIR_ENS; i2++) {
          var cache = fwdNet(d.ens[i2], x, 0.03);
          var dY = [
            huberGrad(cache.y[0], tgt[0], HUBER),
            huberGrad(cache.y[1], tgt[1], HUBER) * 0.7
          ];
          err += Math.abs(cache.y[0] - tgt[0]);
          bwdNet(d.ens[i2], x, cache, dY, lr, 2e-4);
        }
        return err / DIR_ENS;
      }
      function pickBatch(buf, n) {
        if (buf.length <= n) return buf;
        var sum = 0;
        for (var i2 = 0; i2 < buf.length; i2++) sum += buf[i2].p || 0.05;
        var out = [];
        for (var k2 = 0; k2 < n; k2++) {
          var r = Math.random() * sum, acc = 0, hit = buf[buf.length - 1];
          for (var j2 = 0; j2 < buf.length; j2++) {
            acc += buf[j2].p || 0.05;
            if (r <= acc) {
              hit = buf[j2];
              break;
            }
          }
          out.push(hit);
        }
        return out;
      }
      function makeX(wealth, mastery, attn, calm, skill, vel, pressure, L, Xa) {
        var nightmare = 0;
        try {
          nightmare = typeof Xa === "function" ? Xa() ? 1 : -1 : L && L.nightmare ? 1 : -1;
        } catch (e) {
          nightmare = -1;
        }
        var progress = 0;
        try {
          var tank = L && L.up && L.up.tank || 0;
          var tech = L && L.tech ? Object.keys(L.tech).length : 0;
          var loc = L && L.loc || 0;
          progress = clamp((tank + tech * 0.4 + loc * 2) / 14 * 2 - 1, -1, 1);
        } catch (e) {
          progress = 0;
        }
        return feat([
          wealth * 2 - 1,
          mastery * 2 - 1,
          attn * 2 - 1,
          calm * 2 - 1,
          skill * 2 - 1,
          clamp(vel * 14, -1, 1),
          pressure * 2 - 1,
          progress,
          nightmare
        ]);
      }
      HQ.advise = function(wealth, mastery, attn, calm, skill, vel, pid, pressure, L, _b, BS, TH, CD, vfn, Xa) {
        var d = ensureDir(L);
        if (!d) return null;
        HQ.dirN++;
        var xNow = makeX(wealth, mastery, attn, calm, skill, vel, pressure, L, Xa);
        var naive = d.last ? Math.abs(d.last.skill - skill) : 0.08;
        if (d.last) {
          var pred = ensFwd(d, d.last.x, 0);
          var yErr = Math.abs(pred.y[0] - skill);
          d.err = d.err * 0.84 + yErr * 0.16;
          d.errBase = d.errBase * 0.84 + naive * 0.16;
          d.seen++;
          var prio = yErr + 0.04;
          d.buf.push({
            x: d.last.x,
            skill,
            calm,
            p: prio,
            t: d.seen
          });
          if (d.buf.length > DIR_BUF) d.buf.shift();
          var lr = 0.012 * (d.seen < 12 ? 1.6 : 1);
          trainOne(d, d.last.x, skill, calm, lr);
          var batch = pickBatch(d.buf, DIR_BATCH);
          for (var s = 0; s < DIR_STEPS; s++) {
            for (var b = 0; b < batch.length; b++) {
              var sm = batch[b];
              trainOne(d, sm.x, sm.skill, sm.calm, lr * 0.7);
            }
          }
        }
        var lift = d.errBase > 1e-4 ? clamp(1 - d.err / d.errBase, 0, 1) : 0;
        d.lift = lift;
        d.conf = clamp(lift / 0.16, 0, 1) * clamp(d.seen / 18, 0, 1);
        var bestP = pressure;
        var bestC = 1e9;
        var curve = [];
        var calmCurve = [];
        var target = 0.55;
        for (var k2 = 0; k2 <= 20; k2++) {
          var p = k2 / 20;
          var xp = makeX(wealth, mastery, attn, calm, skill, vel, p, L, Xa);
          var out = ensFwd(d, xp, 0);
          curve.push(out.y[0]);
          calmCurve.push(out.y[1]);
          var skillCost = Math.abs(out.y[0] - target);
          var moveCost = Math.abs(p - pressure) * 0.16;
          var calmCost = Math.max(0, 0.36 - out.y[1]) * 0.34;
          var uncCost = out.std[0] * 0.18;
          var cost = skillCost + moveCost + calmCost + uncCost;
          if (cost < bestC) {
            bestC = cost;
            bestP = p;
          }
        }
        d.curve = [];
        for (var i2 = 0; i2 <= 10; i2++) d.curve.push(curve[i2 * 2]);
        d.want = bestP;
        d.disagree = 0;
        try {
          var probe = ensFwd(d, makeX(wealth, mastery, attn, calm, skill, vel, bestP, L, Xa), 0);
          d.disagree = probe.std[0];
          d.pred = probe.y[0];
          d.calmPred = probe.y[1];
        } catch (e) {
        }
        var explore = (Math.random() * 2 - 1) * 0.06 * (1 - 0.7 * d.conf);
        var drive = clamp(pid * (1 - d.conf) + bestP * d.conf + explore, 0, 1);
        d.drive = drive;
        d.last = { x: makeX(wealth, mastery, attn, calm, skill, vel, drive, L, Xa), skill, calm };
        try {
          if (typeof _b === "function" && typeof BS === "function" && typeof TH === "function") {
            var legacy = _b();
            if (legacy) {
              if (legacy.last && typeof CD === "function") {
                var yL = BS(legacy, legacy.last.x).y;
                var yE = Math.abs(yL - skill);
                var wE = Math.abs(legacy.last.skill - skill);
                legacy.err = legacy.err * 0.82 + yE * 0.18;
                legacy.errBase = legacy.errBase * 0.82 + wE * 0.18;
                legacy.seen = (legacy.seen || 0) + 1;
                TH(legacy, legacy.last.x, skill);
              }
              legacy.lift = d.lift;
              legacy.conf = d.conf;
              legacy.want = d.want;
              legacy.drive = d.drive;
              legacy.curve = d.curve;
              legacy.pred = d.pred;
              legacy.seen = Math.max(legacy.seen || 0, d.seen);
              legacy.err = d.err;
              legacy.errBase = d.errBase;
              legacy.last = { x: d.last.x, skill };
            }
          }
        } catch (e) {
        }
        HQ.lastDrive = drive;
        return drive;
      };
      function layout(nn) {
        var Kt = nn.x.length;
        var He = nn.h.length;
        var ft = nn.s.length;
        var Fn = nn.o.length;
        var vl = Kt * He;
        var hidB = vl;
        var gruUz = hidB + He;
        var gruBz = gruUz + ft * He;
        var gruUh = gruBz + ft;
        var gruBh = gruUh + ft * He;
        var outH = gruBh + ft;
        var outS = outH + Fn * He;
        var outB = outS + Fn * ft;
        return { Kt, He, ft, Fn, vl, hidB, gruUz, gruBz, gruUh, gruBh, outH, outS, outB };
      }
      function ensureFish(nn) {
        if (!nn) return null;
        var L = layout(nn);
        var hq = nn._hq;
        var need = L.ft * L.He;
        if (!hq || hq.v !== 2 || !hq.Wr || hq.Wr.length !== need) {
          var sH = 1 / Math.sqrt(L.He);
          var sS = 1 / Math.sqrt(L.ft) * 0.45;
          hq = {
            v: 2,
            Wr: randn(L.ft * L.He, sH),
            br: zeros(L.ft).map(function() {
              return 0.15;
            }),
            Uz: randn(L.ft * L.ft, sS),
            Ur: randn(L.ft * L.ft, sS),
            Uh: randn(L.ft * L.ft, sS),
            attn: randn(L.Kt, 0.15),
            attnb: 0,
            t: 0,
            xp: [],
            lnG: 1,
            lnB: 0
          };
          var keys = ["Wr", "br", "Uz", "Ur", "Uh", "attn"];
          hq.m = {};
          hq.vv = {};
          for (var i2 = 0; i2 < keys.length; i2++) {
            hq.m[keys[i2]] = zeros(hq[keys[i2]].length);
            hq.vv[keys[i2]] = zeros(hq[keys[i2]].length);
          }
          hq.m.attnb = 0;
          hq.vv.attnb = 0;
          nn._hq = hq;
        }
        if (!nn._dm || nn._dm.length !== nn.d.length) {
          nn._dm = new Float32Array(nn.d.length);
          nn._dv = new Float32Array(nn.d.length);
          nn._at = 0;
        }
        return hq;
      }
      function wd(g, d, i2) {
        var x = (g[i2] || 0) + (d[i2] || 0);
        return x === x ? x : 0;
      }
      HQ.fwd = function(nn, Lstate) {
        if (!nn || !nn.g || !nn.x) return nn && nn.o;
        var g = nn.g, d = nn.d, x = nn.x, h = nn.h, hf = nn.hf;
        var s = nn.s, sp = nn.sp, z = nn.z, c = nn.c, xi = nn.xi;
        var ly = layout(nn);
        var hq = ensureFish(nn);
        HQ.fwdN++;
        try {
          if (Lstate && finite(Lstate.t) && finite(nn.tLast) && nn.tLast >= 0) {
            var dt = Lstate.t - nn.tLast;
            if (dt > 6) {
              var decay = Math.pow(0.5, dt / 12);
              for (var ti = 0; ti < ly.ft; ti++) s[ti] *= decay;
            }
          }
          if (Lstate && finite(Lstate.t)) nn.tLast = Lstate.t;
        } catch (e) {
        }
        var i2, j2, sum;
        for (i2 = 0; i2 < ly.He; i2++) {
          sum = wd(g, d, ly.hidB + i2);
          var row2 = i2 * ly.Kt;
          for (j2 = 0; j2 < ly.Kt; j2++) {
            var xv = finite(x[j2]) ? x[j2] : 0;
            var ag = 1 + 0.35 * tanh(hq.attn[j2]);
            sum += wd(g, d, row2 + j2) * xv * ag;
          }
          var act = tanh(sum);
          h[i2] = act;
          hf[i2] = act;
        }
        var prev = new Array(ly.ft);
        for (i2 = 0; i2 < ly.ft; i2++) {
          sp[i2] = s[i2];
          prev[i2] = finite(s[i2]) ? s[i2] : 0;
        }
        var zMean = 0;
        var np = 6;
        for (i2 = 0; i2 < ly.ft; i2++) {
          var zsum = wd(g, d, ly.gruBz + i2);
          var hsum = wd(g, d, ly.gruBh + i2);
          var uz = ly.gruUz + i2 * ly.He;
          var uh = ly.gruUh + i2 * ly.He;
          for (j2 = 0; j2 < ly.He; j2++) {
            var hj = h[j2];
            zsum += wd(g, d, uz + j2) * hj;
            hsum += wd(g, d, uh + j2) * hj;
          }
          var recZ = 0, recR = 0, recH = 0;
          var us = i2 * ly.ft;
          var wr = i2 * ly.He;
          var rsum = hq.br[i2];
          for (j2 = 0; j2 < ly.He; j2++) rsum += hq.Wr[wr + j2] * h[j2];
          for (j2 = 0; j2 < ly.ft; j2++) {
            recZ += hq.Uz[us + j2] * prev[j2];
            recR += hq.Ur[us + j2] * prev[j2];
          }
          zsum += recZ;
          rsum += recR;
          var zg = sig(zsum);
          var rg = sig(rsum);
          z[i2] = zg === zg ? zg : 0.5;
          zMean += z[i2];
          for (j2 = 0; j2 < ly.ft; j2++) recH += hq.Uh[us + j2] * (rg * prev[j2]);
          var cand = hsum + 0.55 * recH;
          cand = cand > np ? np : cand < -np ? -np : cand === cand ? cand : 0;
          c[i2] = cand;
          var nxt = (1 - z[i2]) * prev[i2] + z[i2] * cand;
          s[i2] = nxt === nxt ? nxt : 0;
        }
        nn.zMean = zMean / ly.ft;
        var ser = finite(nn.ser) ? nn.ser : 0.6;
        var noise = 0.35 * (1.2 - ser);
        for (i2 = 0; i2 < ly.Fn; i2++) xi[i2] = nrand() * noise;
        HQ.out(nn);
        return nn.o;
      };
      HQ.out = function(nn) {
        var g = nn.g, d = nn.d, h = nn.h, s = nn.s, o = nn.o, xi = nn.xi;
        var ly = layout(nn);
        for (var i2 = 0; i2 < ly.Fn; i2++) {
          var sum = wd(g, d, ly.outB + i2) + (xi[i2] || 0);
          var rh = ly.outH + i2 * ly.He;
          var rs = ly.outS + i2 * ly.ft;
          for (var j2 = 0; j2 < ly.He; j2++) sum += wd(g, d, rh + j2) * h[j2];
          for (var k2 = 0; k2 < ly.ft; k2++) sum += wd(g, d, rs + k2) * s[k2];
          var y = tanh(sum);
          o[i2] = y === y ? y : 0;
        }
        return o;
      };
      HQ.temp = function(nn) {
        if (!nn || !nn.o) return 0.62;
        var Fn = nn.o.length, m = -1e9, i2;
        for (i2 = 0; i2 < Fn; i2++) if (nn.o[i2] > m) m = nn.o[i2];
        var Z = 0, p = new Array(Fn);
        for (i2 = 0; i2 < Fn; i2++) {
          p[i2] = Math.exp((nn.o[i2] - m) * 2.2);
          Z += p[i2];
        }
        var H = 0;
        for (i2 = 0; i2 < Fn; i2++) {
          var q = p[i2] / (Z || 1);
          H -= q * Math.log(q + 1e-9);
        }
        var conf = 1 - H / Math.log(Fn || 2);
        var age2 = clamp((nn.steps || 0) / 800, 0, 1);
        return 0.48 + conf * 0.22 + age2 * 0.08;
      };
      HQ.chose = function(nn, act) {
        if (!nn) return;
        var hq = ensureFish(nn);
        if (!hq.xp) hq.xp = [];
        var x = new Array(nn.x.length);
        for (var i2 = 0; i2 < nn.x.length; i2++) x[i2] = nn.x[i2];
        hq.xp.push({ a: act, x, rpe: 0, t: nn.steps || 0 });
        if (hq.xp.length > 24) hq.xp.shift();
      };
      function adamDelta(nn, idx, grad, lr, wd2) {
        var m = nn._dm, v = nn._dv;
        nn._at = (nn._at || 0) + 1;
        var b1 = 0.9, b2 = 0.999, t = nn._at;
        if (!finite(grad)) grad = 0;
        grad = clamp(grad, -1, 1);
        m[idx] = b1 * m[idx] + (1 - b1) * grad;
        v[idx] = b2 * v[idx] + (1 - b2) * grad * grad;
        var mh = m[idx] / (1 - Math.pow(b1, t));
        var vh = v[idx] / (1 - Math.pow(b2, t));
        var step = lr * (mh / (Math.sqrt(vh) + 1e-3) + wd2 * nn.d[idx]);
        nn.d[idx] = clamp(nn.d[idx] - step, -0.55, 0.55);
      }
      HQ.learn = function(fish, dt, Lstate, qfn) {
        if (!(dt > 0) || !fish) return false;
        var nn;
        try {
          nn = typeof nnOf === "function" ? nnOf(fish) : fish.nn;
        } catch (e) {
          nn = fish.nn;
        }
        if (!nn) return false;
        if (!nn.d || !nn.ef || !nn.es) return false;
        HQ.learnN++;
        nn.acc = (nn.acc || 0) + dt;
        if (nn.acc < 0.083) return true;
        var t = nn.acc;
        nn.acc = 0;
        if (t > 5) t = 5;
        var hq = ensureFish(nn);
        var brain = fish.brain;
        if (brain) {
          var targetSer = clamp(0.5 * (1 - (brain.stress || 0)) + 0.5 * (fish.hunger || 0), 0, 1);
          nn.ser += (targetSer - nn.ser) * (1 - Math.pow(0.5, t / 15));
        }
        if (!finite(nn.base)) nn.base = 0;
        var rpe = nn.rpe - nn.base * t;
        nn.base += (nn.rpe / Math.max(t, 1e-3) - nn.base) * (1 - Math.pow(0.5, t / 40));
        nn.rpe = 0;
        var signal = Math.abs(rpe) > 1e-4;
        var r = clamp(rpe, -1.5, 1.5);
        if (signal) nn.dop = r;
        else nn.dop *= Math.pow(0.5, t / 2);
        if (signal) nn.err = nn.err * 0.9 + Math.abs(r) * 0.1;
        var smart = 0.5;
        try {
          smart = fish.traits && finite(fish.traits.smart) ? fish.traits.smart : 0.5;
        } catch (e) {
        }
        var research = 0;
        try {
          if (typeof qfn === "function") research = qfn("nnLearn") || 0;
          else if (Lstate && Lstate.tech && finite(Lstate.tech.nnLearn)) research = Lstate.tech.nnLearn;
        } catch (e) {
        }
        var lr0 = 0.04 * (0.35 + smart) * (1 + research * 0.15) * (0.5 + (finite(nn.ach) ? nn.ach : 0.4));
        if (r < 0) lr0 *= 0.65;
        var ly = layout(nn);
        var decayTraceFast = Math.pow(0.5, t / 1.5);
        var decayTraceSlow = Math.pow(0.5, t / 10);
        var l2 = 1 - 6e-3 * t;
        var nW = nn.d.length;
        var learned = 0;
        var eMag = 0;
        if (nn.eMag > 1e-5 || signal) {
          var rms = 0;
          for (var i2 = 0; i2 < nW; i2++) {
            var elig = 0.6 * nn.ef[i2] + 0.4 * nn.es[i2];
            rms += elig < 0 ? -elig : elig;
          }
          rms = rms / Math.sqrt(nW || 1);
          var gain = lr0;
          if (signal && rms > 0) {
            var mag = Math.abs(r);
            var scaled = (mag > 1 ? mag : 1) * rms * lr0 * 2;
            if (scaled > 1) gain = lr0 / scaled;
          }
          var inGain = gain * (0.5 + (finite(nn.ach) ? nn.ach : 0.4));
          for (var w = 0; w < nW; w++) {
            var el = 0.6 * nn.ef[w] + 0.4 * nn.es[w];
            nn.d[w] *= l2;
            if (signal) {
              var grad = -(w < ly.gruUz ? inGain : gain) * r * el;
              adamDelta(nn, w, grad, 0.05, 4e-4);
            }
            var a = nn.d[w];
            learned += a < 0 ? -a : a;
            nn.ef[w] *= decayTraceFast;
            nn.es[w] *= decayTraceSlow;
            var es = nn.es[w];
            eMag += es < 0 ? -es : es;
          }
          nn.learned = learned / nW;
          nn.eMag = eMag / nW;
        } else if (nn.learned > 1e-4) {
          for (var k2 = 0; k2 < nW; k2++) nn.d[k2] *= l2;
          nn.learned *= l2;
        }
        if (signal && hq) {
          hq.t = (hq.t || 0) + 1;
          var lrG = gainSafe(lr0) * 0.45 * r;
          hebbAdam(hq, "Wr", nn.h, ly.He, ly.ft, lrG);
          hebbAdamVec(hq, "br", nn.z, lrG * 0.4);
          hebbAdam(hq, "Uz", nn.sp, ly.ft, ly.ft, lrG * 0.5);
          hebbAdam(hq, "Uh", nn.s, ly.ft, ly.ft, lrG * 0.5);
          for (var a = 0; a < ly.Kt; a++) {
            var gA = -lrG * 0.25 * nn.x[a];
            adamHQ(hq, "attn", a, gA, 6e-3);
          }
        }
        if (hq && hq.xp && hq.xp.length) hq.xp[hq.xp.length - 1].rpe = r;
        return true;
      };
      function gainSafe(x) {
        return finite(x) && x > 0 ? x : 4e-3;
      }
      function adamHQ(hq, key, idx, grad, lr) {
        var m = hq.m[key], v = hq.vv[key], w = hq[key];
        if (!m || !w) return;
        var t = hq.t || 1;
        m[idx] = 0.9 * m[idx] + 0.1 * grad;
        v[idx] = 0.999 * v[idx] + 1e-3 * grad * grad;
        var mh = m[idx] / (1 - Math.pow(0.9, t));
        var vh = v[idx] / (1 - Math.pow(0.999, t));
        w[idx] = clamp(w[idx] - lr * mh / (Math.sqrt(vh) + 1e-3), -4, 4);
      }
      function hebbAdam(hq, key, pre, preN, postN, lr) {
        var w = hq[key];
        if (!w) return;
        for (var i2 = 0; i2 < postN; i2++) {
          for (var j2 = 0; j2 < preN; j2++) {
            var idx = i2 * preN + j2;
            if (idx >= w.length) return;
            adamHQ(hq, key, idx, -lr * (pre[j2] || 0), 8e-3);
          }
        }
      }
      function hebbAdamVec(hq, key, pre, lr) {
        var w = hq[key];
        if (!w) return;
        for (var i2 = 0; i2 < w.length; i2++) adamHQ(hq, key, i2, -lr * (pre[i2] || 0), 8e-3);
      }
      HQ.inherit = function(mom, dad, Lstate) {
        var gm = null, gd = null;
        try {
          gm = mom && (typeof nnOf === "function" ? nnOf(mom).g : mom.nn && mom.nn.g);
          gd = dad && (typeof nnOf === "function" ? nnOf(dad).g : dad.nn && dad.nn.g);
        } catch (e) {
        }
        if (!gm && !gd) return null;
        var n = (gm || gd).length;
        var out = new Float32Array(n);
        var eta = 18;
        var mut = 0.14;
        try {
          if (Lstate && Lstate.tech && finite(Lstate.tech.nnMut)) mut *= 1 + Lstate.tech.nnMut;
        } catch (e) {
        }
        for (var i2 = 0; i2 < n; i2++) {
          var a = gm ? gm[i2] : gd[i2];
          var b = gd ? gd[i2] : gm[i2];
          if (!finite(a)) a = 0;
          if (!finite(b)) b = 0;
          var u = Math.random();
          var beta = u <= 0.5 ? Math.pow(2 * u, 1 / (eta + 1)) : Math.pow(1 / (2 * (1 - u)), 1 / (eta + 1));
          var c1 = 0.5 * (a + b - beta * (b - a));
          var pick = Math.random() < 0.5 ? c1 : 0.5 * (a + b + beta * (b - a));
          if (Math.random() < mut) pick += nrand() * 0.18;
          out[i2] = clamp(pick, -3.2, 3.2);
        }
        if (Math.random() < 0.3) {
          var k2 = Math.random() * n | 0;
          out[k2] = clamp(out[k2] + nrand() * 1.2, -3.2, 3.2);
        }
        return out;
      };
      HQ.culture = function(fish, Lstate) {
        if (!fish || !Lstate || !Array.isArray(Lstate.fish)) return false;
        var nn = fish.nn;
        if (!nn) return false;
        var teachers = [];
        for (var i2 = 0; i2 < Lstate.fish.length; i2++) {
          var f = Lstate.fish[i2];
          if (f === fish || f.sp !== fish.sp || f.stage !== 2 || !f.nn) continue;
          if (!f.nn.d || f.nn.d.length !== nn.d.length || (f.nn.steps || 0) < 80) continue;
          var quality = 1 / (0.08 + (f.nn.err || 0.3)) * Math.log(2 + (f.nn.steps || 0));
          teachers.push({ f, q: quality });
        }
        if (!teachers.length) return true;
        teachers.sort(function(a, b) {
          return b.q - a.q;
        });
        var top = teachers.slice(0, Math.min(5, teachers.length));
        var qSum = 0;
        for (var t = 0; t < top.length; t++) qSum += top[t].q;
        var nW = nn.d.length;
        for (var w = 0; w < nW; w++) {
          var s = 0;
          for (var k2 = 0; k2 < top.length; k2++) s += top[k2].f.nn.d[w] * (top[k2].q / qSum);
          nn.d[w] = clamp(s * 0.28, -0.4, 0.4);
        }
        nn.learned = 0.06;
        try {
          if (top.length >= 3 && Lstate.stats) {
            Lstate.stats.nnTaught = (finite(Lstate.stats.nnTaught) ? Lstate.stats.nnTaught : 0) + 1;
          }
        } catch (e) {
        }
        return true;
      };
      HQ.surprise = function(fish, dt, Lstate) {
        var nn, cog;
        try {
          nn = typeof nnOf === "function" ? nnOf(fish) : fish.nn;
          cog = nn && nn.cog;
        } catch (e3) {
          return null;
        }
        if (!nn || !cog) return null;
        var Kt = nn.x.length, He = nn.h.length;
        if (!cog.hw || cog.hw.length !== He * 10) {
          cog.hw = randn(He * 10, 0.15);
          cog.hb = zeros(10);
          cog.ow = randn(10 * Kt, 0.15);
          cog.ob = zeros(Kt);
        }
        var hid = new Array(10);
        var i2, j2, s;
        for (i2 = 0; i2 < 10; i2++) {
          s = cog.hb[i2];
          for (j2 = 0; j2 < He; j2++) s += cog.hw[i2 * He + j2] * nn.h[j2];
          hid[i2] = tanh(s);
        }
        var err2 = 0;
        var pred = cog.pred;
        for (i2 = 0; i2 < Kt; i2++) {
          s = cog.ob[i2];
          for (j2 = 0; j2 < 10; j2++) s += cog.ow[j2 * Kt + i2] * hid[j2];
          pred[i2] = clamp(s, -1.3, 1.3);
          var e = pred[i2] - nn.x[i2];
          err2 += e * e;
        }
        var d = Math.sqrt(err2 / Kt);
        cog.surprise = d;
        cog.sAvg += (d - cog.sAvg) * (1 - Math.pow(0.5, dt / 25));
        var smart = 0.5;
        try {
          smart = fish.traits && finite(fish.traits.smart) ? fish.traits.smart : 0.5;
        } catch (e3) {
        }
        var lr = 0.035 * (0.4 + smart * 0.8);
        for (i2 = 0; i2 < Kt; i2++) {
          var e2 = pred[i2] - nn.x[i2];
          cog.ob[i2] = clamp(cog.ob[i2] - lr * e2, -4, 4);
          for (j2 = 0; j2 < 10; j2++) {
            cog.ow[j2 * Kt + i2] = clamp(cog.ow[j2 * Kt + i2] - lr * e2 * hid[j2], -4, 4);
          }
        }
        var extra = Math.max(0, d - cog.sAvg * 1.3);
        if (extra > 0.02) {
          try {
            if (typeof nnReward === "function") {
              var curious = fish.traits && finite(fish.traits.curious) ? fish.traits.curious : 0.5;
              nnReward(fish, extra * curious * 1.7);
            }
          } catch (e3) {
          }
        }
        return extra;
      };
      HQ.value = function(fish, reward, dt, cellFn, md, lv, tankW, waterY, floorY) {
        var cog;
        try {
          var nn = typeof nnOf === "function" ? nnOf(fish) : fish.nn;
          cog = nn && nn.cog;
        } catch (e) {
          return false;
        }
        if (!cog || !cog.v) return false;
        md = md || 9;
        lv = lv || 6;
        var Kr = md * lv;
        if (cog.v.length !== Kr) return false;
        var here;
        try {
          here = typeof cellFn === "function" ? cellFn(fish.x, fish.y) : 0;
        } catch (e) {
          here = 0;
        }
        here = clamp(here, 0, Kr - 1) | 0;
        var smart = 0.5;
        try {
          smart = fish.traits && finite(fish.traits.smart) ? fish.traits.smart : 0.5;
        } catch (e) {
        }
        var lr = 0.18 * (0.4 + smart * 0.8);
        var gamma = 0.94;
        if (finite(cog.last) && cog.last >= 0 && cog.last !== here) {
          var td = reward + gamma * cog.v[here] - cog.v[cog.last];
          paint(cog.v, cog.last, md, lv, lr * td, 0.55);
          if (!cog.trail) cog.trail = [];
          cog.trail.push(cog.last);
          if (cog.trail.length > 28) cog.trail.shift();
          if (cog.visits && cog.visits[cog.last] < 65e3) cog.visits[cog.last]++;
        } else if (cog.last === here && reward) {
          cog.v[here] += 0.22 * (reward - cog.v[here]);
        }
        cog.last = here;
        if (dt > 0) {
          var fade = Math.pow(0.99923, dt);
          for (var i2 = 0; i2 < Kr; i2++) cog.v[i2] = clamp(cog.v[i2] * fade, -3, 3);
        }
        return true;
      };
      function paint(v, idx, md, lv, mag, falloff) {
        var col = idx % md, row2 = idx / md | 0;
        for (var dr = -1; dr <= 1; dr++) {
          for (var dc = -1; dc <= 1; dc++) {
            var r = row2 + dr, c = col + dc;
            if (r < 0 || c < 0 || r >= lv || c >= md) continue;
            var w = dr === 0 && dc === 0 ? 1 : falloff * 0.45;
            var j2 = r * md + c;
            v[j2] = clamp(v[j2] + mag * w, -3, 3);
          }
        }
      }
      HQ.replay = function(fish, dt) {
        var cog;
        try {
          var nn = typeof nnOf === "function" ? nnOf(fish) : fish.nn;
          cog = nn && nn.cog;
        } catch (e) {
          return false;
        }
        if (!cog) return false;
        cog.replayT = (cog.replayT || 0) - dt;
        if (cog.replayT > 0 || !cog.trail || cog.trail.length < 3) return true;
        cog.replayT = 2.2 + Math.random() * 3.4;
        var gamma = 0.94, lam = 0.88, a = 0.14;
        var trail = cog.trail;
        for (var sweep = 0; sweep < 2; sweep++) {
          var G2 = 0;
          for (var i2 = trail.length - 1; i2 > 0; i2--) {
            var nxt = trail[i2], cur = trail[i2 - 1];
            G2 = cog.v[nxt] + lam * gamma * G2;
            cog.v[cur] += a * (gamma * G2 - cog.v[cur]);
            cog.v[cur] = clamp(cog.v[cur], -3, 3);
          }
        }
        return true;
      };
      HQ.pack = function(nn) {
        if (!nn || !nn._hq) return null;
        var hq = nn._hq;
        function q(arr, scale) {
          var n = arr.length, o = new Array(n);
          for (var i2 = 0; i2 < n; i2++) o[i2] = clamp(Math.round(arr[i2] / scale), -127, 127) + 128 & 255;
          return o;
        }
        return {
          v: 2,
          Wr: q(hq.Wr, 4 / 127),
          br: q(hq.br, 4 / 127),
          Uz: q(hq.Uz, 4 / 127),
          Ur: q(hq.Ur, 4 / 127),
          Uh: q(hq.Uh, 4 / 127),
          attn: q(hq.attn, 4 / 127),
          attnb: clamp(hq.attnb, -4, 4)
        };
      };
      HQ.unpack = function(nn, blob) {
        if (!nn || !blob || blob.v !== 2) return;
        ensureFish(nn);
        var hq = nn._hq;
        function u(arr, src, scale) {
          if (!src || src.length !== arr.length) return;
          for (var i2 = 0; i2 < arr.length; i2++) arr[i2] = ((src[i2] & 255) - 128) * scale;
        }
        u(hq.Wr, blob.Wr, 4 / 127);
        u(hq.br, blob.br, 4 / 127);
        u(hq.Uz, blob.Uz, 4 / 127);
        u(hq.Ur, blob.Ur, 4 / 127);
        u(hq.Uh, blob.Uh, 4 / 127);
        u(hq.attn, blob.attn, 4 / 127);
        if (finite(blob.attnb)) hq.attnb = blob.attnb;
      };
      HQ.slim = function(d) {
        if (!d || !Array.isArray(d.ens)) return;
        for (var i2 = 0; i2 < d.ens.length; i2++) {
          var n = d.ens[i2];
          if (!n) continue;
          n.m = null;
          n.v = null;
        }
        if (Array.isArray(d.buf) && d.buf.length > 64) d.buf = d.buf.slice(-64);
      };
      HQ.stats = function() {
        var L = null;
        try {
          L = window.G || window.l || null;
        } catch (e) {
        }
        var d = L && L.dnnHQ;
        return {
          v: HQ.v,
          ok: HQ.ok,
          fwd: HQ.fwdN,
          learn: HQ.learnN,
          dir: HQ.dirN,
          conf: d && +(+d.conf).toFixed(3),
          err: d && d.err,
          lift: d && d.lift,
          seen: d && d.seen,
          disagree: d && d.disagree,
          lastErr: HQ.lastErr
        };
      };
      HQ.describeDir = function() {
        return "A residual ensemble of three 9→48→32 nets (GELU, layer-norm skip, AdamW, Huber loss) trained on you alone. Every 12 seconds it predicts your next skill and how calm you will stay, then gets graded. It only steers in proportion to how much it beats a naive guess, so a fresh save does nothing.";
      };
      HQ.describeFish = function() {
        return "Each fish has its own network: 16 senses, 12 GELU hidden units, an 8-unit GRU with reset and recurrent gates, and 15 drives. Learned weights update with AdamW on eligibility traces; fry inherit by simulated-binary crossover and pick up the tank's habits from the adults that have actually been right.";
      };
      try {
        var dummy = {
          g: new Float32Array(727),
          d: new Float32Array(727),
          x: new Float32Array(16),
          h: new Float32Array(12),
          hf: new Float32Array(12),
          o: new Float32Array(15),
          s: new Float32Array(8),
          sp: new Float32Array(8),
          z: new Float32Array(8),
          c: new Float32Array(8),
          xi: new Float32Array(15),
          pred: new Float32Array(16),
          pin: new Float32Array(20),
          ef: new Float32Array(727),
          es: new Float32Array(727),
          ser: 0.6,
          ach: 0.4,
          dop: 0,
          rpe: 0,
          base: 0,
          acc: 1,
          eMag: 1,
          learned: 0,
          steps: 0,
          tLast: -1
        };
        for (var i = 0; i < 16; i++) dummy.x[i] = Math.random() * 2 - 1;
        HQ.fwd(dummy, { t: 1 });
        var ok = true;
        for (var j = 0; j < 15; j++) if (!finite(dummy.o[j])) ok = false;
        HQ.ok = ok ? 1 : 0;
        if (!ok) HQ.lastErr = "self-check produced a non-finite drive";
      } catch (e) {
        HQ.ok = 0;
        HQ.lastErr = String(e && e.message || e);
      }
    })();
  } catch (e) {
    console.error("Layer nn.js failed to start:", e);
  }

  // src/layers/feel.js
  try {
    (function() {
      "use strict";
      var jobs = [];
      window.__onBeat = function(fn, ms) {
        if (typeof fn !== "function") return;
        jobs.push({ fn, ms: Math.max(120, ms || 280), last: 0 });
      };
      function pump() {
        var hidden = false;
        try {
          hidden = !!document.hidden;
        } catch (e) {
        }
        var t = typeof performance !== "undefined" ? performance.now() : Date.now();
        if (!hidden) {
          for (var i = 0; i < jobs.length; i++) {
            var j = jobs[i];
            if (t - j.last >= j.ms) {
              j.last = t;
              try {
                j.fn();
              } catch (err) {
              }
            }
          }
        }
        setTimeout(pump, hidden ? 800 : 80);
      }
      setTimeout(pump, 100);
    })();
    (function() {
      "use strict";
      var reducedOS = false;
      try {
        reducedOS = !!(window.matchMedia && window.matchMedia("(prefers-reduced-motion: reduce)").matches);
      } catch (e) {
      }
      var reduced = reducedOS;
      function refreshCalm() {
        var r = reducedOS;
        try {
          if (window.gameState && gameState.reduceMotion) r = true;
        } catch (e) {
        }
        reduced = r;
      }
      var lastSfx = 0;
      var voices = 0;
      var VOICE_CAP = 14;
      var trauma = 0;
      var flashA = 0;
      var flashCol = "255,220,140";
      var overlay = null;
      var octx = null;
      var floats = [];
      var ripples = [];
      var sparks = [];
      var pellets = [];
      var last = 0;
      var shakeT = 0;
      var shakeSeed = 0;
      var painted = false;
      var hudPulse = /* @__PURE__ */ Object.create(null);
      var wired = false;
      function now() {
        return typeof performance !== "undefined" ? performance.now() : Date.now();
      }
      function markSfx() {
        lastSfx = now();
      }
      function sfxOn() {
        try {
          var I2 = window.I || window.sfx;
          if (I2 && typeof I2.ok === "function") return !!I2.ok();
        } catch (e) {
        }
        return true;
      }
      function bridge() {
        try {
          return window.finsAudio || null;
        } catch (e) {
          return null;
        }
      }
      function audio() {
        try {
          if (window.oe && typeof oe.init === "function") oe.init();
          if (window.oe && oe.ctx) return oe.ctx;
          var b = bridge();
          return b && b.ctx() || null;
        } catch (e) {
          return null;
        }
      }
      function bus() {
        if (window.ge && ge.bus) return ge.bus;
        if (window.oe && oe.sfxBus) return oe.sfxBus;
        if (window.oe && oe.master) return oe.master;
        var b = bridge();
        var lb = b && b.bus();
        if (lb) return lb;
        var c = audio();
        return c ? c.destination : null;
      }
      function vol() {
        if (window.ge && ge.bus || window.oe && oe.sfxBus) return 1;
        var b = bridge();
        if (b && b.bus()) return b.vol();
        return 0.55;
      }
      function ok() {
        if (!sfxOn()) return false;
        var b = bridge();
        if (!(window.oe && oe.ctx) && b) return b.ok();
        var c = audio();
        if (!c) return false;
        if (c.state === "suspended") {
          try {
            c.resume();
          } catch (e) {
          }
        }
        return true;
      }
      function panTo(x) {
        if (!isFinite(x)) return;
        if (window.ge) {
          try {
            var W = typeof window.D === "number" && window.D ? window.D : overlay && overlay.width || 1;
            ge.pan = Math.max(-0.6, Math.min(0.6, x / W * 2 - 1));
            ge.hint = x;
          } catch (e) {
          }
        }
        var b = bridge();
        if (b) {
          try {
            b.panAt(x);
          } catch (e) {
          }
        }
      }
      function endVoice(node, t) {
        voices++;
        node.onended = function() {
          voices--;
          try {
            node.disconnect();
          } catch (e) {
          }
        };
        try {
          node.stop(t);
        } catch (e) {
        }
      }
      function connect(node) {
        var c = audio();
        var b = bridge();
        if (c && b && !(window.ge && ge.bus) && b.bus()) return b.connect(node);
        var out = bus();
        if (!c || !out) return node;
        var p = window.ge && isFinite(ge.pan) && Math.abs(ge.pan) > 0.04 && c.createStereoPanner;
        if (p) {
          try {
            var pn = c.createStereoPanner();
            pn.pan.value = Math.max(-1, Math.min(1, ge.pan));
            node.connect(pn);
            pn.connect(out);
            return node;
          } catch (e) {
          }
        }
        node.connect(out);
        return node;
      }
      function osc(type, freq, dur, amp, slide, delay) {
        if (!ok() || voices > VOICE_CAP) return;
        var c = audio();
        if (!c) return;
        var t = c.currentTime + (delay || 0);
        var o = c.createOscillator();
        var g = c.createGain();
        o.type = type || "sine";
        var f0 = Math.max(20, freq || 440);
        o.frequency.setValueAtTime(f0, t);
        if (slide && slide > 0) {
          try {
            o.frequency.exponentialRampToValueAtTime(Math.max(20, slide), t + dur);
          } catch (e) {
            o.frequency.linearRampToValueAtTime(Math.max(20, slide), t + dur);
          }
        }
        var a = Math.max(8e-4, (amp || 0.08) * vol());
        g.gain.setValueAtTime(1e-4, t);
        g.gain.exponentialRampToValueAtTime(a, t + 0.012);
        g.gain.exponentialRampToValueAtTime(1e-4, t + Math.max(0.03, dur));
        o.connect(g);
        connect(g);
        o.start(t);
        endVoice(o, t + dur + 0.03);
      }
      function noise(dur, amp, freq, kind, delay, q) {
        if (!ok() || voices > VOICE_CAP) return;
        var c = audio();
        if (!c) return;
        var t = c.currentTime + (delay || 0);
        var src = c.createBufferSource();
        src.buffer = pinkBuf(c);
        src.loop = true;
        var skip = Math.random() * Math.max(0.01, src.buffer.duration - dur - 0.05);
        var f = c.createBiquadFilter();
        f.type = kind || "lowpass";
        f.frequency.value = Math.max(60, freq || 800);
        f.Q.value = q == null ? 0.8 : q;
        var g = c.createGain();
        var a = Math.max(8e-4, (amp || 0.06) * vol());
        g.gain.setValueAtTime(1e-4, t);
        g.gain.exponentialRampToValueAtTime(a, t + 8e-3);
        g.gain.exponentialRampToValueAtTime(1e-4, t + dur);
        src.connect(f);
        f.connect(g);
        connect(g);
        src.start(t, skip);
        endVoice(src, t + dur + 0.03);
      }
      var pink = null;
      var pinkCtx = null;
      function pinkBuf(c) {
        var b = bridge();
        if (b && typeof b.noise === "function") {
          var shared = b.noise("pink", 2);
          if (shared) return shared;
        }
        if (pink && pinkCtx === c) return pink;
        var n = Math.max(64, Math.round(c.sampleRate * 2));
        var buf = c.createBuffer(1, n, c.sampleRate);
        var data = buf.getChannelData(0);
        var acc = 0;
        for (var i = 0; i < n; i++) {
          acc = acc * 0.92 + (Math.random() * 2 - 1) * 0.08;
          data[i] = acc * 6;
        }
        pink = buf;
        pinkCtx = c;
        return buf;
      }
      function jitter(f, amt) {
        return f * (1 + (Math.random() * 2 - 1) * (amt == null ? 0.04 : amt));
      }
      var CATALOG = {
        click: function() {
          noise(0.03, 0.07, 3200, "bandpass", 0, 2.4);
          osc("triangle", jitter(920, 0.04), 0.05, 0.05);
        },
        hover: function() {
          osc("sine", jitter(1680, 0.03), 0.04, 0.03);
        },
        modal: function() {
          osc("triangle", 392, 0.08, 0.07);
          osc("triangle", 523.25, 0.12, 0.05, 0, 0.05);
        },
        page: function() {
          noise(0.08, 0.05, 1800, "bandpass", 0, 0.7);
          osc("triangle", jitter(340, 0.08), 0.07, 0.04, 220);
        },
        paper: function() {
          noise(0.12, 0.07, 2400, "highpass", 0, 0.5);
          noise(0.08, 0.04, 4200, "bandpass", 0.04, 1.2);
        },
        rustle: function() {
          noise(0.16, 0.06, 2100, "highpass");
        },
        cash: function() {
          osc("square", jitter(1180, 0.02), 0.05, 0.05);
          osc("square", jitter(1560, 0.02), 0.07, 0.06, 0, 0.045);
          noise(0.05, 0.04, 2800, "bandpass", 0.02);
        },
        register: function() {
          noise(0.05, 0.08, 900, "lowpass");
          osc("square", 1318, 0.08, 0.06, 0, 0.07);
          osc("square", 1760, 0.1, 0.05, 0, 0.13);
        },
        bag: function() {
          noise(0.14, 0.07, 700, "lowpass");
          noise(0.08, 0.04, 2400, "bandpass", 0.05);
        },
        net: function() {
          noise(0.18, 0.08, 1400, "bandpass", 0, 0.6);
          osc("sine", 220, 0.16, 0.04, 90);
        },
        pour: function() {
          noise(0.45, 0.09, 1800, "bandpass", 0, 0.4);
          osc("sine", 180, 0.35, 0.04, 90);
        },
        water: function() {
          noise(0.28, 0.08, 1600, "bandpass");
          osc("sine", jitter(240, 0.1), 0.18, 0.05, 80);
        },
        gravel: function() {
          noise(0.12, 0.1, 500, "lowpass");
          noise(0.08, 0.05, 1600, "bandpass", 0.03);
        },
        lid: function() {
          osc("triangle", 180, 0.1, 0.08, 90);
          noise(0.06, 0.06, 800, "lowpass", 0.02);
        },
        heater: function() {
          osc("sine", 62, 0.22, 0.05);
          noise(0.08, 0.03, 400, "lowpass", 0.04);
        },
        switch: function() {
          noise(0.025, 0.08, 2200, "bandpass", 0, 3);
          osc("square", jitter(240, 0.05), 0.04, 0.04);
        },
        pump: function() {
          osc("sine", 96, 0.28, 0.05, 74);
          osc("sine", 74, 0.28, 0.03, 0, 0.02);
        },
        drip: function() {
          osc("sine", jitter(1400, 0.08), 0.08, 0.06, 480);
          noise(0.05, 0.03, 3e3, "bandpass", 0.03);
        },
        gulp: function() {
          osc("sine", jitter(220, 0.1), 0.1, 0.07, 90);
          noise(0.07, 0.05, 900, "lowpass");
        },
        dart: function() {
          osc("sine", jitter(640, 0.08), 0.08, 0.05, 180);
          noise(0.05, 0.04, 2e3, "highpass");
        },
        death: function() {
          osc("sawtooth", 180, 0.28, 0.07, 70);
          osc("triangle", 110, 0.32, 0.05, 48, 0.04);
        },
        feed: function() {
          noise(0.06, 0.05, 1800, "bandpass");
          osc("triangle", jitter(520, 0.08), 0.07, 0.05, 240);
        },
        pop: function() {
          osc("sine", jitter(880, 0.06), 0.07, 0.07, 220);
          noise(0.04, 0.04, 2600, "bandpass");
        },
        whoosh: function() {
          noise(0.22, 0.08, 900, "bandpass", 0, 0.5);
          osc("sine", 280, 0.18, 0.03, 80);
        },
        swoosh: function() {
          noise(0.16, 0.07, 1400, "highpass");
        },
        stamp: function() {
          noise(0.05, 0.1, 600, "lowpass");
          osc("sine", 140, 0.09, 0.08, 60);
        },
        phone: function() {
          osc("square", 440, 0.18, 0.06);
          osc("square", 480, 0.18, 0.05, 0, 0.02);
          osc("square", 440, 0.18, 0.06, 0, 0.38);
          osc("square", 480, 0.18, 0.05, 0, 0.4);
        },
        keys: function() {
          osc("square", jitter(2100, 0.08), 0.05, 0.04);
          osc("square", jitter(2600, 0.08), 0.04, 0.03, 0, 0.04);
          noise(0.04, 0.03, 4e3, "bandpass", 0.02);
        },
        drawer: function() {
          noise(0.12, 0.07, 500, "lowpass");
          osc("triangle", 140, 0.14, 0.05, 80);
        },
        wood: function() {
          osc("triangle", jitter(180, 0.06), 0.1, 0.07, 90);
          noise(0.06, 0.05, 700, "lowpass");
        },
        metal: function() {
          osc("square", jitter(1320, 0.04), 0.18, 0.05);
          osc("square", jitter(1975, 0.04), 0.12, 0.03, 0, 0.01);
        },
        glass: function() {
          osc("sine", jitter(2100, 0.03), 0.22, 0.05);
          osc("sine", jitter(3150, 0.03), 0.16, 0.03, 0, 0.01);
        },
        scrape: function() {
          noise(0.2, 0.07, 1100, "bandpass", 0, 1.4);
        },
        zipper: function() {
          noise(0.16, 0.06, 2800, "bandpass", 0, 2);
          osc("sawtooth", 400, 0.12, 0.03, 900);
        },
        clock: function() {
          osc("square", jitter(1100, 0.02), 0.03, 0.04);
          noise(0.02, 0.03, 2500, "bandpass");
        },
        thunder: function() {
          noise(0.55, 0.16, 180, "lowpass");
          osc("sine", 48, 0.5, 0.1, 28);
        },
        wind: function() {
          noise(0.7, 0.07, 700, "bandpass", 0, 0.4);
        },
        levelup: function() {
          osc("triangle", 523.25, 0.12, 0.07);
          osc("triangle", 659.25, 0.14, 0.07, 0, 0.1);
          osc("triangle", 783.99, 0.18, 0.08, 0, 0.2);
          osc("triangle", 1046.5, 0.28, 0.07, 0, 0.32);
        },
        research: function() {
          osc("sine", 392, 0.1, 0.05);
          osc("sine", 494, 0.12, 0.05, 0, 0.08);
          osc("sine", 587, 0.18, 0.06, 0, 0.16);
        },
        xp: function() {
          osc("sine", jitter(1320, 0.03), 0.08, 0.05);
          osc("sine", jitter(1760, 0.03), 0.1, 0.04, 0, 0.05);
        },
        debt: function() {
          osc("sawtooth", 98, 0.22, 0.07, 70);
          osc("triangle", 73, 0.28, 0.05, 0, 0.04);
        },
        loan: function() {
          osc("triangle", 196, 0.12, 0.06);
          osc("triangle", 247, 0.16, 0.05, 0, 0.08);
          noise(0.08, 0.04, 900, "lowpass", 0.04);
        },
        siren: function() {
          osc("sine", 680, 0.35, 0.06, 980);
          osc("sine", 980, 0.35, 0.05, 680, 0.32);
        },
        crowd: function() {
          noise(0.4, 0.06, 400, "bandpass", 0, 0.6);
          noise(0.3, 0.04, 900, "bandpass", 0.05, 0.8);
        },
        cheer: function() {
          osc("triangle", 784, 0.2, 0.05);
          osc("triangle", 988, 0.22, 0.05, 0, 0.06);
          noise(0.24, 0.05, 1200, "bandpass", 0.04);
        },
        clap: function() {
          noise(0.06, 0.12, 1800, "bandpass", 0, 1.2);
          noise(0.04, 0.06, 3200, "highpass", 0.01);
        },
        engine: function() {
          osc("sawtooth", 70, 0.4, 0.05, 90);
          noise(0.4, 0.05, 220, "lowpass");
        },
        dock: function() {
          osc("sine", 90, 0.18, 0.08, 50);
          noise(0.12, 0.07, 400, "lowpass");
        },
        snapshot: function() {
          noise(0.04, 0.1, 4e3, "highpass");
          osc("sine", 180, 0.08, 0.05, 60);
        },
        notify: function() {
          osc("sine", 880, 0.08, 0.06);
          osc("sine", 1320, 0.12, 0.05, 0, 0.07);
        },
        success: function() {
          osc("triangle", 659.25, 0.1, 0.07);
          osc("triangle", 880, 0.16, 0.07, 0, 0.09);
        },
        error: function() {
          osc("sawtooth", 160, 0.16, 0.07, 90);
          osc("triangle", 120, 0.18, 0.05, 0, 0.05);
        },
        murmur: function() {
          noise(0.28, 0.04, 280, "bandpass", 0, 2.2);
          osc("triangle", jitter(180, 0.12), 0.22, 0.03);
        },
        gull: function() {
          osc("sawtooth", jitter(1100, 0.08), 0.16, 0.04, 1400);
          osc("sawtooth", jitter(900, 0.08), 0.12, 0.03, 1200, 0.18);
        },
        horn: function() {
          osc("square", jitter(310, 0.04), 0.28, 0.05);
          osc("square", jitter(370, 0.04), 0.28, 0.04, 0, 0.01);
        },
        car: function() {
          noise(0.5, 0.05, 500, "lowpass");
          osc("sawtooth", 90, 0.4, 0.03, 70);
        },
        sneeze: function() {
          noise(0.08, 0.1, 1800, "bandpass");
          osc("sine", 200, 0.1, 0.05, 80);
        },
        cough: function() {
          noise(0.1, 0.08, 700, "lowpass");
          osc("triangle", 140, 0.1, 0.05, 70);
        },
        heartbeat: function() {
          osc("sine", 70, 0.1, 0.08, 40);
          osc("sine", 70, 0.1, 0.06, 40, 0.22);
        },
        type: function() {
          noise(0.02, 0.05, 4200, "bandpass", 0, 3);
          osc("square", jitter(480, 0.1), 0.03, 0.03);
        },
        whistle: function() {
          osc("sine", jitter(1480, 0.04), 0.16, 0.05, 1680);
          osc("sine", jitter(1760, 0.04), 0.12, 0.04, 1980, 0.08);
        },
        shutter: function() {
          noise(0.03, 0.1, 3500, "highpass");
          osc("sine", 90, 0.06, 0.05, 50);
        },
        bubble: function() {
          osc("sine", jitter(420, 0.12), 0.08, 0.05, jitter(900, 0.1));
          osc("sine", jitter(360, 0.12), 0.06, 0.03, jitter(800, 0.1), 0.05);
        },
        rain: function() {
          noise(0.6, 0.06, 3500, "highpass");
        },
        room: function() {
          noise(0.2, 0.04, 900, "bandpass");
        }
      };
      CATALOG.waterchange = CATALOG.pour;
      CATALOG.pellet = CATALOG.feed;
      CATALOG.drop = CATALOG.pop;
      CATALOG.alarm = CATALOG.siren;
      CATALOG.level = CATALOG.levelup;
      CATALOG.achievement = CATALOG.success;
      CATALOG.ui = CATALOG.click;
      CATALOG.open = CATALOG.modal;
      CATALOG.shut = CATALOG.lid;
      CATALOG.foot = CATALOG.wood;
      CATALOG.tune = CATALOG.whistle;
      function playNamed(name, x) {
        if (!name) return false;
        name = String(name);
        if (isFinite(x)) panTo(x);
        var I2 = window.I || window.sfx;
        if (I2 && typeof I2[name] === "function" && !I2[name].__feelSynth) {
          try {
            I2[name]();
            markSfx();
            return true;
          } catch (e) {
          }
        }
        var fn = CATALOG[name];
        if (!fn) return false;
        try {
          fn();
          markSfx();
        } catch (e) {
        }
        if (window.ge) {
          try {
            ge.pan = 0;
            ge.hint = null;
          } catch (err) {
          }
        }
        return true;
      }
      function punch(amt) {
        if (reduced) return;
        trauma = Math.min(1, trauma + (amt == null ? 0.25 : amt));
        shakeSeed = Math.random() * 6.283;
      }
      function flash(color, amt) {
        if (reduced) return;
        flashA = Math.max(flashA, amt == null ? 0.22 : amt);
        if (color) {
          var c = color;
          if (c[0] === "#") {
            var h = c.slice(1);
            if (h.length === 3) h = h[0] + h[0] + h[1] + h[1] + h[2] + h[2];
            var n = parseInt(h, 16);
            flashCol = (n >> 16 & 255) + "," + (n >> 8 & 255) + "," + (n & 255);
          }
        }
      }
      function floatText(text, x, y, color) {
        if (reduced && floats.length > 6) return;
        var cv = document.getElementById("tank");
        var r = cv ? cv.getBoundingClientRect() : { left: 0, top: 0, width: innerWidth, height: innerHeight };
        var str = String(text);
        var mag = Math.abs(parseFloat(str.replace(/[^0-9.\-]/g, ""))) || 0;
        floats.push({
          t: 0,
          dur: 0.95,
          x: x == null ? r.width * 0.5 : x,
          y: y == null ? r.height * 0.42 : y,
          text: str,
          col: color || "#f4c453",
          /* A four-figure sale should not come up the same size as a penny. */
          size: 15 + Math.min(11, Math.log(1 + mag) * 1.9),
          drift: (Math.random() * 2 - 1) * 9
        });
        if (floats.length > 24) floats.shift();
      }
      function ripple(x, y, color) {
        if (reduced) return;
        ripples.push({
          t: 0,
          dur: 0.86,
          x,
          y,
          col: color || "180,220,245",
          r0: 5,
          r1: 62
        });
        if (ripples.length > 18) ripples.shift();
      }
      function spark(x, y, col, opt) {
        opt = opt || {};
        var a = opt.a == null ? Math.random() * Math.PI * 2 : opt.a;
        var sp = opt.sp == null ? 40 + Math.random() * 90 : opt.sp;
        sparks.push({
          t: 0,
          dur: opt.dur || 0.45 + Math.random() * 0.35,
          x,
          y,
          vx: Math.cos(a) * sp,
          vy: Math.sin(a) * sp - (opt.lift == null ? 30 : opt.lift),
          col: col || "#f4c453",
          r: opt.r || 1.4 + Math.random() * 2.2,
          drag: opt.drag == null ? 1.6 : opt.drag,
          grav: opt.grav == null ? 140 : opt.grav,
          flake: !!opt.flake,
          rot: Math.random() * 6.283,
          spin: (Math.random() * 2 - 1) * 7
        });
        if (sparks.length > 90) sparks.splice(0, sparks.length - 90);
      }
      function burst(x, y, color, n) {
        n = reduced ? Math.min(n || 8, 6) : n || 12;
        for (var i = 0; i < n; i++) {
          spark(x, y, color, { a: i / n * Math.PI * 2 + Math.random() * 0.4 });
        }
      }
      function confetti(x, y) {
        var cols = ["#f4c453", "#ff6f59", "#3fbf8a", "#dcd3ff", "#7ac74f", "#e6f1f8"];
        var n = reduced ? 10 : 26;
        for (var i = 0; i < n; i++) {
          spark(x, y, cols[i % cols.length], {
            a: -Math.PI * 0.5 + (Math.random() * 2 - 1) * 1.15,
            sp: 90 + Math.random() * 190,
            lift: 60,
            dur: 0.9 + Math.random() * 0.7,
            r: 2.2 + Math.random() * 2.6,
            drag: 2.4,
            grav: 190,
            flake: true
          });
        }
      }
      function feedDrop(x, y) {
        pellets.push({
          t: 0,
          dur: 0.55,
          x,
          y0: y - 40,
          y1: y,
          r: 2.2 + Math.random()
        });
        if (pellets.length > 20) pellets.shift();
        playNamed("feed", x);
      }
      function coinFly(x, y, amount) {
        var el = document.getElementById("r-coins");
        var r = el ? el.getBoundingClientRect() : { left: innerWidth * 0.2, top: 18, width: 40, height: 20 };
        var cv = document.getElementById("tank");
        var cr = cv ? cv.getBoundingClientRect() : { left: 0, top: 0 };
        floats.push({
          t: 0,
          dur: 0.7,
          x,
          y,
          tx: r.left + r.width * 0.5 - cr.left,
          ty: r.top + r.height * 0.5 - cr.top,
          text: amount != null ? amount > 0 ? "+" + amount : String(amount) : "$",
          col: "#f4c453",
          fly: 1,
          size: 16,
          /* Thrown, not dragged: the control point lifts the arc above the line. */
          cx: x + (r.left + r.width * 0.5 - cr.left - x) * 0.4,
          cy: Math.min(y, r.top + r.height * 0.5 - cr.top) - 70
        });
        pulseHud("r-coins");
      }
      function pulseHud(id) {
        var el = document.getElementById(id);
        if (!el) return;
        var v = el.querySelector(".v") || el;
        v.classList.remove("tick");
        void v.offsetWidth;
        v.classList.add("tick");
        hudPulse[id] = now();
      }
      function ensureOverlay() {
        if (overlay) return;
        overlay = document.createElement("canvas");
        overlay.id = "feelfx";
        overlay.setAttribute("aria-hidden", "true");
        document.body.appendChild(overlay);
        octx = overlay.getContext("2d");
        syncOverlay();
      }
      var ovW = 0;
      var ovH = 0;
      var ovDpr = 0;
      function syncOverlay() {
        if (!overlay) return;
        var dpr = Math.min(window.__finsGlass && window.__finsGlass.dpr || 2, window.devicePixelRatio || 1);
        var w = innerWidth;
        var h = innerHeight;
        if (w === ovW && h === ovH && dpr === ovDpr) return;
        ovW = w;
        ovH = h;
        ovDpr = dpr;
        overlay.width = Math.round(w * dpr);
        overlay.height = Math.round(h * dpr);
        overlay.style.width = w + "px";
        overlay.style.height = h + "px";
        if (octx) octx.setTransform(dpr, 0, 0, dpr, 0, 0);
      }
      var TRAIL = 0.026;
      function busy() {
        return flashA > 2e-3 || trauma > 2e-3 || floats.length > 0 || ripples.length > 0 || sparks.length > 0 || pellets.length > 0;
      }
      function clearAll() {
        floats.length = 0;
        ripples.length = 0;
        sparks.length = 0;
        pellets.length = 0;
        flashA = 0;
        trauma = 0;
      }
      function tick(t) {
        var gap = last ? (t - last) / 1e3 : 0.016;
        if (gap > 0.5) {
          clearAll();
          var tankBack = document.getElementById("tank");
          if (tankBack && tankBack.style.transform) tankBack.style.transform = "";
        }
        var dt = last ? Math.min(0.05, gap) : 0.016;
        last = t;
        refreshCalm();
        if (trauma > 2e-3 && !reduced) {
          trauma = Math.max(0, trauma - dt * 2.6);
          shakeT += dt;
          var sh = trauma * trauma;
          var tank = document.getElementById("tank");
          if (tank) {
            var ox = Math.sin(shakeT * 78.5 + shakeSeed) * sh * 11;
            var oy = Math.sin(shakeT * 61.3 + shakeSeed * 1.7 + 1.1) * sh * 7.5;
            tank.style.transform = "translate(" + ox.toFixed(2) + "px," + oy.toFixed(2) + "px)";
          }
        } else if (trauma) {
          trauma = 0;
          var tank2 = document.getElementById("tank");
          if (tank2) tank2.style.transform = "";
        }
        if (flashA > 2e-3) flashA = Math.max(0, flashA - dt * 2.8);
        else flashA = 0;
        var i;
        for (i = floats.length - 1; i >= 0; i--) {
          floats[i].t += dt;
          if (floats[i].t >= floats[i].dur) floats.splice(i, 1);
        }
        for (i = ripples.length - 1; i >= 0; i--) {
          ripples[i].t += dt;
          if (ripples[i].t >= ripples[i].dur) ripples.splice(i, 1);
        }
        for (i = sparks.length - 1; i >= 0; i--) {
          var s = sparks[i];
          s.t += dt;
          s.x += s.vx * dt;
          s.y += s.vy * dt;
          var k2 = Math.max(0, 1 - s.drag * dt);
          s.vx *= k2;
          s.vy = s.vy * k2 + s.grav * dt;
          s.rot += s.spin * dt;
          if (s.t >= s.dur) sparks.splice(i, 1);
        }
        for (i = pellets.length - 1; i >= 0; i--) {
          pellets[i].t += dt;
          if (pellets[i].t >= pellets[i].dur) pellets.splice(i, 1);
        }
        if (busy()) {
          if (!window.finsPace || finsPace.shouldPaint(t)) {
            draw();
            painted = true;
          }
        } else if (painted) {
          syncOverlay();
          if (octx) octx.clearRect(0, 0, innerWidth, innerHeight);
          painted = false;
        }
        requestAnimationFrame(tick);
      }
      function draw() {
        if (!octx) return;
        syncOverlay();
        var w = innerWidth;
        var h = innerHeight;
        octx.clearRect(0, 0, w, h);
        if (flashA > 0.01) {
          var fa = flashA * flashA * 1.9;
          var gr = octx.createRadialGradient(w * 0.5, h * 0.44, Math.min(w, h) * 0.12, w * 0.5, h * 0.44, Math.max(w, h) * 0.72);
          gr.addColorStop(0, "rgba(" + flashCol + "," + (fa * 0.34).toFixed(3) + ")");
          gr.addColorStop(0.55, "rgba(" + flashCol + "," + (fa * 0.62).toFixed(3) + ")");
          gr.addColorStop(1, "rgba(" + flashCol + "," + Math.min(0.85, fa).toFixed(3) + ")");
          octx.fillStyle = gr;
          octx.fillRect(0, 0, w, h);
        }
        var k2, p, a, u;
        for (k2 = 0; k2 < ripples.length; k2++) {
          p = ripples[k2];
          u = p.t / p.dur;
          var spread = 1 - Math.pow(1 - u, 2.4);
          a = Math.pow(1 - u, 1.7);
          var rad = p.r0 + (p.r1 - p.r0) * spread;
          octx.lineWidth = 0.7 + 2.1 * (1 - u);
          octx.strokeStyle = "rgba(" + p.col + "," + (0.5 * a).toFixed(3) + ")";
          octx.beginPath();
          octx.ellipse(p.x, p.y, rad, rad * 0.88, 0, 0, Math.PI * 2);
          octx.stroke();
          if (rad > 16) {
            octx.lineWidth = 0.6 + 1.1 * (1 - u);
            octx.strokeStyle = "rgba(" + p.col + "," + (0.22 * a).toFixed(3) + ")";
            octx.beginPath();
            octx.ellipse(p.x, p.y, rad * 0.61, rad * 0.54, 0, 0, Math.PI * 2);
            octx.stroke();
          }
        }
        for (k2 = 0; k2 < pellets.length; k2++) {
          p = pellets[k2];
          u = p.t / p.dur;
          var yy = p.y0 + (p.y1 - p.y0) * (u * u);
          octx.fillStyle = "rgba(217,160,107," + (1 - u * 0.3).toFixed(3) + ")";
          octx.beginPath();
          octx.arc(p.x, yy, p.r, 0, 7);
          octx.fill();
        }
        if (sparks.length) {
          octx.lineCap = "round";
          for (k2 = 0; k2 < sparks.length; k2++) {
            p = sparks[k2];
            u = p.t / p.dur;
            a = Math.pow(1 - u, 1.4);
            octx.globalAlpha = a;
            if (p.flake) {
              octx.globalCompositeOperation = "source-over";
              octx.save();
              octx.translate(p.x, p.y);
              octx.rotate(p.rot);
              octx.fillStyle = p.col;
              var fw = p.r * 2.1;
              var fh = p.r * 1.3 * Math.abs(Math.cos(p.rot * 1.6));
              octx.fillRect(-fw * 0.5, -fh * 0.5, fw, Math.max(0.7, fh));
              octx.restore();
            } else {
              octx.globalCompositeOperation = "lighter";
              octx.strokeStyle = p.col;
              octx.lineWidth = Math.max(0.9, p.r * 1.8 * (1 - u * 0.55));
              octx.beginPath();
              octx.moveTo(p.x - p.vx * TRAIL, p.y - p.vy * TRAIL);
              octx.lineTo(p.x, p.y);
              octx.stroke();
            }
          }
          octx.globalCompositeOperation = "source-over";
          octx.globalAlpha = 1;
        }
        octx.textAlign = "center";
        octx.textBaseline = "middle";
        for (k2 = 0; k2 < floats.length; k2++) {
          p = floats[k2];
          var tt = p.t / p.dur;
          var ease = 1 - Math.pow(1 - tt, 3);
          var fx = p.x;
          var fy = p.y - 46 * ease;
          if (p.fly && p.tx != null) {
            var iv = 1 - ease;
            fx = iv * iv * p.x + 2 * iv * ease * p.cx + ease * ease * p.tx;
            fy = iv * iv * p.y + 2 * iv * ease * p.cy + ease * ease * p.ty;
          } else {
            fx += p.drift * ease;
          }
          var sc = tt < 0.16 ? 0.62 + tt / 0.16 * 0.52 : 1.14 - Math.min(1, (tt - 0.16) / 0.22) * 0.14;
          var size = Math.round((p.size || 16) * sc);
          octx.font = "800 " + size + "px Nunito, 'Segoe UI', system-ui, sans-serif";
          octx.globalAlpha = tt < 0.7 ? 1 : 1 - (tt - 0.7) / 0.3;
          octx.strokeStyle = "rgba(6,14,24,.6)";
          octx.lineWidth = Math.max(2, size * 0.17);
          octx.lineJoin = "round";
          octx.strokeText(p.text, fx, fy);
          octx.fillStyle = p.col;
          octx.fillText(p.text, fx, fy);
        }
        octx.globalAlpha = 1;
      }
      function wrapI(name, after) {
        var I2 = window.I || window.sfx;
        if (!I2 || typeof I2[name] !== "function" || I2[name].__feelWrap) return;
        var orig = I2[name];
        I2[name] = function() {
          markSfx();
          var r = orig.apply(this, arguments);
          try {
            after.apply(this, arguments);
          } catch (e) {
          }
          return r;
        };
        I2[name].__feelWrap = 1;
      }
      function fillGaps() {
        var I2 = window.I || window.sfx;
        if (!I2) return;
        Object.keys(CATALOG).forEach(function(name) {
          if (typeof I2[name] !== "function") {
            I2[name] = function(x) {
              playNamed(name, x);
            };
            I2[name].__feelSynth = 1;
          }
        });
      }
      var lastToastJuice = 0;
      function juiceForToast(kind, msg) {
        var k2 = String(kind || "");
        if (!k2 || k2 === "hint") return;
        if (now() - lastToastJuice < 420) return;
        lastToastJuice = now();
        if (k2 === "bad") {
          flash("#ff6f59", 0.1);
          punch(0.08);
        } else if (k2 === "gold") {
          flash("#f4c453", 0.06);
          pulseHud("r-coins");
        } else if (k2 === "prog") {
          flash("#f4c453", 0.05);
          pulseHud("r-level");
        } else if (k2 === "good") {
          pulseHud("r-fish");
        }
        if (now() - lastSfx < 180) return;
        if (k2 === "bad") playNamed(/warn|clog|ill|sick|debt|owe/.test(String(msg || "")) ? "warn" : "bad");
        else if (k2 === "gold") playNamed("coin");
        else if (k2 === "prog") playNamed("sparkle");
        else if (k2 === "good") playNamed("chime");
      }
      function pointerRipple(ev) {
        if (document.body.classList.contains("titling")) return;
        var cv = document.getElementById("tank");
        if (!cv) return;
        var r = cv.getBoundingClientRect();
        var x = (ev.clientX || 0) - r.left;
        var y = (ev.clientY || 0) - r.top;
        var scene = "";
        try {
          if (typeof window.sceneOn === "function") scene = String(window.sceneOn() || "");
        } catch (e) {
        }
        if (scene === "shop") ripple(x, y, "210,190,160");
        else {
          ripple(x, y, "190,230,255");
          if (now() - lastSfx > 220) playNamed("drip", x);
        }
      }
      function onEventCard(node) {
        if (!node || !node.classList) return;
        var r = node.getBoundingClientRect();
        var x = r.left + r.width * 0.5;
        var y = r.top + r.height * 0.5;
        if (node.classList.contains("order") || node.classList.contains("wild")) {
          if (now() - lastSfx > 80) playNamed("bell", x);
          punch(0.08);
          ripple(x, y, "244,196,83");
        } else if (node.classList.contains("sick")) {
          if (now() - lastSfx > 80) playNamed("warn", x);
          flash("#ff6f59", 0.1);
        } else if (node.classList.contains("filter")) {
          if (now() - lastSfx > 80) playNamed("pump", x);
        }
      }
      function wire() {
        if (wired) return;
        wired = true;
        fillGaps();
        var I0 = window.I || window.sfx;
        if (I0 && typeof I0.fx !== "function") I0.fx = playNamed;
        wrapI("coin", function() {
          pulseHud("r-coins");
        });
        wrapI("buy", function() {
          flash("#f4c453", 0.06);
        });
        wrapI("sell", function() {
          pulseHud("r-coins");
          flash("#3fbf8a", 0.06);
        });
        wrapI("bad", function() {
          punch(0.16);
          flash("#ff6f59", 0.12);
        });
        wrapI("thud", function() {
          punch(0.28);
        });
        wrapI("crit", function() {
          punch(0.4);
          flash("#ff6f59", 0.16);
        });
        wrapI("splash", function() {
          ripple(innerWidth * 0.5, innerHeight * 0.28, "190,230,255");
        });
        wrapI("award", function() {
          confetti(innerWidth * 0.5, innerHeight * 0.28);
          punch(0.22);
          flash("#f4c453", 0.14);
        });
        wrapI("chime", function() {
          flash("#f4c453", 0.05);
        });
        wrapI("hatch", function() {
          burst(innerWidth * 0.5, innerHeight * 0.55, "#cfe6f2", 14);
        });
        wrapI("unlock", function() {
          flash("#dcd3ff", 0.1);
          punch(0.12);
        });
        wrapI("warn", function() {
          flash("#ff6f59", 0.1);
        });
        wrapI("door", function() {
          flash("#f4c453", 0.04);
        });
        wrapI("shot", function() {
          punch(0.55);
          flash("#fff6d8", 0.35);
        });
        wrapI("gavel", function() {
          punch(0.2);
        });
        try {
          var prevShow = window.toastShow;
          if (typeof prevShow === "function") {
            window.toastShow = function(msg, kind, n) {
              try {
                juiceForToast(kind, msg);
              } catch (e) {
              }
              return prevShow.apply(this, arguments);
            };
          }
        } catch (e) {
        }
        try {
          var sc = window.spawnCoin;
          if (typeof sc === "function") {
            window.spawnCoin = function(fish) {
              var r = sc.apply(this, arguments);
              try {
                if (fish && isFinite(fish.x)) {
                  coinFly(fish.x, fish.y, fish.v);
                  if (now() - lastSfx > 60) playNamed("coin", fish.x);
                }
              } catch (e) {
              }
              return r;
            };
          }
        } catch (e) {
        }
        var tank = document.getElementById("tank");
        if (tank) {
          tank.addEventListener(
            "pointerdown",
            function(ev) {
              try {
                pointerRipple(ev);
              } catch (e) {
              }
            },
            { passive: true }
          );
        }
        document.addEventListener(
          "click",
          function(ev) {
            var t = ev.target;
            if (!t || !t.closest) return;
            if (t.closest(".buy") && !t.closest(".buy:disabled")) {
              if (now() - lastSfx > 70) playNamed("click");
            } else if (t.closest(".tt-btn")) {
              if (now() - lastSfx > 70) playNamed("click");
            }
          },
          true
        );
        var evs = document.getElementById("events");
        if (evs && typeof MutationObserver === "function") {
          new MutationObserver(function(muts) {
            for (var i = 0; i < muts.length; i++) {
              var nodes = muts[i].addedNodes;
              for (var j = 0; j < nodes.length; j++) onEventCard(nodes[j]);
            }
          }).observe(evs, { childList: true });
        }
        var unlock = function() {
          try {
            if (window.oe && typeof oe.userGesture === "function") oe.userGesture();
            else if (window.oe && typeof oe.init === "function") {
              oe.init();
              if (oe.ctx && oe.ctx.state === "suspended") oe.ctx.resume();
            }
          } catch (e) {
          }
        };
        window.addEventListener("pointerdown", unlock, { once: true, capture: true });
        window.addEventListener("keydown", unlock, { once: true, capture: true });
        document.addEventListener("visibilitychange", function() {
          if (document.visibilityState === "visible") {
            try {
              var c = audio();
              if (c && c.state === "suspended") c.resume();
            } catch (e) {
            }
          }
        });
        var lastStep = 0;
        var engineStep = typeof window.folkStep === "function" ? window.folkStep : null;
        window.folkStep = function(x, y) {
          if (!sfxOn()) return;
          var t = now();
          if (t - lastStep < 90) return;
          lastStep = t;
          panTo(x);
          if (engineStep) {
            try {
              engineStep(x, y);
              return;
            } catch (e) {
            }
          }
          var scene = "";
          try {
            if (typeof sceneNow === "function") scene = String(sceneNow() || "");
            if (!scene && typeof gameState === "object" && gameState && gameState.scene) scene = String(gameState.scene);
            if (!scene) {
              var on = document.querySelector("#scenes .sc.on");
              if (on && on.dataset.scene) scene = on.dataset.scene;
            }
          } catch (e) {
          }
          if (scene === "shop") playNamed("wood", x);
          else if (scene === "street") playNamed("gravel", x);
          else playNamed("foot", x);
        };
      }
      window.feel = {
        play: playNamed,
        has: function(name) {
          var I2 = window.I || window.sfx;
          return !!(CATALOG[name] || I2 && typeof I2[name] === "function");
        },
        list: function() {
          var I2 = window.I || window.sfx;
          var set = /* @__PURE__ */ Object.create(null);
          Object.keys(CATALOG).forEach(function(k2) {
            set[k2] = 1;
          });
          if (I2) Object.keys(I2).forEach(function(k2) {
            if (typeof I2[k2] === "function") set[k2] = 1;
          });
          return Object.keys(set).sort();
        },
        punch,
        flash,
        float: floatText,
        ripple,
        burst,
        confetti,
        feed: feedDrop,
        coinFly,
        pulseHud,
        catalog: CATALOG
      };
      window.__feelTest = function() {
        fillGaps();
        playNamed("click");
        punch(0.2);
        flash("#f4c453", 0.12);
        floatText("+$12", innerWidth * 0.5, innerHeight * 0.4);
        ripple(innerWidth * 0.5, innerHeight * 0.55);
        burst(innerWidth * 0.5, innerHeight * 0.5, "#f4c453", 10);
        return { sounds: window.feel.list().length, trauma, floats: floats.length };
      };
      function boot() {
        ensureOverlay();
        wire();
        requestAnimationFrame(tick);
      }
      if (document.readyState === "loading") document.addEventListener("DOMContentLoaded", boot);
      else boot();
    })();
  } catch (e) {
    console.error("Layer feel.js failed to start:", e);
  }

  // src/layers/tech.js
  try {
    (function() {
      "use strict";
      var origs = window.__techOrigs || (window.__techOrigs = {});
      var T = { v: 3, ok: 1, kelp: 0, folk: 0, fish: 0, lastErr: "" };
      window.__tech = T;
      var reduced = false;
      try {
        reduced = !!(window.matchMedia && window.matchMedia("(prefers-reduced-motion: reduce)").matches);
      } catch (e) {
      }
      var PI2 = Math.PI * 2;
      var STEP = 1 / 60;
      var acc = 0;
      var simT = 0;
      var frameDt = 0.016;
      var lastPtr = { x: 0.5, y: 0.5 };
      var lastTanks = [];
      var lastFloorY = 0;
      var lastWx = {};
      var fishDrawn = 0;
      function clamp(x, a, b) {
        return x < a ? a : x > b ? b : x === x ? x : a;
      }
      function frac(x) {
        return x - Math.floor(x);
      }
      function halton(i, b) {
        var f = 1, r = 0;
        while (i > 0) {
          f /= b;
          r += f * (i % b);
          i = i / b | 0;
        }
        return r;
      }
      function o2q() {
        try {
          if (typeof o2Tank === "function") {
            var v = o2Tank();
            if (v && typeof v === "object") v = v.mean != null ? v.mean : v.worst;
            v = +v;
            if (v === v) return clamp(v, 0.15, 1);
          }
          if (window.G && typeof window.G.o2 === "number") return clamp(window.G.o2, 0.15, 1);
        } catch (e) {
        }
        return 0.7;
      }
      function sceneName() {
        try {
          if (document.body.classList.contains("titling")) return "title";
          if (document.body.classList.contains("work")) return "work";
          if (window.G && window.G.scene) return String(window.G.scene);
          if (typeof rr === "function") return String(rr() || "tank");
        } catch (e) {
        }
        return "tank";
      }
      function isQuiet() {
        var s = sceneName();
        return s === "title" || s === "work";
      }
      var perm = new Uint8Array(512);
      (function seedPerm() {
        var p = new Uint8Array(256);
        for (var i = 0; i < 256; i++) p[i] = i;
        var s = 1337;
        for (var j = 255; j > 0; j--) {
          s = s * 16807 + 11 >>> 0;
          var k2 = s % (j + 1);
          var tmp = p[j];
          p[j] = p[k2];
          p[k2] = tmp;
        }
        for (var n = 0; n < 512; n++) perm[n] = p[n & 255];
      })();
      function grad2(h, x, y) {
        var g = h & 3;
        return (g === 0 || g === 2 ? x : -x) + (g === 1 || g === 2 ? y : -y);
      }
      function simplex2(xin, yin) {
        var F2 = 0.36602540378, G2 = 0.2113248654;
        var s = (xin + yin) * F2;
        var i = Math.floor(xin + s);
        var j = Math.floor(yin + s);
        var t = (i + j) * G2;
        var x0 = xin - (i - t);
        var y0 = yin - (j - t);
        var i1 = x0 > y0 ? 1 : 0;
        var j1 = x0 > y0 ? 0 : 1;
        var x1 = x0 - i1 + G2;
        var y1 = y0 - j1 + G2;
        var x2 = x0 - 1 + 2 * G2;
        var y2 = y0 - 1 + 2 * G2;
        var ii = i & 255, jj = j & 255;
        var n0 = 0, n1 = 0, n2 = 0;
        var t0 = 0.5 - x0 * x0 - y0 * y0;
        if (t0 >= 0) {
          t0 *= t0;
          n0 = t0 * t0 * grad2(perm[ii + perm[jj]], x0, y0);
        }
        var t1 = 0.5 - x1 * x1 - y1 * y1;
        if (t1 >= 0) {
          t1 *= t1;
          n1 = t1 * t1 * grad2(perm[ii + i1 + perm[jj + j1]], x1, y1);
        }
        var t2 = 0.5 - x2 * x2 - y2 * y2;
        if (t2 >= 0) {
          t2 *= t2;
          n2 = t2 * t2 * grad2(perm[ii + 1 + perm[jj + 1]], x2, y2);
        }
        return 70 * (n0 + n1 + n2);
      }
      function fbm(x, y) {
        return simplex2(x, y) * 0.55 + simplex2(x * 2.03, y * 2.03) * 0.3 + simplex2(x * 4.07, y * 4.07) * 0.15;
      }
      var _cx = 0, _cy = 0;
      function curl(x, y, t) {
        var e = 1.7;
        var n1 = fbm(x * 8e-3, (y + e) * 8e-3 + t * 0.07);
        var n2 = fbm(x * 8e-3, (y - e) * 8e-3 + t * 0.07);
        var n3 = fbm((x + e) * 8e-3 + t * 0.05, y * 8e-3);
        var n4 = fbm((x - e) * 8e-3 + t * 0.05, y * 8e-3);
        _cx = (n1 - n2) * 18;
        _cy = (n4 - n3) * 18;
        return _cx;
      }
      function ign(x, y) {
        return frac(52.9829189 * frac(x * 0.06711056 + y * 583715e-8));
      }
      function worley(x, y) {
        var ix = Math.floor(x), iy = Math.floor(y);
        var fx = x - ix, fy = y - iy;
        var d = 9;
        for (var oy = -1; oy <= 1; oy++) {
          for (var ox = -1; ox <= 1; ox++) {
            var hx = ix + ox, hy = iy + oy;
            var n = perm[hx + perm[hy & 255] & 255];
            var px = ox + n / 255 * 0.85;
            var py = oy + perm[n + 37 & 255] / 255 * 0.85;
            var dx = fx - px, dy = fy - py;
            var q = dx * dx + dy * dy;
            if (q < d) d = q;
          }
        }
        return Math.sqrt(d);
      }
      function gerstnerY(x, t, seed) {
        var y = 0;
        y += 2.4 * Math.sin(x * 0.045 + t * 1.15 + seed);
        y += 1.4 * Math.sin(x * 0.09 + t * 1.7 + seed * 1.7);
        y += 0.7 * Math.sin(x * 0.18 + t * 2.4 + seed * 0.4);
        return y;
      }
      var waves = [];
      function addWave(x, y, amp) {
        waves.push({ x, y, r: 6, amp: amp || 1, life: 1 });
        if (waves.length > 14) waves.shift();
      }
      function stepWaves(dt) {
        for (var i = waves.length - 1; i >= 0; i--) {
          var w = waves[i];
          w.r += 70 * dt;
          w.life -= dt * 0.7;
          w.amp *= Math.pow(0.55, dt);
          if (w.life <= 0) waves.splice(i, 1);
        }
      }
      function drawWaves(ctx, clip) {
        if (!waves.length) return;
        ctx.save();
        if (clip) {
          ctx.beginPath();
          if (ctx.roundRect) ctx.roundRect(clip[0], clip[1], clip[2], clip[3], 3);
          else ctx.rect(clip[0], clip[1], clip[2], clip[3]);
          ctx.clip();
        }
        ctx.globalCompositeOperation = "lighter";
        for (var i = 0; i < waves.length; i++) {
          var w = waves[i];
          ctx.globalAlpha = 0.22 * w.life * w.amp;
          ctx.strokeStyle = "rgba(190,230,255,1)";
          ctx.lineWidth = 1.4;
          ctx.beginPath();
          ctx.arc(w.x, w.y, w.r, 0, PI2);
          ctx.stroke();
          ctx.globalAlpha = 0.1 * w.life * w.amp;
          ctx.beginPath();
          ctx.arc(w.x, w.y, w.r * 0.55, 0, PI2);
          ctx.stroke();
        }
        ctx.restore();
      }
      var vortices = [];
      function addVortex(x, y, s) {
        vortices.push({ x, y, s: s || 1, life: 1 });
        if (vortices.length > 8) vortices.shift();
      }
      function vortexAt(x, y) {
        var vx = 0, vy = 0;
        for (var i = 0; i < vortices.length; i++) {
          var v = vortices[i];
          var dx = x - v.x, dy = y - v.y;
          var d2 = dx * dx + dy * dy + 400;
          var mag = v.s * v.life * 140 / d2;
          vx += -dy * mag;
          vy += dx * mag;
        }
        return { x: vx, y: vy };
      }
      function wrap(name, around) {
        if (!origs[name]) {
          if (typeof window[name] !== "function") return false;
          origs[name] = window[name];
        }
        var orig = origs[name];
        window[name] = function() {
          return around(orig, this, arguments);
        };
        return true;
      }
      var causticTile = null;
      (function bakeCaustic() {
        var W = 128, H = 128;
        var cv = document.createElement("canvas");
        cv.width = W;
        cv.height = H;
        var g = cv.getContext("2d");
        var img = g.createImageData(W, H);
        for (var y = 0; y < H; y++) {
          for (var x = 0; x < W; x++) {
            var u = x / W, v = y / H;
            var wx = u + 0.14 * Math.sin(v * PI2 * 3) + 0.07 * Math.sin(v * PI2 * 7);
            var wy = v + 0.14 * Math.sin(u * PI2 * 3) + 0.07 * Math.sin(u * PI2 * 5);
            var n = Math.abs(Math.sin(wx * PI2 * 4) * Math.sin(wy * PI2 * 4));
            n = 1 - Math.abs(n * 2 - 1);
            n = Math.pow(n, 3.4);
            var i = (y * W + x) * 4;
            img.data[i] = 186;
            img.data[i + 1] = 228;
            img.data[i + 2] = 255;
            img.data[i + 3] = n * 150 | 0;
          }
        }
        g.putImageData(img, 0, 0);
        causticTile = cv;
      })();
      function fillCaustics(ctx, x, y, w, h, t, a) {
        if (!causticTile || w < 4 || h < 4) return;
        ctx.save();
        ctx.beginPath();
        ctx.rect(x, y, w, h);
        ctx.clip();
        ctx.globalCompositeOperation = "lighter";
        ctx.globalAlpha = a == null ? 0.22 : a;
        var ox = frac(t * 0.07) * 128;
        var oy = frac(t * 0.045) * 128;
        var pat = ctx.createPattern(causticTile, "repeat");
        if (pat) {
          ctx.translate(x - ox, y - oy);
          ctx.fillStyle = pat;
          ctx.fillRect(ox, oy, w, h);
        }
        ctx.restore();
      }
      var kelp = [];
      var kelpKey = "";
      function makeStrand(x, y, h, seed) {
        var n = reduced ? 5 : 8;
        var pts = [];
        for (var i = 0; i < n; i++) {
          pts.push({
            x: x + (seed % 7 - 3) * 0.4,
            y: y - i / (n - 1) * h,
            px: x,
            py: y - i / (n - 1) * h,
            pin: i === 0,
            bx: x,
            by: y
          });
        }
        pts[0].bx = x;
        pts[0].by = y;
        return { pts, rest: h / (n - 1), hue: 110 + seed % 40, thick: 2.6 + seed % 5 * 0.4 };
      }
      function ensureKelp(tanks) {
        var key = tanks.length + ":" + (tanks[0] && tanks[0][0] | 0) + ":" + (tanks[0] && tanks[0][2] | 0);
        if (key === kelpKey && kelp.length) return;
        kelpKey = key;
        kelp = [];
        for (var i = 0; i < tanks.length; i++) {
          var tk = tanks[i];
          var n = reduced ? 2 : 3;
          for (var s = 0; s < n; s++) {
            var x = tk[0] + tk[2] * (0.18 + (s + 0.4) * 0.22 + (i * 3 + s) % 5 * 0.04);
            var y = tk[1] + tk[3] * 0.82;
            var h = tk[3] * (0.34 + s % 3 * 0.08);
            kelp.push({ tank: i, strand: makeStrand(x, y, h, i * 17 + s * 9) });
          }
        }
      }
      function stepKelp(dt, t) {
        var drag = Math.pow(0.96, dt * 60);
        for (var k2 = 0; k2 < kelp.length; k2++) {
          var pts = kelp[k2].strand.pts;
          var rest = kelp[k2].strand.rest;
          for (var i = 1; i < pts.length; i++) {
            var p = pts[i];
            var vx = (p.x - p.px) * drag;
            var vy = (p.y - p.py) * drag;
            p.px = p.x;
            p.py = p.y;
            curl(p.x, p.y, t);
            var vo = vortexAt(p.x, p.y);
            p.x += vx + _cx * dt * 2.4 + vo.x * dt;
            p.y += vy + _cy * dt * 2.4 + vo.y * dt - 6 * dt;
          }
          for (var it = 0; it < 3; it++) {
            for (var j = 1; j < pts.length; j++) {
              var a = pts[j - 1], b = pts[j];
              var dx = b.x - a.x, dy = b.y - a.y;
              var d = Math.sqrt(dx * dx + dy * dy) || 1e-3;
              var corr = (d - rest) / d * 0.5;
              if (!a.pin) {
                a.x += dx * corr;
                a.y += dy * corr;
              }
              b.x -= dx * corr;
              b.y -= dy * corr;
            }
            if (pts[0].pin) {
              pts[0].x = pts[0].bx;
              pts[0].y = pts[0].by;
            }
          }
        }
        T.kelp = kelp.length;
      }
      function crPoint(p0, p1, p2, p3, t, out) {
        var t2 = t * t, t3 = t2 * t;
        out.x = 0.5 * (2 * p1.x + (-p0.x + p2.x) * t + (2 * p0.x - 5 * p1.x + 4 * p2.x - p3.x) * t2 + (-p0.x + 3 * p1.x - 3 * p2.x + p3.x) * t3);
        out.y = 0.5 * (2 * p1.y + (-p0.y + p2.y) * t + (2 * p0.y - 5 * p1.y + 4 * p2.y - p3.y) * t2 + (-p0.y + 3 * p1.y - 3 * p2.y + p3.y) * t3);
      }
      var _cr = { x: 0, y: 0 };
      function drawKelp(ctx, tanks) {
        if (!kelp.length) return;
        var painted = !!(window.shopBg && window.shopBg.complete && window.shopBg.naturalWidth) && sceneName() === "shop";
        for (var k2 = 0; k2 < kelp.length; k2++) {
          var item = kelp[k2];
          var tk = tanks[item.tank];
          if (!tk) continue;
          var pts = item.strand.pts;
          var n = pts.length;
          ctx.save();
          clipTank(ctx, tk, item.tank);
          ctx.strokeStyle = painted ? "rgba(26," + (72 + item.strand.hue % 30) + ",40,.5)" : "rgba(36," + (110 + item.strand.hue % 40) + ",58,.92)";
          ctx.lineWidth = painted ? item.strand.thick + 0.2 : item.strand.thick + 0.8;
          ctx.lineCap = "round";
          ctx.lineJoin = "round";
          ctx.beginPath();
          ctx.moveTo(pts[0].x, pts[0].y);
          for (var i = 0; i < n - 1; i++) {
            var p0 = pts[i === 0 ? 0 : i - 1];
            var p1 = pts[i];
            var p2 = pts[i + 1];
            var p3 = pts[i + 2] || p2;
            for (var s = 1; s <= 4; s++) {
              crPoint(p0, p1, p2, p3, s / 4, _cr);
              ctx.lineTo(_cr.x, _cr.y);
            }
          }
          ctx.stroke();
          ctx.strokeStyle = painted ? "rgba(120,190,120,.14)" : "rgba(110,200,130,.38)";
          ctx.lineWidth = Math.max(1, item.strand.thick * 0.45);
          ctx.stroke();
          ctx.restore();
        }
      }
      var sph = [];
      var sphKey = "";
      function ensureSph(tanks) {
        var key = tanks.length + ":" + (tanks[0] && tanks[0][2] | 0);
        if (key === sphKey && sph.length) return;
        sphKey = key;
        sph = [];
        var per = reduced ? 5 : 8;
        for (var i = 0; i < tanks.length; i++) {
          for (var b = 0; b < per; b++) {
            var id = i * per + b;
            sph.push({
              tank: i,
              u: 0.1 + halton(id + 3, 2) * 0.8,
              v: halton(id + 5, 3),
              r: 0.9 + halton(id, 5) * 1.7,
              vx: 0,
              vy: 0
            });
          }
        }
      }
      function stepSph(dt, t) {
        var n = sph.length;
        for (var i = 0; i < n; i++) {
          var a = sph[i];
          var tk = lastTanks[a.tank];
          if (!tk) continue;
          var x = tk[0] + 6 + a.u * (tk[2] - 12);
          var y = tk[1] + tk[3] * 0.14 + a.v * tk[3] * 0.66;
          curl(x, y, t);
          a.vx = a.vx * 0.92 + _cx * dt * 4;
          a.vy = a.vy * 0.92 - 0.22 * dt + _cy * dt * 2;
          for (var j = i + 1; j < n; j++) {
            var b = sph[j];
            if (b.tank !== a.tank) continue;
            var du = a.u - b.u, dv = a.v - b.v;
            var d2 = du * du + dv * dv;
            var min = 0.045;
            if (d2 > 1e-5 && d2 < min * min) {
              var d = Math.sqrt(d2);
              var push = (min - d) * 0.5;
              a.u += du / d * push;
              a.v += dv / d * push;
              b.u -= du / d * push;
              b.v -= dv / d * push;
            }
          }
          a.u += a.vx * dt * 0.15;
          a.v += a.vy * dt * 0.55;
          if (a.u < 0.06) {
            a.u = 0.06;
            a.vx *= -0.4;
          }
          if (a.u > 0.94) {
            a.u = 0.94;
            a.vx *= -0.4;
          }
          if (a.v < 0) {
            a.v = 1;
            a.u = 0.1 + halton((i + t * 10 | 0) & 255, 2) * 0.8;
            a.vx = 0;
            a.vy = 0;
          }
          if (a.v > 1) a.v = 1;
        }
      }
      function drawSph(ctx, tk, tankI) {
        ctx.save();
        ctx.globalCompositeOperation = "source-over";
        for (var i = 0; i < sph.length; i++) {
          var a = sph[i];
          if (a.tank !== tankI) continue;
          var bx = tk[0] + 6 + a.u * (tk[2] - 12);
          var by = tk[1] + tk[3] * 0.14 + a.v * tk[3] * 0.66;
          var r = a.r;
          ctx.globalAlpha = 0.28 + 0.4 * (1 - a.v);
          ctx.strokeStyle = "rgba(210,240,255,.85)";
          ctx.lineWidth = 1;
          ctx.beginPath();
          ctx.arc(bx, by, r, 0, PI2);
          ctx.stroke();
          ctx.globalAlpha = 0.35;
          ctx.fillStyle = "rgba(255,255,255,.9)";
          ctx.beginPath();
          ctx.arc(bx - r * 0.3, by - r * 0.35, Math.max(0.5, r * 0.28), 0, PI2);
          ctx.fill();
        }
        ctx.restore();
      }
      var markCache = /* @__PURE__ */ new Map();
      function bakeMark(seed) {
        seed = seed | 0;
        if (markCache.has(seed)) return markCache.get(seed);
        var W = 32, H = 24, N = W * H;
        var A = new Float32Array(N), B = new Float32Array(N);
        var i, x, y;
        for (i = 0; i < N; i++) {
          A[i] = 1;
          B[i] = 0;
        }
        var rng = seed || 1;
        function rnd() {
          rng = rng * 1664525 + 1013904223 >>> 0;
          return rng / 4294967296;
        }
        for (i = 0; i < 18; i++) {
          var cx = 2 + rnd() * (W - 4) | 0;
          var cy = 2 + rnd() * (H - 4) | 0;
          B[cy * W + cx] = 1;
        }
        var F = 0.037 + Math.abs(seed) % 9 * 2e-3;
        var K = 0.06 + Math.abs(seed) % 7 * 15e-4;
        var ru = 0.16, rv = 0.08;
        var steps = reduced ? 12 : 28;
        for (var s = 0; s < steps; s++) {
          var nA = new Float32Array(N), nB = new Float32Array(N);
          for (y = 1; y < H - 1; y++) {
            for (x = 1; x < W - 1; x++) {
              var idx = y * W + x;
              var lapA = A[idx - 1] + A[idx + 1] + A[idx - W] + A[idx + W] - 4 * A[idx];
              var lapB = B[idx - 1] + B[idx + 1] + B[idx - W] + B[idx + W] - 4 * B[idx];
              var a = A[idx], b = B[idx], abb = a * b * b;
              nA[idx] = a + ru * lapA - abb + F * (1 - a);
              nB[idx] = b + rv * lapB + abb - (K + F) * b;
            }
          }
          A = nA;
          B = nB;
        }
        var cv = document.createElement("canvas");
        cv.width = W;
        cv.height = H;
        var g = cv.getContext("2d");
        var img = g.createImageData(W, H);
        for (i = 0; i < N; i++) {
          var v = Math.max(0, Math.min(1, B[i] * 1.8));
          img.data[i * 4] = 255;
          img.data[i * 4 + 1] = 255;
          img.data[i * 4 + 2] = 255;
          img.data[i * 4 + 3] = v * 90 | 0;
        }
        g.putImageData(img, 0, 0);
        if (markCache.size > 48) markCache.clear();
        markCache.set(seed, cv);
        return cv;
      }
      var tails = /* @__PURE__ */ Object.create(null);
      function fishId(f) {
        return f && (f.fid || f.id || f.nick || 0);
      }
      function stepTail(id, x, y, dir, size, dt) {
        var t = tails[id];
        var segs = 6;
        var rest = size * 0.15;
        if (!t) {
          t = tails[id] = { p: [] };
          for (var i = 0; i < segs; i++) t.p.push({ x: x - dir * i * rest, y });
        }
        var k2 = 1 - Math.exp(-16 * dt);
        t.p[0].x += (x - dir * size * 0.18 - t.p[0].x) * k2;
        t.p[0].y += (y - t.p[0].y) * k2;
        for (var j = 1; j < t.p.length; j++) {
          var prev = t.p[j - 1];
          var cur = t.p[j];
          var dx = cur.x - prev.x, dy = cur.y - prev.y;
          var d = Math.sqrt(dx * dx + dy * dy) || 1e-3;
          var want = rest;
          cur.x = prev.x + dx / d * want;
          cur.y = prev.y + dy / d * want;
          var ang = Math.atan2(cur.y - prev.y, cur.x - prev.x);
          var restAng = dir < 0 ? 0 : Math.PI;
          var diff = ang - restAng;
          while (diff > Math.PI) diff -= PI2;
          while (diff < -Math.PI) diff += PI2;
          var maxA = 0.55;
          if (diff > maxA || diff < -maxA) {
            var a2 = restAng + clamp(diff, -maxA, maxA);
            cur.x = prev.x + Math.cos(a2) * want;
            cur.y = prev.y + Math.sin(a2) * want;
          }
        }
        return t.p;
      }
      var folkPrev = [];
      var folkCur = [];
      var folkOff = /* @__PURE__ */ Object.create(null);
      var lastFolkT = 0;
      var overlay = null, octx = null, grain = null, grainT = 0;
      var bloomA = null, bloomB = null, bloomTick = 0;
      var ovW = 0, ovH = 0, ovDpr = 0;
      function ensureOverlay() {
        if (overlay && overlay.parentNode) return;
        overlay = document.getElementById("techfx");
        if (!overlay) {
          overlay = document.createElement("canvas");
          overlay.id = "techfx";
          overlay.setAttribute("aria-hidden", "true");
          document.body.appendChild(overlay);
        }
        octx = overlay.getContext("2d");
        ovW = ovH = ovDpr = 0;
        grain = document.createElement("canvas");
        grain.width = 96;
        grain.height = 96;
        var g = grain.getContext("2d");
        var img = g.createImageData(96, 96);
        for (var y = 0; y < 96; y++) {
          for (var x = 0; x < 96; x++) {
            var n = ign(x, y) * 255 | 0;
            var i = (y * 96 + x) * 4;
            img.data[i] = n;
            img.data[i + 1] = n;
            img.data[i + 2] = n;
            img.data[i + 3] = 20;
          }
        }
        g.putImageData(img, 0, 0);
        bloomA = document.createElement("canvas");
        bloomB = document.createElement("canvas");
      }
      function resizeOverlay() {
        ensureOverlay();
        var dpr = Math.min(window.__finsGlass && window.__finsGlass.dpr || 1.5, window.devicePixelRatio || 1);
        var w = window.innerWidth, h = window.innerHeight;
        if (w === ovW && h === ovH && dpr === ovDpr) return;
        ovW = w;
        ovH = h;
        ovDpr = dpr;
        overlay.width = Math.round(w * dpr);
        overlay.height = Math.round(h * dpr);
        overlay.style.width = w + "px";
        overlay.style.height = h + "px";
        octx.setTransform(dpr, 0, 0, dpr, 0, 0);
      }
      var preFrame = [];
      (function installPreFrame() {
        if (window.__finsPreFrame) {
          preFrame = window.__finsPreFrame;
          return;
        }
        window.__finsPreFrame = preFrame;
        var orig = window.requestAnimationFrame;
        if (typeof orig !== "function") return;
        var lastT = -1;
        window.requestAnimationFrame = function(cb) {
          return orig.call(window, function(t) {
            if (t !== lastT) {
              lastT = t;
              for (var i = 0; i < preFrame.length; i++) {
                try {
                  preFrame[i](t);
                } catch (e) {
                }
              }
            }
            return cb(t);
          });
        };
      })();
      var bloomReady = false;
      function sampleBloom(t) {
        if (reduced || !bloomA) return;
        if (window.finsPace && t != null && !finsPace.shouldPaint(t)) return;
        var tank = document.getElementById("tank");
        if (!tank || !tank.width) return;
        bloomTick++;
        if (bloomTick % 2) return;
        var sc = sceneName();
        if (sc === "work") return;
        var bw = Math.max(96, tank.width / 5 | 0);
        var bh = Math.max(54, tank.height / 5 | 0);
        if (bloomA.width !== bw || bloomA.height !== bh) {
          bloomA.width = bw;
          bloomA.height = bh;
          bloomB.width = bw;
          bloomB.height = bh;
        }
        var a = bloomA.getContext("2d");
        var b = bloomB.getContext("2d");
        a.drawImage(tank, 0, 0, bw, bh);
        b.filter = "brightness(1.55) contrast(1.7) saturate(1.15) blur(7px)";
        b.clearRect(0, 0, bw, bh);
        b.drawImage(bloomA, 0, 0);
        b.filter = "none";
        bloomReady = true;
      }
      preFrame.push(sampleBloom);
      function runBloom(w, h) {
        if (!bloomReady || !octx) return;
        octx.save();
        octx.globalCompositeOperation = "lighter";
        octx.globalAlpha = 0.32;
        octx.drawImage(bloomB, 0, 0, w, h);
        octx.globalAlpha = 0.12;
        octx.filter = "blur(18px)";
        octx.drawImage(bloomB, -w * 0.04, 0, w * 1.08, h);
        octx.filter = "none";
        octx.restore();
      }
      function drawOverlay(now) {
        if (reduced) return;
        resizeOverlay();
        var w = window.innerWidth, h = window.innerHeight;
        octx.clearRect(0, 0, w, h);
        if (sceneName() === "work") return;
        var quiet = sceneName() === "title";
        try {
          runBloom(w, h);
        } catch (e) {
        }
        grainT += 1;
        octx.save();
        octx.globalCompositeOperation = "overlay";
        octx.globalAlpha = quiet ? 0.025 : 0.045;
        var ox = grainT * 1.7 % 96, oy = grainT * 1.1 % 96;
        var pat = octx.createPattern(grain, "repeat");
        octx.translate(-ox, -oy);
        octx.fillStyle = pat;
        octx.fillRect(ox, oy, w, h);
        octx.restore();
        octx.save();
        var vig = octx.createRadialGradient(w * 0.5, h * 0.42, h * 0.18, w * 0.5, h * 0.5, h * 0.82);
        vig.addColorStop(0, "rgba(0,0,0,0)");
        vig.addColorStop(1, "rgba(4,10,18," + (quiet ? "0.14" : "0.22") + ")");
        octx.fillStyle = vig;
        octx.fillRect(0, 0, w, h);
        octx.restore();
        var sc = sceneName();
        if (sc === "tank" && !quiet) {
          var t = now / 1e3;
          fillCaustics(octx, 0, h * 0.08, w, h * 0.92, t, 0.09);
          octx.save();
          octx.globalCompositeOperation = "lighter";
          octx.strokeStyle = "rgba(200,235,255,.22)";
          octx.lineWidth = 2.2;
          octx.beginPath();
          var top = h * 0.09;
          octx.moveTo(0, top);
          for (var x = 0; x <= w; x += 8) {
            octx.lineTo(x, top + gerstnerY(x, t, 0.2));
          }
          octx.stroke();
          var ray = octx.createLinearGradient(w * 0.5, 0, w * 0.55, h * 0.7);
          ray.addColorStop(0, "rgba(180,220,255,.08)");
          ray.addColorStop(1, "rgba(180,220,255,0)");
          octx.fillStyle = ray;
          octx.beginPath();
          octx.moveTo(w * 0.36, 0);
          octx.lineTo(w * 0.64, 0);
          octx.lineTo(w * 0.74, h);
          octx.lineTo(w * 0.26, h);
          octx.fill();
          octx.restore();
          drawWaves(octx, null);
        }
      }
      var drone = null;
      function audioCtx() {
        try {
          if (window.oe && typeof oe.init === "function") oe.init();
          return window.oe && oe.ctx || null;
        } catch (e) {
          return null;
        }
      }
      function ensureDrone() {
        if (drone || reduced) return;
        var ctx = audioCtx();
        if (!ctx) return;
        try {
          var buf = ctx.createBuffer(1, ctx.sampleRate * 2, ctx.sampleRate);
          var data = buf.getChannelData(0);
          for (var i = 0; i < data.length; i++) data[i] = Math.random() * 2 - 1;
          var src = ctx.createBufferSource();
          src.buffer = buf;
          src.loop = true;
          var bp = ctx.createBiquadFilter();
          bp.type = "bandpass";
          bp.frequency.value = 180;
          bp.Q.value = 0.8;
          var lp = ctx.createBiquadFilter();
          lp.type = "lowpass";
          lp.frequency.value = 420;
          var g = ctx.createGain();
          g.gain.value = 0;
          var pan = null;
          try {
            pan = ctx.createStereoPanner();
            pan.pan.value = 0;
          } catch (e2) {
          }
          var comp = ctx.createDynamicsCompressor();
          comp.threshold.value = -24;
          comp.knee.value = 18;
          comp.ratio.value = 3;
          comp.attack.value = 0.01;
          comp.release.value = 0.18;
          var fdnIn = ctx.createGain();
          fdnIn.gain.value = 0.22;
          var fdnOut = ctx.createGain();
          fdnOut.gain.value = 0.16;
          var times = [0.0297, 0.0371, 0.0411, 0.0437];
          var delays = [], fbg = [];
          for (var d = 0; d < 4; d++) {
            var del = ctx.createDelay(0.08);
            del.delayTime.value = times[d];
            var fg = ctx.createGain();
            fg.gain.value = 0.12;
            delays.push(del);
            fbg.push(fg);
            fdnIn.connect(del);
            del.connect(fg);
            fg.connect(fdnOut);
          }
          for (var a = 0; a < 4; a++) {
            fbg[a].connect(delays[a + 1 & 3]);
            fbg[a].connect(delays[a + 2 & 3]);
          }
          src.connect(bp);
          bp.connect(lp);
          lp.connect(g);
          g.connect(fdnIn);
          g.connect(comp);
          fdnOut.connect(comp);
          if (pan) {
            comp.connect(pan);
          }
          var dest = window.oe && oe.sfxBus || window.ge && ge.bus || ctx.destination;
          (pan || comp).connect(dest);
          src.start();
          drone = { src, g, bp, ctx, pan };
        } catch (e) {
          T.lastErr = String(e && e.message || e);
        }
      }
      function tickDrone() {
        if (!drone) return;
        var q = o2q();
        var target = 0.01 + (1 - q) * 0.028;
        var clog = false;
        try {
          clog = !!(window.G && window.G.filter && window.G.filter.clog);
        } catch (e) {
        }
        if (clog) target += 0.01;
        if (isQuiet()) target *= 0.35;
        var g = drone.g.gain;
        var now = drone.ctx.currentTime;
        g.setTargetAtTime(target, now, 0.4);
        drone.bp.frequency.setTargetAtTime(140 + q * 120, now, 0.5);
        if (drone.pan) {
          try {
            drone.pan.pan.setTargetAtTime(clamp(lastPtr.x * 2 - 1, -0.7, 0.7) * 0.35, now, 0.25);
          } catch (e) {
          }
        }
      }
      function nudgeFish(dt, t) {
        var L = window.G;
        if (!L || !Array.isArray(L.fish)) return;
        if (L.gunScare && (L.t || 0) < L.gunScare) return;
        if (sceneName() !== "tank") return;
        var n = L.fish.length;
        T.fish = n;
        var k2 = 12 * dt;
        for (var i = 0; i < n; i++) {
          var f = L.fish[i];
          if (!f || f.dead) continue;
          curl(f.x || 0, f.y || 0, t);
          if (typeof f.vx === "number") f.vx += _cx * k2;
          if (typeof f.vy === "number") f.vy += _cy * k2 * 0.55;
          for (var j = i + 1; j < n && j < i + 6; j++) {
            var o = L.fish[j];
            if (!o || o.dead) continue;
            var dx = (f.x || 0) - (o.x || 0);
            var dy = (f.y || 0) - (o.y || 0);
            var d2 = dx * dx + dy * dy;
            if (d2 > 4 && d2 < 900) {
              var push = 8 * dt / d2;
              if (typeof f.vx === "number") {
                f.vx += dx * push;
                o.vx -= dx * push;
              }
              if (typeof f.vy === "number") {
                f.vy += dy * push * 0.6;
                o.vy -= dy * push * 0.6;
              }
            }
          }
        }
      }
      function clipTank(ctx, tk, i) {
        var pts = window.__shopTankPts;
        var q = pts && pts[i] && pts[i].q;
        ctx.beginPath();
        if (q && q.length >= 8) {
          ctx.moveTo(q[0], q[1]);
          ctx.lineTo(q[2], q[3]);
          ctx.lineTo(q[4], q[5]);
          ctx.lineTo(q[6], q[7]);
          ctx.closePath();
        } else if (ctx.roundRect) ctx.roundRect(tk[0] + 2, tk[1] + tk[3] * 0.08, tk[2] - 4, tk[3] * 0.78, 3);
        else ctx.rect(tk[0] + 2, tk[1] + tk[3] * 0.08, tk[2] - 4, tk[3] * 0.78);
        ctx.clip();
      }
      function paintTankTech(ctx, t, tanks, wx, floorY, live) {
        wx = wx || {};
        var o2 = o2q();
        var mu = 0.1 + (wx.clog ? 0.14 : 0) + (1 - o2) * 0.12;
        if (!(mu === mu)) mu = 0.12;
        if (live) mu *= 0.35;
        var aMid = (mu * 0.45).toFixed(3);
        var aBot = (mu * 0.85).toFixed(3);
        for (var i = 0; i < tanks.length; i++) {
          try {
            var tk = tanks[i];
            var tx = tk[0], ty = tk[1], tw = tk[2], th = tk[3];
            if (tw < 8 || th < 8) continue;
            ctx.save();
            clipTank(ctx, tk, i);
            if (mu > 0.02) {
              var depth = ctx.createLinearGradient(tx, ty + th * 0.1, tx, ty + th * 0.86);
              depth.addColorStop(0, "rgba(0,0,0,0)");
              depth.addColorStop(0.45, "rgba(0,28,46," + aMid + ")");
              depth.addColorStop(1, "rgba(0,18,32," + aBot + ")");
              ctx.fillStyle = depth;
              ctx.fillRect(tx, ty, tw, th);
            }
            fillCaustics(ctx, tx, ty + th * 0.18, tw, th * 0.64, t + i, wx.clog ? 0.1 : live ? 0.16 : 0.24);
            var sy = ty + th * 0.105;
            ctx.strokeStyle = "rgba(210,240,255,.42)";
            ctx.lineWidth = 1.8;
            ctx.beginPath();
            ctx.moveTo(tx, sy);
            for (var px = 0; px <= tw; px += 4) {
              ctx.lineTo(tx + px, sy + gerstnerY(px, t, i * 0.7));
            }
            ctx.stroke();
            ctx.save();
            ctx.globalCompositeOperation = "lighter";
            var snell = ctx.createRadialGradient(tx + tw * 0.5, sy + 4, 2, tx + tw * 0.5, sy + 8, tw * 0.38);
            snell.addColorStop(0, "rgba(220,245,255,.22)");
            snell.addColorStop(1, "rgba(220,245,255,0)");
            ctx.fillStyle = snell;
            ctx.beginPath();
            ctx.ellipse(tx + tw * 0.5, sy + 6, tw * 0.36, 7, 0, 0, PI2);
            ctx.fill();
            ctx.restore();
            if (!reduced) {
              ctx.fillStyle = "rgba(40,70,50,.16)";
              for (var s = 0; s < 6; s++) {
                var ux = 0.12 + (i * 13 + s * 7) % 17 / 22;
                var uy = 0.55 + (i * 5 + s * 3) % 9 / 28;
                var wr = worley(ux * 6 + i, uy * 6 + t * 0.02);
                if (wr < 0.45) {
                  ctx.globalAlpha = (0.45 - wr) * 0.5;
                  ctx.beginPath();
                  ctx.ellipse(tx + ux * tw, ty + uy * th, 3 + (1 - wr) * 4, 2, 0, 0, PI2);
                  ctx.fill();
                }
              }
              ctx.globalAlpha = 1;
            }
            drawSph(ctx, tk, i);
            drawWaves(ctx, [tx + 2, ty + th * 0.08, tw - 4, th * 0.78]);
            ctx.restore();
            glassOnTank(ctx, tk, t, i);
            if (floorY) {
              ctx.save();
              ctx.globalCompositeOperation = "lighter";
              var cx = tx + tw * 0.5 + Math.sin(t * 0.8 + i) * 6;
              var cy = floorY + 8;
              var glow = ctx.createRadialGradient(cx, cy, 2, cx, cy, tw * 0.45);
              glow.addColorStop(0, "rgba(90,180,220," + (0.12 + 0.06 * Math.sin(t * 1.4 + i)).toFixed(3) + ")");
              glow.addColorStop(1, "rgba(90,180,220,0)");
              ctx.fillStyle = glow;
              ctx.beginPath();
              ctx.ellipse(cx, cy, tw * 0.42, 10, 0, 0, PI2);
              ctx.fill();
              ctx.globalCompositeOperation = "multiply";
              ctx.globalAlpha = 0.22;
              ctx.fillStyle = "rgba(8,16,24,1)";
              ctx.beginPath();
              ctx.ellipse(tx + tw * 0.5, floorY + 14, tw * 0.48, 9, 0, 0, PI2);
              ctx.fill();
              ctx.restore();
            }
          } catch (err) {
            T.lastErr = String(err && err.message || err);
            try {
              ctx.restore();
            } catch (e2) {
            }
          }
        }
      }
      function glassOnTank(ctx, tk, t, i) {
        var tx = tk[0], ty = tk[1], tw = tk[2], th = tk[3];
        ctx.save();
        clipTank(ctx, tk, i);
        var gL = ctx.createLinearGradient(tx, ty, tx + tw * 0.28, ty);
        gL.addColorStop(0, "rgba(255,255,255,.12)");
        gL.addColorStop(1, "rgba(255,255,255,0)");
        ctx.fillStyle = gL;
        ctx.fillRect(tx, ty, tw * 0.28, th);
        var gR = ctx.createLinearGradient(tx + tw, ty, tx + tw * 0.72, ty);
        gR.addColorStop(0, "rgba(10,30,50,.18)");
        gR.addColorStop(1, "rgba(10,30,50,0)");
        ctx.fillStyle = gR;
        ctx.fillRect(tx + tw * 0.72, ty, tw * 0.28, th);
        var top = ctx.createLinearGradient(tx, ty + th * 0.08, tx, ty + th * 0.22);
        top.addColorStop(0, "rgba(230,248,255,.09)");
        top.addColorStop(1, "rgba(230,248,255,0)");
        ctx.fillStyle = top;
        ctx.fillRect(tx, ty, tw, th * 0.22);
        ctx.globalCompositeOperation = "lighter";
        ctx.strokeStyle = "rgba(255,252,245,.24)";
        ctx.lineWidth = 1.2;
        ctx.beginPath();
        ctx.moveTo(tx + tw * 0.12, ty + th * 0.12);
        ctx.quadraticCurveTo(tx + tw * 0.38, ty + th * 0.06, tx + tw * 0.62, ty + th * 0.13);
        ctx.stroke();
        if (!reduced) {
          ctx.globalCompositeOperation = "source-over";
          for (var d = 0; d < 7; d++) {
            var dx = tx + tw * (0.12 + (i * 11 + d * 5) % 17 / 22);
            var dy = ty + th * (0.14 + (i * 3 + d) % 5 * 0.018);
            var rr2 = 1.1 + d % 3 * 0.5;
            ctx.globalAlpha = 0.18 + 0.08 * Math.sin(t * 0.7 + d);
            ctx.strokeStyle = "rgba(220,240,255,.7)";
            ctx.lineWidth = 0.8;
            ctx.beginPath();
            ctx.ellipse(dx, dy, rr2, rr2 * 1.4, 0.2, 0, PI2);
            ctx.stroke();
            ctx.globalAlpha = 0.12;
            ctx.fillStyle = "rgba(255,255,255,.8)";
            ctx.beginPath();
            ctx.ellipse(dx - rr2 * 0.3, dy - rr2 * 0.4, rr2 * 0.35, rr2 * 0.45, 0, 0, PI2);
            ctx.fill();
          }
        }
        ctx.restore();
        ctx.save();
        ctx.strokeStyle = "rgba(210,235,255,.2)";
        ctx.lineWidth = 1.4;
        ctx.beginPath();
        ctx.moveTo(tx + 3, ty + th * 0.1);
        ctx.lineTo(tx + 3, ty + th * 0.84);
        ctx.moveTo(tx + tw - 3, ty + th * 0.1);
        ctx.lineTo(tx + tw - 3, ty + th * 0.84);
        ctx.stroke();
        ctx.restore();
      }
      var fishPrev = /* @__PURE__ */ Object.create(null);
      wrap("drawFishSprite", function(orig, self, args) {
        var a = args[0] || {};
        if (a.x != null && a.y != null && a.fish) {
          var id = fishId(a.fish) || a.x;
          var pr = fishPrev[id];
          if (pr) {
            a.x = pr.x + (a.x - pr.x) * 0.38;
            a.y = pr.y + (a.y - pr.y) * 0.38;
            args[0] = a;
          }
          fishPrev[id] = { x: a.x, y: a.y };
        }
        var a = args[0] || {};
        var r = orig.apply(self, args);
        if (!r || !a.ctx || reduced) return r;
        try {
          fishDrawn++;
          var ctx = a.ctx, f = a.fish || {}, size = a.size || 12;
          var dir = f.dir < 0 ? -1 : 1;
          var seed = (f.fid || f.sp || 1) * 17 + (f.gen || 0) | 0;
          ctx.save();
          ctx.translate(a.x, a.y);
          ctx.scale(dir, 1);
          var mark2 = bakeMark(seed);
          ctx.globalAlpha = 0.08;
          ctx.globalCompositeOperation = "overlay";
          ctx.drawImage(mark2, -size * 0.7, -size * 0.4, size * 1.3, size * 0.8);
          ctx.globalCompositeOperation = "lighter";
          ctx.globalAlpha = 0.14;
          var rim = ctx.createRadialGradient(-size * 0.15, -size * 0.2, 0, 0, 0, size * 0.7);
          rim.addColorStop(0, "rgba(180,230,255,.7)");
          rim.addColorStop(1, "rgba(180,230,255,0)");
          ctx.fillStyle = rim;
          ctx.beginPath();
          ctx.ellipse(0, 0, size * 0.7, size * 0.38, 0, 0, 7);
          ctx.fill();
          var tDotL = 0.55;
          var spec = Math.pow(Math.sqrt(Math.max(0, 1 - tDotL * tDotL)), 12);
          ctx.globalAlpha = 0.28 + spec * 0.45;
          ctx.strokeStyle = "rgba(255,252,240,1)";
          ctx.lineWidth = Math.max(0.8, size * 0.06);
          ctx.lineCap = "round";
          ctx.beginPath();
          ctx.moveTo(-size * 0.15, -size * 0.18);
          ctx.quadraticCurveTo(size * 0.1, -size * 0.28, size * 0.42, -size * 0.08);
          ctx.stroke();
          ctx.restore();
          var pts = stepTail(fishId(f) || seed, a.x, a.y, dir, size, frameDt);
          ctx.save();
          ctx.strokeStyle = "rgba(20,40,60,.28)";
          ctx.lineWidth = Math.max(1.2, size * 0.12);
          ctx.lineCap = "round";
          ctx.beginPath();
          ctx.moveTo(pts[0].x, pts[0].y);
          for (var i = 1; i < pts.length; i++) ctx.lineTo(pts[i].x, pts[i].y);
          ctx.stroke();
          ctx.restore();
        } catch (e) {
        }
        return r;
      });
      wrap("shopInteriorFx", function(orig, self, args) {
        var r = orig.apply(self, args);
        try {
          var ctx = args[0], t = args[1] || 0, tanks = args[3], floorY = args[4];
          if (!ctx || !tanks || !tanks.length) return r;
          lastTanks = tanks;
          lastFloorY = floorY || 0;
          lastWx = args[2] || {};
          ensureKelp(tanks);
          ensureSph(tanks);
          var liveShop = !!(window.shopBg && window.shopBg.complete && window.shopBg.naturalWidth);
          paintTankTech(ctx, t, tanks, lastWx, lastFloorY, liveShop);
          drawKelp(ctx, tanks);
        } catch (e) {
          T.lastErr = String(e && e.message || e);
        }
        return r;
      });
      wrap("folkDraw", function(orig, self, args) {
        var ctx = args[0], x = args[1], y = args[2], size = args[3], look = args[9];
        var now = typeof performance !== "undefined" ? performance.now() : Date.now();
        if (now - lastFolkT > 32) {
          folkPrev = folkCur;
          folkCur = [];
          lastFolkT = now;
        }
        var ox = 0, oy = 0;
        var rad = Math.max(10, (size || 20) * 0.38);
        var src = folkPrev.length ? folkPrev : folkCur;
        for (var i = 0; i < src.length; i++) {
          var o = src[i];
          var dx = x - o.x, dy = y - o.y;
          var d2 = dx * dx + dy * dy;
          var min = rad + o.r;
          if (d2 > 0.01 && d2 < min * min) {
            var d = Math.sqrt(d2);
            var push = (min - d) * 0.5;
            ox += dx / d * push;
            oy += dy / d * push * 0.32;
          }
        }
        var id = look && look.id != null ? look.id : (x | 0) + ":" + (y | 0);
        var sm = folkOff[id] || (folkOff[id] = { x: 0, y: 0 });
        sm.x = sm.x * 0.7 + ox * 0.3;
        sm.y = sm.y * 0.7 + oy * 0.3;
        args[1] = x + sm.x;
        args[2] = y + sm.y;
        folkCur.push({ x: args[1], y: args[2], r: rad, id });
        T.folk = folkCur.length;
        return orig.apply(self, args);
      });
      wrap("drawShopBackdrop", function(orig, self, args) {
        var r = orig.apply(self, args);
        try {
          if (reduced || !args[0]) return r;
          var ctx = args[0], x = args[1], y = args[2], w = args[3], h = args[4], t = args[5] || 0;
          ctx.save();
          ctx.globalCompositeOperation = "lighter";
          var pulse = 0.5 + 0.5 * Math.sin(t * 1.7);
          var bloom = ctx.createRadialGradient(x + w * 0.5, y + h * 0.16, 4, x + w * 0.5, y + h * 0.16, w * 0.38);
          bloom.addColorStop(0, "rgba(255,200,120," + (0.08 + pulse * 0.04).toFixed(3) + ")");
          bloom.addColorStop(1, "rgba(255,180,80,0)");
          ctx.fillStyle = bloom;
          ctx.fillRect(x, y, w, h * 0.55);
          ctx.restore();
        } catch (e) {
        }
        return r;
      });
      wrap("paintTownMap", function(orig, self, args) {
        var r = orig.apply(self, args);
        try {
          if (reduced || !args[0]) return r;
          var ctx = args[0], w = args[1], h = args[2];
          var fog = ctx.createLinearGradient(0, 0, 0, h);
          fog.addColorStop(0, "rgba(140,175,210,.08)");
          fog.addColorStop(0.45, "rgba(80,120,160,.04)");
          fog.addColorStop(1, "rgba(20,40,60,.16)");
          ctx.fillStyle = fog;
          ctx.fillRect(0, 0, w, h);
          var t = (typeof performance !== "undefined" ? performance.now() : Date.now()) / 1e3;
          ctx.save();
          ctx.globalAlpha = 0.07;
          ctx.fillStyle = "#0a1824";
          var cx = w * (0.35 + 0.2 * fbm(t * 0.04, 0.2));
          ctx.beginPath();
          ctx.ellipse(cx, h * 0.3, w * 0.34, h * 0.12, 0, 0, PI2);
          ctx.fill();
          ctx.restore();
        } catch (e) {
        }
        return r;
      });
      var last = 0;
      function frame(now) {
        requestAnimationFrame(frame);
        var dt = last ? Math.min(0.05, (now - last) / 1e3) : 0.016;
        last = now;
        frameDt = dt;
        acc += dt;
        if (acc > 0.2) acc = 0.2;
        var t = now / 1e3;
        while (acc >= STEP) {
          simT += STEP;
          for (var i = vortices.length - 1; i >= 0; i--) {
            vortices[i].life -= STEP * 0.55;
            if (vortices[i].life <= 0) vortices.splice(i, 1);
          }
          try {
            stepKelp(STEP, simT);
          } catch (e) {
          }
          try {
            stepSph(STEP, simT);
          } catch (e) {
          }
          try {
            stepWaves(STEP);
          } catch (e) {
          }
          acc -= STEP;
        }
        T.fish = fishDrawn;
        fishDrawn = 0;
        try {
          nudgeFish(dt, t);
        } catch (e) {
        }
        try {
          tickDrone();
        } catch (e) {
        }
        try {
          if (!window.finsPace || finsPace.shouldPaint(now)) drawOverlay(now);
        } catch (e) {
        }
      }
      requestAnimationFrame(frame);
      function onPtr(e) {
        if (reduced) return;
        var tank = document.getElementById("tank");
        if (!tank) return;
        var r = tank.getBoundingClientRect();
        var x = e.clientX - r.left;
        var y = e.clientY - r.top;
        lastPtr.x = r.width ? x / r.width : 0.5;
        lastPtr.y = r.height ? y / r.height : 0.5;
        addVortex(x, y, e.type === "pointerdown" ? 2.2 : 0.7);
        if (e.type === "pointerdown") addWave(x, y, 1.2);
      }
      document.addEventListener("pointerdown", function(e) {
        onPtr(e);
        ensureDrone();
      }, { passive: true });
      document.addEventListener("pointermove", function(e) {
        if (e.buttons) onPtr(e);
        else {
          var tank = document.getElementById("tank");
          if (!tank) return;
          var r = tank.getBoundingClientRect();
          lastPtr.x = r.width ? (e.clientX - r.left) / r.width : 0.5;
          lastPtr.y = r.height ? (e.clientY - r.top) / r.height : 0.5;
        }
      }, { passive: true });
      T.curl = curl;
      T.vortex = addVortex;
      T.wave = addWave;
      T.stats = function() {
        return {
          v: T.v,
          kelp: T.kelp,
          folk: T.folk,
          fish: T.fish,
          bubbles: sph.length,
          waves: waves.length,
          vortices: vortices.length,
          wraps: Object.keys(origs),
          scene: sceneName(),
          ok: T.ok,
          err: T.lastErr
        };
      };
      window.__techTest = T.stats;
    })();
  } catch (e) {
    console.error("Layer tech.js failed to start:", e);
  }

  // src/layers/life.js
  try {
    (function() {
      "use strict";
      var browse = /* @__PURE__ */ Object.create(null);
      var lastWalkins = -1;
      var lastBought = -1;
      var lastStirAt = 0;
      var lastToastAt = 0;
      var lastWhoBuy = -1;
      var repeats = /* @__PURE__ */ Object.create(null);
      var regulars = [];
      var placeBusy = /* @__PURE__ */ Object.create(null);
      var crowd = [];
      var boughtAt = /* @__PURE__ */ Object.create(null);
      var reduced = false;
      try {
        reduced = !!(window.matchMedia && window.matchMedia("(prefers-reduced-motion: reduce)").matches);
      } catch (e) {
      }
      var LOOK = ["Pretty.", "How much?", "That one.", "Look at the tail.", "Is it healthy?", "For the window.", "The gold one.", "Does it stay small?"];
      var NICE = ["Nice shop.", "I'll take one.", "My kid would love that.", "You've done this up.", "Wrap that one.", "This is the one."];
      var CLOG = ["The water's off.", "Smells like the filter.", "I'd wait.", "Maybe after you clean it."];
      var WET = ["The floor's wet.", "I'm not standing in that.", "You ought to mop.", "I'll come back dry."];
      var PANIC = ["What was that?", "I'm leaving.", "Did you hear that?", "Out. Now."];
      var POOR = ["Just looking.", "Not today.", "Maybe later.", "After payday."];
      var WORK = ["On my lunch.", "After the boats.", "Between jobs.", "Quick look."];
      var PAY = ["I'll take it.", "Keep the change.", "And a fish bag?", "For the front tank."];
      var CHEER = ["You're good people.", "Come by the market.", "That was kind."];
      var DANCE = ["Alright!", "This place.", "Ha!"];
      var SICK = ["That one looks off.", "Is it sick?", "I'd skip that tank."];
      var KID = ["Can we get one?", "The little one!", "Look, look."];
      var LEAVE = ["Thanks.", "See you.", "I'll be back."];
      var YEAR = ["Thousand years of this harbor.", "Year of the shop.", "Same street as always.", "Harbor's older than the sign."];
      var HOPED = ["betta", "guppy", "tetra", "goldfish", "angelfish", "cory"];
      var ASK_HAVE = ["That $.", "The $.", "I'll take the $.", "How much for the $?"];
      var ASK_MISS = ["Got a $?", "You don't keep $?", "I wanted a $.", "Maybe a $."];
      var misses = [];
      var dayBook = { y: 1e3, sales: 0, misses: 0, opened: 0, closed: 0, lastHour: -1, lastLock: -1, dayN: -1 };
      var didFish = false;
      function now() {
        return typeof performance !== "undefined" ? performance.now() / 1e3 : Date.now() / 1e3;
      }
      function markEgg(id, line) {
        try {
          if (typeof findEgg === "function") findEgg(id, line);
        } catch (e) {
        }
      }
      function bumpStat(k2, n) {
        try {
          var g = gs();
          if (g) {
            g.stats = g.stats || {};
            g.stats[k2] = (g.stats[k2] || 0) + (n == null ? 1 : n);
          }
        } catch (e) {
        }
      }
      function watchEggs() {
        try {
          if (worldYear() > 1e3) markEgg("yearturn", "The harbor went past the thousand.");
        } catch (e) {
        }
        try {
          var wx = "";
          if (typeof wxWord === "function") wx = String(wxWord() || "").toLowerCase();
          else if (window.wxState) wx = String(wxState.word || wxState.key || "").toLowerCase();
          var sc = "";
          try {
            if (typeof sceneNow === "function") sc = sceneNow() || "";
            if (!sc && typeof gameState === "object" && gameState && gameState.scene) sc = String(gameState.scene);
            if (!sc) {
              var on = document.querySelector("#scenes .sc.on");
              if (on && on.dataset.scene) sc = on.dataset.scene;
            }
          } catch (e2) {
          }
          if ((/rain|storm|snow|sleet/.test(wx) || window.wxState && (wxState.rain > 0.2 || wxState.snow > 0.2)) && (sc === "shop" || sc === "front")) {
            markEgg("glasswx", "Weather on the glass. The street looked in.");
          }
        } catch (e3) {
        }
      }
      function pick(arr, seed) {
        if (!arr || !arr.length) return "";
        return arr[(seed >>> 0) % arr.length];
      }
      function gs() {
        try {
          if (typeof gameState === "function") return gameState();
          if (typeof gameState === "object" && gameState && gameState.stats) return gameState;
        } catch (e) {
        }
        return null;
      }
      function fishAll() {
        try {
          if (typeof allFish === "function") return allFish() || [];
        } catch (e) {
        }
        try {
          var g = gs();
          if (g && Array.isArray(g.fish)) return g.fish;
        } catch (e2) {
        }
        return [];
      }
      function worldYear() {
        try {
          if (window.saga && typeof saga.year === "function") return saga.year();
          if (typeof atlGen === "function") {
            var a = atlGen();
            if (a && a.now) return a.now | 0;
          }
        } catch (e) {
        }
        return 1e3;
      }
      function kindName(f) {
        try {
          if (typeof O !== "undefined" && f && O[f.sp]) return String(O[f.sp].name || O[f.sp].gname || "").toLowerCase();
        } catch (e) {
        }
        try {
          if (typeof SPECIES !== "undefined" && f && SPECIES[f.sp]) return String(SPECIES[f.sp].name || SPECIES[f.sp].gname || "").toLowerCase();
        } catch (e2) {
        }
        return "";
      }
      function sellable(f) {
        if (!f) return false;
        if (f.stage != null && f.stage !== 2) return false;
        if (f.sick || f.ill) return false;
        if (f.risen) return false;
        try {
          if (f.mind && f.mind.mood) return false;
          if (f.mind && f.mind.stress > 0.72) return false;
        } catch (e) {
        }
        return true;
      }
      function stockDupes() {
        var counts = /* @__PURE__ */ Object.create(null);
        try {
          var list = typeof allFish === "function" ? allFish() || [] : [];
          for (var i = 0; i < list.length; i++) {
            var f = list[i];
            if (!sellable(f)) continue;
            var k2 = kindName(f);
            if (!k2) continue;
            counts[k2] = (counts[k2] || 0) + 1;
          }
        } catch (e) {
        }
        var have = [];
        for (var k2 in counts) if (counts[k2] >= 2) have.push(k2);
        return have;
      }
      function wantFor(idx, st) {
        if (st.want) return st.want;
        var have = stockDupes();
        var T = town();
        var seed = (T && T.who && T.who[idx] || idx) >>> 0;
        if (have.length) {
          st.want = have[seed % have.length];
          st.haveWant = true;
        } else {
          st.want = HOPED[seed % HOPED.length];
          st.haveWant = false;
        }
        return st.want;
      }
      function recordMiss(idx, st, why) {
        var nm = whoName(idx) || "Someone";
        var want = st && st.want || "fish";
        misses.push({ name: nm, want, why: why || "left", y: worldYear() });
        if (misses.length > 16) misses.shift();
        dayBook.misses += 1;
        try {
          bumpStat("walked", 1);
          if (why === "stock") markEgg("tillwalk", (nm || "Someone") + " came for a " + want + " we did not have.");
        } catch (eStat) {
        }
        if (Math.random() > 0.4) return;
        try {
          var line = nm + " came for a " + want + ". " + (why === "clog" ? "The water put them off." : why === "stock" ? "We didn't have one." : "They left without a fish bag.");
          if (window.saga && typeof chronicle === "function") chronicle("town", "Year " + worldYear() + ". " + line, []);
          else if (typeof chronicle === "function") chronicle("town", "Year " + worldYear() + ". " + line, []);
        } catch (e) {
        }
      }
      function watchDay() {
        var h = hour();
        var d = 0;
        try {
          if (typeof _ === "function") d = _() | 0;
        } catch (e) {
        }
        if (d !== dayBook.dayN) {
          dayBook.dayN = d;
          dayBook.sales = 0;
          dayBook.misses = 0;
          dayBook.y = worldYear();
        }
        if (dayBook.lastHour >= 0) {
          if (dayBook.lastHour < 8 && h >= 8 && h < 10) {
            dayBook.opened += 1;
            sayKeep("Sign's over.", 2.4, 10);
          }
          if (dayBook.lastHour < 19.35 && h >= 19.4) {
            if (now() - (dayBook.lastLock || 0) > 50) {
              dayBook.lastLock = now();
              dayBook.closed += 1;
              var line = "Locked up. " + dayBook.sales + " sale" + (dayBook.sales === 1 ? "" : "s") + (dayBook.misses ? ", " + dayBook.misses + " walked." : ".");
              sayKeep("That's us.", 2.8, 12);
              markEgg("lockup", line);
              try {
                if (typeof k === "function") k(line, "prog");
              } catch (e2) {
              }
              try {
                if (typeof chronicle === "function") chronicle("note", "Year " + worldYear() + ". " + line, []);
              } catch (e3) {
              }
            }
          }
        }
        dayBook.lastHour = h;
      }
      function hour() {
        try {
          if (typeof jt === "function") return jt() * 24;
          var g = gs();
          if (g && isFinite(g.t)) return (g.t % 2400 + 2400) % 2400 / 100;
        } catch (e) {
        }
        return 12;
      }
      function isLunch() {
        var h = hour();
        return h > 11.4 && h < 13.8;
      }
      function isOpen() {
        var h = hour();
        return h >= 8 && h < 19.5;
      }
      function town() {
        try {
          if (typeof townSim === "function") return townSim();
          if (typeof townSim === "object" && townSim && townSim.who) return townSim;
        } catch (e) {
        }
        return null;
      }
      function jobsOf() {
        try {
          if (typeof JOBS === "function") return JOBS();
          if (Array.isArray(JOBS)) return JOBS;
        } catch (e) {
        }
        return null;
      }
      function whoName(i) {
        try {
          var T = town();
          if (!T || !T.who || i < 0 || i >= (T.n || 0)) return "";
          return nameOfWho(T.who[i]);
        } catch (e) {
        }
        return "";
      }
      function nameOfWho(who) {
        try {
          if (typeof sgPerson === "function") {
            var p = sgPerson(who);
            if (p && p.name && p.name !== "A customer") return p.name;
          }
        } catch (e) {
        }
        try {
          if (typeof personName === "function") {
            var n = personName(who);
            if (n && n !== "A customer") return n;
          }
        } catch (e2) {
        }
        return "";
      }
      function jobId(i) {
        try {
          var T = town();
          var jobs = jobsOf();
          if (!T || !T.job || !jobs) return "";
          var j = jobs[T.job[i]];
          return j && (j.id || j.n) || "";
        } catch (e) {
        }
        return "";
      }
      function payOdds(idx, st) {
        var p = 0.16;
        try {
          var T = town();
          if (T && T.mood) {
            if (T.mood[idx] > 0.55) p += 0.12;
            if (T.mood[idx] < 0.34) p -= 0.1;
          }
        } catch (e) {
        }
        try {
          if (typeof townState === "function") p += (townState().word || 0) * 0.05;
        } catch (e2) {
        }
        try {
          if (window.weave && weave.of) {
            var w = weave.of();
            p += (w.craft || 0) * 0.18;
            var rum = w.rumors || [];
            for (var r = 0; r < rum.length; r++) if (rum[r].k === "risen" && rum[r].heat > 0.7) p -= 0.18;
            if (/war|embargo|plague/i.test(w.lastAtlas || "")) p -= 0.08;
          }
        } catch (e3) {
        }
        try {
          if (window.hookRun) p += Math.min(0.12, (hookRun().combo || 0) * 0.03);
        } catch (e4) {
        }
        try {
          if (window.realm && typeof realm.pressure === "function") p -= realm.pressure();
        } catch (e5) {
        }
        try {
          if (window.wild && st && st.want && typeof wild.scarce === "function" && wild.scarce(st.want)) p -= 0.12;
        } catch (e6) {
        }
        try {
          if (window.road && typeof road.cut === "function" && road.cut()) p -= 0.08;
        } catch (e7) {
        }
        try {
          if (window.beast && typeof beast.near === "function") p -= beast.near();
        } catch (e8) {
        }
        try {
          if (window.faith && typeof faith.bless === "function") p += faith.bless();
        } catch (e9) {
        }
        try {
          if (window.rumor && typeof rumor.heat === "function") p -= Math.min(0.05, rumor.heat() * 0.06);
        } catch (e10) {
        }
        try {
          if (window.guild && typeof guild.edge === "function") p += guild.edge();
        } catch (e11) {
        }
        try {
          if (window.fever && typeof fever.edge === "function") p -= fever.edge();
        } catch (e12) {
        }
        try {
          if (window.craft && typeof craft.edge === "function") p += craft.edge();
        } catch (e13) {
        }
        var EDGES = [
          "age",
          "hold",
          "wonder",
          "envoy",
          "vow",
          "siege",
          "pact",
          "lux",
          "lord",
          "great",
          "spy",
          "seat",
          "house",
          "wed",
          "heir",
          "plot",
          "hush",
          "bond",
          "claim",
          "fray",
          "feast",
          "ward",
          "gyve",
          "fief"
        ];
        for (var ei = 0; ei < EDGES.length; ei++) {
          try {
            var mod = window[EDGES[ei]];
            if (mod && typeof mod.edge === "function") p += mod.edge();
          } catch (eEd) {
          }
        }
        if (clogged2()) p -= 0.16;
        if (aisleWet() && !(st && st.coming)) return 0;
        if (st && st.coming) p = Math.max(p, 0.85);
        if (p < 0.03) p = 0.03;
        if (p > 0.7) p = 0.7;
        return p;
      }
      function aisleWet() {
        try {
          if (window.shopSite && shopSite.wet() > 0.36) return true;
        } catch (e) {
        }
        return false;
      }
      function clogged2() {
        try {
          return !!window.clogged;
        } catch (e) {
        }
        return false;
      }
      function anySick() {
        try {
          return fishAll().some(function(f) {
            return f && (f.sick || f.cond);
          });
        } catch (e) {
        }
        return false;
      }
      function panicking() {
        try {
          if (window.__gunFlash > 0.08 || window.__gunSiren) return true;
          if (window.__shopMood && window.__shopMood.kind === "flee" && now() < window.__shopMood.until) return true;
          var g = gs();
          return !!(window.__shopPanic && g && (g.t || 0) < window.__shopPanic);
        } catch (e) {
        }
        return false;
      }
      function shopMood() {
        var m = window.__shopMood;
        if (m && now() < m.until) return m.kind;
        return "";
      }
      function didBuy(i) {
        if (boughtAt[i]) return true;
        try {
          var T = town();
          if (T && T.didBuy && T.didBuy[i]) return true;
        } catch (e) {
        }
        return false;
      }
      function lineFor(i, phase, st) {
        var T = town();
        var seed = (T && T.who && T.who[i] || i) >>> 0;
        var mood = T && T.mood ? T.mood[i] : 0.5;
        var kit = "";
        try {
          kit = T && T.who && (T.who[i] >>> 0) % 11 === 0 ? "kid" : "";
        } catch (e) {
        }
        if (panicking() || phase === "flee") return pick(PANIC, seed + 3);
        var sm = shopMood();
        if (sm === "cheer") return pick(CHEER, seed);
        if (sm === "dance") return pick(DANCE, seed + 2);
        if (aisleWet()) return pick(WET, seed + 7);
        if (clogged2()) return pick(CLOG, seed + 5);
        if (anySick() && (phase === "look" || phase === "walk") && seed % 3 === 0) return pick(SICK, seed);
        var want = st && st.want || browse[i] && browse[i].want || "";
        if (phase === "pay" || phase === "buy") {
          if (want) return pick(ASK_HAVE, seed + 9).replace("$", want);
          return pick(PAY, seed + 9);
        }
        if (phase === "leave") return pick(LEAVE, seed + 4);
        if (kit === "kid") return pick(KID, seed);
        if (phase === "look") {
          if (want) {
            var tpl = st && st.haveWant || browse[i] && browse[i].haveWant ? ASK_HAVE : ASK_MISS;
            return pick(tpl, seed + 2).replace("$", want);
          }
          if (mood < 0.34) return pick(POOR, seed);
          if (seed % 8 === 0) return pick(YEAR, seed + 11);
          if (/fisher|hand|netter|ship|cooper|diver/.test(jobId(i))) return pick(WORK, seed);
          return pick(LOOK, seed + 1);
        }
        if (mood < 0.36) return pick(POOR, seed);
        return pick(NICE, seed);
      }
      function flavorLine(line, seed) {
        line = String(line || "");
        try {
          if (panicking()) return line;
          if (window.beast && beast.near && beast.near() > 0.12 && seed % 4 === 0) {
            var bl = beast.line && beast.line();
            if (bl) return bl;
          }
          if (window.faith && faith.line && seed % 6 === 0) {
            var fl = faith.line();
            if (fl) return fl;
          }
          if (window.rumor && rumor.whisper && seed % 8 === 0) {
            var rl = rumor.whisper();
            if (rl) return rl;
          }
          if (window.guild && guild.line && seed % 5 === 0) {
            var gl = guild.line();
            if (gl) return gl;
          }
          if (window.fever && fever.line && seed % 7 === 0) {
            var vl = fever.line();
            if (vl) return vl;
          }
          if (window.craft && craft.line && seed % 10 === 0) {
            var cl = craft.line();
            if (cl) return cl;
          }
          if (window.tongue && tongue.say) return tongue.say(seed, line);
        } catch (e) {
        }
        return line;
      }
      var _lineForRaw = lineFor;
      lineFor = function(i, phase, st) {
        var T = town();
        var seed = (T && T.who && T.who[i] || i) >>> 0;
        return flavorLine(_lineForRaw(i, phase, st), seed);
      };
      function tankSpot(tanks, which, W, floorY, personS, slot) {
        if (!tanks || !tanks.length) {
          return { x: W * (0.34 + slot % 3 * 0.05), y: floorY + personS * 0.012, face: 1 };
        }
        var tk = tanks[which % tanks.length];
        var x = Math.max(W * 0.28, Math.min(W * 0.5, (tk[0] || W * 0.62) - W * 0.18 - slot % 3 * W * 0.025));
        var y = floorY + personS * (0.01 + slot % 2 * 8e-3);
        var face = tk[0] + tk[2] * 0.5 > x ? 1 : -1;
        return { x, y, face };
      }
      function doorSpot(W, floorY, personS, slot) {
        return {
          x: W * (0.11 + slot % 3 * 0.018),
          y: floorY + personS * 0.012,
          face: -1
        };
      }
      function registerSpot(W, floorY, personS, slot) {
        return {
          x: W * (0.22 + slot % 3 * 0.028),
          y: floorY + personS * (0.01 + slot % 2 * 8e-3),
          face: -1
        };
      }
      function aisleSpot(W, floorY, personS, slot, simT, idx) {
        var x = W * (0.32 + slot % 4 * 0.06 + 0.01 * Math.sin((simT || 0) * 0.28 + idx));
        try {
          if (window.shopSite && shopSite.wet() > 0.22) {
            var pud = shopSite.puddle();
            var px = (pud && pud.nx ? pud.nx : 0.48) * W;
            if (Math.abs(x - px) < W * 0.1) x = px < W * 0.5 ? px + W * 0.12 : px - W * 0.12;
          }
        } catch (e) {
        }
        return {
          x,
          y: floorY + personS * 0.012 + personS * 4e-3 * (slot % 2),
          face: 1
        };
      }
      window.shopBrowse = function(idx, slot, W, floorY, personS, simT) {
        var tanks = window.__shopTanks || [];
        if (tanks.length < 3 && W) {
          var spots = [
            [0.62, 0.3],
            [0.76, 0.29],
            [0.84, 0.42],
            [0.68, 0.46],
            [0.8, 0.56]
          ];
          tanks = spots.map(function(sp, i) {
            var live = (window.__shopTanks || [])[i];
            if (live) return live;
            return [W * sp[0], 0, W * 0.11, personS * 0.4];
          });
        }
        var t = now();
        if (slot === 0) crowd = [];
        var st = browse[idx];
        var pair = slot > 0 && slot % 2 === 1 ? browse[idx - 1] : null;
        if (!st) {
          st = browse[idx] = {
            tank: (idx + slot) % Math.max(1, tanks.length || 1),
            until: t + 0.7 + idx % 4 * 0.25,
            phase: "enter",
            line: "",
            lineUntil: 0,
            bought: !!didBuy(idx)
          };
          wantFor(idx, st);
        }
        if (didBuy(idx)) st.bought = true;
        if (tanks.length) st.tank = st.tank % tanks.length;
        var sm = shopMood();
        if (panicking() || sm === "flee") {
          st.phase = "flee";
          if (!st.line || t > st.lineUntil) {
            st.line = lineFor(idx, "flee");
            st.lineUntil = t + 2.2;
          }
        } else if (sm === "cheer" && st.phase !== "leave") {
          st.phase = "cheer";
          st.until = Math.max(st.until, t + 1.6);
        } else if (sm === "dance" && st.phase !== "leave" && st.phase !== "pay") {
          st.phase = "dance";
          st.until = Math.max(st.until, t + 1.2);
        } else if (t > st.until) {
          var prevPhase = st.phase;
          var next = st.phase;
          if (st.phase === "enter") next = "walk";
          else if (st.phase === "walk") next = "look";
          else if (st.phase === "look") {
            if (aisleWet() && !st.coming) next = "leave";
            else if (st.bought || Math.random() < payOdds(idx, st)) next = "pay";
            else if (Math.random() < (clogged2() ? 0.5 : 0.22)) next = "leave";
            else next = slot % 2 ? "walk" : "look";
          } else if (st.phase === "pay") next = "leave";
          else if (st.phase === "cheer" || st.phase === "dance") next = "look";
          else if (st.phase === "leave") next = "leave";
          st.phase = next;
          st.tank = (st.tank + 1 + idx % 2) % Math.max(1, tanks.length || 1);
          if (prevPhase === "look" && next === "leave" && !st.bought) {
            recordMiss(idx, st, aisleWet() ? "wet" : clogged2() ? "clog" : st.haveWant ? "pass" : "stock");
          }
          var dwell = next === "look" ? 3.4 + Math.random() * 4.2 + (isLunch() ? 1.4 : 0) : next === "pay" ? 2.2 + Math.random() * 1.6 : next === "leave" ? 2.8 : 1.3 + Math.random() * 1.6;
          if (clogged2() && next === "look") dwell *= 0.55;
          st.until = t + dwell;
          if ((next === "look" || next === "pay" || next === "flee" || next === "cheer") && Math.random() < 0.78) {
            st.line = lineFor(idx, next, st);
            st.lineUntil = t + (next === "look" ? 3.1 : 2.2);
            try {
              if (window.feel && feel.play && Math.random() < 0.4) feel.play("murmur");
            } catch (e) {
            }
          } else if (next !== "look" && next !== "pay") {
            st.line = "";
          }
        }
        var pos;
        if (st.phase === "enter" || st.phase === "flee" || st.phase === "leave") pos = doorSpot(W, floorY, personS, slot);
        else if (st.phase === "pay" || st.phase === "cheer") pos = registerSpot(W, floorY, personS, slot);
        else if (st.phase === "look" || st.phase === "dance") {
          if (pair && pair.tank != null) st.tank = pair.tank;
          pos = tankSpot(tanks, st.tank, W, floorY, personS, slot);
        } else pos = aisleSpot(W, floorY, personS, slot, simT, idx);
        if (st.phase === "dance") {
          pos.x += Math.sin(t * 6 + idx) * personS * 0.04;
          pos.y += Math.abs(Math.sin(t * 8 + idx)) * personS * 0.012;
        }
        var face = pos.face;
        if (st.phase === "look" && tanks.length) {
          var tk = tanks[st.tank % tanks.length];
          face = tk[0] > pos.x ? 1 : -1;
        }
        if (st.phase === "pay" || st.phase === "cheer") face = -1;
        if (st.phase === "flee" || st.phase === "leave") face = -1;
        if (st.phase === "enter") face = 1;
        var rec = {
          x: pos.x,
          y: pos.y,
          face,
          line: t < st.lineUntil ? st.line : "",
          name: st.guestName || whoName(idx) || "",
          job: jobId(idx),
          looking: st.phase === "look" || st.phase === "pay" || st.phase === "cheer",
          flee: st.phase === "flee",
          pay: st.phase === "pay",
          pace: st.phase === "flee" ? 0.92 : st.phase === "enter" || st.phase === "leave" ? 0.5 : st.phase === "look" || st.phase === "pay" ? 0.3 : 0.4,
          phase: st.phase,
          idx,
          slot,
          want: st.want || ""
        };
        crowd[slot] = rec;
        crowd.length = Math.max(crowd.length, slot + 1);
        window.__shopCrowd = crowd;
        return rec;
      };
      window.shopHelp = function(role, idx, W, floorY, personS, simT) {
        var y = floorY + personS * 0.012;
        if (panicking()) return { x: W * 0.16, y, face: -1 };
        var people = window.__shopCrowd || [];
        var looking = [];
        for (var i = 0; i < people.length; i++) {
          if (people[i] && (people[i].looking || people[i].pay)) looking.push(people[i]);
        }
        if (role === "counter") {
          var payer = looking.filter(function(c2) {
            return c2.pay;
          })[0];
          if (payer) return { x: W * 0.2, y, face: payer.x > W * 0.22 ? 1 : -1 };
          return { x: W * 0.205, y, face: 1 };
        }
        if (looking.length) {
          var c = looking[idx % looking.length];
          var hx = Math.max(W * 0.26, Math.min(W * 0.48, c.x - personS * 0.38));
          try {
            if (window.shopSite && shopSite.wet() > 0.22) {
              var pud = shopSite.puddle();
              var px = (pud && pud.nx ? pud.nx : 0.48) * W;
              if (Math.abs(hx - px) < W * 0.1) hx = px < W * 0.5 ? px + W * 0.12 : px - W * 0.12;
            }
          } catch (eL) {
          }
          return { x: hx, y: c.y, face: c.x > hx ? 1 : -1 };
        }
        var h = hour();
        var sway = Math.sin((simT || tNow()) * 0.13 + idx) * W * 0.02;
        var pos;
        if (role === "feeder") pos = { x: W * 0.46 + sway, y, face: 1 };
        else if (role === "cleaner") pos = { x: W * (clogged2() ? 0.44 : 0.38) + sway, y, face: 1 };
        else pos = { x: Math.max(W * 0.28, Math.min(W * 0.48, W * 0.34 + idx * W * 0.05 + sway)), y, face: 1 };
        try {
          if (window.shopSite && shopSite.wet() > 0.22) {
            var pud = shopSite.puddle();
            var px = (pud && pud.nx ? pud.nx : 0.48) * W;
            if (Math.abs(pos.x - px) < W * 0.11) pos.x = px < W * 0.5 ? px + W * 0.13 : px - W * 0.13;
          }
        } catch (eW) {
        }
        return pos;
      };
      var keepLine = "";
      var keepUntil = 0;
      var keepQuiet = 0;
      function wxNow() {
        try {
          if (typeof wxWord === "function") return String(wxWord() || "").toLowerCase();
        } catch (e) {
        }
        try {
          var el = document.getElementById("caldate");
          var tx = (el && el.textContent || "").toLowerCase();
          if (/rain/.test(tx)) return "rain";
          if (/snow/.test(tx)) return "snow";
          if (/storm|gale/.test(tx)) return "storm";
          if (/fog/.test(tx)) return "fog";
          if (/clear/.test(tx)) return "clear";
        } catch (e2) {
        }
        return "";
      }
      function anyReallySick() {
        try {
          return fishAll().some(function(f) {
            return f && (f.sick || f.ill);
          });
        } catch (e) {
        }
        return false;
      }
      function sayKeep(line, hold2, quiet) {
        var t = now();
        keepLine = line;
        keepUntil = t + (hold2 || 2.6);
        keepQuiet = t + (quiet || 8);
        return line;
      }
      function keeperTalk() {
        var t = now();
        if (t < keepUntil) return keepLine;
        var crowd2 = window.__shopCrowd || [];
        var payer = false, nLook = 0;
        for (var i = 0; i < crowd2.length; i++) {
          if (!crowd2[i]) continue;
          if (crowd2[i].pay) payer = true;
          if (crowd2[i].looking) nLook++;
        }
        if (payer) {
          var want = "";
          for (var c = 0; c < crowd2.length; c++) if (crowd2[c] && crowd2[c].pay && crowd2[c].want) want = crowd2[c].want;
          if (want) return sayKeep("The " + want + ".", 2.4, 6);
          return sayKeep(pick(["I'll bag it.", "He's a good one.", "Paper or water?", "Come back."], t * 11 | 0), 2.4, 6);
        }
        if (t < keepQuiet) return "";
        if (anyReallySick()) return "";
        if (panicking()) return sayKeep("Out. Now.", 2.2, 9);
        var wx = wxNow();
        var h = hour();
        var line = "";
        if (clogged2()) line = pick(["Filter's packing.", "Give me a minute on the filter."], t * 7 | 0);
        else if (nLook) line = pick(["They're healthy.", "Take your time.", "That one at the front.", "Need anything?"], t * 9 | 0);
        else if (wx === "rain") line = pick(["Wet out.", "Mats by the door."], t | 0);
        else if (wx === "snow") line = "Boots off the gravel.";
        else if (wx === "storm") line = "Stay as long as you like.";
        else if (h < 9) line = pick(["Morning.", "Lights on."], t | 0);
        else if (h > 18) line = pick(["Closing soon.", "Last hour."], t | 0);
        else line = pick(["Water's holding.", "The tetras are up.", "Need anything?"], t * 3 | 0);
        return sayKeep(line, 2.6, 7 + (t * 13 | 0) % 8);
      }
      function drawSay(ctx, x, y, line) {
        if (!ctx || !line) return;
        ctx.save();
        ctx.font = "600 12px Nunito, sans-serif";
        ctx.textAlign = "center";
        ctx.textBaseline = "middle";
        var w = Math.min(186, ctx.measureText(line).width + 16);
        var h = 22;
        var bx = x - w / 2, by = y - h - 8;
        ctx.fillStyle = "rgba(250,246,236,.95)";
        ctx.strokeStyle = "rgba(20,16,10,.18)";
        ctx.lineWidth = 1;
        var r = 8;
        ctx.beginPath();
        ctx.moveTo(bx + r, by);
        ctx.arcTo(bx + w, by, bx + w, by + h, r);
        ctx.arcTo(bx + w, by + h, bx, by + h, r);
        ctx.arcTo(bx, by + h, bx, by, r);
        ctx.arcTo(bx, by, bx + w, by, r);
        ctx.closePath();
        ctx.fill();
        ctx.stroke();
        ctx.beginPath();
        ctx.moveTo(x - 5, by + h);
        ctx.lineTo(x + 5, by + h);
        ctx.lineTo(x, by + h + 7);
        ctx.closePath();
        ctx.fill();
        ctx.fillStyle = "#1a1a1a";
        ctx.fillText(line, x, by + h / 2 + 0.5);
        ctx.restore();
      }
      function wrapFolk() {
        if (!window.folkDraw || window.folkDraw.__lifeSay) return;
        var orig = window.folkDraw;
        window.folkDraw = function(ctx, x, y, size, colorHex, walk, showFace, face, moving, look) {
          try {
            if (look && look.id) {
              var id = String(look.id);
              if (id.indexOf("fin:") === 0 || id === "fin") {
                var crowd2 = window.__shopCrowd || [];
                var payer = null, looking = 0;
                for (var i = 0; i < crowd2.length; i++) {
                  if (!crowd2[i]) continue;
                  if (crowd2[i].pay) payer = crowd2[i];
                  if (crowd2[i].looking) looking++;
                }
                if (payer) {
                  look.greet = 1;
                  arguments[7] = payer.x > x ? 1 : -1;
                } else if (looking) look.greet = 1;
                else if (hour() * 10 % 10 > 6) look.work = 1;
              }
            }
          } catch (e) {
          }
          var r = orig.apply(this, arguments);
          try {
            if (look && look.id && String(look.id).indexOf("fin:") === 0) {
              var line = keeperTalk();
              if (line) drawSay(ctx, arguments[1], arguments[2] - (size || 40) * 0.92, line);
            }
          } catch (e2) {
          }
          return r;
        };
        window.folkDraw.__lifeSay = 1;
      }
      function tNow() {
        return now();
      }
      function schoolCenter(fish, name) {
        try {
          var list = fishAll();
          if (!list.length) return null;
          var cx = 0, cy = 0, n = 0;
          for (var i = 0; i < list.length; i++) {
            var o = list[i];
            if (!o || o === fish) continue;
            var sp = o.sp || o._sp || {};
            var nm = String(sp.name || sp.n || "").toLowerCase();
            if (!nm || nm.indexOf(name.slice(0, 4)) < 0) continue;
            if (o.x == null) continue;
            cx += o.x;
            cy += o.y || 0;
            n++;
            if (n >= 8) break;
          }
          if (n < 1) return null;
          return { x: cx / n, y: cy / n, n };
        } catch (e) {
          return null;
        }
      }
      function deepenFish(orig, ctx, args) {
        if (!args || !args.fish) return orig(args);
        var f = args.fish;
        var sp = args.sp || {};
        var name = String(sp.name || sp.n || "").toLowerCase();
        var size = args.size || 12;
        var fearBoost = 0;
        try {
          if (window.__shopMood && window.__shopMood.kind === "flee" && now() < window.__shopMood.until) fearBoost = 0.18;
          else if (window.__gunFlash > 0.04) fearBoost = 0.22;
        } catch (e) {
        }
        if (/tetra|danio|rasbora|minnow|neon|cardinal/.test(name)) {
          f.phase = (f.phase || 0) + 0.045 + fearBoost;
          var sch = schoolCenter(f, name);
          if (sch && args.x != null) {
            args = Object.assign({}, args, {
              x: args.x * 0.82 + sch.x * 0.18,
              y: args.y * 0.9 + sch.y * 0.1
            });
          }
        } else if (/betta|fighter/.test(name)) {
          f.phase = (f.phase || 0) - 8e-3;
          var flare = 1;
          try {
            if (fishAll().length > 1) flare = 1.08 + 0.06 * Math.sin((f.phase || 0) * 0.7);
          } catch (e) {
          }
          args = Object.assign({}, args, { size: size * flare });
        } else if (/cory|loach|pleco|sucker|oto/.test(name) && args.y != null) {
          args = Object.assign({}, args, { y: args.y + Math.abs(size) * 0.16, size: size * 0.92 });
          f.phase = (f.phase || 0) + 0.012;
        } else if (/angel|discus/.test(name)) {
          args = Object.assign({}, args, { size: size * 1.1 });
        } else if (/\bkoi\b|carp|goldfish|oranda|comet/.test(name)) {
          f.phase = (f.phase || 0) + 0.018;
          if (args.y != null) args = Object.assign({}, args, { y: args.y + Math.sin((f.phase || 0) * 0.5) * size * 0.04 });
        } else if (/cichlid|oscar|ram|krib|jack/.test(name)) {
          if (f._homeX == null && args.x != null) f._homeX = args.x;
          if (f._homeX != null && args.x != null) {
            args = Object.assign({}, args, { x: args.x * 0.88 + f._homeX * 0.12, size: size * 1.04 });
          }
        } else if (/guppy|endler|platy|molly|sword/.test(name)) {
          f.phase = (f.phase || 0) + 0.05 + fearBoost;
        } else if (/clown|damsel|tang|wrasse|marine/.test(name)) {
          f.phase = (f.phase || 0) + 0.03;
        }
        try {
          var nn = f.nn || f._g;
          var o = nn && nn.o;
          var hunger = f.hunger == null ? 1 : f.hunger;
          if (o && o.length) {
            var fear = o[3] || 0;
            if (fear > 0.5 || fearBoost) f.phase = (f.phase || 0) + 0.14 + fearBoost;
            if (hunger < 0.38) f.gulp = Math.max(f.gulp || 0, 0.1);
          } else if (hunger < 0.32) {
            f.gulp = Math.max(f.gulp || 0, 0.08);
          }
          if (fearBoost) f.gulp = Math.max(f.gulp || 0, 0.16);
        } catch (e) {
        }
        return orig(args);
      }
      function wrapFish() {
        if (didFish || typeof window.drawFishSprite !== "function") return;
        didFish = true;
        var orig = window.drawFishSprite;
        window.drawFishSprite = function(args) {
          try {
            return deepenFish(orig, args && args.ctx, args);
          } catch (e) {
            return orig(args);
          }
        };
      }
      function afterStir(opts, r) {
        opts = opts || {};
        r = r || {};
        var t = now();
        if (t - lastStirAt < 0.12) return;
        lastStirAt = t;
        var kind = opts.kind || "loud";
        try {
          if (window.feel && feel.play) {
            if (kind === "kind") feel.play("cheer");
            else if (kind === "gun") feel.play("shot");
            else if (kind === "harm") feel.play("thud");
            else if (kind === "dance") feel.play("whistle");
            else feel.play("whoosh");
          }
        } catch (e) {
        }
        try {
          var list = fishAll();
          for (var i = 0; i < list.length; i++) {
            var f = list[i];
            if (!f) continue;
            if (kind === "kind") f.gulp = 0.12;
            else f.gulp = 0.22 + Math.random() * 0.22;
            f.phase = (f.phase || 0) + (kind === "gun" ? 0.4 : 0.12);
          }
        } catch (e) {
        }
        try {
          if ((kind === "harm" || kind === "gun") && typeof Ne === "function") {
            var staff = Ne().h || [];
            for (var s = 0; s < staff.length; s++) if (staff[s] && staff[s].on) staff[s].loy = (staff[s].loy || 0) - (kind === "gun" ? 1 : 0.4);
          }
          if (kind === "kind" && typeof Ne === "function") {
            var stf = Ne().h || [];
            for (var u = 0; u < stf.length; u++) if (stf[u] && stf[u].on) stf[u].loy = Math.min(10, (stf[u].loy || 0) + 0.15);
          }
        } catch (e) {
        }
        try {
          var until = t + (kind === "gun" ? 9 : kind === "kind" ? 8 : 5);
          if (kind === "gun" || kind === "harm") window.__shopMood = { kind: "flee", until };
          else if (kind === "kind") window.__shopMood = { kind: "cheer", until };
          else if (kind === "dance") window.__shopMood = { kind: "dance", until: t + 6 };
          else window.__shopMood = { kind: "look", until: t + 3 };
          for (var b in browse) {
            if (!browse[b]) continue;
            if (kind === "gun" || kind === "harm") {
              browse[b].phase = "flee";
              browse[b].until = t + 2;
              browse[b].line = pick(PANIC, (b | 0) + 7);
              browse[b].lineUntil = t + 2.4;
            } else if (kind === "kind") {
              browse[b].phase = "cheer";
              browse[b].until = t + 3;
            } else if (kind === "dance") {
              browse[b].phase = "dance";
              browse[b].until = t + 2.5;
            }
          }
        } catch (e) {
        }
        if (kind === "kind" && r.heard) {
          try {
            if (typeof k === "function") k((r.names && r.names[0] ? r.names[0] + " smiled. " : "") + r.heard + " people on the block noticed.", "good");
          } catch (e) {
          }
        }
      }
      function wrapStir() {
        var orig = window.__qeStir;
        if (!orig || orig.__life) return;
        window.__qeStir = function(opts) {
          var r = orig.apply(this, arguments);
          try {
            afterStir(opts, r);
          } catch (e) {
          }
          return r;
        };
        window.__qeStir.__life = 1;
      }
      function wrapGun() {
        var orig = window.__qeGun;
        if (!orig || orig.__life) return;
        window.__qeGun = function() {
          var r = orig.apply(this, arguments);
          try {
            afterStir({ kind: "gun", flee: true }, typeof r === "object" && r ? r : {});
          } catch (e) {
          }
          return r;
        };
        window.__qeGun.__life = 1;
      }
      function watchGun() {
        if (!(window.__gunFlash > 0.12 || window.__gunSiren)) return;
        var t = now();
        var m = window.__shopMood;
        if (m && m.kind === "flee" && m.until > t + 1) return;
        window.__shopMood = { kind: "flee", until: t + 8 };
        for (var b in browse) {
          if (!browse[b]) continue;
          browse[b].phase = "flee";
          browse[b].until = t + 2.4;
          browse[b].line = pick(PANIC, (b | 0) + 7);
          browse[b].lineUntil = t + 2.6;
        }
      }
      function watchWalkins() {
        try {
          var T = town();
          if (!T || !T.stat) return;
          var w = T.stat.walkins || 0;
          var b = T.stat.bought || 0;
          if (lastWalkins < 0) {
            lastWalkins = w;
            lastBought = b;
            return;
          }
          if (w > lastWalkins) {
            lastWalkins = w;
            try {
              if (window.feel && feel.play) feel.play("door");
            } catch (e) {
            }
            try {
              if (clogged2() && typeof k === "function" && now() - lastToastAt > 14) {
                lastToastAt = now();
                k("Someone stepped in, looked at the water, and thought twice.", "");
              }
            } catch (e) {
            }
          } else lastWalkins = w;
          if (b > lastBought) {
            var nBuy = b - lastBought;
            lastBought = b;
            dayBook.sales += nBuy;
            try {
              if (window.feel && feel.play) feel.play("register");
              if (window.feel && feel.pulseHud) feel.pulseHud("r-coins");
            } catch (e) {
            }
            try {
              var wi = window.__walkIn;
              if (wi && wi.i != null) {
                boughtAt[wi.i] = now();
                if (browse[wi.i]) {
                  browse[wi.i].bought = true;
                  browse[wi.i].phase = "pay";
                  browse[wi.i].until = now() + 2.4;
                  browse[wi.i].line = lineFor(wi.i, "pay");
                  browse[wi.i].lineUntil = now() + 2.2;
                }
                var who = wi.who;
                if (who >= 0) {
                  repeats[who] = (repeats[who] || 0) + 1;
                  recordRegular(who, wi.spent);
                  if (repeats[who] > 1 && now() - lastToastAt > 10 && typeof k === "function" && wi.spent >= 8) {
                    lastToastAt = now();
                    var nm = nameOfWho(who);
                    k((nm && nm !== "A customer" ? nm : "A regular") + " came back.", "good");
                  }
                  lastWhoBuy = who;
                  if (wi.spent && (browse[wi.i] && browse[wi.i].want)) markEgg("wantpay", "They asked for the " + browse[wi.i].want + ".");
                }
              }
              if (nBuy > 1 && window.feel && feel.play) feel.play("chime");
              sayKeep(pick(["I'll bag it.", "He's a good one.", "Come back."], now() * 5 | 0), 2.4, 6);
            } catch (e) {
            }
          } else lastBought = b;
        } catch (e) {
        }
      }
      function wrapMap() {
        if (typeof window.paintTownMap !== "function" || window.paintTownMap.__life) return;
        var orig = window.paintTownMap;
        window.paintTownMap = function(ctx, w, h, H, shop, places, selected, colors, jobs) {
          var r = orig.apply(this, arguments);
          try {
            paintBusy(ctx, w, h, H, shop, places, jobs);
          } catch (e) {
          }
          return r;
        };
        window.paintTownMap.__life = 1;
      }
      function paintBusy(ctx, w, h, H, shop, places, jobs) {
        if (!ctx || !H || !H.n || !window.townCam) return;
        var cam = window.townCam;
        function toV(x, y) {
          return [(x - cam.left) / cam.span * w, (y - cam.top) / cam.span * h];
        }
        var counts = /* @__PURE__ */ Object.create(null);
        var sea = 0;
        var inShop = 0;
        var toShop = 0;
        var commute = 0;
        var n = H.n;
        var step = n > 900 ? 3 : 1;
        for (var i = 0; i < n; i += step) {
          var st = H.st[i];
          if (st === 8) {
            sea++;
            continue;
          }
          if (st === 6) {
            inShop++;
            continue;
          }
          if (st === 5) {
            toShop++;
            continue;
          }
          if (st === 1 || st === 3) commute++;
          if (st === 2 && jobs && jobs[H.job[i]] && jobs[H.job[i]].work) {
            var id = jobs[H.job[i]].work;
            counts[id] = (counts[id] || 0) + 1;
          } else if (st === 4) counts.haymarket = (counts.haymarket || 0) + 1;
        }
        placeBusy = counts;
        placeBusy._shop = inShop * step;
        placeBusy._to = toShop * step;
        placeBusy._sea = sea * step;
        ctx.save();
        ctx.font = "600 " + Math.max(8, Math.round(w * 0.016)) + "px Nunito, sans-serif";
        ctx.textAlign = "center";
        ctx.textBaseline = "top";
        if (places) {
          for (var p = 0; p < places.length; p++) {
            var pl = places[p];
            if (!pl || !counts[pl.id]) continue;
            var xy = toV(pl.x, pl.y);
            if (xy[0] < 12 || xy[0] > w - 12 || xy[1] < 18 || xy[1] > h - 36) continue;
            var label = counts[pl.id] + (pl.id === "haymarket" ? " at market" : " at work");
            ctx.fillStyle = "rgba(8,14,22,.55)";
            var tw = ctx.measureText(label).width + 8;
            ctx.fillRect(xy[0] - tw / 2, xy[1] + 4, tw, 11);
            ctx.fillStyle = "rgba(220,230,236,.88)";
            ctx.fillText(label, xy[0], xy[1] + 5);
          }
        }
        if (shop) {
          var sv = toV(shop.x, shop.y);
          var shopN = inShop * step;
          var toN = toShop * step;
          if (shopN > 0 || toN > 0) {
            var slab = shopN > 0 ? shopN + " in Fin's" : toN + " heading in";
            if (shopN > 0 && toN > 0) slab = shopN + " in · " + toN + " on the way";
            ctx.fillStyle = "rgba(8,14,22,.6)";
            var sw = ctx.measureText(slab).width + 10;
            ctx.fillRect(sv[0] - sw / 2, sv[1] + 10, sw, 12);
            ctx.fillStyle = "#f4c453";
            ctx.fillText(slab, sv[0], sv[1] + 11);
          }
        }
        if (sea > 8) {
          var wharf = null;
          if (places) {
            for (var q = 0; q < places.length; q++) if (places[q] && places[q].id === "battery") wharf = places[q];
          }
          if (wharf) {
            var wv = toV(wharf.x, wharf.y);
            ctx.fillStyle = "rgba(8,14,22,.55)";
            var lab = Math.round(sea * step) + " at sea";
            var ww = ctx.measureText(lab).width + 8;
            ctx.fillRect(wv[0] - ww / 2, wv[1] + 4, ww, 11);
            ctx.fillStyle = "#9ec4e8";
            ctx.fillText(lab, wv[0], wv[1] + 5);
          }
        }
        ctx.restore();
      }
      function recordRegular(who, spent) {
        var nm = nameOfWho(who);
        var job = "";
        try {
          var T = town();
          var jobs = jobsOf();
          if (T && T.job && T.who && jobs) {
            for (var i = 0; i < T.n; i++) {
              if (T.who[i] === who) {
                job = jobs[T.job[i]] && (jobs[T.job[i]].id || jobs[T.job[i]].n) || "";
                break;
              }
            }
          }
        } catch (e2) {
        }
        if (!nm || nm === "A customer") nm = "A regular from the street";
        var hit = null;
        for (var r = 0; r < regulars.length; r++) if (regulars[r].who === who) hit = regulars[r];
        if (!hit) {
          hit = { who, name: nm, job, visits: 0, spent: 0, note: job ? "works as a " + job : "comes in off Salem Street" };
          regulars.push(hit);
        }
        hit.visits += 1;
        hit.spent += +spent || 0;
        hit.name = nm;
        if (!hit.since) hit.since = worldYear();
        if (hit.visits > 2) hit.note = "a regular since year " + hit.since + ". Knows the tanks.";
        if (hit.visits > 1) markEgg("regular", nm + " came back.");
        if (regulars.length > 12) regulars.shift();
        window.__regulars = regulars;
        try {
          var g = gs();
          if (g) {
            g.stats = g.stats || {};
            var nReg = 0;
            for (var rr2 = 0; rr2 < regulars.length; rr2++) if (regulars[rr2].visits > 1) nReg++;
            g.stats.regulars = Math.max(g.stats.regulars || 0, nReg);
          }
        } catch (e4) {
        }
      }
      function seedStreetPeople() {
        if (regulars.length >= 3) {
          window.__regulars = regulars;
          return;
        }
        var T = town();
        if (T && T.n) {
          var n = Math.min(T.n, 600);
          var step = Math.max(1, n / 10 | 0);
          var jobs = jobsOf();
          for (var i = 0; i < n && regulars.length < 5; i += step) {
            var who = T.who && T.who[i];
            if (who == null) continue;
            var nm = nameOfWho(who);
            var job = "";
            try {
              if (jobs && T.job && jobs[T.job[i]]) {
                job = jobs[T.job[i]].id || jobs[T.job[i]].n || "";
              }
            } catch (e3) {
            }
            if (!nm || nm === "A customer") {
              if (job) nm = "A " + job + " off the street";
              else continue;
            }
            var exists = false;
            for (var r = 0; r < regulars.length; r++) if (regulars[r].name === nm) exists = true;
            if (exists) continue;
            regulars.push({
              who,
              name: nm,
              job,
              visits: 0,
              spent: 0,
              since: worldYear() - (2 + who % 9),
              note: job ? "lives nearby · " + job : "lives in the North End"
            });
          }
        }
        if (!regulars.length) {
          regulars.push({ name: "The baker on Salem", job: "baker", visits: 0, spent: 0, note: "walks past the window at six" });
          regulars.push({ name: "A longshoreman", job: "wharf", visits: 0, spent: 0, note: "comes in with salt on his coat" });
          regulars.push({ name: "Mrs. from the yellow house", job: "neighbor", visits: 0, spent: 0, note: "asks after the tetras" });
        }
        window.__regulars = regulars;
      }
      var lastHook = 0;
      function tick() {
        try {
          wrapFolk();
        } catch (e) {
        }
        try {
          wrapFish();
        } catch (e) {
        }
        try {
          wrapStir();
        } catch (e) {
        }
        try {
          wrapGun();
        } catch (e) {
        }
        try {
          wrapMap();
        } catch (e) {
        }
        try {
          watchGun();
        } catch (e) {
        }
        try {
          seedStreetPeople();
        } catch (e) {
        }
        try {
          watchDay();
        } catch (e) {
        }
        try {
          watchEggs();
        } catch (e) {
        }
        if (now() - lastHook > 0.55) {
          lastHook = now();
          try {
            watchWalkins();
          } catch (e) {
          }
        }
      }
      window.keepGuest = function(st) {
        if (!st) return false;
        return !!(st._lateMae || st._goingHold || st._lateKid || st._mask || st._hold || st._lord || st._great || st._envoy || st._vow || st._wonder || st._spy || st._seat || st._siege || st._houseAsk || st._wed || st._heir || st._plot || st._hush || st._feast || st._gyve || st._claim || st._fray);
      };
      window.shopLife = {
        browse: function() {
          return browse;
        },
        busy: function() {
          return placeBusy;
        },
        crowd: function() {
          return crowd;
        },
        regulars: function() {
          return regulars;
        },
        year: worldYear,
        day: function() {
          return dayBook;
        },
        misses: function() {
          return misses;
        },
        stock: stockDupes,
        hoped: function() {
          return HOPED;
        },
        coming: function() {
          try {
            if (window.desk && typeof desk.coming === "function") return desk.coming();
          } catch (e) {
          }
          return null;
        }
      };
      if (window.__onBeat) window.__onBeat(tick, 280);
      else setTimeout(function loop() {
        tick();
        setTimeout(loop, 280);
      }, 280);
    })();
  } catch (e) {
    console.error("Layer life.js failed to start:", e);
  }

  // src/layers/saga.js
  try {
    (function() {
      "use strict";
      var lastFids = null;
      var lastStates = /* @__PURE__ */ Object.create(null);
      var lastPair = /* @__PURE__ */ Object.create(null);
      var lastHook = 0;
      var lastUi = 0;
      var seeded = false;
      var usedNicks = /* @__PURE__ */ Object.create(null);
      var didBrowse = false;
      var ONSET = {
        tetra: ["Ne", "Ve", "I", "Ka", "Ri", "Sa", "Li", "To", "Mi", "Ae"],
        betta: ["Si", "Lu", "Ra", "The", "Mo", "Va", "Shi", "Or", "Pe", "Na"],
        cichlid: ["Khar", "Drun", "Gor", "Bal", "Thok", "Ulm", "Zas", "Rith"],
        guppy: ["Pip", "Zel", "Flo", "Nim", "Twi", "Ski", "Peb", "Glim"],
        gold: ["Ori", "Kin", "Sol", "Han", "Mar", "Gin", "Aure", "Com"],
        angel: ["Isa", "Cel", "Ser", "Ael", "Vin", "Pax", "Lir", "Noe"],
        koi: ["Ko", "Sui", "Hana", "Kyo", "Mai", "Ren", "Asa", "Yuki"],
        clown: ["Nemo", "Rif", "Cor", "Anem", "Teg", "Blu", "Cal", "Daz"],
        default: ["Ur", "Tos", "Id", "Kum", "Fes", "Lor", "At", "Mel", "Dok", "Ing"]
      };
      var CODA = ["rist", "id", "el", "a", "en", "or", "um", "eth", "il", "as", "on", "ek", "ia", "us"];
      function now() {
        return typeof performance !== "undefined" ? performance.now() / 1e3 : Date.now() / 1e3;
      }
      function fishList() {
        try {
          if (typeof allFish === "function") return allFish() || [];
        } catch (e) {
        }
        return [];
      }
      function runSeed() {
        try {
          var w = world2();
          if (w && w.seed) return w.seed;
          if (typeof runState === "function") {
            var r = runState();
            if (r && r.seed) return r.seed;
          }
        } catch (e) {
        }
        return 1;
      }
      function shopName() {
        try {
          if (typeof owState === "function") {
            var o = owState();
            if (o && o.shop) return o.shop;
          }
        } catch (e) {
        }
        var el = document.getElementById("shopname");
        if (el && el.textContent) return el.textContent.replace(/’s.*$/, "'s").trim() || "Fin's";
        return "Fin's";
      }
      function spName(f) {
        try {
          if (typeof pedOf === "function" && f && f.fid) {
            var rec = pedOf(f.fid);
            if (rec && (rec.gname || rec.vname)) return rec.gname || rec.vname;
          }
        } catch (e) {
        }
        try {
          if (typeof O !== "undefined" && f && O[f.sp]) return O[f.sp].name || O[f.sp].gname || "fish";
        } catch (e2) {
        }
        return "fish";
      }
      function spKey(f) {
        try {
          if (typeof pedOf === "function" && f && f.fid) {
            var rec = pedOf(f.fid);
            if (rec && (rec.gname || rec.vname)) return String(rec.gname || rec.vname).toLowerCase();
          }
        } catch (e) {
        }
        return "";
      }
      function family(f) {
        var n = spKey(f);
        if (/tetra|danio|rasbora|minnow|neon|cardinal/.test(n)) return "tetra";
        if (/betta|fighter/.test(n)) return "betta";
        if (/cichlid|oscar|ram|krib/.test(n)) return "cichlid";
        if (/guppy|endler|platy|molly|sword/.test(n)) return "guppy";
        if (/goldfish|comet|oranda/.test(n)) return "gold";
        if (/angel/.test(n)) return "angel";
        if (/\bkoi\b|carp/.test(n)) return "koi";
        if (/clown|damsel|tang|wrasse/.test(n)) return "clown";
        return "default";
      }
      function hash32(s) {
        var h = 2166136261 >>> 0;
        s = String(s);
        for (var i = 0; i < s.length; i++) {
          h ^= s.charCodeAt(i);
          h = Math.imul(h, 16777619) >>> 0;
        }
        return h >>> 0;
      }
      function pick(arr, h) {
        return arr[(h >>> 0) % arr.length];
      }
      function she(f) {
        return f && f.sex === "m" ? "He" : "She";
      }
      function her(f) {
        return f && f.sex === "m" ? "his" : "her";
      }
      function him(f) {
        return f && f.sex === "m" ? "him" : "her";
      }
      function nickFor(f) {
        var fam = family(f);
        var on = ONSET[fam] || ONSET.default;
        var seed = runSeed();
        var h = hash32((f.fid || f.sp || 0) + ":" + seed + ":" + (f.key || "") + ":" + fam);
        var a = pick(on, h);
        var b = pick(CODA, h >>> 8);
        var name = a + b;
        name = name.charAt(0).toUpperCase() + name.slice(1).toLowerCase();
        if (name.length < 3) name = pick(ONSET.default, h) + pick(CODA, h >>> 4);
        var n = 0;
        var tryName = name;
        while (usedNicks[tryName] && n < 9) {
          n++;
          tryName = name + pick(["a", "i", "u", "en", "el"], h + n);
        }
        usedNicks[tryName] = 1;
        return tryName;
      }
      function ensureNamed(f) {
        if (!f) return false;
        try {
          if (typeof initBrain === "function") initBrain(f);
        } catch (e) {
        }
        if (!f.nick && f.fid) {
          try {
            var rec = typeof pedOf === "function" ? pedOf(f.fid) : null;
            if (rec && rec.nick) f.nick = rec.nick;
          } catch (e) {
          }
        }
        var fresh = false;
        if (!f.nick) {
          f.nick = nickFor(f);
          fresh = true;
        }
        usedNicks[f.nick] = 1;
        try {
          if (typeof pedNew === "function" && !f.fid) pedNew(f);
          else if (typeof pedRename === "function") pedRename(f);
        } catch (e) {
        }
        return fresh;
      }
      function write(kind, text, who, opts) {
        try {
          var y = worldYear();
          var line = String(text || "");
          if (line && !/^Year\s+\d/.test(line)) line = "Year " + y + ". " + line;
          if (typeof chronicle === "function") chronicle(kind, line, who || [], opts || { y });
        } catch (e) {
        }
      }
      function worldYear() {
        try {
          if (typeof atlGen === "function") {
            var a = atlGen();
            if (a && a.now) return a.now | 0;
          }
        } catch (e) {
        }
        return 1e3;
      }
      var localBook = { dead: [], sold: [], water: "", wx: "", occ: { known: 0, reads: 0, raised: 0, last: 0 } };
      function book() {
        try {
          var ch = typeof chronState === "function" ? chronState() : null;
          if (ch) {
            if (!ch.book || typeof ch.book !== "object") ch.book = localBook;
            if (!Array.isArray(ch.book.dead)) ch.book.dead = [];
            if (!Array.isArray(ch.book.sold)) ch.book.sold = [];
            return ch.book;
          }
        } catch (e) {
        }
        return localBook;
      }
      function remember(f, kind, text) {
        if (!f || !text) return;
        f._mem = f._mem || [];
        var last = f._mem[f._mem.length - 1];
        if (last && last.s === text) return;
        f._mem.push({ k: kind, s: text, at: now() });
        if (f._mem.length > 8) f._mem.shift();
        try {
          var rec = f.fid && typeof pedOf === "function" ? pedOf(f.fid) : null;
          if (rec) {
            rec.deeds = rec.deeds || [];
            rec.deeds.push({ t: 0, s: text, k: kind || "" });
            if (rec.deeds.length > 24) rec.deeds.shift();
          }
        } catch (e) {
        }
      }
      function rememberAll(kind, text) {
        var list = fishList();
        for (var i = 0; i < list.length; i++) remember(list[i], kind, text);
      }
      function latestMem(f) {
        if (f && f._mem && f._mem.length) return f._mem[f._mem.length - 1].s;
        try {
          var rec = f && f.fid && typeof pedOf === "function" ? pedOf(f.fid) : null;
          if (rec && rec.deeds && rec.deeds.length) return rec.deeds[rec.deeds.length - 1].s;
        } catch (e) {
        }
        return "";
      }
      function motherOf(f) {
        try {
          var rec = f && f.fid && typeof pedOf === "function" ? pedOf(f.fid) : null;
          if (rec && rec.mom) {
            var p = pedOf(rec.mom);
            if (p) return p.nick || p.vname || "";
          }
        } catch (e) {
        }
        return "";
      }
      function waterNow() {
        try {
          if (typeof tankWater === "function") {
            var w = tankWater(0) || tankWater(1);
            if (w) return w;
          }
        } catch (e) {
        }
        return null;
      }
      function nameWater(w) {
        if (!w) return "the tap mix";
        if (w.ph != null && w.ph < 6.5) return "the sour column";
        if (w.ph != null && w.ph > 7.8) return "the hard mix";
        if (w.temp != null && w.temp > 27.5) return "the warm tank";
        if (w.temp != null && w.temp < 22) return "the cold tank";
        if (w.kind && w.kind !== "community") return "the " + w.kind + " water";
        return "the standing mix";
      }
      var lastWxKey = "";
      var lastWaterKey = "";
      var lastOwns = /* @__PURE__ */ Object.create(null);
      var lastClog = false;
      function watchYear() {
        try {
          var y = worldYear();
          var b = book();
          b.year = y;
          if (!watchYear._last) {
            watchYear._last = y;
            return;
          }
          if (y === watchYear._last) return;
          var prev = watchYear._last;
          watchYear._last = y;
          if (!seeded) return;
          var line = "The year turned from " + prev + ". It is " + y + " in the harbor.";
          try {
            var w = world2();
            if (w && w.e && w.e.length && typeof atlLine === "function") {
              var ev = w.e[w.e.length - 1];
              if (ev && (ev.y === y || ev.y === prev + 1)) {
                var said = atlLine(w, ev);
                if (said) line = said;
              }
            }
          } catch (e0) {
          }
          write("era", line);
          rememberAll("year", "the year turning");
        } catch (e) {
        }
      }
      function watchWeather() {
        try {
          var wx = null;
          try {
            if (typeof wxState === "function") wx = wxState();
          } catch (e0) {
          }
          if (!wx && window.__wx) wx = window.__wx;
          if (!wx) return;
          var word = "";
          try {
            if (typeof wxWord === "function") word = String(wxWord() || "");
          } catch (e1) {
          }
          if (!word) {
            try {
              var el = document.getElementById("caldate");
              word = (el && el.textContent || "").split("·").pop().trim();
            } catch (e2) {
            }
          }
          var key = (wx.rain > 0.5 ? "r" : "") + (wx.fog > 0.4 ? "f" : "") + (wx.front > 0.45 ? "F" : "") + (wx.wind >= 28 ? "g" : "") + (wx.cloud > 0.75 ? "c" : "");
          if (key === lastWxKey) {
            var b0 = book();
            if (word) b0.wx = word;
            return;
          }
          var prev = lastWxKey;
          lastWxKey = key;
          if (!seeded) return;
          var b = book();
          b.wx = word;
          if (key.indexOf("F") >= 0 && prev.indexOf("F") < 0) {
            write("disaster", "A front came through the harbor. " + word + ".");
            rememberAll("weather", "the front at the glass");
          } else if (key.indexOf("g") >= 0 && prev.indexOf("g") < 0) {
            write("disaster", "A gale off the harbor. " + word + ". The street emptied.");
            rememberAll("weather", "the gale");
          } else if (key.indexOf("r") >= 0 && prev.indexOf("r") < 0) {
            write("note", "Rain on Salem Street. The storefront ran with it.");
            rememberAll("weather", "rain on the glass");
          } else if (key.indexOf("f") >= 0 && prev.indexOf("f") < 0) {
            write("note", "Fog off the battery. You could not see the yellow house across.");
            rememberAll("weather", "the fog");
          } else if (!key && prev) {
            write("note", "The weather broke. " + (word || "Clear over the North End") + ".");
          }
        } catch (e) {
        }
      }
      function watchWater() {
        try {
          var w = waterNow();
          if (!w) return;
          var key = (w.ph != null ? w.ph.toFixed(1) : "") + ":" + (w.temp != null ? Math.round(w.temp) : "");
          var nm = nameWater(w);
          var b = book();
          if (key !== lastWaterKey && lastWaterKey && seeded) {
            write("note", "The water shifted. It is " + nm + " now" + (w.ph != null ? " · pH " + w.ph.toFixed(1) : "") + (w.temp != null ? " · " + Math.round(w.temp) + "°C" : "") + ".");
            rememberAll("water", nm);
          }
          lastWaterKey = key;
          b.water = nm;
          var clog = false;
          try {
            if (typeof clogged === "function") clog = !!clogged();
          } catch (e) {
          }
          if (clog && !lastClog && seeded) {
            write("illness", "The filter clogged. The column went sour.");
            rememberAll("water", "the dirty filter");
          }
          lastClog = clog;
        } catch (e) {
        }
      }
      function watchTown() {
        try {
          if (typeof townState !== "function") return;
          var t = townState();
          if (!t || !t.people) return;
          for (var id in t.people) {
            var p = t.people[id];
            var n = p.owns && p.owns.length || 0;
            var prev = lastOwns[id] || 0;
            if (n > prev && seeded) {
              var who = "";
              try {
                if (typeof folkName === "function") who = folkName(id);
              } catch (e) {
              }
              if (!who || who === "A customer") {
                try {
                  if (typeof sgPerson === "function") {
                    var sp = sgPerson(+id);
                    if (sp && sp.name) who = sp.name;
                  }
                } catch (e2) {
                }
              }
              var hood = "";
              try {
                if (typeof sgPerson === "function") {
                  var per = sgPerson(+id);
                  if (per && per.gloss) hood = per.gloss;
                }
              } catch (e3) {
              }
              var taken = p.owns[p.owns.length - 1];
              var fishNm = "";
              if (typeof taken === "object" && taken && taken.nick) fishNm = taken.nick;
              write(
                "sale",
                (fishNm || "A fish") + " went home with " + (who || "a regular") + (hood ? ", " + hood : "") + ".",
                []
              );
              var b = book();
              b.sold.push({ n: fishNm || "a fish", who: who || "a customer", hood, day: 0 });
              if (b.sold.length > 48) b.sold.shift();
            }
            lastOwns[id] = n;
          }
        } catch (e) {
        }
      }
      function wrapTownSale() {
        try {
          let wrappedGet = function() {
            var fn = origGet();
            if (fn && !fn.__saga) {
              var inner = function(who, sp, spent, bad) {
                var r = fn.apply(this, arguments);
                try {
                  if (seeded) {
                    var nm = "";
                    try {
                      if (typeof folkName === "function") nm = folkName(who);
                    } catch (e) {
                    }
                    write("sale", (nm && nm !== "A customer" ? nm : "A customer") + " took a fish" + (bad ? " and noticed the water" : "") + ".", []);
                  }
                } catch (e) {
                }
                return r;
              };
              inner.__saga = 1;
              return inner;
            }
            return fn;
          };
          var desc = Object.getOwnPropertyDescriptor(globalThis, "townSale");
          if (!desc || !desc.get || desc.get.__saga) return;
          var origGet = desc.get;
          wrappedGet.__saga = 1;
          Object.defineProperty(globalThis, "townSale", { configurable: true, get: wrappedGet });
        } catch (e) {
        }
      }
      function wrapBrowse() {
        if (didBrowse || !window.shopBrowse) return;
        didBrowse = true;
        var orig = window.shopBrowse;
        window.shopBrowse = function(idx, slot, W, floorY, personS, simT) {
          var rec = orig.apply(this, arguments);
          try {
            if (!rec) return rec;
            var wx = typeof wxState === "function" ? wxState() : null;
            if (wx && rec.phase === "enter" && (!rec.line || Math.random() < 0.22)) {
              if (wx.rain > 0.55) rec.line = pick(["Coming in out of that.", "I'll drip on your floor.", "Wet out."], idx + 3);
              else if (wx.fog > 0.45) rec.line = pick(["Couldn't see the sign.", "Thick out.", "Came by smell."], idx + 5);
              else if (wx.wind >= 28) rec.line = pick(["That wind.", "Hold the door.", "Harbor's up."], idx + 7);
            }
          } catch (e) {
          }
          return rec;
        };
      }
      function world2() {
        try {
          if (typeof civAtlas === "function") {
            var c = civAtlas();
            if (c) return c;
          }
          if (typeof atlGen === "function") return atlGen();
        } catch (e) {
        }
        return null;
      }
      function seedHistory() {
        if (seeded) return;
        try {
          if (!window.chronicle && typeof chronicle !== "function") return;
          var ch = typeof chronState === "function" ? chronState() : null;
          if (ch && ch.sagaSeed) {
            seeded = true;
            return;
          }
          if (ch && ch.n >= 12) {
            if (ch) ch.sagaSeed = 1;
            seeded = true;
            return;
          }
          var w = world2();
          var year = w && w.now ? w.now : 1e3;
          var shop = shopName();
          var faiths = [];
          try {
            if (w && typeof sgFaiths === "function") faiths = sgFaiths(w) || [];
          } catch (e) {
          }
          var faith2 = faiths[0] && faiths[0].name ? faiths[0].name : "the tide";
          var faith22 = faiths[1] && faiths[1].name ? faiths[1].name : "the drowned";
          var someone = "a North End hand";
          try {
            if (typeof sgPerson === "function") {
              var p = sgPerson(14e3) || sgPerson(80);
              if (p && p.name) someone = p.name;
            }
          } catch (e) {
          }
          write("era", "Fourteen faiths still argue over salt and silence.");
          write("town", "People of " + faith2 + " keep the North End. " + faith22 + " holds the outer wharves.");
          write("disaster", "A wreck off the battery still marks the charts. Divers bring up its copper.");
          write("feud", someone + " inherited a grudge from a seizure two generations back. It has not gone cold.");
          write("arrival", shop + " opened on Salem Street. The sign was hung before the first tank was filled.");
          write("note", "The first water in the shop came from the tap, then from a barrel, then from a proper mix.");
          write("town", "Word on the block was that a shop that keeps fish would not last a winter here.");
          write("triumph", "It lasted.");
          write("arrival", "The standing order with the wholesaler began: tetras on Tuesday, plants when the boat was in.");
          write("note", "A regular from the market started coming by after the boats, never buying, always looking.");
          write("town", "The quarter decided the shop was part of the street, which is how a place gets a name that sticks.");
          write("era", "This is the age of the shop. Everything after this line happened under that sign.");
          if (ch) ch.sagaSeed = 1;
          seeded = true;
        } catch (e) {
        }
      }
      function likesOf(f) {
        try {
          if (window.mind && typeof mind.likes === "function") {
            var ml = mind.likes(f);
            if (ml) return ml;
          }
        } catch (e0) {
        }
        var h = hash32((f.fid || 0) + "like" + runSeed());
        var t = f.traits || {};
        var things = [];
        if ((t.social || 0.5) > 0.6) things.push("the company of " + her(f) + " own kind");
        else if ((t.social || 0.5) < 0.3) things.push("an empty stretch of glass");
        if ((t.curious || 0.5) > 0.65) things.push("new objects dropped in the tank");
        if ((t.greed || 0.5) > 0.7) things.push("the first pellets of the day");
        if ((t.bold || 0.5) < 0.3) things.push("the shadow under the driftwood");
        if ((t.aggro || 0.4) > 0.55) things.push("driving others off a corner");
        var hates = ["sudden taps on the glass", "the heater clicking", "a net in the water", "a dirty filter", "being alone"];
        var like = things.length ? pick(things, h) : pick(["still water", "the morning light", "a planted corner"], h);
        var hate = pick(hates, h >>> 5);
        return she(f) + " likes " + like + ". " + she(f) + " detests " + hate + ".";
      }
      function thoughtOf(f) {
        if (!f) return "";
        var bits = [];
        try {
          if (window.mind && typeof mind.dwell === "function") {
            var dw = mind.dwell(f);
            if (dw) bits.push(dw);
          }
        } catch (e0) {
        }
        var b = f.brain || {};
        var hunger = f.hunger == null ? 1 : f.hunger;
        var mem = latestMem(f);
        if (f.risen) bits.unshift(she(f) + " had been dead. The water on this side felt thin.");
        if (mem) {
          var already = false;
          for (var bi = 0; bi < bits.length; bi++) if (bits[bi] && bits[bi].indexOf(mem) >= 0) already = true;
          if (!already) bits.push(she(f) + " remembered " + mem + ".");
        }
        if (f.sick || f.ill) bits.push(she(f) + " was suffering" + (f.ill ? " from " + f.ill : "") + ".");
        else if (hunger < 0.22) bits.push(she(f) + " was starving.");
        else if (hunger > 0.78) bits.push(she(f) + " was satisfied after eating.");
        if ((b.stress || 0) > 0.7) bits.push(she(f) + " was under tremendous stress.");
        else if ((b.stress || 0) < 0.18 && hunger > 0.45 && !f.sick) bits.push(she(f) + " was at peace.");
        if (b.state && b.state !== "settling in" && b.state !== "new") {
          bits.push(she(f) + " was " + String(b.state).replace(/_/g, " ") + ".");
        }
        if (f._foe) bits.push(she(f) + " was forced to endure " + f._foe + ".");
        else if ((b.rank || 1) > 2) bits.push(she(f) + " was forced to yield to a more dominant tankmate.");
        if (f._friend) bits.push(she(f) + " was comforted by the presence of " + f._friend + ".");
        var mom = motherOf(f);
        if (mom && bits.length < 3) bits.push(she(f) + " is the child of " + mom + ".");
        if ((b.trust || 0) > 0.62) bits.push(she(f) + " had grown attached to " + her(f) + " keeper.");
        else if ((b.trust || 0) < 0.12) bits.push(she(f) + " still fled from a hand at the glass.");
        if (b.territory) bits.push(she(f) + " claimed a stretch of the tank as " + her(f) + " own.");
        var bk = book();
        if (bk.year && bits.length < 3 && (hash32((f.fid || 0) + "yr") & 3) === 0)
          bits.push(she(f) + " did not know it was year " + bk.year + ".");
        if (bk.water && bits.length < 3) bits.push(she(f) + " was living in " + bk.water + ".");
        try {
          if (window.shopSite && shopSite.temp() < 14 && bits.length < 4)
            bits.push(she(f) + " felt the street's cold through the glass.");
        } catch (eT) {
        }
        if (f.scar) bits.push(she(f) + " bore an old scar.");
        if (bits.length < 2) bits.push(likesOf(f));
        if (!bits.length) bits.push(she(f) + " was going about " + her(f) + " day.");
        var out = [];
        var seen = /* @__PURE__ */ Object.create(null);
        for (var i = 0; i < bits.length && out.length < 3; i++) {
          if (seen[bits[i]]) continue;
          seen[bits[i]] = 1;
          out.push(bits[i]);
        }
        return out.join(" ");
      }
      function watchFish() {
        var list = fishList();
        var live = /* @__PURE__ */ Object.create(null);
        var namedNew = [];
        for (var i = 0; i < list.length; i++) {
          var f = list[i];
          if (!f) continue;
          var fresh = ensureNamed(f);
          var id = f.fid || i;
          live[id] = f;
          if (fresh) namedNew.push(f);
          var st = f.brain && f.brain.state || "";
          var prev = lastStates[id];
          if (prev && st && st !== prev && st !== "settling in") {
            if (st === "making for cover" || st === "hiding") {
              write("note", (f.nick || "A fish") + " fled for cover.", [f]);
            } else if (st === "resting" && prev !== "resting") {
            }
          }
          lastStates[id] = st;
        }
        if (namedNew.length && lastFids && seeded) {
          for (var n = 0; n < namedNew.length; n++) {
            var nf = namedNew[n];
            var kind = "arrival";
            var line = (nf.nick || "A fish") + " the " + spName(nf).toLowerCase() + " came into the shop.";
            try {
              var rec = typeof pedOf === "function" && nf.fid ? pedOf(nf.fid) : null;
              if (rec && rec.mom) {
                kind = "birth";
                var p = typeof pedOf === "function" ? pedOf(rec.mom) : null;
                var mom = p ? p.nick || p.vname || "" : "";
                if (mom)
                  line = (nf.nick || "A fry") + " was born of " + mom + ", a " + spName(nf).toLowerCase() + ".";
              }
            } catch (e) {
            }
            write(kind, line, [nf]);
            remember(nf, kind === "birth" ? "birth" : "arrival", kind === "birth" ? "being born here" : "coming into the shop");
          }
        }
        if (lastFids && seeded) {
          for (var id2 in lastFids) {
            if (live[id2]) continue;
            var gone = lastFids[id2];
            if (!gone || !gone.fid) continue;
            var how = null;
            try {
              var pr = typeof pedOf === "function" && gone.fid ? pedOf(gone.fid) : null;
              if (pr && pr.fate && pr.fate.how) how = pr.fate.how;
            } catch (e) {
            }
            if (!how) continue;
            var last = thoughtOf(gone);
            var witnesses = [];
            for (var wi in live) {
              if (witnesses.length >= 2) break;
              var wtn = live[wi];
              if (wtn && wtn.nick) witnesses.push(wtn.nick);
            }
            write("death", (gone.nick || spName(gone)) + " " + how + "." + (witnesses.length ? " " + witnesses[0] + " was nearby." : ""), [gone]);
            rememberAll("death", (gone.nick || "a tankmate") + " dying");
            var bd = book();
            bd.dead.push({
              n: gone.nick || spName(gone),
              sp: spName(gone),
              how,
              last,
              saw: witnesses,
              y: worldYear()
            });
            if (bd.dead.length > 40) bd.dead.shift();
          }
        }
        lastFids = live;
        try {
          if (list.length >= 2 && typeof chronicle === "function") {
            for (var a = 0; a < list.length; a++) {
              var A = list[a];
              if (!A || !A.brain || A.traits && A.traits.aggro < 0.55) continue;
              for (var b = a + 1; b < list.length; b++) {
                var B = list[b];
                if (!B || B.sp !== A.sp) continue;
                var key = (A.fid || a) + "|" + (B.fid || b);
                if (lastPair[key]) continue;
                var dx = (A.x || 0) - (B.x || 0);
                var dy = (A.y || 0) - (B.y || 0);
                if (dx * dx + dy * dy < 70 * 70) {
                  lastPair[key] = 1;
                  write(
                    "fight",
                    (A.nick || "One") + " drove " + (B.nick || "another") + " off a corner of the tank.",
                    [A, B]
                  );
                  remember(A, "fight", "driving " + (B.nick || "another") + " off");
                  remember(B, "fight", (A.nick || "a rival") + " in " + her(B) + " corner");
                  A._foe = B.nick || A._foe;
                  B._foe = A.nick || B._foe;
                  if (A.brain) A.brain.stress = Math.min(1, (A.brain.stress || 0) + 0.04);
                  if (B.brain) B.brain.stress = Math.min(1, (B.brain.stress || 0) + 0.08);
                }
              }
            }
          }
        } catch (e) {
        }
        try {
          for (var i2 = 0; i2 < list.length; i2++) {
            var F = list[i2];
            if (!F || !F.nick) continue;
            var best = null, bestD = 1e9;
            for (var j2 = 0; j2 < list.length; j2++) {
              if (j2 === i2) continue;
              var G2 = list[j2];
              if (!G2 || !G2.nick || G2.sp !== F.sp) continue;
              var ddx = (F.x || 0) - (G2.x || 0);
              var ddy = (F.y || 0) - (G2.y || 0);
              var dd = ddx * ddx + ddy * ddy;
              if (dd < bestD) {
                bestD = dd;
                best = G2;
              }
            }
            if (best && bestD < 140 * 140) {
              if (F._friend !== best.nick) {
                F._friend = best.nick;
                if (seeded && Math.random() < 0.12) remember(F, "bond", "swimming with " + best.nick);
              }
            }
          }
        } catch (e) {
        }
      }
      function maybeEra() {
        try {
          var ch = typeof chronState === "function" ? chronState() : null;
          if (!ch) return;
          ch.eras = ch.eras || [];
          if (ch.eras.length > 6) return;
          var list = fishList();
          var counts = /* @__PURE__ */ Object.create(null);
          var top = "", n = 0;
          for (var i = 0; i < list.length; i++) {
            var k2 = spName(list[i]);
            counts[k2] = (counts[k2] || 0) + 1;
            if (counts[k2] > n) {
              n = counts[k2];
              top = k2;
            }
          }
          var last = ch.eras[ch.eras.length - 1];
          if (n >= 4 && top && (!last || last.name.indexOf(top) < 0)) {
            var name = "The time of the " + top.toLowerCase() + "s";
            ch.eras.push({
              name,
              why: n + " of them in the shop at once.",
              from: typeof _ === "function" ? _() : 0
            });
            write("era", name + ". " + n + " of that fish were in the tanks together.");
          }
        } catch (e) {
        }
      }
      function enhanceCards() {
        var body = document.getElementById("dbody");
        if (!body) return;
        var list = fishList();
        if (!list.length) return;
        var rows = body.querySelectorAll(".row[data-fi]");
        if (!rows.length) return;
        for (var i = 0; i < rows.length; i++) {
          var row2 = rows[i];
          var idx = +row2.getAttribute("data-fi");
          var f = list[idx];
          if (!f) continue;
          ensureNamed(f);
          var host = row2.children[1] || row2;
          var el = host.querySelector(".saga-thought");
          if (!el) {
            el = document.createElement("div");
            el.className = "d saga-thought";
            host.appendChild(el);
          }
          var line = thoughtOf(f);
          if (el.textContent !== line) el.textContent = line;
          var kin2 = host.querySelector(".saga-kin");
          var extra = [];
          if (f._friend) extra.push("with " + f._friend);
          var mom = motherOf(f);
          if (mom) extra.push("child of " + mom);
          if (extra.length) {
            if (!kin2) {
              kin2 = document.createElement("div");
              kin2.className = "d saga-kin";
              host.appendChild(kin2);
            }
            var kt = extra.join(" · ");
            if (kin2.textContent !== kt) kin2.textContent = kt;
          }
          var nm = host.querySelector(".n");
          if (nm && f.nick && nm.textContent.indexOf(f.nick) < 0) {
            var gold = document.createElement("span");
            gold.style.color = "var(--gold)";
            gold.textContent = f.nick;
            nm.insertBefore(document.createTextNode(" · "), nm.firstChild);
            nm.insertBefore(gold, nm.firstChild);
          }
        }
      }
      function enhanceLife() {
        try {
          var body = document.getElementById("dbody");
          if (!body) return;
          var old = body.querySelector("saga-street");
          if (old && old.parentNode) old.parentNode.removeChild(old);
        } catch (e) {
        }
      }
      function enhanceChron() {
        var body = document.getElementById("dbody");
        var title = document.getElementById("dtitle");
        if (!body || !title) return;
        if (!/chronicle/i.test(title.textContent || "")) return;
        var list = fishList();
        var b = book();
        var html = '<div class="note">Year ' + worldYear() + (b.wx ? " · " + escapeHtml(b.wx) : "") + ".</div>";
        html += '<div class="sec">What they are thinking</div>';
        var n = 0;
        for (var i = 0; i < list.length && n < 8; i++) {
          var f = list[i];
          if (!f) continue;
          ensureNamed(f);
          var mom = motherOf(f);
          html += '<div class="row"><div></div><div><div class="n">' + escapeHtml(f.nick || spName(f)) + " · " + escapeHtml(spName(f)) + (f._friend ? " · with " + escapeHtml(f._friend) : "") + '</div><div class="d saga-thought">' + escapeHtml(thoughtOf(f)) + "</div>" + (mom ? '<div class="d">Child of ' + escapeHtml(mom) + "</div>" : "") + "</div><div></div></div>";
          n++;
        }
        if (b.water) {
          html += '<div class="sec">The water</div><div class="row"><div></div><div><div class="d">' + escapeHtml(b.water) + (b.wx ? " · outside, " + escapeHtml(b.wx) : "") + "</div></div><div></div></div>";
        }
        if (b.dead && b.dead.length) {
          html += '<div class="sec">Those who were here <span>' + b.dead.length + "</span></div>";
          for (var d = b.dead.length - 1; d >= 0 && d >= b.dead.length - 6; d--) {
            var dd = b.dead[d];
            html += '<div class="row"><div></div><div><div class="n">' + escapeHtml(dd.n) + " · " + escapeHtml(dd.sp || "") + (dd.y ? " · " + escapeHtml(dd.y) : "") + '</div><div class="d saga-thought">' + escapeHtml((dd.how ? dd.how + ". " : "") + (dd.last || "")) + (dd.saw && dd.saw[0] ? " " + escapeHtml(dd.saw[0]) + " saw it." : "") + "</div></div><div></div></div>";
          }
        }
        if (b.sold && b.sold.length) {
          html += '<div class="sec">Those who left in a fish bag <span>' + b.sold.length + "</span></div>";
          for (var s = b.sold.length - 1; s >= 0 && s >= b.sold.length - 5; s--) {
            var sl = b.sold[s];
            html += '<div class="row"><div></div><div><div class="d">' + escapeHtml(sl.n) + " lives with " + escapeHtml(sl.who || "someone") + (sl.hood ? ", " + escapeHtml(sl.hood) : "") + " now.</div></div><div></div></div>";
          }
        }
        if (!n && !b.dead.length && !b.sold.length) return;
        var box = body.querySelector(".saga-minds");
        if (!box) {
          box = document.createElement("div");
          box.className = "saga-minds";
          body.insertBefore(box, body.firstChild);
        }
        if (box.getAttribute("data-h") !== String(html.length) + n) {
          box.innerHTML = html;
          box.setAttribute("data-h", String(html.length) + n);
        }
      }
      function escapeHtml(s) {
        var map = { "&": "&amp;", "<": "&lt;", ">": "&gt;", '"': "&quot;" };
        return String(s || "").replace(/[&<>"]/g, function(c) {
          return map[c];
        });
      }
      function tick() {
        try {
          if (typeof allFish === "function" || typeof chronState === "function") {
            seedHistory();
            if (now() - lastHook > 0.7) {
              lastHook = now();
              wrapTownSale();
              wrapBrowse();
              watchFish();
              watchWeather();
              watchWater();
              watchTown();
              watchYear();
              if (Math.random() < 0.08) maybeEra();
            }
            if (now() - lastUi > 0.45) {
              lastUi = now();
              enhanceCards();
              enhanceLife();
              enhanceChron();
            }
          }
        } catch (e) {
        }
      }
      window.saga = {
        thought: thoughtOf,
        thoughtOf,
        name: ensureNamed,
        likes: likesOf,
        remember,
        book,
        year: worldYear
      };
      if (window.__onBeat) window.__onBeat(tick, 280);
      else setTimeout(function loop() {
        tick();
        setTimeout(loop, 280);
      }, 280);
    })();
  } catch (e) {
    console.error("Layer saga.js failed to start:", e);
  }

  // src/layers/mind.js
  try {
    (function() {
      "use strict";
      var lastTick = 0;
      var lastUi = 0;
      var lastMoodAt = 0;
      var wired = false;
      var LIKE = [
        "shoaling",
        "the planted corner",
        "still water",
        "an empty stretch of glass",
        "the first pellets of the day",
        "the shadow under the driftwood",
        "warmth off the heater",
        "a current they can hold in"
      ];
      var HATE = [
        "a net in the water",
        "taps on the glass",
        "the heater clicking",
        "a packed filter",
        "being alone",
        "a hand at the surface",
        "the shop bell",
        "cold water"
      ];
      var CORE_KIND = { death: 1, rite: 1, birth: 1, year: 1, fight: 1, risen: 1 };
      var VALENCE = {
        death: -2,
        rite: -1,
        fight: -1,
        clog: -1,
        sick: -1,
        year: 1,
        birth: 2,
        arrival: 1,
        bond: 1,
        sale: -1
      };
      function now() {
        return typeof performance !== "undefined" ? performance.now() / 1e3 : Date.now() / 1e3;
      }
      function within(at, secs) {
        var age2 = now() - at;
        return age2 >= 0 && age2 < secs;
      }
      function year() {
        try {
          if (window.saga && typeof saga.year === "function") return saga.year();
        } catch (e) {
        }
        return 1e3;
      }
      function hash32(s) {
        var h = 2166136261;
        s = String(s || "");
        for (var i = 0; i < s.length; i++) {
          h ^= s.charCodeAt(i);
          h = Math.imul(h, 16777619);
        }
        return h >>> 0;
      }
      function pick(arr, h) {
        return arr[(h >>> 0) % arr.length];
      }
      function she(f) {
        if (!f) return "They";
        if (f.sex === "f" || f.fem) return "She";
        if (f.sex === "m") return "He";
        return "They";
      }
      function her(f) {
        if (!f) return "their";
        if (f.sex === "f" || f.fem) return "her";
        if (f.sex === "m") return "his";
        return "their";
      }
      function fishList() {
        try {
          if (typeof allFish === "function") return allFish() || [];
        } catch (e) {
        }
        return [];
      }
      function gs() {
        try {
          if (typeof gameState === "function") return gameState();
          if (typeof gameState === "object" && gameState) return gameState;
        } catch (e) {
        }
        return null;
      }
      function book() {
        try {
          if (window.saga && typeof saga.book === "function") return saga.book();
        } catch (e) {
        }
        var g = gs();
        if (g) {
          g.mindBook = g.mindBook || { arts: [], slabs: [] };
          return g.mindBook;
        }
        return { arts: [], slabs: [] };
      }
      function write(kind, text) {
        try {
          if (typeof chronicle === "function") {
            var y = year();
            var line = String(text || "");
            if (line && !/^Year\s+\d/.test(line)) line = "Year " + y + ". " + line;
            chronicle(kind, line, []);
          }
        } catch (e) {
        }
      }
      function say(msg, kind) {
        try {
          if (typeof toast === "function") toast(msg, kind || "");
          else if (typeof k === "function") k(msg, kind || "");
        } catch (e) {
        }
      }
      function markEgg(id, line) {
        try {
          if (typeof findEgg === "function") findEgg(id, line);
        } catch (e) {
        }
      }
      function mindOf(f) {
        if (!f) return null;
        if (f.mind && f.mind.short) return f.mind;
        var h = hash32((f.fid || f.nick || "") + "mind");
        var t = f.traits || {};
        f.mind = {
          brave: t.bold != null ? t.bold : (h & 255) / 255,
          tender: t.social != null ? t.social : (h >>> 8 & 255) / 255,
          restless: t.curious != null ? t.curious : (h >>> 16 & 255) / 255,
          proud: t.aggro != null ? t.aggro : (h >>> 20 & 255) / 255,
          like: pick(LIKE, h),
          hate: pick(HATE, h >>> 5),
          need: { school: 0.7, clean: 0.8, food: 0.8, quiet: 0.6 },
          short: [],
          long: [],
          core: [],
          stress: f.brain && f.brain.stress || 0.2,
          mood: null,
          changed: ""
        };
        return f.mind;
      }
      function fileMem(f, kind, text) {
        var m = mindOf(f);
        if (!m || !text) return;
        var v = VALENCE[kind] != null ? VALENCE[kind] : 0;
        var rec = { k: kind || "", s: String(text), v, y: year(), at: now() };
        if (m.short.length && m.short[m.short.length - 1].s === rec.s) return;
        m.short.push(rec);
        if (m.short.length > 8) m.short.shift();
        m.stress = Math.max(0, Math.min(1, m.stress - v * 0.04));
        if (f.brain) f.brain.stress = Math.max(0, Math.min(1, (f.brain.stress || 0) - v * 0.03));
        maybePromote(m);
      }
      function maybePromote(m) {
        if (m.short.length < 3) return;
        var best = m.short[0];
        for (var i = 1; i < m.short.length; i++) {
          if (Math.abs(m.short[i].v) > Math.abs(best.v)) best = m.short[i];
        }
        if (Math.abs(best.v) < 1) return;
        var hit = false;
        for (var j = 0; j < m.long.length; j++) if (m.long[j].s === best.s) hit = true;
        if (!hit) {
          m.long.push(best);
          if (m.long.length > 8) m.long.shift();
        }
        if (CORE_KIND[best.k] && Math.random() < 0.33) {
          var cHit = false;
          for (var c = 0; c < m.core.length; c++) if (m.core[c].s === best.s) cHit = true;
          if (!cHit) {
            m.core.push(best);
            if (m.core.length > 6) m.core.shift();
            shiftFacet(m, best);
            markEgg("coremem", "A memory settled in and did not leave.");
          }
        }
      }
      function shiftFacet(m, rec) {
        if (rec.k === "death" || rec.k === "rite") {
          m.tender = Math.max(0, m.tender - 0.08);
          m.brave = Math.min(1, m.brave + 0.06);
          m.changed = rec.v < 0 ? "hardened after " + rec.s : m.changed;
        } else if (rec.k === "birth" || rec.k === "bond") {
          m.tender = Math.min(1, m.tender + 0.08);
          m.changed = "softened after " + rec.s;
        } else if (rec.k === "fight") {
          m.proud = Math.min(1, m.proud + 0.07);
          m.changed = "quicker to claim a corner after " + rec.s;
        } else if (rec.k === "year") {
          m.restless = Math.min(1, m.restless + 0.04);
        }
      }
      function dwellLine(f) {
        var m = mindOf(f);
        if (!m) return "";
        if (m.mood) {
          if (m.mood.kind === "fey") return she(f) + " is circling a corner of the tank and will not eat.";
          if (m.mood.kind === "secret") return she(f) + " has gone behind the heater and will not come out.";
          return she(f) + " is looking out of " + her(f) + " own eyes with something else looking too.";
        }
        if (m.core.length && Math.random() < 0.45) {
          var c = m.core[m.core.length - 1];
          return she(f) + " is dwelling on " + c.s + ".";
        }
        if (m.long.length && Math.random() < 0.35) {
          var L = m.long[(hash32(f.fid + String(now() | 0)) >>> 0) % m.long.length];
          return she(f) + " relived " + L.s + ".";
        }
        if (m.need.school < 0.35) return she(f) + " needs the company of " + her(f) + " own kind.";
        if (m.need.clean < 0.35) return she(f) + " cannot stand the water as it is.";
        if (m.need.food < 0.3) return she(f) + " has gone without long enough to feel it.";
        if (m._cold) return she(f) + " is holding still in water that went thin.";
        if (m.changed && Math.random() < 0.2) return she(f) + " " + m.changed + ".";
        return "";
      }
      function likesLine(f) {
        var m = mindOf(f);
        if (!m) return "";
        var line = she(f) + " likes " + m.like + ". " + she(f) + " detests " + m.hate + ".";
        if (m.changed) line += " " + she(f) + " " + m.changed + ".";
        return line;
      }
      function tickNeeds(list) {
        var clog = false;
        try {
          clog = !!window.clogged;
        } catch (e) {
        }
        var bySp = /* @__PURE__ */ Object.create(null);
        for (var i = 0; i < list.length; i++) {
          var f = list[i];
          if (!f) continue;
          var k2 = String(f.sp);
          bySp[k2] = (bySp[k2] || 0) + 1;
        }
        for (var j = 0; j < list.length; j++) {
          var F = list[j];
          if (!F) continue;
          if (F.nick && /[<>]/.test(F.nick)) F.nick = String(F.nick).replace(/[<>]/g, "");
          var m = mindOf(F);
          var nSame = bySp[String(F.sp)] || 1;
          var schoolWant = F.traits && F.traits.social > 0.55 || m.tender > 0.55;
          m.need.school += nSame >= 2 ? 0.04 : schoolWant ? -0.05 : 0.01;
          m.need.clean += clog ? -0.06 : 0.03;
          var hunger = F.hunger == null ? 0.7 : F.hunger;
          m.need.food += hunger > 0.55 ? 0.04 : -0.05;
          m.need.quiet += m.mood ? -0.03 : 0.01;
          var temp = 18;
          var wet = 0;
          try {
            if (window.shopSite) {
              temp = shopSite.temp() || 18;
              wet = shopSite.wet() || 0;
            }
          } catch (eT) {
          }
          if (temp < 14) {
            m.need.quiet = clamp01(m.need.quiet - 0.04);
            m.stress = clamp01(m.stress + 0.018);
            if (!m._cold) {
              m._cold = 1;
              fileMem(F, "clog", "how thin the warmth got");
            }
          } else if (temp >= 16) m._cold = 0;
          if (clog && m.hate && /filter|packed|dirty/.test(m.hate)) m.stress = clamp01(m.stress + 0.012);
          try {
            if (window.saga && saga.book) {
              var water = String(saga.book().water || "");
              if (/clog|foul|packed|stale|off/i.test(water)) m.need.clean = clamp01(m.need.clean - 0.03);
            }
          } catch (eW) {
          }
          if (wet > 0.4 && m.hate && /bell|tap|hand/.test(m.hate)) m.need.quiet = clamp01(m.need.quiet - 0.03);
          try {
            if (window.desk && desk.coming) {
              var c = desk.coming();
              if (c && c.arrived && within(c.arrived, 8)) m.need.quiet = clamp01(m.need.quiet - 0.02);
            }
          } catch (eC) {
          }
          m.need.school = clamp01(m.need.school);
          m.need.clean = clamp01(m.need.clean);
          m.need.food = clamp01(m.need.food);
          m.need.quiet = clamp01(m.need.quiet);
          var drain = 0;
          if (m.need.school < 0.4) drain += 0.015 * (1 - m.brave);
          if (m.need.clean < 0.4) drain += 0.02;
          if (m.need.food < 0.35) drain += 0.02;
          m.stress = clamp01(m.stress + drain - 8e-3);
          if (F.brain) F.brain.stress = clamp01((F.brain.stress || 0) * 0.85 + m.stress * 0.15);
        }
      }
      function clamp01(n) {
        return n < 0 ? 0 : n > 1 ? 1 : n;
      }
      function tickMoods(list) {
        var t = now();
        var busy = false;
        for (var i = 0; i < list.length; i++) {
          var f = list[i];
          var m = f && mindOf(f);
          if (m && m.mood) {
            busy = true;
            if (t > m.mood.until || m.mood.until - t > 60) finishMood(f, m);
          }
        }
        if (busy) return;
        if (t - lastMoodAt < 90) return;
        if (list.length < 2) return;
        if (Math.random() > 0.012) return;
        var cand = [];
        for (var j = 0; j < list.length; j++) {
          var F = list[j];
          if (!F || !F.nick) continue;
          var M = mindOf(F);
          if (M.restless > 0.45 || M.proud > 0.5) cand.push(F);
        }
        if (!cand.length) return;
        var pickF = cand[Math.random() * cand.length | 0];
        startMood(pickF);
      }
      function startMood(f) {
        var m = mindOf(f);
        var kinds = ["fey", "secret", "possessed"];
        var kind = pick(kinds, hash32(f.fid + "mood" + year()));
        m.mood = { kind, until: now() + 18 + Math.random() * 14, from: now() };
        lastMoodAt = now();
        var line = (f.nick || "A fish") + (kind === "fey" ? " has taken a strange mood. Circling. Will not eat." : kind === "secret" ? " has gone behind the heater. A secretive mood." : " is not looking out of " + her(f) + " own eyes.");
        write("era", line);
        say(line, "gold");
        markEgg("strmood", line);
        try {
          if (window.feel && feel.play) feel.play("whoosh");
        } catch (e) {
        }
      }
      function finishMood(f, m) {
        m.mood = null;
        lastMoodAt = now();
        var b = book();
        b.arts = b.arts || [];
        var mats = ["bogwood", "mica", "driftglass", "shell", "riverstone", "brass"];
        var mat = pick(mats, hash32((f.fid || "") + "art"));
        var mat2 = pick(mats, hash32((f.fid || "") + "art2"));
        var rx = "";
        try {
          if (window.liveAtlas && liveAtlas.react && liveAtlas.mats) {
            var A = null, B = null;
            for (var mi = 0; mi < liveAtlas.mats.length; mi++) {
              if (liveAtlas.mats[mi].id === mat) A = liveAtlas.mats[mi];
              if (liveAtlas.mats[mi].id === mat2) B = liveAtlas.mats[mi];
            }
            if (A && B) rx = liveAtlas.react(A, B);
            if (A && B && A.id !== B.id) mat = A.id + " and " + B.id;
          }
        } catch (eMat) {
        }
        var titles = ["Wake", "Filigree", "Year-Turn", "Quiet", "Claim", "Salt-Mark"];
        var title = (f.nick || "Unnamed") + "'s " + pick(titles, hash32(f.fid + "t"));
        var art = {
          n: title,
          mat,
          who: f.nick || "a fish",
          y: year(),
          img: m.long.length ? m.long[m.long.length - 1].s : m.short.length ? m.short[m.short.length - 1].s : "the shop as it stood",
          rx
        };
        b.arts.push(art);
        if (b.arts.length > 24) b.arts.shift();
        var desc = title + ", " + mat + (rx ? ". " + rx : ". Salt in the grain") + ". Engraved is " + art.img + ".";
        write("triumph", desc);
        say(desc, "gold");
        markEgg("artifact", desc);
        fileMem(f, "birth", "making " + title);
        m.proud = Math.min(1, m.proud + 0.1);
        m.stress = Math.max(0, m.stress - 0.2);
        try {
          if (typeof addRep === "function") addRep(2);
        } catch (e) {
        }
      }
      function memorials() {
        var b = book();
        b.slabs = b.slabs || [];
        var dead = b.dead || [];
        for (var i = 0; i < dead.length; i++) {
          var d = dead[i];
          if (!d || !d.n) continue;
          var hit = false;
          for (var s = 0; s < b.slabs.length; s++) if (b.slabs[s].n === d.n && b.slabs[s].y === d.y) hit = true;
          if (hit) continue;
          b.slabs.push({
            n: d.n,
            sp: d.sp || "fish",
            how: d.how || "died",
            y: d.y || year(),
            last: d.last || ""
          });
        }
        if (b.slabs.length > 20) b.slabs.splice(0, b.slabs.length - 20);
        return b.slabs;
      }
      function drainSaga(f) {
        if (!f || !f._mem || !f._mem.length) return;
        var m = mindOf(f);
        var t = now();
        var last = m._saw || 0;
        if (last > t) last = 0;
        var seen = last;
        for (var i = 0; i < f._mem.length; i++) {
          var rec = f._mem[i];
          if (!rec || rec.at <= last || rec.at > t) continue;
          fileMem(f, rec.k, rec.s);
          if (rec.at > seen) seen = rec.at;
        }
        m._saw = seen;
      }
      function wrapSaga() {
        if (!window.saga || wired) return;
        if (typeof saga.remember === "function" && !saga.remember.__mind) {
          var origR = saga.remember;
          saga.remember = function(f, kind, text) {
            var r = origR.apply(this, arguments);
            try {
              fileMem(f, kind, text);
            } catch (e) {
            }
            return r;
          };
          saga.remember.__mind = 1;
        }
        if (typeof saga.thoughtOf === "function" && !saga.thoughtOf.__mind) {
          var origT = saga.thoughtOf;
          saga.thoughtOf = function(f) {
            try {
              mindOf(f);
            } catch (e) {
            }
            var extra = "";
            try {
              extra = dwellLine(f);
            } catch (e2) {
            }
            var base = origT.apply(this, arguments) || "";
            if (extra && base.indexOf(extra.slice(0, 12)) < 0) return extra + " " + base;
            return base;
          };
          saga.thoughtOf.__mind = 1;
          saga.thought = saga.thoughtOf;
        }
        if (typeof saga.likes === "function" && !saga.likes.__mind) {
          var origL = saga.likes;
          saga.likes = function(f) {
            try {
              var line = likesLine(f);
              if (line) return line;
            } catch (e) {
            }
            return origL.apply(this, arguments);
          };
          saga.likes.__mind = 1;
        }
        wired = true;
      }
      function esc(s) {
        var map = { "&": "&amp;", "<": "&lt;", ">": "&gt;", '"': "&quot;" };
        return String(s || "").replace(/[&<>"]/g, function(c) {
          return map[c];
        });
      }
      function enhanceLife() {
        try {
          var body = document.getElementById("dbody");
          if (!body) return;
          var old = body.querySelector("mind-life");
          if (old && old.parentNode) old.parentNode.removeChild(old);
        } catch (e) {
        }
      }
      function seedWiki() {
        try {
          var w = typeof WIKI === "function" ? WIKI() : WIKI;
          if (!w || !w.push) return;
          var extra = [
            {
              id: "k_mem",
              sec: "The animals",
              t: "Memory",
              tags: "memory short long core dwell thought stress",
              w: "<p>A fish keeps eight short memories and a handful of long ones. The strongest of those, if they are of a death, a birth, a year turning, a fight, or a return from the pages, can settle as core memory. Core memory changes who they are: they harden, or they soften, or they claim a corner faster.</p><p>They relive the long ones. That is not flavor. It is why a fish who watched a tankmate die is still not right a week later. <b>What to do about it:</b> read Life. What they dwell on is the list. A strange mood is a memory with nowhere to go but out.</p>"
            },
            {
              id: "k_need",
              sec: "The animals",
              t: "Needs",
              tags: "need school clean food focus stress company",
              w: "<p>Company, clean water, food, quiet. Not every fish wants the same mix. A social one left alone loses focus. A packed filter is a need failing in public. Unmet needs become stress, and stress is the same number the rest of the sim already uses, so they hide, nip, or go off their food for a reason you can name.</p><p><b>What to do about it:</b> keep pairs of the ones that want company. Click the clog. Feed. Life will say who is going without.</p>"
            },
            {
              id: "k_mood",
              sec: "The animals",
              t: "Strange moods",
              tags: "mood fey secretive possessed artifact engraving legendary",
              w: "<p>Once in a while a named fish is taken. Fey: circling, will not eat. Secretive: behind the heater. Possessed: something else looking out. When it breaks, they leave an artifact in the shop — bogwood, mica, drift glass — with the memory that was in them cut into it. The Chronicle writes the object. The case keeps it.</p><p>If you sell them in the middle of it, the mood dies with the sale. <b>What to do about it:</b> leave them. Watch Life. The artifact is the point, not a multiplier.</p>"
            },
            {
              id: "k_slab",
              sec: "The chronicle",
              t: "Memorials",
              tags: "slab memorial dead memory in memory death",
              w: "<p>A named fish who dies gets a slab. Not a gravestone you place. A line that stays: who, how, the year. People on the floor will still ask after a tetra that is not in the water. The Book of the Dead is the occult copy. The slabs are the shop's.</p><p><b>What to do about it:</b> Life, In memory. Do not expect the street to forget a name you gave.</p>"
            }
          ];
          for (var i = 0; i < extra.length; i++) {
            var hit = false;
            for (var j = 0; j < w.length; j++) if (w[j] && w[j].id === extra[i].id) hit = true;
            if (!hit) w.push(extra[i]);
          }
        } catch (e) {
        }
      }
      function tick() {
        try {
          wrapSaga();
          seedWiki();
          if (now() - lastTick > 1.1) {
            lastTick = now();
            var list = fishList();
            for (var i = 0; i < list.length; i++) {
              mindOf(list[i]);
              drainSaga(list[i]);
            }
            tickNeeds(list);
            tickMoods(list);
            memorials();
          }
          if (now() - lastUi > 0.7) {
            lastUi = now();
            enhanceLife();
          }
        } catch (e) {
        }
      }
      window.mind = {
        of: mindOf,
        book,
        dwell: dwellLine,
        likes: likesLine
      };
      if (window.__onBeat) window.__onBeat(tick, 280);
      else setTimeout(function loop() {
        tick();
        setTimeout(loop, 280);
      }, 280);
    })();
  } catch (e) {
    console.error("Layer mind.js failed to start:", e);
  }

  // src/layers/occult.js
  try {
    (function() {
      "use strict";
      var lastScene = "";
      var open = false;
      var seededDead = false;
      var didBrowse = false;
      var didGo = false;
      function say(msg, kind) {
        try {
          if (typeof toast === "function") toast(msg, kind || "bad");
          else if (typeof k === "function") k(msg, kind || "bad");
        } catch (e) {
        }
      }
      function gs() {
        try {
          if (typeof gameState === "function") return gameState();
          if (typeof gameState === "object" && gameState && gameState.stats) return gameState;
        } catch (e) {
        }
        return null;
      }
      function write(kind, text, who) {
        try {
          var y = worldYear();
          var line = String(text || "");
          if (line && !/^Year\s+\d/.test(line)) line = "Year " + y + ". " + line;
          if (typeof chronicle === "function") chronicle(kind, line, who || []);
        } catch (e) {
        }
      }
      var localBook = { dead: [], sold: [], water: "", wx: "", occ: { known: 0, reads: 0, raised: 0, last: 0 } };
      function book() {
        try {
          if (window.saga && typeof saga.book === "function") {
            var b = saga.book();
            if (b && typeof b === "object") {
              if (!Array.isArray(b.dead)) b.dead = [];
              return b;
            }
          }
        } catch (e) {
        }
        return localBook;
      }
      function occ() {
        var b = book();
        b.occ = b.occ || { known: 0, reads: 0, raised: 0, last: 0 };
        return b.occ;
      }
      function esc(s) {
        var map = { "&": "&amp;", "<": "&lt;", ">": "&gt;", '"': "&quot;" };
        return String(s || "").replace(/[&<>"]/g, function(c) {
          return map[c];
        });
      }
      function worldYear() {
        try {
          if (window.saga && typeof saga.year === "function") return saga.year();
          if (typeof atlGen === "function") {
            var a = atlGen();
            if (a && a.now) return a.now | 0;
          }
        } catch (e) {
        }
        return 1e3;
      }
      function seedDead() {
        if (seededDead) return;
        var b = book();
        if (!b.dead) b.dead = [];
        if (b.dead.length) {
          seededDead = true;
          return;
        }
        b.dead.push({
          n: "Ingum",
          sp: "Goldfish",
          how: "went still the winter the heater failed",
          last: "She was cold.",
          saw: ["the empty tank"],
          old: 1,
          y: 412
        });
        b.dead.push({
          n: "Thokel",
          sp: "Betta",
          how: "was found on the gravel after a fight that nobody admitted",
          last: "He was forced to endure a rival.",
          saw: [],
          old: 1,
          y: 781
        });
        b.dead.push({
          n: "Melas",
          sp: "Neon tetra",
          how: "came up in a net off the wreck and did not survive the walk to Salem Street",
          last: "She remembered the salt.",
          saw: ["the harbor"],
          old: 1,
          y: 996
        });
        seededDead = true;
      }
      function spIndex(name) {
        var S = null;
        try {
          S = typeof SPECIES !== "undefined" ? SPECIES : null;
        } catch (e) {
        }
        if (!S || !S.length) return 0;
        var q = String(name || "").toLowerCase();
        var i, n;
        for (i = 0; i < S.length; i++) {
          n = String(S[i] && (S[i].name || S[i].gname) || "").toLowerCase();
          if (n === q) return i;
        }
        for (i = 0; i < S.length; i++) {
          n = String(S[i] && (S[i].name || S[i].gname || S[i].key) || "").toLowerCase();
          if (n.indexOf(q) >= 0 || q.indexOf(n) >= 0) return i;
        }
        return 0;
      }
      function echo(line) {
        try {
          if (window.__actEcho != null) window.__actEcho = line;
          var body = document.getElementById("conBody");
          if (body) {
            var d = document.createElement("div");
            d.className = "cline street";
            d.textContent = line;
            body.appendChild(d);
            body.scrollTop = body.scrollHeight;
          }
        } catch (e) {
        }
      }
      function raiseAt(i) {
        seedDead();
        var b = book();
        var rec = b.dead[i];
        if (!rec) {
          say("That page is blank.", "bad");
          return false;
        }
        if (rec.raised) {
          say(rec.n + " already came back. The name will not take a second time.", "bad");
          return false;
        }
        if (typeof addFish !== "function") {
          say("The water would not hold them.", "bad");
          return false;
        }
        var f = addFish(spIndex(rec.sp), 2);
        if (!f) {
          say("The tank could not hold another. Empty a space first.", "bad");
          return false;
        }
        f.nick = rec.n;
        f.risen = 1;
        f.scar = 1;
        f.ill = "the returning";
        f.hunger = 0.4;
        try {
          if (typeof initBrain === "function") initBrain(f);
        } catch (e) {
        }
        f.brain = f.brain || {};
        f.brain.stress = 0.82;
        f.brain.trust = 0.05;
        f.brain.state = "remembering the other side";
        try {
          if (typeof pedRename === "function") pedRename(f);
        } catch (e2) {
        }
        rec.raised = 1;
        rec.raisedY = worldYear();
        var o = occ();
        o.raised = (o.raised || 0) + 1;
        o.last = Date.now();
        o.known = 1;
        try {
          var g = gs();
          if (g) {
            g.stats = g.stats || {};
            g.stats.raised = (g.stats.raised || 0) + 1;
          }
        } catch (eSt) {
        }
        try {
          if (typeof findEgg === "function") findEgg("raised", rec.n + " is in the tank. Something about the eyes is wrong.");
        } catch (eEgg) {
        }
        try {
          if (typeof addRep === "function") addRep(-5);
        } catch (e3) {
        }
        try {
          if (window.saga && saga.remember) {
            saga.remember(f, "rite", "the other side");
            var list = typeof allFish === "function" ? allFish() : [];
            for (var n = 0; n < list.length; n++) {
              if (list[n] && list[n] !== f) saga.remember(list[n], "rite", rec.n + " walking the water again");
            }
          }
        } catch (e4) {
        }
        write(
          "disaster",
          rec.n + " the " + String(rec.sp || "fish").toLowerCase() + " was called back from the pages. The water accepted them, then thought better of it.",
          [f]
        );
        say(rec.n + " is in the tank. Something about the eyes is wrong.", "bad");
        echo("The street did not hear a splash. People still crossed themselves.");
        try {
          window.__shopMood = { kind: "flee", until: (typeof performance !== "undefined" ? performance.now() / 1e3 : Date.now() / 1e3) + 12 };
        } catch (eM) {
        }
        try {
          if (window.feel && feel.play) feel.play("warn");
        } catch (eF) {
        }
        try {
          if (window.weave && weave.rumor) weave.rumor("risen", rec.n + " came back", 2.2);
        } catch (eR) {
        }
        try {
          if (typeof townState === "function") {
            var t = townState();
            if (t && typeof t.word === "number") t.word = Math.min(3, (t.word || 0) + 0.4);
          }
        } catch (e5) {
        }
        paintPages();
        return true;
      }
      function pagesHtml() {
        seedDead();
        var b = book();
        var o = occ();
        var html = '<p class="necro-lead">The leather is wet. The name on the board is not English. Names written here do not stay on the far side if you read them aloud.</p>';
        html += '<p class="necro-meta">Read ' + (o.reads || 0) + " time" + ((o.reads || 0) === 1 ? "" : "s") + " · " + (o.raised || 0) + " called back</p>";
        var list = b.dead || [];
        if (!list.length) {
          html += '<p class="necro-empty">The pages are still blank. Nothing of yours has died, and the old names have not found you yet.</p>';
          return html;
        }
        for (var i = list.length - 1; i >= 0; i--) {
          var d = list[i];
          html += '<div class="necro-entry' + (d.raised ? " risen" : "") + '">';
          html += '<div class="necro-name">' + esc(d.n) + " · " + esc(d.sp || "fish") + (d.y ? " · " + d.y : "") + "</div>";
          html += '<div class="necro-how">' + esc(d.how || "died") + (d.last ? " · last: “" + esc(d.last) + "”" : "") + "</div>";
          if (d.raised) html += '<div class="necro-back">Already walking.</div>';
          else
            html += '<button type="button" class="buy danger necro-raise" data-raise="' + i + '">Read the name aloud</button>';
          html += "</div>";
        }
        return html;
      }
      function paintPages() {
        var el = document.getElementById("necroPages");
        if (el) el.innerHTML = pagesHtml();
      }
      function openBook() {
        seedDead();
        var o = occ();
        o.known = 1;
        o.reads = (o.reads || 0) + 1;
        try {
          if (typeof findEgg === "function") findEgg("necro", "The salt in the binding is older than the shop.");
        } catch (eEgg) {
        }
        if (o.reads === 1) {
          write("era", "The Necronomicon was opened in the back room. The salt in the binding is older than the shop.");
        }
        var wrap = document.getElementById("necro");
        if (!wrap) {
          wrap = document.createElement("div");
          wrap.id = "necro";
          wrap.innerHTML = '<div class="necro-card" role="dialog" aria-label="Necronomicon"><button type="button" class="necro-x" id="necroX" aria-label="Close">Close</button><div class="necro-cover"><img src="art/necronomicon.jpg" alt="The Necronomicon"></div><div class="necro-body"><h2>Necronomicon</h2><div id="necroPages"></div></div></div>';
          document.body.appendChild(wrap);
          wrap.addEventListener("click", function(e) {
            if (e.target === wrap) closeBook();
          });
          wrap.addEventListener("click", function(e) {
            var btn = e.target.closest("[data-raise]");
            if (!btn) return;
            raiseAt(+btn.getAttribute("data-raise"));
          });
          var x = document.getElementById("necroX");
          if (x) x.onclick = closeBook;
        }
        paintPages();
        wrap.classList.add("show");
        open = true;
      }
      function closeBook() {
        var wrap = document.getElementById("necro");
        if (wrap) wrap.classList.remove("show");
        open = false;
      }
      function syncHot() {
        var s = lastScene;
        try {
          if (typeof sceneNow === "function") s = sceneNow() || s;
          else if (typeof gameState === "object" && gameState && gameState.scene) s = String(gameState.scene);
          else if (window.G && G.scene) s = String(G.scene);
        } catch (e) {
        }
        var hot = document.getElementById("necroHot");
        if (!hot) {
          hot = document.createElement("button");
          hot.id = "necroHot";
          hot.type = "button";
          hot.textContent = "A bound book";
          hot.title = "The Necronomicon";
          hot.onclick = function(e) {
            e.preventDefault();
            e.stopPropagation();
            openBook();
          };
          document.body.appendChild(hot);
        }
        var show = s === "back" && !document.body.classList.contains("titling");
        hot.classList.toggle("show", !!show);
      }
      function wrapGo() {
        if (didGo || !window.__finsGo) return;
        didGo = true;
        var orig = window.__finsGo;
        window.__finsGo = function(e) {
          lastScene = String(e || "");
          var r = orig.apply(this, arguments);
          syncHot();
          return r;
        };
      }
      function wrapBrowse() {
        if (didBrowse || !window.shopBrowse) return;
        didBrowse = true;
        var orig = window.shopBrowse;
        window.shopBrowse = function() {
          var rec = orig.apply(this, arguments);
          try {
            var o = occ();
            if (rec && o.raised && Date.now() - (o.last || 0) < 8 * 60 * 1e3 && rec.phase === "look" && Math.random() < 0.28) {
              rec.line = ["That tank.", "Did that one die?", "I'm not buying anything that came back.", "The water smells like a church."][rec.idx % 4];
            }
          } catch (e) {
          }
          return rec;
        };
      }
      function isCmd(t) {
        t = String(t || "").replace(/^\/?act\s+/i, "");
        return /necronomicon|raise the dead|raise .*dead|read the book|open the book|the occult|call .* back/i.test(t);
      }
      function runCmd(raw) {
        var t = String(raw || "").replace(/^\/?act\s+/i, "").trim();
        try {
          var body = document.getElementById("conBody");
          if (body) {
            var you = document.createElement("div");
            you.className = "cline you";
            you.textContent = "> " + raw;
            body.appendChild(you);
          }
        } catch (e) {
        }
        if (/raise/.test(t)) {
          seedDead();
          var b = book();
          var idx = -1;
          for (var i = b.dead.length - 1; i >= 0; i--) {
            if (!b.dead[i].raised) {
              idx = i;
              break;
            }
          }
          if (idx < 0) {
            echo("Every name in the book is already walking, or the pages are blank.");
            say("No name left to call.", "bad");
            return;
          }
          openBook();
          raiseAt(idx);
          return;
        }
        occ().known = 1;
        openBook();
        echo("You open the book. The leather is wet.");
      }
      function enhanceChron() {
        var body = document.getElementById("dbody");
        var title = document.getElementById("dtitle");
        if (!body || !title) return;
        if (!/chronicle/i.test(title.textContent || "")) return;
        if (body.querySelector(".saga-necro")) return;
        var box = document.createElement("div");
        box.className = "saga-necro";
        var o = occ();
        box.innerHTML = '<div class="sec">The Necronomicon</div><div class="row"><div></div><div><div class="n">A bound book from the wreck</div><div class="d">' + (o.known ? "Opened in the back room. Names in it can be read aloud." : "It was in a crate that came off the battery. The leather is wet. It does not belong on a shop shelf.") + '</div></div><div><button type="button" class="buy danger" id="openNecro">Open it</button></div></div>';
        body.insertBefore(box, body.firstChild);
        var btn = document.getElementById("openNecro");
        if (btn) btn.onclick = openBook;
      }
      document.addEventListener(
        "keydown",
        function(e) {
          if (e.key === "Escape" && open) {
            closeBook();
            e.stopPropagation();
            return;
          }
          if (e.key !== "Enter") return;
          var a = e.target;
          if (!a || a.id !== "conIn") return;
          var r = a.value.trim();
          if (!isCmd(r)) return;
          e.preventDefault();
          e.stopImmediatePropagation();
          a.value = "";
          runCmd(r);
        },
        true
      );
      function tick() {
        try {
          seedDead();
          wrapGo();
          wrapBrowse();
          syncHot();
          enhanceChron();
        } catch (e) {
        }
      }
      window.occult = { open: openBook, raise: raiseAt, close: closeBook };
      if (window.__onBeat) window.__onBeat(tick, 280);
      else setTimeout(function loop() {
        tick();
        setTimeout(loop, 280);
      }, 280);
    })();
  } catch (e) {
    console.error("Layer occult.js failed to start:", e);
  }

  // src/layers/stock.js
  try {
    (function() {
      "use strict";
      var CATALOG = [
        {
          id: "cond",
          cat: "Water",
          name: "Water conditioner",
          pack: 4,
          cost: 48,
          unit: "bottle",
          blurb: "Takes the chlorine out. The next change costs less and the fish notice less.",
          lot: "Revere bottling, lot"
        },
        {
          id: "floss",
          cat: "Water",
          name: "Filter floss",
          pack: 3,
          cost: 22,
          unit: "pad",
          blurb: "Spare pads. When the filter clogs, you pull one and the flow comes back. The boards dry a bit while you walk the aisle.",
          lot: "Chelsea mill, lot"
        },
        {
          id: "almond",
          cat: "Water",
          name: "Catappa leaves",
          pack: 8,
          cost: 16,
          unit: "leaf",
          blurb: "Indian almond. They tan the water, drop the pH a hair, and timid fish sit easier.",
          lot: "dried in Lynn, lot"
        },
        {
          id: "carbon",
          cat: "Water",
          name: "Carbon pouches",
          pack: 2,
          cost: 28,
          unit: "pouch",
          blurb: "Pulls the yellow out of a tank that has been left. Lasts a few days.",
          lot: "activated in Everett, lot"
        },
        {
          id: "starter",
          cat: "Water",
          name: "Bottled bacteria",
          pack: 1,
          cost: 54,
          unit: "vial",
          blurb: "Seed for a tank you just changed. The column settles faster than it has a right to.",
          lot: "kept cold, lot"
        },
        {
          id: "salt",
          cat: "Water",
          name: "Aquarium salt",
          pack: 2,
          cost: 18,
          unit: "tin",
          blurb: "White-spot hates it. The street notices if you treated after a fish bag went out sick.",
          lot: "evaporated in Revere, lot"
        },
        {
          id: "brine",
          cat: "Feed",
          name: "Frozen brine shrimp",
          pack: 3,
          cost: 32,
          unit: "cube",
          blurb: "A treat. They come to the glass. Trust moves a little.",
          lot: "San Francisco bay, lot"
        },
        {
          id: "worms",
          cat: "Feed",
          name: "Bloodworm cubes",
          pack: 3,
          cost: 44,
          unit: "cube",
          blurb: "Richer than brine. Bettas and gouramis act like they remember a river.",
          lot: "vacuum packed, lot"
        },
        {
          id: "wafers",
          cat: "Feed",
          name: "Sinking wafers",
          pack: 6,
          cost: 20,
          unit: "wafer",
          blurb: "For whoever lives on the gravel. The rest ignore them.",
          lot: "baked in Quincy, lot"
        },
        {
          id: "bags",
          cat: "Counter",
          name: "Paper fish bags",
          pack: 25,
          cost: 14,
          unit: "fish bag",
          blurb: "What a fish goes home in. People notice when you still use a grocery sack.",
          lot: "twisted necks, lot"
        },
        {
          id: "glass",
          cat: "Counter",
          name: "Window wash",
          pack: 1,
          cost: 9,
          unit: "bottle",
          blurb: "Vinegar and newsprint. The storefront reads. Walk-ins mention it.",
          lot: "mixed this morning, lot"
        },
        {
          id: "mat",
          cat: "Counter",
          name: "Rubber doormat",
          pack: 1,
          cost: 36,
          unit: "mat",
          unique: 1,
          blurb: "The harbor comes in on people's shoes. This is where it stays.",
          lot: "cut in Somerville, lot"
        },
        {
          id: "chalk",
          cat: "Counter",
          name: "Counter chalk",
          pack: 4,
          cost: 6,
          unit: "stick",
          blurb: "Names on the wall. The unburied go quiet when you write them. The board also reads from the door.",
          lot: "soft white, lot"
        }
      ];
      var EXTRA_CURIOS = [
        { id: "sponge", name: "Spare sponge", desc: "Filter clogs come 15% less often.", rar: 0, eff: { clog: 0.15 } },
        { id: "leafjar", name: "Jar of catappa", desc: "Fish get hungry 8% slower.", rar: 0, eff: { hunger: 0.08 } },
        { id: "cscale", name: "Counter scale", desc: "Selling fish on the open market pays 8% more.", rar: 0, eff: { sell: 0.08 } },
        { id: "apron", name: "Waxed apron", desc: "Customers come 10% more often.", rar: 0, eff: { orderFreq: 0.1 } },
        { id: "twine", name: "Ball of twine", desc: "Wild catches 8% more likely.", rar: 0, eff: { net: 0.08 } },
        { id: "wick", name: "Spare heater wick", desc: "Ich outbreaks come 12% less often.", rar: 0, eff: { outbreak: 0.12 } },
        { id: "bubwand", name: "Bubble wand", desc: "Coins are worth 5% more.", rar: 0, eff: { coinVal: 0.05 } },
        { id: "soap", name: "Pumice soap", desc: "Medication is 12% cheaper.", rar: 0, eff: { dose: 0.12 } },
        { id: "clip", name: "Plant clip tin", desc: "Algae grows 15% faster.", rar: 0, eff: { algae: 0.15 } },
        { id: "bell2", name: "Brass desk bell", desc: "Orders pay 7% more.", rar: 0, eff: { orderPay: 0.07 } }
      ];
      var EXTRA_GEAR = [
        {
          id: "signlamp",
          name: "Window sign lamps",
          max: 3,
          tech: null,
          desc: function(e) {
            return e ? "The sign on Salem Street has " + e + " lamp" + (e === 1 ? "" : "s") + " on it. People find you in the rain." : "A pair of lamps aimed at the FIN'S sign. Walk-ins in weather.";
          },
          cost: function(e) {
            return { coins: Math.round(220 * Math.pow(2.15, e)) };
          }
        },
        {
          id: "backsink",
          name: "Utility sink",
          max: 2,
          tech: null,
          desc: function(e) {
            return e ? "Water changes run " + e * 18 + "% cheaper. The buckets live here, not on the floor." : "A deep sink in the back. Conditioner and salt have a place.";
          },
          cost: function(e) {
            return { coins: Math.round(480 * Math.pow(2.4, e)) };
          }
        },
        {
          id: "corkboard",
          name: "Shop corkboard",
          max: 2,
          tech: null,
          desc: function(e) {
            return e ? "Prices, hours, and a lost-cat flyer. Regulars read it. Customers come a little more." : "A corkboard by the door. The street likes to know what a place is doing.";
          },
          cost: function(e) {
            return { coins: Math.round(160 * Math.pow(2.2, e)) };
          }
        }
      ];
      var seeded = false;
      var lastUi = 0;
      var didBrowse = false;
      function gs() {
        try {
          if (typeof gameState === "function") return gameState();
          if (gameState && typeof gameState === "object" && gameState.coins != null) return gameState;
        } catch (e) {
        }
        return null;
      }
      function counter() {
        var g = gs();
        if (!g) return { qty: {}, used: {}, lot: {}, wet: 0, glass: 0, bags: 0 };
        if (!g.counter || typeof g.counter !== "object") g.counter = { qty: {}, used: {}, lot: {}, wet: 0, glass: 0, bags: 0 };
        g.counter.qty = g.counter.qty || {};
        g.counter.used = g.counter.used || {};
        return g.counter;
      }
      function say(msg, kind) {
        try {
          if (typeof toastShow === "function") toastShow(msg, kind || "good");
        } catch (e) {
        }
      }
      function write(kind, text) {
        try {
          var y = worldYear();
          var line = String(text || "");
          if (line && !/^Year\s+\d/.test(line)) line = "Year " + y + ". " + line;
          if (typeof chronicle === "function") chronicle(kind, line, []);
        } catch (e) {
        }
      }
      function worldYear() {
        try {
          if (window.saga && typeof saga.year === "function") return saga.year();
          if (typeof atlGen === "function") {
            var a = atlGen();
            if (a && a.now) return a.now | 0;
          }
        } catch (e) {
        }
        return 1e3;
      }
      function lotOf(it) {
        var c = counter();
        if (c.lot[it.id]) return c.lot[it.id];
        var n = 10 + (it.id.charCodeAt(0) * 17 + (c.used[it.id] || 0) * 3) % 80;
        var line = it.lot + " " + n + " · year " + worldYear();
        c.lot[it.id] = line;
        return line;
      }
      function spend(n) {
        var g = gs();
        n = Math.round(+n || 0);
        if (!g) return false;
        if (n <= 0) return true;
        if ((g.coins || 0) < n) return false;
        g.coins -= n;
        return true;
      }
      function till() {
        var g = gs();
        return g && g.coins != null ? g.coins : 0;
      }
      function byId(id) {
        for (var i = 0; i < CATALOG.length; i++) if (CATALOG[i].id === id) return CATALOG[i];
        return null;
      }
      function rememberFish(text) {
        try {
          if (!window.saga || !saga.remember || typeof allFish !== "function") return;
          var list = allFish() || [];
          for (var i = 0; i < list.length; i++) saga.remember(list[i], "stock", text);
        } catch (e) {
        }
      }
      function tintWater(kind, dPh) {
        try {
          if (typeof tankWater !== "function") return;
          var w = tankWater(0) || tankWater(1);
          if (!w) return;
          if (kind) w.kind = kind;
          if (dPh && w.ph != null) w.ph = Math.max(5.8, Math.min(8.4, w.ph + dPh));
        } catch (e) {
        }
      }
      function useItem(id) {
        var it = byId(id);
        var c = counter();
        if (!it) return;
        if ((c.qty[id] || 0) < 1) {
          say("You are out of " + it.name.toLowerCase() + ".", "bad");
          return;
        }
        if (it.unique && (c.used[id] || 0) && id === "mat") {
          say("The mat is already down.", "");
          return;
        }
        c.qty[id] -= 1;
        c.used[id] = (c.used[id] || 0) + 1;
        if (id === "cond") {
          c.wet = (c.wet || 0) + 1;
          say("One bottle in the bucket. The next change will go easier.");
        } else if (id === "floss") {
          try {
            clogged = false;
          } catch (e) {
          }
          try {
            if (typeof cleanFilter === "function") cleanFilter(true);
          } catch (e2) {
          }
          say("Pad swapped. The outflow is a column again.");
          write("note", "The filter was clogged. A spare pad brought the flow back.");
          rememberFish("the filter running again");
          try {
            if (window.shopSite && shopSite.dry) shopSite.dry(0.55);
          } catch (eDry) {
          }
        } else if (id === "almond") {
          tintWater("the leaf tea", -0.25);
          rememberFish("the tannin in the water");
          say("Leaves on the surface. The water went the color of tea.");
        } else if (id === "carbon") {
          tintWater("the polished mix", 0);
          say("Pouch in the filter. The yellow is already leaving.");
        } else if (id === "starter") {
          tintWater("the seeded mix", 0);
          rememberFish("new water that did not sting");
          say("The vial went in. Give it a day.");
          write("note", "Bottled bacteria went into a tank that had just been changed.");
          try {
            if (window.fever && fever.treat) fever.treat();
          } catch (eFv) {
          }
        } else if (id === "salt") {
          rememberFish("the salt in the water");
          say("Salt in the column. White-spot hates it.");
        } else if (id === "brine") {
          treat(0.22, 0.06, "the frozen shrimp");
          say("Cubes thawed. They came up for them.");
        } else if (id === "worms") {
          treat(0.28, 0.08, "the bloodworms");
          say("The water went pink for a second. Nobody stayed on the gravel.");
        } else if (id === "wafers") {
          treatBottom("the sinking wafer");
          say("Wafers on the sand. The ones who live down there found them.");
        } else if (id === "bags") {
          c.bags = (c.bags || 0) + 8;
          say("A stack of fish bags under the counter.");
        } else if (id === "glass") {
          c.glass = Date.now() + 18 * 60 * 1e3;
          say("The storefront is a mirror. Give it an hour of street dust.");
        } else if (id === "mat") {
          say("Mat down. The harbor can stay on it.");
        } else if (id === "chalk") {
          say("The board is readable from the door.");
        }
        redraw();
      }
      function treat(hunger, trust, mem) {
        try {
          if (typeof allFish !== "function") return;
          var list = allFish() || [];
          for (var i = 0; i < list.length; i++) {
            var f = list[i];
            if (!f) continue;
            f.hunger = Math.min(1, (f.hunger == null ? 0.5 : f.hunger) + hunger);
            if (f.brain) {
              f.brain.trust = Math.min(1, (f.brain.trust || 0) + trust);
              f.brain.stress = Math.max(0, (f.brain.stress || 0) - 0.08);
            }
            try {
              if (window.mind && mind.of) {
                var m = mind.of(f);
                if (m) {
                  m.need = m.need || {};
                  m.need.food = Math.min(1, (m.need.food || 0.5) + 0.25);
                  m.stress = Math.max(0, (m.stress || 0) - 0.1);
                }
              }
            } catch (eM) {
            }
            if (window.saga && saga.remember) saga.remember(f, "feed", mem);
          }
        } catch (e) {
        }
      }
      function treatBottom(mem) {
        try {
          var list = typeof allFish === "function" ? allFish() || [] : [];
          for (var i = 0; i < list.length; i++) {
            var f = list[i];
            if (!f) continue;
            var low = (f.y || 0) > 0.55 || /cory|loach|pleco|catfish|khuli|otocinclus/i.test(String(f.sp || ""));
            if (!low) continue;
            f.hunger = Math.min(1, (f.hunger == null ? 0.5 : f.hunger) + 0.3);
            if (window.saga && saga.remember) saga.remember(f, "feed", mem);
          }
        } catch (e) {
        }
      }
      function buyItem(id) {
        var it = byId(id);
        var c = counter();
        if (!it) return;
        if (it.unique && (c.qty[id] || 0) + (c.used[id] || 0) > 0 && id === "mat") {
          say("You already have a mat.", "");
          return;
        }
        if (!spend(it.cost)) {
          say("Not enough in the till for " + it.name.toLowerCase() + ".", "bad");
          return;
        }
        c.qty[id] = (c.qty[id] || 0) + it.pack;
        c.lot[it.id] = it.lot + " " + (14 + c.qty[id] * 9 % 70) + " · year " + worldYear();
        say("Bought " + it.pack + " " + it.unit + (it.pack === 1 ? "" : "s") + " of " + it.name.toLowerCase() + ".");
        write("note", "A case of " + it.name.toLowerCase() + " came in from the harbor mill.");
        if (id === "mat") useItem("mat");
        if ((c.used.cond || 0) + (c.qty.cond || 0) === it.pack && id === "cond") {
          write("note", "The first case of conditioner came in. The tap water stopped being an argument.");
        }
        redraw();
      }
      function esc(s) {
        var map = { "&": "&amp;", "<": "&lt;", ">": "&gt;", '"': "&quot;" };
        return String(s || "").replace(/[&<>"]/g, function(ch) {
          return map[ch];
        });
      }
      function rowsHtml() {
        var c = counter();
        var diary = [];
        if (c.wet) diary.push("conditioner waiting on the next change");
        if (c.bags) diary.push(c.bags + " fish bags under the till");
        if (c.glass && Date.now() < c.glass) diary.push("the storefront still catching the light");
        if (c.used.almond) diary.push("tannin in the column");
        if (c.used.starter) diary.push("seeded water");
        if (c.used.floss) diary.push("a fresh pad in the filter");
        var html = '<div class="sec">Harbor Supply <span>what the tanks actually use</span></div><div class="note">Packed in Revere, Chelsea, Lynn. Dated to the millennial clock. Bottles empty. Pads pack. Cubes thaw.</div>';
        html += '<div class="note">Year ' + worldYear() + " on the shelf.</div>";
        if (diary.length) {
          html += '<div class="note counter-diary">On the counter: ' + esc(diary.join(" · ")) + ".</div>";
        }
        var cat = "";
        for (var i = 0; i < CATALOG.length; i++) {
          var it = CATALOG[i];
          if (it.cat !== cat) {
            cat = it.cat;
            html += '<div class="sec counter-cat">' + esc(cat) + "</div>";
          }
          var q = c.qty[it.id] || 0;
          var used = c.used[it.id] || 0;
          var can = till() >= it.cost;
          html += '<div class="row counter-row">';
          html += '<div class="counter-mark">' + (q ? q : "·") + "</div>";
          html += '<div><div class="n">' + esc(it.name) + "</div>";
          html += '<div class="d">' + esc(it.blurb) + "</div>";
          html += '<div class="d counter-lot">' + esc(lotOf(it));
          if (q) html += " · <b>" + q + "</b> on the shelf";
          if (used) html += " · " + used + " used";
          html += "</div></div>";
          html += '<div class="qty">';
          html += '<button type="button" class="buy" data-stock-buy="' + it.id + '"' + (can ? "" : " disabled") + ">" + it.pack + " · $" + it.cost + "</button>";
          html += '<button type="button" class="buy ghost" data-stock-use="' + it.id + '"' + (q ? "" : " disabled") + ">Use</button>";
          html += "</div></div>";
        }
        return html;
      }
      function isShopTab() {
        var t = document.getElementById("dtitle");
        var s = (t && t.textContent || "").replace(/\s+/g, " ").trim();
        return /^shop\b/i.test(s);
      }
      function enhanceShop() {
        if (!isShopTab()) return;
        var body = document.getElementById("dbody");
        if (!body) return;
        var box = body.querySelector(".counter-stock");
        if (!box) {
          box = document.createElement("div");
          box.className = "counter-stock";
          try {
            body.insertBefore(box, body.firstChild);
          } catch (e) {
            try {
              body.appendChild(box);
            } catch (e2) {
            }
          }
        }
        var html = rowsHtml();
        if (box.getAttribute("data-h") !== String(html.length) + till()) {
          box.innerHTML = html;
          box.setAttribute("data-h", String(html.length) + till());
        }
      }
      function redraw() {
        lastUi = 0;
        try {
          if (typeof refreshDrawer === "function") refreshDrawer();
        } catch (e) {
        }
      }
      function seedExtras() {
        if (seeded) return;
        try {
          if (typeof ITEMS !== "undefined" && ITEMS && ITEMS.push) {
            var have = {};
            for (var i = 0; i < ITEMS.length; i++) have[ITEMS[i].id] = 1;
            for (var j = 0; j < EXTRA_CURIOS.length; j++) {
              if (!have[EXTRA_CURIOS[j].id]) ITEMS.push(EXTRA_CURIOS[j]);
            }
          }
        } catch (e) {
        }
        try {
          if (typeof UPG !== "undefined" && UPG && UPG.push) {
            var uh = {};
            for (var u = 0; u < UPG.length; u++) uh[UPG[u].id] = 1;
            for (var g = 0; g < EXTRA_GEAR.length; g++) {
              if (!uh[EXTRA_GEAR[g].id]) UPG.push(EXTRA_GEAR[g]);
            }
          }
        } catch (e2) {
        }
        seeded = true;
      }
      function wrapWater() {
        try {
          let wrappedGet = function() {
            var fn = origGet();
            if (fn && !fn.__stock) {
              var inner = function() {
                var c = counter();
                var g = gs();
                var sink = g && g.up ? g.up.backsink || 0 : 0;
                var r = fn.apply(this, arguments);
                try {
                  var used = false;
                  if (c.wet > 0) {
                    c.wet -= 1;
                    used = true;
                  } else if ((c.qty.cond || 0) > 0) {
                    c.qty.cond -= 1;
                    used = true;
                  }
                  if (used) {
                    if (g && typeof g.coins === "number") {
                      g.coins += Math.round(10 + sink * 8);
                    }
                    say("Conditioner in the new water.");
                    rememberFish("water that did not bite");
                  } else if (sink) {
                    rememberFish("a quiet change");
                  }
                } catch (e) {
                }
                return r;
              };
              inner.__stock = 1;
              return inner;
            }
            return fn;
          };
          var desc = Object.getOwnPropertyDescriptor(globalThis, "waterChange");
          if (!desc || !desc.get || desc.get.__stock) return;
          var origGet = desc.get;
          wrappedGet.__stock = 1;
          Object.defineProperty(globalThis, "waterChange", { configurable: true, get: wrappedGet });
        } catch (e) {
        }
      }
      function wrapSale() {
        try {
          let wrappedGet = function() {
            var fn = origGet();
            if (fn && !fn.__bags) {
              var inner = function() {
                var r = fn.apply(this, arguments);
                var c = counter();
                if (c.bags > 0) {
                  c.bags -= 1;
                  try {
                    if (typeof addRep === "function") addRep(0.15);
                  } catch (e) {
                  }
                }
                return r;
              };
              inner.__bags = 1;
              return inner;
            }
            return fn;
          };
          var desc = Object.getOwnPropertyDescriptor(globalThis, "townSale");
          if (!desc || !desc.get || desc.get.__bags) return;
          var origGet = desc.get;
          wrappedGet.__bags = 1;
          Object.defineProperty(globalThis, "townSale", { configurable: true, get: wrappedGet });
        } catch (e) {
        }
      }
      function wrapBrowse() {
        if (didBrowse || !window.shopBrowse) return;
        didBrowse = true;
        var orig = window.shopBrowse;
        window.shopBrowse = function() {
          var rec = orig.apply(this, arguments);
          try {
            if (!rec) return rec;
            var c = counter();
            var g = gs();
            if (c.glass && Date.now() < c.glass && rec.phase === "enter" && Math.random() < 0.3) {
              rec.line = ["You can read the sign from the corner.", "Glass is clean.", "Looked open."][rec.idx % 3];
            }
            if (g && g.up && g.up.signlamp && rec.phase === "enter" && Math.random() < 0.2) {
              rec.line = ["Saw the sign in the rain.", "The lamps.", "Couldn't miss it."][rec.idx % 3];
            }
          } catch (e) {
          }
          return rec;
        };
      }
      document.addEventListener("click", function(e) {
        var buy = e.target.closest("[data-stock-buy]");
        var use = e.target.closest("[data-stock-use]");
        if (buy) {
          e.preventDefault();
          e.stopPropagation();
          buyItem(buy.getAttribute("data-stock-buy"));
        } else if (use) {
          e.preventDefault();
          e.stopPropagation();
          useItem(use.getAttribute("data-stock-use"));
        }
      });
      function tick() {
        try {
          seedExtras();
          wrapWater();
          wrapSale();
          wrapBrowse();
          if (performance.now() - lastUi > 120) {
            lastUi = performance.now();
            enhanceShop();
          }
        } catch (e) {
        }
      }
      window.stock = { catalog: CATALOG, buy: buyItem, use: useItem, counter };
      if (window.__onBeat) window.__onBeat(tick, 280);
      else setTimeout(function loop() {
        tick();
        setTimeout(loop, 280);
      }, 280);
    })();
  } catch (e) {
    console.error("Layer stock.js failed to start:", e);
  }

  // src/layers/know.js
  try {
    (function() {
      "use strict";
      var lastUi = 0;
      var mode = "shop";
      var query = "";
      var seeded = false;
      var painting = false;
      var openId = "";
      var seen = /* @__PURE__ */ Object.create(null);
      var factCache = null;
      var EXTRA = [
        {
          id: "k_hour",
          sec: "When you are stuck",
          t: "The first hour",
          tags: "start stuck beginner first hour pellets filter tank shop gold line letter",
          w: "<p>You open in Year {year}. The tanks are already running. Click the water to feed. If the filter pill goes sour, click it. That is the whole first hour, and the rest of the shop is waiting on it.</p><p>People come in off Salem Street. They will look. They will ask for a fish by name. They only buy if you keep two adults of the same kind.</p><p>The gold line under the name is the unfinished thing. This morning it is often a letter. {letter}</p><p><b>What to do about it:</b> feed, watch the water, do not sell the last of a pair, and read the gold line before you click anything else.</p>"
        },
        {
          id: "k_nobuy",
          sec: "When you are stuck",
          t: "Nobody is buying",
          tags: "stuck walkins till stock pair HA sales customers want",
          w: "<p>Walk-ins are not sales. Someone comes in because they have need and a bit of cash. They buy if the shop has two grown fish of one kind and the water is not off. Otherwise they look and leave, and Life writes it on the till: walked, not sold.</p><p>If they asked for a betta and you do not keep two, that is the lesson. A unique fish is a display. A pair is stock.</p><p><b>What to do about it:</b> buy or breed a pair, clean the filter, and be open at lunch. Reputation and ads pull from further down the street. A shop nobody has heard of is a neighborhood shop.</p>"
        },
        {
          id: "k_water",
          sec: "When you are stuck",
          t: "The water is going off",
          tags: "stuck filter clog sick ich cycle heater filter clogged",
          w: "<p>A clogged filter is the shop turning people around at the door. Sick fish make the ones who stay talk. Ich is an outbreak, not a mood. The nitrogen cycle does not care that you were in the Atlas.</p><p>{filter}</p><p><b>What to do about it:</b> click the clog pill on the HUD. Feed less if ammonia is up. Heat if the room is following a cold street. The Calendar tells you the season; winter will punish an unheated tank.</p>"
        },
        {
          id: "k_broke",
          sec: "When you are stuck",
          t: "The till is thin",
          tags: "stuck coins money orders sell exchange levy bills",
          w: "<p>Coins come from sales, orders, and the odd curiosity. Bills do not wait. Selling the last of a pair to make rent is how the till stays quiet tomorrow.</p><p>Today the till has {coins} on it. Sold {sales}. Walked {walked}.</p><p><b>What to do about it:</b> fill an order if one is on the book. Check the Exchange before you dump a group. Standing orders pay slowly and do not empty the rack. The Money tab is where the levy lives, not a surprise.</p>"
        },
        {
          id: "k_bored",
          sec: "When you are stuck",
          t: "When the shop is running itself",
          tags: "bored curious atlas chronicle name secrets map boat book almanac",
          w: "<p>If the water is holding and people are still looking, the game is not empty. It is waiting for you to read it. Fish have names and thoughts. The street has regulars. The Atlas has a thousand years. The back room has a book that should not be there.</p><p>This shop, this seed, this year: {year}. {named} named in the water. {age}</p><p><b>What to do about it:</b> open Guide, This shop. Name a fish. Open Life. Walk the Map. Open the Chronicle. Click a keep on the Atlas.</p>"
        },
        {
          id: "k_till",
          sec: "The shop floor",
          t: "The till today",
          tags: "till sales walked want regulars life lockup",
          w: "<p>Life keeps a till for the day: sold, walked, and who asked for what. A walk-out with a name is the shop teaching you. Lock the door after last hour and the chronicle writes the tally. Year of the millennial clock goes on the line with it.</p><p>This morning: sold {sales}, walked {walked}. Regulars come back. The second sale is the one that puts them on the book.</p><p><b>What to do about it:</b> read Life when the floor is quiet. If walked is beating sold, you are short a pair or the water is off.</p>"
        },
        {
          id: "k_site",
          sec: "The shop floor",
          t: "The site",
          tags: "site physics tiles heat water leak filter oak boards aisle gravity daybook",
          w: "<p>The shop is a place. Oak boards, glass, iron, standing water. Heat moves. A clogged filter leaks downhill. Rain at the door wets the aisle. Life shows the site as a little map because the building is not a menu.</p><p><b>What to do about it:</b> clean the filter before the boards go dark. The gold line will say it. The daybook will keep it.</p>"
        },
        {
          id: "k_want",
          sec: "The shop floor",
          t: "What they came for",
          tags: "want betta guppy tetra stock pair floor speech coming",
          w: "<p>People on the floor ask for a fish by name. If you have two of that fish they will say they will take it. If you do not, they ask whether you keep it, and then they leave.</p><p>{coming}</p><p><b>What to do about it:</b> keep pairs of what the street is asking for. A unique fish is a display. A pair is stock. The gold line names who is coming if someone is.</p>"
        },
        {
          id: "k_clock",
          sec: "The world before you",
          t: "Present day",
          tags: "year 1000 millennial clock calendar atlas history indefinite",
          w: "<p>Present day is Year {year}. A thousand years of the harbor already ran, and the clock does not stop. Seasons are twenty-eight days. A play day is twenty minutes unless you follow the real clock. HUD, Chronicle, lots, Life and the Atlas all use the same year.</p><p>The year does not run out. {age}</p><p><b>What to do about it:</b> the Calendar is the desk for this. The Atlas is the history. Watching a year turn is a secret.</p>"
        },
        {
          id: "k_necro",
          sec: "The world before you",
          t: "The Necronomicon",
          tags: "occult book dead raise back room necronomicon pages",
          w: "<p>There is a book in the back room. It is not a ledger. Names in it are dead fish the harbor still remembers. Calling a name back puts them in the tank with something wrong about the eyes. The street does not hear a splash. People still cross themselves.</p><p>Opening it is a secret. Raising is another. A risen fish is not stock. The choir comes back a half-step off.</p><p><b>What to do about it:</b> go Back, click the book. Do not do it because you are short on stock.</p>"
        },
        {
          id: "k_wx",
          sec: "The shop floor",
          t: "Weather on the glass",
          tags: "weather rain snow fog gale window shop",
          w: "<p>The shop window is the street. Rain runs on it. Fog takes the yellow house. A gale empties Salem Street. The room the tank sits in follows the outside with a lag, so a cold snap is a heater, and a heatwave is a tank you cannot cool.</p><p><b>What to do about it:</b> stand in the Shop and watch. Calendar has the numbers. A secret unlocks if you are in the shop while weather is on the glass.</p>"
        },
        {
          id: "k_secrets",
          sec: "You and your people",
          t: "Secrets and records",
          tags: "secrets eggs achievements records legendary tillwalk necro year",
          w: "<p>Secrets are hidden things: Konami, a name, midnight, the other book, a walk-out for a fish you did not have. Each one pays reputation and a pearl. Records on Level climb forever on numbers the shop already keeps: sales, years past the thousand, people who left without a fish bag, names called back.</p><p>Legendaries are six late things that survive a restart. None of them multiply income, because that would just make the game shorter.</p><p><b>What to do about it:</b> Secrets tab to see what you have. Level for the records. Do not hunt them with a list in another window. The shop tells you.</p>"
        },
        {
          id: "k_supply",
          sec: "Money",
          t: "Harbor Supply",
          tags: "stock conditioner bags gravel lots year shop tab fish bag",
          w: "<p>Harbor Supply is the shop tab's top: lots dated to the millennial year. Conditioner, fish bags, pads, cubes. They empty. They pack. They thaw. Buying them writes the year into the chronicle.</p><p>Customers do not come in for conditioner. That is you. A fish bag is not a receipt. It is a vector. What leaves in it can come back as fry, as fever, as a window on Salem.</p><p><b>What to do about it:</b> open Shop, read the lots, buy before a tank goes sour rather than after.</p>"
        },
        {
          id: "k_name",
          sec: "The animals",
          t: "Names and thoughts",
          tags: "name nick thoughts chronicle saga fish curious",
          w: "<p>Every fish gets a name. They think about the water, the weather, the year, who they swim with, and whether someone came back from the pages. Click a fish for its card. Life lists what they are thinking. The Chronicle is the chain of why.</p><p>In this water: {named}.</p><p><b>What to do about it:</b> name twenty and you have a secret. Read a thought, then open the Chronicle and see the line that put it there.</p>"
        },
        {
          id: "k_map",
          sec: "The quarter",
          t: "The street outside",
          tags: "map salem north end jobs crowd walkins bored baker window",
          w: "<p>The Map is the North End, not a circle of ants. People have homes, trades, faiths, and a reason to be on a block. Some of them walk to your door. Some go to Haymarket. Some are at sea.</p><p>{baker} {windows}</p><p><b>What to do about it:</b> open Map when the shop is quiet. Click a window. Life lists who has been in. A campaign is a bigger circle, not just a multiplier.</p>"
        },
        {
          id: "k_act",
          sec: "You and your people",
          t: "The console and /act",
          tags: "console act command gun street crowd",
          w: "<p>Slash opens the console. <kbd>/act</kbd> is you doing a thing in the world: the street notices, the floor notices, the water notices. A loud thing on Salem Street empties the shop. Kindness is cheaper and still shows.</p><p><b>What to do about it:</b> try a quiet act before a loud one. The Guide is not a list of acts. The street is the list.</p>"
        },
        {
          id: "k_guild",
          sec: "The shop floor",
          t: "The hall",
          tags: "guild dues standing mandate boycott till hall",
          w: "<p>There is a hall for people who sell living water. Dues, standing, a mandate pair. Fill it and they speak well. Miss the week and they put the word out. A boycott is an empty aisle.</p><p>{hall}</p><p><b>What to do about it:</b> keep the pair they asked for. Pay the week. Life will say if they have turned.</p>"
        },
        {
          id: "k_fever",
          sec: "The shop floor",
          t: "Fever",
          tags: "fever ich sick street heat treat clog beast",
          w: "<p>A sickness that walks the road. Heat, sometimes a name. People look from the door. The glass can catch it. A clogged filter feeds it. A beast off the harbor feeds it.</p><p>{sick}</p><p><b>What to do about it:</b> treat the water. Clean the filter. The gold line will say the name if it has one.</p>"
        },
        {
          id: "k_craft",
          sec: "The shop floor",
          t: "The hands",
          tags: "craft quality bag masterwork plaque combo till fish bag",
          w: "<p>A fish bag has quality. A tank has a plaque in a tongue spoken inland. String sales and the hands remember. A masterwork is someone who pays and tells the hall.</p><p>A day without a fish bag and the knot is sloppy. People say so. They walk. Treat with rusty water-hands and still-sickness comes back.</p><p><b>What to do about it:</b> do not break the run. The gold line says when a fish bag was good.</p>"
        },
        {
          id: "k_gold",
          sec: "The shop floor",
          t: "The gold line",
          tags: "gold line whisper unfinished letter till hud hook",
          w: "<p>One line under the name. Always one unfinished thing. A letter, a run, a wet aisle, a name due on the wall. It is not a quest log. It is the thing the shop will not let you forget.</p><p>This morning: {gold}</p><p>A letter wins the first morning. The rest of the street has to wait. If you answer the letter, the line can move.</p><p><b>What to do about it:</b> read it. Do that thing. Do not hunt a second HUD. There isn't one.</p>"
        },
        {
          id: "k_bag",
          sec: "The shop floor",
          t: "A fish bag",
          tags: "fish bag sale vector window fry fever stock pair",
          w: "<p>A fish bag is not a deletion. They go home with someone on Salem. The baker's window is a tank. If they live, she will say so. If they breed, she brings the fry. If they die on the block, the name goes in the book from the street, not the glass.</p><p>The fish bag carries the water. White-spot, gold-dust, fin-rot, still-sickness, salt-itch: each one hates a different bottle. Treat it, or the choir sours until the name breaks.</p><p><b>What to do about it:</b> keep two of a kind before you bag. Open Map after a sale. Their window is lit.</p>"
        },
        {
          id: "k_still",
          sec: "The animals",
          t: "Still-sickness",
          tags: "still-sickness still holding sick named remaining pair",
          w: "<p>A fish holding still is not always rest. Still-sickness is a name for the ones who stop because the other of the pair is gone, or because the water is wrong, or because a fight ended on the gravel.</p><p>{still}</p><p>The remaining of a pair holds still because the other one is in a window on Salem. That is not a meter. That is grief in the water.</p><p><b>What to do about it:</b> do not bag the last of a pair. Treat if a bottle will do it. Life will name who is holding still.</p>"
        },
        {
          id: "k_plate",
          sec: "The world before you",
          t: "A name on the wall",
          tags: "name on the wall plate chalk dead unburied ingum haunt",
          w: "<p>A name cannot stay in the book without writing. The dead want a name on the wall. Chalk writes it. The water goes quiet. You inherited the unburied.</p><p>{unburied}</p><p>Ingum has been dead since Year 412. The previous keeper never put her on the wall. At night she is in the shop window. People leave. Named fish hold still. Chalk writes the name. She rests.</p><p><b>What to do about it:</b> when the gold line says a name is due, write it. Vinegar does not clean the morning. Chalk does.</p>"
        },
        {
          id: "k_inland",
          sec: "The quarter",
          t: "The inland town",
          tags: "inland town letter road war hold atlas boston",
          w: "<p>Salem Street is one block. Inland is a continent. When a road is cut, the inland town writes. A letter on the counter: a place, a pair, a fish. Fill it. A road to the harbor can reopen. Leave it five days and the gold line stays late.</p><p>{letter} {war}</p><p>Boston is one harbor. The rest of the map was generated from the same seed as the shop. Click a keep on the Atlas. That is a town with a tongue, a fish, a war, a road.</p><p><b>What to do about it:</b> keep two of what they asked for. The letter is not a tab. It is the unfinished thing.</p>"
        },
        {
          id: "k_filter",
          sec: "The shop floor",
          t: "Filter clogged",
          tags: "filter clogged packed leak aisle boards oak",
          w: "<p>Filter clogged is not a metaphor. The pad is full. Water finds downhill. The aisle wets. People turn around at the door. The gold line will say it if the filter is the unfinished thing.</p><p>{filter}</p><p><b>What to do about it:</b> click the clog pill. Buy pads at Harbor Supply before it happens again. A wet aisle from the harbor at night is a different page.</p>"
        },
        {
          id: "k_beast",
          sec: "The chronicle",
          t: "Forgotten beasts",
          tags: "beast forgotten harbor scare till unique body",
          w: "<p>Things that still move, with a year they wake. Not a bestiary screen. A body, a hunger, a material. If they come to the harbor the till feels it. The choir sours. People look from the door and do not come in.</p><p><b>What to do about it:</b> Atlas, The living year. The gold line will say if one is near. Do not treat it like stock.</p>"
        },
        {
          id: "k_geo",
          sec: "The chronicle",
          t: "Under the map",
          tags: "geo elevation rain heat drainage volcanism savagery biome",
          w: "<p>The continent is not a picture. Elevation, rain, heat, drainage, volcanism, savagery. Rivers run downhill. Biomes follow. Civilizations found where the water and the food agreed.</p><p>Boston is a harbor because the east is sea and the west is land, not because a designer put a pin there.</p><p><b>What to do about it:</b> Atlas. The painted plate is the same seed. Click a keep. The water they keep is the biome they sit on.</p>"
        },
        {
          id: "k_tongue",
          sec: "The chronicle",
          t: "Tongues",
          tags: "tongue language plaque name inland civ",
          w: "<p>Every civ keeps a tongue. Names on the map, plaques on the tank, a word a traveler says that is not English. The shop is on a harbor that speaks more than one.</p><p><b>What to do about it:</b> a traveler will say a town you have only read. Atlas has the tongue next to the civ. A plaque in the tank is not decoration. It is a language that got here in a fish bag.</p>"
        },
        {
          id: "k_wild",
          sec: "The animals",
          t: "The living water",
          tags: "wild river scarce stock inland fish bag",
          w: "<p>Inland water has populations. Overfish a kind and the river thins. A traveler will say they heard the river is empty of what you sell. The shop is not an infinite warehouse. It is a mouth on a watershed.</p><p><b>What to do about it:</b> if someone says the river is empty, believe them. Breed here. Do not strip the last wild pair for the till.</p>"
        },
        {
          id: "k_rumor",
          sec: "The quarter",
          t: "Rumour",
          tags: "rumor word street hall heat because",
          w: "<p>The street talks. A death, a theft, a letter, a boycott, a name on the wall. Rumour is heat, not a feed. It lands in the choir and in who still walks in.</p><p><b>What to do about it:</b> Life, Because. The gold line is the rumour that made it to the counter.</p>"
        },
        {
          id: "k_faith",
          sec: "The chronicle",
          t: "The old names",
          tags: "faith god taboo bless civ vow holy",
          w: "<p>Civs keep a faith. Gods take seats. A vow can make a kind holy, and the last of it taboo. Blessing sweetens the choir. A taboo on the aisle is someone who will not bag what you keep.</p><p><b>What to do about it:</b> if they will not take a kind, it is not a price. It is a belief. Atlas names the faith. Do not argue it at the till.</p>"
        },
        {
          id: "k_room",
          sec: "The shop floor",
          t: "The room",
          tags: "room ear morning puddle continent",
          w: "<p>The shop is a room with an ear. The morning has a sound. The puddle has a sound. The continent is not silent just because you are looking at the tank.</p><p><b>What to do about it:</b> stand still. Work mode is the fluorescent hum, and the hum still sours when the water is off.</p>"
        },
        {
          id: "k_hook",
          sec: "The shop floor",
          t: "The run",
          tags: "hook combo fish bag till juice run miss",
          w: "<p>The till is a run. Two fish bags and the coins heat. Five and you should not miss. A miss at three writes the break. Hands remember a run. Hands forget a week away.</p><p><b>What to do about it:</b> keep pairs before lunch. The gold line counts. Do not sell the last of a kind to keep a number going. The number is not the shop.</p>"
        },
        {
          id: "k_mind",
          sec: "The animals",
          t: "What a fish keeps",
          tags: "mind memory needs strange mood memorial thought",
          w: "<p>A fish has a memory, needs, a mood that can turn strange, a memorial if someone they knew died. Life lists the thought. A strange mood can leave an object in the glass.</p><p>{named}</p><p><b>What to do about it:</b> click a fish. Read the card. If they are in a mood, do not bag them yet.</p>"
        },
        {
          id: "k_keeper",
          sec: "You and your people",
          t: "The previous keeper",
          tags: "keeper late rook nedda asa wren pim lila keys",
          w: "<p>You hung the sign. Someone kept this shop before you. The street still uses their names. A fish in the tank is theirs. The baker will say you're not them.</p><p>{keeper}</p><p><b>What to do about it:</b> read Life. The first page is their handwriting. Do not bag the one they named unless you mean it.</p>"
        }
      ];
      function now() {
        return typeof performance !== "undefined" ? performance.now() / 1e3 : Date.now() / 1e3;
      }
      function esc(s) {
        var map = { "&": "&amp;", "<": "&lt;", ">": "&gt;", '"': "&quot;" };
        return String(s || "").replace(/[&<>"]/g, function(c) {
          return map[c];
        });
      }
      function gs() {
        try {
          if (typeof gameState === "function") return gameState();
          if (typeof gameState === "object" && gameState) return gameState;
        } catch (e) {
        }
        return null;
      }
      function wikiList() {
        try {
          if (typeof WIKI === "function") return WIKI() || [];
          if (Array.isArray(WIKI)) return WIKI;
        } catch (e) {
        }
        return [];
      }
      function seedWiki() {
        if (seeded) return;
        var w = wikiList();
        if (!w || !w.push) return;
        for (var i = 0; i < EXTRA.length; i++) {
          var a = EXTRA[i];
          var hit = false;
          for (var j = 0; j < w.length; j++) if (w[j] && w[j].id === a.id) hit = true;
          if (!hit) w.push(a);
        }
        seeded = true;
      }
      function year() {
        try {
          if (window.saga && typeof saga.year === "function") return saga.year();
        } catch (e) {
        }
        return 1e3;
      }
      function fishAll() {
        try {
          if (typeof allFish === "function") return allFish() || [];
        } catch (e) {
        }
        var g = gs();
        return g && g.fish || [];
      }
      function havePair() {
        var c = /* @__PURE__ */ Object.create(null);
        var list = fishAll();
        for (var i = 0; i < list.length; i++) {
          var f = list[i];
          if (!f || f.stage != null && f.stage !== 2) continue;
          var k2 = String(f.sp);
          c[k2] = (c[k2] || 0) + 1;
          if (c[k2] >= 2) return true;
        }
        return false;
      }
      function clogged2() {
        try {
          return !!window.clogged;
        } catch (e) {
        }
        return false;
      }
      function goldLine() {
        try {
          var el = document.getElementById("hookWhisper");
          if (el && el.textContent) return String(el.textContent).trim();
        } catch (e) {
        }
        return "";
      }
      function facts() {
        if (factCache) return factCache;
        var g = gs() || {};
        var st = g.stats || {};
        var list = fishAll();
        var day = { sales: 0, misses: 0 };
        try {
          if (window.shopLife && shopLife.day) day = shopLife.day() || day;
        } catch (e0) {
        }
        var named = [];
        var sick = 0;
        var still = 0;
        for (var i = 0; i < list.length; i++) {
          var f = list[i];
          if (!f) continue;
          if (f.nick) named.push(f.nick);
          if (f.sick || f.cond || f.ill) sick++;
          if (f.still || f.mind && f.mind.stress > 0.68) still++;
        }
        var L = null;
        try {
          if (window.going && going.letter) L = going.letter();
        } catch (e1) {
        }
        var letter = "No letter is open.";
        var from = "no town";
        var want = "a fish";
        if (L && L.open && !L.filled && L.from) {
          from = L.from;
          want = L.want || "goldfish";
          letter = "A letter from " + from + ". They asked for a pair of " + want + ".";
        }
        var baker = "The baker on Salem has not been named yet.";
        try {
          if (window.desk && desk.neighbor) {
            var nb = desk.neighbor();
            if (nb) baker = (nb.known ? nb.name : "The baker on Salem") + " wants " + (nb.want || "goldfish") + ".";
          }
        } catch (e2) {
        }
        var keeper = "Someone kept this shop before you.";
        try {
          if (window.late && late.keeper) {
            var kp = late.keeper();
            if (kp && kp.n) keeper = kp.n + (kp.note ? ". " + kp.note : " left the keys.");
          }
        } catch (e3) {
        }
        var age2 = "";
        var war = "No war is warm at the harbor.";
        var town = "Boston";
        try {
          if (window.realm && realm.world) {
            var w = realm.world();
            if (w) {
              age2 = w.age || "";
              town = w.sites && w.sites[0] && w.sites[0].n || "Boston";
              if (realm.embargo && realm.embargo()) war = "A war inland is still warm. Holds come in late.";
            }
          }
        } catch (e4) {
        }
        var hall = "The hall has not spoken today.";
        try {
          if (window.guild && guild.boycott && guild.boycott()) hall = "The hall put the word out. The aisle is empty.";
          else if (window.guild && guild.mandate) hall = "The hall wants a pair of " + (guild.mandate() || "goldfish") + ".";
        } catch (e5) {
        }
        var unburied = "No name is due on the wall.";
        try {
          if (window.mark && mark.due) {
            var due = mark.due() || [];
            if (due.length) unburied = due[0].n + " wants a name on the wall.";
          }
        } catch (e6) {
        }
        var windows = "";
        try {
          if (window.sill && sill.lit && sill.dark) {
            windows = sill.lit() + " windows lit on Salem. " + sill.dark() + " gone dark.";
          }
        } catch (e7) {
        }
        var coming = "Nobody is on the book as coming.";
        try {
          if (window.desk && desk.coming) {
            var c = desk.coming();
            if (c && !c.done && c.name) coming = (c.name || "Someone") + " is coming, for " + (c.want || "a fish") + ".";
          }
        } catch (e8) {
        }
        var filter = clogged2() ? "The filter is clogged. People turn around at the door." : "The filter is holding.";
        var gold = goldLine() || letter;
        var out = {
          year: year(),
          age: age2,
          seed: g.seed || "",
          baker,
          keeper,
          from,
          want,
          letter,
          named: named.length ? named.slice(0, 6).join(", ") : "nobody named yet",
          nnamed: String(named.length),
          still: still ? still + " holding still." : "Nobody is holding still.",
          sick: sick ? sick + " in the water need you." : "The water is not sick.",
          filter,
          war,
          town,
          hall,
          unburied,
          windows,
          coming,
          sales: String(day.sales || 0),
          walked: String(day.misses || st.walked || 0),
          coins: String(Math.floor(g.coins || 0)),
          gold,
          pellets: String(Math.floor(g.pellets || 0))
        };
        factCache = out;
        return out;
      }
      function fillLive(html) {
        var f = facts();
        return String(html || "").replace(/\{([a-z]+)\}/g, function(_2, k2) {
          return f[k2] != null ? esc(String(f[k2])) : "";
        });
      }
      function tokens(s) {
        return String(s || "").toLowerCase().replace(/[^a-z0-9]+/g, " ").split(" ").filter(function(w) {
          return w.length > 1;
        });
      }
      function matchArt(a, q) {
        if (!q) return true;
        var t = tokens(q);
        if (!t.length) return true;
        var hay = (a.t + " " + a.sec + " " + (a.tags || "") + " " + String(a.w || "").replace(/<[^>]+>/g, " ")).toLowerCase();
        for (var i = 0; i < t.length; i++) if (hay.indexOf(t[i]) < 0) return false;
        return true;
      }
      function findArt(id) {
        var w = wikiList().concat(EXTRA);
        for (var i = 0; i < w.length; i++) if (w[i] && w[i].id === id) return w[i];
        return null;
      }
      function allArts() {
        var w = wikiList().concat(EXTRA);
        var seenA = /* @__PURE__ */ Object.create(null);
        var out = [];
        for (var i = 0; i < w.length; i++) {
          var a = w[i];
          if (!a || !a.id || seenA[a.id]) continue;
          seenA[a.id] = 1;
          out.push(a);
        }
        out.sort(function(x, y) {
          var s = String(x.sec || "").localeCompare(String(y.sec || ""));
          if (s) return s;
          return String(x.t || "").localeCompare(String(y.t || ""));
        });
        return out;
      }
      function diagnose() {
        var g = gs() || {};
        var st = g.stats || {};
        var list = fishAll();
        var day = window.shopLife && shopLife.day ? shopLife.day() : { sales: 0, misses: 0 };
        var f = facts();
        var sick = 0;
        var named = 0;
        for (var i = 0; i < list.length; i++) {
          if (list[i] && (list[i].sick || list[i].cond || list[i].ill)) sick++;
          if (list[i] && list[i].nick) named++;
        }
        var pellets = Math.floor(g.pellets || 0);
        var coins = g.coins || 0;
        var out = [];
        if (f.letter && /letter from/i.test(f.letter) && f.from !== "no town")
          out.push({ id: "letter", title: "A letter is on the counter", body: f.letter + " Keep two. Bag that fish. The town is answered.", go: "life", golabel: "Life", art: "k_inland" });
        if (clogged2()) out.push({ id: "clog", title: "The filter is clogged", body: "People are turning around at the door. Click the clog pill on the HUD.", go: "tank", golabel: "The tank", art: "k_filter" });
        if (sick) out.push({ id: "sick", title: sick + " in the water need you", body: "A sick fish is not a mood. Open the tank, click them, and get to the vet if a bottle will not do it.", go: "tank", golabel: "The tank", art: "k_water" });
        if (f.unburied && /wants a name/.test(f.unburied))
          out.push({ id: "wall", title: "A name is due on the wall", body: f.unburied + " Chalk writes it. Vinegar does not.", go: "life", golabel: "Life", art: "k_plate" });
        if (/boycott|word out/i.test(f.hall))
          out.push({ id: "hall", title: "The hall put the word out", body: f.hall, go: "life", golabel: "Life", art: "k_guild" });
        if (/war inland/i.test(f.war))
          out.push({ id: "war", title: "A war inland is still warm", body: f.war + " Atlas names the civs.", go: "atlas", golabel: "Atlas", art: "k_inland" });
        if (list.length && pellets <= 0) out.push({ id: "feed", title: "The tin is empty", body: "Click the water once you have pellets. Shop tab sells the tin.", go: "shop", golabel: "Shop", art: "k_hour" });
        if (!havePair() && (day.misses > 0 || (st.walked || 0) > 0)) out.push({ id: "pair", title: "They came for a fish you did not have", body: "Walk-ins look. They buy a pair. A unique fish is a display. Life has the till.", go: "life", golabel: "Your life", art: "k_nobuy" });
        if (coins < 80 && (st.sold || 0) === 0) out.push({ id: "thin", title: "The till is thin", body: "Do not sell the last of a fish. Fill an order or wait for lunch. Money tab is the levy, not a surprise.", go: "bank", golabel: "Money", art: "k_broke" });
        if ((day.sales || 0) === 0 && (day.misses || 0) >= 2) out.push({ id: "walk", title: "The till is all walked", body: day.misses + " left without a fish bag. Water, stock, or both.", go: "life", golabel: "The till", art: "k_till" });
        if (!out.length && named < 1 && list.length) out.push({ id: "name", title: "Nobody in the water has a name you gave them", body: "Click a fish. They already have a name. Read what they are thinking in Life.", go: "life", golabel: "Your life", art: "k_name" });
        if (!out.length) out.push({ id: "hold", title: "The water is holding", body: "Year " + year() + ". If you are bored, that is a different page. If you are curious, open the book.", go: "", golabel: "", art: "k_bored" });
        return out.slice(0, 6);
      }
      function bored() {
        var g = gs() || {};
        var list = fishAll();
        var f = facts();
        var items = [
          { title: "This shop, this seed", body: "Year " + f.year + ". " + f.named + ". Open the almanac.", go: "", golabel: "" },
          { title: "Name someone in the water", body: "Click a fish. Life lists what they think. Twenty names is a secret.", go: "tank", golabel: "The tank" },
          { title: "Read the till", body: "Sold " + f.sales + " against walked " + f.walked + ". Who asked for what.", go: "life", golabel: "Your life" },
          { title: "Walk the North End", body: f.baker + " The Map is jobs and homes, not a circle around you.", go: "street", golabel: "Map" },
          { title: "Click a keep on the Atlas", body: "A thousand years already ran. Present is Year " + year() + ". Every keep is a town with a tongue.", go: "atlas", golabel: "Atlas" },
          { title: "The Chronicle", body: "Cause chains. Weather. The year turning. Who came back from the pages.", go: "chron", golabel: "Chronicle" },
          { title: "The book in the back", body: "Not the wiki. The other one. Names that should stay in it.", go: "back", golabel: "Back" },
          { title: "Secrets", body: (Object.keys(g.eggs || {}).length || 0) + " found. The rest are in the shop, not in a list.", go: "eggs", golabel: "Secrets" },
          { title: "Harbor Supply", body: "Lots dated to Year " + year() + ". Conditioner, fish bags, pads.", go: "shop", golabel: "Shop" },
          { title: "The Calendar", body: "Season, holidays, millennial clock. It does not stop.", go: "cal", golabel: "Calendar" }
        ];
        if (list.length < 2) items.unshift({ title: "Stock a pair", body: "One fish is a display. Two is a shop.", go: "shop", golabel: "Shop" });
        return items.slice(0, 7);
      }
      function curious() {
        var want = [
          "The gold line",
          "A fish bag",
          "The inland town",
          "A name on the wall",
          "Still-sickness",
          "The world beyond the harbor",
          "You are late",
          "The choir",
          "Windows on Salem are tanks",
          "The previous keeper"
        ];
        var picks = [];
        var all = allArts();
        for (var i = 0; i < want.length; i++) {
          for (var j = 0; j < all.length; j++) if (all[j] && all[j].t === want[i]) picks.push(all[j]);
        }
        if (picks.length < 4) {
          for (var k2 = 0; k2 < EXTRA.length && picks.length < 8; k2++) picks.push(EXTRA[k2]);
        }
        return picks.slice(0, 10);
      }
      function articlesForQuery() {
        var all = allArts();
        var out = [];
        for (var i = 0; i < all.length; i++) if (matchArt(all[i], query)) out.push(all[i]);
        return out;
      }
      function btn(go2, label) {
        if (!go2) return "";
        return '<button type="button" class="buy ghost" data-know-go="' + esc(go2) + '">' + esc(label || "Open") + "</button>";
      }
      function artBody(a) {
        return fillLive(a.w || "");
      }
      function artRow(a) {
        return '<div class="row know-art" data-know-art="' + esc(a.id) + '"><div></div><div><div class="n">' + esc(a.t) + '</div><div class="d">' + artBody(a) + "</div></div><div>" + (a.go ? btn(a.go, a.golabel) : "") + "</div></div>";
      }
      function card(c) {
        return '<div class="row know-card"' + (c.art ? ' data-know-art="' + esc(c.art) + '"' : "") + '><div></div><div><div class="n">' + esc(c.title) + '</div><div class="d">' + esc(c.body) + "</div></div><div>" + btn(c.go, c.golabel) + "</div></div>";
      }
      function almanac() {
        var f = facts();
        var g = gs() || {};
        var list = fishAll();
        var html = "";
        html += '<div class="sec">This shop</div>';
        html += '<div class="note">Year ' + esc(f.year) + (f.age ? " · " + esc(f.age) : "") + (f.seed ? " · seed " + esc(f.seed) : "") + ". " + list.length + " in the water. The book is this run, not a generic manual.</div>";
        html += '<div class="row know-card"><div></div><div><div class="n">The counter</div><div class="d">';
        html += esc(f.gold) + "<br>" + esc(f.letter) + "<br>" + esc(f.coming);
        html += "</div></div><div>" + btn("life", "Life") + "</div></div>";
        html += '<div class="row know-card"><div></div><div><div class="n">The water</div><div class="d">';
        html += esc(f.named) + " named.<br>" + esc(f.sick) + " " + esc(f.still) + "<br>" + esc(f.filter);
        html += "</div></div><div>" + btn("tank", "The tank") + "</div></div>";
        html += '<div class="row know-card"><div></div><div><div class="n">The street</div><div class="d">';
        html += esc(f.baker) + "<br>" + esc(f.keeper) + (f.windows ? "<br>" + esc(f.windows) : "");
        html += "<br>Sold " + esc(f.sales) + ". Walked " + esc(f.walked) + ". Coins " + esc(f.coins) + ".";
        html += "</div></div><div>" + btn("street", "Map") + "</div></div>";
        html += '<div class="row know-card"><div></div><div><div class="n">Inland</div><div class="d">';
        html += esc(f.town) + " is the harbor.<br>" + esc(f.war) + "<br>" + esc(f.hall);
        html += "</div></div><div>" + btn("atlas", "Atlas") + "</div></div>";
        if (f.unburied && /wants a name/.test(f.unburied)) {
          html += '<div class="row know-card" data-know-art="k_plate"><div></div><div><div class="n">The wall</div><div class="d">';
          html += esc(f.unburied);
          html += "</div></div><div></div></div>";
        }
        var nArt = allArts().length;
        var nSeen = 0;
        for (var k2 in seen) if (seen[k2]) nSeen++;
        html += '<div class="note">' + nArt + " entries in the book. You have opened " + nSeen + ". Search, or open the whole book. Click a keep on the Atlas for the town itself.</div>";
        return html;
      }
      function renderBook() {
        var html = "";
        if (openId) {
          var art = findArt(openId);
          html += '<div class="row"><div></div><div><button type="button" class="buy ghost" data-know-mode="book" data-know-back="1">All entries</button></div><div></div></div>';
          if (art) {
            var first = !seen[art.id];
            seen[art.id] = 1;
            html += '<div class="sec">' + esc(art.sec) + "</div>";
            html += '<div class="row know-art know-open"><div></div><div><div class="n">' + esc(art.t) + '</div><div class="d">' + artBody(art) + "</div></div><div></div></div>";
            var all = allArts();
            var related = [];
            for (var i = 0; i < all.length; i++) {
              if (all[i].id === art.id) continue;
              if (all[i].sec === art.sec) related.push(all[i]);
            }
            if (related.length) {
              html += '<div class="sec">Also in ' + esc(art.sec) + "</div>";
              for (var r = 0; r < related.length && r < 8; r++) {
                html += '<div class="row" data-know-art="' + esc(related[r].id) + '"><div></div><div><div class="n">' + esc(related[r].t) + "</div></div><div></div></div>";
              }
            }
            if (first) {
              try {
                if (window.desk && desk.think) desk.think("book", art.t);
              } catch (eT) {
              }
            }
          } else {
            html += '<div class="note">That page is not in this book.</div>';
          }
          return html;
        }
        var all2 = articlesForQuery();
        html += '<div class="note">' + all2.length + " entries. Click a title. The page fills with this shop, this year, this letter.</div>";
        var sec = "";
        for (var a = 0; a < all2.length; a++) {
          if (all2[a].sec !== sec) {
            sec = all2[a].sec;
            html += '<div class="sec">' + esc(sec) + "</div>";
          }
          html += '<div class="row know-toc" data-know-art="' + esc(all2[a].id) + '"><div></div><div><div class="n">' + esc(all2[a].t) + "</div></div><div>" + (seen[all2[a].id] ? '<span class="pill">read</span>' : "") + "</div></div>";
        }
        return html;
      }
      function renderDesk() {
        factCache = null;
        var html = '<div class="know-desk" id="knowDesk">';
        html += '<div class="sec">The book</div>';
        html += '<div class="note">Year ' + year() + ". A living knowledge base: this shop as it stands, what to do if you are stuck or bored, and every system with a name. " + allArts().length + " entries.</div>";
        html += '<div class="row"><div></div><div class="qty know-lanes"><button type="button" class="buy ' + (mode === "shop" ? "" : "ghost") + '" data-know-mode="shop">This shop</button><button type="button" class="buy ' + (mode === "now" ? "" : "ghost") + `" data-know-mode="now">I'm stuck</button><button type="button" class="buy ` + (mode === "bored" ? "" : "ghost") + `" data-know-mode="bored">I'm bored</button><button type="button" class="buy ` + (mode === "curious" ? "" : "ghost") + `" data-know-mode="curious">I'm curious</button><button type="button" class="buy ` + (mode === "book" ? "" : "ghost") + '" data-know-mode="book">The whole book</button></div><div></div></div>';
        html += '<div class="row"><div style="text-align:center;color:var(--muted);font-size:11px">find</div><div><input id="knowSearch" type="search" value="' + esc(query) + '" placeholder="letter, fish bag, inland town, still-sickness, gold line…" autocomplete="off" spellcheck="false"></div><div>' + (query ? '<button type="button" class="buy ghost" data-know-mode="book" data-know-clear="1">Clear</button>' : "") + "</div></div>";
        if (query) {
          var found = articlesForQuery();
          html += '<div class="note">' + found.length + " entries match.</div>";
          for (var fq = 0; fq < found.length && fq < 16; fq++) html += artRow(found[fq]);
          html += "</div>";
          return html;
        }
        if (mode === "shop") html += almanac();
        else if (mode === "now") {
          html += '<div class="sec">Right now</div>';
          var d = diagnose();
          for (var i = 0; i < d.length; i++) html += card(d[i]);
        } else if (mode === "bored") {
          html += '<div class="sec">If the shop is running itself</div>';
          var b = bored();
          for (var j = 0; j < b.length; j++) html += card(b[j]);
        } else if (mode === "curious") {
          html += '<div class="sec">If you just want to know</div>';
          var c = curious();
          for (var k2 = 0; k2 < c.length; k2++) html += artRow(c[k2]);
        } else {
          html += renderBook();
        }
        html += "</div>";
        return html;
      }
      function isGuide() {
        var t = document.getElementById("dtitle");
        return !!(t && /guide|the book/i.test(t.textContent || ""));
      }
      function enhance(force) {
        if (!isGuide()) return;
        seedWiki();
        var body = document.getElementById("dbody");
        if (!body) return;
        var existing = document.getElementById("knowDesk");
        if (existing && !force) return;
        painting = true;
        var html = renderDesk();
        if (existing) existing.outerHTML = html;
        else body.insertAdjacentHTML("afterbegin", html);
        painting = false;
        try {
          var desk2 = document.getElementById("knowDesk");
          if (desk2) {
            for (var n = body.firstChild; n; n = n.nextSibling) {
              if (n === desk2 || n.nodeType !== 1) continue;
              n.style.display = "none";
            }
          }
        } catch (eH) {
        }
      }
      function go(id) {
        if (id === "map") id = "street";
        if (id === "front") id = "shop";
        var scenes = { tank: 1, shop: 1, street: 1, back: 1, cell: 1 };
        if (scenes[id]) {
          var b = document.querySelector('#scenes [data-scene="' + id + '"]');
          if (b) b.click();
          else if (typeof sceneGo === "function") try {
            sceneGo(id);
          } catch (e) {
          }
          return;
        }
        try {
          if (typeof openTab === "function") openTab(id);
        } catch (e2) {
        }
      }
      function bind() {
        var body = document.getElementById("dbody");
        if (!body || body.__know) return;
        body.__know = 1;
        body.addEventListener("click", function(e) {
          var artEl = e.target.closest("[data-know-art]");
          if (artEl && isGuide() && !e.target.closest("[data-know-go]")) {
            openId = artEl.getAttribute("data-know-art") || "";
            mode = "book";
            query = "";
            lastUi = 0;
            enhance(true);
            return;
          }
          var t = e.target.closest("[data-know-mode],[data-know-go],[data-know-clear],[data-know-back]");
          if (!t || !isGuide()) return;
          if (t.getAttribute("data-know-clear") || t.getAttribute("data-know-back")) {
            query = "";
            openId = "";
          }
          var m = t.getAttribute("data-know-mode");
          if (m) {
            mode = m;
            if (m !== "book") {
              query = "";
              openId = "";
            }
          }
          var g = t.getAttribute("data-know-go");
          if (g) {
            go(g);
            return;
          }
          lastUi = 0;
          enhance(true);
          var input = document.getElementById("knowSearch");
          if (input && m === "book") input.focus();
        });
        body.addEventListener("input", function(e) {
          if (!e.target || e.target.id !== "knowSearch") return;
          query = e.target.value;
          mode = "book";
          openId = "";
          enhance(true);
          var input = document.getElementById("knowSearch");
          if (input) {
            input.focus();
            try {
              input.setSelectionRange(query.length, query.length);
            } catch (err) {
            }
          }
        });
      }
      function tick() {
        try {
          bind();
          seedWiki();
          if (now() - lastUi > 0.6) {
            lastUi = now();
            if (isGuide() && !document.getElementById("knowDesk")) enhance();
          }
        } catch (e) {
        }
      }
      window.know = {
        diagnose,
        year,
        facts,
        open: function(id) {
          openId = id || "";
          mode = id ? "book" : "shop";
          enhance(true);
        }
      };
      seedWiki();
      if (window.__onBeat) window.__onBeat(tick, 280);
      else setTimeout(function loop() {
        tick();
        setTimeout(loop, 280);
      }, 280);
    })();
  } catch (e) {
    console.error("Layer know.js failed to start:", e);
  }

  // src/layers/weave.js
  try {
    (function() {
      "use strict";
      var lastTick = 0;
      var lastUi = 0;
      var lastSales = -1;
      var seenDead = {};
      var seenArts = {};
      var lastRaised = 0;
      var raisedBook = null;
      var lastMissKey = "";
      var wired = false;
      var didBrowse = false;
      var nextLine = "";
      var nextUntil = 0;
      function now() {
        return typeof performance !== "undefined" ? performance.now() / 1e3 : Date.now() / 1e3;
      }
      function year() {
        try {
          if (window.saga && typeof saga.year === "function") return saga.year();
        } catch (e) {
        }
        return 1e3;
      }
      function gs() {
        try {
          if (typeof gameState === "function") return gameState();
          if (typeof gameState === "object" && gameState) return gameState;
        } catch (e) {
        }
        return null;
      }
      function state() {
        var g = gs();
        if (g && g.weave && Array.isArray(g.weave.rumors)) return g.weave;
        var w = {
          rumors: [],
          because: [],
          craft: 0.35,
          word: 0,
          lastAtlas: ""
        };
        try {
          if (g) g.weave = w;
        } catch (e) {
        }
        return w;
      }
      function clamp01(n) {
        return n < 0 ? 0 : n > 1 ? 1 : n;
      }
      function think(text) {
        try {
          if (window.desk && typeof desk.think === "function") desk.think("weave", text);
        } catch (e) {
        }
      }
      function write(kind, text) {
        try {
          if (typeof chronicle === "function") {
            var line = String(text || "");
            if (line && !/^Year\s+\d/.test(line)) line = "Year " + year() + ". " + line;
            chronicle(kind, line, []);
          }
        } catch (e) {
        }
      }
      function because(text) {
        if (!text) return;
        var w = state();
        if (w.because.length && w.because[w.because.length - 1].s === text) return;
        w.because.push({ s: text, y: year(), at: now() });
        if (w.because.length > 12) w.because.shift();
      }
      function rumor2(kind, text, heat) {
        if (!text) return;
        var w = state();
        for (var i = 0; i < w.rumors.length; i++) {
          if (w.rumors[i].s === text) {
            w.rumors[i].heat = Math.min(3, w.rumors[i].heat + (heat || 1) * 0.4);
            w.rumors[i].at = now();
            return;
          }
        }
        w.rumors.push({ k: kind || "", s: String(text), heat: heat || 1, y: year(), at: now() });
        if (w.rumors.length > 14) w.rumors.shift();
      }
      function hottest() {
        var w = state();
        var best = null;
        for (var i = 0; i < w.rumors.length; i++) {
          if (!best || w.rumors[i].heat > best.heat) best = w.rumors[i];
        }
        return best && best.heat > 0.35 ? best : null;
      }
      function fishList() {
        try {
          if (typeof allFish === "function") return allFish() || [];
        } catch (e) {
        }
        return [];
      }
      function ped(f) {
        try {
          if (f && f.fid && typeof pedOf === "function") return pedOf(f.fid);
        } catch (e) {
        }
        return null;
      }
      function bloodline(f) {
        var rec = ped(f);
        if (!rec) return null;
        var mom = null, dad = null;
        try {
          if (rec.mom && typeof pedOf === "function") mom = pedOf(rec.mom);
          if (rec.dad && typeof pedOf === "function") dad = pedOf(rec.dad);
        } catch (e) {
        }
        var of = mom && (mom.nick || mom.vname) || dad && (dad.nick || dad.vname) || "";
        var deeds = rec.deeds && rec.deeds.length ? rec.deeds.length : 0;
        return {
          nick: rec.nick || f && f.nick || "",
          of,
          deeds,
          born: rec.born || rec.bornT || 0,
          fate: rec.fate || null
        };
      }
      function namedBlood() {
        var list = fishList();
        var out = [];
        for (var i = 0; i < list.length; i++) {
          var b = bloodline(list[i]);
          if (b && (b.of || b.deeds > 1) && list[i].nick) out.push({ f: list[i], b });
        }
        return out;
      }
      function atlasLine() {
        try {
          var a = typeof atlGen === "function" ? atlGen() : typeof civAtlas === "function" ? civAtlas() : null;
          if (!a || !a.e || !a.e.length) return "";
          var ev = a.e[a.e.length - 1];
          if (typeof atlLine === "function") return String(atlLine(a, ev) || "");
          if (ev && ev.s) return String(ev.s);
        } catch (e) {
        }
        return "";
      }
      function book() {
        try {
          if (window.saga && typeof saga.book === "function") return saga.book();
        } catch (e) {
        }
        return { dead: [], arts: [], occ: {} };
      }
      function bumpWord(n) {
        try {
          if (typeof townState === "function") {
            var t = townState();
            if (t && typeof t.word === "number") t.word = Math.max(-3, Math.min(3, (t.word || 0) + n));
          }
        } catch (e) {
        }
        var w = state();
        w.word = clamp01((w.word || 0) + n);
      }
      function wrapBrowse() {
        if (didBrowse || !window.shopBrowse) return;
        didBrowse = true;
        var orig = window.shopBrowse;
        window.shopBrowse = function(idx, slot, W, floorY, personS, simT) {
          var rec = orig.apply(this, arguments);
          try {
            if (!rec) return rec;
            var st = window.shopLife && shopLife.browse ? shopLife.browse()[idx] : null;
            var hot = hottest();
            var blood2 = namedBlood();
            var w = state();
            if (hot && hot.k === "risen" && rec.phase === "look" && Math.random() < Math.min(0.45, hot.heat * 0.25)) {
              rec.line = ["I'm not buying anything that came back.", "The water smells like a church.", "That tank."][idx % 3];
              rec.kind = rec.kind || "collector";
            } else if (hot && hot.k === "clog" && rec.phase === "look" && Math.random() < 0.3) {
              rec.line = "I heard the water was off.";
            } else if (blood2.length && (rec.kind === "collector" || st && st.kind === "collector" || Math.random() < 0.12 + w.craft * 0.1)) {
              var pick = blood2[idx % blood2.length];
              if (pick && rec.phase === "look") {
                rec.line = pick.b.of ? "Is that one of " + pick.b.of + "'s?" : "The " + (pick.f.nick || "named one") + ". I know the line.";
                rec.kind = "collector";
                if (st) st.kind = "collector";
              }
              if (pick && rec.phase === "pay") {
                rec.line = pick.b.of ? "Of " + pick.b.of + ". I'll take it." : "The named one. Wrap it.";
              }
            } else if (hot && hot.k === "art" && rec.phase === "look" && Math.random() < 0.28) {
              rec.line = "What's in the window.";
            }
            if (rec.phase === "leave" && st && !st.bought && st.want && !st.coming && !st._weaveMiss) {
              st._weaveMiss = 1;
              because(
                (rec.name || "Someone") + " walked because " + (st.haveWant ? window.shopSite && shopSite.wet() > 0.36 ? "the aisle was wet" : "they would not pay" : "we had no pair of " + st.want)
              );
            }
          } catch (e) {
          }
          return rec;
        };
        wired = true;
      }
      function since(mark2, list) {
        var last = list.length ? list[list.length - 1] : null;
        if (mark2.list !== list) {
          mark2.list = list;
          mark2.last = last;
          return [];
        }
        if (last === mark2.last) return [];
        var at = mark2.last ? list.lastIndexOf(mark2.last) : -1;
        mark2.last = last;
        return list.slice(at + 1);
      }
      function watchWorld() {
        var w = state();
        var line = atlasLine();
        if (line && line !== w.lastAtlas) {
          w.lastAtlas = line;
          var loud = /war|plague|fished out|closed|extinct|made |embargo|listing/i.test(line) && !/was born of/i.test(line);
          if (loud) {
            rumor2("atlas", line, 1.1);
            because("The harbor still carries it: " + line.replace(/^Year\s+\d+\.\s*/, ""));
            nextLine = /war/i.test(line) ? "War on the water. Stock will go dear." : "The year in the atlas is not quiet.";
            nextUntil = now() + 18;
            if (/war|embargo|plague|fished out/i.test(line)) bumpWord(-0.14);
          }
        }
        var b = book();
        var newDead = since(seenDead, b.dead || []);
        if (newDead.length) {
          var d = newDead[newDead.length - 1];
          if (d && d.n) {
            rumor2("death", d.n + " is gone", 1.1);
            because(d.n + " died" + (d.how ? " (" + d.how + ")" : "") + ", and the tankmates will dwell on it.");
            bumpWord(-0.08);
          }
        }
        var newArts = since(seenArts, b.arts || []);
        if (newArts.length) {
          var art = newArts[newArts.length - 1];
          if (art && art.n) {
            rumor2("art", art.n + " is in the shop", 1.3);
            because(art.n + " was made, so collectors started asking what was in the window.");
            bumpWord(0.12);
            nextLine = "Something new in the window.";
            nextUntil = now() + 20;
          }
        }
        var raised = b.occ && b.occ.raised || 0;
        if (b !== raisedBook) {
          raisedBook = b;
          lastRaised = raised;
        }
        if (raised > lastRaised) {
          lastRaised = raised;
          rumor2("risen", "something came back from the pages", 2.2);
          because("A name was read aloud. The street heard, even if they did not see the splash.");
          bumpWord(0.35);
          nextLine = "The street is crossing itself.";
          nextUntil = now() + 24;
        } else lastRaised = raised;
      }
      function watchTill() {
        var w = state();
        try {
          if (!window.shopLife || !shopLife.day) return;
          var day = shopLife.day();
          if (day && lastSales >= 0 && day.sales > lastSales) {
            var n = day.sales - lastSales;
            w.craft = clamp01(w.craft + 0.012 * n);
            if (n && w.craft > 0.7) because("The fish bags are coming easier. That is craft, not luck.");
          }
          if (day) lastSales = day.sales;
          var miss = shopLife.misses ? shopLife.misses() : [];
          if (miss && miss.length) {
            var ms = miss[miss.length - 1];
            var key = (ms.name || "") + "|" + (ms.want || "") + "|" + (ms.y || "");
            if (key !== lastMissKey) {
              lastMissKey = key;
              w.craft = clamp01(w.craft - 0.02);
              if (ms.why === "stock") rumor2("stock", "no " + (ms.want || "fish") + " on Salem", 0.9);
              if (ms.why === "clog") rumor2("clog", "the water was off", 1.2);
            }
          }
        } catch (e) {
        }
        try {
          if (window.clogged) rumor2("clog", "the filter is clogging", 0.7);
        } catch (e2) {
        }
      }
      function tickRumors() {
        var w = state();
        for (var i = w.rumors.length - 1; i >= 0; i--) {
          w.rumors[i].heat *= 0.992;
          if (w.rumors[i].heat < 0.12) w.rumors.splice(i, 1);
        }
      }
      function next() {
        if (nextLine && now() < nextUntil) return nextLine;
        var hot = hottest();
        if (hot && hot.heat > 0.8) return "The street is saying: " + hot.s + ".";
        var blood2 = namedBlood();
        if (blood2.length && Math.random() < 0.4) {
          var p = blood2[0];
          return p.b.of ? p.b.nick + " is of " + p.b.of + "." : p.b.nick + " has a line.";
        }
        var w = state();
        if (w.craft > 0.65 && Math.random() < 0.3) return "The fish bags are coming easier.";
        return "";
      }
      function esc(s) {
        var map = { "&": "&amp;", "<": "&lt;", ">": "&gt;", '"': "&quot;" };
        return String(s || "").replace(/[&<>"]/g, function(c) {
          return map[c];
        });
      }
      function enhanceLife() {
        try {
          var body = document.getElementById("dbody");
          if (!body) return;
          var old = body.querySelector("weave-life");
          if (old && old.parentNode) old.parentNode.removeChild(old);
        } catch (e) {
        }
      }
      function enhanceChron() {
        var body = document.getElementById("dbody");
        var title = document.getElementById("dtitle");
        if (!body || !title) return;
        if (!/chronicle/i.test(title.textContent || "")) return;
        var w = state();
        if (!w.because.length) return;
        var html = '<div class="sec">Cause</div>';
        var bec = w.because.slice(-5).reverse();
        for (var i = 0; i < bec.length; i++) {
          html += '<div class="row"><div></div><div><div class="d">' + esc(bec[i].s) + "</div></div><div></div></div>";
        }
        var wrap = body.querySelector(".weave-chron");
        if (!wrap) {
          wrap = document.createElement("div");
          wrap.className = "weave-chron";
          body.insertBefore(wrap, body.firstChild);
        }
        if (wrap.getAttribute("data-h") !== String(html.length)) {
          wrap.innerHTML = html;
          wrap.setAttribute("data-h", String(html.length));
        }
      }
      function seedWiki() {
        try {
          var w = typeof WIKI === "function" ? WIKI() : WIKI;
          if (!w || !w.push) return;
          var extra = [
            {
              id: "k_because",
              sec: "The chronicle",
              t: "Because",
              tags: "because cause weave rumor pedigree bloodline atlas depth",
              w: "<p>This person hates that person because they saw a death, so they started a fight, so a fish bag went wrong. The shop keeps that kind of sentence.</p><p>A collector asks if that tetra is of Idel because the pedigree says so. Someone walks because there is no pair. The street says the water is off because the filter clogged. A name read aloud in the back room becomes a rumor with heat, and the heat changes who comes in the door. <b>What to do about it:</b> Life, Because. Chronicle, Cause. The gold line will sometimes carry the loudest rumor.</p>"
            },
            {
              id: "k_blood",
              sec: "The animals",
              t: "Blood",
              tags: "pedigree bloodline collector named mother father deeds lineage",
              w: "<p>A named fish with a mother is not the same as a named fish. Collectors can tell. They will ask for the line. Deeds on the pedigree — a fight, a year turning, a return from the pages — are why one tetra is worth a conversation and the other is stock.</p><p><b>What to do about it:</b> do not sell the last of a line. Life, Blood. If they ask “is that one of Idel’s?”, that is the atlas talking through a person.</p>"
            }
          ];
          for (var i = 0; i < extra.length; i++) {
            var hit = false;
            for (var j = 0; j < w.length; j++) if (w[j] && w[j].id === extra[i].id) hit = true;
            if (!hit) w.push(extra[i]);
          }
        } catch (e) {
        }
      }
      function tick() {
        try {
          wrapBrowse();
          seedWiki();
          if (now() - lastTick > 1) {
            lastTick = now();
            watchWorld();
            watchTill();
            tickRumors();
          }
          if (now() - lastUi > 0.8) {
            lastUi = now();
            enhanceLife();
            enhanceChron();
          }
        } catch (e) {
        }
      }
      window.weave = {
        next,
        because,
        rumor: rumor2,
        of: state,
        blood: namedBlood,
        bumpWord
      };
      if (window.__onBeat) window.__onBeat(tick, 280);
      else setTimeout(function loop() {
        tick();
        setTimeout(loop, 280);
      }, 280);
    })();
  } catch (e) {
    console.error("Layer weave.js failed to start:", e);
  }

  // src/layers/live.js
  try {
    (function() {
      "use strict";
      var lastNow = 0;
      var lastUi = 0;
      var lastTick = 0;
      var wired = false;
      var yearLog = [];
      var facCache = /* @__PURE__ */ Object.create(null);
      var FAC_N = 0;
      var FACETS = [
        { id: "love", lo: "has little time for affection", hi: "falls in love easily" },
        { id: "hate", lo: "rarely holds a grudge", hi: "is quick to hatred" },
        { id: "envy", lo: "is content with a neighbor's luck", hi: "is eaten by envy" },
        { id: "cheer", lo: "seldom smiles", hi: "is given to mirth" },
        { id: "depress", lo: "is not easily brought low", hi: "sinks into melancholy" },
        { id: "anger", lo: "is slow to anger", hi: "is often inflamed" },
        { id: "anxiety", lo: "is calm under the weather", hi: "is a nervous wreck" },
        { id: "lust", lo: "has a modest appetite", hi: "is given to lust" },
        { id: "stress", lo: "is impervious to strain", hi: "cracks under little weight" },
        { id: "greed", lo: "does not care for coin", hi: "is greedy" },
        { id: "immod", lo: "is temperate", hi: "cannot leave a bottle" },
        { id: "violent", lo: "avoids a fight", hi: "is given to violence" },
        { id: "persist", lo: "gives up early", hi: "will not let a thing go" },
        { id: "waste", lo: "is careful with goods", hi: "is wasteful" },
        { id: "discord", lo: "keeps the peace", hi: "enjoys discord" },
        { id: "friend", lo: "keeps their own company", hi: "makes friends of strangers" },
        { id: "polite", lo: "is blunt", hi: "is unfailingly polite" },
        { id: "advice", lo: "takes counsel", hi: "disdains advice" },
        { id: "brave", lo: "is a coward", hi: "is brave to a fault" },
        { id: "conf", lo: "doubts themselves", hi: "is overconfident" },
        { id: "vanity", lo: "does not look in a glass", hi: "is vain" },
        { id: "ambition", lo: "has no ambition", hi: "would rule water" },
        { id: "gratitude", lo: "forgets a kindness", hi: "never forgets a kindness" },
        { id: "modest", lo: "is modest", hi: "cannot stop talking of themselves" },
        { id: "humor", lo: "has no humor", hi: "finds a joke in a wreck" },
        { id: "vengeful", lo: "lets injuries go", hi: "is vengeful" },
        { id: "proud", lo: "has no pride", hi: "is fiercely proud" },
        { id: "cruel", lo: "cannot stand cruelty", hi: "is cruel" },
        { id: "single", lo: "is easily distracted", hi: "is single-minded" },
        { id: "hope", lo: "expects the worst", hi: "is hopeful past reason" },
        { id: "curious", lo: "does not ask", hi: "is endlessly curious" },
        { id: "bashful", lo: "has no shame", hi: "is bashful" },
        { id: "privacy", lo: "lives in public", hi: "guards their privacy" },
        { id: "perfect", lo: "is sloppy", hi: "is a perfectionist" },
        { id: "closed", lo: "will hear anything", hi: "is close-minded" },
        { id: "tolerant", lo: "cannot stand difference", hi: "is tolerant" },
        { id: "obsess", lo: "lets feelings pass", hi: "is emotionally obsessive" },
        { id: "swayed", lo: "is unmoved", hi: "is swayed by every feeling" },
        { id: "altruism", lo: "looks to themselves", hi: "is altruistic" },
        { id: "duty", lo: "shades a duty", hi: "is dutiful" },
        { id: "thought", lo: "thinks things through", hi: "is thoughtless" },
        { id: "order", lo: "lives in a mess", hi: "cannot stand disorder" },
        { id: "trust", lo: "trusts no one", hi: "trusts too easily" },
        { id: "gregarious", lo: "avoids a crowd", hi: "is gregarious" },
        { id: "assert", lo: "will not speak up", hi: "is assertive" },
        { id: "active", lo: "is idle", hi: "cannot sit still" },
        { id: "excite", lo: "wants a quiet life", hi: "seeks excitement" },
        { id: "imagine", lo: "has no imagination", hi: "lives in imagined water" },
        { id: "abstract", lo: "wants the concrete", hi: "loves an abstract argument" },
        { id: "art", lo: "has no eye for art", hi: "is moved by craft" }
      ];
      var MATS = [
        { id: "bogwood", cls: "wood", hard: 2, dense: 0.4, val: 3, react: { salt: "swells", iron: "stains black", water: "softens" } },
        { id: "oak", cls: "wood", hard: 3, dense: 0.75, val: 5, react: { water: "rots in the end", salt: "holds" } },
        { id: "mica", cls: "stone", hard: 3, dense: 0.9, val: 8, react: { iron: "sparks", glass: "cuts" } },
        { id: "riverstone", cls: "stone", hard: 6, dense: 2.7, val: 2, react: { water: "rounds" } },
        { id: "brass", cls: "metal", hard: 4, dense: 8.4, val: 12, react: { salt: "tarnishes", water: "dulls" } },
        { id: "iron", cls: "metal", hard: 5, dense: 7.8, val: 6, react: { salt: "rusts", water: "rusts", copper: "pairs" } },
        { id: "copper", cls: "metal", hard: 3, dense: 8.9, val: 8, react: { salt: "goes green", water: "patinas" } },
        { id: "gold", cls: "metal", hard: 2, dense: 19.3, val: 40, react: {} },
        { id: "salt", cls: "crystal", hard: 2, dense: 2.2, val: 1, react: { iron: "eats", copper: "greens", bone: "cures" } },
        { id: "glass", cls: "glass", hard: 5, dense: 2.5, val: 7, react: { mica: "is cut", salt: "etches" } },
        { id: "driftglass", cls: "glass", hard: 5, dense: 2.4, val: 9, react: { salt: "is already of it" } },
        { id: "bone", cls: "organic", hard: 3, dense: 1.8, val: 4, react: { salt: "cures", water: "yellows" } },
        { id: "shell", cls: "organic", hard: 3, dense: 2.6, val: 6, react: { salt: "brightens", mica: "is scored" } },
        { id: "leather", cls: "organic", hard: 1, dense: 0.9, val: 5, react: { salt: "cures", water: "stiffens then rots" } },
        { id: "silk", cls: "fiber", hard: 1, dense: 0.3, val: 14, react: { water: "spots", salt: "crisps" } },
        { id: "gravel", cls: "stone", hard: 6, dense: 2.4, val: 1, react: { water: "settles" } }
      ];
      function now() {
        return typeof performance !== "undefined" ? performance.now() / 1e3 : Date.now() / 1e3;
      }
      function atlas() {
        try {
          if (typeof atlGen === "function") return atlGen();
        } catch (e) {
        }
        return null;
      }
      function rng(seed) {
        var s = seed >>> 0 || 1;
        return function() {
          s = Math.imul(s, 1664525) + 1013904223 >>> 0;
          return s / 4294967296;
        };
      }
      function pick(arr, r) {
        if (!arr || !arr.length) return null;
        return arr[Math.floor(r() * arr.length)];
      }
      function facetsOf(seed, inherit) {
        var key = String(seed);
        if (facCache[key]) return facCache[key];
        var r = rng(seed * 2654435761 >>> 0);
        var a = new Array(FACETS.length);
        for (var i = 0; i < FACETS.length; i++) {
          var base = inherit && inherit[i] != null ? inherit[i] : 50;
          a[i] = Math.max(0, Math.min(100, Math.round(base + (r() - 0.5) * 48 + (r() - 0.5) * 20)));
        }
        if (FAC_N > 4e3) {
          facCache = /* @__PURE__ */ Object.create(null);
          FAC_N = 0;
        }
        facCache[key] = a;
        FAC_N++;
        return a;
      }
      function ensureFig(fig) {
        if (!fig) return null;
        if (!fig.fac || fig.fac.length !== FACETS.length) fig.fac = facetsOf((fig.i + 1) * 9973 + (fig.born || 0));
        fig.spouse = fig.spouse == null ? -1 : fig.spouse;
        fig.kids = fig.kids || [];
        fig.feud = fig.feud == null ? -1 : fig.feud;
        return fig;
      }
      function notable(fac) {
        var out = [];
        for (var i = 0; i < FACETS.length; i++) {
          var v = fac[i];
          if (v <= 18) out.push(FACETS[i].lo);
          else if (v >= 82) out.push(FACETS[i].hi);
        }
        return out;
      }
      function describe(fac, she) {
        var n = notable(fac);
        if (!n.length) return (she || "They") + " is unremarkable in the ways that get written down.";
        var who = she || "They";
        return who + " " + n.slice(0, 4).join("; ") + ".";
      }
      function sheOf(fig) {
        if (!fig) return "They";
        if (fig.sex === "f") return "She";
        if (fig.sex === "m") return "He";
        var r = rng((fig.i + 11) * 17)();
        return r < 0.48 ? "He" : r < 0.96 ? "She" : "They";
      }
      function matById(id) {
        for (var i = 0; i < MATS.length; i++) if (MATS[i].id === id) return MATS[i];
        return MATS[0];
      }
      function react(a, b) {
        if (!a || !b || a.id === b.id) return "";
        if (a.react && a.react[b.id]) return a.id + " " + a.react[b.id] + " against " + b.id;
        if (b.react && b.react[a.id]) return b.id + " " + b.react[a.id] + " against " + a.id;
        if (a.react && a.react[b.cls]) return a.id + " " + a.react[b.cls] + " with " + b.id;
        if (b.react && b.react[a.cls]) return b.id + " " + b.react[a.cls] + " with " + a.id;
        return a.id + " and " + b.id + " sit together without a quarrel";
      }
      function pushEv(a, k2, rec) {
        var ev = Object.assign({ k: k2, y: a.now }, rec || {});
        a.e.push(ev);
        if (a.e.length > 1600) a.e.splice(0, a.e.length - 1200);
        if (a.e.length > 4200) a.e.splice(0, 400);
        yearLog.push(ev);
        if (yearLog.length > 24) yearLog.shift();
        return ev;
      }
      function writeShop(text) {
        try {
          if (typeof chronicle === "function") chronicle("era", "Year " + (atlas() && atlas().now) + ". " + text, []);
        } catch (e) {
        }
        try {
          if (window.weave && typeof weave.because === "function") weave.because(text);
          if (window.weave && typeof weave.rumor === "function") weave.rumor("atlas", text, 0.7);
        } catch (e2) {
        }
      }
      function firstName(n) {
        n = String(n || "Someone");
        return n.split(" ")[0] || n;
      }
      function childName(parent, r) {
        var bits = ["Ren", "Kae", "Sol", "Mir", "Tov", "Ash", "Len", "Iri", "Nem", "Ves", "Ora", "Sid"];
        var last = String(parent.n || "").split(" ").pop() || "of the water";
        return pick(bits, r) + " " + last;
      }
      function openTheFuture(a) {
        if (!a || a._future) return;
        a._future = 1;
        var open = 1e3;
        for (var i = 0; i < a.f.length; i++) {
          var f = a.f[i];
          if (!f) continue;
          if (f.died >= open) {
            var remain = 18 + f.i * 7919 % 42;
            f.died = open + remain;
            f._opened = 1;
          }
        }
      }
      function kinOf(fig, other) {
        if (!fig || !other || fig.i === other.i) return "";
        if (fig.par != null && other.par != null && fig.par === other.par) return "sibling";
        if (other.i === fig.par || fig.i === other.par) return "parent";
        if (fig.par != null && other.par != null) {
          var a = atlas();
          if (a && a.f) {
            var fp = a.f[fig.par];
            var op = a.f[other.par];
            if (fp && op && fp.par != null && op.par != null && fp.par === op.par) return "cousin";
          }
        }
        return "";
      }
      function yearSim() {
        var a = atlas();
        if (!a || !a.f || !a.f.length) return;
        openTheFuture(a);
        if (a.now === lastNow) return;
        lastNow = a.now;
        var r = rng((a.seed ^ a.now * 2654435761) >>> 0);
        var living = [];
        for (var i = 0; i < a.f.length; i++) {
          var f = a.f[i];
          if (!f) continue;
          if (f.died >= a.now) living.push(f);
        }
        a.living = living.map(function(f2) {
          return f2.i;
        });
        if (!living.length) return;
        living.sort(function(x, y) {
          return (y.fame || 0) - (x.fame || 0);
        });
        var nAct = Math.min(48, living.length);
        var births = 0;
        var arts = a.goods || (a.goods = []);
        function adult(fig2) {
          if (fig2._opened) return a.now < fig2.died - 1;
          var ag = a.now - fig2.born;
          return ag >= 16 && ag < 58;
        }
        for (var n = 0; n < nAct; n++) {
          var fig = ensureFig(living[n]);
          var fac = fig.fac;
          var place = fig.h >= 0 ? fig.h : 0;
          if (fig.died <= a.now && !fig._deadEv) {
            fig._deadEv = 1;
            fig.died = a.now;
            pushEv(a, "death", { a: fig.i, p: place });
            fig.deeds = (fig.deeds || 0) + 1;
            continue;
          }
          if (!adult(fig)) continue;
          if (fig.spouse < 0 && r() < 0.1) {
            var other = pick(living, r);
            if (other && other.i !== fig.i && adult(other)) {
              ensureFig(other);
              if (other.spouse < 0) {
                fig.spouse = other.i;
                other.spouse = fig.i;
                var blood2 = kinOf(fig, other);
                fig.close = !!blood2;
                other.close = !!blood2;
                fig.kin = blood2;
                other.kin = blood2;
                var mline = fig.n + " and " + other.n + " were married.";
                if (blood2 === "sibling") mline += " They are of one blood — siblings.";
                else if (blood2 === "parent") mline += " They are of one blood — parent and child.";
                else if (blood2 === "cousin") mline += " They are of one blood — cousins.";
                pushEv(a, blood2 ? "incest" : "marry", { a: fig.i, b: other.i, p: place, s: mline, kin: blood2 });
                fig.deeds++;
                fig.fame = (fig.fame || 0) + (blood2 ? 2 : 1);
                if (blood2) {
                  try {
                    if (window.weave && weave.because) weave.because(mline);
                    if (window.weave && weave.rumor) weave.rumor("blood", mline, 1.4);
                  } catch (eKin) {
                  }
                }
              }
            }
          }
          if (fig.spouse >= 0 && births < 3 && r() < 0.09) {
            var sp = a.f[fig.spouse];
            var close = !!(fig.close || sp && sp.close || kinOf(fig, sp));
            if (close && r() < 0.38) {
              pushEv(a, "still", {
                a: fig.i,
                p: place,
                s: "A child of " + fig.n + " and " + (sp && sp.n ? sp.n : "theirs") + " did not thrive. The line is close."
              });
              fig.deeds++;
              births++;
            } else {
              var kid = {
                i: a.f.length,
                n: childName(fig, r),
                r: fig.r,
                born: a.now,
                died: a.now + (close ? 8 + Math.floor(r() * 22) : 38 + Math.floor(r() * 50)),
                h: fig.h,
                w: fig.w,
                fame: 0,
                deeds: 0,
                par: fig.i,
                close
              };
              kid.fac = facetsOf(kid.i * 13 + a.now, fig.fac && sp && sp.fac ? fig.fac.map(function(v, ix) {
                return (v + (sp.fac[ix] || 50)) / 2;
              }) : fig.fac);
              if (close && kid.fac) {
                kid.fac[4] = Math.min(100, (kid.fac[4] || 50) + 25);
                kid.fac[8] = Math.min(100, (kid.fac[8] || 50) + 20);
              }
              a.f.push(kid);
              fig.kids = fig.kids || [];
              fig.kids.push(kid.i);
              births++;
              pushEv(a, "birth", {
                a: fig.i,
                b: kid.i,
                p: place,
                s: kid.n + " was born of " + fig.n + (close ? ". The blood is close." : ".")
              });
              fig.deeds++;
            }
          }
          if (fac[25] > 78 && fig.feud < 0 && r() < 0.04) {
            var foe = pick(living, r);
            if (foe && foe.i !== fig.i) {
              fig.feud = foe.i;
              pushEv(a, "feud", { a: fig.i, b: foe.i, p: place, s: fig.n + " took against " + foe.n + "." });
              fig.deeds++;
              fig.fame += 1;
            }
          }
          if (fac[49] > 76 && r() < 0.045) {
            var m1 = pick(MATS, r);
            var m2 = pick(MATS, r);
            var rx = react(m1, m2);
            var art = {
              n: firstName(fig.n) + "'s " + pick(["Wake", "Measure", "Chart", "Claim", "Quiet", "Salt-Mark"], r),
              who: fig.n,
              y: a.now,
              mat: m1.id + (m2 && m2.id !== m1.id ? " and " + m2.id : ""),
              rx
            };
            arts.push(art);
            if (arts.length > 40) arts.shift();
            pushEv(a, "art", { a: fig.i, p: place, s: fig.n + " made " + art.n + " of " + art.mat + ". " + rx + "." });
            fig.deeds++;
            fig.fame += 2;
            if (r() < 0.35) writeShop(fig.n + " made " + art.n + " of " + art.mat + ".");
            try {
              if (window.saga && saga.book) {
                var b = saga.book();
                b.arts = b.arts || [];
                b.arts.push(art);
                if (b.arts.length > 24) b.arts.shift();
              }
            } catch (eArt) {
            }
          }
          if (fac[30] > 74 && a.p && a.p.length && r() < 0.04) {
            var pl = a.p[Math.floor(r() * a.p.length)];
            if (pl && !pl.disc) {
              pl.disc = a.now;
              pushEv(a, "chart", { a: fig.i, p: pl.i });
              fig.deeds++;
              fig.fame += 3;
            } else if (pl) {
              fig.h = pl.i;
              fig.deeds++;
            }
          }
          if (fac[21] > 80 && fig.w >= 0 && r() < 0.012 && a.w && a.w.length > 1) {
            var otherW = Math.floor(r() * a.w.length);
            if (otherW !== fig.w && a.w[otherW] && !a.w[otherW].gone) {
              pushEv(a, "war", { w: fig.w, b: otherW, a: fig.i });
              fig.deeds += 2;
              fig.fame += 4;
              writeShop(fig.n + " of a power put water to war.");
            }
          }
        }
      }
      function wrapTick() {
        try {
          var d = Object.getOwnPropertyDescriptor(globalThis, "atlTick");
          if (d && d.get && !d.get.__live) {
            let ng = function() {
              var fn = g.call(this);
              if (typeof fn === "function" && !fn.__live) {
                var inner = function() {
                  var res = fn.apply(this, arguments);
                  try {
                    yearSim();
                  } catch (e) {
                  }
                  return res;
                };
                inner.__live = 1;
                return inner;
              }
              return fn;
            };
            var g = d.get;
            ng.__live = 1;
            Object.defineProperty(globalThis, "atlTick", { configurable: true, get: ng });
          }
        } catch (e) {
        }
      }
      function wrapLine() {
        try {
          var d = Object.getOwnPropertyDescriptor(globalThis, "atlLine");
          if (d && d.get && !d.get.__live) {
            let ng = function() {
              var fn = g.call(this);
              if (typeof fn === "function" && !fn.__live) {
                var inner = function(atl, ev) {
                  if (ev && ev.s) return ev.s;
                  if (ev && ev.k === "marry") return (ateName(atl, ev.a) || "Someone") + " and " + (ateName(atl, ev.b) || "another") + " were married.";
                  if (ev && ev.k === "incest") return ev.s || ateName(atl, ev.a) + " and " + ateName(atl, ev.b) + " were married. They are of one blood.";
                  if (ev && ev.k === "still") return ev.s || "A child of a close line did not thrive.";
                  if (ev && ev.k === "birth") return (ateName(atl, ev.b) || "A child") + " was born of " + (ateName(atl, ev.a) || "someone") + ".";
                  if (ev && ev.k === "feud") return (ateName(atl, ev.a) || "Someone") + " took against " + (ateName(atl, ev.b) || "another") + ".";
                  if (ev && ev.k === "art") return ev.s || ateName(atl, ev.a) + " made a thing that the record kept.";
                  return fn.apply(this, arguments);
                };
                inner.__live = 1;
                return inner;
              }
              return fn;
            };
            var g = d.get;
            ng.__live = 1;
            Object.defineProperty(globalThis, "atlLine", { configurable: true, get: ng });
          }
        } catch (e) {
        }
      }
      function ateName(atl, i) {
        try {
          if (atl && atl.f && atl.f[i]) return atl.f[i].n;
        } catch (e) {
        }
        return "somebody";
      }
      function wrapPerson() {
        try {
          if (typeof sgPerson !== "function" || sgPerson.__live) return;
          var orig = sgPerson;
          var wrapped = function(who) {
            var p = orig.apply(this, arguments);
            if (p && !p.fac) {
              p.fac = facetsOf((who || 0) >>> 0);
              p.mindLine = describe(p.fac, p.name ? p.name.split(" ")[0] : "They");
            }
            return p;
          };
          wrapped.__live = 1;
          try {
            Object.defineProperty(globalThis, "sgPerson", { configurable: true, get: function() {
              return wrapped;
            } });
          } catch (e2) {
          }
        } catch (e) {
        }
      }
      function esc(s) {
        var map = { "&": "&amp;", "<": "&lt;", ">": "&gt;", '"': "&quot;" };
        return String(s || "").replace(/[&<>"]/g, function(c) {
          return map[c];
        });
      }
      function enhanceAtlas() {
        var body = document.getElementById("dbody");
        var title = document.getElementById("dtitle");
        if (!body || !title) return;
        if (!/atlas/i.test(title.textContent || "")) return;
        var a = atlas();
        if (!a) return;
        yearSim();
        var html = '<div class="sec">The living year <span>' + a.now + "</span></div>";
        var livingN = 0;
        for (var i = 0; i < a.f.length; i++) if (a.f[i] && a.f[i].died >= a.now) livingN++;
        html += '<div class="row"><div></div><div><div class="d">' + livingN + " historical figures still alive. Each year they marry, feud, chart, die, and make things. The clock does not stop.</div></div><div></div></div>";
        var log = yearLog.slice().reverse();
        for (var y = 0; y < log.length && y < 6; y++) {
          var line = log[y].s;
          try {
            if (!line && typeof atlLine === "function") line = atlLine(a, log[y]);
          } catch (e) {
          }
          if (!line) continue;
          html += '<div class="row"><div></div><div><div class="d">' + esc(String(log[y].y)) + " · " + esc(line) + "</div></div><div></div></div>";
        }
        var goods = a.goods || [];
        if (goods.length) {
          html += '<div class="sec">Things made of matter</div>';
          for (var g = goods.length - 1; g >= 0 && g >= goods.length - 4; g--) {
            html += '<div class="row"><div></div><div><div class="n">' + esc(goods[g].n) + '</div><div class="d">' + esc(goods[g].mat) + (goods[g].rx ? ". " + esc(goods[g].rx) : "") + " · " + goods[g].y + "</div></div><div></div></div>";
          }
        }
        html += '<div class="sec">Matter</div>';
        html += '<div class="row"><div></div><div><div class="d">' + MATS.length + " substances. Hardness, density, value, and what they do to each other. Iron rusts in salt. Bogwood swells. Gold does nothing, which is the point of gold.</div></div><div></div></div>";
        var famed = a.f.slice().sort(function(x, y2) {
          return (y2.fame || 0) - (x.fame || 0);
        });
        var shown = 0;
        for (var fi = 0; fi < famed.length && shown < 3; fi++) {
          var fig = famed[fi];
          if (!fig || fig.died < a.now) continue;
          ensureFig(fig);
          html += '<div class="row"><div></div><div><div class="n">' + esc(fig.n) + ' <span class="pill">living</span></div><div class="d">' + esc(describe(fig.fac, sheOf(fig))) + "</div></div><div></div></div>";
          shown++;
        }
        var wrap = body.querySelector(".live-atlas");
        if (!wrap) {
          wrap = document.createElement("div");
          wrap.className = "live-atlas";
          body.insertBefore(wrap, body.firstChild);
        }
        if (wrap.getAttribute("data-h") !== String(html.length)) {
          wrap.innerHTML = html;
          wrap.setAttribute("data-h", String(html.length));
        }
      }
      function enhanceLife() {
        try {
          var body = document.getElementById("dbody");
          if (!body) return;
          var old = body.querySelector("live-life");
          if (old && old.parentNode) old.parentNode.removeChild(old);
        } catch (e) {
        }
      }
      function seedWiki() {
        try {
          var w = typeof WIKI === "function" ? WIKI() : WIKI;
          if (!w || !w.push) return;
          var extra = [
            {
              id: "k_live",
              sec: "The chronicle",
              t: "The living atlas",
              tags: "atlas living year historical figure marry feud birth death agent",
              w: "<p>Year 1000 was not the last year. Every day in the shop is a year out there. The people in the record are still marrying, charting, feuding, dying, and having children. Close blood is not forbidden in the record. Siblings marry. A parent and a child marry. The line is written: they are of one blood. Children of that union often do not thrive; the ones who live are sickly and short-lived. Atlas, The living year, is the feed.</p><p><b>What to do about it:</b> open Atlas after a few days. The green mark is alive. A marriage with a blood note is not a bug. It is the line paying.</p>"
            },
            {
              id: "k_facets",
              sec: "The street",
              t: "Fifty facets",
              tags: "personality facets brave greedy vengeful art curious",
              w: "<p>Every person in the record, and every person who can walk into the shop, is rated on fifty facets: greed, bravery, vengefulness, curiosity, art, duty, and the rest. Only the extremes get written down, the way a thought only writes down the extreme. Average people are silent. Extreme people start wars, make artifacts, or take against a neighbor.</p><p><b>What to do about it:</b> Atlas, a living name. The four lines under it are the facets that crossed the line.</p>"
            },
            {
              id: "k_matter",
              sec: "The shop floor",
              t: "Matter",
              tags: "material science iron salt rust bogwood gold hardness density react artifact",
              w: "<p>Bogwood, oak, mica, riverstone, brass, iron, copper, gold, salt, glass, driftglass, bone, shell, leather, silk, gravel. Each has hardness, density, value, and a table of what it does to the others. Iron rusts in salt. Copper goes green. Bogwood swells. Gold does nothing, which is why people want it. When a historical figure in a strange craft-mood makes an artifact, two substances are put together and the reaction is the description.</p><p><b>What to do about it:</b> Atlas, Things made of matter. The shop's own strange moods use the same table.</p>"
            }
          ];
          for (var i = 0; i < extra.length; i++) {
            var hit = false;
            for (var j = 0; j < w.length; j++) if (w[j] && w[j].id === extra[i].id) hit = true;
            if (!hit) w.push(extra[i]);
          }
        } catch (e) {
        }
      }
      function tick() {
        try {
          wrapTick();
          wrapLine();
          wrapPerson();
          seedWiki();
          if (now() - lastTick > 1.2) {
            lastTick = now();
            yearSim();
          }
          if (now() - lastUi > 0.9) {
            lastUi = now();
            enhanceAtlas();
            enhanceLife();
          }
        } catch (e) {
        }
      }
      window.liveAtlas = {
        sim: yearSim,
        facets: facetsOf,
        describe,
        mats: MATS,
        react,
        log: function() {
          return yearLog;
        }
      };
      if (window.__onBeat) window.__onBeat(tick, 280);
      else setTimeout(function loop() {
        tick();
        setTimeout(loop, 280);
      }, 280);
    })();
  } catch (e) {
    console.error("Layer live.js failed to start:", e);
  }

  // src/layers/desk.js
  try {
    (function() {
      "use strict";
      var lastUi = 0;
      var lastTick = 0;
      var lastHour = -1;
      var lastMissKey = "";
      var lastSaleN = -1;
      var tillReady = false;
      var wired = false;
      var didBrowse = false;
      var keep = null;
      var floor = [];
      var coming = null;
      function neighbor() {
        var g = gs();
        if (g && g.neighbor && g.neighbor.name) return g.neighbor;
        var n = {
          name: "Mae Costa",
          call: "The baker",
          job: "bakes on Salem Street",
          want: "goldfish",
          money: 34,
          visits: 0,
          bags: 0,
          last: "",
          nextD: 0,
          reason: "walks past when the ovens cool",
          known: false,
          trust: 0.4
        };
        try {
          if (g) g.neighbor = n;
        } catch (e) {
        }
        return n;
      }
      function neighborName() {
        var n = neighbor();
        return n.known ? n.name : "The baker on Salem";
      }
      function now() {
        return typeof performance !== "undefined" ? performance.now() / 1e3 : Date.now() / 1e3;
      }
      function year() {
        try {
          if (window.saga && typeof saga.year === "function") return saga.year();
        } catch (e) {
        }
        return 1e3;
      }
      function gs() {
        try {
          if (typeof gameState === "function") return gameState();
          if (typeof gameState === "object" && gameState) return gameState;
        } catch (e) {
        }
        return null;
      }
      function shopDay() {
        try {
          var g = gs();
          if (g && isFinite(g.t)) return Math.floor((g.t % 1e9 + 1e9) % 1e9 / 2400);
        } catch (e) {
        }
        return 0;
      }
      function hour() {
        try {
          if (typeof gameState === "object" && gameState && isFinite(gameState.t))
            return (gameState.t % 2400 + 2400) % 2400 / 100;
        } catch (e) {
        }
        return 12;
      }
      function hash32(s) {
        var h = 2166136261;
        s = String(s || "");
        for (var i = 0; i < s.length; i++) {
          h ^= s.charCodeAt(i);
          h = Math.imul(h, 16777619);
        }
        return h >>> 0;
      }
      function pick(arr, h) {
        if (!arr || !arr.length) return "";
        return arr[(h >>> 0) % arr.length];
      }
      function desk2() {
        var g = gs();
        if (g && g.desk && g.desk.short) {
          keep = g.desk;
          return keep;
        }
        if (keep && keep.short) {
          try {
            if (g) g.desk = keep;
          } catch (e) {
          }
          return keep;
        }
        keep = {
          short: [],
          long: [],
          core: [],
          fatigue: 0.2,
          pride: 0.4,
          worry: 0.2,
          last: "",
          coming: null
        };
        try {
          if (g) g.desk = keep;
        } catch (e2) {
        }
        return keep;
      }
      function think(kind, text) {
        if (!text) return;
        var d = desk2();
        for (var i = d.short.length - 1; i >= 0 && i >= d.short.length - 3; i--) {
          if (d.short[i] && d.short[i].s === text) return;
        }
        d.last = text;
        var rec = { k: kind || "", s: String(text), y: year(), at: now() };
        d.short.push(rec);
        if (d.short.length > 8) {
          var old = d.short.shift();
          if (old && (old.k === "miss" || old.k === "close" || old.k === "pair")) {
            d.long.push(old);
            if (d.long.length > 8) d.long.shift();
          }
        }
        if ((kind === "pair" || kind === "close" || kind === "year") && d.long.length && Math.random() < 0.3) {
          var hit = false;
          for (var i = 0; i < d.core.length; i++) if (d.core[i].s === text) hit = true;
          if (!hit) {
            d.core.push(rec);
            if (d.core.length > 5) d.core.shift();
          }
        }
      }
      function say(msg, kind) {
        try {
          if (typeof k === "function") k(msg, kind || "");
          else if (typeof toast === "function") toast(msg, kind || "");
        } catch (e) {
        }
      }
      function write(kind, text) {
        try {
          if (typeof chronicle === "function") {
            var line = String(text || "");
            if (line && !/^Year\s+\d/.test(line)) line = "Year " + year() + ". " + line;
            chronicle(kind, line, []);
          }
        } catch (e) {
        }
      }
      function namedFront() {
        try {
          var list = typeof allFish === "function" ? allFish() : [];
          var n = [];
          for (var i = 0; i < list.length; i++) if (list[i] && list[i].nick) n.push(list[i].nick);
          return n;
        } catch (e) {
        }
        return [];
      }
      function kindFor(idx, rec) {
        if (rec && rec.kind) return rec.kind;
        var seed = idx * 9973 + 17 >>> 0;
        try {
          if (window.shopLife && shopLife.regulars) {
            var regs = shopLife.regulars();
            if (regs && regs.length && rec && rec.idx != null) {
              for (var r = 0; r < regs.length; r++) if (regs[r] && regs[r].visits > 1 && seed % 5 === 0) return "neighbor";
            }
          }
        } catch (e) {
        }
        if (namedFront().length && seed % 6 === 0) return "collector";
        return KIND[seed % KIND.length];
      }
      function lineForKind(kind, rec) {
        var want = rec && rec.want || "fish";
        var have = !!(rec && rec.haveWant);
        var h = hash32(kind + want + (rec && rec.idx));
        if (kind === "kid") {
          return have ? pick(["Can we get the " + want + "?", "The little one. How much?", "Look, look — that " + want + "."], h) : pick(["Do you have a " + want + "?", "I wanted a " + want + ".", "Mum said maybe a " + want + "."], h);
        }
        if (kind === "collector") {
          return have ? pick(["I know a " + want + " when I see one.", "That's the one. Name?", "How much for the " + want + ". Don't round it."], h) : pick(["You don't keep " + want + "?", "I came for a " + want + ". The window promised otherwise.", "I'll wait for stock."], h);
        }
        if (kind === "lunch") {
          return have ? pick(["The " + want + ". I have twelve minutes.", "How much. Lunch.", "Bag it."], h) : pick(["Got a " + want + "? I'm on lunch.", "Not today then.", "After the boats."], h);
        }
        if (kind === "neighbor") {
          return have ? pick(["The usual.", "You still keep " + want + ".", "Wrap it, I know the way."], h) : pick(["Where's the " + want + " gone?", "I'll come back when you have them.", "Tell me when the pair's in."], h);
        }
        if (kind === "seller") {
          return pick(["I've got a " + want + " in a bucket.", "You buy, or I walk it down the street.", "Off the boats. Cheap."], h);
        }
        return "";
      }
      function wrapBrowse() {
        if (didBrowse || !window.shopBrowse) return;
        didBrowse = true;
        var orig = window.shopBrowse;
        window.shopBrowse = function(idx, slot, W, floorY, personS, simT) {
          var rec = orig.apply(this, arguments);
          try {
            if (!rec) return rec;
            var st = window.shopLife && shopLife.browse ? shopLife.browse()[idx] : null;
            var kind = kindFor(idx, st || rec);
            rec.kind = kind;
            if (st) st.kind = kind;
            if ((rec.phase === "look" || rec.phase === "pay") && Math.random() < 0.62) {
              var line = lineForKind(kind, st || rec);
              if (line) rec.line = line;
            }
            if (rec.line && (rec.phase === "look" || rec.phase === "pay" || rec.phase === "enter")) {
              hear(rec.name || rec.kind || "Someone", rec.line, kind);
            }
            if (kind === "seller" && rec.phase === "look" && st && !st.offered) {
              st.offered = 1;
              onSeller(st, rec);
            }
            if (rec.phase === "leave" && st && !st.bought && !st._deskMiss) {
              st._deskMiss = 1;
              if (!st.coming && st.want && !st.haveWant) think("pair", "A unique fish is a display. A pair is stock.");
            }
          } catch (e) {
          }
          return rec;
        };
      }
      function onSeller(st, rec) {
        var d = desk2();
        var want = st && st.want || "fish";
        var coins = 0;
        try {
          var g = gs();
          coins = g && g.coins;
        } catch (e) {
        }
        var short = true;
        try {
          if (st.haveWant) short = false;
        } catch (e2) {
        }
        if (short && coins > 40) {
          d.worry = Math.min(1, d.worry + 0.04);
          think("buy", "A bucket on the counter. I took the " + want + ". We didn't have a pair.");
          write("sale", "A " + want + " came in off the street in a bucket. The shop was short a pair.");
          try {
            if (typeof addRep === "function") addRep(1);
          } catch (e3) {
          }
        } else {
          think("pass", "A bucket on the counter. I didn't take it.");
        }
      }
      function watchTill() {
        try {
          if (!window.shopLife || !shopLife.day) return;
          var day = shopLife.day();
          var miss = shopLife.misses ? shopLife.misses() : [];
          if (!tillReady) {
            if (!day || !isFinite(day.sales)) return;
            lastSaleN = day.sales;
            if (miss && miss.length) {
              var m0 = miss[miss.length - 1];
              lastMissKey = (m0.name || "") + "|" + (m0.want || "") + "|" + (m0.why || "") + "|" + (m0.y || "");
            }
            tillReady = true;
            return;
          }
          if (day && day.sales === lastSaleN + 1) {
            lastSaleN = day.sales;
            var d = desk2();
            d.pride = Math.min(1, d.pride + 0.05);
            d.fatigue = Math.min(1, d.fatigue + 0.03);
            think("sale", "Paper. Water. The bell.");
          } else if (day) lastSaleN = day.sales;
          if (miss && miss.length) {
            var ms = miss[miss.length - 1];
            var key = (ms.name || "") + "|" + (ms.want || "") + "|" + (ms.why || "") + "|" + (ms.y || "");
            if (key !== lastMissKey) {
              lastMissKey = key;
              var d3 = desk2();
              d3.worry = Math.min(1, d3.worry + 0.06);
              if (ms.why === "stock") {
                think("miss", (ms.name || "Someone") + " came for a " + (ms.want || "fish") + " I did not have.");
                think("pair", "A unique fish is a display. A pair is stock.");
              } else if (ms.why === "clog") {
                think("miss", "They looked at the water and thought twice. I would have too.");
              } else {
                think("miss", (ms.name || "Someone") + " left without a fish bag.");
              }
            }
          }
        } catch (e) {
        }
      }
      function watchHours() {
        var h = hour();
        var d = desk2();
        if (lastHour < 0) {
          lastHour = h;
          return;
        }
        if (lastHour < 8 && h >= 8) {
          think("open", "Lights on. Year " + year() + ".");
          d.fatigue = Math.max(0.1, d.fatigue * 0.5);
        }
        if (lastHour < 19.4 && h >= 19.4) {
          var day = window.shopLife && shopLife.day ? shopLife.day() : null;
          var line = "Sign over." + (day ? " " + (day.sales || 0) + " sold, " + (day.misses || 0) + " walked." : "");
          think("close", line);
          write("note", line);
          say(line, "");
          d.fatigue = Math.min(1, d.fatigue + 0.15);
          if (day && day.misses > day.sales) {
            think("close", "Walked beat sold. That is the lesson.");
            d.core.push({ k: "close", s: "Walked beat sold.", y: year(), at: now() });
          }
        }
        lastHour = h;
        d.fatigue = Math.max(0, d.fatigue - 2e-3);
      }
      function currentThought() {
        var d = desk2();
        if (d.core.length && Math.random() < 0.25) return d.core[d.core.length - 1].s;
        if (d.short.length) return d.short[d.short.length - 1].s;
        if (d.worry > 0.55) return "The filter is going to pack and I can hear it from here.";
        if (d.fatigue > 0.7) return "Last hour always lasts.";
        if (d.pride > 0.6) return "The tetras are up. That is enough for a morning.";
        return "Year " + year() + " and I still look at the door when it opens.";
      }
      function esc(s) {
        var map = { "&": "&amp;", "<": "&lt;", ">": "&gt;", '"': "&quot;" };
        return String(s || "").replace(/[&<>"]/g, function(c) {
          return map[c];
        });
      }
      function hear(who, line, kind) {
        if (!line) return;
        if (floor.length && floor[floor.length - 1].s === line) return;
        floor.push({ who: who || "Someone", s: String(line), k: kind || "", at: now() });
        if (floor.length > 8) floor.shift();
      }
      function clockWord(h) {
        var hr = Math.floor(h);
        var m = Math.round((h - hr) * 60);
        if (m >= 60) {
          hr++;
          m = 0;
        }
        var h12 = hr % 12;
        if (!h12) h12 = 12;
        if (m === 0) return h12 + (hr >= 12 ? " this afternoon" : " this morning");
        if (m === 30) return "half past " + h12;
        return h12 + ":" + (m < 10 ? "0" : "") + m;
      }
      function hoped() {
        try {
          if (window.shopLife && typeof shopLife.hoped === "function") return shopLife.hoped() || ["guppy"];
        } catch (e) {
        }
        return ["betta", "guppy", "tetra", "goldfish", "angelfish", "cory"];
      }
      function stockHave() {
        try {
          if (window.shopLife && typeof shopLife.stock === "function") return shopLife.stock() || [];
        } catch (e) {
        }
        return [];
      }
      function seedComing() {
        var d = desk2();
        var n = neighbor();
        if (d.coming && d.coming.d === shopDay()) {
          coming = d.coming;
          return coming;
        }
        var h = hour();
        if (h > 17.5) return coming;
        var due = n.nextD === shopDay() || n.nextD === 0;
        var who = due ? neighborName() : "";
        if (!due) {
          try {
            var regs = window.shopLife && shopLife.regulars ? shopLife.regulars() : [];
            for (var r = 0; r < (regs || []).length; r++) if (regs[r] && regs[r].name && regs[r].name !== n.name) {
              who = regs[r].name;
              break;
            }
          } catch (e) {
          }
          if (!who) who = "Someone off the boats";
        }
        var have = stockHave();
        var want = due ? n.bags ? n.want || "goldfish" : "goldfish" : hoped()[(hash32(year() + "w") >>> 0) % hoped().length];
        if (due && n.bags && have.indexOf("goldfish") >= 0 && Math.random() < 0.4) {
          want = n.want && n.want !== "goldfish" ? n.want : "tetra";
        }
        var when = due ? n.bags ? 11.2 : 16.9 : Math.max(h + 0.8, 10.5 + (hash32(year() + "h") >>> 0) % 70 / 10);
        if (when > 17.2) when = 16.5;
        coming = {
          name: who,
          want,
          hour: when,
          done: false,
          y: year(),
          d: shopDay(),
          had: have.indexOf(want) >= 0,
          neighbor: due ? 1 : 0
        };
        d.coming = coming;
        think("open", coming.name + " at " + clockWord(coming.hour) + ", for " + coming.want + ".");
        return coming;
      }
      function watchComing() {
        seedComing();
        if (!coming || coming.done) return;
        var h = hour();
        if (h < coming.hour) return;
        coming.done = true;
        coming.arrived = now();
        var d = desk2();
        d.coming = coming;
        var n = neighbor();
        var have = stockHave();
        var wet = false;
        try {
          var wetCut = coming.neighbor && n.trust > 0.55 ? 0.62 : 0.42;
          wet = !!(window.shopSite && shopSite.wet() > wetCut);
        } catch (eW) {
        }
        var broke = coming.neighbor && n.money < 12;
        var ok = have.indexOf(coming.want) >= 0 && !wet && !broke;
        coming.had = ok;
        var line;
        if (wet && coming.neighbor) {
          line = n.known ? "I bake. I don't stand in water." : "The floor's wet. I'll come back.";
        } else if (broke && coming.neighbor) {
          line = "After the flour bill. Not today.";
        } else if (coming.neighbor && n.last && /walked/.test(n.last) && ok) {
          line = "You kept the " + coming.want + ". I knew you would.";
        } else if (coming.neighbor && n.last && /walked/.test(n.last) && !ok) {
          line = "Still no " + coming.want + ". I asked.";
        } else if (coming.neighbor && n.bags && ok) {
          line = "The last one is still in the bakery window. Another " + coming.want + ".";
        } else if (coming.neighbor && n.bags && !ok) {
          line = "I thought you'd keep " + coming.want + " after last time.";
        } else if (ok) {
          line = "The " + coming.want + ". I said I'd come.";
        } else {
          line = "You don't keep " + coming.want + "?";
        }
        try {
          if (window.faith && faith.line && coming.want && faith.taboo && faith.taboo() === coming.want) {
            var fl = faith.line();
            if (fl) line = fl;
          } else if (window.beast && beast.near && beast.near() > 0.14) {
            var bl = beast.line && beast.line();
            if (bl) line = bl;
          } else if (window.tongue && tongue.say) {
            line = tongue.say(coming.name, line);
          }
        } catch (eF) {
        }
        hear(coming.name, line, "neighbor");
        try {
          var browse = window.shopLife && shopLife.browse ? shopLife.browse() : null;
          if (browse) {
            var t = now();
            browse[2] = {
              tank: 0,
              until: t + 5.5,
              phase: ok ? "pay" : "look",
              line,
              lineUntil: t + 5.5,
              bought: !!ok,
              want: coming.want,
              haveWant: ok,
              coming: 1,
              guestName: coming.name
            };
          }
        } catch (eB) {
        }
        if (ok) {
          think("sale", coming.name + " came for the " + coming.want + ". I had a pair.");
          try {
            var day = window.shopLife && shopLife.day ? shopLife.day() : null;
            if (day) day.sales = (day.sales || 0) + 1;
          } catch (eD) {
          }
          if (coming.neighbor) {
            n.bags = (n.bags || 0) + 1;
            n.visits = (n.visits || 0) + 1;
            n.known = true;
            n.last = "took the " + coming.want;
            n.money = Math.max(0, (n.money || 0) - 12);
            n.trust = Math.min(1, (n.trust || 0.4) + 0.2);
            n.nextD = shopDay() + 1;
            n.want = coming.want;
            think("close", n.name + " will be back. She has a name now.");
          }
          try {
            var regs = window.shopLife && shopLife.regulars ? shopLife.regulars() : [];
            for (var r = 0; r < (regs || []).length; r++) {
              if (regs[r] && regs[r].name === coming.name) {
                regs[r].visits = (regs[r].visits || 0) + 1;
                regs[r].spent = (regs[r].spent || 0) + 12;
              }
            }
          } catch (eR) {
          }
          try {
            var g = gs();
            if (g) g.coins = (g.coins || 0) + 12;
          } catch (eC) {
          }
          try {
            if (window.weave && weave.because) weave.because(coming.name + " bought because we kept a pair of " + coming.want + ".");
          } catch (e) {
          }
          try {
            if (window.faith && faith.onSale) faith.onSale(coming.want);
          } catch (eF2) {
          }
          try {
            if (window.guild && guild.noteSale) guild.noteSale(coming.want);
          } catch (eG) {
          }
          try {
            if (window.craft && craft.noteBag) craft.noteBag(true);
          } catch (eK) {
          }
          try {
            if (typeof k === "function") k(coming.name + " came for the " + coming.want + ".", "gold");
          } catch (e2) {
          }
          try {
            if (window.feel && feel.play) feel.play("chime");
          } catch (e3) {
          }
        } else {
          think(
            "miss",
            wet ? coming.name + " turned around. The aisle was wet." : broke ? coming.name + " had no money today." : coming.name + " came for a " + coming.want + " I did not have."
          );
          try {
            var day2 = window.shopLife && shopLife.day ? shopLife.day() : null;
            if (day2) day2.misses = (day2.misses || 0) + 1;
            var miss = window.shopLife && shopLife.misses ? shopLife.misses() : null;
            if (miss) {
              miss.push({
                name: coming.name,
                want: coming.want,
                why: wet ? "wet" : broke ? "broke" : "stock",
                y: year()
              });
              if (miss.length > 16) miss.shift();
            }
          } catch (eM) {
          }
          try {
            if (window.weave && weave.because)
              weave.because(
                coming.name + (wet ? " walked because the aisle was wet" : broke ? " walked because the flour bill came first" : " walked because we had no pair of " + coming.want)
              );
          } catch (e4) {
          }
          try {
            if (window.guild && guild.noteMiss) guild.noteMiss(coming.want);
          } catch (eGm) {
          }
          try {
            if (window.craft && craft.noteBag) craft.noteBag(false);
          } catch (eKm) {
          }
          try {
            if (typeof k === "function")
              k(
                wet ? coming.name + " saw the wet boards and left." : coming.name + " came. We didn't have the " + coming.want + ".",
                "bad"
              );
          } catch (e5) {
          }
          if (coming.neighbor) {
            n.visits = (n.visits || 0) + 1;
            if (wet) n.last = "turned. the aisle was wet";
            else if (broke) n.last = "couldn't pay. flour bill";
            else n.last = "walked. no " + coming.want;
            n.nextD = shopDay() + 1;
            n.want = coming.want;
            n.trust = Math.max(0, (n.trust || 0.4) - 0.12);
            think("close", wet ? n.call + " turned around. The boards were wet." : n.call + " will try again tomorrow.");
          }
        }
      }
      function watchFish() {
        try {
          if (typeof allFish !== "function") return;
          var list = allFish() || [];
          for (var i = 0; i < list.length; i++) {
            var f = list[i];
            if (f && f.nick && f.mind && f.mind.stress > 0.7 && !f.mind._kept) {
              f.mind._kept = 1;
              think("water", (f.nick || "One of them") + " is holding too still. I would not bag that.");
              return;
            }
          }
        } catch (e) {
        }
      }
      function watchWalk() {
        var n = neighbor();
        if (!coming || !coming.neighbor || coming.done) return;
        if (hour() < 9 || hour() >= coming.hour - 0.1) return;
        if (n._walked === shopDay()) return;
        n._walked = shopDay();
        hear(neighborName(), n.last && /wet/.test(n.last) ? "Still wet?" : n.bags ? "Just looking. The last one is fine." : "Just looking. Ovens are cooling.", "neighbor");
      }
      function enhanceLife() {
        var body = document.getElementById("dbody");
        var title = document.getElementById("dtitle");
        if (!body || !title) return;
        if (!/Your life|Life/i.test(title.textContent || "")) return;
        ["hook-life", "weave-life", "mind-life", "saga-street", "saga-life", "live-life", "life-so-far-fill"].forEach(function(cls) {
          var n = body.querySelectorAll("." + cls);
          for (var i = 0; i < n.length; i++) n[i].style.display = "none";
        });
        var d = desk2();
        var html = '<div class="sec">Today</div>';
        var wxLine = "";
        try {
          if (window.saga && saga.book) {
            var b = saga.book();
            if (b && b.wx) wxLine = b.wx;
            if (b && b.water) wxLine = (wxLine ? wxLine + " · " : "") + b.water;
          }
        } catch (eW) {
        }
        html += '<div class="note">Year ' + year() + " · " + clockWord(hour()) + (wxLine ? " · " + esc(wxLine) : "") + ".</div>";
        try {
          var nb = neighbor();
          html += '<div class="row"><div></div><div><div class="n">' + esc(nb.known ? nb.name : nb.call) + '</div><div class="d">' + esc(nb.job) + " · " + esc(nb.reason) + (nb.last ? " · last: " + esc(nb.last) : " · wants " + esc(nb.want)) + (nb.nextD === shopDay() ? " · due today" : "") + ".</div></div><div></div></div>";
        } catch (eN) {
        }
        try {
          if (window.shopSite && shopSite.grid) {
            html += '<div class="sec">The site</div>' + shopSite.grid();
            html += '<div class="row"><div></div><div><div class="d">' + esc(shopSite.line()) + "</div></div><div></div></div>";
          }
        } catch (eS) {
        }
        var entries = [];
        function add(kind, text, who) {
          if (!text) return;
          for (var i = 0; i < entries.length; i++) if (entries[i].s === text) return;
          entries.push({ k: kind || "", s: String(text), who: who || "" });
        }
        if (coming && !coming.done) {
          add(
            "due",
            coming.name + " at " + clockWord(coming.hour) + ", for " + coming.want + ". " + (stockHave().indexOf(coming.want) >= 0 ? "We have a pair." : "We do not have a pair.")
          );
        } else if (coming && coming.done) {
          add("came", coming.name + (coming.had ? " took the " + coming.want + "." : " walked. No " + coming.want + "."));
        }
        for (var si = 0; si < d.short.length; si++) {
          if (d.short[si].k === "sale" && /Paper/.test(d.short[si].s || "")) continue;
          add(d.short[si].k, d.short[si].s);
        }
        for (var f = 0; f < floor.length; f++) add("glass", floor[f].s, floor[f].who);
        try {
          if (window.shopSite && shopSite.log) {
            var sl = shopSite.log();
            for (var L = 0; L < sl.length; L++) add("site", sl[L].s);
          }
        } catch (eL) {
        }
        try {
          var bags = window.__bagsThisOpen || 0;
          add("run", bags ? bags + (bags === 1 ? " fish bag since the door opened." : " fish bags since the door opened.") : "The till is quiet.");
        } catch (eH) {
        }
        try {
          if (window.weave && weave.of) {
            var bec = weave.of().because || [];
            if (bec.length) add("because", bec[bec.length - 1].s);
          }
        } catch (eB) {
        }
        try {
          if (window.choir && choir.line) {
            var cl = choir.line();
            if (cl) add("glass", cl);
          }
        } catch (eCh) {
        }
        try {
          if (window.going && going.line) {
            var gl = going.line();
            if (gl) add("block", gl);
          }
          if (window.going && going.homes) {
            var hs = going.homes();
            var shown = 0;
            for (var hi = 0; hi < hs.length && shown < 2; hi++) {
              if (!hs[hi] || hs[hi].dead) continue;
              var call = hs[hi].nick || "the " + hs[hi].sp;
              add("block", call + " is in " + hs[hi].who + "'s window.");
              shown++;
            }
          }
          if (window.going && going.letter) {
            var lt = going.letter();
            if (lt && lt.open && !lt.filled) {
              add("hold", "A letter from " + lt.from + ". They asked for a pair of " + lt.want + ".");
            }
          }
        } catch (eGo) {
        }
        try {
          if (window.late && late.line) {
            var ll = late.line();
            if (ll) add("late", ll);
          }
          if (window.late && late.keeper) {
            var kp = late.keeper();
            if (kp && kp.n) add("late", kp.n + " kept this shop through Year " + kp.leftY + ".");
          }
          if (window.late && late.missed) {
            var md = late.missed();
            for (var mi = 0; mi < md.length && mi < 3; mi++) add("away", md[mi]);
          }
        } catch (eLa) {
        }
        try {
          if (window.pane && pane.line) {
            var pl = pane.line();
            if (pl) add("pane", pl);
          }
          if (window.pane && pane.clarity && pane.clarity() < 0.5) {
            add("pane", "They can't see in.");
          }
        } catch (ePa) {
        }
        try {
          if (window.sill && sill.line) {
            var sln = sill.line();
            if (sln) add("sill", sln);
          }
        } catch (eSi) {
        }
        try {
          if (window.kin && kin.line) {
            var kl = kin.line();
            if (kl) add("kin", kl);
          }
          if (window.kin && kin.folk) {
            var fk = kin.folk();
            var shownK = 0;
            for (var ki = 0; ki < fk.length && shownK < 3; ki++) {
              if (!fk[ki] || fk[ki].dead) continue;
              add("kin", fk[ki].n + " · " + fk[ki].job + (fk[ki].last ? " · " + fk[ki].last : "."));
              shownK++;
            }
          }
        } catch (eKi) {
        }
        try {
          if (window.relic && relic.line) {
            var rl = relic.line();
            if (rl) add("relic", rl);
          }
        } catch (eRe) {
        }
        try {
          if (window.ill && ill.line) {
            var il = ill.line();
            if (il) add("ill", il);
          }
        } catch (eIl) {
        }
        try {
          if (window.mark && mark.line) {
            var ml = mark.line();
            if (ml) add("mark", ml);
          }
        } catch (eMk) {
        }
        try {
          if (window.soul && soul.line) {
            var soln = soul.line();
            if (soln) add("soul", soln);
          }
        } catch (eSo) {
        }
        try {
          if (window.pinch && pinch.line) {
            var pnl = pinch.line();
            if (pnl) add("pinch", pnl);
          }
        } catch (ePi) {
        }
        try {
          if (window.hand && hand.line) {
            var hl = hand.line();
            if (hl) add("hand", hl);
          }
        } catch (eHa) {
        }
        try {
          if (window.cut && cut.line) {
            var ctl = cut.line();
            if (ctl) add("cut", ctl);
          }
        } catch (eCu) {
        }
        try {
          if (window.wane && wane.line) {
            var wl = wane.line();
            if (wl) add("wane", wl);
          }
        } catch (eWa) {
        }
        try {
          if (window.spiral && spiral.line) {
            var spl = spiral.line();
            if (spl) add("spiral", spl);
          }
        } catch (eSp) {
        }
        try {
          if (window.blood && blood.line) {
            var bll = blood.line();
            if (bll) add("blood", bll);
          }
        } catch (eBl) {
        }
        try {
          if (window.stray && stray.line) {
            var srl = stray.line();
            if (srl) add("stray", srl);
          }
        } catch (eSr) {
        }
        try {
          if (window.row && row.line) {
            var rwl = row.line();
            if (rwl) add("row", rwl);
          }
        } catch (eRw) {
        }
        try {
          if (window.mask && mask.line) {
            var msl = mask.line();
            if (msl) add("mask", msl);
          }
        } catch (eMs) {
        }
        try {
          if (window.age && age.line) {
            var agl = age.line();
            if (agl) add("age", agl);
          }
        } catch (eAg) {
        }
        try {
          if (window.hold && hold.line) {
            var hol = hold.line();
            if (hol) add("hold", hol);
          }
        } catch (eHo) {
        }
        try {
          if (window.lord && lord.line) {
            var ldl = lord.line();
            if (ldl) add("lord", ldl);
          }
        } catch (eLd) {
        }
        try {
          if (window.great && great.line) {
            var grl = great.line();
            if (grl) add("great", grl);
          }
        } catch (eGr) {
        }
        try {
          if (window.wonder && wonder.line) {
            var wdl = wonder.line();
            if (wdl) add("wonder", wdl);
          }
        } catch (eWd) {
        }
        try {
          if (window.envoy && envoy.line) {
            var evl = envoy.line();
            if (evl) add("envoy", evl);
          }
        } catch (eEv) {
        }
        try {
          if (window.vow && vow.line) {
            var vwl = vow.line();
            if (vwl) add("vow", vwl);
          }
        } catch (eVw) {
        }
        try {
          if (window.siege && siege.line) {
            var sgl = siege.line();
            if (sgl) add("siege", sgl);
          }
        } catch (eSg) {
        }
        try {
          if (window.pact && pact.line) {
            var pcl = pact.line();
            if (pcl) add("pact", pcl);
          }
        } catch (ePc) {
        }
        try {
          if (window.lux && lux.line) {
            var lxl = lux.line();
            if (lxl) add("lux", lxl);
          }
        } catch (eLx) {
        }
        try {
          if (window.spy && spy.line) {
            var syl = spy.line();
            if (syl) add("spy", syl);
          }
        } catch (eSy) {
        }
        try {
          if (window.seat && seat.line) {
            var sel = seat.line();
            if (sel) add("seat", sel);
          }
        } catch (eSe) {
        }
        try {
          if (window.house && house.line) {
            var hul = house.line();
            if (hul) add("house", hul);
          }
        } catch (eHu) {
        }
        try {
          if (window.wed && wed.line) {
            var wel = wed.line();
            if (wel) add("wed", wel);
          }
        } catch (eWe) {
        }
        try {
          if (window.heir && heir.line) {
            var hil = heir.line();
            if (hil) add("heir", hil);
          }
        } catch (eHi) {
        }
        try {
          if (window.bent && bent.line) {
            var bel = bent.line();
            if (bel) add("bent", bel);
          }
        } catch (eBe) {
        }
        try {
          if (window.plot && plot.line) {
            var ptl = plot.line();
            if (ptl) add("plot", ptl);
          }
        } catch (ePt) {
        }
        try {
          if (window.hush && hush.line) {
            var hsl = hush.line();
            if (hsl) add("hush", hsl);
          }
        } catch (eHs) {
        }
        try {
          if (window.bond && bond.line) {
            var bol = bond.line();
            if (bol) add("bond", bol);
          }
        } catch (eBo) {
        }
        try {
          if (window.claim && claim.line) {
            var cml = claim.line();
            if (cml) add("claim", cml);
          }
        } catch (eCm) {
        }
        try {
          if (window.fray && fray.line) {
            var fll = fray.line();
            if (fll) add("fray", fll);
          }
        } catch (eFl) {
        }
        try {
          if (window.feast && feast.line) {
            var fsl = feast.line();
            if (fsl) add("feast", fsl);
          }
        } catch (eFs) {
        }
        try {
          if (window.ward && ward.line) {
            var wdl2 = ward.line();
            if (wdl2) add("ward", wdl2);
          }
        } catch (eWr) {
        }
        try {
          if (window.gyve && gyve.line) {
            var gyl = gyve.line();
            if (gyl) add("gyve", gyl);
          }
        } catch (eGy) {
        }
        try {
          if (window.fief && fief.line) {
            var fil = fief.line();
            if (fil) add("fief", fil);
          }
        } catch (eFi) {
        }
        try {
          if (window.mind && mind.dwell && typeof allFish === "function") {
            var fish = allFish() || [];
            for (var fi = 0; fi < fish.length; fi++) {
              if (fish[fi] && fish[fi].nick) {
                var dw = mind.dwell(fish[fi]);
                if (dw) {
                  add("tank", dw, fish[fi].nick);
                  break;
                }
              }
            }
          }
        } catch (eM) {
        }
        try {
          if (window.liveAtlas && liveAtlas.log) {
            var yl = liveAtlas.log();
            if (yl.length && yl[yl.length - 1].s) add("out", yl[yl.length - 1].s);
          }
        } catch (eO) {
        }
        html += '<div class="sec">The daybook</div>';
        var start = Math.max(0, entries.length - 16);
        for (var e = start; e < entries.length; e++) {
          html += '<div class="row book-line"><div>' + esc(entries[e].who || entries[e].k || "") + '</div><div><div class="d">' + esc(entries[e].s) + "</div></div><div></div></div>";
        }
        if (entries.length <= 1) {
          html += '<div class="row"><div></div><div><div class="d">The book is still the first page. The street will write the rest.</div></div><div></div></div>';
        }
        var wrap = body.querySelector(".desk-life");
        if (!wrap) {
          wrap = document.createElement("div");
          wrap.className = "desk-life";
          body.insertBefore(wrap, body.firstChild);
        }
        if (wrap.getAttribute("data-h") !== String(html.length)) {
          wrap.innerHTML = html;
          wrap.setAttribute("data-h", String(html.length));
        }
      }
      function seedWiki() {
        try {
          var w = typeof WIKI === "function" ? WIKI() : WIKI;
          if (!w || !w.push) return;
          var extra = [
            {
              id: "k_desk",
              sec: "The shop floor",
              t: "Behind the counter",
              tags: "keeper thoughts desk fatigue pride sale miss closing",
              w: "<p>Life is a daybook. One page for the day: who is due, what the keeper thought, what the water did, what the street said, and the site under the boards. Heat moves. Standing water runs downhill. A packed filter leaks onto oak. The little map is the shop as a place, not a menu.</p><p><b>What to do about it:</b> read Life when the floor is quiet. The gold line is the unfinished thing. The daybook is why.</p>"
            },
            {
              id: "k_talk",
              sec: "The shop floor",
              t: "The sale is a conversation",
              tags: "haggle kid collector lunch neighbor seller counter recettear want",
              w: "<p>People do not all come in the same. A kid asks if they can get the little one. A collector knows a tetra from a tetra and does not want you to round it. Lunch has twelve minutes. A neighbor wants the usual. Once in a while someone puts a bucket on the counter.</p><p>Someone is always coming. Life says who, and for what. Keep a pair before they walk in. That is the next hour. <b>What to do about it:</b> the gold line. Life, Coming. At the glass is what they actually said.</p>"
            }
          ];
          for (var i = 0; i < extra.length; i++) {
            var hit = false;
            for (var j = 0; j < w.length; j++) if (w[j] && w[j].id === extra[i].id) hit = true;
            if (!hit) w.push(extra[i]);
          }
        } catch (e) {
        }
      }
      var opened = false;
      function tick() {
        try {
          wrapBrowse();
          seedWiki();
          if (!opened) {
            opened = true;
            think("open", "Year " + year() + " and I still look at the door when it opens.");
          }
          if (now() - lastTick > 0.9) {
            lastTick = now();
            watchTill();
            watchHours();
            watchComing();
            watchWalk();
            watchFish();
          }
          if (now() - lastUi > 0.7) {
            lastUi = now();
            enhanceLife();
          }
        } catch (e) {
        }
      }
      window.desk = {
        think,
        of: desk2,
        thought: currentThought,
        coming: function() {
          return coming || desk2() && desk2().coming;
        },
        neighbor,
        floor: function() {
          return floor;
        }
      };
      if (window.__onBeat) window.__onBeat(tick, 280);
      else setTimeout(function loop() {
        tick();
        setTimeout(loop, 280);
      }, 280);
    })();
  } catch (e) {
    console.error("Layer desk.js failed to start:", e);
  }

  // src/layers/hook.js
  try {
    (function() {
      "use strict";
      var lastTick = 0;
      var lastUi = 0;
      var lastSales = -1;
      var lastMissKey = "";
      var whisperAt = 0;
      var shown = "";
      var comboUntil = 0;
      var tillReady = false;
      var sawTill = 0;
      var booted = false;
      function now() {
        return typeof performance !== "undefined" ? performance.now() / 1e3 : Date.now() / 1e3;
      }
      function within(at, secs) {
        var age2 = now() - at;
        return age2 >= 0 && age2 < secs;
      }
      function gs() {
        try {
          if (typeof gameState === "function") return gameState();
          if (typeof gameState === "object" && gameState) return gameState;
        } catch (e) {
        }
        return null;
      }
      function year() {
        try {
          if (window.saga && typeof saga.year === "function") return saga.year();
        } catch (e) {
        }
        return 1e3;
      }
      function callName(n) {
        var p = String(n || "Someone").split(/\s+/);
        if (!p[0]) return "Someone";
        if (/^(the|a|an)$/i.test(p[0])) return p.slice(0, 2).join(" ");
        if (/^(mrs|mr|ms)\.?$/i.test(p[0])) return p.slice(0, Math.min(2, p.length)).join(" ");
        return p[0];
      }
      function hour() {
        try {
          if (typeof gameState === "object" && gameState && isFinite(gameState.t))
            return (gameState.t % 2400 + 2400) % 2400 / 100;
        } catch (e) {
        }
        return 12;
      }
      function scene() {
        try {
          if (typeof sceneNow === "function") return sceneNow() || "";
          if (typeof gameState === "object" && gameState && gameState.scene) return String(gameState.scene);
        } catch (e) {
        }
        return "";
      }
      function hook() {
        var g = gs();
        if (g && g.hook && typeof g.hook.combo === "number") {
          if (!booted) {
            booted = true;
            g.hook.combo = 0;
          }
          return g.hook;
        }
        var h = {
          combo: 0,
          best: 0,
          days: 0,
          lastOpenDay: -1,
          bags: 0,
          alive: 0
        };
        try {
          if (g) g.hook = h;
        } catch (e) {
        }
        return h;
      }
      function play(name) {
        try {
          if (window.feel && typeof feel.play === "function") feel.play(name);
          else if (window.I && typeof I[name] === "function") I[name]();
        } catch (e) {
        }
      }
      function say(msg, kind) {
        try {
          if (typeof k === "function") k(msg, kind || "gold");
          else if (typeof toast === "function") toast(msg, kind || "gold");
        } catch (e) {
        }
      }
      function markEgg(id, line) {
        try {
          if (typeof findEgg === "function") findEgg(id, line);
        } catch (e) {
        }
      }
      function think(text) {
        try {
          if (window.desk && typeof desk.think === "function") desk.think("run", text);
        } catch (e) {
        }
      }
      function fishList() {
        try {
          if (typeof allFish === "function") return allFish() || [];
        } catch (e) {
        }
        return [];
      }
      function stockDupes() {
        var list = fishList();
        var counts = /* @__PURE__ */ Object.create(null);
        var names = /* @__PURE__ */ Object.create(null);
        for (var i = 0; i < list.length; i++) {
          var f = list[i];
          if (!f) continue;
          var k2 = String(f.sp);
          counts[k2] = (counts[k2] || 0) + 1;
          try {
            var S = typeof SPECIES === "function" ? SPECIES() : typeof SPECIES !== "undefined" ? SPECIES : typeof O !== "undefined" ? O : null;
            var rec = S && (S[f.sp] || S[k2]);
            names[k2] = rec ? rec.gname || rec.name || rec.vname || rec.n || k2 : k2;
          } catch (e) {
            names[k2] = k2;
          }
        }
        return { counts, names };
      }
      function pickImminent() {
        var t = now();
        var h = hook();
        var sc = scene();
        var list = fishList();
        var crowd = window.__shopCrowd || [];
        var looking = 0, paying = 0;
        for (var i = 0; i < crowd.length; i++) {
          if (!crowd[i]) continue;
          if (crowd[i].looking) looking++;
          if (crowd[i].pay) paying++;
        }
        if (paying) return "They're waiting on a fish bag.";
        try {
          if (window.going && typeof going.whisper === "function") {
            var gw0 = going.whisper();
            if (gw0 && /letter from/i.test(gw0)) return gw0;
          }
        } catch (eLet) {
        }
        if (h.combo >= 2 && t < comboUntil) {
          if (h.combo >= 5) return h.combo + " fish bags. Don't miss.";
          return h.combo + " in a row. The till is a run.";
        }
        try {
          if (window.desk && typeof desk.coming === "function") {
            var c0 = desk.coming();
            if (c0 && c0.done && c0.arrived && within(c0.arrived, 22)) {
              if (c0.had) return callName(c0.name) + " took the " + c0.want + ".";
              return callName(c0.name) + " walked. No " + c0.want + ".";
            }
          }
        } catch (eC) {
        }
        try {
          if (window.late && typeof late.whisper === "function") {
            var lw = late.whisper();
            if (lw) return lw;
          }
        } catch (eLa) {
        }
        try {
          if (window.pane && typeof pane.whisper === "function") {
            var pw = pane.whisper();
            if (pw) return pw;
          }
        } catch (ePa) {
        }
        try {
          if (window.pinch && typeof pinch.whisper === "function") {
            var ph = pinch.whisper();
            if (ph) return ph;
          }
        } catch (ePi) {
        }
        try {
          if (window.spiral && typeof spiral.whisper === "function") {
            var spw = spiral.whisper();
            if (spw) return spw;
          }
        } catch (eSp) {
        }
        try {
          if (window.row && typeof row.whisper === "function") {
            var rw2 = row.whisper();
            if (rw2) return rw2;
          }
        } catch (eRo) {
        }
        try {
          if (window.siege && typeof siege.whisper === "function") {
            var sgw = siege.whisper();
            if (sgw) return sgw;
          }
        } catch (eSg) {
        }
        try {
          if (window.great && typeof great.whisper === "function") {
            var grw = great.whisper();
            if (grw) return grw;
          }
        } catch (eGr) {
        }
        try {
          if (window.envoy && typeof envoy.whisper === "function") {
            var evw = envoy.whisper();
            if (evw) return evw;
          }
        } catch (eEv) {
        }
        try {
          if (window.wed && typeof wed.whisper === "function") {
            var wdw2 = wed.whisper();
            if (wdw2) return wdw2;
          }
        } catch (eWd2) {
        }
        try {
          if (window.plot && typeof plot.whisper === "function") {
            var plw = plot.whisper();
            if (plw) return plw;
          }
        } catch (ePl) {
        }
        try {
          if (window.hush && typeof hush.whisper === "function") {
            var hsw = hush.whisper();
            if (hsw) return hsw;
          }
        } catch (eHs) {
        }
        try {
          if (window.claim && typeof claim.whisper === "function") {
            var clw = claim.whisper();
            if (clw) return clw;
          }
        } catch (eCl) {
        }
        try {
          if (window.feast && typeof feast.whisper === "function") {
            var few = feast.whisper();
            if (few) return few;
          }
        } catch (eFe) {
        }
        try {
          if (window.house && typeof house.whisper === "function") {
            var houw = house.whisper();
            if (houw) return houw;
          }
        } catch (eHu) {
        }
        try {
          if (window.heir && typeof heir.whisper === "function") {
            var hrw = heir.whisper();
            if (hrw) return hrw;
          }
        } catch (eHr) {
        }
        try {
          if (window.gyve && typeof gyve.whisper === "function") {
            var gyw = gyve.whisper();
            if (gyw) return gyw;
          }
        } catch (eGy) {
        }
        try {
          if (window.fray && typeof fray.whisper === "function") {
            var frw = fray.whisper();
            if (frw) return frw;
          }
        } catch (eFr) {
        }
        try {
          if (window.going && typeof going.whisper === "function") {
            var gw = going.whisper();
            if (gw) return gw;
          }
        } catch (eGo) {
        }
        try {
          if (window.desk && typeof desk.coming === "function") {
            var c = desk.coming();
            if (c && !c.done && hour() < c.hour) {
              var have = window.shopLife && shopLife.stock ? shopLife.stock() : [];
              if (have && have.indexOf(c.want) >= 0) return "We have the " + c.want + ". " + callName(c.name) + " is coming.";
              return callName(c.name) + " is coming, for " + c.want + ".";
            }
          }
        } catch (eC2) {
        }
        try {
          if (window.wane && typeof wane.whisper === "function") {
            var ww = wane.whisper();
            if (ww) return ww;
          }
        } catch (eWa) {
        }
        try {
          if (window.mask && typeof mask.whisper === "function") {
            var maw = mask.whisper();
            if (maw) return maw;
          }
        } catch (eMa) {
        }
        try {
          if (window.stray && typeof stray.whisper === "function") {
            var stw = stray.whisper();
            if (stw) return stw;
          }
        } catch (eSt) {
        }
        try {
          if (window.blood && typeof blood.whisper === "function") {
            var bw = blood.whisper();
            if (bw) return bw;
          }
        } catch (eBl) {
        }
        try {
          if (window.ill && typeof ill.whisper === "function") {
            var iw = ill.whisper();
            if (iw) return iw;
          }
        } catch (eIl) {
        }
        try {
          if (window.relic && typeof relic.whisper === "function") {
            var rw = relic.whisper();
            if (rw) return rw;
          }
        } catch (eRe) {
        }
        try {
          if (window.kin && typeof kin.whisper === "function") {
            var kw = kin.whisper();
            if (kw) return kw;
          }
        } catch (eKi) {
        }
        try {
          if (window.mark && typeof mark.whisper === "function") {
            var mw = mark.whisper();
            if (mw) return mw;
          }
        } catch (eMk) {
        }
        try {
          if (window.soul && typeof soul.whisper === "function") {
            var sws = soul.whisper();
            if (sws) return sws;
          }
        } catch (eSo) {
        }
        try {
          if (window.hand && typeof hand.whisper === "function") {
            var hw = hand.whisper();
            if (hw) return hw;
          }
        } catch (eHa) {
        }
        try {
          if (window.cut && typeof cut.whisper === "function") {
            var cw2 = cut.whisper();
            if (cw2) return cw2;
          }
        } catch (eCu) {
        }
        try {
          if (window.age && typeof age.whisper === "function") {
            var agw = age.whisper();
            if (agw) return agw;
          }
        } catch (eAg) {
        }
        try {
          if (window.hold && typeof hold.whisper === "function") {
            var how = hold.whisper();
            if (how) return how;
          }
        } catch (eHo) {
        }
        try {
          if (window.lord && typeof lord.whisper === "function") {
            var ldw = lord.whisper();
            if (ldw) return ldw;
          }
        } catch (eLd) {
        }
        try {
          if (window.wonder && typeof wonder.whisper === "function") {
            var wdw = wonder.whisper();
            if (wdw) return wdw;
          }
        } catch (eWd) {
        }
        try {
          if (window.vow && typeof vow.whisper === "function") {
            var vww = vow.whisper();
            if (vww) return vww;
          }
        } catch (eVw) {
        }
        try {
          if (window.pact && typeof pact.whisper === "function") {
            var pcw = pact.whisper();
            if (pcw) return pcw;
          }
        } catch (ePc) {
        }
        try {
          if (window.lux && typeof lux.whisper === "function") {
            var lxw = lux.whisper();
            if (lxw) return lxw;
          }
        } catch (eLx) {
        }
        try {
          if (window.spy && typeof spy.whisper === "function") {
            var spyw = spy.whisper();
            if (spyw) return spyw;
          }
        } catch (eSy) {
        }
        try {
          if (window.seat && typeof seat.whisper === "function") {
            var stw2 = seat.whisper();
            if (stw2) return stw2;
          }
        } catch (eSe) {
        }
        try {
          if (window.bent && typeof bent.whisper === "function") {
            var bnw = bent.whisper();
            if (bnw) return bnw;
          }
        } catch (eBn) {
        }
        try {
          if (window.bond && typeof bond.whisper === "function") {
            var bdw = bond.whisper();
            if (bdw) return bdw;
          }
        } catch (eBd) {
        }
        try {
          if (window.ward && typeof ward.whisper === "function") {
            var waw = ward.whisper();
            if (waw) return waw;
          }
        } catch (eWa2) {
        }
        try {
          if (window.fief && typeof fief.whisper === "function") {
            var fiw = fief.whisper();
            if (fiw) return fiw;
          }
        } catch (eFi) {
        }
        try {
          if (window.sill && typeof sill.whisper === "function") {
            var sw = sill.whisper();
            if (sw) return sw;
          }
        } catch (eSi) {
        }
        try {
          if (window.shopSite && shopSite.wet() > 0.36) return "The aisle is wet. They're turning around.";
        } catch (eW2) {
        }
        try {
          if (window.choir && typeof choir.whisper === "function") {
            var cw = choir.whisper();
            if (cw) return cw;
          }
        } catch (eCh) {
        }
        try {
          if (window.weave && typeof weave.next === "function") {
            var wn = weave.next();
            if (wn) return wn;
          }
        } catch (e0) {
        }
        var moodName = "";
        try {
          for (var m = 0; m < list.length; m++) {
            var f = list[m];
            if (f && f.mind && f.mind.mood) {
              moodName = f.nick || "A fish";
              break;
            }
          }
        } catch (e) {
        }
        if (moodName) return moodName + " is in a mood. Don't sell them yet.";
        var fry = 0;
        for (var j = 0; j < list.length; j++) {
          if (list[j] && list[j].stage != null && list[j].stage < 2) fry++;
        }
        if (fry) return fry === 1 ? "Something is still small in the water." : fry + " still small. Wait.";
        var stock2 = stockDupes();
        var almost = "";
        var kinds = 0;
        for (var k2 in stock2.counts) {
          kinds++;
          if (stock2.counts[k2] === 1 && !almost) {
            var nm = String(stock2.names[k2] || "fish").toLowerCase();
            almost = "One " + nm + ". A unique fish is a display.";
          }
        }
        if (almost && Math.random() < 0.55) return almost;
        if (looking && sc !== "shop") return "Someone is at the glass.";
        if (looking && sc === "shop") return looking === 1 ? "They're still looking." : looking + " at the glass.";
        var hr = hour();
        if (hr > 11.4 && hr < 13.8) return "Lunch. They have twelve minutes.";
        if (hr >= 18.5 && looking) return "Last hour. They're still looking.";
        if (hr >= 19 && h.combo >= 2) return "The run is still on. Sign isn't over.";
        try {
          if (window.shopSite && shopSite.wet() > 0.28) return "The aisle is wet. They're stepping around it.";
        } catch (eW) {
        }
        try {
          if (window.desk && typeof desk.neighbor === "function") {
            var nb = desk.neighbor();
            if (nb && nb.nextD === 0 && hour() < 16.9)
              return (nb.known ? nb.name : "The baker") + " is coming, for " + (nb.want || "goldfish") + ".";
          }
        } catch (eN) {
        }
        try {
          var regs = window.shopLife && shopLife.regulars ? shopLife.regulars() : [];
          if (regs && regs[0] && regs[0].name && Math.random() < 0.4)
            return regs[0].name + " usually comes after the boats.";
        } catch (e3) {
        }
        if (h.days >= 3 && hr < 9) return h.days + " mornings. Lights on.";
        if (h.best >= 5 && h.combo === 0 && Math.random() < 0.25) return "Best run was " + h.best + ". The till is quiet.";
        var named = 0;
        for (var n = 0; n < list.length; n++) if (list[n] && list[n].nick) named++;
        if (named && Math.random() < 0.35) {
          var pick = list[Math.random() * list.length | 0];
          if (pick && pick.nick) {
            try {
              if (window.mind && mind.dwell) {
                var dw = mind.dwell(pick);
                if (dw && dw.length < 72) return dw;
              }
            } catch (e4) {
            }
            return pick.nick + " is in the water.";
          }
        }
        if (kinds && kinds < 4) return kinds + " kinds in the water. The case wants more.";
        if (hr < 8) return "Morning. The street isn't in yet.";
        return "";
      }
      function ensureWhisper() {
        var el = document.getElementById("hookWhisper");
        if (el) return el;
        el = document.createElement("span");
        el.id = "hookWhisper";
        el.setAttribute("aria-live", "polite");
        var hud = document.getElementById("hudres");
        if (hud) hud.appendChild(el);
        else document.body.appendChild(el);
        return el;
      }
      function setWhisper(text, force) {
        var el = ensureWhisper();
        if (!el) return;
        if (!text) {
          if (now() - whisperAt > 8) {
            el.textContent = "";
            el.classList.remove("on");
            shown = "";
          }
          return;
        }
        if (!force && text === shown && now() - whisperAt < 4) return;
        shown = text;
        whisperAt = now();
        el.textContent = text;
        el.classList.add("on");
        el.classList.remove("pop");
        void el.offsetWidth;
        el.classList.add("pop");
      }
      function onBag() {
        var h = hook();
        h.combo += 1;
        h.bags += 1;
        try {
          window.__bagsThisOpen = (window.__bagsThisOpen || 0) + 1;
        } catch (eB) {
        }
        try {
          if (window.feel) {
            if (feel.play) feel.play("paper");
            if (feel.punch) feel.punch(0.12);
          }
        } catch (eF) {
        }
        comboUntil = now() + 90;
        if (h.combo > h.best) {
          h.best = h.combo;
          if (h.best >= 3) {
            var day = 0;
            try {
              var g = gs();
              if (g && isFinite(g.t)) day = Math.floor((g.t % 1e9 + 1e9) % 1e9 / 2400);
            } catch (eDay) {
            }
            var letterOpen = false;
            try {
              var L = window.going && going.letter && going.letter();
              letterOpen = !!(L && L.open && !L.filled);
            } catch (eL) {
            }
            if (day > 0 && !letterOpen) say(h.best + " fish bags. A run.", "gold");
            markEgg("bagrun", h.best + " fish bags without a miss.");
          }
        }
        if (h.combo === 3) {
          play("chime");
          think("Three fish bags. The till is a run.");
        } else if (h.combo === 5) {
          play("award");
          think("Five. Don't miss.");
        } else if (h.combo >= 2) play("coin");
        var extra = Math.random();
        if (extra < 0.08) {
          setWhisper("A perfect bag.", true);
          play("chime");
        } else {
          setWhisper(h.combo >= 2 ? h.combo + " in a row." : "Paper. Water. The bell.", true);
        }
      }
      function onMiss(ms) {
        var h = hook();
        if (h.combo >= 3) {
          var broke = "The run broke at " + h.combo + ".";
          think(broke);
          setWhisper(broke, true);
          play("warn");
        }
        h.combo = 0;
        if (ms && ms.why === "stock") setWhisper("They wanted a " + (ms.want || "fish") + ". We didn't have a pair.", true);
        if (ms && ms.why === "wet") setWhisper("They saw the wet boards and left.", true);
      }
      function watchTill() {
        try {
          if (!window.shopLife || !shopLife.day) return;
          var day = shopLife.day();
          if (!day) return;
          if (!tillReady) {
            if (!isFinite(day.sales)) return;
            if (!sawTill) sawTill = now();
            lastSales = day.sales;
            var miss0 = shopLife.misses ? shopLife.misses() : [];
            if (miss0 && miss0.length) {
              var m0 = miss0[miss0.length - 1];
              lastMissKey = (m0.name || "") + "|" + (m0.want || "") + "|" + (m0.y || "");
            }
            if (now() - sawTill < 1.6) return;
            tillReady = true;
            return;
          }
          if (day.sales === lastSales + 1) {
            if (now() - sawTill < 8) lastSales = day.sales;
            else onBag();
          }
          lastSales = day.sales;
          var miss = shopLife.misses ? shopLife.misses() : [];
          if (miss && miss.length) {
            var ms = miss[miss.length - 1];
            var key = (ms.name || "") + "|" + (ms.want || "") + "|" + (ms.y || "");
            if (key !== lastMissKey) {
              lastMissKey = key;
              onMiss(ms);
            }
          }
        } catch (e) {
        }
      }
      var lastDawn = -1;
      function watchDays() {
        var h = hook();
        var hr = hour();
        if (lastDawn < 0) {
          lastDawn = hr;
          if (!h.days) h.days = 1;
          return;
        }
        var dawn = lastDawn > 20 && hr < 10 || lastDawn < 8 && hr >= 8;
        lastDawn = hr;
        if (!dawn) return;
        h.days += 1;
        if (h.days === 3 || h.days === 7 || h.days === 30) {
          say(h.days + " mornings in a row.", "gold");
          markEgg("openchain", h.days + " mornings. Lights on.");
          setWhisper(h.days + " mornings. Lights on.", true);
        }
      }
      function juiceHud() {
        try {
          var coins = document.querySelector("#r-coins .v");
          if (coins && hook().combo >= 2) coins.classList.add("hook-hot");
          else if (coins) coins.classList.remove("hook-hot");
        } catch (e) {
        }
      }
      function enhanceLife() {
        try {
          var body = document.getElementById("dbody");
          if (!body) return;
          var old = body.querySelector("hook-life");
          if (old && old.parentNode) old.parentNode.removeChild(old);
        } catch (e) {
        }
      }
      function seedWiki() {
        try {
          var w = typeof WIKI === "function" ? WIKI() : WIKI;
          if (!w || !w.push) return;
          var art = {
            id: "k_run",
            sec: "The shop floor",
            t: "The run",
            tags: "combo streak bags addictive one more till mornings whisper",
            w: "<p>The line in the top bar is the unfinished thing. A customer at the glass. A pair one away. A fish in a mood. A run of fish bags. This shop calls it the till.</p><p>Miss and the run breaks. Open in the morning and the mornings count. <b>What to do about it:</b> read the gold line. Do not tab out in the last hour if someone is still looking.</p>"
          };
          for (var i = 0; i < w.length; i++) if (w[i] && w[i].id === art.id) return;
          w.push(art);
        } catch (e) {
        }
      }
      function tick() {
        try {
          ensureWhisper();
          seedWiki();
          if (now() - lastTick > 0.7) {
            lastTick = now();
            watchTill();
            watchDays();
            juiceHud();
            if (now() - whisperAt > 5.5) setWhisper(pickImminent());
          }
          if (now() - lastUi > 0.8) {
            lastUi = now();
            enhanceLife();
          }
        } catch (e) {
        }
      }
      window.hookRun = hook;
      if (window.__onBeat) window.__onBeat(tick, 280);
      else setTimeout(function loop() {
        tick();
        setTimeout(loop, 280);
      }, 280);
    })();
  } catch (e) {
    console.error("Layer hook.js failed to start:", e);
  }

  // src/layers/flow.js
  try {
    (function() {
      "use strict";
      var reduced = false;
      try {
        reduced = !!(window.matchMedia && window.matchMedia("(prefers-reduced-motion: reduce)").matches);
      } catch (e) {
      }
      var veil = null;
      var grade = null;
      var lastScene = "";
      var wired = false;
      var VEIL = {
        tank: "rgba(6, 22, 34, 0.86)",
        shop: "rgba(16, 10, 6, 0.78)",
        street: "rgba(8, 14, 20, 0.84)",
        back: "rgba(12, 8, 5, 0.82)",
        cell: "rgba(6, 8, 10, 0.9)"
      };
      var STING = {
        "shop>tank": "water",
        "tank>shop": "wood",
        "shop>street": "door",
        "shop>back": "wood",
        "back>shop": "wood",
        "street>tank": "whoosh",
        "back>tank": "water",
        "tank>street": "whoosh"
      };
      function play(name) {
        try {
          if (window.feel && typeof feel.play === "function") feel.play(name);
        } catch (e) {
        }
      }
      function sceneNow2() {
        try {
          var on = document.querySelector("#scenes .sc.on");
          if (on && on.dataset.scene) return on.dataset.scene;
        } catch (e) {
        }
        try {
          if (window.__scene) return String(window.__scene);
        } catch (e) {
        }
        return lastScene || "tank";
      }
      function ensureDom() {
        if (!veil) {
          veil = document.createElement("div");
          veil.id = "flowVeil";
          document.body.appendChild(veil);
        }
        if (!grade) {
          grade = document.createElement("div");
          grade.id = "flowGrade";
          document.body.appendChild(grade);
        }
      }
      var lastYear = 0;
      function worldYear() {
        try {
          if (window.saga && typeof saga.year === "function") return saga.year();
          if (typeof atlGen === "function") {
            var a = atlGen();
            if (a && a.now) return a.now | 0;
          }
        } catch (e) {
        }
        return 1e3;
      }
      function tickYear() {
        var y = worldYear();
        if (!y) return;
        document.body.setAttribute("data-year", String(y));
        if (!lastYear) {
          lastYear = y;
          return;
        }
        if (y === lastYear) return;
        lastYear = y;
        flashVeil(sceneNow2());
        play("chime");
      }
      function sting(to, from) {
        var key = (from || "") + ">" + to;
        var name = STING[key];
        if (!name) {
          if (to === "tank") name = "water";
          else if (to === "shop") name = "wood";
          else if (to === "street") name = "whoosh";
        }
        if (name) play(name);
      }
      function flashVeil(to) {
        ensureDom();
        if (reduced) return;
        veil.style.background = VEIL[to] || "rgba(8,10,14,.8)";
        veil.classList.add("on");
        window.setTimeout(function() {
          veil.classList.remove("on");
        }, 70);
      }
      function dayAmt() {
        try {
          if (typeof kn === "function") return kn();
          if (isFinite(window.__day)) return window.__day;
        } catch (e) {
        }
        return 0.65;
      }
      function tickGrade() {
        ensureDom();
        var scene = sceneNow2();
        var day = dayAmt();
        var wx = {};
        try {
          if (typeof bt === "function") wx = bt() || {};
          else if (window.__wx) wx = window.__wx;
        } catch (e) {
          wx = window.__wx || {};
        }
        var rain = Math.max(0, wx.rain || 0);
        var fog = Math.max(0, wx.fog || 0);
        var night = day < 0.32;
        var dusk = day > 0.68 && day < 0.88;
        var dawn = day > 0.18 && day < 0.4;
        var rgb = [18, 24, 34];
        var a = 0;
        if (night) {
          rgb = [8, 12, 26];
          a = (0.32 - day) * 0.9;
        } else if (dusk) {
          rgb = [42, 22, 14];
          a = (day - 0.68) * 0.35;
        } else if (dawn) {
          rgb = [40, 24, 16];
          a = (0.4 - day) * 0.28;
        }
        if (rain > 0.08) {
          rgb = [
            rgb[0] * 0.7 + 20 | 0,
            rgb[1] * 0.75 + 28 | 0,
            rgb[2] * 0.8 + 40 | 0
          ];
          a += rain * 0.12;
        }
        if (fog > 0.15) a += fog * 0.08;
        if (scene === "shop") a *= 0.42;
        else if (scene === "tank") a *= 0.55;
        else if (scene === "back") a *= 0.5;
        else if (scene === "street") a *= 0.85;
        else if (scene === "cell") a *= 0.7;
        a = Math.max(0, Math.min(0.34, a));
        grade.style.background = "rgba(" + rgb[0] + "," + rgb[1] + "," + rgb[2] + "," + a.toFixed(3) + ")";
        document.body.setAttribute("data-scene", scene);
        document.body.setAttribute("data-tod", night ? "night" : dusk ? "dusk" : dawn ? "dawn" : "day");
        if (rain > 0.2) document.body.setAttribute("data-wx", "rain");
        else if (fog > 0.25) document.body.setAttribute("data-wx", "fog");
        else document.body.setAttribute("data-wx", "clear");
      }
      function onScene(to, from) {
        if (!to || to === from) return;
        lastScene = to;
        document.body.setAttribute("data-scene", to);
        flashVeil(to);
        sting(to, from);
        tickGrade();
        if (to === "tank" && from === "shop" && !reduced) {
          document.body.classList.add("flow-lean");
          window.setTimeout(function() {
            document.body.classList.remove("flow-lean");
          }, 520);
        }
      }
      function hookGo() {
        if (typeof window.__onScene !== "function") window.__onScene = onScene;
      }
      function watchTitle() {
        var titling = document.body.classList.contains("titling");
        if (watchTitle._was && !titling) {
          flashVeil("shop");
          play("door");
        }
        watchTitle._was = titling;
      }
      function tick() {
        hookGo();
        watchTitle();
        tickGrade();
        tickYear();
        window.setTimeout(tick, 900);
      }
      function wrapSafe(name, ok) {
        try {
          let ng = function() {
            var fn = g.call(this);
            if (typeof fn !== "function" || fn.__safe) return fn;
            var inner = function() {
              try {
                if (ok && !ok.apply(null, arguments)) return;
                return fn.apply(this, arguments);
              } catch (e) {
                return;
              }
            };
            inner.__safe = 1;
            return inner;
          };
          var d = Object.getOwnPropertyDescriptor(globalThis, name);
          if (!d || !d.get || d.get.__safe) return;
          var g = d.get;
          ng.__safe = 1;
          Object.defineProperty(globalThis, name, { configurable: true, get: ng });
        } catch (e) {
        }
      }
      function boot() {
        wrapSafe("buyDecor", function(id) {
          try {
            return typeof decorById === "function" && !!decorById(id);
          } catch (e) {
            return false;
          }
        });
        wrapSafe("spawnCoin", function(e) {
          return !!(e && typeof e === "object" && e.sp != null);
        });
        wrapSafe("pedRename", function(e) {
          if (e && typeof e.nick === "string") e.nick = String(e.nick).replace(/[<>]/g, "");
          return true;
        });
        try {
          setTimeout(function() {
            var list = typeof allFish === "function" ? allFish() : [];
            for (var i = 0; i < (list || []).length; i++) {
              if (list[i] && typeof list[i].nick === "string") list[i].nick = list[i].nick.replace(/[<>]/g, "");
            }
          }, 400);
        } catch (eScrub) {
        }
        if (wired) return;
        wired = true;
        ensureDom();
        hookGo();
        window.__onScene = onScene;
        lastScene = sceneNow2();
        document.body.setAttribute("data-scene", lastScene);
        try {
          window.sceneNow = sceneNow2;
        } catch (e) {
        }
        tick();
      }
      if (document.readyState === "loading") document.addEventListener("DOMContentLoaded", boot);
      else boot();
    })();
  } catch (e) {
    console.error("Layer flow.js failed to start:", e);
  }

  // src/layers/site.js
  try {
    (function() {
      "use strict";
      var W = 16;
      var H = 10;
      var lastTick = 0;
      var lastNote = "";
      var log = [];
      var KIND2 = {
        wall: { ch: "#", col: "#2a3340", mat: "stone", solid: 1 },
        floor: { ch: ".", col: "#6b5340", mat: "oak", solid: 0 },
        tank: { ch: "~", col: "#1a6a88", mat: "glass", solid: 1 },
        counter: { ch: "=", col: "#8a6a44", mat: "oak", solid: 1 },
        filter: { ch: "F", col: "#4a5a4a", mat: "iron", solid: 1 },
        door: { ch: "+", col: "#c4a06a", mat: "oak", solid: 0 },
        window: { ch: "W", col: "#7ec8e8", mat: "glass", solid: 1 },
        island: { ch: "o", col: "#185a48", mat: "glass", solid: 1 }
      };
      function now() {
        return typeof performance !== "undefined" ? performance.now() / 1e3 : Date.now() / 1e3;
      }
      function gs() {
        try {
          if (typeof gameState === "function") return gameState();
          if (typeof gameState === "object" && gameState) return gameState;
        } catch (e) {
        }
        return null;
      }
      function wx() {
        try {
          if (window.wxState) return wxState;
        } catch (e) {
        }
        return { rain: 0, day: 0.6, wind: 8 };
      }
      function outsideC() {
        try {
          if (typeof wxWord === "function") {
            var s = String(wxWord() || "");
            var f = s.match(/(-?\d+)\s*°F/);
            if (f) return (parseInt(f[1], 10) - 32) * 5 / 9;
            var c = s.match(/(-?\d+)\s*°C/);
            if (c) return parseInt(c[1], 10);
          }
        } catch (e) {
        }
        var w = wx();
        if (w.seaT != null) return w.seaT;
        var day = w.day == null ? 0.6 : w.day;
        var rain = w.rain || 0;
        return 6 + day * 16 - rain * 4;
      }
      function blank() {
        var cells = [];
        for (var y = 0; y < H; y++) {
          for (var x = 0; x < W; x++) {
            var kind = "floor";
            if (y === 0 || y === H - 1 || x === 0 || x === W - 1) kind = "wall";
            if (y === 0 && x >= 2 && x <= 4) kind = "window";
            if (x === W - 1 && y >= 4 && y <= 6) kind = "door";
            if (y === 1 && x >= 5 && x <= 13) kind = "tank";
            if (y === 2 && x >= 5 && x <= 7) kind = "tank";
            if (y === 5 && x >= 6 && x <= 9) kind = "island";
            if (y === 8 && x >= 4 && x <= 11) kind = "counter";
            if (y === 8 && x === 12) kind = "filter";
            var spec = KIND2[kind];
            cells.push({
              x,
              y,
              kind,
              mat: spec.mat,
              temp: 18,
              water: kind === "tank" || kind === "island" ? 0.92 : 0,
              wet: 0,
              press: kind === "tank" || kind === "island" ? 1 : 0
            });
          }
        }
        return { w: W, h: H, cells, leak: 0, aisleC: 18, wetMax: 0 };
      }
      function site() {
        var g = gs();
        if (g && g.site && g.site.cells && g.site.cells.length === W * H) return g.site;
        var s = blank();
        try {
          if (g) g.site = s;
        } catch (e) {
        }
        return s;
      }
      function at(s, x, y) {
        if (x < 0 || y < 0 || x >= W || y >= H) return null;
        return s.cells[y * W + x];
      }
      function note(text) {
        if (!text || text === lastNote) return;
        lastNote = text;
        log.push({ s: text, at: now() });
        if (log.length > 8) log.shift();
        try {
          if (window.desk && desk.think) desk.think("site", text);
        } catch (e) {
        }
      }
      function tickSite() {
        var s = site();
        var out = outsideC();
        var clog = false;
        try {
          clog = !!window.clogged;
        } catch (e) {
        }
        var rain = wx().rain || 0;
        var wind = wx().wind || 0;
        var wetMax = 0;
        var aisle = 0;
        var aisleN = 0;
        for (var i = 0; i < s.cells.length; i++) {
          var c = s.cells[i];
          var spec = KIND2[c.kind] || KIND2.floor;
          var nbs = [at(s, c.x + 1, c.y), at(s, c.x - 1, c.y), at(s, c.x, c.y + 1), at(s, c.x, c.y - 1)];
          var tsum = c.temp;
          var tn = 1;
          for (var n = 0; n < 4; n++) {
            if (nbs[n]) {
              tsum += nbs[n].temp;
              tn++;
            }
          }
          var target = tsum / tn;
          if (c.kind === "window") target = target * 0.55 + out * 0.45;
          if (c.kind === "door") target = target * 0.7 + out * 0.3;
          c.temp += (target - c.temp) * 0.18;
          if (c.kind === "filter" && clog) {
            c.water = Math.min(1, c.water + 0.05);
            c.press = Math.min(2, (c.press || 0) + 0.08);
            var floor = at(s, c.x, c.y - 1);
            if (floor && floor.kind === "floor") floor.wet = Math.min(1, floor.wet + 0.11);
          }
          if ((c.kind === "tank" || c.kind === "island") && clog && c.press > 1.15) {
            var south = at(s, c.x, c.y + 1);
            if (south && south.kind === "floor") south.wet = Math.min(1, south.wet + 0.04);
          }
          if (c.kind === "door" && rain > 0.35) {
            var inF = at(s, c.x - 1, c.y);
            if (inF && inF.kind === "floor") inF.wet = Math.min(1, inF.wet + rain * 0.03);
          }
          if (c.kind === "window" && wind > 22 && rain > 0.2) {
            var inW = at(s, c.x, c.y + 1);
            if (inW && inW.kind === "floor") inW.wet = Math.min(1, inW.wet + 0.02);
          }
          if (c.kind === "floor" && c.wet > 0.02) {
            for (var k2 = 0; k2 < 4; k2++) {
              var nb = nbs[k2];
              if (nb && nb.kind === "floor" && nb.wet < c.wet - 0.08) {
                var give = (c.wet - nb.wet) * 0.08;
                c.wet -= give;
                nb.wet += give;
              }
            }
            c.wet *= c.temp > 20 ? 0.97 : 0.985;
          }
          if (c.kind === "floor") {
            aisle += c.temp;
            aisleN++;
            if (c.wet > wetMax) wetMax = c.wet;
          }
        }
        s.aisleC = aisleN ? aisle / aisleN : 18;
        s.wetMax = wetMax;
        s.leak = clog ? Math.min(1, (s.leak || 0) + 0.04) : Math.max(0, (s.leak || 0) - 0.06);
        if (s.leak > 0.45 && s.leak < 0.55) note("The filter is pushing water onto the boards.");
        if (wetMax > 0.55 && wetMax < 0.7) note("The aisle is wet under the tanks.");
        if (s.aisleC < 12) note("The room is taking the street's cold.");
        if (s.aisleC > 24) note("Heat is pooling by the glass.");
        var dripAge = now() - (s._dripAt || 0);
        if (wetMax > 0.22 && (dripAge > 1.7 || dripAge < 0)) {
          s._dripAt = now();
          try {
            if (window.feel && feel.play) feel.play("drip");
          } catch (eD) {
          }
        }
      }
      function dry(amt) {
        var s = site();
        amt = amt == null ? 0.4 : amt;
        for (var i = 0; i < s.cells.length; i++) {
          var c = s.cells[i];
          if (c.kind === "floor") c.wet = Math.max(0, c.wet - amt);
          if (c.kind === "filter") {
            c.water = Math.max(0, c.water - amt);
            c.press = Math.max(0, (c.press || 0) - amt);
          }
        }
        s.leak = Math.max(0, (s.leak || 0) - amt);
        s.wetMax = Math.max(0, (s.wetMax || 0) - amt * 0.8);
        note("The boards went drier.");
      }
      function line() {
        var s = site();
        var bits = [];
        bits.push(s.aisleC.toFixed(1) + "° on the boards");
        if (s.wetMax > 0.25) bits.push("the aisle is wet");
        if (s.leak > 0.3) bits.push("the filter is leaking");
        try {
          if (window.clogged) bits.push("the filter is clogged");
        } catch (e) {
        }
        return bits.join(" · ");
      }
      function paintGrid() {
        var s = site();
        var html = '<div class="site-grid" aria-label="The shop as a site">';
        for (var i = 0; i < s.cells.length; i++) {
          var c = s.cells[i];
          var spec = KIND2[c.kind] || KIND2.floor;
          var col = spec.col;
          if (c.kind === "floor" && c.wet > 0.15) col = "#3d5a6a";
          if (c.kind === "floor" && c.wet > 0.45) col = "#2a4a62";
          html += '<i style="background:' + col + '" title="' + spec.mat + " " + c.temp.toFixed(1) + '°"></i>';
        }
        html += "</div>";
        return html;
      }
      function tick() {
        if (now() - lastTick < 0.8) return;
        lastTick = now();
        tickSite();
      }
      if (window.__onBeat) window.__onBeat(tick, 400);
      else setTimeout(function loop() {
        tick();
        setTimeout(loop, 400);
      }, 400);
      window.shopSite = {
        of: site,
        line,
        log: function() {
          return log;
        },
        grid: paintGrid,
        wet: function() {
          return site().wetMax || 0;
        },
        temp: function() {
          return site().aisleC || 18;
        },
        puddle: function() {
          var s = site();
          var best = null;
          for (var i = 0; i < s.cells.length; i++) {
            var c = s.cells[i];
            if (c.kind === "floor" && c.wet > 0.1 && (!best || c.wet > best.wet)) best = c;
          }
          if (!best) return { nx: 0.48, ny: 0.74, wet: 0 };
          return {
            nx: (best.x + 0.5) / W,
            ny: 0.58 + best.y / H * 0.32,
            wet: best.wet
          };
        },
        dry
      };
    })();
  } catch (e) {
    console.error("Layer site.js failed to start:", e);
  }

  // src/layers/realm.js
  try {
    (function() {
      "use strict";
      var GW = 48;
      var GH = 32;
      var HARBOR = { x: 38, y: 13 };
      var lastTick = 0;
      var lastUi = 0;
      var cached = null;
      var selected = -1;
      var BIOME = [
        { id: "ocean", ch: "~", name: "ocean", fish: "marine" },
        { id: "coast", ch: ".", name: "coast", fish: "brackish" },
        { id: "marsh", ch: "n", name: "marsh", fish: "brackish" },
        { id: "plain", ch: ",", name: "plain", fish: "river" },
        { id: "forest", ch: "t", name: "forest", fish: "river" },
        { id: "hill", ch: "h", name: "hills", fish: "stream" },
        { id: "mount", ch: "^", name: "mountains", fish: "none" },
        { id: "desert", ch: ":", name: "desert", fish: "none" },
        { id: "tundra", ch: "_", name: "tundra", fish: "cold" },
        { id: "taiga", ch: "T", name: "taiga", fish: "cold" },
        { id: "jungle", ch: "Y", name: "jungle", fish: "warm" },
        { id: "lake", ch: "o", name: "lake", fish: "fresh" }
      ];
      var CIV_KIND = ["harbor league", "inland kingdom", "hill clan", "river republic", "desert satrapy", "forest hold", "island thanedom"];
      var SITE_KIND = ["city", "town", "hamlet", "fort", "ruin", "port", "abbey", "mine"];
      var GODS = ["the Salt Mother", "the Dry Eye", "the Wheel", "the First Net", "the Quiet Oak", "the Red Vein", "the Night Ledger"];
      var ETHIC = ["trade", "war", "craft", "faith", "law", "kin", "sea"];
      var JOBS2 = ["fisher", "smith", "scribe", "captain", "priest", "merchant", "soldier", "glassblower", "miller", "judge"];
      var ON = ["Al", "Ber", "Cal", "Dor", "El", "Fen", "Gar", "Hel", "Ith", "Jon", "Kel", "Lor", "Mar", "Ner", "Or", "Pel", "Quin", "Rav", "Sel", "Tor", "Ul", "Ver", "Wen", "Xor", "Yl", "Zed"];
      var OD = ["a", "en", "or", "um", "ith", "el", "an", "os", "ia", "ek", "ard", "ine"];
      var PLACE = ["wick", "ford", "haven", "holm", "mouth", "bridge", "fell", "dale", "port", "stead", "burg", "gate"];
      var AGE_A = ["Glass", "Salt", "Oak", "Iron", "Fog", "Net", "Ash", "Bone"];
      var AGE_B = ["Quiet Water", "the Open Road", "the First Bell", "Broken Keels", "the Long Freeze", "Green Years"];
      function mulberry(a) {
        return function() {
          var t = a += 1831565813;
          t = Math.imul(t ^ t >>> 15, t | 1);
          t ^= t + Math.imul(t ^ t >>> 7, t | 61);
          return ((t ^ t >>> 14) >>> 0) / 4294967296;
        };
      }
      function seedOf() {
        try {
          if (typeof world === "function" && world() && world().seed) return world().seed >>> 0;
        } catch (e) {
        }
        try {
          if (typeof gameState === "function") {
            var g = gameState();
            if (g && g.seed) return g.seed >>> 0;
          }
        } catch (e2) {
        }
        return 1e3;
      }
      function yearNow() {
        try {
          if (window.saga && typeof saga.year === "function") return saga.year();
        } catch (e) {
        }
        return 1e3;
      }
      function nameFrom(rng, a, b) {
        return a[rng() * a.length | 0] + b[rng() * b.length | 0];
      }
      function personName2(rng) {
        return nameFrom(rng, ON, OD) + " " + nameFrom(rng, ON, OD);
      }
      function placeName(rng) {
        return nameFrom(rng, ON, OD) + PLACE[rng() * PLACE.length | 0];
      }
      function noise2(table, x, y) {
        return table[(y & 15) * 16 + (x & 15)];
      }
      function valueNoise(table, x, y, oct) {
        var v = 0;
        var amp = 1;
        var freq = 1;
        var n = 0;
        for (var o = 0; o < oct; o++) {
          var fx = x * freq;
          var fy = y * freq;
          var x0 = Math.floor(fx);
          var y0 = Math.floor(fy);
          var tx = fx - x0;
          var ty = fy - y0;
          var a = noise2(table, x0, y0);
          var b = noise2(table, x0 + 1, y0);
          var c = noise2(table, x0, y0 + 1);
          var d = noise2(table, x0 + 1, y0 + 1);
          var u = a + (b - a) * tx;
          var w = c + (d - c) * tx;
          v += (u + (w - u) * ty) * amp;
          n += amp;
          amp *= 0.5;
          freq *= 2;
        }
        return v / n;
      }
      function biomeOf(elev, rain, temp, drain) {
        if (elev < 0.28) return "ocean";
        if (elev < 0.34) return rain > 0.55 ? "marsh" : "coast";
        if (elev > 0.78) return "mount";
        if (temp < 0.28) return rain > 0.45 ? "taiga" : "tundra";
        if (temp > 0.72 && rain < 0.35) return "desert";
        if (temp > 0.68 && rain > 0.62) return "jungle";
        if (elev > 0.58) return "hill";
        if (rain > 0.58 && drain < 0.4) return "lake";
        if (rain > 0.52) return "forest";
        return "plain";
      }
      function biomeMeta(id) {
        for (var i = 0; i < BIOME.length; i++) if (BIOME[i].id === id) return BIOME[i];
        return BIOME[3];
      }
      function idx(x, y) {
        return y * GW + x;
      }
      function emptyWorld(seed) {
        return {
          seed,
          w: GW,
          h: GH,
          name: "",
          age: "",
          harbor: { x: HARBOR.x, y: HARBOR.y, name: "the Harbor", site: 0 },
          elev: [],
          rain: [],
          temp: [],
          drain: [],
          volc: [],
          savage: [],
          biome: [],
          rivers: [],
          civs: [],
          sites: [],
          figs: [],
          rels: [],
          arts: [],
          wars: [],
          events: [],
          now: 1,
          lastSim: 0
        };
      }
      function generate(seed) {
        seed = seed >>> 0 || seedOf();
        var rng = mulberry(seed ^ 2654435769);
        var table = [];
        for (var i = 0; i < 256; i++) table[i] = rng();
        var w = emptyWorld(seed);
        w.name = "The " + AGE_A[rng() * AGE_A.length | 0] + " and " + AGE_A[rng() * AGE_A.length | 0];
        w.age = "Age of " + AGE_B[rng() * AGE_B.length | 0];
        for (var y = 0; y < GH; y++) {
          for (var x = 0; x < GW; x++) {
            var nx = x / GW;
            var ny = y / GH;
            var e = valueNoise(table, nx * 6, ny * 6, 4);
            e = e * 0.72 + (1 - nx) * 0.18 + (0.5 - Math.abs(ny - 0.42)) * 0.1;
            if (x > GW - 8) e -= (x - (GW - 8)) / 18;
            var r = valueNoise(table, nx * 5 + 20, ny * 5, 3);
            var t = 0.15 + (1 - ny) * 0.7 + (valueNoise(table, nx * 3, ny * 3 + 8, 2) - 0.5) * 0.2;
            var d = valueNoise(table, nx * 4 + 3, ny * 4 + 11, 3);
            var v = valueNoise(table, nx * 7 + 40, ny * 7, 2);
            var s = valueNoise(table, nx * 4 + 90, ny * 4, 2);
            var i2 = idx(x, y);
            w.elev[i2] = e;
            w.rain[i2] = r;
            w.temp[i2] = t;
            w.drain[i2] = d;
            w.volc[i2] = v;
            w.savage[i2] = s;
            w.biome[i2] = biomeOf(e, r, t, d);
          }
        }
        w.biome[idx(HARBOR.x, HARBOR.y)] = "coast";
        w.biome[idx(HARBOR.x + 1, HARBOR.y)] = "ocean";
        carveRivers(w, rng);
        birthCivs(w, rng);
        birthSites(w, rng);
        birthRels(w, rng);
        history(w, rng);
        w.now = yearNow();
        w.lastSim = w.now;
        return w;
      }
      function carveRivers(w, rng) {
        var n = 6 + (rng() * 4 | 0);
        for (var r = 0; r < n; r++) {
          var x = rng() * (GW - 10) | 0;
          var y = rng() * (GH - 8) + 2 | 0;
          var path = [];
          for (var step = 0; step < 40; step++) {
            path.push([x, y]);
            var i = idx(x, y);
            if (w.elev[i] < 0.3) break;
            if (w.elev[i] < 0.36) w.biome[i] = "marsh";
            else if (w.biome[i] === "desert") w.biome[i] = "plain";
            var best = [1, 0];
            var be = w.elev[i];
            var dirs = [[1, 0], [1, 1], [1, -1], [0, 1], [0, -1]];
            for (var d = 0; d < dirs.length; d++) {
              var nx = x + dirs[d][0];
              var ny = y + dirs[d][1];
              if (nx < 0 || ny < 0 || nx >= GW || ny >= GH) continue;
              var e = w.elev[idx(nx, ny)];
              if (e < be) {
                be = e;
                best = dirs[d];
              }
            }
            x += best[0];
            y += best[1];
          }
          w.rivers.push({ i: r, path, name: placeName(rng) + " Water" });
        }
      }
      function landCells(w) {
        var out = [];
        for (var y = 0; y < GH; y++) {
          for (var x = 0; x < GW; x++) {
            var b = w.biome[idx(x, y)];
            if (b !== "ocean" && b !== "mount") out.push([x, y, b]);
          }
        }
        return out;
      }
      function birthCivs(w, rng) {
        var land = landCells(w);
        var n = 7 + (rng() * 4 | 0);
        for (var i = 0; i < n && land.length; i++) {
          var pick = land[rng() * land.length | 0];
          w.civs.push({
            i,
            n: placeName(rng),
            kind: CIV_KIND[rng() * CIV_KIND.length | 0],
            x: pick[0],
            y: pick[1],
            found: 40 + (rng() * 220 | 0),
            power: 0.3 + rng() * 0.6,
            faith: GODS[rng() * GODS.length | 0],
            ethic: ETHIC[rng() * ETHIC.length | 0],
            tongue: nameFrom(rng, ON, OD),
            alive: 1
          });
        }
      }
      function nearestCiv(w, x, y) {
        var best = w.civs[0];
        var bd = 1e9;
        for (var i = 0; i < w.civs.length; i++) {
          var c = w.civs[i];
          var d = (c.x - x) * (c.x - x) + (c.y - y) * (c.y - y);
          if (d < bd) {
            bd = d;
            best = c;
          }
        }
        return best;
      }
      function birthSites(w, rng) {
        var civ0 = nearestCiv(w, HARBOR.x, HARBOR.y);
        w.sites.push({
          i: 0,
          n: "the Harbor",
          kind: "port",
          x: HARBOR.x,
          y: HARBOR.y,
          civ: civ0 ? civ0.i : 0,
          found: 1,
          pop: 1200,
          ruin: 0,
          fish: "brackish"
        });
        var land = landCells(w);
        var n = 28 + (rng() * 16 | 0);
        for (var i = 1; i <= n && land.length; i++) {
          var pick = land[rng() * land.length | 0];
          var civ = nearestCiv(w, pick[0], pick[1]);
          var kind = SITE_KIND[rng() * SITE_KIND.length | 0];
          if (w.biome[idx(pick[0], pick[1])] === "coast") kind = rng() < 0.6 ? "port" : "town";
          w.sites.push({
            i,
            n: placeName(rng),
            kind,
            x: pick[0],
            y: pick[1],
            civ: civ.i,
            found: civ.found + (rng() * 80 | 0),
            pop: kind === "city" ? 800 + (rng() * 2400 | 0) : kind === "hamlet" ? 40 + (rng() * 120 | 0) : 200 + (rng() * 600 | 0),
            ruin: kind === "ruin" ? 1 : 0,
            fish: biomeMeta(w.biome[idx(pick[0], pick[1])]).fish
          });
        }
      }
      function birthRels(w, rng) {
        for (var i = 0; i < GODS.length; i++) {
          w.rels.push({ i, n: GODS[i], seats: 1 + (rng() * 4 | 0), found: 10 + (rng() * 200 | 0) });
        }
      }
      function addFig(w, rng, year, civ, site) {
        var fig = {
          i: w.figs.length,
          n: personName2(rng),
          born: year - (16 + (rng() * 30 | 0)),
          died: 0,
          civ,
          site,
          job: JOBS2[rng() * JOBS2.length | 0],
          fame: rng() * 0.4,
          parent: -1
        };
        w.figs.push(fig);
        return fig;
      }
      function ev(w, y, kind, text) {
        w.events.push({ y, k: kind, s: text });
        if (w.events.length > 500) w.events.shift();
      }
      function history(w, rng) {
        for (var y = 1; y <= 1e3; y += 1 + (rng() * 3 | 0)) {
          if (rng() < 0.12 && w.civs.length >= 2) {
            var a = w.civs[rng() * w.civs.length | 0];
            var b = w.civs[rng() * w.civs.length | 0];
            if (a !== b && a.alive && b.alive) {
              var win = rng() < a.power / (a.power + b.power) ? a : b;
              var lose = win === a ? b : a;
              lose.power = Math.max(0.05, lose.power * 0.86);
              win.power = Math.min(1, win.power * 1.06);
              w.wars.push({ y, a: a.i, b: b.i, win: win.i });
              ev(w, y, "war", win.n + " broke " + lose.n + " in the field.");
              if (rng() < 0.2) {
                var site = w.sites[rng() * w.sites.length | 0];
                if (site && site.i !== 0 && rng() < 0.5) {
                  site.ruin = 1;
                  site.pop = Math.max(10, site.pop * 0.3 | 0);
                  ev(w, y, "sack", site.n + " was sacked. The stones remember.");
                }
              }
            }
          }
          if (rng() < 0.08) {
            var s = w.sites[rng() * w.sites.length | 0];
            if (s && !s.ruin) {
              s.pop = s.pop * (0.55 + rng() * 0.2) | 0;
              ev(w, y, "plague", "A fever walked " + s.n + ".");
            }
          }
          if (rng() < 0.1) {
            var fig = addFig(w, rng, y, rng() * w.civs.length | 0, rng() * w.sites.length | 0);
            if (rng() < 0.35 && w.figs.length > 4) fig.parent = rng() * (w.figs.length - 1) | 0;
            if (rng() < 0.15) {
              fig.fame += 0.5;
              w.arts.push({
                y,
                n: fig.n + "'s " + ["cup", "blade", "net", "tablet", "ring"][rng() * 5 | 0],
                who: fig.i,
                site: fig.site
              });
              ev(w, y, "art", fig.n + " made a thing that will outlast the hand.");
            }
          }
          if (rng() < 0.04) {
            var c = w.civs[rng() * w.civs.length | 0];
            ev(w, y, "faith", "They kept " + c.faith + " in " + c.n + ".");
          }
        }
        ev(w, 1e3, "now", "The Harbor opens a shop. The rest of the map does not pause.");
      }
      function gs() {
        try {
          if (typeof gameState === "function") return gameState();
          if (typeof gameState === "object" && gameState) return gameState;
        } catch (e) {
        }
        return null;
      }
      function worldState() {
        var g = gs();
        if (g && g.realm && g.realm.sites && g.realm.sites.length) {
          cached = g.realm;
          return cached;
        }
        var w = generate(seedOf());
        try {
          if (g) g.realm = w;
        } catch (e) {
        }
        cached = w;
        return w;
      }
      function tickYear() {
        var w = worldState();
        var y = yearNow();
        if (w.lastSim >= y) return;
        w.lastSim = y;
        w.now = y;
        var rng = mulberry((w.seed ^ y * 997) >>> 0);
        if (rng() < 0.18 && w.civs.length >= 2) {
          var a = w.civs[rng() * w.civs.length | 0];
          var b = w.civs[rng() * w.civs.length | 0];
          if (a !== b) {
            w.wars.push({ y, a: a.i, b: b.i, win: a.i });
            ev(w, y, "war", a.n + " and " + b.n + " are at war beyond the harbor.");
            try {
              if (window.weave && weave.bumpWord) weave.bumpWord(-0.1);
              if (window.weave && weave.because) weave.because("War inland. Stock will go dear.");
            } catch (e) {
            }
          }
        }
        if (rng() < 0.12) {
          var s = w.sites[rng() * w.sites.length | 0];
          ev(w, y, "trade", "A hold from " + s.n + " is on the water.");
        }
        if (w.events.length > 480) w.events = w.events.slice(-400);
      }
      function mapLines(w) {
        w = w || worldState();
        var lines = [];
        for (var y = 0; y < GH; y++) {
          var row2 = "";
          for (var x = 0; x < GW; x++) {
            if (x === HARBOR.x && y === HARBOR.y) {
              row2 += "@";
              continue;
            }
            var mark2 = "";
            for (var s = 0; s < w.sites.length; s++) {
              if (w.sites[s].x === x && w.sites[s].y === y) {
                mark2 = w.sites[s].ruin ? "x" : w.sites[s].kind === "city" ? "#" : w.sites[s].kind === "port" ? "P" : "*";
                break;
              }
            }
            row2 += mark2 || biomeMeta(w.biome[idx(x, y)]).ch;
          }
          lines.push(row2);
        }
        return lines;
      }
      function embargo() {
        var w = worldState();
        var y = yearNow();
        for (var i = w.wars.length - 1; i >= 0 && i >= w.wars.length - 6; i--) {
          if (y - w.wars[i].y < 8) return w.wars[i];
        }
        return null;
      }
      function traveler() {
        var w = worldState();
        var rng = mulberry((w.seed ^ yearNow() * 131) >>> 0);
        var s = w.sites[1 + (rng() * Math.max(1, w.sites.length - 1) | 0)];
        var fig = w.figs.length ? w.figs[rng() * w.figs.length | 0] : null;
        return {
          site: s,
          fig,
          line: (fig ? fig.n : "Someone") + " of " + s.n + " came in off a longer road."
        };
      }
      function pressure() {
        return embargo() ? 0.22 : 0;
      }
      function harborCell() {
        var w = worldState();
        var i = idx(HARBOR.x, HARBOR.y);
        return { elev: w.elev[i], rain: w.rain[i], temp: w.temp[i], volc: w.volc[i], savage: w.savage[i], biome: w.biome[i] };
      }
      function esc(s) {
        var map = { "&": "&amp;", "<": "&lt;", ">": "&gt;", '"': "&quot;" };
        return String(s || "").replace(/[&<>"]/g, function(c) {
          return map[c];
        });
      }
      var BIOME_RGB = {
        ocean: [156, 186, 204],
        coast: [214, 196, 148],
        marsh: [132, 148, 108],
        plain: [176, 180, 112],
        forest: [92, 124, 82],
        hill: [186, 152, 96],
        mount: [198, 186, 164],
        desert: [204, 168, 104],
        tundra: [214, 210, 196],
        taiga: [110, 132, 104],
        jungle: [74, 112, 72],
        lake: [148, 176, 186]
      };
      var CIV_WASH = [
        [176, 132, 74],
        [124, 148, 92],
        [186, 166, 96],
        [148, 112, 74],
        [108, 132, 96],
        [196, 156, 88],
        [132, 120, 78],
        [164, 140, 92]
      ];
      var PAPER = [243, 234, 212];
      var WATER_SHAL = [176, 202, 214];
      var WATER_DEEP = [128, 166, 188];
      var INK = [72, 58, 42];
      function clamp01(v) {
        return v < 0 ? 0 : v > 1 ? 1 : v;
      }
      function smooth(t) {
        t = clamp01(t);
        return t * t * (3 - 2 * t);
      }
      function lerp(a, b, t) {
        return a + (b - a) * t;
      }
      function hash2(x, y, s) {
        var n = Math.sin(x * 127.1 + y * 311.7 + (s || 0) * 0.013) * 43758.5453;
        return n - Math.floor(n);
      }
      function fbm(x, y, s) {
        return hash2(x, y, s) * 0.5 + hash2(x * 2.13, y * 2.13, s + 1) * 0.27 + hash2(x * 4.27, y * 4.27, s + 2) * 0.14 + hash2(x * 8.1, y * 8.1, s + 3) * 0.07;
      }
      function sampleGrid(arr, x, y) {
        if (!arr || !arr.length) return 0.4;
        x = x < 0 ? 0 : x > GW - 1.001 ? GW - 1.001 : x;
        y = y < 0 ? 0 : y > GH - 1.001 ? GH - 1.001 : y;
        var x0 = x | 0;
        var y0 = y | 0;
        var x1 = x0 + 1 < GW ? x0 + 1 : GW - 1;
        var y1 = y0 + 1 < GH ? y0 + 1 : GH - 1;
        var tx = smooth(x - x0);
        var ty = smooth(y - y0);
        var a = arr[idx(x0, y0)] || 0;
        var b = arr[idx(x1, y0)] || 0;
        var c = arr[idx(x0, y1)] || 0;
        var d = arr[idx(x1, y1)] || 0;
        return a + (b - a) * tx + (c - a) * ty + (a - b - c + d) * tx * ty;
      }
      function harborBias(x, y) {
        var dx = x - HARBOR.x;
        var dy = y - HARBOR.y;
        var wx = dx + (fbm(x * 1.15, y * 1.15, 7) - 0.5) * 1.55;
        var wy = dy + (fbm(x * 1.15 + 21, y * 1.15, 9) - 0.5) * 1.35;
        var shelf = wx * 0.055;
        var k1 = (wx - 0.95) * (wx - 0.95) / 10.5 + (wy + 0.12) * (wy + 0.12) / 3.6;
        var k2 = (wx - 0.35) * (wx - 0.35) / 5.8 + (wy - 0.62) * (wy - 0.62) / 2.7;
        var kidney = k1 < k2 ? k1 : k2;
        var basin = smooth(1.08 - kidney) * 0.27;
        if (wx < -1.05) basin *= 0.06;
        if (wx > 1.15 && wy * wy < 3.4) basin = basin > 0.16 ? basin : 0.16;
        var hook = Math.exp(-((wx + 0.4) * (wx + 0.4)) / 0.78) * Math.exp(-(wy * wy) / 6.2) * 0.175;
        if (wx > 0.12) hook *= 0.42;
        var meander = (fbm(x * 0.65, y * 2.35, 3) - 0.5) * 0.85;
        var charles = 0;
        if (wx < 1.25) {
          var ch = wy + 0.18 + meander;
          charles = Math.exp(-ch * ch * 4.1) * 0.095;
        }
        return hook - shelf - basin - charles;
      }
      function sampleElev(w, x, y) {
        var ux = x + (fbm(x * 0.72, y * 0.72, 15) - 0.5) * 1.15;
        var uy = y + (fbm(x * 0.72 + 9, y * 0.72, 16) - 0.5) * 1.15;
        var e = sampleGrid(w.elev, ux, uy);
        e += harborBias(x, y);
        e += (fbm(x * 0.42, y * 0.42, w.seed) - 0.5) * 0.05;
        e += (fbm(x * 3.6, y * 3.6, (w.seed || 0) + 4) - 0.5) * 0.1;
        e += (fbm(x * 7.4, y * 7.4, (w.seed || 0) + 8) - 0.5) * 0.035;
        return e;
      }
      function biomeAt(w, x, y) {
        var x0 = x | 0;
        var y0 = y | 0;
        if (x0 < 0 || y0 < 0 || x0 >= GW || y0 >= GH) return "ocean";
        return w.biome[idx(x0, y0)] || "ocean";
      }
      function rgbOf(id) {
        return BIOME_RGB[id] || BIOME_RGB.plain;
      }
      function mixRgb(a, b, t) {
        t = clamp01(t);
        return [a[0] + (b[0] - a[0]) * t, a[1] + (b[1] - a[1]) * t, a[2] + (b[2] - a[2]) * t];
      }
      function landColor(w, x, y) {
        var ux = x + (fbm(x * 0.85, y * 0.85, 5) - 0.5) * 1.7;
        var uy = y + (fbm(x * 0.85 + 11, y * 0.85, 6) - 0.5) * 1.7;
        var col = rgbOf(biomeAt(w, Math.round(ux), Math.round(uy)));
        var speckle = hash2(x * 18.3, y * 18.3, 9);
        if (speckle > 0.74) col = mixRgb(col, INK, 0.08);
        else if (speckle < 0.08) col = mixRgb(col, PAPER, 0.14);
        return mixRgb(col, PAPER, 0.1);
      }
      function civWash(w, x, y) {
        var civs = w.civs;
        if (!civs || !civs.length) return null;
        var ux = x + (fbm(x * 0.55, y * 0.55, 22) - 0.5) * 3.4;
        var uy = y + (fbm(x * 0.55 + 6, y * 0.55, 23) - 0.5) * 3.4;
        var best = 0;
        var bd = 1e9;
        var second = 1e9;
        for (var i = 0; i < civs.length; i++) {
          var c = civs[i];
          var d = (c.x - ux) * (c.x - ux) + (c.y - uy) * (c.y - uy);
          if (d < bd) {
            second = bd;
            bd = d;
            best = i;
          } else if (d < second) second = d;
        }
        var edge = second > 0 ? bd / second : 1;
        var alpha = 0.34 * smooth(1.15 - edge * 1.05);
        return { rgb: CIV_WASH[best % CIV_WASH.length], a: alpha };
      }
      function chaikin(pts, rounds) {
        var p = pts;
        for (var r = 0; r < rounds; r++) {
          if (p.length < 2) break;
          var n = [p[0]];
          for (var i = 0; i < p.length - 1; i++) {
            var a = p[i];
            var b = p[i + 1];
            n.push([a[0] * 0.75 + b[0] * 0.25, a[1] * 0.75 + b[1] * 0.25]);
            n.push([a[0] * 0.25 + b[0] * 0.75, a[1] * 0.25 + b[1] * 0.75]);
          }
          n.push(p[p.length - 1]);
          p = n;
        }
        return p;
      }
      function riverPts(path, seed) {
        if (!path || path.length < 2) return [];
        var pts = [];
        var last = null;
        for (var i = 0; i < path.length; i++) {
          var x = path[i][0] + 0.5;
          var y = path[i][1] + 0.5;
          if (last && last[0] === x && last[1] === y) continue;
          pts.push([x, y]);
          last = pts[pts.length - 1];
        }
        if (pts.length < 2) return pts;
        var sm = chaikin(pts, 4);
        for (var k2 = 1; k2 < sm.length - 1; k2++) {
          var dx = sm[k2 + 1][0] - sm[k2 - 1][0];
          var dy = sm[k2 + 1][1] - sm[k2 - 1][1];
          var len = Math.sqrt(dx * dx + dy * dy) || 1;
          var mag = (hash2(sm[k2][0], sm[k2][1], seed) - 0.5) * 0.7;
          mag += (hash2(sm[k2][0] * 2.2, sm[k2][1] * 2.2, seed + 3) - 0.5) * 0.35;
          sm[k2][0] += -dy / len * mag;
          sm[k2][1] += dx / len * mag;
        }
        return chaikin(sm, 3);
      }
      function drawKeep(ctx, x, y, scale, here) {
        ctx.save();
        ctx.translate(x, y);
        ctx.scale(scale, scale);
        ctx.fillStyle = "rgba(70,50,30,.22)";
        ctx.beginPath();
        ctx.ellipse(0, 5.5, 7, 2.4, 0, 0, 7);
        ctx.fill();
        ctx.fillStyle = here ? "#e8d9a4" : "#d2c4a4";
        ctx.strokeStyle = here ? "#8a6a28" : "#5a4a34";
        ctx.lineWidth = 0.7;
        ctx.beginPath();
        ctx.moveTo(-6, 4);
        ctx.lineTo(-6, -3);
        ctx.lineTo(-2.2, -3);
        ctx.lineTo(-2.2, -11);
        ctx.lineTo(3.4, -11);
        ctx.lineTo(3.4, -3);
        ctx.lineTo(6, -3);
        ctx.lineTo(6, 4);
        ctx.closePath();
        ctx.fill();
        ctx.stroke();
        ctx.fillRect(-2.2, -13.2, 1.4, 2.2);
        ctx.fillRect(0, -13.2, 1.4, 2.2);
        ctx.fillRect(2, -13.2, 1.4, 2.2);
        ctx.fillStyle = here ? "#c45a3a" : "#6a4a38";
        ctx.fillRect(-1.4, -16, 1.2, 3);
        ctx.beginPath();
        ctx.moveTo(-1.4, -16);
        ctx.lineTo(3.6, -15);
        ctx.lineTo(-1.4, -14);
        ctx.fill();
        if (here) {
          ctx.beginPath();
          ctx.arc(0, -1, 11, 0, 7);
          ctx.strokeStyle = "rgba(212, 160, 48, 0.95)";
          ctx.lineWidth = 1.6;
          ctx.stroke();
          ctx.beginPath();
          ctx.arc(0, -1, 13.2, 0, 7);
          ctx.strokeStyle = "rgba(244, 196, 83, 0.55)";
          ctx.lineWidth = 0.8;
          ctx.stroke();
        }
        ctx.restore();
      }
      function paintTerrain(cv, w) {
        var W = cv.width;
        var H = cv.height;
        var ctx = cv.getContext("2d");
        var img = ctx.createImageData(W, H);
        var data = img.data;
        var sx = W / GW;
        var sy = H / GH;
        var i = 0;
        for (var py = 0; py < H; py++) {
          var gy = py / sy;
          for (var px = 0; px < W; px++) {
            var gx = px / sx;
            var e = sampleElev(w, gx, gy);
            var grain = 0.94 + hash2(px, py, 1) * 0.1;
            var col;
            if (e < 0.278) {
              var depth = smooth((0.278 - e) / 0.12);
              col = mixRgb(WATER_SHAL, WATER_DEEP, depth);
              col = mixRgb(col, PAPER, 0.16);
            } else if (e < 0.302) {
              var t = smooth((e - 0.278) / 0.024);
              var land = landColor(w, gx, gy);
              var wet = mixRgb(WATER_SHAL, [214, 202, 168], 0.4);
              col = mixRgb(wet, land, t);
              col = mixRgb(col, INK, (1 - Math.abs(t - 0.5) * 2) * 0.1);
            } else {
              col = landColor(w, gx, gy);
              var wash = civWash(w, gx, gy);
              if (wash) col = mixRgb(col, wash.rgb, wash.a);
              if (e > 0.7) col = mixRgb(col, rgbOf("mount"), smooth((e - 0.7) / 0.18) * 0.45);
            }
            data[i++] = Math.min(255, col[0] * grain);
            data[i++] = Math.min(255, col[1] * grain);
            data[i++] = Math.min(255, col[2] * grain);
            data[i++] = 255;
          }
        }
        ctx.putImageData(img, 0, 0);
        ctx.save();
        ctx.lineCap = "round";
        ctx.lineJoin = "round";
        var rivers = w.rivers || [];
        for (var r = 0; r < rivers.length; r++) {
          var rp = riverPts(rivers[r].path, (w.seed || 0) + r * 17);
          if (rp.length < 2) continue;
          ctx.beginPath();
          ctx.moveTo(rp[0][0] * sx, rp[0][1] * sy);
          for (var p = 1; p < rp.length; p++) ctx.lineTo(rp[p][0] * sx, rp[p][1] * sy);
          ctx.strokeStyle = "rgba(120, 148, 156, 0.35)";
          ctx.lineWidth = 3.4;
          ctx.stroke();
          ctx.beginPath();
          ctx.moveTo(rp[0][0] * sx, rp[0][1] * sy);
          for (var p2 = 1; p2 < rp.length; p2++) ctx.lineTo(rp[p2][0] * sx, rp[p2][1] * sy);
          ctx.strokeStyle = "rgba(92, 128, 148, 0.72)";
          ctx.lineWidth = 1.45;
          ctx.stroke();
        }
        var routes = [];
        try {
          if (window.road && road.of) routes = road.of().routes || [];
        } catch (eR) {
        }
        if (!routes.length && w.sites) {
          for (var si = 0; si < w.sites.length; si++) {
            var a = w.sites[si];
            if (!a || a.ruin) continue;
            var best = -1;
            var bd = 1e9;
            for (var sj = 0; sj < w.sites.length; sj++) {
              if (si === sj) continue;
              var b = w.sites[sj];
              if (!b || b.ruin) continue;
              var d = (a.x - b.x) * (a.x - b.x) + (a.y - b.y) * (a.y - b.y);
              if (d < bd) {
                bd = d;
                best = sj;
              }
            }
            if (best >= 0 && si < best) routes.push({ a: si, b: best, cut: 0 });
          }
        }
        ctx.strokeStyle = "rgba(46, 86, 128, 0.55)";
        ctx.lineWidth = 1.55;
        for (var rt = 0; rt < routes.length; rt++) {
          var ra = w.sites[routes[rt].a];
          var rb = w.sites[routes[rt].b];
          if (!ra || !rb) continue;
          var ax = (ra.x + 0.5) * sx;
          var ay = (ra.y + 0.5) * sy;
          var bx = (rb.x + 0.5) * sx;
          var by = (rb.y + 0.5) * sy;
          var mx = (ax + bx) / 2;
          var my = (ay + by) / 2;
          var rdx = bx - ax;
          var rdy = by - ay;
          var rlen = Math.sqrt(rdx * rdx + rdy * rdy) || 1;
          var bend = (hash2(ra.x, ra.y, 40 + rt) - 0.5) * Math.min(70, rlen * 0.28);
          mx += -rdy / rlen * bend;
          my += rdx / rlen * bend;
          ctx.beginPath();
          ctx.moveTo(ax, ay);
          ctx.quadraticCurveTo(mx, my, bx, by);
          if (routes[rt].cut) ctx.setLineDash([5, 6]);
          else ctx.setLineDash([]);
          ctx.stroke();
        }
        ctx.setLineDash([]);
        ctx.restore();
        var sites = w.sites || [];
        for (var s = 0; s < sites.length; s++) {
          var site = sites[s];
          if (!site) continue;
          var px2 = (site.x + 0.5) * sx;
          var py2 = (site.y + 0.5) * sy;
          var here = site.x === HARBOR.x && site.y === HARBOR.y;
          var sc = here ? 1.15 : site.kind === "city" || site.kind === "port" ? 0.92 : 0.72;
          drawKeep(ctx, px2, py2, sc, here);
          if (here) {
            ctx.font = "700 13px Nunito, sans-serif";
            ctx.fillStyle = "rgba(90, 64, 28, 0.9)";
            ctx.fillText("Boston", px2 + 16, py2 - 10);
          }
        }
      }
      var mapCv = null;
      var mapKey = "";
      var mapFor = null;
      function mapCanvas(w) {
        if (w._mapCv || w._mapKey) {
          delete w._mapCv;
          delete w._mapKey;
        }
        var key = "v5:" + String(w.seed) + ":" + (w.rivers && w.rivers.length) + ":" + (w.sites && w.sites.length);
        if (mapCv && mapFor === w && mapKey === key) return mapCv;
        var cv = document.createElement("canvas");
        cv.width = 960;
        cv.height = 640;
        paintTerrain(cv, w);
        mapCv = cv;
        mapKey = key;
        mapFor = w;
        return cv;
      }
      function paintRealmMap(el, w) {
        if (!el || !w) return;
        var src = mapCanvas(w);
        var ctx = el.getContext("2d");
        if (!ctx) return;
        if (el.width !== src.width) el.width = src.width;
        if (el.height !== src.height) el.height = src.height;
        ctx.drawImage(src, 0, 0);
      }
      function dossier(w, site) {
        if (!w || !site) return "";
        var html = '<div class="sec">' + esc(site.n) + (site.i === 0 ? ' <span class="pill">here</span>' : "") + "</div>";
        html += '<div class="note">' + esc(site.kind) + (site.ruin ? " · ruin" : "") + " · pop " + site.pop + " · " + esc(site.fish) + " water. Click another keep on the plate, or a name below.</div>";
        var civ = null;
        if (w.civs) {
          for (var i = 0; i < w.civs.length; i++) {
            if (w.civs[i] && (w.civs[i].seat === site.i || w.civs[i].x === site.x)) civ = w.civs[i];
          }
          if (!civ) {
            var bd = 1e9;
            for (var c = 0; c < w.civs.length; c++) {
              var d = (w.civs[c].x - site.x) * (w.civs[c].x - site.x) + (w.civs[c].y - site.y) * (w.civs[c].y - site.y);
              if (d < bd) {
                bd = d;
                civ = w.civs[c];
              }
            }
          }
        }
        if (civ) {
          html += '<div class="row"><div></div><div><div class="n">' + esc(civ.n) + '</div><div class="d">' + esc(civ.kind) + " · " + esc(civ.ethic) + " · " + esc(civ.faith) + " · " + esc(civ.tongue) + " · founded " + civ.found + "</div></div><div></div></div>";
        }
        var evs = [];
        if (w.events) {
          for (var e = w.events.length - 1; e >= 0 && evs.length < 6; e--) {
            var ev2 = w.events[e];
            if (!ev2 || !ev2.s) continue;
            if (ev2.s.indexOf(site.n) >= 0 || civ && ev2.s.indexOf(civ.n) >= 0) evs.push(ev2);
          }
        }
        if (evs.length) {
          html += '<div class="sec">What happened here</div>';
          for (var k2 = 0; k2 < evs.length; k2++) {
            html += '<div class="row"><div></div><div><div class="d">' + evs[k2].y + " · " + esc(evs[k2].s) + "</div></div><div></div></div>";
          }
        }
        try {
          var L = window.going && going.letter && going.letter();
          if (L && L.open && L.from === site.n) {
            html += '<div class="note">The letter on the counter is from here. They asked for a pair of ' + esc(L.want) + ". Fill it and a road can reopen.</div>";
          }
        } catch (eL) {
        }
        var figs = [];
        if (w.figs) {
          for (var f = 0; f < w.figs.length && figs.length < 4; f++) {
            if (w.figs[f] && w.figs[f].site === site.i) figs.push(w.figs[f]);
          }
        }
        if (figs.length) {
          html += '<div class="sec">People of ' + esc(site.n) + "</div>";
          for (var p = 0; p < figs.length; p++) {
            html += '<div class="row"><div></div><div><div class="d">' + esc(figs[p].n) + (figs[p].fame > 0.4 ? " · known inland" : "") + "</div></div><div></div></div>";
          }
        }
        return html;
      }
      function nearestSite(w, gx, gy) {
        if (!w || !w.sites) return -1;
        var best = -1;
        var bd = 2.8;
        for (var i = 0; i < w.sites.length; i++) {
          var s = w.sites[i];
          if (!s) continue;
          var d = (s.x + 0.5 - gx) * (s.x + 0.5 - gx) + (s.y + 0.5 - gy) * (s.y + 0.5 - gy);
          if (d < bd) {
            bd = d;
            best = i;
          }
        }
        return best;
      }
      function panelHtml() {
        var w = worldState();
        tickYear();
        var html = '<div class="sec">' + esc(w.name) + " <span>" + esc(w.age) + " · seed " + w.seed + "</span></div>";
        html += '<div class="note">Boston is one harbor. Click a keep. The plate is the same seed as the shop: elevation, rain, heat, drainage, a tongue, a war, a fish. Gold is here.</div>';
        html += '<div class="realm-map-wrap"><canvas class="realm-map" width="960" height="640" aria-label="The continent. Click a keep. The gold mark is Boston."></canvas></div>';
        var site = selected >= 0 && w.sites[selected] ? w.sites[selected] : w.sites[0];
        if (site) html += '<div class="realm-dossier">' + dossier(w, site) + "</div>";
        html += '<div class="sec">Civilizations <span>' + w.civs.length + "</span></div>";
        for (var i = 0; i < w.civs.length && i < 12; i++) {
          var c = w.civs[i];
          html += '<div class="row"><div></div><div><div class="n">' + esc(c.n) + '</div><div class="d">' + esc(c.kind) + " · " + esc(c.ethic) + " · " + esc(c.faith) + " · " + esc(c.tongue) + " · founded " + c.found + "</div></div><div></div></div>";
        }
        html += '<div class="sec">Sites <span>' + w.sites.length + "</span></div>";
        for (var s = 0; s < w.sites.length && s < 16; s++) {
          var st = w.sites[s];
          html += '<div class="row" data-realm-site="' + s + '"><div></div><div><div class="n">' + esc(st.n) + (st.i === 0 ? ' <span class="pill">here</span>' : "") + '</div><div class="d">' + esc(st.kind) + (st.ruin ? " · ruin" : "") + " · pop " + st.pop + " · " + esc(st.fish) + " water</div></div><div></div></div>";
        }
        html += '<div class="sec">The years inland</div>';
        for (var e = w.events.length - 1, n = 0; e >= 0 && n < 10; e--, n++) {
          html += '<div class="row"><div></div><div><div class="d">' + w.events[e].y + " · " + esc(w.events[e].s) + "</div></div><div></div></div>";
        }
        if (embargo()) html += '<div class="note">A war is still warm. Holds come in late. The till will feel it.</div>';
        return html;
      }
      function bindMap(wrap) {
        if (!wrap) return;
        var cv = wrap.querySelector("canvas.realm-map");
        if (cv && !cv.__realmClick) {
          cv.__realmClick = 1;
          cv.style.cursor = "pointer";
          cv.addEventListener("click", function(e) {
            var r = cv.getBoundingClientRect();
            if (!r.width || !r.height) return;
            var gx = (e.clientX - r.left) / r.width * GW;
            var gy = (e.clientY - r.top) / r.height * GH;
            var hit = nearestSite(worldState(), gx, gy);
            if (hit < 0) return;
            selected = hit;
            wrap.removeAttribute("data-h");
            enhance();
          });
        }
        if (!wrap.__realmRows) {
          wrap.__realmRows = 1;
          wrap.addEventListener("click", function(e) {
            var row2 = e.target.closest("[data-realm-site]");
            if (!row2) return;
            var i = +row2.getAttribute("data-realm-site");
            if (!isFinite(i)) return;
            selected = i;
            wrap.removeAttribute("data-h");
            enhance();
          });
        }
      }
      function enhance() {
        var body = document.getElementById("dbody");
        var title = document.getElementById("dtitle");
        if (!body || !title) return;
        if (!/atlas/i.test(title.textContent || "")) return;
        var wrap = body.querySelector(".realm-atlas");
        var html = panelHtml();
        var key = String(html.length) + ":" + selected;
        if (!wrap) {
          wrap = document.createElement("div");
          wrap.className = "realm-atlas";
          var live = body.querySelector(".live-atlas");
          if (live && live.nextSibling) body.insertBefore(wrap, live.nextSibling);
          else body.appendChild(wrap);
        }
        if (wrap.getAttribute("data-h") !== key) {
          wrap.innerHTML = html;
          wrap.setAttribute("data-h", key);
        }
        bindMap(wrap);
        try {
          paintRealmMap(wrap.querySelector("canvas.realm-map"), worldState());
        } catch (eP) {
        }
      }
      function seedWiki() {
        try {
          var w = typeof WIKI === "function" ? WIKI() : window.WIKI;
          if (!w || !w.push) return;
          var extra = {
            id: "k_realm",
            sec: "The chronicle",
            t: "The world beyond the harbor",
            tags: "world realm continent biome war atlas beyond boston",
            w: "<p>The shop is one harbor on a generated continent. Elevation, rainfall, heat, drainage, volcanism, and savagery decide the biome. Rivers run downhill. Civilizations found sites and keep a tongue and an ethic. Gods take seats. Wars sack towns. Artifacts outlast hands. A thousand years of that, and then you hang a sign. The gold mark is Boston.</p><p>A war inland is not flavor. Towns come late. Word on the street drops. Someone will walk in off a longer road and name a town you have never stood in.</p><p><b>What to do about it:</b> Atlas. The painted map under The living year. Same seed as the shop.</p>"
          };
          for (var i = 0; i < w.length; i++) if (w[i] && w[i].id === extra.id) return;
          w.push(extra);
        } catch (e) {
        }
      }
      function now() {
        return typeof performance !== "undefined" ? performance.now() / 1e3 : Date.now() / 1e3;
      }
      function tick() {
        try {
          seedWiki();
          if (now() - lastTick > 1.4) {
            lastTick = now();
            tickYear();
          }
          if (now() - lastUi > 1.1) {
            lastUi = now();
            enhance();
          }
        } catch (e) {
        }
      }
      window.realm = {
        generate,
        world: worldState,
        tick: tickYear,
        map: mapLines,
        embargo,
        traveler,
        pressure,
        harbor: harborCell,
        panel: panelHtml,
        paint: paintRealmMap
      };
      if (window.__onBeat) window.__onBeat(tick, 400);
      else setTimeout(function loop() {
        tick();
        setTimeout(loop, 400);
      }, 400);
    })();
  } catch (e) {
    console.error("Layer realm.js failed to start:", e);
  }

  // src/layers/geo.js
  try {
    (function() {
      "use strict";
      var lastTick = 0;
      var lastUi = 0;
      var ORE = ["iron", "copper", "salt", "mica", "bogwood", "gold"];
      var LAYER = ["soil", "stone", "cavern", "deep", "magma"];
      function gs() {
        try {
          if (typeof gameState === "function") return gameState();
          if (typeof gameState === "object" && gameState) return gameState;
        } catch (e) {
        }
        return null;
      }
      function mulberry(a) {
        return function() {
          var t = a += 1831565813;
          t = Math.imul(t ^ t >>> 15, t | 1);
          t ^= t + Math.imul(t ^ t >>> 7, t | 61);
          return ((t ^ t >>> 14) >>> 0) / 4294967296;
        };
      }
      function realmW() {
        try {
          if (window.realm && typeof realm.world === "function") return realm.world();
        } catch (e) {
        }
        return null;
      }
      function state() {
        var g = gs();
        if (g && g.geo && g.geo.veins) return g.geo;
        var rw = realmW();
        var seed = rw && rw.seed || 1e3;
        var rng = mulberry(seed ^ 20973);
        var veins = [];
        var mines = 4 + (rng() * 5 | 0);
        for (var i = 0; i < mines; i++) {
          veins.push({
            i,
            ore: ORE[rng() * ORE.length | 0],
            layer: LAYER[1 + (rng() * 3 | 0)],
            x: rng() * 48 | 0,
            y: rng() * 32 | 0,
            yield: 0.3 + rng() * 0.7,
            dry: 0
          });
        }
        var caverns = [];
        for (var c = 0; c < 3; c++) {
          caverns.push({
            i: c,
            n: ["the Upper Dark", "the Still Water", "the Magma Shore"][c],
            fish: ["cave tetra", "blind cat", "ash goby"][c],
            heat: c === 2 ? 0.9 : 0.2 + c * 0.2,
            wet: c === 1 ? 0.8 : 0.3
          });
        }
        var geo = { veins, caverns, heat: 0, note: "", lastY: 0 };
        try {
          if (g) g.geo = geo;
        } catch (e) {
        }
        return geo;
      }
      function harborVolc() {
        try {
          if (window.realm && typeof realm.harbor === "function") return realm.harbor().volc || 0;
        } catch (e) {
        }
        return 0.3;
      }
      function heat() {
        var g = state();
        var v = harborVolc();
        g.heat = v > 0.72 ? 0.18 : v > 0.55 ? 0.08 : 0;
        return g.heat;
      }
      function dryMines() {
        var rw = realmW();
        if (!rw) return 0;
        var n = 0;
        for (var i = 0; i < rw.sites.length; i++) {
          if (rw.sites[i].kind === "mine" && rw.sites[i].ruin) n++;
        }
        return n;
      }
      function scarce(ore) {
        var g = state();
        var hit = 0;
        var live = 0;
        for (var i = 0; i < g.veins.length; i++) {
          if (g.veins[i].ore !== ore) continue;
          live++;
          if (g.veins[i].dry) hit++;
        }
        if (!live) return false;
        return hit / live > 0.5 || dryMines() > 2;
      }
      function lotOf(ore) {
        var rw = realmW();
        var site = "inland";
        if (rw && rw.sites) {
          for (var i = 0; i < rw.sites.length; i++) {
            if (rw.sites[i].kind === "mine" && !rw.sites[i].ruin) {
              site = rw.sites[i].n;
              break;
            }
          }
        }
        return ore + " from " + site;
      }
      function tickYear() {
        var g = state();
        var y = 1e3;
        try {
          if (window.saga && typeof saga.year === "function") y = saga.year();
        } catch (e) {
        }
        if (g.lastY >= y) return;
        g.lastY = y;
        var rng = mulberry((g.veins.length * 17 ^ y) >>> 0);
        if (rng() < 0.12 && g.veins.length) {
          var v = g.veins[rng() * g.veins.length | 0];
          v.dry = 1;
          g.note = "The " + v.ore + " vein in the " + v.layer + " packed.";
          try {
            if (window.weave && weave.because) weave.because("A mine inland went quiet. " + v.ore + " will cost.");
          } catch (e2) {
          }
        }
        if (heat() > 0.1) {
          g.note = "The boards run warm. Something under the street is not weather.";
          try {
            if (window.shopSite && shopSite.grid) {
            }
          } catch (e3) {
          }
        }
      }
      function coupleSite() {
        try {
          if (!window.shopSite || typeof shopSite.tick !== "function") return;
          var h = heat();
          if (h > 0.1 && shopSite.state) {
            var st = shopSite.state();
            if (st && typeof st.temp === "number") st.temp = Math.min(1, (st.temp || 0.4) + h * 0.02);
          }
        } catch (e) {
        }
      }
      function enhance() {
        var body = document.getElementById("dbody");
        var title = document.getElementById("dtitle");
        if (!body || !title) return;
        if (!/atlas/i.test(title.textContent || "")) return;
        var g = state();
        var html = '<div class="sec">Under the map</div>';
        html += '<div class="note">Three caverns. Veins of ' + ORE.join(", ") + ". A sacked mine is a dry counter.</div>";
        for (var i = 0; i < g.caverns.length; i++) {
          var c = g.caverns[i];
          html += '<div class="row"><div></div><div><div class="n">' + c.n + '</div><div class="d">' + c.fish + " · heat " + c.heat.toFixed(1) + "</div></div><div></div></div>";
        }
        for (var v = 0; v < g.veins.length && v < 5; v++) {
          var vein = g.veins[v];
          html += '<div class="row"><div></div><div><div class="d">' + vein.ore + " in the " + vein.layer + (vein.dry ? " · dry" : "") + "</div></div><div></div></div>";
        }
        if (g.note) html += '<div class="note">' + g.note + "</div>";
        var wrap = body.querySelector(".geo-atlas");
        if (!wrap) {
          wrap = document.createElement("div");
          wrap.className = "geo-atlas";
          var realm2 = body.querySelector(".realm-atlas");
          if (realm2 && realm2.nextSibling) body.insertBefore(wrap, realm2.nextSibling);
          else body.appendChild(wrap);
        }
        if (wrap.getAttribute("data-h") !== String(html.length)) {
          wrap.innerHTML = html;
          wrap.setAttribute("data-h", String(html.length));
        }
      }
      function seedWiki() {
        try {
          var w = typeof WIKI === "function" ? WIKI() : window.WIKI;
          if (!w || !w.push) return;
          var extra = {
            id: "k_geo",
            sec: "The shop floor",
            t: "Under the map",
            tags: "geology cavern mine ore magma vein stone",
            w: "<p>The continent has a basement. Soil, stone, an upper dark, still water, a magma shore. Veins of iron, copper, salt, mica, bogwood, gold. A mine that was sacked is a dry lot on the counter. If the boards run warm, that is volcanism under the harbor, not the weather.</p><p><b>What to do about it:</b> Atlas, Under the map. The cavern fish are not stock until a hold comes in.</p>"
          };
          for (var i = 0; i < w.length; i++) if (w[i] && w[i].id === extra.id) return;
          w.push(extra);
        } catch (e) {
        }
      }
      function now() {
        return typeof performance !== "undefined" ? performance.now() / 1e3 : Date.now() / 1e3;
      }
      function tick() {
        try {
          seedWiki();
          if (now() - lastTick > 1.6) {
            lastTick = now();
            tickYear();
            coupleSite();
          }
          if (now() - lastUi > 1.2) {
            lastUi = now();
            enhance();
          }
        } catch (e) {
        }
      }
      window.geo = {
        of: state,
        heat,
        scarce,
        lot: lotOf,
        caverns: function() {
          return state().caverns;
        }
      };
      if (window.__onBeat) window.__onBeat(tick, 420);
      else setTimeout(function loop() {
        tick();
        setTimeout(loop, 420);
      }, 420);
    })();
  } catch (e) {
    console.error("Layer geo.js failed to start:", e);
  }

  // src/layers/wild.js
  try {
    (function() {
      "use strict";
      var lastTick = 0;
      var lastUi = 0;
      var KINDS = ["tetra", "betta", "guppy", "gold", "cichlid", "koi", "angel", "clown"];
      var BIOME_KIND = {
        river: ["tetra", "guppy", "gold"],
        stream: ["betta", "tetra"],
        brackish: ["guppy", "clown"],
        marine: ["clown", "angel"],
        fresh: ["gold", "koi", "cichlid"],
        warm: ["discus", "angel", "betta"],
        cold: ["gold"],
        none: []
      };
      function gs() {
        try {
          if (typeof gameState === "function") return gameState();
          if (typeof gameState === "object" && gameState) return gameState;
        } catch (e) {
        }
        return null;
      }
      function mulberry(a) {
        return function() {
          var t = a += 1831565813;
          t = Math.imul(t ^ t >>> 15, t | 1);
          t ^= t + Math.imul(t ^ t >>> 7, t | 61);
          return ((t ^ t >>> 14) >>> 0) / 4294967296;
        };
      }
      function yearNow() {
        try {
          if (window.saga && typeof saga.year === "function") return saga.year();
        } catch (e) {
        }
        return 1e3;
      }
      function state() {
        var g = gs();
        if (g && g.wild && g.wild.pop) return g.wild;
        var pop = {};
        for (var i = 0; i < KINDS.length; i++) pop[KINDS[i]] = 0.55 + Math.random() * 0.35;
        var st = { pop, events: [], lastY: 0, note: "" };
        try {
          if (g) g.wild = st;
        } catch (e) {
        }
        return st;
      }
      function scarce(kind) {
        if (!kind) return false;
        var k2 = String(kind).toLowerCase();
        var st = state();
        var n = st.pop[k2];
        if (typeof n !== "number") {
          for (var key in st.pop) if (k2.indexOf(key) >= 0) return st.pop[key] < 0.22;
          return false;
        }
        return n < 0.22;
      }
      function stockOf(kind) {
        var st = state();
        var k2 = String(kind || "").toLowerCase();
        if (typeof st.pop[k2] === "number") return st.pop[k2];
        return 0.5;
      }
      function ev(st, y, text) {
        st.events.push({ y, s: text });
        if (st.events.length > 24) st.events.shift();
        st.note = text;
      }
      function tickYear() {
        var st = state();
        var y = yearNow();
        if (st.lastY >= y) return;
        st.lastY = y;
        var rng = mulberry(y * 7919 >>> 0);
        var k2 = KINDS[rng() * KINDS.length | 0];
        if (rng() < 0.2) {
          st.pop[k2] = Math.max(0.05, st.pop[k2] * 0.45);
          ev(st, y, "The " + k2 + " run failed inland. Holds of them will be thin.");
          try {
            if (window.weave && weave.because) weave.because("No " + k2 + " on the water. People will ask and leave.");
            if (window.weave && weave.rumor) weave.rumor("stock", "no " + k2 + " inland", 1);
          } catch (e) {
          }
        } else if (rng() < 0.18) {
          st.pop[k2] = Math.min(1, st.pop[k2] + 0.35);
          ev(st, y, "A bloom of " + k2 + ". The river is loud with them.");
        } else if (rng() < 0.15) {
          var k22 = KINDS[rng() * KINDS.length | 0];
          st.pop[k22] = Math.min(1, (st.pop[k22] || 0.4) + 0.2);
          ev(st, y, k22 + " came down with the season.");
        }
        for (var i = 0; i < KINDS.length; i++) {
          st.pop[KINDS[i]] = Math.max(0.05, Math.min(1, st.pop[KINDS[i]] + (rng() - 0.5) * 0.04));
        }
      }
      function enhance() {
        var body = document.getElementById("dbody");
        var title = document.getElementById("dtitle");
        if (!body || !title) return;
        if (!/atlas/i.test(title.textContent || "")) return;
        var st = state();
        var html = '<div class="sec">The living water</div>';
        html += '<div class="note">Populations inland, not in your glass. A failed run is a walk-in who asks and leaves.</div>';
        for (var i = 0; i < KINDS.length; i++) {
          var k2 = KINDS[i];
          var n = st.pop[k2];
          var tag = n < 0.22 ? "scarce" : n > 0.8 ? "bloom" : "";
          html += '<div class="row"><div></div><div><div class="n">' + k2 + (tag ? ' <span class="pill">' + tag + "</span>" : "") + '</div><div class="d">' + Math.round(n * 100) + " on the water</div></div><div></div></div>";
        }
        for (var e = st.events.length - 1, c = 0; e >= 0 && c < 4; e--, c++) {
          html += '<div class="row"><div></div><div><div class="d">' + st.events[e].y + " · " + st.events[e].s + "</div></div><div></div></div>";
        }
        var wrap = body.querySelector(".wild-atlas");
        if (!wrap) {
          wrap = document.createElement("div");
          wrap.className = "wild-atlas";
          body.appendChild(wrap);
        }
        if (wrap.getAttribute("data-h") !== String(html.length)) {
          wrap.innerHTML = html;
          wrap.setAttribute("data-h", String(html.length));
        }
      }
      function seedWiki() {
        try {
          var w = typeof WIKI === "function" ? WIKI() : window.WIKI;
          if (!w || !w.push) return;
          var extra = {
            id: "k_wild",
            sec: "The animals",
            t: "The living water",
            tags: "wild ecology overfish bloom migration population river",
            w: "<p>The tanks are not the only water. Inland, each kind has a population. A failed run makes that kind scarce on the counter: people still ask, they just do not buy. A bloom is the opposite. The season can walk a kind down from a colder river.</p><p><b>What to do about it:</b> Atlas, The living water. If the gold line says nobody is buying, check whether the river itself is empty.</p>"
          };
          for (var i = 0; i < w.length; i++) if (w[i] && w[i].id === extra.id) return;
          w.push(extra);
        } catch (e) {
        }
      }
      function now() {
        return typeof performance !== "undefined" ? performance.now() / 1e3 : Date.now() / 1e3;
      }
      function tick() {
        try {
          seedWiki();
          if (now() - lastTick > 1.5) {
            lastTick = now();
            tickYear();
          }
          if (now() - lastUi > 1.2) {
            lastUi = now();
            enhance();
          }
        } catch (e) {
        }
      }
      window.wild = {
        of: state,
        scarce,
        stock: stockOf,
        kinds: KINDS,
        biome: BIOME_KIND
      };
      if (window.__onBeat) window.__onBeat(tick, 430);
      else setTimeout(function loop() {
        tick();
        setTimeout(loop, 430);
      }, 430);
    })();
  } catch (e) {
    console.error("Layer wild.js failed to start:", e);
  }

  // src/layers/road.js
  try {
    (function() {
      "use strict";
      var lastTick = 0;
      var lastUi = 0;
      var lastComing = "";
      var didBrowse = false;
      function gs() {
        try {
          if (typeof gameState === "function") return gameState();
          if (typeof gameState === "object" && gameState) return gameState;
        } catch (e) {
        }
        return null;
      }
      function realmW() {
        try {
          if (window.realm && typeof realm.world === "function") return realm.world();
        } catch (e) {
        }
        return null;
      }
      function yearNow() {
        try {
          if (window.saga && typeof saga.year === "function") return saga.year();
        } catch (e) {
        }
        return 1e3;
      }
      function dist2(a, b) {
        var dx = a.x - b.x;
        var dy = a.y - b.y;
        return dx * dx + dy * dy;
      }
      function buildRoutes(w) {
        var routes = [];
        if (!w || !w.sites) return routes;
        for (var i = 0; i < w.sites.length; i++) {
          var a = w.sites[i];
          if (a.ruin) continue;
          var best = -1;
          var bd = 1e9;
          var best2 = -1;
          var bd2 = 1e9;
          for (var j = 0; j < w.sites.length; j++) {
            if (i === j) continue;
            var b = w.sites[j];
            if (b.ruin) continue;
            var d = dist2(a, b);
            if (d < bd) {
              bd2 = bd;
              best2 = best;
              bd = d;
              best = j;
            } else if (d < bd2) {
              bd2 = d;
              best2 = j;
            }
          }
          if (best >= 0) routes.push({ a: i, b: best, cut: 0, name: a.n + "–" + w.sites[best].n });
          if (best2 >= 0 && i === 0) routes.push({ a: i, b: best2, cut: 0, name: a.n + "–" + w.sites[best2].n });
        }
        return routes;
      }
      function state() {
        var g = gs();
        if (g && g.road && g.road.routes && g.road.routes.length) return g.road;
        var w = realmW();
        var st = { routes: buildRoutes(w), lastY: 0, note: "", caravan: "" };
        try {
          if (g) g.road = st;
        } catch (e) {
        }
        return st;
      }
      function cut2() {
        var st = state();
        for (var i = 0; i < st.routes.length; i++) {
          if (st.routes[i].cut && (st.routes[i].a === 0 || st.routes[i].b === 0)) return true;
        }
        try {
          if (window.realm && realm.embargo && realm.embargo()) return true;
        } catch (e) {
        }
        return false;
      }
      function tickYear() {
        var st = state();
        var y = yearNow();
        if (st.lastY >= y) return;
        st.lastY = y;
        var w = realmW();
        if (!w) return;
        var war = null;
        try {
          war = window.realm && realm.embargo ? realm.embargo() : null;
        } catch (e) {
        }
        if (war) {
          for (var i = 0; i < st.routes.length; i++) {
            if (Math.random() < 0.35) st.routes[i].cut = 1;
          }
          st.note = "Roads inland are cut. The baker is still next door. The rest is late.";
          try {
            if (window.weave && weave.bumpWord) weave.bumpWord(-0.08);
            if (window.weave && weave.because) weave.because("A road was cut. Towns come late.");
          } catch (e2) {
          }
        } else {
          for (var r = 0; r < st.routes.length; r++) {
            if (st.routes[r].cut && Math.random() < 0.4) st.routes[r].cut = 0;
          }
        }
        if (w.sites && w.sites.length > 2 && Math.random() < 0.22) {
          var s = w.sites[1 + (Math.random() * (w.sites.length - 1) | 0)];
          st.caravan = s.n;
          st.note = "A hold from " + s.n + " is on the water.";
        }
      }
      function injectComing() {
        try {
          var coming = window.desk && desk.coming ? desk.coming() : null;
          if (!coming || coming.done) return;
          var key = (coming.name || "") + "|" + (coming.want || "");
          if (key === lastComing) return;
          lastComing = key;
          if (coming._road) return;
          if (Math.random() > 0.22) return;
          var t = window.realm && realm.traveler ? realm.traveler() : null;
          if (!t || !t.site) return;
          coming._road = 1;
          coming.fromSite = t.site.n;
          if (t.fig && t.fig.n) coming.guestName = t.fig.n;
          var want = coming.want || "";
          var scarce = window.wild && want && wild.scarce(want);
          try {
            if (window.desk && desk.think) {
              desk.think("road", (t.fig ? t.fig.n : "Someone") + " of " + t.site.n + (scarce ? " asking for " + want + " that the river no longer has" : " off a longer road") + ".");
            }
            if (window.weave && weave.because) weave.because("They walked from " + t.site.n + ".");
          } catch (e) {
          }
        } catch (e2) {
        }
      }
      function wrapBrowse() {
        if (didBrowse || !window.shopBrowse) return;
        didBrowse = true;
        var orig = window.shopBrowse;
        window.shopBrowse = function() {
          var rec = orig.apply(this, arguments);
          try {
            if (!rec) return rec;
            var coming = window.desk && desk.coming ? desk.coming() : null;
            if (coming && coming.fromSite && rec.phase === "look" && !rec._roadLine) {
              rec._roadLine = 1;
              rec.line = rec.line || "From " + coming.fromSite + ".";
              rec.name = coming.guestName || rec.name;
            }
            if (window.wild && rec.want && wild.scarce(rec.want) && rec.phase === "look" && Math.random() < 0.4) {
              rec.line = "I heard the river is empty of " + rec.want + ".";
            }
            if (cut2() && rec.phase === "look" && Math.random() < 0.2) {
              rec.line = "The inland town is late.";
            }
          } catch (e) {
          }
          return rec;
        };
      }
      function enhance() {
        var body = document.getElementById("dbody");
        var title = document.getElementById("dtitle");
        if (!body || !title) return;
        if (!/atlas/i.test(title.textContent || "")) return;
        var st = state();
        var html = '<div class="sec">The roads</div>';
        html += '<div class="note">Every site keeps a nearest road. A war cuts the ones that touch the harbor. Then the inland town is late, and a person walks in who did not start on this block.</div>';
        var shown = 0;
        for (var i = 0; i < st.routes.length && shown < 8; i++) {
          var r = st.routes[i];
          html += '<div class="row"><div></div><div><div class="d">' + r.name + (r.cut ? " · cut" : "") + "</div></div><div></div></div>";
          shown++;
        }
        if (st.note) html += '<div class="note">' + st.note + "</div>";
        if (cut2()) html += '<div class="note">The road to the harbor is not open. Odds drop. People still come; they just do not pay.</div>';
        var wrap = body.querySelector(".road-atlas");
        if (!wrap) {
          wrap = document.createElement("div");
          wrap.className = "road-atlas";
          body.appendChild(wrap);
        }
        if (wrap.getAttribute("data-h") !== String(html.length)) {
          wrap.innerHTML = html;
          wrap.setAttribute("data-h", String(html.length));
        }
      }
      function seedWiki() {
        try {
          var w = typeof WIKI === "function" ? WIKI() : window.WIKI;
          if (!w || !w.push) return;
          var extra = {
            id: "k_road",
            sec: "The street",
            t: "The roads",
            tags: "road caravan traveler war embargo trade route",
            w: "<p>Salem Street is one block. The map has roads. A war inland cuts the ones that touch the harbor. Towns come late. Word drops. Someone walks in who started in a town you have only read. They will say the name.</p><p><b>What to do about it:</b> Atlas, The roads. If they say the inland town is late, that is not a random line.</p>"
          };
          for (var i = 0; i < w.length; i++) if (w[i] && w[i].id === extra.id) return;
          w.push(extra);
        } catch (e) {
        }
      }
      function now() {
        return typeof performance !== "undefined" ? performance.now() / 1e3 : Date.now() / 1e3;
      }
      function tick() {
        try {
          wrapBrowse();
          seedWiki();
          if (now() - lastTick > 1.5) {
            lastTick = now();
            tickYear();
            injectComing();
          }
          if (now() - lastUi > 1.2) {
            lastUi = now();
            enhance();
          }
        } catch (e) {
        }
      }
      window.road = {
        of: state,
        cut: cut2,
        tick: tickYear
      };
      if (window.__onBeat) window.__onBeat(tick, 440);
      else setTimeout(function loop() {
        tick();
        setTimeout(loop, 440);
      }, 440);
    })();
  } catch (e) {
    console.error("Layer road.js failed to start:", e);
  }

  // src/layers/tongue.js
  try {
    (function() {
      "use strict";
      var lastUi = 0;
      var cached = null;
      var ONSETS = ["b", "d", "f", "g", "h", "k", "l", "m", "n", "p", "r", "s", "t", "v", "w", "z", "sh", "th", "kh", "br", "kr", "st"];
      var NUC = ["a", "e", "i", "o", "u", "ae", "ie", "ou", "a", "e", "o"];
      var CODA = ["", "", "n", "r", "l", "s", "th", "k", "m"];
      var GLOSS = [
        ["the", "ta"],
        ["a", "a"],
        ["one", "un"],
        ["fish", "fin"],
        ["shop", "haus"],
        ["water", "ava"],
        ["gold", "aur"],
        ["good", "bel"],
        ["no", "na"],
        ["yes", "ya"],
        ["I", "mi"],
        ["you", "ti"],
        ["come", "ven"],
        ["keep", "hold"],
        ["take", "nem"]
      ];
      function mulberry(a) {
        return function() {
          var t = a += 1831565813;
          t = Math.imul(t ^ t >>> 15, t | 1);
          t ^= t + Math.imul(t ^ t >>> 7, t | 61);
          return ((t ^ t >>> 14) >>> 0) / 4294967296;
        };
      }
      function seedOf() {
        try {
          if (window.realm && realm.world) {
            var w = realm.world();
            if (w && w.seed) return w.seed >>> 0;
          }
        } catch (e) {
        }
        return 1e3;
      }
      function pick(rng, a) {
        return a[rng() * a.length | 0];
      }
      function syll(rng, ph) {
        return pick(rng, ph.on) + pick(rng, ph.nu) + pick(rng, ph.co);
      }
      function build() {
        var rng = mulberry(seedOf() ^ 28897);
        var civs = [];
        try {
          var w = window.realm && realm.world && realm.world();
          civs = w && w.civs || [];
        } catch (e) {
        }
        if (!civs.length) {
          civs = [{ n: "Harbor", i: 0 }, { n: "Inland", i: 1 }, { n: "Hill", i: 2 }];
        }
        var langs = [];
        for (var i = 0; i < civs.length; i++) {
          var r = mulberry((seedOf() ^ 28897) + i * 9973);
          var on = [], nu = [], co = [];
          var nOn = 6 + (r() * 5 | 0);
          var nNu = 3 + (r() * 3 | 0);
          var nCo = 3 + (r() * 3 | 0);
          var j;
          for (j = 0; j < nOn; j++) on.push(pick(r, ONSETS));
          for (j = 0; j < nNu; j++) nu.push(pick(r, NUC));
          for (j = 0; j < nCo; j++) co.push(pick(r, CODA));
          langs.push({
            i,
            civ: civs[i].n || civs[i].name || "folk",
            on,
            nu,
            co,
            order: r() > 0.5 ? "svo" : "sov",
            soft: r() > 0.45
          });
        }
        cached = { langs, seed: seedOf() };
        return cached;
      }
      function pack() {
        if (!cached || cached.seed !== seedOf()) return build();
        return cached;
      }
      function langOf(id) {
        var p = pack();
        var langs = p.langs || [];
        if (!langs.length) return null;
        var n = typeof id === "number" ? id : Math.abs(String(id || "0").split("").reduce(function(a, c) {
          return a + c.charCodeAt(0);
        }, 0));
        return langs[n % langs.length];
      }
      function word(id, nSyl) {
        var ph = langOf(id);
        if (!ph) return "fin";
        var rng = mulberry((seedOf() ^ 2748) + (nSyl || 2) * 13 + (typeof id === "number" ? id : 1));
        nSyl = nSyl || 2;
        var s = "";
        for (var i = 0; i < nSyl; i++) s += syll(rng, ph);
        return s.charAt(0).toUpperCase() + s.slice(1);
      }
      function name(id) {
        return word(id, 2) + " " + word(typeof id === "number" ? id + 3 : 3, 2);
      }
      function say(id, english) {
        english = String(english || "");
        var ph = langOf(id);
        if (!ph || !ph.soft) return english;
        var out = english;
        for (var i = 0; i < GLOSS.length; i++) {
          var re = new RegExp("\\b" + GLOSS[i][0] + "\\b", "ig");
          if (Math.random() < 0.22) out = out.replace(re, GLOSS[i][1]);
        }
        if (ph.soft && out.length > 8 && Math.random() < 0.18) out = out.replace(/\.$/, "") + ", eh.";
        return out;
      }
      function plaque(subject) {
        var w = word(7, 2);
        return w + " · " + String(subject || "the glass");
      }
      function panelHtml() {
        var p = pack();
        var lines = (p.langs || []).slice(0, 6).map(function(L) {
          return L.civ + ": " + word(L.i, 2) + " / " + word(L.i + 1, 2);
        });
        return "<p>The tongues.</p><p>" + lines.join(". ") + ".</p>";
      }
      function seedWiki() {
        try {
          var w = typeof WIKI === "function" ? WIKI() : WIKI;
          if (!w || !w.push) return;
          if (w.some(function(a) {
            return a && a.id === "k_tongue";
          })) return;
          w.push({
            id: "k_tongue",
            sec: "The world before you",
            t: "Tongues",
            tags: "language dialect civ name plaque speech",
            w: "<p>Every people inland has a mouth of their own. A traveler on Salem will still speak English, and then a word that is not. Names in the Atlas are not random letters. They were said that way.</p><p><b>What to do about it:</b> listen on the floor. The plaque on a tank is a word from the harbor league.</p>"
          });
        } catch (e) {
        }
      }
      function now() {
        return typeof performance !== "undefined" ? performance.now() / 1e3 : Date.now() / 1e3;
      }
      function tick() {
        try {
          seedWiki();
          if (now() - lastUi > 2) {
            lastUi = now();
            pack();
          }
        } catch (e) {
        }
      }
      window.tongue = { word, name, say, plaque, lang: langOf, panel: panelHtml };
      if (window.__onBeat) window.__onBeat(tick, 900);
      else setTimeout(function loop() {
        tick();
        setTimeout(loop, 900);
      }, 900);
    })();
  } catch (e) {
    console.error("Layer tongue.js failed to start:", e);
  }

  // src/layers/beast.js
  try {
    (function() {
      "use strict";
      var lastTick = 0;
      var lastUi = 0;
      var lastToast = "";
      var BODY = ["serpent", "crab", "eel", "ray", "turtle", "whale-thing", "jellyfish", "leech"];
      var MAT = ["scale", "shell", "glass", "bone", "kelp", "iron", "pearl", "salt"];
      var ADJ = ["pale", "blind", "horned", "winged", "two-mouthed", "salt-burnt", "long", "silent"];
      var ACT = ["sleeps", "hunts", "circles", "feeds", "nests", "dies inland"];
      function mulberry(a) {
        return function() {
          var t = a += 1831565813;
          t = Math.imul(t ^ t >>> 15, t | 1);
          t ^= t + Math.imul(t ^ t >>> 7, t | 61);
          return ((t ^ t >>> 14) >>> 0) / 4294967296;
        };
      }
      function gs() {
        try {
          if (typeof gameState === "function") return gameState();
          if (typeof gameState === "object" && gameState) return gameState;
        } catch (e) {
        }
        return null;
      }
      function yearNow() {
        try {
          if (window.saga && typeof saga.year === "function") return saga.year();
        } catch (e) {
        }
        return 1e3;
      }
      function seedOf() {
        try {
          var w = window.realm && realm.world && realm.world();
          if (w && w.seed) return w.seed >>> 0;
        } catch (e) {
        }
        return 1e3;
      }
      function pick(rng, a) {
        return a[rng() * a.length | 0];
      }
      function nameBeast(rng, i) {
        if (window.tongue && tongue.word) return tongue.word(40 + i, 3);
        return pick(rng, ADJ) + pick(rng, BODY);
      }
      function state() {
        var g = gs();
        if (g && g.beasts && g.beasts.list && g.beasts.list.length) return g.beasts;
        var rng = mulberry(seedOf() ^ 48727);
        var n = 10 + (rng() * 6 | 0);
        var list = [];
        for (var i = 0; i < n; i++) {
          list.push({
            i,
            n: nameBeast(rng, i),
            body: pick(rng, BODY),
            mat: pick(rng, MAT),
            adj: pick(rng, ADJ),
            x: rng() * 48 | 0,
            y: rng() * 32 | 0,
            hp: 0.4 + rng() * 0.6,
            act: "sleeps",
            near: 0
          });
        }
        var st = { list, events: [], lastY: 0, scare: 0, note: "" };
        try {
          if (g) g.beasts = st;
        } catch (e) {
        }
        return st;
      }
      function harborDist(b) {
        var dx = (b.x || 0) - 38;
        var dy = (b.y || 0) - 13;
        return Math.sqrt(dx * dx + dy * dy);
      }
      function tickYear() {
        var st = state();
        var y = yearNow();
        if (st.lastY === y) return;
        st.lastY = y;
        var rng = mulberry((seedOf() ^ y) * 7919);
        st.scare = 0;
        for (var i = 0; i < st.list.length; i++) {
          var b = st.list[i];
          if (rng() < 0.18) {
            b.x = Math.max(0, Math.min(47, b.x + (rng() * 5 | 0) - 2));
            b.y = Math.max(0, Math.min(31, b.y + (rng() * 5 | 0) - 2));
          }
          var d = harborDist(b);
          if (d < 6 && rng() < 0.35) {
            b.act = "circles";
            b.near = 1;
            st.scare = Math.max(st.scare, 0.07);
            st.note = b.n + " the " + b.adj + " " + b.body + " is off the harbor.";
            st.events.push({ y, n: b.n, k: "near" });
            if (st.events.length > 24) st.events.shift();
            try {
              if (window.weave && weave.because) weave.because(st.note);
              if (window.weave && weave.rumor) weave.rumor("beast", st.note, 0.8);
              if (window.rumor && rumor.add) rumor.add("beast", st.note, 0.85);
            } catch (e) {
            }
          } else if (d < 12) {
            b.act = "hunts";
            b.near = 0;
          } else {
            b.act = pick(rng, ACT);
            b.near = 0;
          }
          if (rng() < 0.03) {
            b.act = "dies inland";
            b.hp = 0;
            st.events.push({ y, n: b.n, k: "dead" });
          }
        }
      }
      function near() {
        var st = state();
        return st.scare || 0;
      }
      function line() {
        var st = state();
        if (st.note) return st.note.replace(/^\w/, function(c) {
          return "They say " + c.toLowerCase();
        });
        var live = st.list.filter(function(b) {
          return b.near;
        })[0];
        if (live) return "Something " + live.adj + " in the harbor. I saw the wake.";
        return "";
      }
      function toastOnce() {
        var st = state();
        if (!st.note || st.note === lastToast) return;
        lastToast = st.note;
        try {
          if (typeof k === "function") k(st.note, "bad");
        } catch (e) {
        }
      }
      function panelHtml() {
        var st = state();
        var rows = st.list.slice(0, 8).map(function(b) {
          return b.n + ", " + b.adj + " " + b.body + ", " + b.act;
        });
        return "<p>Beasts still moving.</p><p>" + rows.join(". ") + ".</p>" + (st.note ? "<p>" + st.note + "</p>" : "");
      }
      function seedWiki() {
        try {
          var w = typeof WIKI === "function" ? WIKI() : WIKI;
          if (!w || !w.push) return;
          if (w.some(function(a) {
            return a && a.id === "k_beast";
          })) return;
          w.push({
            id: "k_beast",
            sec: "The world before you",
            t: "Forgotten beasts",
            tags: "beast harbor scare till atlas unique",
            w: "<p>The continent still has things that were never stocked. They have a body, a material, a hunger, and a year they wake. When one circles the harbor, people on Salem do not buy. They talk. The till feels it.</p><p><b>What to do about it:</b> read Life. If the gold line says the harbor, wait. The Atlas keeps the name.</p>"
          });
        } catch (e) {
        }
      }
      function now() {
        return typeof performance !== "undefined" ? performance.now() / 1e3 : Date.now() / 1e3;
      }
      function tick() {
        try {
          seedWiki();
          if (now() - lastTick > 1.6) {
            lastTick = now();
            tickYear();
            toastOnce();
          }
        } catch (e) {
        }
      }
      window.beast = { state, near, line, tick: tickYear, panel: panelHtml };
      if (window.__onBeat) window.__onBeat(tick, 500);
      else setTimeout(function loop() {
        tick();
        setTimeout(loop, 500);
      }, 500);
    })();
  } catch (e) {
    console.error("Layer beast.js failed to start:", e);
  }

  // src/layers/faith.js
  try {
    (function() {
      "use strict";
      var lastTick = 0;
      var GODS = [
        { id: "salt", n: "the Salt Mother", sphere: "sea", fish: "clown", day: 3 },
        { id: "dry", n: "the Dry Eye", sphere: "sun", fish: "cichlid", day: 11 },
        { id: "wheel", n: "the Wheel", sphere: "craft", fish: "goldfish", day: 18 },
        { id: "net", n: "the First Net", sphere: "catch", fish: "tetra", day: 7 },
        { id: "oak", n: "the Quiet Oak", sphere: "wood", fish: "betta", day: 22 },
        { id: "vein", n: "the Red Vein", sphere: "blood", fish: "discus", day: 14 },
        { id: "ledger", n: "the Night Ledger", sphere: "debt", fish: "guppy", day: 27 }
      ];
      function gs() {
        try {
          if (typeof gameState === "function") return gameState();
          if (typeof gameState === "object" && gameState) return gameState;
        } catch (e) {
        }
        return null;
      }
      function yearNow() {
        try {
          if (window.saga && typeof saga.year === "function") return saga.year();
        } catch (e) {
        }
        return 1e3;
      }
      function doy() {
        try {
          var g = gs();
          if (g && isFinite(g.t)) return Math.floor((g.t % 1e9 + 1e9) % 1e9 / 2400) % 28;
        } catch (e) {
        }
        return yearNow() % 28;
      }
      function state() {
        var g = gs();
        if (g && g.faith && g.faith.gods) return g.faith;
        var st = { gods: GODS.map(function(x) {
          return { id: x.id, n: x.n, favor: 0.5 };
        }), lastFest: "", offended: "" };
        try {
          if (g) g.faith = st;
        } catch (e) {
        }
        return st;
      }
      function today() {
        var d = doy();
        for (var i = 0; i < GODS.length; i++) {
          if (GODS[i].day === d) return GODS[i];
        }
        return null;
      }
      function bless() {
        var g = today();
        if (!g) return 0;
        var st = state();
        var rec = st.gods.filter(function(x) {
          return x.id === g.id;
        })[0];
        var fav = rec ? rec.favor : 0.5;
        return 0.06 + fav * 0.08;
      }
      function taboo() {
        var g = today();
        return g ? g.fish : "";
      }
      function line() {
        var g = today();
        if (!g) return "";
        var lines = [
          "It's " + g.n + "'s day. The " + g.fish + " is the one.",
          "We don't skip the " + g.fish + " today.",
          g.n + " is looking. Keep a pair of " + g.fish + "."
        ];
        return lines[doy() % lines.length];
      }
      function onSale(kind) {
        kind = String(kind || "").toLowerCase();
        var g = today();
        var st = state();
        if (!g) return;
        var rec = st.gods.filter(function(x) {
          return x.id === g.id;
        })[0];
        if (!rec) return;
        if (kind.indexOf(g.fish) >= 0) {
          rec.favor = Math.min(1, rec.favor + 0.08);
          st.lastFest = g.n + " was pleased. A " + g.fish + " left the shop on her day.";
          try {
            if (window.weave && weave.because) weave.because(st.lastFest);
          } catch (e) {
          }
        } else if (Math.random() < 0.2) {
          rec.favor = Math.max(0, rec.favor - 0.04);
          st.offended = g.n;
        }
      }
      function panelHtml() {
        var g = today();
        var st = state();
        var rows = st.gods.map(function(x) {
          return x.n + " " + (x.favor > 0.6 ? "warm" : x.favor < 0.4 ? "cold" : "quiet");
        });
        return "<p>" + (g ? "Today is " + g.n + "'s. Favored: " + g.fish + "." : "No holy day.") + "</p><p>" + rows.join(". ") + ".</p>";
      }
      function seedWiki() {
        try {
          var w = typeof WIKI === "function" ? WIKI() : WIKI;
          if (!w || !w.push) return;
          if (w.some(function(a) {
            return a && a.id === "k_faith";
          })) return;
          w.push({
            id: "k_faith",
            sec: "The world before you",
            t: "The old names",
            tags: "faith god festival holy fish taboo till salt mother",
            w: "<p>The harbor still keeps seven names. Each has a day in the twenty-eight, a sphere, and a fish. Sell that fish on that day and the street is easier. Ignore it and they still come, they just pay less.</p><p><b>What to do about it:</b> Life will say the name. Keep a pair of whatever is holy this week. The Atlas already knew them.</p>"
          });
        } catch (e) {
        }
      }
      function now() {
        return typeof performance !== "undefined" ? performance.now() / 1e3 : Date.now() / 1e3;
      }
      function tick() {
        try {
          seedWiki();
          state();
          var g = today();
          if (g && now() - lastTick > 8) {
            lastTick = now();
            try {
              if (window.weave && weave.because && Math.random() < 0.15) weave.because("Festival: " + g.n + ".");
            } catch (e) {
            }
          }
        } catch (e2) {
        }
      }
      window.faith = { today, bless, taboo, line, onSale, panel: panelHtml };
      if (window.__onBeat) window.__onBeat(tick, 700);
      else setTimeout(function loop() {
        tick();
        setTimeout(loop, 700);
      }, 700);
    })();
  } catch (e) {
    console.error("Layer faith.js failed to start:", e);
  }

  // src/layers/rumor.js
  try {
    (function() {
      "use strict";
      var lastTick = 0;
      function gs() {
        try {
          if (typeof gameState === "function") return gameState();
          if (typeof gameState === "object" && gameState) return gameState;
        } catch (e) {
        }
        return null;
      }
      function yearNow() {
        try {
          if (window.saga && typeof saga.year === "function") return saga.year();
        } catch (e) {
        }
        return 1e3;
      }
      function state() {
        var g = gs();
        if (g && g.rumors && Array.isArray(g.rumors.list)) return g.rumors;
        var st = { list: [], heat: 0, last: "" };
        try {
          if (g) g.rumors = st;
        } catch (e) {
        }
        return st;
      }
      function add(kind, line, heat2) {
        var st = state();
        kind = String(kind || "talk");
        line = String(line || "").slice(0, 160);
        if (!line) return;
        for (var i = 0; i < st.list.length; i++) {
          if (st.list[i].line === line) {
            st.list[i].heat = Math.min(1, st.list[i].heat + (heat2 || 0.2));
            return;
          }
        }
        st.list.push({ k: kind, line, heat: heat2 == null ? 0.5 : heat2, y: yearNow() });
        if (st.list.length > 18) st.list.shift();
        st.last = line;
      }
      function heat() {
        var st = state();
        var h = 0;
        for (var i = 0; i < st.list.length; i++) h += st.list[i].heat || 0;
        st.heat = st.list.length ? h / st.list.length : 0;
        return st.heat;
      }
      function whisper() {
        var st = state();
        var hot = st.list.filter(function(r2) {
          return r2.heat > 0.4;
        });
        if (!hot.length) return "";
        var r = hot[Math.random() * hot.length | 0];
        return r.line;
      }
      function tickYear() {
        var st = state();
        for (var i = st.list.length - 1; i >= 0; i--) {
          st.list[i].heat *= 0.86;
          if (st.list[i].heat < 0.08) st.list.splice(i, 1);
        }
        heat();
        try {
          if (window.beast && beast.line) {
            var bl = beast.line();
            if (bl) add("beast", bl, 0.6);
          }
          if (window.faith && faith.today && faith.today()) {
            var g = faith.today();
            if (g && Math.random() < 0.2) add("faith", "It's " + g.n + "'s day.", 0.4);
          }
          if (window.road && road.cut && road.cut() && Math.random() < 0.25) add("road", "The inland road is cut.", 0.7);
        } catch (e) {
        }
      }
      function panelHtml() {
        var st = state();
        var rows = st.list.slice(-6).map(function(r) {
          return r.line;
        });
        return "<p>What the street is saying.</p><p>" + (rows.join(" ") || "Nothing loud.") + "</p>";
      }
      function seedWiki() {
        try {
          var w = typeof WIKI === "function" ? WIKI() : WIKI;
          if (!w || !w.push) return;
          if (w.some(function(a) {
            return a && a.id === "k_rumor";
          })) return;
          w.push({
            id: "k_rumor",
            sec: "The world before you",
            t: "Rumour",
            tags: "rumor heat street talk beast festival till",
            w: "<p>A fact that walks. Heat, a source, a line. People on the floor repeat it. If the harbor has a beast, if a road is cut, if a god has a day, it shows up as speech before it shows up as a number.</p><p><b>What to do about it:</b> listen. Life writes some of it. The till feels the rest.</p>"
          });
        } catch (e) {
        }
      }
      function now() {
        return typeof performance !== "undefined" ? performance.now() / 1e3 : Date.now() / 1e3;
      }
      function tick() {
        try {
          seedWiki();
          if (now() - lastTick > 2.2) {
            lastTick = now();
            tickYear();
          }
        } catch (e) {
        }
      }
      window.rumor = { add, heat, whisper, list: function() {
        return state().list;
      }, panel: panelHtml };
      if (window.__onBeat) window.__onBeat(tick, 800);
      else setTimeout(function loop() {
        tick();
        setTimeout(loop, 800);
      }, 800);
    })();
  } catch (e) {
    console.error("Layer rumor.js failed to start:", e);
  }

  // src/layers/guild.js
  try {
    (function() {
      "use strict";
      var lastTick = 0;
      var lastToast = "";
      var didBrowse = false;
      function gs() {
        try {
          if (typeof gameState === "function") return gameState();
          if (typeof gameState === "object" && gameState) return gameState;
        } catch (e) {
        }
        return null;
      }
      function yearNow() {
        try {
          if (window.saga && typeof saga.year === "function") return saga.year();
        } catch (e) {
        }
        return 1e3;
      }
      function shopDay() {
        try {
          var g = gs();
          if (g && isFinite(g.t)) return Math.floor((g.t % 1e9 + 1e9) % 1e9 / 2400);
        } catch (e) {
        }
        return 0;
      }
      function clamp(n) {
        return n < 0 ? 0 : n > 1 ? 1 : n;
      }
      function state() {
        var g = gs();
        if (g && g.guild && typeof g.guild.stand === "number") return g.guild;
        var st = {
          stand: 0.42,
          dues: 8,
          paid: 0,
          mandate: "goldfish",
          last: "",
          boycott: 0
        };
        try {
          if (g) g.guild = st;
        } catch (e) {
        }
        return st;
      }
      function standing() {
        return state().stand;
      }
      function boycott() {
        return state().stand < 0.22;
      }
      function edge() {
        var s = standing();
        if (s < 0.22) return -0.09;
        if (s > 0.72) return 0.07;
        return (s - 0.42) * 0.12;
      }
      function mandate() {
        return state().mandate;
      }
      function line() {
        var st = state();
        if (boycott()) return "The guild said stay away. I'm just looking through the glass.";
        if (st.stand > 0.7) return "The hall speaks well of this counter.";
        if (st.mandate) return "The hall wants a pair of " + st.mandate + " on the rack.";
        return "";
      }
      function noteSale(kind) {
        var st = state();
        st.stand = clamp(st.stand + 0.018);
        if (kind && String(kind).toLowerCase().indexOf(st.mandate) >= 0) {
          st.stand = clamp(st.stand + 0.05);
          st.last = "Mandate filled. " + st.mandate + ".";
          try {
            if (window.weave && weave.because) weave.because("The hall marked the " + st.mandate + " sale.");
          } catch (e) {
          }
          rollMandate(st);
        }
      }
      function noteMiss(kind) {
        var st = state();
        st.stand = clamp(st.stand - 0.025);
        if (kind && String(kind).toLowerCase().indexOf(st.mandate) >= 0) {
          st.stand = clamp(st.stand - 0.05);
          st.last = "Missed the mandate.";
        }
      }
      function rollMandate(st) {
        var opts = ["goldfish", "betta", "guppy", "tetra", "cichlid"];
        try {
          if (window.faith && faith.taboo && faith.taboo()) opts.push(faith.taboo());
        } catch (e) {
        }
        st.mandate = opts[Math.random() * opts.length | 0];
      }
      function collectDues() {
        var st = state();
        var d = shopDay();
        if (st.duesDay === d) return;
        st.duesDay = d;
        if (d % 7 !== 0 || d === 0) return;
        var g = gs();
        if (!g) return;
        if ((g.coins || 0) >= st.dues) {
          g.coins -= st.dues;
          st.paid++;
          st.stand = clamp(st.stand + 0.04);
          st.last = "Dues paid. " + st.dues + " to the hall.";
        } else {
          st.stand = clamp(st.stand - 0.1);
          st.last = "Dues missed. The hall noticed.";
          try {
            if (window.rumor && rumor.add) rumor.add("guild", "They skipped the hall.", 0.7);
            if (window.weave && weave.because) weave.because("Guild dues missed.");
          } catch (e) {
          }
        }
      }
      function toastBoycott() {
        var st = state();
        if (!boycott()) {
          st.boycott = 0;
          return;
        }
        if (st.boycott) return;
        st.boycott = 1;
        var msg = "The hall put the word out. People are walking past.";
        if (msg === lastToast) return;
        lastToast = msg;
        try {
          if (typeof k === "function") k(msg, "bad");
        } catch (e) {
        }
        try {
          var el = document.getElementById("hookWhisper");
          if (el) {
            el.textContent = msg;
            el.classList.add("on", "pop");
          }
        } catch (e2) {
        }
      }
      function wrapBrowse() {
        if (didBrowse || !window.shopBrowse) return;
        didBrowse = true;
        var orig = window.shopBrowse;
        window.shopBrowse = function(idx, slot, W, floorY, personS, simT) {
          var rec = orig.apply(this, arguments);
          try {
            if (!rec) return rec;
            var st = window.shopLife && shopLife.browse ? shopLife.browse()[idx] : null;
            var keep = typeof keepGuest === "function" ? keepGuest(st) : !!(st && (st._lateMae || st._goingHold || st._lateKid || st._mask));
            if (keep) return rec;
            if (boycott() && rec.phase === "look" && st && !st._guildSaid) {
              st._guildSaid = 1;
              rec.line = rec.kind === "kid" ? "We're not supposed to come in." : "The hall said stay away. I'm just looking through the glass.";
              rec.phase = "leave";
              st.phase = "leave";
              st.bought = false;
              st.line = rec.line;
            } else if (!boycott() && rec.phase === "look" && st && !st._guildSaid && state().mandate) {
              st._guildSaid = 1;
              if (rec.kind === "collector" || Math.random() < 0.22) {
                rec.line = "The hall wants a pair of " + state().mandate + " on the rack.";
                rec.want = rec.want || state().mandate;
                st.want = st.want || state().mandate;
                st.line = rec.line;
              }
            }
          } catch (e) {
          }
          return rec;
        };
      }
      function panelHtml() {
        var st = state();
        return "<p>The hall. Standing " + Math.round(st.stand * 100) + ". Mandate: " + st.mandate + (boycott() ? ". Boycott." : ".") + "</p><p>" + (st.last || "Quiet.") + "</p>";
      }
      function seedWiki() {
        try {
          var w = typeof WIKI === "function" ? WIKI() : WIKI;
          if (!w || !w.push) return;
          if (w.some(function(a) {
            return a && a.id === "k_guild";
          })) return;
          w.push({
            id: "k_guild",
            sec: "The shop floor",
            t: "The hall",
            tags: "guild dues standing mandate boycott till hall",
            w: "<p>There is a hall for people who sell living water. They want dues, and they want a pair of something on the rack. Fill it and standing rises. Miss the dues and they put the word out. A boycott is an empty aisle, not a letter.</p><p><b>What to do about it:</b> keep the mandate pair. Pay the week. Life will say if they have turned.</p>"
          });
        } catch (e) {
        }
      }
      function now() {
        return typeof performance !== "undefined" ? performance.now() / 1e3 : Date.now() / 1e3;
      }
      function tick() {
        try {
          wrapBrowse();
          seedWiki();
          if (now() - lastTick > 1.8) {
            lastTick = now();
            collectDues();
            toastBoycott();
          }
        } catch (e) {
        }
      }
      window.guild = {
        standing,
        boycott,
        edge,
        mandate,
        line,
        noteSale,
        noteMiss,
        panel: panelHtml
      };
      if (window.__onBeat) window.__onBeat(tick, 700);
      else setTimeout(function loop() {
        tick();
        setTimeout(loop, 700);
      }, 700);
    })();
  } catch (e) {
    console.error("Layer guild.js failed to start:", e);
  }

  // src/layers/fever.js
  try {
    (function() {
      "use strict";
      var lastTick = 0;
      var lastY = 0;
      var lastToast = "";
      function gs() {
        try {
          if (typeof gameState === "function") return gameState();
          if (typeof gameState === "object" && gameState) return gameState;
        } catch (e) {
        }
        return null;
      }
      function yearNow() {
        try {
          if (window.saga && typeof saga.year === "function") return saga.year();
        } catch (e) {
        }
        return 1e3;
      }
      function clamp(n) {
        return n < 0 ? 0 : n > 1 ? 1 : n;
      }
      function state() {
        var g = gs();
        if (g && g.fever && typeof g.fever.heat === "number") return g.fever;
        var st = { heat: 0.08, name: "", last: "", sicked: 0 };
        try {
          if (g) g.fever = st;
        } catch (e) {
        }
        return st;
      }
      function heat() {
        return state().heat;
      }
      function edge() {
        var h = heat();
        if (h < 0.28) return 0;
        return Math.min(0.1, (h - 0.28) * 0.22);
      }
      function line() {
        var st = state();
        if (st.heat < 0.32) return "";
        if (st.name) return "There's " + st.name + " on the street. I'm not lingering.";
        return "Something's going around. I'll look from the door.";
      }
      function infectFish() {
        var st = state();
        if (st.heat < 0.55) return;
        var g = gs();
        var fish = g && g.fish || [];
        var n = 0;
        for (var i = 0; i < fish.length && n < 2; i++) {
          var f = fish[i];
          if (!f || f.dead) continue;
          if ((f.sick || 0) > 0) continue;
          if (Math.random() < st.heat * 0.08) {
            f.sick = Math.max(f.sick || 0, 0.35);
            n++;
          }
        }
        if (n) {
          st.sicked += n;
          st.last = n + " in the glass caught it.";
        }
      }
      function tickHeat() {
        var st = state();
        var add = 0;
        try {
          if (window.beast && beast.near) add += beast.near() * 0.4;
        } catch (e) {
        }
        try {
          if (window.wild && wild.scarce) {
            add += 0.01;
          }
        } catch (e2) {
        }
        try {
          if (window.shopSite && shopSite.wet && shopSite.wet() > 0.4) add += 0.02;
        } catch (e3) {
        }
        try {
          var g = gs();
          if (g && g.clog) add += 0.03;
        } catch (e4) {
        }
        st.heat = clamp(st.heat * 0.97 + add);
        if (st.heat < 0.2) st.name = "";
      }
      function yearPulse() {
        var y = yearNow();
        if (y === lastY) return;
        lastY = y;
        var st = state();
        if (Math.random() < 0.12) {
          st.heat = clamp(st.heat + 0.35);
          st.name = st.name || (window.tongue && tongue.word ? tongue.word(9, 2) + " fever" : "harbor fever");
          st.last = st.name + " came up the road.";
          try {
            if (window.rumor && rumor.add) rumor.add("fever", st.last, 0.8);
            if (window.weave && weave.because) weave.because(st.last);
          } catch (e) {
          }
          var msg = st.last;
          if (msg !== lastToast) {
            lastToast = msg;
            try {
              if (typeof k === "function") k(msg, "bad");
            } catch (e2) {
            }
          }
        } else {
          st.heat = clamp(st.heat * 0.7);
        }
      }
      function treat() {
        var st = state();
        st.heat = clamp(st.heat - 0.18);
        st.last = "The column was treated. The street is easier.";
      }
      function panelHtml() {
        var st = state();
        return "<p>" + (st.name ? st.name : "No named fever") + ". Heat " + Math.round(st.heat * 100) + ".</p><p>" + (st.last || "The water is holding.") + "</p>";
      }
      function seedWiki() {
        try {
          var w = typeof WIKI === "function" ? WIKI() : WIKI;
          if (!w || !w.push) return;
          if (w.some(function(a) {
            return a && a.id === "k_fever";
          })) return;
          w.push({
            id: "k_fever",
            sec: "The shop floor",
            t: "Fever",
            tags: "fever ich sick street heat treat clog beast",
            w: "<p>A sickness that walks the road. It has heat, sometimes a name. When it is up, people look from the door and the glass can catch it. A clogged filter and a wet aisle feed it. A beast off the harbor feeds it.</p><p><b>What to do about it:</b> treat the water. Clean the filter. Wait. The gold line will say the name if it has one.</p>"
          });
        } catch (e) {
        }
      }
      function now() {
        return typeof performance !== "undefined" ? performance.now() / 1e3 : Date.now() / 1e3;
      }
      function tick() {
        try {
          seedWiki();
          if (now() - lastTick > 1.5) {
            lastTick = now();
            yearPulse();
            tickHeat();
            infectFish();
          }
        } catch (e) {
        }
      }
      window.fever = { heat, edge, line, treat, panel: panelHtml };
      if (window.__onBeat) window.__onBeat(tick, 600);
      else setTimeout(function loop() {
        tick();
        setTimeout(loop, 600);
      }, 600);
    })();
  } catch (e) {
    console.error("Layer fever.js failed to start:", e);
  }

  // src/layers/craft.js
  try {
    (function() {
      "use strict";
      var lastTick = 0;
      var lastCombo = 0;
      function gs() {
        try {
          if (typeof gameState === "function") return gameState();
          if (typeof gameState === "object" && gameState) return gameState;
        } catch (e) {
        }
        return null;
      }
      function yearNow() {
        try {
          if (window.saga && typeof saga.year === "function") return saga.year();
        } catch (e) {
        }
        return 1e3;
      }
      function clamp(n) {
        return n < 0 ? 0 : n > 1 ? 1 : n;
      }
      function state() {
        var g = gs();
        if (g && g.craft && typeof g.craft.q === "number") return g.craft;
        var st = { q: 0.4, bags: 0, master: 0, plaque: "", last: "" };
        try {
          if (window.tongue && tongue.plaque) st.plaque = tongue.plaque("the glass");
        } catch (e) {
        }
        try {
          if (g) g.craft = st;
        } catch (e2) {
        }
        return st;
      }
      function quality() {
        return state().q;
      }
      function edge() {
        return (quality() - 0.4) * 0.16;
      }
      function line() {
        var st = state();
        if (st.q > 0.78) return "Whoever bags here knows their hands.";
        if (st.q < 0.28) return "The last fish bag was sloppy.";
        if (st.plaque) return "The plaque says " + st.plaque.split("·")[0].trim() + ".";
        return "";
      }
      function noteBag(good) {
        var st = state();
        st.bags++;
        if (good) st.q = clamp(st.q + 0.03);
        else st.q = clamp(st.q - 0.04);
        try {
          if (window.weave && weave.of) {
            var w = weave.of();
            w.craft = st.q;
          }
        } catch (e) {
        }
      }
      function noteCombo(n) {
        n = n || 0;
        if (n === lastCombo) return;
        lastCombo = n;
        var st = state();
        if (n >= 4) {
          st.q = clamp(st.q + 0.08);
          st.master++;
          st.last = "A fish bag that will be talked about.";
          try {
            if (window.weave && weave.because) weave.because("A masterwork fish bag. Combo " + n + ".");
            if (window.rumor && rumor.add) rumor.add("craft", "They bag like a hall shop.", 0.55);
            if (typeof k === "function") k("That bag will be talked about.", "gold");
          } catch (e) {
          }
        } else if (n >= 2) {
          st.q = clamp(st.q + 0.02);
        }
      }
      function plaque() {
        var st = state();
        if (!st.plaque) {
          try {
            if (window.tongue && tongue.plaque) st.plaque = tongue.plaque("the glass");
          } catch (e) {
          }
        }
        return st.plaque;
      }
      function panelHtml() {
        var st = state();
        return "<p>Hands. Quality " + Math.round(st.q * 100) + ". Masterworks " + st.master + ".</p><p>" + (st.plaque ? "Plaque: " + st.plaque + ". " : "") + (st.last || "") + "</p>";
      }
      function seedWiki() {
        try {
          var w = typeof WIKI === "function" ? WIKI() : WIKI;
          if (!w || !w.push) return;
          if (w.some(function(a) {
            return a && a.id === "k_craft";
          })) return;
          w.push({
            id: "k_craft",
            sec: "The shop floor",
            t: "The hands",
            tags: "craft quality bag masterwork plaque combo till",
            w: "<p>A fish bag has quality. A tank has a plaque in a tongue that was spoken inland. String sales and the hands remember. A masterwork is not a menu. Someone pays and tells the hall.</p><p><b>What to do about it:</b> do not break the run. The gold line will say when a fish bag was good. The till keeps the rest.</p>"
          });
        } catch (e) {
        }
      }
      function now() {
        return typeof performance !== "undefined" ? performance.now() / 1e3 : Date.now() / 1e3;
      }
      function tick() {
        try {
          seedWiki();
          if (now() - lastTick > 1.2) {
            lastTick = now();
            try {
              if (window.hookRun) noteCombo(hookRun().combo || 0);
            } catch (e) {
            }
            plaque();
          }
        } catch (e2) {
        }
      }
      window.craft = {
        quality,
        edge,
        line,
        noteBag,
        noteCombo,
        plaque,
        panel: panelHtml
      };
      if (window.__onBeat) window.__onBeat(tick, 500);
      else setTimeout(function loop() {
        tick();
        setTimeout(loop, 500);
      }, 500);
    })();
  } catch (e) {
    console.error("Layer craft.js failed to start:", e);
  }

  // src/layers/room.js
  try {
    (function() {
      "use strict";
      var lastTick = 0;
      var lastSale = -1;
      var lastArrive = "";
      var lastClog = false;
      var lastWetCue = 0;
      var lastAtlas = "";
      var lastCrowd = 0;
      var overlay = null;
      var octx = null;
      var drops = [];
      var stillIds = /* @__PURE__ */ Object.create(null);
      var didBrowse = false;
      var didFish = false;
      function now() {
        return typeof performance !== "undefined" ? performance.now() / 1e3 : Date.now() / 1e3;
      }
      function gs() {
        try {
          if (typeof gameState === "function") return gameState();
          if (typeof gameState === "object" && gameState) return gameState;
        } catch (e) {
        }
        return null;
      }
      function hour() {
        try {
          if (typeof jt === "function") return jt() * 24;
          var g = gs();
          if (g && g.clock === "real") {
            var d = /* @__PURE__ */ new Date();
            return d.getHours() + d.getMinutes() / 60;
          }
          if (g && isFinite(g.t)) return (g.t / 1200 % 1 + 1) % 1 * 24;
        } catch (e) {
        }
        return 12;
      }
      function sceneName() {
        try {
          if (document.body.classList.contains("titling")) return "title";
          if (document.body.classList.contains("work")) return "work";
          if (typeof sceneNow === "function") return String(sceneNow() || "");
          if (window.G && window.G.scene) return String(window.G.scene);
        } catch (e) {
        }
        return "shop";
      }
      function play(name, x) {
        try {
          if (window.feel && feel.play) feel.play(name, x);
        } catch (e) {
        }
      }
      function rainAmt() {
        try {
          if (window.wxState && isFinite(wxState.rain)) return wxState.rain;
          if (typeof wxWord === "function" && /rain|storm|sleet/i.test(String(wxWord() || ""))) return 0.55;
        } catch (e) {
        }
        return 0;
      }
      function night() {
        var h = hour();
        return h < 6.2 || h >= 19.2;
      }
      function wet() {
        try {
          if (window.shopSite && shopSite.wet) return shopSite.wet();
        } catch (e) {
        }
        return 0;
      }
      function crowdN() {
        try {
          var c = window.shopLife && shopLife.crowd ? shopLife.crowd() : [];
          return c && c.length || 0;
        } catch (e) {
        }
        return 0;
      }
      function isStill(f) {
        if (!f) return false;
        if (f.sick || f.ill) return true;
        try {
          if (f.mind && f.mind.stress > 0.68) return true;
        } catch (e) {
        }
        return false;
      }
      function fixPuddle() {
        if (!window.shopSite || shopSite.__roomPuddle) return;
        var orig = shopSite.puddle;
        shopSite.puddle = function() {
          try {
            var s = shopSite.of && shopSite.of();
            if (s && s.cells) {
              var best = null;
              for (var i = 0; i < s.cells.length; i++) {
                var c = s.cells[i];
                if (c.kind === "floor" && c.wet > 0.1 && (!best || c.wet > best.wet)) best = c;
              }
              if (best) {
                return {
                  nx: (best.x + 0.5) / (s.w || 16),
                  ny: 0.58 + best.y / (s.h || 10) * 0.32,
                  wet: best.wet
                };
              }
            }
          } catch (e) {
          }
          return orig ? orig() : { nx: 0.48, ny: 0.74, wet: 0 };
        };
        shopSite.__roomPuddle = 1;
      }
      function continentBite() {
        var bite = { war: false, cut: false, scarce: "", fever: 0, heat: 0, line: "" };
        try {
          if (window.road && road.cut && road.cut()) {
            bite.cut = true;
            bite.line = "The inland town is late.";
          }
        } catch (e) {
        }
        try {
          if (window.fever && fever.heat) bite.fever = fever.heat();
          if (bite.fever > 0.4) bite.line = bite.line || fever.line && fever.line() || "Something's going around.";
        } catch (e2) {
        }
        try {
          if (window.wild && wild.of) {
            var st = wild.of();
            for (var k2 in st.pop) {
              if (st.pop[k2] < 0.22) {
                bite.scarce = k2;
                bite.line = bite.line || "I heard the river is empty of " + k2 + ".";
                break;
              }
            }
          }
        } catch (e3) {
        }
        try {
          if (window.weave && weave.of) {
            var w = weave.of();
            if (/war|embargo|plague/i.test(w.lastAtlas || "")) {
              bite.war = true;
              bite.line = bite.line || "War on the water. Stock will go dear.";
            }
          }
        } catch (e4) {
        }
        try {
          if (window.rumor && rumor.heat) bite.heat = rumor.heat();
        } catch (e5) {
        }
        return bite;
      }
      function wrapBrowse() {
        if (didBrowse || !window.shopBrowse) return;
        didBrowse = true;
        var orig = window.shopBrowse;
        window.shopBrowse = function(idx, slot, W, floorY, personS, simT) {
          var rec = orig.apply(this, arguments);
          try {
            if (!rec) return rec;
            var bite = continentBite();
            var st = window.shopLife && shopLife.browse ? shopLife.browse()[idx] : null;
            var coming = window.desk && desk.coming ? desk.coming() : null;
            if (coming && coming.arrived && coming.neighbor) {
              if (coming._roomIdx == null) coming._roomIdx = idx;
              if (coming._roomIdx === idx) {
                rec.name = coming.name || rec.name;
                rec.kind = "neighbor";
                if (st) {
                  st.guestName = rec.name;
                  st.kind = "neighbor";
                }
              }
            }
            if (rec.phase === "look" && bite.line && st && !st._roomBite && Math.random() < 0.28) {
              st._roomBite = 1;
              rec.line = bite.line;
              st.line = bite.line;
              st.lineUntil = now() + 3.1;
            }
            if (rec.phase === "pay" && st && !st._roomWalk && st.want && window.wild && wild.scarce && wild.scarce(st.want)) {
              st._roomWalk = 1;
              st.phase = "leave";
              rec.phase = "leave";
              rec.line = "I heard there were none. I'll wait.";
              st.line = rec.line;
              st.lineUntil = now() + 2.8;
              st.bought = false;
            }
            if (rec.phase === "pay" && st && !st._roomWalk && bite.fever > 0.55 && Math.random() < 0.45) {
              st._roomWalk = 1;
              st.phase = "leave";
              rec.phase = "leave";
              rec.line = "I'll look from the door.";
              st.line = rec.line;
              st.lineUntil = now() + 2.8;
            }
            if (rec.phase === "pay" && st && !st._roomBag) {
              st._roomBag = 1;
              play("bag", rec.x);
            }
          } catch (e) {
          }
          return rec;
        };
      }
      function wrapFish() {
        if (didFish || typeof window.drawFishSprite !== "function") return;
        didFish = true;
        var orig = window.drawFishSprite;
        window.drawFishSprite = function(a) {
          try {
            if (a && a.fish && isStill(a.fish) && a.x != null) {
              var id = a.fish.fid || a.fish.id || a.x;
              var pr = stillIds[id];
              if (pr) {
                a.x = pr.x + (a.x - pr.x) * 0.12;
                a.y = pr.y + (a.y - pr.y) * 0.12;
              }
              stillIds[id] = { x: a.x, y: a.y };
              a.fish._holdStill = 1;
            }
          } catch (e) {
          }
          return orig.apply(this, arguments);
        };
      }
      function ensureOverlay() {
        if (overlay && overlay.parentNode) return;
        overlay = document.getElementById("roomVeil");
        if (!overlay) {
          overlay = document.createElement("canvas");
          overlay.id = "roomVeil";
          overlay.setAttribute("aria-hidden", "true");
          document.body.appendChild(overlay);
        }
        octx = overlay.getContext("2d");
      }
      function resize() {
        ensureOverlay();
        var dpr = Math.min(2, window.devicePixelRatio || 1);
        var w = window.innerWidth;
        var h = window.innerHeight;
        if (overlay.width !== (w * dpr | 0) || overlay.height !== (h * dpr | 0)) {
          overlay.width = w * dpr | 0;
          overlay.height = h * dpr | 0;
          overlay.style.cssText = "position:fixed;inset:0;width:100%;height:100%;pointer-events:none;z-index:3";
          octx.setTransform(dpr, 0, 0, dpr, 0, 0);
        }
      }
      function drawRoom() {
        var sc = sceneName();
        if (sc === "title" || sc === "work") {
          if (octx && overlay) octx.clearRect(0, 0, overlay.width, overlay.height);
          return;
        }
        resize();
        var w = window.innerWidth;
        var h = window.innerHeight;
        octx.clearRect(0, 0, w, h);
        if (sc === "shop" || sc === "front") {
          var rain = rainAmt();
          if (rain > 0.12) {
            if (!drops.length) {
              for (var i = 0; i < 42; i++) {
                drops.push({
                  x: Math.random() * w,
                  y: Math.random() * h * 0.55,
                  z: 0.4 + Math.random() * 0.8,
                  l: 8 + Math.random() * 14
                });
              }
            }
            octx.strokeStyle = "rgba(190,220,240," + (0.08 + rain * 0.18).toFixed(3) + ")";
            octx.lineWidth = 1;
            octx.beginPath();
            for (var d = 0; d < drops.length; d++) {
              var p = drops[d];
              p.y += (4 + rain * 10) * p.z;
              p.x += rain * 0.6;
              if (p.y > h * 0.62) {
                p.y = -10;
                p.x = Math.random() * w;
              }
              octx.moveTo(p.x, p.y);
              octx.lineTo(p.x + 1.2, p.y + p.l);
            }
            octx.stroke();
            octx.fillStyle = "rgba(12,28,44," + (rain * 0.1).toFixed(3) + ")";
            octx.fillRect(0, 0, w, h * 0.62);
          }
          var pudW = wet();
          if (pudW > 0.18) {
            var pud = { nx: 0.48, ny: 0.76 };
            try {
              pud = shopSite.puddle() || pud;
            } catch (eP) {
            }
            var px = (pud.nx || 0.48) * w;
            var py = (pud.ny || 0.76) * h;
            var a = Math.min(0.42, pudW * 0.55);
            octx.save();
            var g = octx.createRadialGradient(px, py, 4, px, py, w * (0.08 + pudW * 0.1));
            g.addColorStop(0, "rgba(30,70,90," + (a + 0.08).toFixed(3) + ")");
            g.addColorStop(0.55, "rgba(18,48,66," + a.toFixed(3) + ")");
            g.addColorStop(1, "rgba(18,48,66,0)");
            octx.fillStyle = g;
            octx.beginPath();
            octx.ellipse(px, py, w * (0.1 + pudW * 0.08), 16 + pudW * 18, -0.18, 0, Math.PI * 2);
            octx.fill();
            octx.globalCompositeOperation = "lighter";
            octx.globalAlpha = 0.12 + pudW * 0.1;
            octx.strokeStyle = "rgba(160,210,230,.55)";
            octx.lineWidth = 1;
            octx.beginPath();
            octx.ellipse(px, py - 2, w * (0.07 + pudW * 0.05), 8 + pudW * 8, -0.18, 0.2, Math.PI * 1.4);
            octx.stroke();
            octx.restore();
          }
          if (night()) {
            octx.fillStyle = "rgba(4,12,28,0.18)";
            octx.fillRect(0, 0, w, h);
            var lamp = octx.createRadialGradient(w * 0.62, h * 0.22, 20, w * 0.55, h * 0.4, h * 0.7);
            lamp.addColorStop(0, "rgba(244,196,83,0.05)");
            lamp.addColorStop(1, "rgba(4,10,22,0.22)");
            octx.fillStyle = lamp;
            octx.fillRect(0, 0, w, h);
          }
        }
      }
      function audioBed() {
        var sc = sceneName();
        if (sc === "title" || sc === "work") return;
        var rain = rainAmt();
        var n = crowdN();
        var clog = false;
        try {
          clog = !!window.clogged;
        } catch (e) {
        }
        if (rain > 0.2 && now() - (audioBed._rainAt || 0) > 1.8) {
          audioBed._rainAt = now();
          play("rain");
        }
        if (clog && !lastClog) play("pump");
        if (clog && now() - (audioBed._pumpAt || 0) > 3.4) {
          audioBed._pumpAt = now();
          play("pump");
        }
        lastClog = clog;
        if (n > lastCrowd && n > 0) play("murmur");
        if (n >= 3 && now() - (audioBed._crowdAt || 0) > 5.5) {
          audioBed._crowdAt = now();
          play("crowd");
        }
        lastCrowd = n;
        if (n === 0 && wet() > 0.28 && now() - lastWetCue > 2.4) {
          lastWetCue = now();
          play("drip");
        }
      }
      function watchMorning() {
        try {
          var coming = window.desk && desk.coming ? desk.coming() : null;
          if (coming && coming.arrived && coming.name) {
            var key = coming.name + "|" + coming.arrived;
            if (key !== lastArrive) {
              lastArrive = key;
              play("wood", window.innerWidth * 0.12);
              play("whoosh", window.innerWidth * 0.12);
              if (coming.neighbor) {
                try {
                  if (window.weave && weave.because) {
                    weave.because("The baker came at lunch for " + (coming.want || "goldfish") + ".");
                  }
                } catch (eB) {
                }
              }
            }
          }
        } catch (e) {
        }
        try {
          var day = window.shopLife && shopLife.day ? shopLife.day() : null;
          if (day && lastSale >= 0 && day.sales > lastSale) {
            play("cash");
            play("register");
          }
          if (day) lastSale = day.sales;
        } catch (e2) {
        }
      }
      function watchAtlas() {
        var bite = continentBite();
        var line = bite.line;
        if (!line) return;
        var key = (bite.war ? "w" : "") + (bite.cut ? "c" : "") + bite.scarce + Math.round(bite.fever * 4);
        if (key === lastAtlas) return;
        if (bite.war || bite.cut || bite.scarce || bite.fever > 0.45) {
          lastAtlas = key;
          try {
            if (window.weave && weave.because) weave.because(line);
            if (window.rumor && rumor.add) rumor.add("atlas", line, bite.war ? 0.9 : 0.6);
            var gold = document.getElementById("hookWhisper");
            if (gold && (bite.war || bite.cut)) {
              gold.textContent = line;
              gold.classList.add("on", "pop");
            }
          } catch (e) {
          }
        }
      }
      function seedWiki() {
        try {
          var w = typeof WIKI === "function" ? WIKI() : window.WIKI;
          if (!w || !w.push) return;
          if (w.some(function(a) {
            return a && a.id === "k_room";
          })) return;
          w.push({
            id: "k_room",
            sec: "The shop floor",
            t: "The room",
            tags: "puddle night rain still fish aisle baker pair bag door",
            w: "<p>The aisle is wet or it isn't. Night cools the glass. A fish that is holding still will not bag. Rain on the street comes in at the door. None of that is a meter.</p><p><b>What to do about it:</b> swap a filter pad — the boards dry with it. Heat if the street is cold. Keep two of the same fish before lunch. The baker remembers the last fish bag.</p>"
          });
          w.push({
            id: "k_ear",
            sec: "The shop floor",
            t: "The ear",
            tags: "door bag filter rain murmur quiet packed",
            w: "<p>A quiet shop drips. A packed one murmurs. The door has a weight. A fish bag is paper, then the register. The filter, when it packs, is a pump that is working too hard.</p><p><b>What to do about it:</b> listen. If you cannot hear the room, the mute is on.</p>"
          });
        } catch (e) {
        }
      }
      function tick() {
        try {
          fixPuddle();
          wrapBrowse();
          wrapFish();
          seedWiki();
          if (now() - lastTick > 0.4) {
            lastTick = now();
            watchMorning();
            watchAtlas();
            audioBed();
          }
          drawRoom();
        } catch (e) {
        }
      }
      window.room = {
        wet,
        night,
        bite: continentBite,
        still: isStill
      };
      if (window.__onBeat) window.__onBeat(tick, 80);
      else setTimeout(function loop() {
        tick();
        setTimeout(loop, 80);
      }, 120);
    })();
  } catch (e) {
    console.error("Layer room.js failed to start:", e);
  }

  // src/layers/choir.js
  try {
    (function() {
      "use strict";
      var lastTick = 0;
      var lastUi = 0;
      var lastNamed = -1;
      var lastRaised = -1;
      var lastWhisper = "";
      var ptr = { x: 0.5, y: 0.5, on: 0 };
      var overlay = null;
      var octx = null;
      var master = null;
      var bedOsc = null;
      var bedGain = null;
      var hum = null;
      var voices = [];
      var VOICE_N = 6;
      var wiredPtr = false;
      var lastLost = "";
      var lostAt = -1e9;
      var lastSour = "";
      var didFish = false;
      var didBrowse = false;
      var PENT = [0, 2, 4, 7, 9, 12];
      function now() {
        return typeof performance !== "undefined" ? performance.now() / 1e3 : Date.now() / 1e3;
      }
      function gs() {
        try {
          if (typeof gameState === "function") return gameState();
          if (typeof gameState === "object" && gameState) return gameState;
        } catch (e) {
        }
        return null;
      }
      function sceneName() {
        try {
          if (document.body.classList.contains("titling")) return "title";
          if (document.body.classList.contains("work")) return "work";
          if (typeof sceneNow === "function") return String(sceneNow() || "");
          var on = document.querySelector("#scenes .sc.on");
          if (on && on.dataset.scene) return on.dataset.scene;
        } catch (e) {
        }
        return "tank";
      }
      function fishList() {
        try {
          if (typeof allFish === "function") return allFish() || [];
        } catch (e) {
        }
        return [];
      }
      function clamp01(n) {
        return n < 0 ? 0 : n > 1 ? 1 : n;
      }
      function because(text) {
        if (!text) return;
        try {
          if (window.weave && weave.because) weave.because(text);
        } catch (e) {
        }
        try {
          if (window.rumor && rumor.add) rumor.add("choir", text, 0.7);
        } catch (e2) {
        }
        try {
          if (window.desk && desk.think) desk.think("glass", text);
        } catch (e3) {
        }
      }
      function raisedN() {
        try {
          if (window.saga && saga.book) {
            var b = saga.book();
            if (b && b.occ && b.occ.raised) return b.occ.raised | 0;
          }
        } catch (e) {
        }
        return 0;
      }
      function tonicOf(f) {
        var sp = String(f && f.sp || "").toLowerCase();
        if (/betta/.test(sp)) return 220;
        if (/tetra/.test(sp)) return 196;
        if (/guppy/.test(sp)) return 247;
        if (/gold/.test(sp)) return 175;
        if (/cichlid/.test(sp)) return 147;
        if (/koi/.test(sp)) return 165;
        if (/angel/.test(sp)) return 262;
        if (/clown/.test(sp)) return 294;
        if (/discus/.test(sp)) return 185;
        if (/shrimp|snail|crab/.test(sp)) return 330;
        return 196;
      }
      function cents(n) {
        return Math.pow(2, n / 1200);
      }
      function weather() {
        var w = {
          wet: 0,
          fever: 0,
          feverName: "",
          beast: 0,
          beastLine: "",
          boycott: false,
          guild: 0,
          faith: "",
          bless: 0,
          scarce: "",
          cut: false,
          craft: 0.4,
          rumor: 0,
          raised: raisedN(),
          named: 0,
          still: 0,
          clog: false,
          crowd: 0,
          watching: 0,
          sour: 0,
          sweet: 0,
          line: "",
          word: ""
        };
        try {
          if (window.shopSite && shopSite.wet) w.wet = shopSite.wet() || 0;
        } catch (e) {
        }
        try {
          if (window.fever && fever.heat) w.fever = fever.heat() || 0;
          if (window.fever && fever.line) w.feverName = fever.line() || "";
        } catch (e2) {
        }
        try {
          if (window.beast && beast.near) w.beast = beast.near() || 0;
          if (window.beast && beast.line) w.beastLine = beast.line() || "";
        } catch (e3) {
        }
        try {
          if (window.guild && guild.boycott) w.boycott = !!guild.boycott();
          if (window.guild && guild.standing) w.guild = guild.standing();
        } catch (e4) {
        }
        try {
          if (window.faith && faith.taboo) w.faith = faith.taboo() || "";
          if (window.faith && faith.bless) w.bless = faith.bless() || 0;
        } catch (e5) {
        }
        try {
          if (window.wild && wild.of) {
            var st = wild.of();
            for (var k2 in st.pop) {
              if (st.pop[k2] < 0.22) {
                w.scarce = k2;
                break;
              }
            }
          }
        } catch (e6) {
        }
        try {
          if (window.road && road.cut && road.cut()) w.cut = true;
        } catch (e7) {
        }
        try {
          if (window.craft && craft.quality) w.craft = craft.quality();
        } catch (e8) {
        }
        try {
          if (window.rumor && rumor.heat) w.rumor = rumor.heat() || 0;
        } catch (e9) {
        }
        try {
          var g = gs();
          if (g && (g.clog || g.filterClog)) w.clog = true;
          if (typeof window.clogged !== "undefined") w.clog = w.clog || !!window.clogged;
        } catch (e10) {
        }
        try {
          var c = window.shopLife && shopLife.crowd ? shopLife.crowd() : [];
          w.crowd = c && c.length || 0;
        } catch (e11) {
        }
        try {
          var browse = window.shopLife && shopLife.browse ? shopLife.browse() : null;
          if (browse) {
            for (var i = 0; i < browse.length; i++) {
              if (browse[i] && browse[i].phase === "look") w.watching = 1;
            }
          }
        } catch (e12) {
        }
        var list = fishList();
        for (var f = 0; f < list.length; f++) {
          var fish = list[f];
          if (!fish) continue;
          if (fish.nick) w.named++;
          if (fish.sick || fish.mind && fish.mind.stress > 0.68) w.still++;
        }
        if (ptr.on && sceneName() === "tank") w.watching = 1;
        w.sour = clamp01(w.fever * 0.7 + w.beast * 4 + (w.boycott ? 0.45 : 0) + w.wet * 0.25 + (w.clog ? 0.2 : 0) + (w.raised ? 0.12 : 0));
        w.sweet = clamp01(w.bless * 4 + (w.craft - 0.4) * 0.8 + (w.named ? 0.08 : 0) + (w.guild > 0.7 ? 0.12 : 0));
        if (w.beastLine) w.line = w.beastLine;
        else if (w.fever > 0.4 && w.feverName) w.line = w.feverName;
        else if (w.boycott) w.line = "The hall put the word out.";
        else if (w.cut) w.line = "The inland town is late.";
        else if (w.scarce) w.line = "I heard the river is empty of " + w.scarce + ".";
        else if (w.wet > 0.36) w.line = "The aisle is wet.";
        else if (w.raised && w.watching) w.line = "That one watched me. It shouldn't be here.";
        else if (w.watching && w.named) w.line = "That one watched me.";
        if (w.line) w.word = w.line;
        else if (w.named) w.word = w.named + (w.named === 1 ? " named voice in the water." : " named voices in the water.");
        else w.word = "The water is holding a chord.";
        return w;
      }
      function sfxOk() {
        try {
          var I2 = window.I || window.sfx;
          if (I2 && typeof I2.ok === "function") return !!I2.ok();
        } catch (e) {
        }
        return true;
      }
      function audioCtx() {
        try {
          var fa = window.finsAudio;
          if (!fa) return null;
          return fa.ctx() || null;
        } catch (e) {
          return null;
        }
      }
      function ensureBus() {
        var ctx = audioCtx();
        if (!ctx) return null;
        if (master && master.context === ctx) return ctx;
        try {
          master = ctx.createGain();
          master.gain.value = 0;
          var fa = window.finsAudio;
          if (fa && fa.connect) fa.connect(master, true);
          else master.connect(ctx.destination);
        } catch (e) {
          return null;
        }
        return ctx;
      }
      function fade(node, v, t) {
        if (!node) return;
        try {
          var g = node.gain || node;
          var ctx = audioCtx();
          var at = ctx ? ctx.currentTime : 0;
          g.cancelScheduledValues(at);
          g.setTargetAtTime(v, at, t || 0.18);
        } catch (e) {
        }
      }
      function killVoices() {
        for (var i = 0; i < voices.length; i++) {
          try {
            voices[i].osc.stop();
          } catch (e) {
          }
          try {
            voices[i].osc.disconnect();
            voices[i].gain.disconnect();
          } catch (e2) {
          }
        }
        voices = [];
        if (bedOsc) {
          try {
            bedOsc.stop();
            bedOsc.disconnect();
            bedGain.disconnect();
          } catch (e3) {
          }
          bedOsc = null;
          bedGain = null;
        }
        if (hum) {
          try {
            hum.a.stop();
            hum.b.stop();
            hum.g.disconnect();
          } catch (e4) {
          }
          hum = null;
        }
      }
      function pickVoices(list) {
        var named = [];
        var rest = [];
        for (var i = 0; i < list.length; i++) {
          if (!list[i] || list[i].dead) continue;
          if (list[i].nick) named.push(list[i]);
          else rest.push(list[i]);
        }
        named.sort(function(a, b) {
          return String(a.nick).localeCompare(String(b.nick));
        });
        var out = named.concat(rest);
        return out.slice(0, VOICE_N);
      }
      function tuneVoice(v, f, w, ctx) {
        var base = tonicOf(f);
        var id = (f.fid || f.id || 0) | 0;
        var step = PENT[id % PENT.length];
        var det = (f.mind && f.mind.stress ? f.mind.stress * 28 : 0) + w.fever * 24 + w.beast * 180 + (w.raised ? 50 : 0) + (f.sick ? 18 : 0) - w.sweet * 12;
        var freq = base * Math.pow(2, step / 12) * cents(det);
        var amp = 0.028 + (f.nick ? 0.012 : 0);
        if (w.sour > 0.5) amp *= 0.7;
        if (sceneName() === "work") amp *= 0.45;
        try {
          v.osc.frequency.setTargetAtTime(freq, ctx.currentTime, 0.22);
          v.gain.gain.setTargetAtTime(amp, ctx.currentTime, 0.22);
        } catch (e) {
        }
      }
      function syncVoices(w) {
        var ctx = ensureBus();
        if (!ctx) return;
        var sc = sceneName();
        if (sc === "title" || document.hidden || !sfxOk()) {
          fade(master, 0, 0.3);
          return;
        }
        var list = pickVoices(fishList());
        var target = sc === "work" ? 0.045 : 0.09 + w.sweet * 0.04 - w.sour * 0.03;
        if (w.crowd >= 3) target *= 0.85;
        fade(master, Math.max(0, target), 0.4);
        if (sc === "work") {
          if (!hum) {
            try {
              var g = ctx.createGain();
              g.gain.value = 0.04;
              var a = ctx.createOscillator();
              var b = ctx.createOscillator();
              a.type = "sine";
              b.type = "sine";
              a.frequency.value = 60;
              b.frequency.value = 120;
              a.connect(g);
              b.connect(g);
              g.connect(master);
              a.start();
              b.start();
              hum = { a, b, g };
            } catch (e) {
            }
          }
          if (hum) fade(hum.g, 0.03 + w.sour * 0.04, 0.3);
          for (var h = 0; h < voices.length; h++) fade(voices[h].gain, 4e-3, 0.3);
          return;
        }
        if (hum) fade(hum.g, 0, 0.4);
        if (!bedOsc) {
          try {
            bedGain = ctx.createGain();
            bedGain.gain.value = 0.012;
            bedOsc = ctx.createOscillator();
            bedOsc.type = "sine";
            bedOsc.frequency.value = 55;
            bedOsc.connect(bedGain);
            bedGain.connect(master);
            bedOsc.start();
          } catch (e2) {
          }
        }
        if (bedOsc) {
          try {
            bedOsc.frequency.setTargetAtTime(w.sour > 0.45 ? 49 : 55, ctx.currentTime, 0.4);
            bedGain.gain.setTargetAtTime(0.01 + w.sour * 0.012, ctx.currentTime, 0.4);
          } catch (e3) {
          }
        }
        while (voices.length < list.length) {
          try {
            var gn = ctx.createGain();
            gn.gain.value = 0;
            var osc = ctx.createOscillator();
            osc.type = voices.length % 2 ? "triangle" : "sine";
            osc.connect(gn);
            gn.connect(master);
            osc.start();
            voices.push({ osc, gain: gn, id: "" });
          } catch (e4) {
            break;
          }
        }
        while (voices.length > list.length) {
          var drop = voices.pop();
          fade(drop.gain, 0, 0.12);
          (function(gone) {
            setTimeout(function() {
              try {
                gone.osc.stop();
                gone.osc.disconnect();
                gone.gain.disconnect();
              } catch (e5) {
              }
            }, 400);
          })(drop);
        }
        for (var i = 0; i < list.length && i < voices.length; i++) {
          voices[i].id = list[i].fid || list[i].nick || i;
          tuneVoice(voices[i], list[i], w, ctx);
        }
      }
      function ensureOverlay() {
        if (overlay && overlay.parentNode) return;
        overlay = document.getElementById("choirVeil");
        if (!overlay) {
          overlay = document.createElement("canvas");
          overlay.id = "choirVeil";
          overlay.setAttribute("aria-hidden", "true");
          document.body.appendChild(overlay);
        }
        octx = overlay.getContext("2d");
      }
      function drawGaze(w) {
        var sc = sceneName();
        if (sc === "title" || sc === "work") {
          if (octx && overlay) octx.clearRect(0, 0, overlay.width, overlay.height);
          return;
        }
        ensureOverlay();
        var dpr = Math.min(2, window.devicePixelRatio || 1);
        var vw = window.innerWidth;
        var vh = window.innerHeight;
        var bw = vw * dpr | 0;
        var bh = vh * dpr | 0;
        if (overlay.width !== bw || overlay.height !== bh) {
          overlay.width = bw;
          overlay.height = bh;
          overlay.style.cssText = "position:fixed;inset:0;width:100%;height:100%;pointer-events:none;z-index:3";
          octx.setTransform(dpr, 0, 0, dpr, 0, 0);
        }
        octx.clearRect(0, 0, vw, vh);
        if (!w.watching && w.sour < 0.22 && w.sweet < 0.2) return;
        var cx = sc === "shop" ? vw * 0.62 : vw * 0.5;
        var cy = sc === "shop" ? vh * 0.38 : vh * 0.46;
        var rad = Math.min(vw, vh) * (sc === "shop" ? 0.18 : 0.28);
        var a = 0.04 + w.watching * 0.05 + w.sweet * 0.04;
        if (w.sour > 0.4) a += 0.03;
        var col = w.sour > 0.45 ? "40,90,110" : w.raised ? "180,140,90" : "244,196,83";
        var g = octx.createRadialGradient(cx, cy, 8, cx, cy, rad);
        g.addColorStop(0, "rgba(" + col + "," + a.toFixed(3) + ")");
        g.addColorStop(1, "rgba(" + col + ",0)");
        octx.fillStyle = g;
        octx.fillRect(0, 0, vw, vh);
      }
      function wrapFish() {
        if (didFish || typeof window.drawFishSprite !== "function") return;
        didFish = true;
        var orig = window.drawFishSprite;
        window.drawFishSprite = function(a) {
          try {
            if (a && a.fish && a.x != null) {
              var sc = sceneName();
              if (sc === "tank" && ptr.on) {
                var tank = document.getElementById("tank");
                var tw = tank && tank.clientWidth || window.innerWidth;
                var th = tank && tank.clientHeight || window.innerHeight;
                var px = ptr.x * tw;
                var py = ptr.y * th;
                var dx = px - a.x;
                var dy = py - a.y;
                if (dx * dx + dy * dy < 140 * 140) {
                  a.x += dx * 0.018;
                  a.y += dy * 0.012;
                  a.fish._gaze = now();
                }
              } else if (sc === "shop" || sc === "front") {
                var looking = false;
                try {
                  var br = window.shopLife && shopLife.browse ? shopLife.browse() : null;
                  if (br) {
                    for (var i = 0; i < br.length; i++) if (br[i] && br[i].phase === "look") looking = true;
                  }
                } catch (eL) {
                }
                if (looking) {
                  a.x += 1.1;
                  a.fish._gaze = now();
                }
              }
            }
          } catch (e) {
          }
          return orig.apply(this, arguments);
        };
      }
      function wrapBrowse() {
        if (didBrowse || !window.shopBrowse) return;
        didBrowse = true;
        var orig = window.shopBrowse;
        window.shopBrowse = function(idx, slot, W, floorY, personS, simT) {
          var rec = orig.apply(this, arguments);
          try {
            if (!rec) return rec;
            var w = weather();
            var st = window.shopLife && shopLife.browse ? shopLife.browse()[idx] : null;
            if (rec.phase === "look" && w.line && st && !st._choirSaid && Math.random() < 0.34) {
              st._choirSaid = 1;
              rec.line = w.line;
              st.line = w.line;
              st.lineUntil = now() + 3;
            }
            if (rec.phase === "pay" && st && !st._choirWalk && w.sour > 0.52 && Math.random() < 0.4) {
              st._choirWalk = 1;
              st.phase = "leave";
              rec.phase = "leave";
              rec.line = w.beast ? "I saw the wake. I'm not staying." : "I'll look from the door.";
              st.line = rec.line;
              st.bought = false;
            }
            if (rec.phase === "pay" && st && w.sweet > 0.28 && w.craft > 0.7 && Math.random() < 0.25) {
              rec.line = rec.line || "Whoever bags here knows their hands.";
            }
          } catch (e) {
          }
          return rec;
        };
      }
      function watchVoices(w) {
        if (lastNamed < 0) {
          lastNamed = w.named;
          lastRaised = w.raised;
          return;
        }
        if (w.named < lastNamed) {
          var n = lastNamed - w.named;
          lastNamed = w.named;
          var msg = n === 1 ? "The water lost a voice." : n + " voices left the water.";
          if (msg !== lastLost || now() - lostAt > 8) {
            lastLost = msg;
            lostAt = now();
            because(msg);
            try {
              if (typeof k === "function") k(msg, "bad");
            } catch (e) {
            }
          }
        } else {
          lastNamed = w.named;
        }
        if (w.raised > lastRaised) {
          lastRaised = w.raised;
          because("A voice came back a half-step off.");
          try {
            if (typeof k === "function") k("The chord went wrong.", "bad");
          } catch (e2) {
          }
        }
        if (w.sour > 0.55 && w.line && w.line !== lastSour) {
          lastSour = w.line;
          because(w.line);
        }
      }
      function whisper(w) {
        w = w || weather();
        if (w.beast > 0.12 && w.beastLine) return w.beastLine;
        if (w.fever > 0.42 && w.feverName) return w.feverName;
        if (w.boycott) return "The hall put the word out. The aisle is empty.";
        if (w.raised && w.watching) return "That one watched me. It shouldn't be here.";
        if (lastLost && now() - lostAt < 8) return lastLost;
        if (w.watching && w.named && sceneName() === "tank") return "They're watching the hand.";
        return "";
      }
      function line() {
        return weather().line || weather().word;
      }
      function seedWiki() {
        try {
          var wiki = typeof WIKI === "function" ? WIKI() : window.WIKI;
          if (!wiki || !wiki.push) return;
          if (wiki.some(function(a) {
            return a && a.id === "k_choir";
          })) return;
          wiki.push({
            id: "k_choir",
            sec: "The shop floor",
            t: "The choir",
            tags: "choir glass voice music named raise watch score tank",
            w: "<p>The tank is a choir. Every living fish is a voice. The shop's music is not a playlist — it is the water, singing what it has seen. A named fish carries the melody. Sell the last of a line and a voice drops. Raise a name from the pages and it comes back a half-step off. Fever, a beast off the harbor, a boycott: they detune the chord. People at the glass can hear it. They say so.</p><p><b>What to do about it:</b> listen. Mute the playlist if you have to. Work mode turns it into the fluorescent hum, and the hum still sours when the water is off. The gold line will name a lost voice.</p>"
          });
          wiki.push({
            id: "k_gaze",
            sec: "The animals",
            t: "The glass looks back",
            tags: "gaze watch pointer customer witness fish glass",
            w: "<p>They can see you. The hand at the surface, the person on the aisle, the baker at lunch. Named fish lean toward whoever is looking. A customer will say it: that one watched me. A risen name watches harder, and wrong.</p><p><b>What to do about it:</b> stand still over the tank. They will come to the glass. That is not a meter. That is the shop looking back.</p>"
          });
        } catch (e) {
        }
      }
      function bindPtr() {
        if (wiredPtr) return;
        wiredPtr = true;
        var on = function(e) {
          var tank = document.getElementById("tank");
          if (!tank) return;
          var r = tank.getBoundingClientRect();
          if (!r.width) return;
          ptr.x = (e.clientX - r.left) / r.width;
          ptr.y = (e.clientY - r.top) / r.height;
          ptr.on = e.clientX >= r.left && e.clientX <= r.right && e.clientY >= r.top && e.clientY <= r.bottom ? 1 : 0;
        };
        window.addEventListener("pointermove", on, { passive: true });
        window.addEventListener("pointerleave", function() {
          ptr.on = 0;
        });
      }
      function tick() {
        try {
          bindPtr();
          wrapFish();
          wrapBrowse();
          seedWiki();
          var w = weather();
          if (now() - lastTick > 0.45) {
            lastTick = now();
            watchVoices(w);
            syncVoices(w);
            var wh = whisper(w);
            if (wh && wh !== lastWhisper) {
              lastWhisper = wh;
              try {
                var el = document.getElementById("hookWhisper");
                if (el && (w.beast > 0.12 || w.raised || now() - lostAt < 8)) {
                  el.textContent = wh;
                  el.classList.add("on", "pop");
                }
              } catch (eW) {
              }
            }
          }
          if (now() - lastUi > 0.08) {
            lastUi = now();
            drawGaze(w);
          }
        } catch (e) {
        }
      }
      window.choir = {
        weather,
        line,
        whisper,
        watching: function() {
          return weather().watching;
        }
      };
      window.addEventListener("visibilitychange", function() {
        if (document.hidden) fade(master, 0, 0.2);
      });
      window.addEventListener("pagehide", killVoices);
      if (window.__onBeat) window.__onBeat(tick, 80);
      else
        setTimeout(function loop() {
          tick();
          setTimeout(loop, 80);
        }, 140);
    })();
  } catch (e) {
    console.error("Layer choir.js failed to start:", e);
  }

  // src/layers/going.js
  try {
    (function() {
      "use strict";
      var lastTick = 0;
      var lastUi = 0;
      var lastSales = -1;
      var tillReady = false;
      var lastSnap = [];
      var lastDeath = "";
      var lastDeathAt = 0;
      var lastFry = "";
      var lastFryAt = 0;
      var lastBagLine = "";
      var lastBagAt = 0;
      var lastTideSaid = 0;
      var lastHour = -1;
      var didBrowse = false;
      var didChoir = false;
      var didFish = false;
      function now() {
        return typeof performance !== "undefined" ? performance.now() / 1e3 : Date.now() / 1e3;
      }
      function within(at, secs) {
        var age2 = now() - at;
        return age2 >= 0 && age2 < secs;
      }
      function gs() {
        try {
          if (typeof gameState === "function") return gameState();
          if (typeof gameState === "object" && gameState) return gameState;
        } catch (e) {
        }
        return null;
      }
      function year() {
        try {
          if (window.saga && typeof saga.year === "function") return saga.year();
        } catch (e) {
        }
        return 1e3;
      }
      function hour() {
        try {
          if (typeof jt === "function") return jt() * 24;
          var g = gs();
          if (g && isFinite(g.t)) return (g.t % 2400 + 2400) % 2400 / 100;
        } catch (e) {
        }
        return 12;
      }
      function shopDay() {
        try {
          var g = gs();
          if (g && isFinite(g.t)) return Math.floor((g.t % 1e9 + 1e9) % 1e9 / 2400);
        } catch (e) {
        }
        return 0;
      }
      function sceneName() {
        try {
          if (document.body.classList.contains("titling")) return "title";
          if (document.body.classList.contains("work")) return "work";
          if (typeof sceneNow === "function") return String(sceneNow() || "");
        } catch (e) {
        }
        return "tank";
      }
      function fishList() {
        try {
          if (typeof allFish === "function") return allFish() || [];
        } catch (e) {
        }
        return [];
      }
      function clamp01(n) {
        return n < 0 ? 0 : n > 1 ? 1 : n;
      }
      function because(text) {
        if (!text) return;
        try {
          if (window.weave && weave.because) weave.because(text);
        } catch (e) {
        }
        try {
          if (window.rumor && rumor.add) rumor.add("block", text, 0.65);
        } catch (e2) {
        }
        try {
          if (window.desk && desk.think) desk.think("block", text);
        } catch (e3) {
        }
      }
      function say(msg, kind) {
        try {
          if (typeof k === "function") k(msg, kind || "");
        } catch (e) {
        }
      }
      function play(name, x) {
        try {
          if (window.feel && feel.play) feel.play(name, x);
        } catch (e) {
        }
      }
      function egg(id, line2) {
        try {
          if (typeof findEgg === "function") findEgg(id, line2);
        } catch (e) {
        }
      }
      function kindOf(f) {
        if (!f) return "fish";
        try {
          var S = typeof O !== "undefined" ? O : typeof SPECIES !== "undefined" ? SPECIES : null;
          if (S && f.sp != null && S[f.sp]) {
            return String(S[f.sp].gname || S[f.sp].name || S[f.sp].vname || "fish").toLowerCase();
          }
        } catch (e) {
        }
        var sp = String(f && f.sp || "fish").toLowerCase();
        if (/gold/.test(sp)) return "goldfish";
        if (/betta/.test(sp)) return "betta";
        if (/guppy/.test(sp)) return "guppy";
        if (/tetra/.test(sp)) return "tetra";
        if (/angel/.test(sp)) return "angelfish";
        if (/cichlid/.test(sp)) return "cichlid";
        return sp || "fish";
      }
      function fidOf(f) {
        if (!f) return "";
        if (f.fid != null && f.fid !== "") return "fid:" + f.fid;
        if (f.id != null && String(f.id).length) return "id:" + f.id;
        if (f.nick) return "nick:" + f.nick;
        var sp = f.sp != null ? String(f.sp) : "";
        var born = f.born != null ? String(f.born) : "";
        var px = f.px != null ? Math.round(f.px) : "";
        var py = f.py != null ? Math.round(f.py) : "";
        return "k:" + sp + ":" + born + ":" + px + ":" + py;
      }
      function snapFish() {
        var list = fishList();
        var out = [];
        for (var i = 0; i < list.length; i++) {
          var f = list[i];
          if (!f) continue;
          out.push({
            id: fidOf(f),
            nick: f.nick || "",
            sp: kindOf(f),
            sick: !!(f.sick || f.ill || f.risen),
            named: !!f.nick,
            risen: !!f.risen
          });
        }
        return out;
      }
      function state() {
        var g = gs();
        if (g && g.going && Array.isArray(g.going.homes)) {
          if (!g.going._purged) {
            g.going._purged = 1;
            g.going.homes = g.going.homes.filter(function(h) {
              return h && h.sp && h.sp !== "fish";
            });
          }
          return g.going;
        }
        var st = {
          homes: [],
          letter: { open: false, from: "", want: "", y: 0, day: -1, filled: false },
          lastSaleN: -1,
          notes: []
        };
        try {
          if (g) g.going = st;
        } catch (e) {
        }
        return st;
      }
      function note(text) {
        if (!text) return;
        var st = state();
        if (st.notes[st.notes.length - 1] === text) return;
        st.notes.push(text);
        if (st.notes.length > 10) st.notes.shift();
      }
      function moon() {
        return shopDay() % 28 / 28;
      }
      function springTide() {
        var d = shopDay() % 28;
        return d <= 1 || d >= 27 || d >= 13 && d <= 15;
      }
      function tide() {
        var h = hour();
        var spring = springTide();
        var base = spring ? 0.62 : 0.22;
        var twice = Math.abs(Math.sin(h / 12 * Math.PI));
        var night = h < 6.4 || h >= 19;
        var lift = night ? 1 : 0.28;
        return clamp01(base * (0.35 + twice * 0.75) * lift);
      }
      function bakerName() {
        try {
          var n = window.desk && desk.neighbor ? desk.neighbor() : null;
          if (n) return n.known && n.name ? n.name : "The baker";
        } catch (e) {
        }
        return "The baker";
      }
      function whoBought(sp) {
        try {
          var c = window.desk && desk.coming ? desk.coming() : null;
          if (c && c.arrived && within(c.arrived, 28)) {
            return {
              who: c.name || bakerName(),
              whoKind: c.neighbor ? "neighbor" : "lunch",
              neighbor: !!c.neighbor,
              care: c.neighbor ? 0.82 : 0.48
            };
          }
        } catch (e) {
        }
        try {
          var br = window.shopLife && shopLife.browse ? shopLife.browse() : null;
          if (br) {
            for (var i = 0; i < br.length; i++) {
              var st = br[i];
              if (st && (st.bought || st.phase === "pay") && now() - (st.payAt || now()) < 8) {
                var kind = st.kind || "lunch";
                var care = 0.5;
                if (kind === "neighbor") care = 0.82;
                else if (kind === "collector") care = 0.7;
                else if (kind === "kid") care = 0.42;
                else if (kind === "lunch") care = 0.44;
                return {
                  who: st.guestName || st.name || "Someone on Salem",
                  whoKind: kind,
                  neighbor: kind === "neighbor",
                  care
                };
              }
            }
          }
        } catch (e2) {
        }
        var baker = /gold/.test(String(sp || ""));
        return {
          who: baker ? bakerName() : "Someone on Salem",
          whoKind: baker ? "neighbor" : "lunch",
          neighbor: baker,
          care: baker ? 0.78 : 0.5
        };
      }
      function kindIs(sold, want) {
        sold = String(sold || "").toLowerCase();
        want = String(want || "").toLowerCase();
        if (!sold || !want) return false;
        if (sold === want) return true;
        var words = sold.split(/\s+/);
        for (var i = 0; i < words.length; i++) if (words[i].indexOf(want) === 0) return true;
        return false;
      }
      function bumpWild(kind, dlt) {
        if (!kind) return;
        try {
          if (window.wild && wild.of) {
            var w = wild.of();
            if (w && w.pop) {
              if (w.pop[kind] == null) {
                for (var k2 in w.pop) {
                  if (kindIs(kind, k2)) {
                    kind = k2;
                    break;
                  }
                }
              }
              var cur = w.pop[kind];
              if (cur == null) cur = 0.45;
              w.pop[kind] = clamp01(cur + dlt);
            }
          }
        } catch (e) {
        }
      }
      function takeHome(gone) {
        if (!gone) return;
        var st = state();
        var buyer = whoBought(gone.sp);
        var home = {
          nick: gone.nick || "",
          sp: gone.sp || "fish",
          who: buyer.who,
          whoKind: buyer.whoKind,
          neighbor: !!buyer.neighbor,
          care: buyer.care,
          health: gone.sick ? 0.32 : 0.72 + buyer.care * 0.12,
          named: !!gone.named,
          sick: !!gone.sick,
          risen: !!gone.risen,
          day: shopDay(),
          y: year(),
          dead: false,
          fry: 0,
          told: false
        };
        st.homes.push(home);
        if (st.homes.length > 36) st.homes.shift();
        bumpWild(home.sp, 0.04);
        var call = home.nick || "The " + home.sp;
        var line2 = call + " went home with " + home.who + ". The block keeps them.";
        lastBagLine = line2;
        lastBagAt = now();
        note(line2);
        because(line2);
        egg("blockkeep", line2);
        if (home.neighbor) {
          say(home.who + "'s window has a " + home.sp + " now.", "gold");
        }
        fillLetterIf(home.sp);
      }
      function watchBags() {
        try {
          if (sceneName() === "title") return;
          if (!window.shopLife || !shopLife.day) return;
          var day = shopLife.day();
          if (!day || !isFinite(day.sales)) return;
          if (!tillReady) {
            lastSales = day.sales;
            lastSnap = snapFish();
            tillReady = true;
            return;
          }
          if (day.sales === lastSales + 1) {
            lastSales = day.sales;
            var nowSnap = snapFish();
            var have = /* @__PURE__ */ Object.create(null);
            for (var i = 0; i < nowSnap.length; i++) have[nowSnap[i].id] = 1;
            var taken = null;
            for (var j = 0; j < lastSnap.length; j++) {
              if (!lastSnap[j].id || have[lastSnap[j].id]) continue;
              taken = lastSnap[j];
              if (taken.named) break;
            }
            if (taken && taken.sp) takeHome(taken);
            lastSnap = nowSnap;
          } else {
            lastSnap = snapFish();
            lastSales = day.sales;
          }
        } catch (e) {
        }
      }
      function tickHomes() {
        var st = state();
        var d = shopDay();
        for (var i = 0; i < st.homes.length; i++) {
          var h = st.homes[i];
          if (h.dead) continue;
          if (h._day === d) continue;
          h._day = d;
          var drift = (h.care - 0.46) * 0.09;
          if (h.sick) drift -= 0.08;
          if (h.risen) drift -= 0.12;
          h.health = clamp01(h.health + drift);
          if (h.health < 0.14) {
            dieOnBlock(h);
            continue;
          }
          if (h.fry === 0 && h.health > 0.62 && d - h.day >= 2) {
            var pair = 0;
            for (var k2 = 0; k2 < st.homes.length; k2++) {
              var o = st.homes[k2];
              if (!o.dead && o.sp === h.sp && o.who === h.who) pair++;
            }
            if (pair >= 2 || h.neighbor && h.care > 0.7) {
              h.fry = 1;
              var fryLine = h.who + " has fry. Off the " + (h.nick || h.sp) + " that went home.";
              lastFry = fryLine;
              lastFryAt = now();
              note(fryLine);
              because(fryLine);
              egg("blockfry", fryLine);
            }
          }
        }
      }
      function dieOnBlock(h) {
        h.dead = true;
        h.health = 0;
        bumpWild(h.sp, -0.05);
        var call = h.nick || "The " + h.sp;
        var line2 = call + " died in " + h.who + "'s window.";
        lastDeath = line2;
        lastDeathAt = now();
        note(line2);
        because(line2);
        say(line2, "bad");
        egg("blockdead", line2);
        try {
          if (window.saga && saga.book) {
            var b = saga.book();
            if (b && Array.isArray(b.dead)) {
              b.dead.push({ n: h.nick || h.sp, how: "on the block", y: year(), who: h.who, street: 1 });
              if (b.dead.length > 80) b.dead.shift();
            }
          }
        } catch (e) {
        }
        try {
          if (window.desk && h.neighbor && desk.neighbor) {
            var n = desk.neighbor();
            if (n) n.trust = Math.max(0.08, (n.trust || 0.4) - 0.12);
          }
        } catch (e2) {
        }
      }
      function fillLetterIf(sp) {
        var st = state();
        var L = st.letter;
        if (!L.open || L.filled) return;
        if (kindIs(sp, L.want)) {
          L.filled = true;
          L.open = false;
          var line2 = "The town was answered. A pair of " + L.want + " is on the water.";
          note(line2);
          because(line2);
          say(line2, "gold");
          egg("holdsent", line2);
          play("chime");
          try {
            if (window.weave && weave.bumpWord) weave.bumpWord(0.08);
          } catch (e) {
          }
          try {
            if (window.road && road.of) {
              var r = road.of();
              if (r && r.routes) {
                for (var i = 0; i < r.routes.length; i++) {
                  if (r.routes[i].cut && (r.routes[i].a === 0 || r.routes[i].b === 0)) {
                    r.routes[i].cut = 0;
                    r.note = "A hold from " + (L.from || "inland") + " made the harbor.";
                    break;
                  }
                }
              }
            }
          } catch (e2) {
          }
        }
      }
      function watchLetter() {
        var st = state();
        var L = st.letter;
        var cut2 = false;
        var from = "";
        try {
          if (window.road && road.cut) cut2 = !!road.cut();
          if (window.road && road.of) {
            var r = road.of();
            if (r && r.caravan) from = r.caravan;
          }
        } catch (e) {
        }
        if (!from) {
          try {
            if (window.realm && realm.traveler) {
              var t = realm.traveler();
              if (t && t.site && t.site.n) from = t.site.n;
            }
          } catch (e2) {
          }
        }
        if (!from) from = "the inland town";
        var want = "";
        try {
          if (window.wild && wild.of) {
            var w = wild.of();
            for (var k2 in w.pop) {
              if (w.pop[k2] < 0.28) {
                want = k2;
                break;
              }
            }
          }
        } catch (e3) {
        }
        if (!want) {
          try {
            var c = window.desk && desk.coming ? desk.coming() : null;
            if (c && c.want) want = c.want;
          } catch (e4) {
          }
        }
        if (!want) want = "tetra";
        if (cut2 && !L.open && !L.filled) {
          L.open = true;
          L.filled = false;
          L.from = from;
          L.want = want;
          L.y = year();
          L.day = shopDay();
          var line2 = "A letter from " + from + ". They asked for a pair of " + want + ".";
          note(line2);
          because(line2);
          play("paper");
        }
        if (L.open && !L.filled && shopDay() - (L.day || 0) > 5) {
          L.open = false;
          var late2 = "The letter went unanswered. The town is still late.";
          note(late2);
          because(late2);
          try {
            if (window.weave && weave.bumpWord) weave.bumpWord(-0.04);
          } catch (e5) {
          }
        }
        if (!cut2 && L.filled) {
          L.filled = false;
        }
      }
      function applyTide() {
        var t = tide();
        if (t < 0.2) return;
        try {
          if (!window.shopSite || !shopSite.of) return;
          var s = shopSite.of();
          if (!s || !s.cells) return;
          var W = s.w || 16;
          for (var i = 0; i < s.cells.length; i++) {
            var c = s.cells[i];
            if (c.kind !== "floor") continue;
            var nearDoor = c.x >= W - 4 && c.y >= 3 && c.y <= 7;
            if (nearDoor) c.wet = Math.max(c.wet || 0, t * 0.72);
          }
          if (s.wetMax < t * 0.72) s.wetMax = t * 0.72;
        } catch (e) {
        }
        if (t > 0.4 && now() - lastTideSaid > 28) {
          lastTideSaid = now();
          var line2 = springTide() ? "Spring tide. The harbor's in under the boards." : "The harbor's in under the boards.";
          note(line2);
          because(line2);
          play("drip");
          egg("tidein", line2);
        }
      }
      function wrapBrowse() {
        if (didBrowse || !window.shopBrowse) return;
        didBrowse = true;
        var orig = window.shopBrowse;
        window.shopBrowse = function(idx, slot, W, floorY, personS, simT) {
          var rec = orig.apply(this, arguments);
          try {
            if (!rec) return rec;
            var st = window.shopLife && shopLife.browse ? shopLife.browse()[idx] : null;
            var go = state();
            var L = go.letter;
            if (L.open && !L.filled && rec.phase === "look" && st && !st._goingHold) {
              if ((idx | 0) === 0 || rec.kind === "collector") {
                st._goingHold = 1;
                rec.name = rec.name || "Someone from " + L.from;
                rec.line = "From " + L.from + ". A pair of " + L.want + ". The letter said.";
                rec.want = L.want;
                st.want = L.want;
                st.line = rec.line;
                st.lineUntil = now() + 4;
              }
            }
            if (st && !st._goingSaid && (rec.phase === "look" || rec.phase === "pay")) {
              var hit = null;
              for (var i = 0; i < go.homes.length; i++) {
                var h = go.homes[i];
                if (h.whoKind === rec.kind || h.neighbor && rec.kind === "neighbor") {
                  hit = h;
                  if (h.neighbor) break;
                }
              }
              if (hit) {
                st._goingSaid = 1;
                if (hit.dead && !hit.told) {
                  hit.told = true;
                  rec.line = (hit.nick || "The " + hit.sp) + " died on me. In the window.";
                  st.line = rec.line;
                  if (rec.phase === "pay" && Math.random() < 0.55) {
                    rec.phase = "leave";
                    st.phase = "leave";
                    st.bought = false;
                  }
                } else if (hit.fry === 1) {
                  hit.fry = 2;
                  rec.line = "I brought the little one. Off mine.";
                  st.line = rec.line;
                  lastFry = rec.line;
                  lastFryAt = now();
                  play("hatch");
                  because(hit.who + " brought fry off the " + (hit.nick || hit.sp) + " that went home.");
                  try {
                    if (typeof addRep === "function") addRep(2);
                  } catch (eR) {
                  }
                } else if (!hit.dead && rec.phase === "look") {
                  rec.line = rec.line || "The last one is still in the window. Another " + (hit.sp || "fish") + ".";
                  st.line = rec.line;
                }
              }
            }
          } catch (e) {
          }
          return rec;
        };
      }
      function wrapChoir() {
        if (didChoir || !window.choir || !choir.weather) return;
        didChoir = true;
        var orig = choir.weather;
        choir.weather = function() {
          var w = orig.apply(this, arguments);
          try {
            var n = 0;
            var homes = state().homes;
            for (var i = 0; i < homes.length; i++) {
              if (!homes[i].dead && homes[i].named) n++;
            }
            w.echo = n;
            w.tide = tide();
            if (n) w.sweet = Math.min(1, w.sweet + Math.min(0.22, n * 0.05));
            if (lastDeath && now() - lastDeathAt < 24) w.sour = Math.min(1, w.sour + 0.16);
            if (!w.line) {
              if (state().letter.open && !state().letter.filled) {
                w.line = "A letter from " + state().letter.from + ". They asked for a pair of " + state().letter.want + ".";
              } else if (w.tide > 0.42) {
                w.line = "The harbor's in under the boards.";
              } else if (n) {
                w.line = echoLine();
              }
            }
          } catch (e) {
          }
          return w;
        };
      }
      function wrapFish() {
        if (didFish || typeof window.drawFishSprite !== "function") return;
        didFish = true;
        var orig = window.drawFishSprite;
        window.drawFishSprite = function(a) {
          try {
            var t = tide();
            if (a && a.fish && a.x != null && t > 0.28 && sceneName() === "tank") {
              a.y += t * 0.9;
              a.x += Math.sin((now() + (a.fish.fid || 0)) * 0.7) * t * 0.6;
            }
          } catch (e) {
          }
          return orig.apply(this, arguments);
        };
      }
      function echoLine() {
        var homes = state().homes;
        for (var i = 0; i < homes.length; i++) {
          var h = homes[i];
          if (!h.dead && h.named) {
            return (h.nick || "The one you bagged") + " is still in " + h.who + "'s window.";
          }
        }
        for (var j = 0; j < homes.length; j++) {
          if (!homes[j].dead) {
            return "The " + homes[j].sp + " you bagged is still on Salem.";
          }
        }
        return "";
      }
      function whisper() {
        var st = state();
        var L = st.letter;
        if (L && L.open && !L.filled) {
          return "A letter from " + L.from + ". They asked for a pair of " + L.want + ".";
        }
        if (lastDeath && now() - lastDeathAt < 18) return lastDeath;
        if (lastFry && now() - lastFryAt < 14) return lastFry;
        if (lastBagLine && now() - lastBagAt < 10) return lastBagLine;
        var t = tide();
        if (t > 0.42) {
          return springTide() ? "Spring tide. The harbor's in under the boards." : "The harbor's in under the boards.";
        }
        var echo = echoLine();
        if (echo) return echo;
        return "";
      }
      function line() {
        var st = state();
        if (st.notes.length) return st.notes[st.notes.length - 1];
        return whisper();
      }
      function seedWiki() {
        try {
          var wiki = typeof WIKI === "function" ? WIKI() : window.WIKI;
          if (!wiki || !wiki.push) return;
          if (wiki.some(function(a) {
            return a && a.id === "k_going";
          })) return;
          wiki.push({
            id: "k_going",
            sec: "The shop floor",
            t: "The block keeps them",
            tags: "sold bag street window baker fry die going home block",
            w: "<p>A fish bag is not a deletion. They go home with someone on Salem. The baker's window is a tank. If they live, she will say so. If they breed, she brings the fry. If they die on the block, the name goes in the book from the street, not the glass. The choir can still hear a named one that left — a voice in a window.</p><p><b>What to do about it:</b> do not sell a sick fish to the baker. Read Life. The gold line will name the window.</p>"
          });
          wiki.push({
            id: "k_tide",
            sec: "The shop floor",
            t: "The harbor comes in",
            tags: "tide moon harbor boards night spring sea wet door",
            w: "<p>Rain at the door is weather. The harbor at night is the sea. Spring tide, new moon or full, the water comes in under the boards by the door — not from the filter. Named fish lean with it. The aisle dries. The tide does not care. It will be back at the next high.</p><p><b>What to do about it:</b> mop. Heat if the boards run cold. The gold line will say if it is the harbor, not the filter.</p>"
          });
          wiki.push({
            id: "k_letter",
            sec: "The quarter",
            t: "The letter",
            tags: "letter hold road cut pair inland send answer",
            w: "<p>When a road inland is cut, the inland town writes. A letter on the counter: a place, a pair, a fish. Someone walks in off that road and says the letter's words. Fill the pair. Bag that fish. The town is answered, and a road to the harbor can reopen. Leave it five days and the letter goes unanswered. The gold line stays late.</p><p>{letter}</p><p><b>What to do about it:</b> keep two of what they asked for. The letter is not a tab. It is the unfinished thing. Guide, This shop, names it if it is open.</p>"
          });
        } catch (e) {
        }
      }
      function tick() {
        try {
          wrapBrowse();
          wrapChoir();
          wrapFish();
          seedWiki();
          if (now() - lastTick > 0.7) {
            lastTick = now();
            watchBags();
            watchLetter();
            tickHomes();
            applyTide();
            try {
              var el = document.getElementById("hookWhisper");
              var L = state().letter;
              if (el && L && L.open && !L.filled && L.from) {
                var letter = "A letter from " + L.from + ". They asked for a pair of " + L.want + ".";
                var cur = el.textContent || "";
                if (!cur || cur === letter || /in a row|Paper\. Water|The till is a run|Don't miss|waiting on a (fish )?bag|inland town is late/.test(cur)) {
                  if (cur !== letter) {
                    el.textContent = letter;
                    el.classList.add("on", "pop");
                  }
                }
              }
            } catch (eW) {
            }
            var hr = hour();
            if (lastHour >= 0 && (lastHour > 20 && hr < 8 || lastHour < 8 && hr >= 8)) {
              tickHomes();
            }
            lastHour = hr;
          }
          if (now() - lastUi > 1.1) lastUi = now();
        } catch (e) {
        }
      }
      window.going = {
        whisper,
        line,
        homes: function() {
          return state().homes;
        },
        letter: function() {
          return state().letter;
        },
        tide,
        moon,
        echo: function() {
          var n = 0;
          var homes = state().homes;
          for (var i = 0; i < homes.length; i++) if (!homes[i].dead && homes[i].named) n++;
          return n;
        }
      };
      if (window.__onBeat) window.__onBeat(tick, 220);
      else
        setTimeout(function loop() {
          tick();
          setTimeout(loop, 220);
        }, 180);
    })();
  } catch (e) {
    console.error("Layer going.js failed to start:", e);
  }

  // src/layers/late.js
  try {
    (function() {
      "use strict";
      var lastTick = 0;
      var lastUi = 0;
      var awayAt = 0;
      var wiredVis = false;
      var lastGold = "";
      var lastGoldAt = 0;
      var stillSp = /* @__PURE__ */ Object.create(null);
      var didBrowse = false;
      var didFish = false;
      var KEEP = [
        { n: "Rook Hale", note: "He left the keys on the blotter." },
        { n: "Nedda Quill", note: "She wrote the lots in a smaller hand." },
        { n: "Asa Marr", note: "He owed the hall a week when he walked." },
        { n: "Wren Pell", note: "She named every fish. The street still uses those names." },
        { n: "Pim Voss", note: "He sold the last of a pair the winter he left." },
        { n: "Lila Costa", note: "Mae's aunt. The ovens knew this counter." }
      ];
      function now() {
        return typeof performance !== "undefined" ? performance.now() / 1e3 : Date.now() / 1e3;
      }
      function within(at, secs) {
        var age2 = now() - at;
        return age2 >= 0 && age2 < secs;
      }
      function gs() {
        try {
          if (typeof gameState === "function") return gameState();
          if (typeof gameState === "object" && gameState) return gameState;
        } catch (e) {
        }
        return null;
      }
      function year() {
        try {
          if (window.saga && typeof saga.year === "function") return saga.year();
        } catch (e) {
        }
        return 1e3;
      }
      function shopDay() {
        try {
          var g = gs();
          if (g && isFinite(g.t)) return Math.floor((g.t % 1e9 + 1e9) % 1e9 / 2400);
        } catch (e) {
        }
        return 0;
      }
      function sceneName() {
        try {
          if (document.body.classList.contains("titling")) return "title";
          if (document.body.classList.contains("work")) return "work";
          if (typeof sceneNow === "function") return String(sceneNow() || "");
        } catch (e) {
        }
        return "tank";
      }
      function fishList() {
        try {
          if (typeof allFish === "function") return allFish() || [];
        } catch (e) {
        }
        return [];
      }
      function hash32(s) {
        var h = 2166136261;
        s = String(s || "");
        for (var i = 0; i < s.length; i++) {
          h ^= s.charCodeAt(i);
          h = Math.imul(h, 16777619);
        }
        return h >>> 0;
      }
      function because(text) {
        if (!text) return;
        try {
          if (window.weave && weave.because) weave.because(text);
        } catch (e) {
        }
        try {
          if (window.desk && desk.think) desk.think("late", text);
        } catch (e2) {
        }
      }
      function say(msg, kind) {
        try {
          if (typeof k === "function") k(msg, kind || "");
        } catch (e) {
        }
      }
      function egg(id, line2) {
        try {
          if (typeof findEgg === "function") findEgg(id, line2);
        } catch (e) {
        }
      }
      function play(name) {
        try {
          if (window.feel && feel.play) feel.play(name);
        } catch (e) {
        }
      }
      function runSeed() {
        try {
          if (typeof runState === "function") {
            var r = runState();
            if (r && r.seed) return r.seed;
          }
        } catch (e) {
        }
        try {
          if (window.realm && realm.world) {
            var w = realm.world();
            if (w && w.seed) return w.seed;
          }
        } catch (e2) {
        }
        return 1;
      }
      function kindOf(f) {
        if (!f) return "fish";
        try {
          var S = typeof O !== "undefined" ? O : typeof SPECIES !== "undefined" ? SPECIES : null;
          if (S && f.sp != null && S[f.sp]) {
            var nm = S[f.sp].gname || S[f.sp].name || S[f.sp].vname;
            if (nm) return String(nm).toLowerCase();
          }
        } catch (e) {
        }
        var sp = String(f && f.sp || "");
        if (/gold/.test(sp)) return "goldfish";
        if (/betta/.test(sp)) return "betta";
        if (/guppy/.test(sp)) return "guppy";
        if (/tetra/.test(sp)) return "tetra";
        if (/angel/.test(sp)) return "angelfish";
        if (/cichlid/.test(sp)) return "cichlid";
        if (!sp || /^\d+$/.test(sp)) return "fish";
        return sp.toLowerCase();
      }
      function state() {
        var g = gs();
        if (g && g.late && g.late.keeper && g.late.keeper.n) return g.late;
        var h = hash32("late:" + runSeed() + ":" + year());
        var pick = KEEP[h % KEEP.length];
        var st = {
          keeper: {
            n: pick.n,
            note: pick.note,
            leftY: Math.max(990, year() - 1 - h % 3),
            kin: /Costa/.test(pick.n)
          },
          fid: "",
          nick: "",
          sp: "",
          greeted: false,
          soldKept: false,
          missed: [],
          missedAt: 0,
          awayMin: 0
        };
        try {
          if (g) g.late = st;
        } catch (e) {
        }
        return st;
      }
      function markKept() {
        var st = state();
        if (st.fid || st.nick) return;
        var list = fishList();
        var named = [];
        var gold2 = [];
        for (var i = 0; i < list.length; i++) {
          if (!list[i]) continue;
          if (list[i].nick) named.push(list[i]);
          if (/gold/.test(kindOf(list[i]))) gold2.push(list[i]);
        }
        var f = named[0] || gold2[0] || list[0];
        if (!f) return;
        st.fid = f.fid != null ? String(f.fid) : "";
        st.nick = f.nick || "";
        st.sp = kindOf(f);
        if (f.nick) {
          because(st.keeper.n + " named " + f.nick + ". The street still says it.");
        }
      }
      function keptFish() {
        var st = state();
        var list = fishList();
        for (var i = 0; i < list.length; i++) {
          var f = list[i];
          if (!f) continue;
          if (st.fid && String(f.fid) === st.fid) return f;
          if (st.nick && f.nick === st.nick) return f;
        }
        return null;
      }
      function seedOpen() {
        var st = state();
        if (st._seeded) return;
        st._seeded = 1;
        markKept();
        var k2 = st.keeper;
        var line2 = k2.n + " kept this shop through Year " + k2.leftY + ". " + k2.note + " You hung the sign in Year " + year() + ".";
        because(line2);
        try {
          if (window.saga && typeof chronicle === "function") {
            chronicle("era", "Year " + k2.leftY + ". " + k2.n + " locked the door and did not come back.");
          }
        } catch (e) {
        }
        egg("latekeep", line2);
      }
      function gold(text, force) {
        if (!text) return;
        lastGold = text;
        lastGoldAt = now();
        try {
          var el = document.getElementById("hookWhisper");
          if (el && (force || el.textContent !== text)) {
            el.textContent = text;
            el.classList.add("on", "pop");
          }
        } catch (e) {
        }
      }
      function wetBoards(amt) {
        try {
          if (!window.shopSite || !shopSite.of) return;
          var s = shopSite.of();
          if (!s || !s.cells) return;
          var W = s.w || 16;
          for (var i = 0; i < s.cells.length; i++) {
            var c = s.cells[i];
            if (c.kind === "floor" && c.x >= W - 6) {
              c.wet = Math.min(1, (c.wet || 0) + amt);
            }
            if (c.kind === "filter") {
              c.water = Math.min(1, (c.water || 0) + amt);
              c.press = Math.min(2, (c.press || 0) + amt);
            }
          }
          s.wetMax = Math.min(1, Math.max(s.wetMax || 0, amt));
          s.leak = Math.min(1, (s.leak || 0) + amt * 0.6);
        } catch (e) {
        }
      }
      function catchUp() {
        if (!awayAt) return;
        var min = (Date.now() - awayAt) / 6e4;
        awayAt = 0;
        if (min < 0.4) return;
        if (sceneName() === "title") return;
        var st = state();
        st.awayMin = min;
        var hours = Math.min(14, Math.max(1, Math.floor(min * 0.45)));
        var h = hash32("away:" + runSeed() + ":" + shopDay() + ":" + Math.floor(min));
        var missed = [];
        if (h % 10 < 5 || hours >= 4) {
          wetBoards(0.42);
          try {
            var g0 = gs();
            if (g0) g0.filterClog = true;
          } catch (eC) {
          }
          missed.push("The filter clogged while you were gone.");
        }
        try {
          var n = window.desk && desk.neighbor ? desk.neighbor() : null;
          var have = window.shopLife && shopLife.stock ? shopLife.stock() : [];
          var wet = window.shopSite && shopSite.wet ? shopSite.wet() : 0;
          if (n && hours >= 2) {
            var call = n.known && n.name ? n.name : "The baker";
            var ok = have && have.indexOf(n.want || "goldfish") >= 0 && wet < 0.36;
            if (ok && missed[0] !== "The filter clogged while you were gone.") {
              missed.push(call + " came for " + (n.want || "goldfish") + ". The fish bag landed. You weren't at the counter.");
              n.bags = (n.bags || 0) + 1;
              n.known = true;
              n.trust = Math.min(1, (n.trust || 0.4) + 0.08);
              n.last = "took the " + (n.want || "goldfish");
              try {
                var g = gs();
                if (g) g.coins = (g.coins || 0) + 12;
              } catch (eG) {
              }
              try {
                if (window.going && going.homes) {
                  going.homes().push({
                    nick: "",
                    sp: n.want || "goldfish",
                    who: n.known ? n.name : "The baker",
                    whoKind: "neighbor",
                    neighbor: true,
                    care: 0.82,
                    health: 0.78,
                    named: false,
                    sick: false,
                    risen: false,
                    day: shopDay(),
                    y: year(),
                    dead: false,
                    fry: 0,
                    told: false
                  });
                }
              } catch (eH) {
              }
            } else {
              var why = wet > 0.34 ? "The aisle was wet. She walked." : "No " + (n.want || "goldfish") + ". She walked.";
              missed.push(call + " came. " + why);
              n.trust = Math.max(0.08, (n.trust || 0.4) - 0.08);
              n.last = "walked";
            }
          }
        } catch (eB) {
        }
        if (hours >= 6) {
          missed.push("The harbor came in under the boards.");
          wetBoards(0.28);
        }
        var kept = keptFish();
        if (kept && (hours >= 3 || missed.length)) {
          try {
            kept.mind = kept.mind || {};
            kept.mind.stress = Math.min(1, (kept.mind.stress || 0.2) + 0.22);
          } catch (eS) {
          }
          var kn2 = kept.nick || "The one " + st.keeper.n.split(" ")[0] + " kept";
          missed.push(kn2 + " is holding still. You weren't at the glass.");
        }
        try {
          if (window.going && going.homes) {
            var hs = going.homes();
            for (var i = 0; i < hs.length; i++) {
              if (!hs[i] || hs[i].dead) continue;
              hs[i].health = Math.max(0, (hs[i].health || 0.7) - hours * 0.02 * (1.1 - (hs[i].care || 0.5)));
              if (hs[i].health < 0.14 && !hs[i].dead) {
                hs[i].dead = true;
                missed.push((hs[i].nick || "The " + hs[i].sp) + " died in " + hs[i].who + "'s window while you were gone.");
              }
            }
          }
        } catch (eD) {
        }
        if (!missed.length) {
          missed.push("The shop ran. Nobody bagged. The water held.");
        }
        st.missed = missed.slice(0, 6);
        st.missedAt = now();
        for (var m = 0; m < st.missed.length; m++) {
          because(st.missed[m]);
        }
        gold(st.missed[0], true);
        say(st.missed[0], hours >= 4 ? "bad" : "");
        play("drip");
        egg("away", st.missed[0]);
      }
      function bindAway() {
        if (wiredVis) return;
        wiredVis = true;
        var hide = function() {
          awayAt = Date.now();
        };
        var show = function() {
          try {
            catchUp();
          } catch (e) {
          }
        };
        document.addEventListener("visibilitychange", function() {
          if (document.hidden) hide();
          else show();
        });
        window.addEventListener("pagehide", hide);
        window.addEventListener("pageshow", show);
      }
      function wrapBrowse() {
        if (didBrowse || !window.shopBrowse) return;
        didBrowse = true;
        var orig = window.shopBrowse;
        window.shopBrowse = function(idx, slot, W, floorY, personS, simT) {
          var rec = orig.apply(this, arguments);
          try {
            if (!rec) return rec;
            var st = window.shopLife && shopLife.browse ? shopLife.browse()[idx] : null;
            var L = state();
            var first = L.keeper.n.split(" ")[0];
            var kept = keptFish();
            if (rec.kind === "neighbor" && rec.phase === "look" && st && !st._lateMae && !L.greeted) {
              st._lateMae = 1;
              L.greeted = true;
              rec.line = L.keeper.kin ? "You're not " + first + ". She was my aunt." : "You're not " + first + ".";
              st.line = rec.line;
              st.lineUntil = now() + 4;
              because(rec.line);
              gold(rec.line, true);
            } else if (rec.kind === "kid" && rec.phase === "look" && st && !st._lateKid && kept && L.nick) {
              st._lateKid = 1;
              rec.line = "Is " + L.nick + " still in there? " + first + "'s " + (L.sp || "fish") + ".";
              st.line = rec.line;
            } else if (L.soldKept && rec.phase === "look" && st && !st._lateGone) {
              st._lateGone = 1;
              rec.line = "Where's the one " + first + " kept?";
              st.line = rec.line;
              if (rec.kind !== "kid" && Math.random() < 0.35) {
                rec.phase = "leave";
                st.phase = "leave";
                st.bought = false;
              }
            } else if (kept && rec.phase === "look" && st && !st._lateLook && rec.kind === "collector") {
              st._lateLook = 1;
              rec.line = rec.line || first + " used to keep that " + (L.sp || "one") + ". Don't round it.";
              st.line = rec.line;
            }
          } catch (e) {
          }
          return rec;
        };
      }
      function refreshStill() {
        stillSp = /* @__PURE__ */ Object.create(null);
        try {
          if (!window.going || !going.homes) return;
          var hs = going.homes();
          var gone = /* @__PURE__ */ Object.create(null);
          for (var j = 0; j < hs.length; j++) {
            if (hs[j] && !hs[j].dead && hs[j].sp) gone[hs[j].sp] = 1;
          }
          var list = fishList();
          var counts = /* @__PURE__ */ Object.create(null);
          for (var i = 0; i < list.length; i++) {
            if (!list[i] || !list[i].nick) continue;
            var k2 = kindOf(list[i]);
            counts[k2] = (counts[k2] || 0) + 1;
          }
          for (var sp in counts) {
            if (counts[sp] === 1 && gone[sp]) stillSp[sp] = 1;
          }
        } catch (e) {
        }
      }
      function wrapFish() {
        if (didFish || typeof window.drawFishSprite !== "function") return;
        didFish = true;
        var orig = window.drawFishSprite;
        window.drawFishSprite = function(a) {
          try {
            var L = state();
            if (a && a.fish) {
              var isKept = L.fid && String(a.fish.fid) === L.fid || L.nick && a.fish.nick === L.nick;
              if (isKept && a.fish.mind && a.fish.mind.stress > 0.55) {
                a.fish._holdStill = 1;
              }
              if (a.fish.nick && stillSp[kindOf(a.fish)]) a.fish._holdStill = 1;
            }
          } catch (e) {
          }
          return orig.apply(this, arguments);
        };
      }
      function watchKept() {
        var st = state();
        if (st.soldKept) return;
        if (!st.fid && !st.nick) return;
        if (sceneName() === "title") return;
        if (!keptFish() && fishList().length) {
          st.soldKept = true;
          var first = st.keeper.n.split(" ")[0];
          var line2 = (st.nick || "The one " + first + " kept") + " went in a fish bag. The street will say so.";
          because(line2);
          gold(line2, true);
          say(line2, "bad");
          egg("latesold", line2);
          try {
            if (window.desk && desk.neighbor) {
              var n = desk.neighbor();
              if (n) n.trust = Math.max(0.08, (n.trust || 0.4) - 0.1);
            }
          } catch (e) {
          }
        }
      }
      function whisper() {
        var st = state();
        if (st.missed && st.missed.length && within(st.missedAt || 0, 22)) return st.missed[0];
        if (lastGold && now() - lastGoldAt < 12) return lastGold;
        if (st.soldKept) {
          return "Where's the one " + st.keeper.n.split(" ")[0] + " kept?";
        }
        return "";
      }
      function line() {
        var st = state();
        if (st.missed && st.missed.length) return st.missed[st.missed.length - 1];
        if (st.keeper) return st.keeper.n + " kept this shop through Year " + st.keeper.leftY + ". " + st.keeper.note;
        return whisper();
      }
      function seedWiki() {
        try {
          var wiki = typeof WIKI === "function" ? WIKI() : window.WIKI;
          if (!wiki || !wiki.push) return;
          if (wiki.some(function(a) {
            return a && a.id === "k_late";
          })) return;
          wiki.push({
            id: "k_late",
            sec: "You and your people",
            t: "You are late",
            tags: "previous keeper rook inherited sign year late keys",
            w: "<p>You hung the sign in Year 1000. Someone kept this shop before you. The street still uses their names. A fish in the tank is theirs. The baker will say you're not them. Sell that fish and the street will ask where it went.</p><p><b>What to do about it:</b> read Life. The first page is their handwriting, not yours. Do not bag the one they named unless you mean it.</p>"
          });
          wiki.push({
            id: "k_away",
            sec: "The shop floor",
            t: "While you were gone",
            tags: "away hidden tab idle missed baker filter tide daybook",
            w: "<p>Idle games pay you for looking away. Fin's writes what you missed. The baker came, or she walked. The filter clogged. The harbor came in. A named fish held still. A window on Salem went dark. The gold line is the first thing that happened without you. Life has the rest.</p><p><b>What to do about it:</b> read the line. Mop. Keep the pair. You were not at the counter. The shop was.</p>"
          });
        } catch (e) {
        }
      }
      function tick() {
        try {
          bindAway();
          wrapBrowse();
          wrapFish();
          seedWiki();
          if (sceneName() === "title") return;
          seedOpen();
          markKept();
          if (now() - lastTick > 0.8) {
            lastTick = now();
            watchKept();
            refreshStill();
          }
          if (now() - lastUi > 1.2) lastUi = now();
        } catch (e) {
        }
      }
      window.late = {
        whisper,
        line,
        keeper: function() {
          return state().keeper;
        },
        missed: function() {
          return state().missed || [];
        },
        kept: keptFish
      };
      if (window.__onBeat) window.__onBeat(tick, 260);
      else
        setTimeout(function loop() {
          tick();
          setTimeout(loop, 260);
        }, 200);
    })();
  } catch (e) {
    console.error("Layer late.js failed to start:", e);
  }

  // src/layers/pane.js
  try {
    (function() {
      "use strict";
      var lastTick = 0;
      var lastUi = 0;
      var lastGold = "";
      var lastGoldAt = 0;
      var lastMopSaid = 0;
      var overlay = null;
      var octx = null;
      var scratches = [];
      var didStock = false;
      var didSite = false;
      var didChoir = false;
      var didBrowse = false;
      function now() {
        return typeof performance !== "undefined" ? performance.now() / 1e3 : Date.now() / 1e3;
      }
      function within(at, secs) {
        var age2 = now() - at;
        return age2 >= 0 && age2 < secs;
      }
      function gs() {
        try {
          if (typeof gameState === "function") return gameState();
          if (typeof gameState === "object" && gameState) return gameState;
        } catch (e) {
        }
        return null;
      }
      function year() {
        try {
          if (window.saga && typeof saga.year === "function") return saga.year();
        } catch (e) {
        }
        return 1e3;
      }
      function sceneName() {
        try {
          if (document.body.classList.contains("titling")) return "title";
          if (document.body.classList.contains("work")) return "work";
          if (typeof sceneNow === "function") return String(sceneNow() || "");
        } catch (e) {
        }
        return "tank";
      }
      function fishList() {
        try {
          if (typeof allFish === "function") return allFish() || [];
        } catch (e) {
        }
        return [];
      }
      function clamp01(n) {
        return n < 0 ? 0 : n > 1 ? 1 : n;
      }
      function hash32(s) {
        var h = 2166136261;
        s = String(s || "");
        for (var i = 0; i < s.length; i++) {
          h ^= s.charCodeAt(i);
          h = Math.imul(h, 16777619);
        }
        return h >>> 0;
      }
      function because(text) {
        if (!text) return;
        try {
          if (window.weave && weave.because) weave.because(text);
        } catch (e) {
        }
        try {
          if (window.desk && desk.think) desk.think("pane", text);
        } catch (e2) {
        }
      }
      function say(msg, kind) {
        try {
          if (typeof k === "function") k(msg, kind || "");
        } catch (e) {
        }
      }
      function egg(id, line2) {
        try {
          if (typeof findEgg === "function") findEgg(id, line2);
        } catch (e) {
        }
      }
      function gold(text, force) {
        if (!text) return;
        lastGold = text;
        lastGoldAt = now();
        try {
          var el = document.getElementById("hookWhisper");
          if (el && (force || el.textContent !== text)) {
            el.textContent = text;
            el.classList.add("on", "pop");
          }
        } catch (e) {
        }
      }
      function kindOf(f) {
        if (!f) return "fish";
        try {
          var S = typeof O !== "undefined" ? O : typeof SPECIES !== "undefined" ? SPECIES : null;
          if (S && f.sp != null && S[f.sp]) {
            var nm = S[f.sp].gname || S[f.sp].name || S[f.sp].vname;
            if (nm) return String(nm).toLowerCase();
          }
        } catch (e) {
        }
        var sp = String(f && f.sp || "");
        if (/gold/.test(sp)) return "goldfish";
        if (/betta/.test(sp)) return "betta";
        if (/guppy/.test(sp)) return "guppy";
        if (/tetra/.test(sp)) return "tetra";
        if (/angel/.test(sp)) return "angelfish";
        if (/cichlid/.test(sp)) return "cichlid";
        if (!sp || /^\d+$/.test(sp)) return "fish";
        return sp.toLowerCase();
      }
      function tide() {
        try {
          if (window.going && going.tide) return going.tide() || 0;
        } catch (e) {
        }
        return 0;
      }
      function wet() {
        try {
          if (window.shopSite && shopSite.wet) return shopSite.wet() || 0;
        } catch (e) {
        }
        return 0;
      }
      function clogged2() {
        try {
          return !!window.clogged;
        } catch (e) {
        }
        return false;
      }
      function hour() {
        try {
          if (typeof jt === "function") return jt() * 24;
          var g = gs();
          if (g && g.clock === "real") {
            var d = /* @__PURE__ */ new Date();
            return d.getHours() + d.getMinutes() / 60;
          }
          if (g && isFinite(g.t)) return (g.t / 1200 % 1 + 1) % 1 * 24;
        } catch (e) {
        }
        return 12;
      }
      function night() {
        var h = hour();
        return h < 6.2 || h >= 19.2;
      }
      function rainAmt() {
        try {
          if (window.wxState && isFinite(wxState.rain)) return wxState.rain;
          if (typeof wxWord === "function" && /rain|storm|sleet/i.test(String(wxWord() || ""))) return 0.55;
        } catch (e) {
        }
        return 0;
      }
      function seedEtch() {
        var left = 997;
        try {
          if (window.late && late.keeper) {
            var k2 = late.keeper();
            if (k2 && isFinite(k2.leftY)) left = k2.leftY;
          }
        } catch (e) {
        }
        var age2 = Math.max(0, year() - left);
        return clamp01(0.1 + age2 * 0.05 + hash32("etch:" + year()) % 17 / 120);
      }
      function state() {
        var g = gs();
        if (g && g.pane && isFinite(g.pane.etch)) return g.pane;
        var st = {
          etch: seedEtch(),
          rot: 0.1,
          rust: 0.06,
          seen: "",
          seenSp: "",
          seenAt: 0,
          seenKind: "",
          mopped: 0
        };
        try {
          if (g) g.pane = st;
        } catch (e) {
        }
        return st;
      }
      function face() {
        var list = fishList();
        var named = null;
        var still = null;
        var risen = null;
        var sick = null;
        var n = 0;
        for (var i = 0; i < list.length; i++) {
          var f = list[i];
          if (!f) continue;
          n++;
          if (f.nick && !named) named = f;
          if (f.risen || f._risen) risen = f;
          if (f.sick || f.ill || f.mind && f.mind.stress > 0.62 || f._holdStill) {
            if (!still) still = f;
          }
          if (f.sick || f.ill || f.cond) sick = f;
        }
        return { named, still, risen, sick, n };
      }
      function clarity() {
        var st = state();
        var rain = rainAmt();
        var nite = night() ? 0.22 : 0;
        return clamp01(1 - st.etch * 0.85 - rain * 0.28 - nite);
      }
      function applyCells() {
        var st = state();
        try {
          if (!window.shopSite || !shopSite.of) return;
          var s = shopSite.of();
          if (!s || !s.cells) return;
          for (var i = 0; i < s.cells.length; i++) {
            var c = s.cells[i];
            if (c.kind === "window") c.etch = st.etch;
            if (c.kind === "floor" && c.mat === "oak") {
              c.rot = Math.max(c.rot || 0, st.rot * (0.4 + (c.wet || 0)));
              if (c.rot > 0.28) {
                c.wet = Math.min(1, (c.wet || 0) + c.rot * 0.012);
              }
            }
            if (c.kind === "filter") {
              c.rust = st.rust;
              if (st.rust > 0.35) {
                c.press = Math.min(2, (c.press || 0) + st.rust * 0.02);
                c.water = Math.min(1, (c.water || 0) + st.rust * 0.012);
              }
            }
            if (c.kind === "counter" && st.rot > 0.4) c.rot = st.rot * 0.4;
          }
          if (st.rot > 0.42 && s.wetMax < st.rot * 0.5) {
            s.wetMax = Math.max(s.wetMax || 0, st.rot * 0.38);
          }
        } catch (e) {
        }
      }
      function wearTick() {
        var st = state();
        var t = tide();
        var w = wet();
        var clog = clogged2();
        if (t > 0.25) st.etch = clamp01(st.etch + 4e-5 + t * 6e-5);
        if (w > 0.28) st.rot = clamp01(st.rot + 5e-5 + w * 7e-5);
        if (clog) st.rust = clamp01(st.rust + 8e-5);
        if (w < 0.16) {
          st.rot = clamp01(st.rot - 15e-4);
          if (t < 0.2) st.etch = clamp01(st.etch - 7e-4);
        }
        if (!clog) st.rust = clamp01(st.rust - 12e-4);
        applyCells();
        if (st.etch > 0.52 && !st._etchSaid) {
          st._etchSaid = 1;
          var line2 = "The glass is etching. They can't see in.";
          because(line2);
          gold(line2, true);
          egg("etch", line2);
        }
        if (st.rot > 0.55 && !st._rotSaid) {
          st._rotSaid = 1;
          var rline = "The oak is going. The wet stays.";
          because(rline);
          gold(rline, true);
          egg("oakrot", rline);
        }
        if (st.rust > 0.55 && !st._rustSaid) {
          st._rustSaid = 1;
          var uline = "The housing is rusting. The filter clogs faster.";
          because(uline);
          gold(uline, true);
        }
        if (st.etch < 0.4) st._etchSaid = 0;
        if (st.rot < 0.4) st._rotSaid = 0;
        if (st.rust < 0.4) st._rustSaid = 0;
      }
      function hexMix(a, b, t) {
        function h(s) {
          return [parseInt(s.slice(1, 3), 16), parseInt(s.slice(3, 5), 16), parseInt(s.slice(5, 7), 16)];
        }
        var A = h(a), B = h(b);
        var r = A[0] + (B[0] - A[0]) * t | 0;
        var g = A[1] + (B[1] - A[1]) * t | 0;
        var bl = A[2] + (B[2] - A[2]) * t | 0;
        return "rgb(" + r + "," + g + "," + bl + ")";
      }
      function paintWornGrid() {
        try {
          if (!window.shopSite || !shopSite.of) return "";
          var s = shopSite.of();
          var st = state();
          var html = '<div class="site-grid" aria-label="The shop as a site">';
          for (var i = 0; i < s.cells.length; i++) {
            var c = s.cells[i];
            var col = "#6b5340";
            if (c.kind === "wall") col = "#2a3340";
            else if (c.kind === "tank" || c.kind === "island") col = "#1a6a88";
            else if (c.kind === "counter") col = "#8a6a44";
            else if (c.kind === "filter") col = hexMix("#4a5a4a", "#8a3a18", st.rust);
            else if (c.kind === "door") col = "#c4a06a";
            else if (c.kind === "window") col = hexMix("#7ec8e8", "#cdd6cc", st.etch);
            else if (c.kind === "floor") {
              col = "#6b5340";
              if (c.wet > 0.15) col = "#3d5a6a";
              if (c.wet > 0.45) col = "#2a4a62";
              if (st.rot > 0.18) col = hexMix(col.length === 7 ? col : "#3d5a6a", "#3a2414", st.rot);
            }
            var tip = (c.mat || "") + " " + (c.temp || 0).toFixed(1) + "°";
            if (c.kind === "window" && st.etch > 0.2) tip += " · etching";
            if (c.kind === "floor" && st.rot > 0.2) tip += " · oak going";
            if (c.kind === "filter" && st.rust > 0.2) tip += " · rust";
            html += '<i style="background:' + col + '" title="' + tip + '"></i>';
          }
          html += "</div>";
          return html;
        } catch (e) {
          return "";
        }
      }
      function wrapStock() {
        if (didStock || !window.stock || !stock.use) return;
        didStock = true;
        var orig = stock.use;
        stock.use = function(id) {
          var r = orig.apply(this, arguments);
          try {
            if (id === "glass") {
              var st = state();
              var before = st.etch;
              st.etch = clamp01(st.etch * 0.22);
              st._etchSaid = 0;
              applyCells();
              var line2 = before > 0.22 ? "Vinegar and newsprint. They can see in." : "The storefront is a mirror.";
              because(line2);
              gold(line2, true);
              egg("wash", line2);
            }
          } catch (e) {
          }
          return r;
        };
      }
      function wrapSite() {
        if (didSite || !window.shopSite || !shopSite.grid) return;
        didSite = true;
        var origGrid = shopSite.grid;
        shopSite.grid = function() {
          var worn = paintWornGrid();
          return worn || origGrid.apply(this, arguments);
        };
        var origLine = shopSite.line;
        shopSite.line = function() {
          var s = origLine.apply(this, arguments);
          var st = state();
          var bits = [];
          if (st.etch > 0.28) bits.push("the glass is etching");
          if (st.rot > 0.28) bits.push("the oak is going");
          if (st.rust > 0.28) bits.push("the housing is rusting");
          return bits.length ? s + " · " + bits.join(" · ") : s;
        };
        var origDry = shopSite.dry;
        shopSite.dry = function(amt) {
          origDry.apply(this, arguments);
          var st = state();
          st.rot = clamp01(st.rot - (amt == null ? 0.4 : amt) * 0.35);
          st.mopped = (st.mopped || 0) + 1;
          applyCells();
          if (st.etch > 0.32 && now() - lastMopSaid > 18) {
            lastMopSaid = now();
            var line2 = "The aisle is drier. It does not clean the glass.";
            because(line2);
            gold(line2, true);
            say(line2, "");
          }
        };
      }
      function wrapChoir() {
        if (didChoir || !window.choir || !choir.weather) return;
        didChoir = true;
        var orig = choir.weather;
        choir.weather = function() {
          var w = orig.apply(this, arguments);
          try {
            var st = state();
            var cl = clarity();
            w.etch = st.etch;
            w.rot = st.rot;
            w.rust = st.rust;
            w.clear = cl;
            if (st.etch > 0.34) w.sour = Math.min(1, (w.sour || 0) + st.etch * 0.22);
            if (st.rot > 0.4) w.sour = Math.min(1, (w.sour || 0) + 0.08);
            if (!w.line) {
              if (st.etch > 0.5) w.line = "The glass is etching. They can't see in.";
              else if (st.rot > 0.52) w.line = "The oak is going. The wet stays.";
              else if (cl < 0.45) w.line = "Rain on the glass. They look from the door.";
            }
          } catch (e) {
          }
          return w;
        };
      }
      function wrapBrowse() {
        if (didBrowse || !window.shopBrowse) return;
        didBrowse = true;
        var orig = window.shopBrowse;
        window.shopBrowse = function(idx, slot, W, floorY, personS, simT) {
          var rec = orig.apply(this, arguments);
          try {
            if (!rec) return rec;
            var st = window.shopLife && shopLife.browse ? shopLife.browse()[idx] : null;
            var P = state();
            var f = face();
            var cl = clarity();
            var seenNick = f.named && f.named.nick ? f.named.nick : "";
            var seenSp = f.named ? kindOf(f.named) : f.n ? "fish" : "";
            if (rec.phase === "look" && st && !st._paneSaid) {
              st._paneSaid = 1;
              var had = rec.line;
              var keep = typeof keepGuest === "function" ? keepGuest(st) : !!(st && (st._lateMae || st._goingHold || st._lateKid || st._mask));
              if (f.risen && P.etch < 0.55 && !keep) {
                rec.line = "What's wrong with its eyes. From the street.";
                st.line = rec.line;
                st.lineUntil = now() + 3.4;
                rec.phase = "leave";
                st.phase = "leave";
                st.bought = false;
                P.seen = seenNick || "the risen one";
                P.seenKind = "risen";
                P.seenAt = now();
                because(rec.line);
                gold("They saw it from the street. They walked.", true);
              } else if (f.still && cl > 0.38 && !keep) {
                rec.line = rec.kind === "kid" ? "Is that one dead? I saw it from outside." : "That one isn't moving. From outside.";
                st.line = rec.line;
                st.lineUntil = now() + 3.6;
                P.seen = f.still.nick || "" || "a still one";
                P.seenSp = kindOf(f.still);
                P.seenKind = "still";
                P.seenAt = now();
                if (Math.random() < 0.4) {
                  rec.phase = "leave";
                  st.phase = "leave";
                  st.bought = false;
                }
              } else if ((P.etch > 0.48 || cl < 0.36) && !keep) {
                rec.line = rec.kind === "kid" ? "I can't see the fish." : "Can't see a thing in the window.";
                st.line = rec.line;
                st.lineUntil = now() + 3.2;
                P.seenKind = "etch";
                P.seenAt = now();
                if (P.etch > 0.58 && rec.kind !== "neighbor" && Math.random() < 0.45) {
                  rec.phase = "leave";
                  st.phase = "leave";
                  st.bought = false;
                }
              } else if (!had && seenNick && cl > 0.5 && rec.kind !== "neighbor") {
                rec.line = "I saw " + seenNick + " from the street.";
                st.line = rec.line;
                st.lineUntil = now() + 4;
                st.until = Math.max(st.until || 0, now() + 5.2);
                P.seen = seenNick;
                P.seenSp = seenSp;
                P.seenKind = "named";
                P.seenAt = now();
                because(rec.line);
              } else if (!had && f.n && cl > 0.55 && rec.kind === "lunch") {
                rec.line = rec.line || "That one watched me. From the glass on the street.";
                st.line = rec.line;
                P.seen = seenNick || "the window";
                P.seenKind = "watch";
                P.seenAt = now();
              }
            }
            if (rec.phase === "pay" && st && !st._panePay) {
              st._panePay = 1;
              if (P.etch > 0.6 && rec.kind !== "neighbor" && Math.random() < 0.5) {
                rec.phase = "leave";
                st.phase = "leave";
                st.bought = false;
                rec.line = "I couldn't see what you keep.";
                st.line = rec.line;
              }
            }
          } catch (e) {
          }
          return rec;
        };
      }
      function ensureOverlay() {
        if (overlay && overlay.parentNode) return;
        overlay = document.getElementById("paneVeil");
        if (!overlay) {
          overlay = document.createElement("canvas");
          overlay.id = "paneVeil";
          overlay.setAttribute("aria-hidden", "true");
          document.body.appendChild(overlay);
        }
        octx = overlay.getContext("2d");
      }
      function ensureScratches() {
        if (scratches.length) return;
        var h = hash32("scratch:" + year());
        for (var i = 0; i < 18; i++) {
          h = Math.imul(h, 1664525) + 1013904223 >>> 0;
          scratches.push({
            x: h % 1e3 / 1e3,
            y: (h >>> 10) % 1e3 / 1e3 * 0.55,
            w: 0.04 + (h >>> 20) % 40 / 400,
            a: 0.4 + (h >>> 4) % 50 / 100
          });
        }
      }
      function resize() {
        ensureOverlay();
        var dpr = Math.min(2, window.devicePixelRatio || 1);
        var w = window.innerWidth;
        var h = window.innerHeight;
        if (overlay.width !== (w * dpr | 0) || overlay.height !== (h * dpr | 0)) {
          overlay.width = w * dpr | 0;
          overlay.height = h * dpr | 0;
          overlay.style.cssText = "position:fixed;inset:0;width:100%;height:100%;pointer-events:none;z-index:3";
          octx.setTransform(dpr, 0, 0, dpr, 0, 0);
        }
      }
      function drawPane() {
        var sc = sceneName();
        if (sc === "title" || sc === "work" || sc === "street") {
          if (octx && overlay) octx.clearRect(0, 0, overlay.width, overlay.height);
          return;
        }
        resize();
        var w = window.innerWidth;
        var h = window.innerHeight;
        octx.clearRect(0, 0, w, h);
        var st = state();
        ensureScratches();
        if (sc === "shop" || sc === "front" || sc === "tank") {
          if (st.etch > 0.12) {
            var a = st.etch * (sc === "tank" ? 0.16 : 0.28);
            octx.fillStyle = "rgba(210,220,214," + a.toFixed(3) + ")";
            octx.fillRect(0, 0, w, h * (sc === "tank" ? 1 : 0.58));
            octx.save();
            octx.globalAlpha = Math.min(0.45, st.etch * 0.55);
            octx.strokeStyle = "rgba(236,240,234,.55)";
            octx.lineWidth = 1;
            for (var i = 0; i < scratches.length; i++) {
              var s = scratches[i];
              octx.beginPath();
              octx.moveTo(s.x * w, s.y * h);
              octx.lineTo(s.x * w + s.w * w, s.y * h + 6 * s.a);
              octx.stroke();
            }
            octx.restore();
          }
        }
        if (sc === "shop" || sc === "front") {
          if (st.rot > 0.18) {
            octx.save();
            var rx = w * 0.42;
            var ry = h * 0.78;
            var g = octx.createRadialGradient(rx, ry, 8, rx, ry, w * (0.16 + st.rot * 0.12));
            g.addColorStop(0, "rgba(42,22,10," + (st.rot * 0.38).toFixed(3) + ")");
            g.addColorStop(1, "rgba(42,22,10,0)");
            octx.fillStyle = g;
            octx.beginPath();
            octx.ellipse(rx, ry, w * (0.14 + st.rot * 0.1), 18 + st.rot * 16, -0.12, 0, Math.PI * 2);
            octx.fill();
            octx.restore();
          }
          if (st.rust > 0.2) {
            octx.save();
            var ux = w * 0.84;
            var uy = h * 0.18;
            var rg = octx.createRadialGradient(ux, uy, 2, ux, uy, 48 + st.rust * 40);
            rg.addColorStop(0, "rgba(140,52,18," + (0.12 + st.rust * 0.28).toFixed(3) + ")");
            rg.addColorStop(1, "rgba(140,52,18,0)");
            octx.fillStyle = rg;
            octx.fillRect(ux - 60, uy - 40, 120, 90);
            octx.restore();
          }
        }
      }
      function whisper() {
        var st = state();
        if (lastGold && now() - lastGoldAt < 12) return lastGold;
        if (st.seenKind && within(st.seenAt || 0, 10)) {
          if (st.seenKind === "named" && st.seen) return "They saw " + st.seen + " from the street.";
          if (st.seenKind === "still") return "That one isn't moving. They saw it from outside.";
          if (st.seenKind === "risen") return "They saw it from the street. They walked.";
          if (st.seenKind === "etch") return "Can't see a thing in the window.";
        }
        if (st.etch > 0.52) return "The glass is etching. They can't see in.";
        if (st.rot > 0.55) return "The oak is going. The wet stays.";
        return "";
      }
      function line() {
        var st = state();
        var bits = [];
        bits.push("etch " + Math.round(st.etch * 100) + "%");
        if (st.rot > 0.18) bits.push("oak going");
        if (st.rust > 0.18) bits.push("housing rusting");
        var cl = clarity();
        if (cl < 0.5) bits.push("they can't see in");
        else bits.push("they can see in");
        if (st.seen && within(st.seenAt || 0, 40)) bits.push("they saw " + st.seen);
        return bits.join(" · ");
      }
      function seedWiki() {
        try {
          var wiki = typeof WIKI === "function" ? WIKI() : window.WIKI;
          if (!wiki || !wiki.push) return;
          if (wiki.some(function(a) {
            return a && a.id === "k_pane";
          })) return;
          wiki.push({
            id: "k_pane",
            sec: "The shop floor",
            t: "The glass is two-way",
            tags: "window shopfront street etch see glass rain night risen still",
            w: "<p>The shop window is a shopfront. People on Salem see the tank. Rain, night, etch, a risen name, a fish holding still: they see that from the street. They come in because of what they saw, or they walk. That is not a backdrop. That is odds.</p><p><b>What to do about it:</b> keep the named ones moving. Do not raise a name into the window. The gold line will say if they saw it from outside.</p>"
          });
          wiki.push({
            id: "k_wear",
            sec: "The shop floor",
            t: "The building remembers water",
            tags: "oak rot rust etch iron filter floss mop tide salt boards",
            w: "<p>The glass etches from the harbor. Oak rots where the puddle sat. Iron rusts at the filter. Rotten boards hold the wet. A rusty housing packs faster. The aisle is drier. It does not clean the glass. Window wash does — vinegar and newsprint, Harbor Supply, a dollar nine. Then they can see in.</p><p><b>What to do about it:</b> mop. Unclog the filter. Wash the glass when the gold line says they can't see in. The little map in Life goes the color of the damage. That is the shop as a body, not a menu.</p>"
          });
        } catch (e) {
        }
      }
      function tick() {
        try {
          wrapSite();
          wrapChoir();
          wrapBrowse();
          wrapStock();
          seedWiki();
          if (sceneName() === "title") return;
          if (now() - lastTick > 0.85) {
            lastTick = now();
            wearTick();
          }
          if (now() - lastUi > 0.08) {
            lastUi = now();
            drawPane();
          }
        } catch (e) {
        }
      }
      window.pane = {
        whisper,
        line,
        etch: function() {
          return state().etch;
        },
        rot: function() {
          return state().rot;
        },
        rust: function() {
          return state().rust;
        },
        clarity,
        seen: function() {
          return state().seen;
        }
      };
      if (window.__onBeat) window.__onBeat(tick, 90);
      else
        setTimeout(function loop() {
          tick();
          setTimeout(loop, 90);
        }, 180);
    })();
  } catch (e) {
    console.error("Layer pane.js failed to start:", e);
  }

  // src/layers/sill.js
  try {
    (function() {
      "use strict";
      var lastTick = 0;
      var lastUi = 0;
      var lastGold = "";
      var lastGoldAt = 0;
      var lastDeathKey = "";
      var overlay = null;
      var octx = null;
      var hits = [];
      var wiredClick = false;
      var hover = -1;
      var didChoir = false;
      var didBrowse = false;
      function now() {
        return typeof performance !== "undefined" ? performance.now() / 1e3 : Date.now() / 1e3;
      }
      function gs() {
        try {
          if (typeof gameState === "function") return gameState();
          if (typeof gameState === "object" && gameState) return gameState;
        } catch (e) {
        }
        return null;
      }
      function sceneName() {
        try {
          if (document.body.classList.contains("titling")) return "title";
          if (document.body.classList.contains("work")) return "work";
          if (typeof sceneNow === "function") return String(sceneNow() || "");
        } catch (e) {
        }
        return "tank";
      }
      function hour() {
        try {
          if (typeof jt === "function") return jt() * 24;
          var g = gs();
          if (g && isFinite(g.t)) return (g.t % 2400 + 2400) % 2400 / 100;
        } catch (e) {
        }
        return 12;
      }
      function night() {
        var h = hour();
        return h < 6.2 || h >= 19.2;
      }
      function hash32(s) {
        var h = 2166136261;
        s = String(s || "");
        for (var i = 0; i < s.length; i++) {
          h ^= s.charCodeAt(i);
          h = Math.imul(h, 16777619);
        }
        return h >>> 0;
      }
      function because(text) {
        if (!text) return;
        try {
          if (window.weave && weave.because) weave.because(text);
        } catch (e) {
        }
        try {
          if (window.desk && desk.think) desk.think("sill", text);
        } catch (e2) {
        }
      }
      function egg(id, line2) {
        try {
          if (typeof findEgg === "function") findEgg(id, line2);
        } catch (e) {
        }
      }
      function play(name) {
        try {
          if (window.feel && feel.play) feel.play(name);
        } catch (e) {
        }
      }
      function gold(text, force) {
        if (!text) return;
        lastGold = text;
        lastGoldAt = now();
        try {
          var el = document.getElementById("hookWhisper");
          if (el && (force || el.textContent !== text)) {
            el.textContent = text;
            el.classList.add("on", "pop");
          }
        } catch (e) {
        }
      }
      function homes() {
        try {
          if (window.going && going.homes) return going.homes() || [];
        } catch (e) {
        }
        return [];
      }
      function fishList() {
        try {
          if (typeof allFish === "function") return allFish() || [];
        } catch (e) {
        }
        return [];
      }
      function places() {
        return window.__townPlaces || [];
      }
      function placeById(id) {
        var ps = places();
        for (var i = 0; i < ps.length; i++) if (ps[i] && ps[i].id === id) return ps[i];
        return null;
      }
      function neIds() {
        var ps = places();
        var ids = [];
        for (var i = 0; i < ps.length; i++) {
          var p = ps[i];
          if (!p || !p.id || p.id === "shop" || p.id === "sea") continue;
          if (p.q && p.q !== "ne" && p.q !== "dt") continue;
          ids.push(p.id);
        }
        if (!ids.length) ids = ["salem", "copps", "prince", "hanover", "prado", "park"];
        return ids;
      }
      function placeOf(home) {
        if (!home) return "salem";
        if (home.place) return home.place;
        if (home.neighbor) home.place = "salem";
        else {
          var ids = neIds();
          home.place = ids[hash32(String(home.who || "") + ":" + String(home.sp || "")) % ids.length];
        }
        return home.place;
      }
      function toView(x, y, w, h) {
        try {
          if (typeof window.townToView === "function") return window.townToView(x, y, w, h);
        } catch (e) {
        }
        var cam = window.townCam || { left: 482, top: 472, span: 96 };
        return [(x - cam.left) / cam.span * w, (y - cam.top) / cam.span * h];
      }
      function shopXY() {
        var p = placeById("shop");
        if (p && isFinite(p.x)) return [p.x, p.y];
        return [528, 531];
      }
      function kindColor(sp) {
        var s = String(sp || "");
        if (/gold/.test(s)) return "#e0a030";
        if (/betta/.test(s)) return "#c45a7a";
        if (/guppy/.test(s)) return "#6ec4a8";
        if (/tetra/.test(s)) return "#5aa0d4";
        if (/angel/.test(s)) return "#c8c0e8";
        if (/cichlid/.test(s)) return "#d4783c";
        return "#7eb8d4";
      }
      function paneEtch() {
        try {
          if (window.pane && pane.etch) return pane.etch() || 0;
        } catch (e) {
        }
        return 0;
      }
      function ensureOverlay() {
        if (overlay && overlay.parentNode) return;
        overlay = document.getElementById("sillVeil");
        if (!overlay) {
          overlay = document.createElement("canvas");
          overlay.id = "sillVeil";
          overlay.setAttribute("aria-hidden", "true");
          document.body.appendChild(overlay);
        }
        octx = overlay.getContext("2d");
      }
      function resize() {
        ensureOverlay();
        var dpr = Math.min(2, window.devicePixelRatio || 1);
        var w = window.innerWidth;
        var h = window.innerHeight;
        if (overlay.width !== (w * dpr | 0) || overlay.height !== (h * dpr | 0)) {
          overlay.width = w * dpr | 0;
          overlay.height = h * dpr | 0;
          overlay.style.cssText = "position:fixed;inset:0;width:100%;height:100%;pointer-events:none;z-index:4";
          octx.setTransform(dpr, 0, 0, dpr, 0, 0);
        }
      }
      function windowRect(home, index, w, h) {
        var s = shopXY();
        var v = toView(s[0], s[1], w, h);
        var ang = -2.35 + index * 0.62;
        var rad = Math.max(78, w * 0.085);
        var bw = Math.max(30, w * 0.028);
        var bh = Math.max(38, w * 0.036);
        var wx = v[0] + Math.cos(ang) * rad;
        var wy = v[1] + Math.sin(ang) * rad * 0.72;
        return {
          x: wx - bw * 0.5,
          y: wy - bh,
          w: bw,
          h: bh,
          home,
          place: placeOf(home)
        };
      }
      function shopWindowRect(w, h) {
        var s = shopXY();
        var v = toView(s[0], s[1], w, h);
        var bw = Math.max(34, w * 0.032);
        var bh = Math.max(42, w * 0.04);
        return { x: v[0] + 16, y: v[1] - bh - 10, w: bw, h: bh, shop: true };
      }
      function drawTinyFish(ctx, rx, t, col, dead) {
        if (dead) return;
        var cx = rx.x + rx.w * (0.3 + 0.4 * (0.5 + 0.5 * Math.sin(t * 1.4 + rx.x)));
        var cy = rx.y + rx.h * (0.45 + 0.18 * Math.sin(t * 2.1 + rx.y));
        var dir = Math.cos(t * 1.4 + rx.x) >= 0 ? 1 : -1;
        ctx.fillStyle = col;
        ctx.beginPath();
        ctx.ellipse(cx, cy, Math.max(2.2, rx.w * 0.18), Math.max(1.1, rx.h * 0.1), 0, 0, 7);
        ctx.fill();
        ctx.beginPath();
        ctx.moveTo(cx - dir * rx.w * 0.18, cy);
        ctx.lineTo(cx - dir * rx.w * 0.32, cy - rx.h * 0.08);
        ctx.lineTo(cx - dir * rx.w * 0.32, cy + rx.h * 0.08);
        ctx.closePath();
        ctx.fill();
      }
      function drawOne(ctx, rx, t, nite, hi) {
        var home = rx.home;
        var dead = !!(home && home.dead);
        var live = home && !dead;
        ctx.save();
        ctx.fillStyle = "rgba(18,12,8,.72)";
        ctx.fillRect(rx.x - 2, rx.y - 3, rx.w + 4, rx.h + 6);
        if (dead) {
          ctx.fillStyle = "rgba(6,8,12,.92)";
          ctx.fillRect(rx.x, rx.y, rx.w, rx.h);
          ctx.fillStyle = "rgba(20,16,14,.8)";
          ctx.fillRect(rx.x + 1, rx.y + rx.h * 0.35, rx.w - 2, rx.h * 0.65);
        } else {
          var glow = nite ? 0.42 : 0.22;
          if (rx.shop) glow += 0.12;
          ctx.fillStyle = "rgba(244,210,120," + glow.toFixed(3) + ")";
          ctx.fillRect(rx.x, rx.y, rx.w, rx.h);
          ctx.fillStyle = "rgba(40,90,110,0.55)";
          ctx.fillRect(rx.x + 1, rx.y + 2, rx.w - 2, rx.h - 4);
          if (live) drawTinyFish(ctx, rx, t, kindColor(home.sp), false);
          if (rx.shop) {
            var n = fishList().length;
            for (var k2 = 0; k2 < Math.min(3, n); k2++) {
              var fake = { x: rx.x + k2 * 3, y: rx.y, w: rx.w, h: rx.h };
              drawTinyFish(ctx, fake, t + k2 * 0.7, "#8fd0e8", false);
            }
            var etch = paneEtch();
            if (etch > 0.25) {
              ctx.fillStyle = "rgba(210,220,214," + (etch * 0.45).toFixed(3) + ")";
              ctx.fillRect(rx.x, rx.y, rx.w, rx.h);
            }
          }
        }
        ctx.strokeStyle = hi ? "rgba(244,196,83,.95)" : "rgba(244,214,160,.55)";
        ctx.lineWidth = hi ? 2.5 : 1.4;
        ctx.strokeRect(rx.x, rx.y, rx.w, rx.h);
        if (home && home.named && !dead) {
          ctx.strokeStyle = "rgba(244,196,83,.85)";
          ctx.lineWidth = 2;
          ctx.strokeRect(rx.x - 1.5, rx.y - 1.5, rx.w + 3, rx.h + 3);
        }
        var cap = "";
        if (rx.shop) cap = "Fin's";
        else if (home && home.dead) cap = "dark";
        else if (home && home.neighbor) cap = (home.who || "Mae").split(" ")[0];
        else if (home && home.nick) cap = home.nick;
        else if (home) cap = home.sp || "window";
        if (cap) {
          ctx.font = "700 " + Math.max(10, rx.w * 0.36 | 0) + "px Nunito, sans-serif";
          ctx.textAlign = "center";
          ctx.fillStyle = "rgba(8,12,18,.78)";
          var cw = ctx.measureText(cap).width + 8;
          ctx.fillRect(rx.x + rx.w * 0.5 - cw / 2, rx.y + rx.h + 2, cw, 13);
          ctx.fillStyle = dead ? "rgba(200,210,220,.7)" : "#f4c453";
          ctx.fillText(cap, rx.x + rx.w * 0.5, rx.y + rx.h + 12);
        }
        ctx.restore();
      }
      function labelOf(rx) {
        if (rx.shop) {
          var n = fishList().length;
          var etch = paneEtch();
          if (etch > 0.5) return "Fin's window. They can't see in.";
          if (!n) return "Fin's window. The tanks are empty from the street.";
          return "Fin's window. " + n + (n === 1 ? " fish" : " fish") + " from the street.";
        }
        var h = rx.home;
        if (!h) return "";
        var call = h.nick || "The " + (h.sp || "fish");
        if (h.dead) return call + " died in " + (h.who || "someone") + "'s window. It went dark.";
        return call + " is still in " + (h.who || "someone") + "'s window.";
      }
      function drawSill() {
        var sc = sceneName();
        if (sc !== "street") {
          hits = [];
          if (octx && overlay) octx.clearRect(0, 0, overlay.width, overlay.height);
          return;
        }
        resize();
        var w = window.innerWidth;
        var h = window.innerHeight;
        octx.clearRect(0, 0, w, h);
        var t = now();
        var nite = night();
        var hs = homes();
        hits = [];
        var shopR = shopWindowRect(w, h);
        hits.push(shopR);
        drawOne(octx, shopR, t, nite, hover === 0);
        for (var i = 0; i < hs.length; i++) {
          var home = hs[i];
          if (!home) continue;
          var rx = windowRect(home, i, w, h);
          hits.push(rx);
          drawOne(octx, rx, t, nite, hover === hits.length - 1);
        }
        if (hover >= 0 && hits[hover]) {
          var lab = labelOf(hits[hover]);
          if (lab) {
            var hx = hits[hover].x + hits[hover].w * 0.5;
            var hy = hits[hover].y - 8;
            octx.save();
            octx.font = "600 12px Nunito, sans-serif";
            octx.textAlign = "center";
            var tw = octx.measureText(lab).width + 14;
            octx.fillStyle = "rgba(8,12,18,.78)";
            octx.fillRect(hx - tw / 2, hy - 16, tw, 18);
            octx.fillStyle = "#f4c453";
            octx.fillText(lab, hx, hy - 3);
            octx.restore();
          }
        }
      }
      function hitAt(cx, cy) {
        for (var i = hits.length - 1; i >= 0; i--) {
          var r = hits[i];
          if (cx >= r.x - 2 && cx <= r.x + r.w + 2 && cy >= r.y - 2 && cy <= r.y + r.h + 2) return i;
        }
        return -1;
      }
      function bindClick() {
        if (wiredClick) return;
        wiredClick = true;
        window.addEventListener(
          "click",
          function(e) {
            if (sceneName() !== "street") return;
            var i = hitAt(e.clientX, e.clientY);
            if (i < 0) return;
            var line2 = labelOf(hits[i]);
            if (!line2) return;
            gold(line2, true);
            because(line2);
            play("wood");
            egg("sillwin", line2);
          },
          true
        );
        window.addEventListener(
          "mousemove",
          function(e) {
            if (sceneName() !== "street") {
              hover = -1;
              return;
            }
            hover = hitAt(e.clientX, e.clientY);
          },
          true
        );
      }
      function watchDeaths() {
        var hs = homes();
        for (var i = 0; i < hs.length; i++) {
          var h = hs[i];
          if (!h || !h.dead) continue;
          var key = (h.nick || h.sp || i) + ":" + (h.who || "") + ":dead";
          if (key === lastDeathKey) continue;
          if (h._sillTold) continue;
          h._sillTold = 1;
          lastDeathKey = key;
          var line2 = (h.nick || "The " + h.sp) + " died in " + h.who + "'s window. It went dark.";
          because(line2);
          gold(line2, true);
          play("drip");
          egg("silldark", line2);
        }
      }
      function wrapChoir() {
        if (didChoir || !window.choir || !choir.weather) return;
        didChoir = true;
        var orig = choir.weather;
        choir.weather = function() {
          var w = orig.apply(this, arguments);
          try {
            var hs = homes();
            var live = 0;
            var dark = 0;
            for (var i = 0; i < hs.length; i++) {
              if (!hs[i]) continue;
              if (hs[i].dead) dark++;
              else live++;
            }
            w.windows = live;
            w.dark = dark;
            if (live) w.sweet = Math.min(1, (w.sweet || 0) + Math.min(0.16, live * 0.04));
            if (dark && now() - lastGoldAt < 20) w.sour = Math.min(1, (w.sour || 0) + 0.1);
            if (!w.line && dark && now() - lastGoldAt < 16) {
              w.line = lastGold || "A window on Salem went dark.";
            }
          } catch (e) {
          }
          return w;
        };
      }
      function wrapBrowse() {
        if (didBrowse || !window.shopBrowse) return;
        didBrowse = true;
        var orig = window.shopBrowse;
        window.shopBrowse = function(idx, slot, W, floorY, personS, simT) {
          var rec = orig.apply(this, arguments);
          try {
            if (!rec) return rec;
            var st = window.shopLife && shopLife.browse ? shopLife.browse()[idx] : null;
            if (!st || st._sillSaid) return rec;
            if (rec.phase !== "look") return rec;
            var hs = homes();
            var hit = null;
            for (var i = 0; i < hs.length; i++) {
              var h = hs[i];
              if (!h) continue;
              if (h.whoKind === rec.kind || h.neighbor && rec.kind === "neighbor") {
                hit = h;
                if (h.neighbor) break;
              }
            }
            if (hit && hit.dead) {
              st._sillSaid = 1;
              rec.line = rec.line || "The window went dark. " + (hit.nick || "The " + hit.sp) + ".";
              st.line = rec.line;
            } else if (hit && !hit.dead && rec.kind === "kid") {
              st._sillSaid = 1;
              rec.line = rec.line || "I can see " + (hit.nick || "yours") + " from the street. In their window.";
              st.line = rec.line;
            }
          } catch (e) {
          }
          return rec;
        };
      }
      function whisper() {
        if (lastGold && now() - lastGoldAt < 14) return lastGold;
        return "";
      }
      function line() {
        var hs = homes();
        var live = 0;
        var dark = 0;
        var first = "";
        for (var i = 0; i < hs.length; i++) {
          if (!hs[i]) continue;
          if (hs[i].dead) dark++;
          else {
            live++;
            if (!first) first = (hs[i].nick || "The " + hs[i].sp) + " in " + hs[i].who + "'s window";
          }
        }
        if (lastGold && now() - lastGoldAt < 40) return lastGold;
        if (first) return first + (live > 1 ? " · " + live + " windows lit" : ".");
        if (dark) return dark + (dark === 1 ? " window" : " windows") + " went dark.";
        return "Open Map. The windows on Salem are tanks, if anyone took one home.";
      }
      function seedWiki() {
        try {
          var wiki = typeof WIKI === "function" ? WIKI() : window.WIKI;
          if (!wiki || !wiki.push) return;
          if (wiki.some(function(a) {
            return a && a.id === "k_sill";
          })) return;
          wiki.push({
            id: "k_sill",
            sec: "The quarter",
            t: "Windows on Salem are tanks",
            tags: "map window salem baker home swim dark click block sold",
            w: "<p>You sold a fish. Open Map. Their window is lit. A tiny one swims in it. Night, they glow. They die, the window goes dark. Click it. The gold line names the house. The baker's window is the first one you learn to look for. The choir already heard them. Now you can see them.</p><p><b>What to do about it:</b> open Map after a fish bag. Do not sell a sick fish to the baker. A dark window is a name in the book from the street, not the glass.</p>"
          });
        } catch (e) {
        }
      }
      function tick() {
        try {
          bindClick();
          wrapChoir();
          wrapBrowse();
          seedWiki();
          if (now() - lastTick > 0.8) {
            lastTick = now();
            watchDeaths();
          }
          if (now() - lastUi > 0.05) {
            lastUi = now();
            drawSill();
          }
        } catch (e) {
        }
      }
      window.sill = {
        whisper,
        line,
        lit: function() {
          var hs = homes();
          var n = 0;
          for (var i = 0; i < hs.length; i++) if (hs[i] && !hs[i].dead) n++;
          return n;
        },
        dark: function() {
          var hs = homes();
          var n = 0;
          for (var i = 0; i < hs.length; i++) if (hs[i] && hs[i].dead) n++;
          return n;
        }
      };
      if (window.__onBeat) window.__onBeat(tick, 70);
      else
        setTimeout(function loop() {
          tick();
          setTimeout(loop, 70);
        }, 160);
    })();
  } catch (e) {
    console.error("Layer sill.js failed to start:", e);
  }

  // src/layers/kin.js
  try {
    (function() {
      "use strict";
      var lastTick = 0;
      var lastUi = 0;
      var lastGold = "";
      var lastGoldAt = 0;
      var lastSales = -1;
      var tillReady = false;
      var didBrowse = false;
      var didChoir = false;
      var seeded = false;
      var GIVEN = ["Tomas", "Rita", "Ev", "Cal", "Nora", "Gio", "Bess", "Ned", "Pia", "Walt", "Inez", "Sal"];
      var SURN = ["Russo", "Chen", "Ward", "DiPietro", "Kelley", "Sousa", "Pell", "Hale", "Quill", "Marr", "Voss", "Costa"];
      var JOBS2 = [
        { job: "off the boats", kind: "lunch", want: "tetra" },
        { job: "keeps a list", kind: "collector", want: "betta" },
        { job: "still in school", kind: "kid", want: "guppy" },
        { job: "hauls at Commercial", kind: "lunch", want: "cory" },
        { job: "teaches at Copp's", kind: "neighbor", want: "angelfish" },
        { job: "mends nets", kind: "lunch", want: "clown" },
        { job: "keeps the hall", kind: "collector", want: "goldfish" },
        { job: "cuts fish at Haymarket", kind: "lunch", want: "tetra" }
      ];
      function now() {
        return typeof performance !== "undefined" ? performance.now() / 1e3 : Date.now() / 1e3;
      }
      function gs() {
        try {
          if (typeof gameState === "function") return gameState();
          if (typeof gameState === "object" && gameState) return gameState;
        } catch (e) {
        }
        return null;
      }
      function year() {
        try {
          if (window.saga && typeof saga.year === "function") return saga.year();
        } catch (e) {
        }
        return 1e3;
      }
      function shopDay() {
        try {
          var g = gs();
          if (g && isFinite(g.t)) return Math.floor((g.t % 1e9 + 1e9) % 1e9 / 2400);
        } catch (e) {
        }
        return 0;
      }
      function sceneName() {
        try {
          if (document.body.classList.contains("titling")) return "title";
          if (typeof sceneNow === "function") return String(sceneNow() || "");
        } catch (e) {
        }
        return "shop";
      }
      function hash32(s) {
        var h = 2166136261;
        s = String(s || "");
        for (var i = 0; i < s.length; i++) {
          h ^= s.charCodeAt(i);
          h = Math.imul(h, 16777619);
        }
        return h >>> 0;
      }
      function pick(arr, h) {
        if (!arr || !arr.length) return arr;
        return arr[(h >>> 0) % arr.length];
      }
      function runSeed() {
        try {
          if (typeof runState === "function") {
            var r = runState();
            if (r && r.seed) return r.seed;
          }
        } catch (e) {
        }
        return 1;
      }
      function because(text) {
        if (!text) return;
        try {
          if (window.weave && weave.because) weave.because(text);
        } catch (e) {
        }
        try {
          if (window.desk && desk.think) desk.think("kin", text);
        } catch (e2) {
        }
      }
      function say(msg, kind) {
        try {
          if (typeof k === "function") k(msg, kind || "");
        } catch (e) {
        }
      }
      function egg(id, line2) {
        try {
          if (typeof findEgg === "function") findEgg(id, line2);
        } catch (e) {
        }
      }
      function gold(text, force) {
        if (!text) return;
        lastGold = text;
        lastGoldAt = now();
        try {
          var el = document.getElementById("hookWhisper");
          if (el && (force || el.textContent !== text)) {
            el.textContent = text;
            el.classList.add("on", "pop");
          }
        } catch (e) {
        }
      }
      function stockHave() {
        try {
          if (window.shopLife && shopLife.stock) return shopLife.stock() || [];
        } catch (e) {
        }
        return [];
      }
      function kindOf(f) {
        if (!f) return "fish";
        try {
          var S = typeof O !== "undefined" ? O : typeof SPECIES !== "undefined" ? SPECIES : null;
          if (S && f.sp != null && S[f.sp]) {
            var nm = S[f.sp].gname || S[f.sp].name || S[f.sp].vname;
            if (nm) return String(nm).toLowerCase();
          }
        } catch (e) {
        }
        var sp = String(f && f.sp || "");
        if (/gold/.test(sp)) return "goldfish";
        if (/betta/.test(sp)) return "betta";
        if (/guppy/.test(sp)) return "guppy";
        if (/tetra/.test(sp)) return "tetra";
        if (/angel/.test(sp)) return "angelfish";
        if (!sp || /^\d+$/.test(sp)) return "fish";
        return sp.toLowerCase();
      }
      function namedOf(want) {
        try {
          var list = typeof allFish === "function" ? allFish() || [] : [];
          for (var i = 0; i < list.length; i++) {
            if (list[i] && list[i].nick && (!want || kindOf(list[i]) === want)) return list[i];
          }
        } catch (e) {
        }
        return null;
      }
      function gods() {
        return [
          "the Salt Mother",
          "the Dry Eye",
          "the Wheel",
          "the First Net",
          "the Quiet Oak",
          "the Red Vein",
          "the Night Ledger"
        ];
      }
      function sgName(i) {
        try {
          if (typeof sgPerson === "function") {
            var p = sgPerson(14e3 + i * 17) || sgPerson(80 + i);
            if (p && p.name && p.name !== "A customer") return p.name;
          }
        } catch (e) {
        }
        return "";
      }
      function keeper() {
        try {
          if (window.late && late.keeper) return late.keeper();
        } catch (e) {
        }
        return null;
      }
      function mae() {
        try {
          if (window.desk && desk.neighbor) return desk.neighbor();
        } catch (e) {
        }
        return null;
      }
      function state() {
        var g = gs();
        if (g && g.kin && Array.isArray(g.kin.folk) && g.kin.folk.length) return g.kin;
        var st = { folk: [], due: "", last: "" };
        try {
          if (g) g.kin = st;
        } catch (e) {
        }
        return st;
      }
      function byId(id) {
        var folk = state().folk;
        for (var i = 0; i < folk.length; i++) if (folk[i] && folk[i].id === id) return folk[i];
        return null;
      }
      function byName(n) {
        var folk = state().folk;
        for (var i = 0; i < folk.length; i++) if (folk[i] && folk[i].n === n) return folk[i];
        return null;
      }
      function living() {
        return state().folk.filter(function(f) {
          return f && !f.dead;
        });
      }
      function seedFolk() {
        var st = state();
        if (st.folk.length) {
          seeded = true;
          return;
        }
        var h = hash32("kin:" + runSeed() + ":" + year());
        var kpr = keeper();
        var baker = mae();
        var folk = [];
        var keepId = "keep";
        if (kpr && kpr.n) {
          folk.push({
            id: keepId,
            n: kpr.n,
            job: "kept this shop",
            kind: "neighbor",
            want: "goldfish",
            god: pick(gods(), h),
            kinOf: "",
            how: "",
            trust: 0.5,
            visits: 0,
            bags: 0,
            last: kpr.note || "left the keys",
            nextD: -1,
            dead: true,
            known: true
          });
        }
        var maeId = "mae";
        folk.push({
          id: maeId,
          n: baker && baker.name || "Mae Costa",
          job: "bakes on Salem",
          kind: "neighbor",
          want: baker && baker.want || "goldfish",
          god: "the Wheel",
          kinOf: kpr && kpr.kin ? keepId : "",
          how: kpr && kpr.kin ? "aunt" : "",
          trust: baker && baker.trust != null ? baker.trust : 0.4,
          visits: baker && baker.visits || 0,
          bags: baker && baker.bags || 0,
          last: baker && baker.last || "",
          nextD: baker && baker.nextD != null ? baker.nextD : 0,
          dead: false,
          known: !!(baker && baker.known)
        });
        var used = /* @__PURE__ */ Object.create(null);
        used[baker && baker.name || "Mae Costa"] = 1;
        if (kpr && kpr.n) used[kpr.n] = 1;
        var parentId = maeId;
        for (var i = 0; i < 7; i++) {
          var hh = hash32("f:" + h + ":" + i);
          var nm = sgName(i);
          if (!nm || used[nm]) {
            nm = pick(GIVEN, hh) + " " + pick(SURN, hh >>> 8);
          }
          if (used[nm]) nm = pick(GIVEN, hh + 3) + " " + pick(SURN, hh >>> 4);
          used[nm] = 1;
          var spec = JOBS2[i % JOBS2.length];
          var how = i === 0 ? "child" : i === 1 ? "cousin" : i === 2 ? "sibling" : "";
          var kinOf2 = "";
          if (i === 0) kinOf2 = parentId;
          else if (i === 1) kinOf2 = keepId;
          else if (i === 2) kinOf2 = "f0";
          folk.push({
            id: "f" + i,
            n: nm,
            job: spec.job,
            kind: spec.kind,
            want: spec.want,
            god: pick(gods(), hh >>> 3),
            kinOf: kinOf2,
            how,
            trust: 0.32 + (hh >>> 12) % 28 / 100,
            visits: 0,
            bags: 0,
            last: "",
            nextD: hh % 4 - 1,
            dead: false,
            known: false
          });
        }
        st.folk = folk;
        seeded = true;
        var kid = byId("f0");
        var bakerN = baker && baker.name || "Mae";
        if (kid) {
          because(kid.n + " is " + bakerN.split(" ")[0] + "'s. The street already knows.");
        }
        egg("kinfolk", folk.length + " people on this block. They have names.");
      }
      function growKids(d) {
        var folk = state().folk;
        for (var i = 0; i < folk.length; i++) {
          var f = folk[i];
          if (!f || f.dead) continue;
          if (f.job === "still small" && d - (f.bornD || 0) >= 3) {
            f.job = "still in school";
            f.kind = "kid";
            f.nextD = d;
            f.last = "old enough to come in";
            because(f.n.split(" ")[0] + " is old enough to come to the glass.");
          }
        }
      }
      function birthChild(d, hh) {
        var live = living().filter(function(f) {
          return f.id !== "keep" && f.job !== "still small";
        });
        if (!live.length || living().length >= 14) return;
        var parent = live[hh % live.length];
        var kid0 = byId("f0");
        if (parent.id === "mae" && kid0 && !kid0.dead) {
          parent = live[(hh + 3) % live.length] || parent;
        }
        var surn = parent.n && parent.n.split(" ")[1] || pick(SURN, hh >>> 8);
        var nm = pick(GIVEN, hh >>> 4) + " " + surn;
        if (byName(nm)) nm = pick(GIVEN, hh >>> 2) + " " + pick(SURN, hh >>> 6);
        state().folk.push({
          id: "b" + d + hh % 97,
          n: nm,
          job: "still small",
          kind: "kid",
          want: parent.want || "guppy",
          god: parent.god || pick(gods(), hh),
          kinOf: parent.id,
          how: "child",
          trust: parent.trust || 0.4,
          visits: 0,
          bags: 0,
          last: "born on the block",
          nextD: d + 3,
          dead: false,
          known: true,
          bornD: d
        });
        because(nm.split(" ")[0] + " was born to " + parent.n.split(" ")[0] + " on this block.");
        egg("kinborn", nm + " was born on Salem.");
      }
      function dieFig(d, hh) {
        var cands = living().filter(function(f2) {
          return f2.id !== "mae" && f2.id !== "keep" && f2.job !== "still small" && (f2.visits || 0) === 0;
        });
        if (!cands.length) return;
        var f = cands[hh % cands.length];
        f.dead = true;
        f.last = "died on the block";
        var k2 = kinOf(f);
        because(f.n + " died. " + (k2 && !k2.dead ? k2.n.split(" ")[0] + " will keep the name." : "The street keeps the name."));
        try {
          if (window.mark && mark.want) mark.want(f.n, "on the street", "kin", year(), false);
        } catch (e) {
        }
        if (k2 && !k2.dead) {
          k2.last = f.n.split(" ")[0] + " died";
          k2.trust = Math.max(0.08, (k2.trust || 0.4) - 0.06);
        }
      }
      function ageStreet() {
        var st = state();
        var d = shopDay();
        if (st.aged === d) return;
        st.aged = d;
        if (d < 2) return;
        var hh = hash32("age:" + runSeed() + ":" + d);
        growKids(d);
        if (hh % 100 < 20) birthChild(d, hh);
        if (d >= 6 && hh % 100 > 90) dieFig(d, hh);
      }
      function syncMae() {
        var baker = mae();
        var fig = byId("mae");
        if (!baker || !fig) return;
        fig.trust = baker.trust;
        fig.bags = baker.bags || 0;
        fig.visits = baker.visits || 0;
        fig.last = baker.last || fig.last;
        fig.known = baker.known;
        fig.want = baker.want || fig.want;
      }
      function kinOf(fig) {
        if (!fig || !fig.kinOf) return null;
        return byId(fig.kinOf);
      }
      function familyTrust(fig) {
        var t = fig.trust || 0.4;
        var k2 = kinOf(fig);
        if (k2 && !k2.dead) t = Math.min(t, (t + (k2.trust || 0.4)) / 2);
        return t;
      }
      function deadHome(fig) {
        try {
          if (!window.going || !going.homes) return null;
          var hs = going.homes();
          for (var i = 0; i < hs.length; i++) {
            if (hs[i] && hs[i].dead && (hs[i].who === fig.n || fig.id === "mae" && hs[i].neighbor)) return hs[i];
          }
        } catch (e) {
        }
        return null;
      }
      function liveHome(fig) {
        try {
          if (!window.going || !going.homes) return null;
          var hs = going.homes();
          for (var i = 0; i < hs.length; i++) {
            if (hs[i] && !hs[i].dead && (hs[i].who === fig.n || fig.id === "mae" && hs[i].neighbor)) return hs[i];
          }
        } catch (e) {
        }
        return null;
      }
      function attach(idx, rec, st) {
        if (st && st._kinId) return byId(st._kinId);
        var fig = null;
        if (rec && rec.kind === "neighbor") fig = byId("mae");
        if (!fig && rec && rec.name) fig = byName(rec.name);
        if (!fig) {
          var live = living();
          if (!live.length) return null;
          fig = live[(idx + shopDay()) % live.length];
          if (fig.id === "mae" && rec && rec.kind !== "neighbor") {
            fig = live[(idx + shopDay() + 1) % live.length] || fig;
          }
        }
        if (st && fig) st._kinId = fig.id;
        return fig;
      }
      function hurt(fig, amt, why) {
        if (!fig || fig.dead) return;
        fig.trust = Math.max(0.04, (fig.trust || 0.4) - amt);
        fig.last = why || "walked";
        var k2 = kinOf(fig);
        if (k2 && !k2.dead) {
          k2.trust = Math.max(0.04, (k2.trust || 0.4) - amt * 0.4);
          k2.last = k2.last || fig.n.split(" ")[0] + " walked";
        }
        if (fig.id === "mae") {
          try {
            var n = mae();
            if (n) {
              n.trust = fig.trust;
              n.last = fig.last;
            }
          } catch (e) {
          }
        }
      }
      function gift(fig, amt, why) {
        if (!fig || fig.dead) return;
        fig.trust = Math.min(1, (fig.trust || 0.4) + amt);
        fig.bags = (fig.bags || 0) + 1;
        fig.visits = (fig.visits || 0) + 1;
        fig.known = true;
        fig.last = why || "took a fish bag";
        fig.nextD = shopDay() + 1 + (hash32(fig.id + shopDay()) % 3 | 0);
        if (fig.id === "mae") {
          try {
            var n = mae();
            if (n) {
              n.trust = fig.trust;
              n.bags = fig.bags;
              n.visits = fig.visits;
              n.known = true;
              n.last = fig.last;
            }
          } catch (e) {
          }
        }
      }
      function wrapBrowse() {
        if (didBrowse || !window.shopBrowse) return;
        didBrowse = true;
        var orig = window.shopBrowse;
        window.shopBrowse = function(idx, slot, W, floorY, personS, simT) {
          var rec = orig.apply(this, arguments);
          try {
            if (!rec) return rec;
            var st = window.shopLife && shopLife.browse ? shopLife.browse()[idx] : null;
            var fig = attach(idx, rec, st);
            if (!fig || fig.dead) return rec;
            rec.name = fig.n;
            rec.kind = rec.kind || fig.kind;
            if (st) {
              st.guestName = fig.n;
              st.kind = rec.kind;
              if (!st.want) st.want = fig.want;
            }
            var keep = typeof keepGuest === "function" ? keepGuest(st) : !!(st && (st._lateMae || st._goingHold || st._lateKid || st._mask));
            var k2 = kinOf(fig);
            var died = deadHome(fig);
            var kept = liveHome(fig);
            var named = namedOf(fig.want);
            var ft = familyTrust(fig);
            if (rec.phase === "look" && st && !st._kinSaid && !keep) {
              st._kinSaid = 1;
              var line2 = "";
              if (died && !died.told) {
                died.told = true;
                line2 = (died.nick || "The " + died.sp) + " died on me. I came for the line.";
                because(fig.n + " came because the one that went home died.");
                gold(line2, true);
              } else if (k2 && k2.dead && !fig._lateKin) {
                fig._lateKin = 1;
                line2 = k2.n.split(" ")[0] + " taught me the names. You're not them.";
              } else if (k2 && !k2.dead && k2.trust < 0.22) {
                line2 = k2.n.split(" ")[0] + " said you sold a sick one. I'm looking.";
                if (Math.random() < 0.45) {
                  rec.phase = "leave";
                  st.phase = "leave";
                  st.bought = false;
                  hurt(fig, 0.04, "walked for " + k2.n.split(" ")[0]);
                }
              } else if (kept && rec.kind !== "kid") {
                line2 = (kept.nick || "The last one") + " is still in my window. Another " + (fig.want || "fish") + ".";
              } else if (named && fig.kind === "collector") {
                line2 = "That's " + named.nick + ". I know the line. Don't round it.";
                st.want = kindOf(named);
                rec.want = st.want;
              } else if (fig.kind === "kid" && named) {
                line2 = "Is " + named.nick + " the little one?";
              } else if (ft < 0.18) {
                line2 = "Just looking. Word's off.";
                rec.phase = "leave";
                st.phase = "leave";
                st.bought = false;
              } else if (!rec.line) {
                line2 = rec.kind === "kid" ? "Can we get a " + (fig.want || "fish") + "?" : fig.known ? "The usual. " + (fig.want || "A fish") + "." : "I'm " + fig.n.split(" ")[0] + ". " + fig.job + ".";
              }
              if (line2) {
                rec.line = line2;
                st.line = line2;
                st.lineUntil = now() + 3.6;
              }
            }
            if (rec.phase === "pay" && st && !st._kinPay) {
              st._kinPay = 1;
              if (ft < 0.16 && rec.kind !== "neighbor") {
                rec.phase = "leave";
                st.phase = "leave";
                st.bought = false;
                rec.line = "Not from this counter.";
                st.line = rec.line;
                hurt(fig, 0.03, "turned at the till");
              } else if (fig.kind === "collector" && !namedOf(st.want || fig.want) && Math.random() < 0.4) {
                rec.phase = "leave";
                st.phase = "leave";
                st.bought = false;
                rec.line = "I wanted a named one. The line.";
                st.line = rec.line;
                hurt(fig, 0.05, "wanted a named " + (fig.want || "fish"));
                because(fig.n + " walked. A unique fish is a display.");
              } else {
                gift(fig, 0.08, "took the " + (st.want || fig.want || "fish"));
                because(fig.n + " took a fish bag. " + (k2 && !k2.dead ? k2.n.split(" ")[0] + " will hear." : "The block will hear."));
              }
            }
            if (rec.phase === "leave" && st && !st.bought && !st._kinMiss && st._kinSaid) {
              st._kinMiss = 1;
              hurt(fig, 0.06, "walked");
              var k22 = kinOf(fig);
              if (k22 && !k22.dead && fig.trust < 0.28) {
                gold(k22.n.split(" ")[0] + " will hear that " + fig.n.split(" ")[0] + " walked.", true);
              }
            }
          } catch (e) {
          }
          return rec;
        };
      }
      function wrapChoir() {
        if (didChoir || !window.choir || !choir.weather) return;
        didChoir = true;
        var orig = choir.weather;
        choir.weather = function() {
          var w = orig.apply(this, arguments);
          try {
            var live = living();
            var low = 0;
            var known = 0;
            for (var i = 0; i < live.length; i++) {
              if (live[i].known) known++;
              if (live[i].trust < 0.22) low++;
            }
            if (known) w.sweet = Math.min(1, (w.sweet || 0) + Math.min(0.12, known * 0.02));
            if (low) w.sour = Math.min(1, (w.sour || 0) + Math.min(0.18, low * 0.05));
            if (!w.line && low >= 2) w.line = "Word on the block is off. A family is talking.";
          } catch (e) {
          }
          return w;
        };
      }
      function steerComing() {
        try {
          if (!window.desk || !desk.coming) return;
          var c = desk.coming();
          if (!c || c.done || c.neighbor || c._kin) return;
          if (c.name && byName(c.name) && c.name !== "Someone off the boats") {
            c._kin = byName(c.name).id;
            return;
          }
          var live = living();
          var d = shopDay();
          var pickF = null;
          for (var i = 0; i < live.length; i++) {
            if (live[i].id === "mae") continue;
            if (live[i].nextD === d || live[i].trust < 0.24 && live[i].visits > 0) {
              pickF = live[i];
              break;
            }
          }
          if (!pickF) return;
          c.name = pickF.n;
          c.want = pickF.want;
          c._kin = pickF.id;
          c.had = stockHave().indexOf(pickF.want) >= 0;
          c.hour = Math.max(c.hour || 11, 10.5);
        } catch (e) {
        }
      }
      function watchTill() {
        try {
          if (!window.shopLife || !shopLife.day) return;
          var day = shopLife.day();
          if (!day || !isFinite(day.sales)) return;
          if (!tillReady) {
            lastSales = day.sales;
            tillReady = true;
            return;
          }
          lastSales = day.sales;
        } catch (e) {
        }
      }
      function dueLine() {
        var live = living();
        var d = shopDay();
        for (var i = 0; i < live.length; i++) {
          if (live[i].nextD === d && live[i].id !== "mae") {
            return live[i].n.split(" ")[0] + " is coming, for " + live[i].want + ".";
          }
        }
        return "";
      }
      function whisper() {
        if (lastGold && now() - lastGoldAt < 12) return lastGold;
        var due = dueLine();
        if (due) return due;
        var live = living();
        for (var i = 0; i < live.length; i++) {
          if (live[i].trust < 0.18 && live[i].visits) {
            var k2 = kinOf(live[i]);
            if (k2 && !k2.dead) return k2.n.split(" ")[0] + " still talks about the miss.";
            return live[i].n.split(" ")[0] + " will not bag. Word's off.";
          }
        }
        return "";
      }
      function line() {
        var live = living();
        var bits = [];
        for (var i = 0; i < live.length && bits.length < 3; i++) {
          var f = live[i];
          var k2 = kinOf(f);
          var who = f.n + ", " + f.job;
          if (k2) who += " · " + (f.how || "kin") + " of " + k2.n.split(" ")[0];
          if (f.last) who += " · " + f.last;
          bits.push(who);
        }
        return bits.join(" / ") || whisper();
      }
      function seedWiki() {
        try {
          var wiki = typeof WIKI === "function" ? WIKI() : window.WIKI;
          if (!wiki || !wiki.push) return;
          if (wiki.some(function(a) {
            return a && a.id === "k_kin";
          })) return;
          wiki.push({
            id: "k_kin",
            sec: "You and your people",
            t: "The street is a civ",
            tags: "kin family regular cousin grudge name baker mae people",
            w: "<p>Walk-ins are not kinds. They are people. A name, a job, kin, a god, a preference, a memory of a fish bag. Mae has a child on this block. The previous keeper left cousins. A miss walks to the family. A dead fish in a window is a grudge. A collector wants a named line, not a SKU.</p><p><b>What to do about it:</b> read Life. The names repeat. Keep the pair the cousin asked for. Do not sell a sick one to a family that talks.</p>"
          });
        } catch (e) {
        }
      }
      function tick() {
        try {
          wrapBrowse();
          wrapChoir();
          seedWiki();
          if (sceneName() === "title") return;
          seedFolk();
          syncMae();
          if (now() - lastTick > 0.9) {
            lastTick = now();
            ageStreet();
            steerComing();
            watchTill();
          }
          if (now() - lastUi > 1.2) lastUi = now();
        } catch (e) {
        }
      }
      window.kin = {
        whisper,
        line,
        folk: function() {
          return state().folk;
        },
        of: byName,
        id: byId,
        hurt: function(n, amt, why) {
          hurt(byName(n) || byId(n), amt, why);
        },
        gift: function(n, amt, why) {
          gift(byName(n) || byId(n), amt, why);
        }
      };
      if (window.__onBeat) window.__onBeat(tick, 240);
      else
        setTimeout(function loop() {
          tick();
          setTimeout(loop, 240);
        }, 200);
    })();
  } catch (e) {
    console.error("Layer kin.js failed to start:", e);
  }

  // src/layers/relic.js
  try {
    (function() {
      "use strict";
      var lastTick = 0;
      var lastUi = 0;
      var lastGold = "";
      var lastGoldAt = 0;
      var seenArts = {};
      var overlay = null;
      var octx = null;
      var didBrowse = false;
      var didChoir = false;
      var didFish = false;
      function now() {
        return typeof performance !== "undefined" ? performance.now() / 1e3 : Date.now() / 1e3;
      }
      function gs() {
        try {
          if (typeof gameState === "function") return gameState();
          if (typeof gameState === "object" && gameState) return gameState;
        } catch (e) {
        }
        return null;
      }
      function year() {
        try {
          if (window.saga && typeof saga.year === "function") return saga.year();
        } catch (e) {
        }
        return 1e3;
      }
      function sceneName() {
        try {
          if (document.body.classList.contains("titling")) return "title";
          if (document.body.classList.contains("work")) return "work";
          if (typeof sceneNow === "function") return String(sceneNow() || "");
        } catch (e) {
        }
        return "shop";
      }
      function hash32(s) {
        var h = 2166136261;
        s = String(s || "");
        for (var i = 0; i < s.length; i++) {
          h ^= s.charCodeAt(i);
          h = Math.imul(h, 16777619);
        }
        return h >>> 0;
      }
      function because(text) {
        if (!text) return;
        try {
          if (window.weave && weave.because) weave.because(text);
        } catch (e) {
        }
        try {
          if (window.desk && desk.think) desk.think("relic", text);
        } catch (e2) {
        }
      }
      function egg(id, line2) {
        try {
          if (typeof findEgg === "function") findEgg(id, line2);
        } catch (e) {
        }
      }
      function gold(text, force) {
        if (!text) return;
        lastGold = text;
        lastGoldAt = now();
        try {
          var el = document.getElementById("hookWhisper");
          if (el && (force || el.textContent !== text)) {
            el.textContent = text;
            el.classList.add("on", "pop");
          }
        } catch (e) {
        }
      }
      function arts() {
        try {
          if (window.mind && mind.book) {
            var b = mind.book();
            if (b && Array.isArray(b.arts)) return b.arts;
          }
        } catch (e) {
        }
        return [];
      }
      function state() {
        var g = gs();
        if (g && g.relic && typeof g.relic === "object") return g.relic;
        var st = { window: null, case: [], seen: 0, lastWho: "" };
        try {
          if (g) g.relic = st;
        } catch (e) {
        }
        return st;
      }
      function keeperRelic() {
        var k2 = null;
        try {
          if (window.late && late.keeper) k2 = late.keeper();
        } catch (e) {
        }
        if (!k2 || !k2.n) {
          k2 = { n: "the last keeper", leftY: year() - 1 };
        }
        var first = k2.n.split(" ")[0];
        return {
          n: first + "'s Key",
          mat: "brass",
          who: k2.n,
          y: k2.leftY || year() - 1,
          img: "the last lock",
          rx: "Salt in the grain",
          from: "keeper"
        };
      }
      function seedWindow() {
        var st = state();
        if (st.window && st.window.n) return;
        st.window = keeperRelic();
        st.seen = 1;
        because(st.window.n + " is in the window. " + st.window.who + " left it.");
        egg("relickey", st.window.n + " in the glass.");
      }
      function since(mark2, list) {
        var last = list.length ? list[list.length - 1] : null;
        if (mark2.list !== list) {
          mark2.list = list;
          mark2.last = last;
          return [];
        }
        if (last === mark2.last) return [];
        var at = mark2.last ? list.lastIndexOf(mark2.last) : -1;
        mark2.last = last;
        return list.slice(at + 1);
      }
      function watchArts() {
        var fresh = since(seenArts, arts());
        if (!fresh.length) return;
        var art = fresh[fresh.length - 1];
        if (!art || !art.n) return;
        var st = state();
        if (st.window && st.window.n && st.window.n !== art.n) {
          st.case.push(st.window);
          if (st.case.length > 12) st.case.shift();
        }
        st.window = {
          n: art.n,
          mat: art.mat || "driftglass",
          who: art.who || "a fish",
          y: art.y || year(),
          img: art.img || "the shop as it stood",
          rx: art.rx || "",
          from: "mood"
        };
        var line2 = st.window.n + " is in the window.";
        because(line2);
        gold(line2, true);
        egg("relicin", line2);
      }
      function matColor(mat) {
        var s = String(mat || "");
        if (/brass|gold|mica/.test(s)) return "rgba(244,196,83,.85)";
        if (/shell|bone/.test(s)) return "rgba(232,220,200,.9)";
        if (/bog|oak|wood/.test(s)) return "rgba(120,78,42,.9)";
        if (/glass|drift/.test(s)) return "rgba(140,200,220,.85)";
        if (/stone|river/.test(s)) return "rgba(150,150,160,.85)";
        return "rgba(200,180,140,.85)";
      }
      function ensureOverlay() {
        if (overlay && overlay.parentNode) return;
        overlay = document.getElementById("relicVeil");
        if (!overlay) {
          overlay = document.createElement("canvas");
          overlay.id = "relicVeil";
          overlay.setAttribute("aria-hidden", "true");
          document.body.appendChild(overlay);
        }
        octx = overlay.getContext("2d");
      }
      function resize() {
        ensureOverlay();
        var dpr = Math.min(2, window.devicePixelRatio || 1);
        var w = window.innerWidth;
        var h = window.innerHeight;
        if (overlay.width !== (w * dpr | 0) || overlay.height !== (h * dpr | 0)) {
          overlay.width = w * dpr | 0;
          overlay.height = h * dpr | 0;
          overlay.style.cssText = "position:fixed;inset:0;width:100%;height:100%;pointer-events:none;z-index:3";
          octx.setTransform(dpr, 0, 0, dpr, 0, 0);
        }
      }
      function drawRelic() {
        var sc = sceneName();
        if (sc === "title" || sc === "work") {
          if (octx && overlay) octx.clearRect(0, 0, overlay.width, overlay.height);
          return;
        }
        if (sc !== "shop" && sc !== "front") {
          if (octx && overlay) octx.clearRect(0, 0, overlay.width, overlay.height);
          return;
        }
        resize();
        var w = window.innerWidth;
        var h = window.innerHeight;
        octx.clearRect(0, 0, w, h);
        var st = state();
        var art = st.window;
        if (!art) return;
        var x = w * 0.18;
        var y = h * 0.22;
        var t = now();
        var glow = 0.18 + 0.08 * Math.sin(t * 1.4);
        octx.save();
        octx.translate(x, y);
        octx.rotate(-0.08);
        var g = octx.createRadialGradient(0, 0, 2, 0, 0, 28);
        g.addColorStop(0, "rgba(244,196,83," + glow.toFixed(3) + ")");
        g.addColorStop(1, "rgba(244,196,83,0)");
        octx.fillStyle = g;
        octx.beginPath();
        octx.arc(0, 0, 28, 0, 7);
        octx.fill();
        octx.fillStyle = "rgba(18,12,8,.72)";
        octx.fillRect(-9, -13, 18, 22);
        octx.strokeStyle = matColor(art.mat);
        octx.lineWidth = 1.4;
        octx.strokeRect(-9, -13, 18, 22);
        octx.beginPath();
        octx.moveTo(-5, -4);
        octx.lineTo(0, 2);
        octx.lineTo(5, -6);
        octx.stroke();
        octx.restore();
        octx.font = "600 11px Nunito, sans-serif";
        octx.fillStyle = "rgba(244,196,83,.82)";
        octx.textAlign = "center";
        octx.fillText(art.n, x, y + 26);
      }
      function wrapBrowse() {
        if (didBrowse || !window.shopBrowse) return;
        didBrowse = true;
        var orig = window.shopBrowse;
        window.shopBrowse = function(idx, slot, W, floorY, personS, simT) {
          var rec = orig.apply(this, arguments);
          try {
            if (!rec) return rec;
            var st = window.shopLife && shopLife.browse ? shopLife.browse()[idx] : null;
            var art = state().window;
            if (!art || !art.n) return rec;
            var keep = typeof keepGuest === "function" ? keepGuest(st) : !!(st && (st._lateMae || st._goingHold || st._lateKid || st._mask));
            if (rec.phase === "look" && st && !st._relicSaid && !keep) {
              st._relicSaid = 1;
              if (rec.kind === "collector" || rec.kind === "neighbor" || Math.random() < 0.28) {
                rec.line = rec.kind === "kid" ? "What's the shiny one?" : "I came for " + art.n + ".";
                st.line = rec.line;
                st.lineUntil = now() + 4;
                st.until = Math.max(st.until || 0, now() + 5.5);
                state().lastWho = rec.name || rec.kind || "";
                because((rec.name || "Someone") + " came to see " + art.n + ".");
                if (rec.kind === "collector") gold("A collector came for " + art.n + ".", true);
              }
            }
            if (rec.phase === "pay" && st && !st._relicPay && rec.kind === "collector" && art.from === "mood") {
              st._relicPay = 1;
              rec.line = rec.line || "The " + (art.who || "one") + " that made it. I'll take that line.";
              st.line = rec.line;
            }
          } catch (e) {
          }
          return rec;
        };
      }
      function wrapChoir() {
        if (didChoir || !window.choir || !choir.weather) return;
        didChoir = true;
        var orig = choir.weather;
        choir.weather = function() {
          var w = orig.apply(this, arguments);
          try {
            var art = state().window;
            if (art && art.n) {
              w.sweet = Math.min(1, (w.sweet || 0) + (art.from === "mood" ? 0.14 : 0.06));
              if (!w.line) w.line = art.n + " is in the window.";
            }
          } catch (e) {
          }
          return w;
        };
      }
      function wrapFish() {
        if (didFish || typeof window.drawFishSprite !== "function") return;
        didFish = true;
        var orig = window.drawFishSprite;
        window.drawFishSprite = function(a) {
          try {
            var art = state().window;
            if (art && a && a.fish && a.fish.nick && art.who && a.fish.nick === art.who) {
              a.fish._gaze = now();
            }
          } catch (e) {
          }
          return orig.apply(this, arguments);
        };
      }
      function whisper() {
        if (lastGold && now() - lastGoldAt < 12) return lastGold;
        return "";
      }
      function line() {
        var st = state();
        if (st.window && st.window.n) {
          return st.window.n + ", " + st.window.mat + (st.window.rx ? ". " + st.window.rx : "") + ". Engraved is " + (st.window.img || "the shop") + ".";
        }
        return whisper();
      }
      function seedWiki() {
        try {
          var wiki = typeof WIKI === "function" ? WIKI() : window.WIKI;
          if (!wiki || !wiki.push) return;
          if (wiki.some(function(a) {
            return a && a.id === "k_relic";
          })) return;
          wiki.push({
            id: "k_relic",
            sec: "The chronicle",
            t: "The artifact is in the window",
            tags: "artifact relic mood window brass engraving collector",
            w: "<p>A strange mood leaves an object. Fin's puts it in the glass. The previous keeper left a key. A named fish leaves a Wake, a Filigree, a Salt-Mark. Collectors come to see it, not to buy a SKU. The choir sweetens while it sits. The gold line names it.</p><p><b>What to do about it:</b> leave the mood. Watch the window. A collector who came for the object will take the line that made it. Do not bag the fish in the middle of the work.</p>"
          });
        } catch (e) {
        }
      }
      function tick() {
        try {
          wrapBrowse();
          wrapChoir();
          wrapFish();
          seedWiki();
          if (sceneName() === "title") return;
          seedWindow();
          if (now() - lastTick > 0.8) {
            lastTick = now();
            watchArts();
          }
          if (now() - lastUi > 0.08) {
            lastUi = now();
            drawRelic();
          }
        } catch (e) {
        }
      }
      window.relic = {
        whisper,
        line,
        of: function() {
          return state().window;
        }
      };
      if (window.__onBeat) window.__onBeat(tick, 90);
      else
        setTimeout(function loop() {
          tick();
          setTimeout(loop, 90);
        }, 180);
    })();
  } catch (e) {
    console.error("Layer relic.js failed to start:", e);
  }

  // src/layers/ill.js
  try {
    (function() {
      "use strict";
      var lastTick = 0;
      var lastUi = 0;
      var lastGold = "";
      var lastGoldAt = 0;
      var didBrowse = false;
      var didChoir = false;
      var didStock = false;
      var namedOnce = false;
      var KINDS = [
        { id: "spot", n: "white-spot", looks: "went white", treat: { salt: 1, almond: 1 }, spread: 0.38 },
        { id: "gold", n: "gold-dust", looks: "dusted gold", treat: { salt: 1, carbon: 1 }, spread: 0.3 },
        { id: "rot", n: "fin-rot", looks: "the fins went", treat: { almond: 1, starter: 1 }, spread: 0.24 },
        { id: "still", n: "still-sickness", looks: "holding too still", treat: { starter: 1, cond: 1 }, spread: 0.2 },
        { id: "itch", n: "salt-itch", looks: "flashing on the glass", treat: { carbon: 1, almond: 1 }, spread: 0.42 }
      ];
      function now() {
        return typeof performance !== "undefined" ? performance.now() / 1e3 : Date.now() / 1e3;
      }
      function gs() {
        try {
          if (typeof gameState === "function") return gameState();
          if (typeof gameState === "object" && gameState) return gameState;
        } catch (e) {
        }
        return null;
      }
      function year() {
        try {
          if (window.saga && typeof saga.year === "function") return saga.year();
        } catch (e) {
        }
        return 1e3;
      }
      function shopDay() {
        try {
          var g = gs();
          if (g && isFinite(g.t)) return Math.floor((g.t % 1e9 + 1e9) % 1e9 / 2400);
        } catch (e) {
        }
        return 0;
      }
      function sceneName() {
        try {
          if (document.body.classList.contains("titling")) return "title";
          if (typeof sceneNow === "function") return String(sceneNow() || "");
        } catch (e) {
        }
        return "shop";
      }
      function hash32(s) {
        var h = 2166136261;
        s = String(s || "");
        for (var i = 0; i < s.length; i++) {
          h ^= s.charCodeAt(i);
          h = Math.imul(h, 16777619);
        }
        return h >>> 0;
      }
      function clamp01(n) {
        return n < 0 ? 0 : n > 1 ? 1 : n;
      }
      function because(text) {
        if (!text) return;
        try {
          if (window.weave && weave.because) weave.because(text);
        } catch (e) {
        }
        try {
          if (window.desk && desk.think) desk.think("ill", text);
        } catch (e2) {
        }
      }
      function egg(id, line2) {
        try {
          if (typeof findEgg === "function") findEgg(id, line2);
        } catch (e) {
        }
      }
      function gold(text, force) {
        if (!text) return;
        lastGold = text;
        lastGoldAt = now();
        try {
          var el = document.getElementById("hookWhisper");
          if (el && (force || el.textContent !== text)) {
            el.textContent = text;
            el.classList.add("on", "pop");
          }
        } catch (e) {
        }
      }
      function runSeed() {
        try {
          if (typeof runState === "function") {
            var r = runState();
            if (r && r.seed) return r.seed;
          }
        } catch (e) {
        }
        return 1;
      }
      function fishList() {
        try {
          if (typeof allFish === "function") return allFish() || [];
        } catch (e) {
        }
        return [];
      }
      function kindOf(id) {
        for (var i = 0; i < KINDS.length; i++) if (KINDS[i].id === id) return KINDS[i];
        return KINDS[0];
      }
      function pickKind() {
        var hh = hash32("ill:" + runSeed() + ":" + year());
        try {
          if (window.beast && beast.near && beast.near() > 0.45) {
            return {
              id: "beast",
              n: (window.tongue && tongue.word ? tongue.word(9, 2) : "harbor") + " rot",
              looks: "went strange",
              treat: { starter: 1, salt: 1 },
              spread: 0.5
            };
          }
        } catch (e) {
        }
        return KINDS[hh % KINDS.length];
      }
      function state() {
        var g = gs();
        if (g && g.ill && typeof g.ill === "object") return g.ill;
        var st = { id: "", name: "", tank: 0, last: "", homes: 0, treated: 0 };
        try {
          if (g) g.ill = st;
        } catch (e) {
        }
        return st;
      }
      function syn() {
        var st = state();
        if (st.id) {
          var k2 = kindOf(st.id);
          if (st.id === "beast") {
            return {
              id: "beast",
              n: st.name || "harbor rot",
              looks: "went strange",
              treat: { starter: 1, salt: 1 },
              spread: 0.5
            };
          }
          return k2;
        }
        return null;
      }
      function nameOutbreak(why) {
        var st = state();
        if (st.name) return syn();
        var k2 = pickKind();
        st.id = k2.id;
        st.name = k2.n;
        st.last = k2.n + (why ? " — " + why : " in the water.");
        because(st.name + " has a name now. " + (why || "The glass caught it."));
        egg("illname", st.name);
        if (!namedOnce) {
          namedOnce = true;
          gold("There's " + st.name + " in the water.", true);
        }
        return k2;
      }
      function infectTank(amt, why) {
        var st = state();
        var k2 = nameOutbreak(why);
        st.tank = clamp01((st.tank || 0) + amt);
        var list = fishList();
        var n = 0;
        for (var i = 0; i < list.length && n < 3; i++) {
          var f = list[i];
          if (!f || f.dead) continue;
          if ((f.sick || 0) > 0.4) continue;
          if (Math.random() < st.tank * 0.55 + 0.2) {
            f.sick = Math.max(f.sick || 0, 0.28 + st.tank * 0.4);
            f.ill = st.name || true;
            n++;
          }
        }
        if (n) st.last = n + " in the glass caught " + (st.name || "it") + ".";
        return k2;
      }
      function clearFish() {
        var list = fishList();
        for (var i = 0; i < list.length; i++) {
          var f = list[i];
          if (!f) continue;
          f.sick = Math.max(0, (f.sick || 0) * 0.15);
          if (f.sick < 0.08) {
            f.sick = 0;
            f.ill = 0;
          }
        }
      }
      function watchTank() {
        var list = fishList();
        var sick = 0;
        var n = 0;
        for (var i = 0; i < list.length; i++) {
          if (!list[i] || list[i].dead) continue;
          n++;
          if (list[i].sick || list[i].ill) sick++;
        }
        if (!n) return;
        var st = state();
        if (sick && !st.name) nameOutbreak("it was already in the glass");
        if (sick) st.tank = clamp01(Math.max(st.tank || 0, sick / n));
        else st.tank = clamp01((st.tank || 0) * 0.96);
        try {
          if (window.fever && fever.heat && fever.heat() > 0.62 && !st.name) {
            infectTank(0.22, "the street fever walked in");
          }
        } catch (e) {
        }
      }
      function homes() {
        try {
          if (window.going && going.homes) return going.homes() || [];
        } catch (e) {
        }
        return [];
      }
      function watchHomes() {
        var hs = homes();
        var st = state();
        var k2 = syn();
        var hit = 0;
        for (var i = 0; i < hs.length; i++) {
          var h = hs[i];
          if (!h || h.dead) continue;
          if (h.sick && !h._ill) {
            if (!k2) k2 = nameOutbreak("a fish bag went out carrying it");
            h._ill = k2.id;
            h.syn = k2.n;
            st.homes = (st.homes || 0) + 1;
            var call = h.nick || "The " + h.sp;
            var line2 = call + " went home with " + k2.n + ". " + h.who + "'s window.";
            st.last = line2;
            because(line2);
            gold(h.who.split(" ")[0] + "'s window has " + k2.n + ".", true);
            egg("illbag", line2);
            hurtWho(h.who, 0.08, "sold a sick one");
          }
          if (h._ill) hit++;
          if (h.fry === 2 && h._ill && !h._illFry) {
            h._illFry = 1;
            infectTank(0.4, "the fry came back with it");
            gold("The fry came back with " + (st.name || "it") + ".", true);
            because("The fry from " + h.who + " put " + (st.name || "it") + " back in the glass.");
          }
        }
        if (!hit && st.tank < 0.08) {
          if (st.treated && st.tank < 0.04) {
            st.name = "";
            st.id = "";
          }
        }
      }
      function spreadDay() {
        var d = shopDay();
        if (d < 1) return;
        var st = state();
        if (st.spreadDay === d) return;
        st.spreadDay = d;
        var k2 = syn();
        if (!k2) return;
        var hs = homes();
        var carriers = [];
        var clean = [];
        for (var i = 0; i < hs.length; i++) {
          if (!hs[i] || hs[i].dead) continue;
          if (hs[i]._ill || hs[i].sick) carriers.push(hs[i]);
          else clean.push(hs[i]);
        }
        if (!carriers.length || !clean.length) return;
        var hh = hash32("spread:" + runSeed() + ":" + d);
        if (hh % 100 / 100 > k2.spread) return;
        var src = carriers[hh % carriers.length];
        var dst = null;
        for (var j = 0; j < clean.length; j++) {
          if (clean[j].who === src.who) {
            dst = clean[j];
            break;
          }
        }
        if (!dst) dst = clean[(hh >>> 8) % clean.length];
        dst.sick = true;
        dst._ill = k2.id;
        dst.syn = k2.n;
        dst.health = Math.min(dst.health || 0.5, 0.34);
        state().homes = (state().homes || 0) + 1;
        var line2 = k2.n + " walked from " + src.who.split(" ")[0] + " to " + dst.who.split(" ")[0] + ".";
        because(line2);
        gold(line2, true);
        hurtWho(dst.who, 0.05, k2.n);
      }
      function hurtWho(who, amt, why) {
        try {
          if (window.kin && kin.of) {
            var fig = kin.of(who);
            if (fig && !fig.dead) {
              fig.trust = Math.max(0.04, (fig.trust || 0.4) - amt);
              fig.last = why || "sold a sick one";
            }
          }
        } catch (e) {
        }
        try {
          if (window.desk && desk.neighbor) {
            var n = desk.neighbor();
            if (n && who && (n.name === who || /mae|baker/i.test(who))) {
              n.trust = Math.max(0.08, (n.trust || 0.4) - amt);
              n.last = why || n.last;
            }
          }
        } catch (e2) {
        }
      }
      function treat(id) {
        var k2 = syn();
        var st = state();
        if (!k2 && !st.tank) return false;
        var helped = !k2 || !!(k2.treat && k2.treat[id]);
        if (!helped && id !== "salt" && id !== "starter") return false;
        var drop = helped ? 0.55 : 0.18;
        st.tank = clamp01((st.tank || 0) - drop);
        st.treated = (st.treated || 0) + 1;
        clearFish();
        var hs = homes();
        for (var i = 0; i < hs.length; i++) {
          if (!hs[i] || !hs[i]._ill) continue;
          if (helped) {
            hs[i].sick = false;
            hs[i].health = Math.min(1, (hs[i].health || 0.4) + 0.22);
            if (id === "salt" || id === "starter") {
              hs[i]._ill = 0;
              hs[i].syn = "";
            }
          }
        }
        var line2 = helped ? (st.name || "The water") + " hates the " + id + ". The column is easier." : "It took some of it. Not the name.";
        st.last = line2;
        because(line2);
        if (helped) gold(line2, true);
        if (st.tank < 0.06) {
          st.tank = 0;
          if (st.name) because(st.name + " broke. The glass is holding.");
          st.name = "";
          st.id = "";
        }
        return true;
      }
      function wrapBrowse() {
        if (didBrowse || !window.shopBrowse) return;
        didBrowse = true;
        var orig = window.shopBrowse;
        window.shopBrowse = function(idx, slot, W, floorY, personS, simT) {
          var rec = orig.apply(this, arguments);
          try {
            if (!rec) return rec;
            var st = window.shopLife && shopLife.browse ? shopLife.browse()[idx] : null;
            var keep = typeof keepGuest === "function" ? keepGuest(st) : !!(st && (st._lateMae || st._goingHold || st._lateKid || st._mask));
            var k2 = syn();
            if (!k2 && !(state().tank > 0.2)) return rec;
            var who = rec && rec.name || st && st.guestName || "";
            var hs = homes();
            var theirs = null;
            for (var i = 0; i < hs.length; i++) {
              if (hs[i] && !hs[i].dead && hs[i]._ill && (hs[i].who === who || rec.kind === "neighbor" && hs[i].neighbor)) {
                theirs = hs[i];
                break;
              }
            }
            if (rec.phase === "look" && st && !st._illSaid && !keep) {
              st._illSaid = 1;
              var line2 = "";
              if (theirs) {
                line2 = (theirs.nick || "The last one") + " " + (k2 ? k2.looks : "went off") + ". I came for the line, not the spots.";
                because(who + " came because of " + (k2 ? k2.n : "the spots") + ".");
                gold(line2, true);
                if (Math.random() < 0.55) {
                  rec.phase = "leave";
                  st.phase = "leave";
                  st.bought = false;
                  hurtWho(who, 0.05, "walked. sick fish bag");
                }
              } else if (state().tank > 0.34 && rec.kind === "collector") {
                line2 = "That's " + (k2 ? k2.n : "off") + " in the glass. I'm not bagging it.";
                rec.phase = "leave";
                st.phase = "leave";
                st.bought = false;
              } else if (state().tank > 0.5 && rec.kind !== "kid") {
                line2 = "I'll look from the door. Something's going around.";
                rec.phase = "leave";
                st.phase = "leave";
                st.bought = false;
              }
              if (line2) {
                rec.line = line2;
                st.line = line2;
                st.lineUntil = now() + 3.8;
              }
            }
            if (rec.phase === "pay" && st && !st._illPay && state().tank > 0.42 && rec.kind !== "neighbor") {
              st._illPay = 1;
              rec.phase = "leave";
              st.phase = "leave";
              st.bought = false;
              rec.line = "Not from a tank that's off.";
              st.line = rec.line;
            }
          } catch (e) {
          }
          return rec;
        };
      }
      function wrapChoir() {
        if (didChoir || !window.choir || !choir.weather) return;
        didChoir = true;
        var orig = choir.weather;
        choir.weather = function() {
          var w = orig.apply(this, arguments);
          try {
            var st = state();
            if (st.tank > 0.12 || st.homes) {
              w.sour = Math.min(1, (w.sour || 0) + Math.min(0.28, (st.tank || 0) * 0.4 + (st.homes || 0) * 0.04));
              if (!w.line && st.name) w.line = st.name + " is in the water.";
            }
          } catch (e) {
          }
          return w;
        };
      }
      function wrapStock() {
        if (didStock || !window.stock || !stock.use) return;
        didStock = true;
        var orig = stock.use;
        stock.use = function(id) {
          var r = orig.apply(this, arguments);
          try {
            if (id === "salt" || id === "almond" || id === "carbon" || id === "starter" || id === "cond") {
              treat(id);
            }
          } catch (e) {
          }
          return r;
        };
      }
      function whisper() {
        if (lastGold && now() - lastGoldAt < 12) return lastGold;
        return "";
      }
      function line() {
        var st = state();
        if (st.name && (st.tank > 0.08 || st.homes)) {
          return st.name + (st.tank > 0.08 ? " in the glass" : "") + (st.homes ? (st.tank > 0.08 ? ", " : " ") + st.homes + " window" + (st.homes === 1 ? "" : "s") + " on the block" : "") + ".";
        }
        return st.last || "";
      }
      function seedWiki() {
        try {
          var wiki = typeof WIKI === "function" ? WIKI() : window.WIKI;
          if (!wiki || !wiki.push) return;
          if (wiki.some(function(a) {
            return a && a.id === "k_ill";
          })) return;
          wiki.push({
            id: "k_ill",
            sec: "The shop floor",
            t: "The fish bag carries the water",
            tags: "ill sick syndrome white-spot bag window fry salt vector",
            w: "<p>A fish bag is a vector. Fin's lets a named sickness walk a block. You sold a slightly off tetra. Mae's window went white. Her child came in flashing. The fry she brought back put it in your glass. White-spot, gold-dust, fin-rot, still-sickness, salt-itch — each one hates a different bottle.</p><p><b>What to do about it:</b> do not bag a sick one. Salt, the leaf, carbon, the vial. Life will name the window. The choir sours until the name breaks.</p>"
          });
        } catch (e) {
        }
      }
      function tick() {
        try {
          wrapBrowse();
          wrapChoir();
          wrapStock();
          seedWiki();
          if (sceneName() === "title") return;
          if (now() - lastTick > 0.9) {
            lastTick = now();
            watchTank();
            watchHomes();
            spreadDay();
          }
          if (now() - lastUi > 1.2) lastUi = now();
        } catch (e) {
        }
      }
      window.ill = {
        whisper,
        line,
        of: function() {
          return state();
        },
        treat,
        seed: function() {
          infectTank(0.55, "it was already in the glass");
          return state();
        }
      };
      if (window.__onBeat) window.__onBeat(tick, 280);
      else
        setTimeout(function loop() {
          tick();
          setTimeout(loop, 280);
        }, 220);
    })();
  } catch (e) {
    console.error("Layer ill.js failed to start:", e);
  }

  // src/layers/mark.js
  try {
    (function() {
      "use strict";
      var lastTick = 0;
      var lastUi = 0;
      var lastGold = "";
      var lastGoldAt = 0;
      var seenDead = {};
      var overlay = null;
      var octx = null;
      var didBrowse = false;
      var didChoir = false;
      var didStock = false;
      var seeded = false;
      function now() {
        return typeof performance !== "undefined" ? performance.now() / 1e3 : Date.now() / 1e3;
      }
      function gs() {
        try {
          if (typeof gameState === "function") return gameState();
          if (typeof gameState === "object" && gameState) return gameState;
        } catch (e) {
        }
        return null;
      }
      function year() {
        try {
          if (window.saga && typeof saga.year === "function") return saga.year();
        } catch (e) {
        }
        return 1e3;
      }
      function sceneName() {
        try {
          if (document.body.classList.contains("titling")) return "title";
          if (document.body.classList.contains("work")) return "work";
          if (typeof sceneNow === "function") return String(sceneNow() || "");
        } catch (e) {
        }
        return "shop";
      }
      function because(text) {
        if (!text) return;
        try {
          if (window.weave && weave.because) weave.because(text);
        } catch (e) {
        }
        try {
          if (window.desk && desk.think) desk.think("mark", text);
        } catch (e2) {
        }
      }
      function egg(id, line2) {
        try {
          if (typeof findEgg === "function") findEgg(id, line2);
        } catch (e) {
        }
      }
      function gold(text, force) {
        if (!text) return;
        lastGold = text;
        lastGoldAt = now();
        try {
          var el = document.getElementById("hookWhisper");
          if (el && (force || el.textContent !== text)) {
            el.textContent = text;
            el.classList.add("on", "pop");
          }
        } catch (e) {
        }
      }
      function book() {
        try {
          if (window.saga && saga.book) return saga.book();
        } catch (e) {
        }
        try {
          if (window.mind && mind.book) return mind.book();
        } catch (e2) {
        }
        return { dead: [], slabs: [] };
      }
      function state() {
        var g = gs();
        if (g && g.mark && Array.isArray(g.mark.due)) return g.mark;
        var st = { due: [], plates: [], last: "" };
        try {
          if (g) g.mark = st;
        } catch (e) {
        }
        return st;
      }
      function plated(n, y) {
        var st = state();
        for (var i = 0; i < st.plates.length; i++) {
          if (st.plates[i].n === n && (y == null || st.plates[i].y === y)) return true;
        }
        return false;
      }
      function alreadyDue(n, y) {
        var st = state();
        for (var i = 0; i < st.due.length; i++) {
          if (st.due[i].n === n && (y == null || st.due[i].y === y)) return true;
        }
        return false;
      }
      function want(n, how, where, y, quiet) {
        if (!n) return;
        y = y == null ? year() : y;
        if (plated(n, y) || alreadyDue(n, y)) return;
        var rec = { n, how: how || "died", where: where || "the shop", y };
        state().due.push(rec);
        if (state().due.length > 16) state().due.shift();
        if (!quiet) {
          var line2 = n + " has no name on the wall.";
          because(line2);
          gold(line2, true);
          egg("markdue", line2);
        }
      }
      function seedDue() {
        if (seeded) return;
        var b = book();
        var dead = b && b.dead || [];
        for (var i = 0; i < dead.length; i++) {
          var d = dead[i];
          if (!d || !d.n) continue;
          want(d.n, d.how || "died", d.street ? "the block" : "the shop", d.y, true);
        }
        seeded = true;
        if (state().due.length) {
          because(state().due[0].n + " has been in the book. Nobody put them on the wall.");
          egg("markinherit", state().due[0].n + " has no name on the wall.");
        }
      }
      function since(mark2, list) {
        var last = list.length ? list[list.length - 1] : null;
        if (mark2.list !== list) {
          mark2.list = list;
          mark2.last = last;
          return [];
        }
        if (last === mark2.last) return [];
        var at = mark2.last ? list.lastIndexOf(mark2.last) : -1;
        mark2.last = last;
        return list.slice(at + 1);
      }
      function watchDead() {
        var b = book();
        var fresh = since(seenDead, b && b.dead || []);
        for (var i = 0; i < fresh.length; i++) {
          var d = fresh[i];
          if (!d || !d.n) continue;
          want(d.n, d.how || "died", d.street ? "the block" : "the shop", d.y, false);
        }
      }
      function plateOne() {
        var st = state();
        if (!st.due.length) return null;
        var rec = st.due.shift();
        st.plates.push({ n: rec.n, y: rec.y, how: rec.how, at: year() });
        if (st.plates.length > 14) st.plates.shift();
        var line2 = rec.n + " is on the board.";
        st.last = line2;
        because(line2);
        gold(line2, true);
        egg("markplate", line2);
        try {
          if (window.mind && mind.book) {
            var b = mind.book();
            b.slabs = b.slabs || [];
            b.slabs.push({ n: rec.n, how: rec.how, y: rec.y, wall: 1 });
          }
        } catch (e) {
        }
        return rec;
      }
      function haunt() {
        return state().due.length;
      }
      function ensureOverlay() {
        if (overlay && overlay.parentNode) return;
        overlay = document.getElementById("markVeil");
        if (!overlay) {
          overlay = document.createElement("canvas");
          overlay.id = "markVeil";
          overlay.setAttribute("aria-hidden", "true");
          document.body.appendChild(overlay);
        }
        octx = overlay.getContext("2d");
      }
      function resize() {
        ensureOverlay();
        var dpr = Math.min(2, window.devicePixelRatio || 1);
        var w = window.innerWidth;
        var h = window.innerHeight;
        if (overlay.width !== (w * dpr | 0) || overlay.height !== (h * dpr | 0)) {
          overlay.width = w * dpr | 0;
          overlay.height = h * dpr | 0;
          overlay.style.cssText = "position:fixed;inset:0;width:100%;height:100%;pointer-events:none;z-index:3";
          octx.setTransform(dpr, 0, 0, dpr, 0, 0);
        }
      }
      function drawPlates() {
        var sc = sceneName();
        if (sc === "title" || sc === "work" || sc !== "shop" && sc !== "front") {
          if (octx && overlay) octx.clearRect(0, 0, overlay.width, overlay.height);
          return;
        }
        resize();
        var w = window.innerWidth;
        var h = window.innerHeight;
        octx.clearRect(0, 0, w, h);
        var st = state();
        var x = w * 0.84;
        var y = h * 0.28;
        var show = st.plates.slice(-3);
        for (var i = 0; i < show.length; i++) {
          var py = y + i * 28;
          octx.fillStyle = "rgba(18,12,8,.7)";
          octx.fillRect(x - 34, py - 10, 68, 20);
          octx.strokeStyle = "rgba(196,160,88,.8)";
          octx.lineWidth = 1.1;
          octx.strokeRect(x - 34, py - 10, 68, 20);
          octx.font = "600 10px Nunito, sans-serif";
          octx.fillStyle = "rgba(232,214,170,.88)";
          octx.textAlign = "center";
          octx.fillText(show[i].n, x, py + 4);
        }
        if (st.due.length) {
          var ny = y + show.length * 28;
          octx.fillStyle = "rgba(40,28,18,.55)";
          octx.beginPath();
          octx.arc(x, ny, 3.2, 0, 7);
          octx.fill();
          octx.font = "600 10px Nunito, sans-serif";
          octx.fillStyle = "rgba(180,150,110,.55)";
          octx.textAlign = "center";
          octx.fillText(st.due[0].n, x, ny + 16);
        }
      }
      function wrapBrowse() {
        if (didBrowse || !window.shopBrowse) return;
        didBrowse = true;
        var orig = window.shopBrowse;
        window.shopBrowse = function(idx, slot, W, floorY, personS, simT) {
          var rec = orig.apply(this, arguments);
          try {
            if (!rec) return rec;
            var st = window.shopLife && shopLife.browse ? shopLife.browse()[idx] : null;
            var keep = typeof keepGuest === "function" ? keepGuest(st) : !!(st && (st._lateMae || st._goingHold || st._lateKid || st._mask));
            var due = state().due;
            if (!due.length) return rec;
            if (rec.phase === "look" && st && !st._markSaid && !keep) {
              st._markSaid = 1;
              if (rec.kind === "neighbor" || rec.kind === "collector" || Math.random() < 0.22) {
                var n = due[0].n;
                rec.line = rec.kind === "kid" ? "Who is " + n + "?" : "You never put " + n + " up. The water knows.";
                st.line = rec.line;
                st.lineUntil = now() + 3.8;
                because((rec.name || "Someone") + " asked after " + n + ".");
              }
            }
          } catch (e) {
          }
          return rec;
        };
      }
      function wrapChoir() {
        if (didChoir || !window.choir || !choir.weather) return;
        didChoir = true;
        var orig = choir.weather;
        choir.weather = function() {
          var w = orig.apply(this, arguments);
          try {
            var n = haunt();
            if (n) {
              w.sour = Math.min(1, (w.sour || 0) + Math.min(0.22, n * 0.06));
              if (!w.line) w.line = state().due[0].n + " has no name on the wall.";
            } else if (state().plates.length && !w.line) {
              w.sweet = Math.min(1, (w.sweet || 0) + 0.05);
            }
          } catch (e) {
          }
          return w;
        };
      }
      function wrapStock() {
        if (didStock || !window.stock || !stock.use) return;
        didStock = true;
        var orig = stock.use;
        stock.use = function(id) {
          var r = orig.apply(this, arguments);
          try {
            if (id === "chalk") plateOne();
          } catch (e) {
          }
          return r;
        };
      }
      function whisper() {
        if (lastGold && now() - lastGoldAt < 12) return lastGold;
        return "";
      }
      function line() {
        var st = state();
        if (st.due.length) {
          var bits = [];
          for (var i = 0; i < st.due.length && bits.length < 3; i++) {
            bits.push(st.due[i].n + " has no name on the wall");
          }
          return bits.join(". ") + ".";
        }
        if (st.plates.length) {
          return st.plates[st.plates.length - 1].n + " is on the board.";
        }
        return st.last || "";
      }
      function seedWiki() {
        try {
          var wiki = typeof WIKI === "function" ? WIKI() : window.WIKI;
          if (!wiki || !wiki.push) return;
          if (wiki.some(function(a) {
            return a && a.id === "k_mark";
          })) return;
          wiki.push({
            id: "k_mark",
            sec: "The chronicle",
            t: "The dead want a name on the wall",
            tags: "slab memorial plate chalk ghost unburied ingum dead",
            w: "<p>Ingum has been dead since Year 412. The previous keeper never put her on the wall. The choir keeps a wrong note. Walk-ins ask who she is. Chalk writes the name. The water goes quiet.</p><p><b>What to do about it:</b> Harbor Supply, counter chalk. Write the oldest name. Life lists who is still waiting. Do not raise them instead. That is a different wrong note.</p>"
          });
        } catch (e) {
        }
      }
      function tick() {
        try {
          wrapBrowse();
          wrapChoir();
          wrapStock();
          seedWiki();
          if (sceneName() === "title") return;
          seedDue();
          if (now() - lastTick > 0.9) {
            lastTick = now();
            watchDead();
          }
          if (now() - lastUi > 0.12) {
            lastUi = now();
            drawPlates();
          }
        } catch (e) {
        }
      }
      window.mark = {
        whisper,
        line,
        want,
        plate: plateOne,
        due: function() {
          return state().due;
        },
        of: function() {
          return state();
        }
      };
      if (window.__onBeat) window.__onBeat(tick, 160);
      else
        setTimeout(function loop() {
          tick();
          setTimeout(loop, 160);
        }, 200);
    })();
  } catch (e) {
    console.error("Layer mark.js failed to start:", e);
  }

  // src/layers/pinch.js
  try {
    (function() {
      "use strict";
      var lastTick = 0;
      var lastGold = "";
      var lastGoldAt = 0;
      var didBrowse = false;
      var didChoir = false;
      function now() {
        return typeof performance !== "undefined" ? performance.now() / 1e3 : Date.now() / 1e3;
      }
      function gs() {
        try {
          if (typeof gameState === "function") return gameState();
          if (typeof gameState === "object" && gameState) return gameState;
        } catch (e) {
        }
        return null;
      }
      function year() {
        try {
          if (window.saga && typeof saga.year === "function") return saga.year();
        } catch (e) {
        }
        return 1e3;
      }
      function shopDay() {
        try {
          var g = gs();
          if (g && isFinite(g.t)) return Math.floor((g.t % 1e9 + 1e9) % 1e9 / 2400);
        } catch (e) {
        }
        return 0;
      }
      function sceneName() {
        try {
          if (document.body.classList.contains("titling")) return "title";
          if (typeof sceneNow === "function") return String(sceneNow() || "");
        } catch (e) {
        }
        return "shop";
      }
      function hash32(s) {
        var h = 2166136261;
        s = String(s || "");
        for (var i = 0; i < s.length; i++) {
          h ^= s.charCodeAt(i);
          h = Math.imul(h, 16777619);
        }
        return h >>> 0;
      }
      function because(text) {
        if (!text) return;
        try {
          if (window.weave && weave.because) weave.because(text);
        } catch (e) {
        }
        try {
          if (window.desk && desk.think) desk.think("pinch", text);
        } catch (e2) {
        }
      }
      function egg(id, line2) {
        try {
          if (typeof findEgg === "function") findEgg(id, line2);
        } catch (e) {
        }
      }
      function gold(text, force) {
        if (!text) return;
        lastGold = text;
        lastGoldAt = now();
        try {
          var el = document.getElementById("hookWhisper");
          if (el && (force || el.textContent !== text)) {
            el.textContent = text;
            el.classList.add("on", "pop");
          }
        } catch (e) {
        }
      }
      function kindOf(f) {
        if (!f) return "fish";
        try {
          var S = typeof O !== "undefined" ? O : typeof SPECIES !== "undefined" ? SPECIES : null;
          if (S && f.sp != null && S[f.sp]) {
            var nm = S[f.sp].gname || S[f.sp].name || S[f.sp].vname;
            if (nm) return String(nm).toLowerCase();
          }
        } catch (e) {
        }
        var sp = String(f && f.sp || "");
        if (/gold/.test(sp)) return "goldfish";
        if (/betta/.test(sp)) return "betta";
        if (/guppy/.test(sp)) return "guppy";
        if (/tetra/.test(sp)) return "tetra";
        if (/angel/.test(sp)) return "angelfish";
        if (!sp || /^\d+$/.test(sp)) return "fish";
        return sp.toLowerCase();
      }
      function fishList() {
        try {
          if (typeof allFish === "function") return allFish() || [];
        } catch (e) {
        }
        return [];
      }
      function state() {
        var g = gs();
        if (g && g.pinch && typeof g.pinch === "object") return g.pinch;
        var st = { open: null, cold: [], last: "" };
        try {
          if (g) g.pinch = st;
        } catch (e) {
        }
        return st;
      }
      function hurtKin(n, amt, why) {
        try {
          if (window.kin && kin.hurt) kin.hurt(n, amt, why);
        } catch (e) {
        }
      }
      function giftKin(n, amt, why) {
        try {
          if (window.kin && kin.gift) kin.gift(n, amt, why);
        } catch (e) {
        }
      }
      function takeFish(prefer, allowNamed) {
        var list = fishList();
        var counts = /* @__PURE__ */ Object.create(null);
        var i;
        for (i = 0; i < list.length; i++) {
          if (!list[i] || list[i].dead || list[i].stolen) continue;
          var k2 = kindOf(list[i]);
          counts[k2] = (counts[k2] || 0) + 1;
        }
        var pick = null;
        for (i = 0; i < list.length; i++) {
          var f = list[i];
          if (!f || f.dead || f.stolen) continue;
          var kk = kindOf(f);
          if (prefer && kk !== prefer && pick) continue;
          if (f.nick && !allowNamed) continue;
          if ((counts[kk] || 0) < 2 && pick) continue;
          pick = f;
          if (prefer && kk === prefer && !f.nick) break;
        }
        if (!pick) {
          for (i = 0; i < list.length; i++) {
            if (list[i] && !list[i].dead && !list[i].stolen) {
              pick = list[i];
              break;
            }
          }
        }
        if (!pick) return null;
        pick.stolen = 1;
        var arrays = [list];
        try {
          var g = gs();
          if (g && Array.isArray(g.fish) && g.fish !== list) arrays.push(g.fish);
        } catch (e) {
        }
        for (i = 0; i < arrays.length; i++) {
          var ix = arrays[i].indexOf(pick);
          if (ix >= 0) arrays[i].splice(ix, 1);
        }
        return {
          nick: pick.nick || "",
          sp: kindOf(pick),
          named: !!pick.nick
        };
      }
      function putHome(gone, who) {
        try {
          if (!window.going || !going.homes || !gone) return;
          var hs = going.homes();
          hs.push({
            nick: gone.nick || "",
            sp: gone.sp,
            who,
            neighbor: false,
            care: 0.28,
            health: 0.55,
            named: !!gone.named,
            sick: false,
            stolen: true,
            day: shopDay(),
            y: year(),
            dead: false,
            fry: 0,
            told: false
          });
        } catch (e) {
        }
      }
      function chance(fig, rec, st) {
        if (shopDay() < 1) return 0;
        if (state().open) return 0;
        if (!fig || fig.dead || fig.id === "mae" || fig.id === "keep") return 0;
        if (rec.kind === "neighbor" || rec.kind === "collector") return 0;
        var keep = typeof keepGuest === "function" ? keepGuest(st) : !!(st && (st._lateMae || st._goingHold || st._lateKid || st._mask));
        if (keep) return 0;
        var c = 0.03;
        var sc = sceneName();
        if (sc === "tank" || sc === "street" || sc === "map") c += 0.16;
        try {
          if (window.shopSite && shopSite.wet && shopSite.wet() > 0.36) c += 0.12;
        } catch (e) {
        }
        try {
          var g = gs();
          if (g && g.clog) c += 0.08;
        } catch (e2) {
        }
        if (fig.kind === "kid") c += 0.1;
        if ((fig.trust || 0.4) < 0.22) c += 0.1;
        return c;
      }
      function steal(fig, rec, st) {
        var gone = takeFish(fig.want || rec.want, fig.kind === "kid");
        if (!gone) return false;
        var call = gone.nick || "the " + gone.sp;
        var who = fig.n;
        var open = {
          who,
          kinId: fig.id,
          nick: gone.nick || "",
          sp: gone.sp,
          named: !!gone.named,
          day: shopDay(),
          y: year(),
          paid: false
        };
        state().open = open;
        state().last = who.split(" ")[0] + " took " + call + ".";
        putHome(gone, who);
        fig.last = "took " + call;
        fig.known = true;
        hurtKin(who, 0.12, "took " + call);
        because(who + " took " + call + ". No fish bag. No sale.");
        gold(who.split(" ")[0] + " took " + call + ". No fish bag.", true);
        egg("pinch", state().last);
        rec.line = fig.kind === "kid" ? "I didn't." : "I was just looking.";
        rec.phase = "leave";
        if (st) {
          st.phase = "leave";
          st.bought = false;
          st.line = rec.line;
          st._pinch = 1;
        }
        return true;
      }
      function closeCase(why) {
        var st = state();
        if (!st.open) return;
        st.open.paid = true;
        st.open.why = why || "made right";
        st.cold.push(st.open);
        if (st.cold.length > 8) st.cold.shift();
        var who = st.open.who;
        giftKin(who, 0.1, why || "made it right");
        because(who.split(" ")[0] + " made it right. " + (st.open.nick || st.open.sp) + " is off the books.");
        gold(who.split(" ")[0] + " made it right.", true);
        st.open = null;
        st.last = why || "made right";
      }
      function goCold() {
        var st = state();
        if (!st.open) return;
        if (shopDay() - st.open.day < 3) return;
        var who = st.open.who;
        hurtKin(who, 0.08, "never made it right");
        because(who + " never brought " + (st.open.nick || "the " + st.open.sp) + " back. The street kept the story.");
        gold("The " + (st.open.nick || st.open.sp) + " is still gone.", true);
        st.cold.push(st.open);
        if (st.cold.length > 8) st.cold.shift();
        st.last = "went cold";
        st.open = null;
      }
      function wrapBrowse() {
        if (didBrowse || !window.shopBrowse) return;
        didBrowse = true;
        var orig = window.shopBrowse;
        window.shopBrowse = function(idx, slot, W, floorY, personS, simT) {
          var rec = orig.apply(this, arguments);
          try {
            if (!rec) return rec;
            var st = window.shopLife && shopLife.browse ? shopLife.browse()[idx] : null;
            var fig = null;
            try {
              if (window.kin && kin.of && rec.name) fig = kin.of(rec.name);
            } catch (e) {
            }
            var open = state().open;
            if (open && rec.phase === "look" && st && !st._pinchSaid) {
              st._pinchSaid = 1;
              var thief = fig && (fig.n === open.who || fig.id === open.kinId);
              var kinOfThief = false;
              try {
                if (fig && window.kin && kin.folk) {
                  var all = kin.folk();
                  for (var i = 0; i < all.length; i++) {
                    if (all[i] && all[i].id === open.kinId && fig.kinOf === all[i].id) kinOfThief = true;
                    if (all[i] && fig && fig.id === all[i].id && all[i].kinOf === open.kinId) kinOfThief = true;
                  }
                }
              } catch (eK) {
              }
              var call = open.nick || "the " + open.sp;
              if (thief) {
                rec.line = "I didn't take " + call + ".";
                rec.phase = "leave";
                st.phase = "leave";
                st.bought = false;
              } else if (kinOfThief) {
                rec.line = "I'll make it right. A " + open.sp + ".";
                st.want = open.sp;
                rec.want = open.sp;
                st._pinchPay = 1;
              } else if (rec.kind === "collector" || rec.kind === "neighbor") {
                rec.line = call + " walked out without a fish bag. I'm looking.";
                if (Math.random() < 0.4) {
                  rec.phase = "leave";
                  st.phase = "leave";
                  st.bought = false;
                }
              }
              if (rec.line) {
                st.line = rec.line;
                st.lineUntil = now() + 3.8;
              }
            }
            if (open && rec.phase === "pay" && st && st._pinchPay && fig && !st._pinchClosed) {
              st._pinchClosed = 1;
              closeCase("brought a " + open.sp);
              rec.line = rec.line || "For what " + open.who.split(" ")[0] + " took.";
              st.line = rec.line;
            }
            if (rec.phase === "leave" && st && !st.bought && !st._pinch && fig) {
              var ch = chance(fig, rec, st);
              var hh = hash32(fig.id + ":" + shopDay() + ":" + idx);
              if (ch > 0 && hh % 1e3 / 1e3 < ch) {
                steal(fig, rec, st);
              }
            }
          } catch (e) {
          }
          return rec;
        };
      }
      function wrapChoir() {
        if (didChoir || !window.choir || !choir.weather) return;
        didChoir = true;
        var orig = choir.weather;
        choir.weather = function() {
          var w = orig.apply(this, arguments);
          try {
            if (state().open) {
              w.sour = Math.min(1, (w.sour || 0) + 0.16);
              if (!w.line) w.line = (state().open.nick || "One") + " walked out without a fish bag.";
            }
          } catch (e) {
          }
          return w;
        };
      }
      function whisper() {
        if (lastGold && now() - lastGoldAt < 12) return lastGold;
        return "";
      }
      function line() {
        var st = state();
        if (st.open) {
          return (st.open.nick || "A " + st.open.sp) + " walked out with " + st.open.who.split(" ")[0] + ". No fish bag. Day " + (shopDay() - st.open.day) + " of it.";
        }
        if (st.last) return st.last;
        return whisper();
      }
      function seedWiki() {
        try {
          var wiki = typeof WIKI === "function" ? WIKI() : window.WIKI;
          if (!wiki || !wiki.push) return;
          if (wiki.some(function(a) {
            return a && a.id === "k_pinch";
          }))
            return;
          wiki.push({
            id: "k_pinch",
            sec: "The shop floor",
            t: "Someone took it",
            tags: "theft steal pinch justice aisle kid bag missing",
            w: "<p>Someone took it. A kid on a wet aisle. A lunch when you are in the tank. A named one gone, no sale, no fish bag. It is in their window. The family hears. They come to make it right — a pair of that fish, paid — or they never bag here again. Three days and the story goes cold, and the choir keeps it.</p><p><b>What to do about it:</b> stay on the aisle when the boards are wet. Keep a pair of what walked. Life names who took it. The gold line does not cheer you.</p>"
          });
        } catch (e) {
        }
      }
      function tick() {
        try {
          wrapBrowse();
          wrapChoir();
          seedWiki();
          if (sceneName() === "title") return;
          if (now() - lastTick > 1.1) {
            lastTick = now();
            goCold();
          }
        } catch (e) {
        }
      }
      window.pinch = {
        whisper,
        line,
        of: function() {
          return state().open;
        },
        seed: function() {
          var fig = null;
          try {
            if (window.kin && kin.folk) {
              var all = kin.folk();
              for (var i = 0; i < all.length; i++) {
                if (all[i] && !all[i].dead && all[i].id !== "mae" && all[i].id !== "keep") {
                  fig = all[i];
                  break;
                }
              }
            }
          } catch (e) {
          }
          if (!fig) return null;
          steal(fig, { kind: fig.kind || "kid", phase: "leave", line: "" }, { bought: false });
          return state().open;
        }
      };
      if (window.__onBeat) window.__onBeat(tick, 260);
      else
        setTimeout(function loop() {
          tick();
          setTimeout(loop, 260);
        }, 220);
    })();
  } catch (e) {
    console.error("Layer pinch.js failed to start:", e);
  }

  // src/layers/soul.js
  try {
    (function() {
      "use strict";
      var lastTick = 0;
      var lastGold = "";
      var lastGoldAt = 0;
      var seenBecause = {};
      var didBrowse = false;
      var didChoir = false;
      var LIKES = [
        "named lines",
        "the quiet tank",
        "the key in the window",
        "a dry aisle",
        "a pair on the rack",
        "the baker's goldfish"
      ];
      var HATES = [
        "the wet boards",
        "a sick fish bag",
        "something risen",
        "a sloppy knot",
        "an empty plate",
        "a loud choir"
      ];
      function now() {
        return typeof performance !== "undefined" ? performance.now() / 1e3 : Date.now() / 1e3;
      }
      function gs() {
        try {
          if (typeof gameState === "function") return gameState();
          if (typeof gameState === "object" && gameState) return gameState;
        } catch (e) {
        }
        return null;
      }
      function year() {
        try {
          if (window.saga && typeof saga.year === "function") return saga.year();
        } catch (e) {
        }
        return 1e3;
      }
      function sceneName() {
        try {
          if (document.body.classList.contains("titling")) return "title";
          if (typeof sceneNow === "function") return String(sceneNow() || "");
        } catch (e) {
        }
        return "shop";
      }
      function hash32(s) {
        var h = 2166136261;
        s = String(s || "");
        for (var i = 0; i < s.length; i++) {
          h ^= s.charCodeAt(i);
          h = Math.imul(h, 16777619);
        }
        return h >>> 0;
      }
      function pick(arr, h) {
        if (!arr || !arr.length) return "";
        return arr[(h >>> 0) % arr.length];
      }
      function because(text) {
        if (!text) return;
        try {
          if (window.weave && weave.because) weave.because(text);
        } catch (e) {
        }
        try {
          if (window.desk && desk.think) desk.think("soul", text);
        } catch (e2) {
        }
      }
      function gold(text, force) {
        if (!text) return;
        lastGold = text;
        lastGoldAt = now();
        try {
          var el = document.getElementById("hookWhisper");
          if (el && (force || el.textContent !== text)) {
            el.textContent = text;
            el.classList.add("on", "pop");
          }
        } catch (e) {
        }
      }
      function folk() {
        try {
          if (window.kin && kin.folk) return kin.folk() || [];
        } catch (e) {
        }
        return [];
      }
      function soulOf(fig) {
        if (!fig) return null;
        if (fig.soul && fig.soul.like) return fig.soul;
        var h = hash32("soul:" + (fig.id || "") + ":" + (fig.n || ""));
        fig.soul = {
          brave: (h & 255) / 255,
          tender: (h >>> 8 & 255) / 255,
          proud: (h >>> 16 & 255) / 255,
          like: pick(LIKES, h),
          hate: pick(HATES, h >>> 5),
          short: [],
          dwell: ""
        };
        return fig.soul;
      }
      function file(fig, kind, text) {
        var so = soulOf(fig);
        if (!so || !text) return;
        if (so.short.length && so.short[so.short.length - 1].s === text) return;
        so.short.push({ k: kind || "", s: String(text), y: year(), at: now() });
        if (so.short.length > 8) so.short.shift();
        var tender = so.tender == null ? 0.5 : so.tender;
        var brave = so.brave == null ? 0.5 : so.brave;
        if (kind === "death" || kind === "pinch" || kind === "ill") {
          so.tender = Math.max(0, tender - 0.06);
          so.brave = Math.min(1, brave + 0.04);
          so.dwell = text;
        } else if (kind === "gift" || kind === "plate" || kind === "bag") {
          so.tender = Math.min(1, tender + 0.04);
          if (!so.dwell) so.dwell = text;
        }
      }
      function seedSouls() {
        var all = folk();
        for (var i = 0; i < all.length; i++) if (all[i] && !all[i].dead) soulOf(all[i]);
      }
      function since(mark2, list) {
        var last = list.length ? list[list.length - 1] : null;
        if (mark2.list !== list) {
          mark2.list = list;
          mark2.last = last;
          return [];
        }
        if (last === mark2.last) return [];
        var at = mark2.last ? list.lastIndexOf(mark2.last) : -1;
        mark2.last = last;
        return list.slice(at + 1);
      }
      function watchBecause() {
        try {
          if (!window.weave || !weave.of) return;
          var fresh = since(seenBecause, weave.of().because || []);
          if (!fresh.length) return;
          var rec = fresh[fresh.length - 1];
          if (!rec || !rec.s) return;
          var kind = "note";
          if (/died|dead|plate/.test(rec.s)) kind = "death";
          else if (/took |stole|pinch/.test(rec.s)) kind = "pinch";
          else if (/white-spot|gold-dust|fin-rot|still-sickness|salt-itch/.test(rec.s)) kind = "ill";
          else if (/bag|took a fish bag|made it right/.test(rec.s)) kind = "bag";
          var all = folk();
          for (var i = 0; i < all.length; i++) {
            if (!all[i] || all[i].dead) continue;
            if (all[i].known || all[i].id === "mae" || kind === "death" || kind === "pinch") {
              file(all[i], kind, rec.s);
            }
          }
        } catch (e) {
        }
      }
      function aisleWet() {
        try {
          if (window.shopSite && shopSite.wet) return shopSite.wet() > 0.36;
        } catch (e) {
        }
        return false;
      }
      function tankIll() {
        try {
          if (window.ill && ill.of) return (ill.of().tank || 0) > 0.2;
        } catch (e) {
        }
        return false;
      }
      function relicN() {
        try {
          if (window.relic && relic.of) {
            var a = relic.of();
            return a && a.n ? a.n : "";
          }
        } catch (e) {
        }
        return "";
      }
      function wrapBrowse() {
        if (didBrowse || !window.shopBrowse) return;
        didBrowse = true;
        var orig = window.shopBrowse;
        window.shopBrowse = function(idx, slot, W, floorY, personS, simT) {
          var rec = orig.apply(this, arguments);
          try {
            if (!rec) return rec;
            var st = window.shopLife && shopLife.browse ? shopLife.browse()[idx] : null;
            var fig = null;
            try {
              if (window.kin && kin.of && rec.name) fig = kin.of(rec.name);
            } catch (e) {
            }
            if (!fig || fig.dead) return rec;
            var so = soulOf(fig);
            var keep = typeof keepGuest === "function" ? keepGuest(st) : !!(st && (st._lateMae || st._goingHold || st._lateKid || st._mask));
            if (rec.phase === "look" && st && !st._soulSaid && !keep) {
              st._soulSaid = 1;
              var wet = aisleWet();
              var illn = tankIll();
              var key = relicN();
              var line2 = "";
              if (so.hate === "the wet boards" && wet) {
                line2 = "I'm not standing in that. I said so last time.";
                rec.phase = "leave";
                st.phase = "leave";
                st.bought = false;
                file(fig, "note", "turned around. the boards were wet");
                because(fig.n.split(" ")[0] + " will not stand in the wet. They said so.");
                try {
                  if (window.kin && kin.hurt) kin.hurt(fig.n, 0.03, "hates the wet");
                } catch (eH) {
                }
              } else if (so.hate === "a sick fish bag" && illn) {
                line2 = "That tank is off. I can see it.";
                rec.phase = "leave";
                st.phase = "leave";
                st.bought = false;
                file(fig, "ill", "saw still-sickness in the glass");
              } else if (so.hate === "something risen") {
                try {
                  var list = typeof allFish === "function" ? allFish() || [] : [];
                  for (var r = 0; r < list.length; r++) {
                    if (list[r] && (list[r].risen || list[r]._risen)) {
                      line2 = "That one came back. I'm not bagging it.";
                      rec.phase = "leave";
                      st.phase = "leave";
                      st.bought = false;
                      break;
                    }
                  }
                } catch (eR) {
                }
              } else if (so.hate === "an empty plate") {
                try {
                  if (window.mark && mark.line && /no plate/.test(mark.line() || "")) {
                    var ml = mark.line() || "";
                    var whoP = ml.split(" ")[0] || "Someone";
                    line2 = whoP + " still has no name on the wall. I can hear it.";
                  }
                } catch (eM) {
                }
              } else if (so.like === "the key in the window" && key) {
                line2 = "That's " + key + ". I came for that, not a fish bag.";
                st.until = Math.max(st.until || 0, now() + 5);
              } else if (so.like === "named lines" && rec.kind === "collector") {
                line2 = rec.line;
              } else if (so.dwell && so.tender < 0.35) {
                line2 = "I keep thinking about it. " + so.dwell.replace(/^Year\s+\d+\.\s*/, "");
              } else if (so.short.length && fig.known) {
                var last = so.short[so.short.length - 1];
                if (last && last.k === "bag") line2 = "The last one is still in the window. You bagged it right.";
              }
              if (line2) {
                var had = rec.line || "";
                var clobber = rec.phase === "leave" || !had || /I'm |Just looking|The usual|Can we get/.test(had);
                if (clobber) {
                  rec.line = line2;
                  st.line = line2;
                  st.lineUntil = now() + 3.8;
                }
              }
              file(fig, "note", wet ? "the aisle was wet" : "looked");
            }
            if (rec.phase === "pay" && st && !st._soulPay && fig) {
              st._soulPay = 1;
              file(fig, "bag", "took a fish bag");
              if (so.like === "a dry aisle" && aisleWet()) {
                rec.phase = "leave";
                st.phase = "leave";
                st.bought = false;
                rec.line = "Not with the floor like that.";
                st.line = rec.line;
              }
            }
          } catch (e) {
          }
          return rec;
        };
      }
      function wrapChoir() {
        if (didChoir || !window.choir || !choir.weather) return;
        didChoir = true;
        var orig = choir.weather;
        choir.weather = function() {
          var w = orig.apply(this, arguments);
          try {
            var all = folk();
            var hard = 0;
            for (var i = 0; i < all.length; i++) {
              if (all[i] && all[i].soul && all[i].soul.tender < 0.28 && all[i].known) hard++;
            }
            if (hard) {
              w.sour = Math.min(1, (w.sour || 0) + Math.min(0.12, hard * 0.04));
              if (!w.line) w.line = "A family is still on that morning.";
            }
          } catch (e) {
          }
          return w;
        };
      }
      function whisper() {
        if (lastGold && now() - lastGoldAt < 12) return lastGold;
        return "";
      }
      function line() {
        var all = folk();
        for (var i = 0; i < all.length; i++) {
          var f = all[i];
          if (!f || f.dead) continue;
          var so = soulOf(f);
          if (so.dwell) {
            return f.n.split(" ")[0] + " still dwells on it. Likes " + so.like + ". Hates " + so.hate + ".";
          }
        }
        for (var j = 0; j < all.length; j++) {
          if (all[j] && !all[j].dead && all[j].id === "mae") {
            var sm = soulOf(all[j]);
            return "Mae likes " + sm.like + ". Hates " + sm.hate + ".";
          }
        }
        return whisper();
      }
      function seedWiki() {
        try {
          var wiki = typeof WIKI === "function" ? WIKI() : window.WIKI;
          if (!wiki || !wiki.push) return;
          if (wiki.some(function(a) {
            return a && a.id === "k_soul";
          }))
            return;
          wiki.push({
            id: "k_soul",
            sec: "You and your people",
            t: "People have inner lives",
            tags: "soul memory like hate dwell person kin",
            w: "<p>The tanks already remember. The street did not. A walk-in has a like, a hate, a short memory, a morning they dwell on. Mae hates the wet, or she likes the key. A person who saw a death hardens. They will not bag from a tank that is off. They will not stand in the puddle they already named. That is not a modifier. That is why they walked.</p><p><b>What to do about it:</b> read Life. The same name comes back with the last morning in their mouth. Keep the aisle dry for the ones who said so.</p>"
          });
        } catch (e) {
        }
      }
      function tick() {
        try {
          wrapBrowse();
          wrapChoir();
          seedWiki();
          if (sceneName() === "title") return;
          seedSouls();
          if (now() - lastTick > 0.9) {
            lastTick = now();
            watchBecause();
          }
        } catch (e) {
        }
      }
      window.soul = {
        whisper,
        line,
        of: function(n) {
          try {
            var f = window.kin && kin.of ? kin.of(n) : null;
            return f ? soulOf(f) : null;
          } catch (e) {
            return null;
          }
        }
      };
      if (window.__onBeat) window.__onBeat(tick, 250);
      else
        setTimeout(function loop() {
          tick();
          setTimeout(loop, 250);
        }, 210);
    })();
  } catch (e) {
    console.error("Layer soul.js failed to start:", e);
  }

  // src/layers/hand.js
  try {
    (function() {
      "use strict";
      var lastTick = 0;
      var lastGold = "";
      var lastGoldAt = 0;
      var lastSales = -1;
      var tillReady = false;
      var didBrowse = false;
      var didChoir = false;
      var didStock = false;
      var didTreat = false;
      function now() {
        return typeof performance !== "undefined" ? performance.now() / 1e3 : Date.now() / 1e3;
      }
      function gs() {
        try {
          if (typeof gameState === "function") return gameState();
          if (typeof gameState === "object" && gameState) return gameState;
        } catch (e) {
        }
        return null;
      }
      function shopDay() {
        try {
          var g = gs();
          if (g && isFinite(g.t)) return Math.floor((g.t % 1e9 + 1e9) % 1e9 / 2400);
        } catch (e) {
        }
        return 0;
      }
      function sceneName() {
        try {
          if (document.body.classList.contains("titling")) return "title";
          if (typeof sceneNow === "function") return String(sceneNow() || "");
        } catch (e) {
        }
        return "shop";
      }
      function clamp01(n) {
        return n < 0 ? 0 : n > 1 ? 1 : n;
      }
      function because(text) {
        if (!text) return;
        try {
          if (window.weave && weave.because) weave.because(text);
        } catch (e) {
        }
        try {
          if (window.desk && desk.think) desk.think("hand", text);
        } catch (e2) {
        }
      }
      function gold(text, force) {
        if (!text) return;
        lastGold = text;
        lastGoldAt = now();
        try {
          var el = document.getElementById("hookWhisper");
          if (el && (force || el.textContent !== text)) {
            el.textContent = text;
            el.classList.add("on", "pop");
          }
        } catch (e) {
        }
      }
      function egg(id, line2) {
        try {
          if (typeof findEgg === "function") findEgg(id, line2);
        } catch (e) {
        }
      }
      function state() {
        var g = gs();
        if (g && g.hand && typeof g.hand.bag === "number") return g.hand;
        var d = shopDay();
        var st = { bag: 0.62, water: 0.55, glass: 0.5, lastBag: d, lastWater: d, lastGlass: d, last: "" };
        try {
          if (g) g.hand = st;
        } catch (e) {
        }
        return st;
      }
      function rustDay() {
        var d = shopDay();
        if (d < 1) return;
        var st = state();
        if (st.rustDay === d) return;
        st.rustDay = d;
        var rusted = [];
        if (d - (st.lastBag || 0) >= 1) {
          st.bag = clamp01(st.bag - 0.07);
          rusted.push("the knot");
          try {
            if (window.craft && craft.noteBag) craft.noteBag(false);
          } catch (e) {
          }
        }
        if (d - (st.lastWater || 0) >= 2) {
          st.water = clamp01(st.water - 0.06);
          rusted.push("the column");
        }
        if (d - (st.lastGlass || 0) >= 2) {
          st.glass = clamp01(st.glass - 0.05);
          rusted.push("the pane");
        }
        if (rusted.length && st.bag < 0.34) {
          var line2 = "Your hands forgot " + rusted[0] + ".";
          st.last = line2;
          because(line2);
          gold(line2, true);
          egg("handrust", line2);
        }
      }
      function noteBag() {
        var st = state();
        st.bag = clamp01(st.bag + 0.05);
        st.lastBag = shopDay();
        st.last = "The knot held.";
        try {
          if (window.craft && craft.noteBag) craft.noteBag(st.bag > 0.42);
        } catch (e) {
        }
      }
      function watchTill() {
        try {
          if (!window.shopLife || !shopLife.day) return;
          var day = shopLife.day();
          if (!day || !isFinite(day.sales)) return;
          if (!tillReady) {
            lastSales = day.sales;
            tillReady = true;
            return;
          }
          if (day.sales === lastSales + 1) {
            lastSales = day.sales;
            noteBag();
          } else if (day.sales !== lastSales) {
            lastSales = day.sales;
          }
        } catch (e) {
        }
      }
      function wrapBrowse() {
        if (didBrowse || !window.shopBrowse) return;
        didBrowse = true;
        var orig = window.shopBrowse;
        window.shopBrowse = function(idx, slot, W, floorY, personS, simT) {
          var rec = orig.apply(this, arguments);
          try {
            if (!rec) return rec;
            var st = window.shopLife && shopLife.browse ? shopLife.browse()[idx] : null;
            var h = state();
            var keep = typeof keepGuest === "function" ? keepGuest(st) : !!(st && (st._lateMae || st._goingHold || st._lateKid || st._mask));
            if (rec.phase === "pay" && st && !st._handPay && !keep) {
              st._handPay = 1;
              if (h.bag < 0.3 && Math.random() < 0.42) {
                rec.phase = "leave";
                st.phase = "leave";
                st.bought = false;
                rec.line = "The last knot was loose. Not this one.";
                st.line = rec.line;
                because((rec.name || "Someone") + " walked. The knot was loose.");
                gold("The knot was loose. They walked.", true);
              } else if (h.bag > 0.78 && rec.kind === "collector") {
                rec.line = rec.line || "Whoever bags here knows their hands.";
                st.line = rec.line;
              }
            }
            if (rec.phase === "look" && st && !st._handSaid && !keep && h.bag < 0.32) {
              st._handSaid = 1;
              rec.line = rec.line || "I saw the last fish bag. The water went everywhere.";
              st.line = rec.line;
              st.lineUntil = now() + 3.2;
            }
          } catch (e) {
          }
          return rec;
        };
      }
      function wrapChoir() {
        if (didChoir || !window.choir || !choir.weather) return;
        didChoir = true;
        var orig = choir.weather;
        choir.weather = function() {
          var w = orig.apply(this, arguments);
          try {
            var h = state();
            if (h.bag < 0.32) {
              w.sour = Math.min(1, (w.sour || 0) + 0.1);
              if (!w.line) w.line = "The knot is rusting.";
            } else if (h.bag > 0.8) {
              w.sweet = Math.min(1, (w.sweet || 0) + 0.08);
            }
          } catch (e) {
          }
          return w;
        };
      }
      function wrapStock() {
        if (didStock || !window.stock || !stock.use) return;
        didStock = true;
        var orig = stock.use;
        stock.use = function(id) {
          var r = orig.apply(this, arguments);
          try {
            var st = state();
            var d = shopDay();
            if (id === "salt" || id === "almond" || id === "carbon" || id === "starter" || id === "cond") {
              st.water = clamp01(st.water + 0.08);
              st.lastWater = d;
              st.last = "The column remembers.";
            }
            if (id === "glass" || id === "chalk") {
              st.glass = clamp01(st.glass + 0.1);
              st.lastGlass = d;
            }
          } catch (e) {
          }
          return r;
        };
      }
      function wrapTreat() {
        if (didTreat || !window.ill || !ill.treat) return;
        didTreat = true;
        var orig = ill.treat;
        ill.treat = function(id) {
          var r = orig.apply(this, arguments);
          try {
            var st = state();
            if (st.water < 0.32) {
              var s = ill.of ? ill.of() : null;
              if (s) s.tank = Math.min(1, (s.tank || 0) + 0.22);
              because("The vial went in. The hands were rusty. The still held.");
              gold("The hands were rusty. The still held.", true);
            } else {
              st.water = clamp01(st.water + 0.05);
              st.lastWater = shopDay();
            }
          } catch (e) {
          }
          return r;
        };
      }
      function whisper() {
        if (lastGold && now() - lastGoldAt < 12) return lastGold;
        return "";
      }
      function line() {
        var st = state();
        var bits = [];
        bits.push("knot " + Math.round(st.bag * 100));
        bits.push("column " + Math.round(st.water * 100));
        bits.push("pane " + Math.round(st.glass * 100));
        if (st.bag < 0.34) return "The knot is rusting. " + bits.join(" · ") + ".";
        if (st.last) return st.last + " " + bits.join(" · ") + ".";
        return bits.join(" · ") + ".";
      }
      function seedWiki() {
        try {
          var wiki = typeof WIKI === "function" ? WIKI() : window.WIKI;
          if (!wiki || !wiki.push) return;
          if (wiki.some(function(a) {
            return a && a.id === "k_hand";
          }))
            return;
          wiki.push({
            id: "k_hand",
            sec: "The shop floor",
            t: "The hands rust",
            tags: "skill rust bag knot hands craft idle",
            w: "<p>The knot goes. Fish bags, water, glass: three hands. A day without a fish bag and the next one is sloppy. People say so. They walk. Treat with rusty water-hands and still-sickness comes back. String a run and the hands remember.</p><p><b>What to do about it:</b> bag. Treat. Wash. Do not tab out for a week and expect the first knot to hold. The gold line will say when the hands forgot.</p>"
          });
        } catch (e) {
        }
      }
      function tick() {
        try {
          wrapBrowse();
          wrapChoir();
          wrapStock();
          wrapTreat();
          seedWiki();
          if (sceneName() === "title") return;
          if (now() - lastTick > 1) {
            lastTick = now();
            rustDay();
            watchTill();
          }
        } catch (e) {
        }
      }
      window.hand = {
        whisper,
        line,
        of: function() {
          return state();
        }
      };
      if (window.__onBeat) window.__onBeat(tick, 280);
      else
        setTimeout(function loop() {
          tick();
          setTimeout(loop, 280);
        }, 230);
    })();
  } catch (e) {
    console.error("Layer hand.js failed to start:", e);
  }

  // src/layers/cut.js
  try {
    (function() {
      "use strict";
      var lastTick = 0;
      var lastUi = 0;
      var lastGold = "";
      var lastGoldAt = 0;
      var seenBecause = {};
      var overlay = null;
      var octx = null;
      var didBrowse = false;
      var didChoir = false;
      var didStock = false;
      function now() {
        return typeof performance !== "undefined" ? performance.now() / 1e3 : Date.now() / 1e3;
      }
      function gs() {
        try {
          if (typeof gameState === "function") return gameState();
          if (typeof gameState === "object" && gameState) return gameState;
        } catch (e) {
        }
        return null;
      }
      function year() {
        try {
          if (window.saga && typeof saga.year === "function") return saga.year();
        } catch (e) {
        }
        return 1e3;
      }
      function shopDay() {
        try {
          var g = gs();
          if (g && isFinite(g.t)) return Math.floor((g.t % 1e9 + 1e9) % 1e9 / 2400);
        } catch (e) {
        }
        return 0;
      }
      function sceneName() {
        try {
          if (document.body.classList.contains("titling")) return "title";
          if (document.body.classList.contains("work")) return "work";
          if (typeof sceneNow === "function") return String(sceneNow() || "");
        } catch (e) {
        }
        return "shop";
      }
      function because(text) {
        if (!text) return;
        try {
          if (window.weave && weave.because) weave.because(text);
        } catch (e) {
        }
        try {
          if (window.desk && desk.think) desk.think("cut", text);
        } catch (e2) {
        }
      }
      function gold(text, force) {
        if (!text) return;
        lastGold = text;
        lastGoldAt = now();
        try {
          var el = document.getElementById("hookWhisper");
          if (el && (force || el.textContent !== text)) {
            el.textContent = text;
            el.classList.add("on", "pop");
          }
        } catch (e) {
        }
      }
      function egg(id, line2) {
        try {
          if (typeof findEgg === "function") findEgg(id, line2);
        } catch (e) {
        }
      }
      function state() {
        var g = gs();
        if (g && g.cut && Array.isArray(g.cut.cuts)) return g.cut;
        var st = { cuts: [], last: "", washed: 0 };
        try {
          if (g) g.cut = st;
        } catch (e) {
        }
        return st;
      }
      function loud(s) {
        s = String(s || "");
        return /died|stole|took |is on the board|name on the wall|white-spot|gold-dust|fin-rot|still-sickness|salt-itch|was born|walked|strange mood|artifact|pinched|made it right|never brought|in the glass|walking the glass|died in a fight|little one is in|brought fry|had a row|is on the aisle|left the till|left a cat|golden age|dark age|was founded|raising a shop|envoy|Haymarket|the .* is on the aisle|is raised|took the |under a keel|harbor is shut|inland town|on the glass|the pact|denounced|without |writing the names|the others follow|brought an edict|taking vows|are here for a pair|houses bind|is .* now|waiting for |has a hook|claims |broke on the aisle|laying a table|said no|did not come back|harbor is cruel|faction emptied|filter clogged/i.test(
          s
        );
      }
      function shortOf(s) {
        s = String(s || "").replace(/^Year\s+\d+\.\s*/, "");
        if (s.length > 42) s = s.slice(0, 40) + "…";
        return s;
      }
      function addCut(text, quiet) {
        if (!text || !loud(text)) return;
        var st = state();
        var s = shortOf(text);
        if (st.cuts.length && st.cuts[st.cuts.length - 1].s === s) return;
        st.cuts.push({ s, y: year(), d: shopDay(), at: now() });
        if (st.cuts.length > 3) st.cuts.shift();
        st.last = s;
        try {
          var g = gs();
          if (g && g.pane && typeof g.pane.etch === "number") {
            g.pane.etch = Math.min(1, g.pane.etch + 0.04);
          }
        } catch (e) {
        }
        if (!quiet) {
          because("The glass cut it: " + s);
          gold("The glass cut it. " + s, true);
          egg("cutglass", s);
        }
      }
      function since(mark2, list) {
        var last = list.length ? list[list.length - 1] : null;
        if (mark2.list !== list) {
          mark2.list = list;
          mark2.last = last;
          return [];
        }
        if (last === mark2.last) return [];
        var at = mark2.last ? list.lastIndexOf(mark2.last) : -1;
        mark2.last = last;
        return list.slice(at + 1);
      }
      function watchBecause() {
        try {
          if (!window.weave || !weave.of) return;
          var fresh = since(seenBecause, weave.of().because || []);
          if (!fresh.length) return;
          var rec = fresh[fresh.length - 1];
          if (rec && rec.s && !/^The glass cut it/.test(rec.s)) addCut(rec.s, false);
        } catch (e) {
        }
      }
      function clarity() {
        try {
          if (window.pane && pane.clarity) return pane.clarity();
        } catch (e) {
        }
        return 0.7;
      }
      function ensureOverlay() {
        if (overlay && overlay.parentNode) return;
        overlay = document.getElementById("cutVeil");
        if (!overlay) {
          overlay = document.createElement("canvas");
          overlay.id = "cutVeil";
          overlay.setAttribute("aria-hidden", "true");
          document.body.appendChild(overlay);
        }
        octx = overlay.getContext("2d");
      }
      function resize() {
        ensureOverlay();
        var dpr = Math.min(2, window.devicePixelRatio || 1);
        var w = window.innerWidth;
        var h = window.innerHeight;
        if (overlay.width !== (w * dpr | 0) || overlay.height !== (h * dpr | 0)) {
          overlay.width = w * dpr | 0;
          overlay.height = h * dpr | 0;
          overlay.style.cssText = "position:fixed;inset:0;width:100%;height:100%;pointer-events:none;z-index:3";
          octx.setTransform(dpr, 0, 0, dpr, 0, 0);
        }
      }
      function drawCuts() {
        var sc = sceneName();
        if (sc === "title" || sc === "work" || sc !== "shop" && sc !== "front") {
          if (octx && overlay) octx.clearRect(0, 0, overlay.width, overlay.height);
          return;
        }
        resize();
        var w = window.innerWidth;
        var h = window.innerHeight;
        octx.clearRect(0, 0, w, h);
        var cuts = state().cuts;
        if (!cuts.length) return;
        var cl = clarity();
        var a = 0.18 + cl * 0.42;
        octx.save();
        octx.font = "italic 600 12px Nunito, serif";
        octx.textAlign = "center";
        octx.fillStyle = "rgba(210, 200, 170," + a.toFixed(3) + ")";
        var x = w * 0.5;
        var y = h * 0.16;
        for (var i = 0; i < cuts.length; i++) {
          octx.fillText(cuts[i].s, x, y + i * 16);
        }
        octx.restore();
      }
      function wrapBrowse() {
        if (didBrowse || !window.shopBrowse) return;
        didBrowse = true;
        var orig = window.shopBrowse;
        window.shopBrowse = function(idx, slot, W, floorY, personS, simT) {
          var rec = orig.apply(this, arguments);
          try {
            if (!rec) return rec;
            var st = window.shopLife && shopLife.browse ? shopLife.browse()[idx] : null;
            var cuts = state().cuts;
            if (!cuts.length) return rec;
            var keep = typeof keepGuest === "function" ? keepGuest(st) : !!(st && (st._lateMae || st._goingHold || st._lateKid || st._mask));
            if (rec.phase === "look" && st && !st._cutSaid && !keep) {
              st._cutSaid = 1;
              var latest = cuts[cuts.length - 1];
              var cl = clarity();
              if (cl < 0.38) {
                rec.line = rec.kind === "kid" ? "There's writing. I can't read it." : "Something's cut in the glass. Can't read it from here.";
              } else {
                rec.line = rec.kind === "kid" ? "It says " + latest.s + "." : 'I saw it from the street. "' + latest.s + '."';
                if (/died|took |still gone/i.test(latest.s) && Math.random() < 0.35) {
                  rec.phase = "leave";
                  st.phase = "leave";
                  st.bought = false;
                } else {
                  st.until = Math.max(st.until || 0, now() + 5);
                }
              }
              st.line = rec.line;
              st.lineUntil = now() + 4;
            }
          } catch (e) {
          }
          return rec;
        };
      }
      function wrapChoir() {
        if (didChoir || !window.choir || !choir.weather) return;
        didChoir = true;
        var orig = choir.weather;
        choir.weather = function() {
          var w = orig.apply(this, arguments);
          try {
            var cuts = state().cuts;
            if (cuts.length) {
              w.sweet = Math.min(1, (w.sweet || 0) + 0.04);
              if (!w.line) w.line = "The glass is keeping " + cuts[cuts.length - 1].s + ".";
            }
          } catch (e) {
          }
          return w;
        };
      }
      function wrapStock() {
        if (didStock || !window.stock || !stock.use) return;
        didStock = true;
        var orig = stock.use;
        stock.use = function(id) {
          var r = orig.apply(this, arguments);
          try {
            if (id === "glass") {
              state().washed = (state().washed || 0) + 1;
              var cuts = state().cuts;
              if (cuts.length) {
                var line2 = "Vinegar took the haze. The morning is still in the glass.";
                because(line2);
                gold(line2, true);
              }
            }
          } catch (e) {
          }
          return r;
        };
      }
      function whisper() {
        if (lastGold && now() - lastGoldAt < 12) return lastGold;
        return "";
      }
      function line() {
        var cuts = state().cuts;
        if (!cuts.length) return whisper();
        return "Cut in the glass: " + cuts.map(function(c) {
          return c.s;
        }).join(" / ") + ".";
      }
      function seedWiki() {
        try {
          var wiki = typeof WIKI === "function" ? WIKI() : window.WIKI;
          if (!wiki || !wiki.push) return;
          if (wiki.some(function(a) {
            return a && a.id === "k_cut";
          }))
            return;
          wiki.push({
            id: "k_cut",
            sec: "The chronicle",
            t: "The glass keeps the morning",
            tags: "etch engraving glass morning history pane",
            w: "<p>Fin's glass was etching as weather. Now it cuts a sentence: who died, who took it, who was born, who was written on the wall. Vinegar cleans the haze. It does not clean the morning. People on Salem read it from the street. They come in because of what it says, or they walk.</p><p><b>What to do about it:</b> wash the window if you want them to read it. Leave the haze if you do not. The cut stays either way. Life lists the mornings.</p>"
          });
        } catch (e) {
        }
      }
      function tick() {
        try {
          wrapBrowse();
          wrapChoir();
          wrapStock();
          seedWiki();
          if (sceneName() === "title") return;
          if (now() - lastTick > 0.8) {
            lastTick = now();
            watchBecause();
          }
          if (now() - lastUi > 0.12) {
            lastUi = now();
            drawCuts();
          }
        } catch (e) {
        }
      }
      window.cut = {
        whisper,
        line,
        of: function() {
          return state().cuts;
        },
        add: function(s) {
          addCut(s, true);
        }
      };
      if (window.__onBeat) window.__onBeat(tick, 90);
      else
        setTimeout(function loop() {
          tick();
          setTimeout(loop, 90);
        }, 180);
    })();
  } catch (e) {
    console.error("Layer cut.js failed to start:", e);
  }

  // src/layers/wane.js
  try {
    (function() {
      "use strict";
      var lastTick = 0;
      var lastUi = 0;
      var lastGold = "";
      var lastGoldAt = 0;
      var overlay = null;
      var octx = null;
      var didBrowse = false;
      var didChoir = false;
      var didFish = false;
      var saidNight = -1;
      function now() {
        return typeof performance !== "undefined" ? performance.now() / 1e3 : Date.now() / 1e3;
      }
      function gs() {
        try {
          if (typeof gameState === "function") return gameState();
          if (typeof gameState === "object" && gameState) return gameState;
        } catch (e) {
        }
        return null;
      }
      function year() {
        try {
          if (window.saga && typeof saga.year === "function") return saga.year();
        } catch (e) {
        }
        return 1e3;
      }
      function hour() {
        try {
          if (typeof jt === "function") return jt() * 24;
          var g = gs();
          if (g && isFinite(g.t)) return (g.t % 2400 + 2400) % 2400 / 100;
        } catch (e) {
        }
        return 12;
      }
      function shopDay() {
        try {
          var g = gs();
          if (g && isFinite(g.t)) return Math.floor((g.t % 1e9 + 1e9) % 1e9 / 2400);
        } catch (e) {
        }
        return 0;
      }
      function night() {
        var h = hour();
        return h < 6.2 || h >= 19.2;
      }
      function sceneName() {
        try {
          if (document.body.classList.contains("titling")) return "title";
          if (document.body.classList.contains("work")) return "work";
          if (typeof sceneNow === "function") return String(sceneNow() || "");
        } catch (e) {
        }
        return "shop";
      }
      function because(text) {
        if (!text) return;
        try {
          if (window.weave && weave.because) weave.because(text);
        } catch (e) {
        }
        try {
          if (window.desk && desk.think) desk.think("wane", text);
        } catch (e2) {
        }
      }
      function egg(id, line2) {
        try {
          if (typeof findEgg === "function") findEgg(id, line2);
        } catch (e) {
        }
      }
      function gold(text, force) {
        if (!text) return;
        lastGold = text;
        lastGoldAt = now();
        try {
          var el = document.getElementById("hookWhisper");
          if (el && (force || el.textContent !== text)) {
            el.textContent = text;
            el.classList.add("on", "pop");
          }
        } catch (e) {
        }
      }
      function due() {
        try {
          if (window.mark && mark.due) return mark.due() || [];
        } catch (e) {
        }
        return [];
      }
      function oldest() {
        var d = due();
        return d.length ? d[0] : null;
      }
      function standing() {
        return !!(night() && oldest());
      }
      function state() {
        var g = gs();
        if (g && g.wane && typeof g.wane === "object") return g.wane;
        var st = { last: "", seen: 0 };
        try {
          if (g) g.wane = st;
        } catch (e) {
        }
        return st;
      }
      function notice() {
        if (!standing()) return;
        var rec = oldest();
        if (!rec || !rec.n) return;
        var d = shopDay();
        if (saidNight === d) return;
        saidNight = d;
        var line2 = rec.n + " is in the glass.";
        state().last = line2;
        state().seen = (state().seen || 0) + 1;
        because(line2);
        gold(line2, true);
        egg("waneghost", line2);
        try {
          if (window.feel && feel.play) feel.play("warn");
        } catch (e) {
        }
      }
      function ensureOverlay() {
        if (overlay && overlay.parentNode) return;
        overlay = document.getElementById("waneVeil");
        if (!overlay) {
          overlay = document.createElement("canvas");
          overlay.id = "waneVeil";
          overlay.setAttribute("aria-hidden", "true");
          document.body.appendChild(overlay);
        }
        octx = overlay.getContext("2d");
      }
      function resize() {
        ensureOverlay();
        var dpr = Math.min(2, window.devicePixelRatio || 1);
        var w = window.innerWidth;
        var h = window.innerHeight;
        if (overlay.width !== (w * dpr | 0) || overlay.height !== (h * dpr | 0)) {
          overlay.width = w * dpr | 0;
          overlay.height = h * dpr | 0;
          overlay.style.cssText = "position:fixed;inset:0;width:100%;height:100%;pointer-events:none;z-index:3";
          octx.setTransform(dpr, 0, 0, dpr, 0, 0);
        }
      }
      function drawGhost() {
        var sc = sceneName();
        if (sc === "title" || sc === "work" || sc !== "shop" && sc !== "front") {
          if (octx && overlay) octx.clearRect(0, 0, overlay.width, overlay.height);
          return;
        }
        resize();
        var w = window.innerWidth;
        var h = window.innerHeight;
        octx.clearRect(0, 0, w, h);
        if (!standing()) return;
        var rec = oldest();
        if (!rec) return;
        var t = now();
        var x = w * (0.52 + Math.sin(t * 0.35) * 0.04);
        var y = h * (0.34 + Math.cos(t * 0.28) * 0.03);
        var a = 0.16 + 0.1 * Math.sin(t * 1.1);
        octx.save();
        octx.translate(x, y);
        octx.globalAlpha = a;
        octx.fillStyle = "rgba(210, 226, 232, 1)";
        octx.beginPath();
        octx.moveTo(-22, 0);
        octx.quadraticCurveTo(-4, -10, 16, -3);
        octx.quadraticCurveTo(22, 0, 16, 4);
        octx.quadraticCurveTo(-4, 10, -22, 0);
        octx.fill();
        octx.beginPath();
        octx.moveTo(-18, 0);
        octx.lineTo(-28, -8);
        octx.lineTo(-26, 0);
        octx.lineTo(-28, 8);
        octx.closePath();
        octx.fill();
        octx.fillStyle = "rgba(40, 70, 88, 0.9)";
        octx.beginPath();
        octx.arc(10, -1.5, 1.6, 0, 7);
        octx.fill();
        octx.restore();
        octx.font = "italic 600 11px Nunito, serif";
        octx.textAlign = "center";
        octx.fillStyle = "rgba(196, 214, 220," + (0.35 + a * 0.8).toFixed(3) + ")";
        octx.fillText(rec.n, x, y + 22);
      }
      function wrapBrowse() {
        if (didBrowse || !window.shopBrowse) return;
        didBrowse = true;
        var orig = window.shopBrowse;
        window.shopBrowse = function(idx, slot, W, floorY, personS, simT) {
          var rec = orig.apply(this, arguments);
          try {
            if (!rec) return rec;
            if (!standing()) return rec;
            var st = window.shopLife && shopLife.browse ? shopLife.browse()[idx] : null;
            var keep = typeof keepGuest === "function" ? keepGuest(st) : !!(st && (st._lateMae || st._goingHold || st._lateKid || st._mask));
            if (keep) return rec;
            var n = oldest();
            n = n && n.n;
            if (!n) return rec;
            if (rec.phase === "look" && st && !st._waneSaid) {
              st._waneSaid = 1;
              rec.line = rec.kind === "kid" ? "Who's that in the window?" : rec.kind === "neighbor" ? n + " is in your glass. I'm not staying." : "There's someone in the glass. I'm going.";
              rec.phase = "leave";
              st.phase = "leave";
              st.bought = false;
              st.line = rec.line;
              st.lineUntil = now() + 3.8;
              because((rec.name || "Someone") + " saw " + n + " in the glass and walked.");
            }
          } catch (e) {
          }
          return rec;
        };
      }
      function wrapChoir() {
        if (didChoir || !window.choir || !choir.weather) return;
        didChoir = true;
        var orig = choir.weather;
        choir.weather = function() {
          var w = orig.apply(this, arguments);
          try {
            if (standing()) {
              var n = oldest();
              w.sour = Math.min(1, (w.sour || 0) + 0.14);
              if (n && n.n && !w.line) w.line = n.n + " is walking the glass.";
            }
          } catch (e) {
          }
          return w;
        };
      }
      function wrapFish() {
        if (didFish || typeof window.drawFishSprite !== "function") return;
        didFish = true;
        var orig = window.drawFishSprite;
        window.drawFishSprite = function(a) {
          try {
            if (standing() && a && a.fish && a.fish.nick) {
              a.fish._holdStill = 1;
            }
          } catch (e) {
          }
          return orig.apply(this, arguments);
        };
      }
      function whisper() {
        if (lastGold && now() - lastGoldAt < 12) return lastGold;
        if (standing()) {
          var rec = oldest();
          if (rec && rec.n) return rec.n + " is walking the glass.";
        }
        return "";
      }
      function line() {
        var rec = oldest();
        if (standing() && rec) return rec.n + " is in the glass. Chalk would rest them.";
        if (rec) return rec.n + " has no name on the wall. Night will bring them to the glass.";
        return state().last || whisper();
      }
      function seedWiki() {
        try {
          var wiki = typeof WIKI === "function" ? WIKI() : window.WIKI;
          if (!wiki || !wiki.push) return;
          if (wiki.some(function(a) {
            return a && a.id === "k_wane";
          }))
            return;
          wiki.push({
            id: "k_wane",
            sec: "The chronicle",
            t: "The unwritten walk the glass",
            tags: "ghost haunt slab plate night ingum glass unburied",
            w: "<p>Fin's haunt was a count, a sour note, an empty nail. Ingum has been dead since Year 412. At night she is in the shop window. People leave. Named fish hold still. The choir keeps a wronger note. Chalk writes the name. She rests.</p><p><b>What to do about it:</b> Harbor Supply, counter chalk. Write the oldest name before last hour. Life lists who is still walking. Do not raise them instead. That is a different wrong note.</p>"
          });
        } catch (e) {
        }
      }
      function tick() {
        try {
          wrapBrowse();
          wrapChoir();
          wrapFish();
          seedWiki();
          if (sceneName() === "title") return;
          if (now() - lastTick > 0.8) {
            lastTick = now();
            notice();
          }
          if (now() - lastUi > 0.1) {
            lastUi = now();
            drawGhost();
          }
        } catch (e) {
        }
      }
      window.wane = {
        whisper,
        line,
        of: function() {
          return oldest();
        },
        standing,
        seed: function() {
          var rec = oldest();
          if (!rec) {
            try {
              if (window.mark && mark.want) mark.want("Ingum", "has no name on the wall", "the shop", 412, true);
            } catch (e) {
            }
            rec = oldest();
          }
          if (!rec) return null;
          saidNight = -1;
          notice();
          if (!lastGold) gold(rec.n + " is in the glass.", true);
          return rec;
        }
      };
      if (window.__onBeat) window.__onBeat(tick, 90);
      else
        setTimeout(function loop() {
          tick();
          setTimeout(loop, 90);
        }, 180);
    })();
  } catch (e) {
    console.error("Layer wane.js failed to start:", e);
  }

  // src/layers/spiral.js
  try {
    (function() {
      "use strict";
      var lastTick = 0;
      var lastGold = "";
      var lastGoldAt = 0;
      var didBrowse = false;
      var didChoir = false;
      var didFish = false;
      var lastPair = /* @__PURE__ */ Object.create(null);
      function now() {
        return typeof performance !== "undefined" ? performance.now() / 1e3 : Date.now() / 1e3;
      }
      function gs() {
        try {
          if (typeof gameState === "function") return gameState();
          if (typeof gameState === "object" && gameState) return gameState;
        } catch (e) {
        }
        return null;
      }
      function year() {
        try {
          if (window.saga && typeof saga.year === "function") return saga.year();
        } catch (e) {
        }
        return 1e3;
      }
      function shopDay() {
        try {
          var g = gs();
          if (g && isFinite(g.t)) return Math.floor((g.t % 1e9 + 1e9) % 1e9 / 2400);
        } catch (e) {
        }
        return 0;
      }
      function sceneName() {
        try {
          if (document.body.classList.contains("titling")) return "title";
          if (typeof sceneNow === "function") return String(sceneNow() || "");
        } catch (e) {
        }
        return "tank";
      }
      function because(text) {
        if (!text) return;
        try {
          if (window.weave && weave.because) weave.because(text);
        } catch (e) {
        }
        try {
          if (window.desk && desk.think) desk.think("spiral", text);
        } catch (e2) {
        }
      }
      function egg(id, line2) {
        try {
          if (typeof findEgg === "function") findEgg(id, line2);
        } catch (e) {
        }
      }
      function gold(text, force) {
        if (!text) return;
        lastGold = text;
        lastGoldAt = now();
        try {
          var el = document.getElementById("hookWhisper");
          if (el && (force || el.textContent !== text)) {
            el.textContent = text;
            el.classList.add("on", "pop");
          }
        } catch (e) {
        }
      }
      function fishList() {
        try {
          if (typeof allFish === "function") return allFish() || [];
        } catch (e) {
        }
        return [];
      }
      function stressOf(f) {
        if (!f) return 0;
        if (f.mind && isFinite(f.mind.stress)) return f.mind.stress;
        if (f.brain && isFinite(f.brain.stress)) return f.brain.stress;
        return 0;
      }
      function proudOf(f) {
        if (!f) return 0;
        var t = f.traits || {};
        return Math.max(t.proud || 0, t.aggro || 0, t.violent || 0, t.anger || 0);
      }
      function callOf(f) {
        return f && (f.nick || "") || "One";
      }
      function kindOf(f) {
        if (!f) return "fish";
        try {
          var S = typeof O !== "undefined" ? O : typeof SPECIES !== "undefined" ? SPECIES : null;
          if (S && f.sp != null && S[f.sp]) {
            var nm = S[f.sp].gname || S[f.sp].name || S[f.sp].vname;
            if (nm) return String(nm).toLowerCase();
          }
        } catch (e) {
        }
        return "fish";
      }
      function state() {
        var g = gs();
        if (g && g.spiral && typeof g.spiral === "object") return g.spiral;
        var st = { last: "", lastDay: -1, dead: [] };
        try {
          if (g) g.spiral = st;
        } catch (e) {
        }
        return st;
      }
      function takeOut(pick) {
        if (!pick) return false;
        pick.dead = 1;
        var list = fishList();
        var arrays = [list];
        try {
          var g = gs();
          if (g && Array.isArray(g.fish) && g.fish !== list) arrays.push(g.fish);
        } catch (e) {
        }
        for (var i = 0; i < arrays.length; i++) {
          var ix = arrays[i].indexOf(pick);
          if (ix >= 0) arrays[i].splice(ix, 1);
        }
        return true;
      }
      function stillMate(sp, killer) {
        var list = fishList();
        for (var i = 0; i < list.length; i++) {
          var f = list[i];
          if (!f || f === killer || f.dead) continue;
          if (f.sp !== sp) continue;
          f._holdStill = 1;
          f._foe = killer && killer.nick || f._foe || "a rival";
          try {
            if (typeof initBrain === "function") initBrain(f);
          } catch (e) {
          }
          if (f.brain) f.brain.stress = Math.min(1, Math.max(f.brain.stress || 0, 0.78));
          if (f.mind) f.mind.stress = Math.min(1, Math.max(f.mind.stress || 0, 0.78));
        }
      }
      function kill(victim, killer, how) {
        if (!victim) return null;
        var nick = callOf(victim);
        var sp = kindOf(victim);
        var who = killer ? callOf(killer) : "a rival";
        how = how || "died in a fight";
        takeOut(victim);
        stillMate(victim.sp, killer);
        var rec = {
          n: nick,
          sp,
          how,
          by: who,
          y: year(),
          day: shopDay()
        };
        var st = state();
        st.last = nick + " died in the tank. " + who + " is still.";
        st.lastDay = shopDay();
        st.dead.push(rec);
        if (st.dead.length > 12) st.dead.shift();
        try {
          if (window.saga && saga.book) {
            var b = saga.book();
            if (b && Array.isArray(b.dead)) {
              b.dead.push({ n: nick, sp, how, y: year(), saw: who !== "One" ? [who] : [] });
              if (b.dead.length > 80) b.dead.shift();
            }
          }
        } catch (eB) {
        }
        try {
          if (window.saga && saga.remember && killer) saga.remember(killer, "fight", nick + " dying");
        } catch (eR) {
        }
        try {
          if (window.mark && mark.want) mark.want(nick, how, "the tank", year(), true);
        } catch (eM) {
        }
        because(nick + " " + how + ". " + who + " is holding still.");
        gold(nick + " died in the tank. " + who + " is still.", true);
        egg("spiralkill", nick + " died in a fight.");
        try {
          if (window.feel && feel.play) feel.play("warn");
        } catch (eF) {
        }
        try {
          if (typeof chronicle === "function") {
            chronicle("death", "Year " + year() + ". " + nick + " " + how + ". " + who + " was nearby.", killer ? [killer] : []);
          }
        } catch (eC) {
        }
        return rec;
      }
      function ready(f) {
        if (!f || f.dead || f.stolen || f.risen) return false;
        if (f.stage != null && f.stage < 2) return false;
        var s = stressOf(f);
        if (s <= 0.72) return false;
        if (f._foe) return true;
        if (proudOf(f) > 0.62) return true;
        return false;
      }
      function watchFights() {
        if (shopDay() < 1) return;
        var st = state();
        if (st.lastDay === shopDay()) return;
        var list = fishList();
        if (list.length < 2) return;
        var i, j, A, B, key, dx, dy;
        for (i = 0; i < list.length; i++) {
          A = list[i];
          if (!ready(A)) continue;
          for (j = 0; j < list.length; j++) {
            if (j === i) continue;
            B = list[j];
            if (!B || B.dead || B.sp !== A.sp) continue;
            if (B.stage != null && B.stage < 2) continue;
            key = (A.fid || A.nick || i) + "|" + (B.fid || B.nick || j);
            if (lastPair[key]) continue;
            dx = (A.x || 0) - (B.x || 0);
            dy = (A.y || 0) - (B.y || 0);
            if (dx * dx + dy * dy > 90 * 90 && !A._foe) continue;
            lastPair[key] = 1;
            var victim = stressOf(B) >= stressOf(A) ? B : A;
            var killer = victim === A ? B : A;
            if (A._foe && B.nick && A._foe === B.nick) {
              victim = A;
              killer = B;
            }
            kill(victim, killer, "died in a fight");
            return;
          }
        }
      }
      function wrapBrowse() {
        if (didBrowse || !window.shopBrowse) return;
        didBrowse = true;
        var orig = window.shopBrowse;
        window.shopBrowse = function(idx, slot, W, floorY, personS, simT) {
          var rec = orig.apply(this, arguments);
          try {
            if (!rec) return rec;
            var st = window.shopLife && shopLife.browse ? shopLife.browse()[idx] : null;
            var keep = typeof keepGuest === "function" ? keepGuest(st) : !!(st && (st._lateMae || st._goingHold || st._lateKid || st._mask));
            var last = state().last;
            if (!last || keep) return rec;
            if (rec.phase === "look" && st && !st._spiralSaid && shopDay() - (state().lastDay || 0) <= 1) {
              st._spiralSaid = 1;
              rec.line = rec.kind === "kid" ? "One of them is gone." : rec.kind === "collector" ? "The line just broke. I can see it." : "Something died in there. I'm looking.";
              st.line = rec.line;
              st.lineUntil = now() + 3.8;
              if (rec.kind !== "collector" && Math.random() < 0.4) {
                rec.phase = "leave";
                st.phase = "leave";
                st.bought = false;
              }
            }
          } catch (e) {
          }
          return rec;
        };
      }
      function wrapChoir() {
        if (didChoir || !window.choir || !choir.weather) return;
        didChoir = true;
        var orig = choir.weather;
        choir.weather = function() {
          var w = orig.apply(this, arguments);
          try {
            if (state().lastDay === shopDay() && state().last) {
              w.sour = Math.min(1, (w.sour || 0) + 0.18);
              w.still = (w.still || 0) + 1;
              if (!w.line) w.line = state().last;
            }
          } catch (e) {
          }
          return w;
        };
      }
      function wrapFish() {
        if (didFish || typeof window.drawFishSprite !== "function") return;
        didFish = true;
        var orig = window.drawFishSprite;
        window.drawFishSprite = function(a) {
          try {
            if (a && a.fish && a.fish._holdStill && state().lastDay === shopDay()) {
              a.fish._holdStill = 1;
            }
          } catch (e) {
          }
          return orig.apply(this, arguments);
        };
      }
      function whisper() {
        if (lastGold && now() - lastGoldAt < 12) return lastGold;
        return "";
      }
      function line() {
        if (state().last) return state().last;
        return whisper();
      }
      function seedWiki() {
        try {
          var wiki = typeof WIKI === "function" ? WIKI() : window.WIKI;
          if (!wiki || !wiki.push) return;
          if (wiki.some(function(a) {
            return a && a.id === "k_spiral";
          }))
            return;
          wiki.push({
            id: "k_spiral",
            sec: "The tanks",
            t: "A fight can kill",
            tags: "tantrum spiral fight stress proud foe death gravel",
            w: "<p>Fin's fights wrote the book and raised the pulse. They did not empty a slot. Two of the same fish, proud, a foe already named, the pulse over the line: one dies on the gravel. The name goes in the book. The remaining holds still. Walk-ins see it. The choir drops a voice.</p><p><b>What to do about it:</b> split a pair that is already fighting. Feed. Do not leave two proud ones in a corner overnight. Life names who died. Chalk is due. The gold line does not cheer you.</p>"
          });
        } catch (e) {
        }
      }
      function tick() {
        try {
          wrapBrowse();
          wrapChoir();
          wrapFish();
          seedWiki();
          if (sceneName() === "title") return;
          if (now() - lastTick > 1.1) {
            lastTick = now();
            watchFights();
          }
        } catch (e) {
        }
      }
      window.spiral = {
        whisper,
        line,
        of: function() {
          return state();
        },
        seed: function() {
          var list = fishList();
          var a = null, b = null;
          var i;
          for (i = 0; i < list.length; i++) {
            if (!list[i] || list[i].dead) continue;
            if (!a) {
              a = list[i];
              continue;
            }
            if (list[i].sp === a.sp) {
              b = list[i];
              break;
            }
          }
          if (!a) return null;
          if (!b) {
            for (i = 0; i < list.length; i++) {
              if (list[i] && list[i] !== a && !list[i].dead) {
                b = list[i];
                break;
              }
            }
          }
          if (!b) return null;
          if (a.stage != null && a.stage < 2) a.stage = 2;
          if (b.stage != null && b.stage < 2) b.stage = 2;
          try {
            if (typeof initBrain === "function") {
              initBrain(a);
              initBrain(b);
            }
          } catch (e) {
          }
          a._foe = b.nick || "a rival";
          b._foe = a.nick || "a rival";
          if (a.brain) a.brain.stress = 0.88;
          if (b.brain) b.brain.stress = 0.8;
          if (a.mind) a.mind.stress = 0.88;
          if (b.mind) b.mind.stress = 0.8;
          state().lastDay = -1;
          return kill(a, b, "died in a fight");
        }
      };
      if (window.__onBeat) window.__onBeat(tick, 240);
      else
        setTimeout(function loop() {
          tick();
          setTimeout(loop, 240);
        }, 220);
    })();
  } catch (e) {
    console.error("Layer spiral.js failed to start:", e);
  }

  // src/layers/blood.js
  try {
    (function() {
      "use strict";
      var lastTick = 0;
      var lastGold = "";
      var lastGoldAt = 0;
      var didBrowse = false;
      var didChoir = false;
      var didFish = false;
      function now() {
        return typeof performance !== "undefined" ? performance.now() / 1e3 : Date.now() / 1e3;
      }
      function gs() {
        try {
          if (typeof gameState === "function") return gameState();
          if (typeof gameState === "object" && gameState) return gameState;
        } catch (e) {
        }
        return null;
      }
      function year() {
        try {
          if (window.saga && typeof saga.year === "function") return saga.year();
        } catch (e) {
        }
        return 1e3;
      }
      function shopDay() {
        try {
          var g = gs();
          if (g && isFinite(g.t)) return Math.floor((g.t % 1e9 + 1e9) % 1e9 / 2400);
        } catch (e) {
        }
        return 0;
      }
      function sceneName() {
        try {
          if (document.body.classList.contains("titling")) return "title";
          if (typeof sceneNow === "function") return String(sceneNow() || "");
        } catch (e) {
        }
        return "shop";
      }
      function because(text) {
        if (!text) return;
        try {
          if (window.weave && weave.because) weave.because(text);
        } catch (e) {
        }
        try {
          if (window.desk && desk.think) desk.think("blood", text);
        } catch (e2) {
        }
      }
      function egg(id, line2) {
        try {
          if (typeof findEgg === "function") findEgg(id, line2);
        } catch (e) {
        }
      }
      function gold(text, force) {
        if (!text) return;
        lastGold = text;
        lastGoldAt = now();
        try {
          var el = document.getElementById("hookWhisper");
          if (el && (force || el.textContent !== text)) {
            el.textContent = text;
            el.classList.add("on", "pop");
          }
        } catch (e) {
        }
      }
      function fishList() {
        try {
          if (typeof allFish === "function") return allFish() || [];
        } catch (e) {
        }
        return [];
      }
      function homes() {
        try {
          if (window.going && going.homes) return going.homes() || [];
        } catch (e) {
        }
        return [];
      }
      function state() {
        var g = gs();
        if (g && g.blood && typeof g.blood === "object") return g.blood;
        var st = { last: "", kids: [] };
        try {
          if (g) g.blood = st;
        } catch (e) {
        }
        return st;
      }
      function spIndex(name) {
        var S = null;
        try {
          S = typeof SPECIES !== "undefined" ? SPECIES : typeof O !== "undefined" ? O : null;
        } catch (e) {
        }
        if (!S || !S.length) return 0;
        var q = String(name || "").toLowerCase();
        var i, n;
        for (i = 0; i < S.length; i++) {
          n = String(S[i] && (S[i].name || S[i].gname || S[i].vname) || "").toLowerCase();
          if (n === q) return i;
        }
        for (i = 0; i < S.length; i++) {
          n = String(S[i] && (S[i].name || S[i].gname || S[i].key || S[i].vname) || "").toLowerCase();
          if (n && (n.indexOf(q) >= 0 || q.indexOf(n) >= 0)) return i;
        }
        return 0;
      }
      function fidByNick(n) {
        if (!n) return "";
        try {
          var list = fishList();
          for (var i = 0; i < list.length; i++) {
            if (list[i] && list[i].nick === n && list[i].fid) return list[i].fid;
          }
        } catch (e) {
        }
        try {
          if (typeof pedState === "function") {
            var ps = pedState();
            for (var k2 in ps) {
              if (ps[k2] && ps[k2].nick === n) return ps[k2].id || k2;
            }
          }
        } catch (e2) {
        }
        return "";
      }
      function takeIn(home) {
        if (!home || home._blood) return null;
        if (typeof addFish !== "function") return null;
        var f = addFish(spIndex(home.sp), 1);
        if (!f) {
          home._bloodTry = (home._bloodTry || 0) + 1;
          return null;
        }
        home._blood = 1;
        f.stage = 1;
        f._bloodMom = home.nick || home.sp;
        f._bloodWho = home.who || "";
        try {
          if (typeof initBrain === "function") initBrain(f);
        } catch (e) {
        }
        var momFid = fidByNick(home.nick);
        try {
          if (typeof pedNew === "function" && !f.fid) pedNew(f);
        } catch (eP) {
        }
        try {
          if (typeof pedOf === "function" && f.fid) {
            var rec = pedOf(f.fid);
            if (rec) {
              if (momFid) rec.mom = momFid;
              rec.born = rec.born || year();
            }
          }
        } catch (e2) {
        }
        try {
          if (typeof pedRename === "function") pedRename(f);
        } catch (e3) {
        }
        try {
          if (window.saga && saga.remember) saga.remember(f, "birth", "being born of " + (home.nick || home.sp));
        } catch (e4) {
        }
        var call = f.nick || "The little one";
        var of = home.nick || home.sp;
        var line2 = call + " is in the water. Off " + of + ".";
        var st = state();
        st.last = line2;
        st.kids.push({ n: call, of, who: home.who || "", y: year(), day: shopDay() });
        if (st.kids.length > 10) st.kids.shift();
        because((home.who || "Someone") + " brought fry off " + of + ". " + call + " is in the tank.");
        gold("The little one is in the water. Off " + of + ".", true);
        egg("bloodfry", line2);
        try {
          if (typeof addRep === "function") addRep(1);
        } catch (eR) {
        }
        return f;
      }
      function watchHomes() {
        var hs = homes();
        for (var i = 0; i < hs.length; i++) {
          var h = hs[i];
          if (!h || h.dead || h._blood) continue;
          if (h.fry !== 2) continue;
          if ((h._bloodTry || 0) > 4) continue;
          takeIn(h);
          return;
        }
      }
      function wrapBrowse() {
        if (didBrowse || !window.shopBrowse) return;
        didBrowse = true;
        var orig = window.shopBrowse;
        window.shopBrowse = function(idx, slot, W, floorY, personS, simT) {
          var rec = orig.apply(this, arguments);
          try {
            if (!rec) return rec;
            watchHomes();
            var st = window.shopLife && shopLife.browse ? shopLife.browse()[idx] : null;
            var keep = typeof keepGuest === "function" ? keepGuest(st) : !!(st && (st._lateMae || st._goingHold || st._lateKid || st._mask));
            if (keep) return rec;
            var kids = state().kids;
            if (!kids.length) return rec;
            var last = kids[kids.length - 1];
            if (rec.phase === "look" && st && !st._bloodSaid && last && last.of) {
              if (rec.kind === "collector" || rec.kind === "neighbor" || Math.random() < 0.2) {
                st._bloodSaid = 1;
                rec.line = rec.kind === "kid" ? "The little one. Off " + last.of + "?" : "Is that one of " + last.of + "'s?";
                st.line = rec.line;
                st.lineUntil = now() + 3.8;
                because((rec.name || "Someone") + " asked if it was of " + last.of + ".");
              }
            }
          } catch (e) {
          }
          return rec;
        };
      }
      function wrapChoir() {
        if (didChoir || !window.choir || !choir.weather) return;
        didChoir = true;
        var orig = choir.weather;
        choir.weather = function() {
          var w = orig.apply(this, arguments);
          try {
            var kids = state().kids;
            if (kids.length && kids[kids.length - 1].day === shopDay()) {
              w.sweet = Math.min(1, (w.sweet || 0) + 0.06);
              if (!w.line) w.line = "A little one of " + kids[kids.length - 1].of + " is in the water.";
            }
          } catch (e) {
          }
          return w;
        };
      }
      function wrapFish() {
        if (didFish || typeof window.drawFishSprite !== "function") return;
        didFish = true;
        var orig = window.drawFishSprite;
        window.drawFishSprite = function(a) {
          try {
            if (a && a.fish && a.fish._bloodMom) {
              a.fish._gaze = a.fish._gaze || now();
            }
          } catch (e) {
          }
          return orig.apply(this, arguments);
        };
      }
      function whisper() {
        if (lastGold && now() - lastGoldAt < 12) return lastGold;
        return "";
      }
      function line() {
        if (state().last) return state().last;
        return whisper();
      }
      function seedWiki() {
        try {
          var wiki = typeof WIKI === "function" ? WIKI() : window.WIKI;
          if (!wiki || !wiki.push) return;
          if (wiki.some(function(a) {
            return a && a.id === "k_blood";
          }))
            return;
          wiki.push({
            id: "k_blood",
            sec: "The shop floor",
            t: "The little one comes home",
            tags: "fry blood line pedigree child bag window mae collector",
            w: "<p>Fin's fry was speech. Mae said she brought the little one. Nothing entered the water. The fish bag that went home can send a child back: same kind, a mother already in the book, a collector who can tell. They will ask if that one is of Glimia's. It is.</p><p><b>What to do about it:</b> keep a space in the tank. Do not sell the last of a line and expect the child to mean nothing. Life names who came back. The gold line does not cheer you — it tells you the water took them.</p>"
          });
        } catch (e) {
        }
      }
      function tick() {
        try {
          wrapBrowse();
          wrapChoir();
          wrapFish();
          seedWiki();
          if (sceneName() === "title") return;
          if (now() - lastTick > 1.2) {
            lastTick = now();
            watchHomes();
          }
        } catch (e) {
        }
      }
      window.blood = {
        whisper,
        line,
        of: function() {
          return state();
        },
        seed: function() {
          var hs = homes();
          var hit = null;
          var i;
          for (i = 0; i < hs.length; i++) {
            if (hs[i] && !hs[i].dead) {
              hit = hs[i];
              break;
            }
          }
          if (!hit) {
            var list = fishList();
            var src = null;
            for (i = 0; i < list.length; i++) {
              if (list[i] && list[i].nick) {
                src = list[i];
                break;
              }
            }
            if (!src && list[0]) src = list[0];
            if (!src) return null;
            var sp = "goldfish";
            try {
              var S = typeof O !== "undefined" ? O : null;
              if (S && src.sp != null && S[src.sp]) sp = String(S[src.sp].gname || S[src.sp].name || "goldfish").toLowerCase();
            } catch (e) {
            }
            hit = {
              nick: src.nick || "",
              sp,
              who: "Mae Costa",
              fry: 2,
              dead: false
            };
            try {
              if (window.going && going.homes) going.homes().push(hit);
            } catch (eH) {
            }
          }
          hit.fry = 2;
          hit._blood = 0;
          hit._bloodTry = 0;
          return takeIn(hit);
        }
      };
      if (window.__onBeat) window.__onBeat(tick, 260);
      else
        setTimeout(function loop() {
          tick();
          setTimeout(loop, 260);
        }, 220);
    })();
  } catch (e) {
    console.error("Layer blood.js failed to start:", e);
  }

  // src/layers/stray.js
  try {
    (function() {
      "use strict";
      var lastTick = 0;
      var lastUi = 0;
      var lastGold = "";
      var lastGoldAt = 0;
      var overlay = null;
      var octx = null;
      var didBrowse = false;
      var didChoir = false;
      var didFish = false;
      var NAMES = ["Moth", "Soot", "Ink", "Rill", "Pebble", "Ash", "Nettle", "Cinder"];
      function now() {
        return typeof performance !== "undefined" ? performance.now() / 1e3 : Date.now() / 1e3;
      }
      function gs() {
        try {
          if (typeof gameState === "function") return gameState();
          if (typeof gameState === "object" && gameState) return gameState;
        } catch (e) {
        }
        return null;
      }
      function year() {
        try {
          if (window.saga && typeof saga.year === "function") return saga.year();
        } catch (e) {
        }
        return 1e3;
      }
      function shopDay() {
        try {
          var g = gs();
          if (g && isFinite(g.t)) return Math.floor((g.t % 1e9 + 1e9) % 1e9 / 2400);
        } catch (e) {
        }
        return 0;
      }
      function sceneName() {
        try {
          if (document.body.classList.contains("titling")) return "title";
          if (document.body.classList.contains("work")) return "work";
          if (typeof sceneNow === "function") return String(sceneNow() || "");
        } catch (e) {
        }
        return "shop";
      }
      function hash32(s) {
        var h = 2166136261;
        s = String(s || "");
        for (var i = 0; i < s.length; i++) {
          h ^= s.charCodeAt(i);
          h = Math.imul(h, 16777619);
        }
        return h >>> 0;
      }
      function because(text) {
        if (!text) return;
        try {
          if (window.weave && weave.because) weave.because(text);
        } catch (e) {
        }
        try {
          if (window.desk && desk.think) desk.think("stray", text);
        } catch (e2) {
        }
      }
      function egg(id, line2) {
        try {
          if (typeof findEgg === "function") findEgg(id, line2);
        } catch (e) {
        }
      }
      function gold(text, force) {
        if (!text) return;
        lastGold = text;
        lastGoldAt = now();
        try {
          var el = document.getElementById("hookWhisper");
          if (el && (force || el.textContent !== text)) {
            el.textContent = text;
            el.classList.add("on", "pop");
          }
        } catch (e) {
        }
      }
      function keeper() {
        try {
          if (window.late && late.keeper) return late.keeper();
        } catch (e) {
        }
        return null;
      }
      function keeperFirst() {
        var k2 = keeper();
        if (!k2 || !k2.n) return "Nedda";
        return k2.n.split(" ")[0];
      }
      function state() {
        var g = gs();
        if (g && g.stray && g.stray.n) return g.stray;
        var k2 = keeper();
        var h = hash32("stray:" + (k2 && k2.n || "Nedda") + ":" + year());
        var st = {
          n: NAMES[h % NAMES.length],
          alive: true,
          hunger: 0.18,
          lastBagDay: 0,
          lastBags: -1,
          said: false,
          last: "",
          hunted: 0,
          left: false,
          who: k2 && k2.n || "Nedda Quill"
        };
        try {
          if (g) g.stray = st;
        } catch (e) {
        }
        return st;
      }
      function notice() {
        var st = state();
        if (!st.alive || st.said) return;
        st.said = true;
        var line2 = keeperFirst() + " left a cat. " + st.n + " is on the till.";
        st.last = line2;
        because(line2);
        gold(line2, true);
        egg("stray", line2);
      }
      function watchBags() {
        var st = state();
        if (!st.alive) return;
        try {
          var bags = 0;
          if (window.hookRun) {
            var h = hookRun();
            if (h && isFinite(h.bags)) bags = h.bags;
          }
          if (st.lastBags == null || st.lastBags < 0) {
            st.lastBags = bags;
            return;
          }
          if (bags > st.lastBags) {
            st.hunger = Math.max(0, (st.hunger || 0) - 0.28 * (bags - st.lastBags));
            st.lastBagDay = shopDay();
            st.lastBags = bags;
          }
        } catch (e) {
        }
      }
      function hunt() {
        var st = state();
        if (!st.alive || st.hunger > 0.62) return;
        var tank = 0;
        try {
          if (window.ill && ill.of) tank = ill.of().tank || 0;
        } catch (e) {
        }
        if (tank < 0.12) return;
        try {
          var list = typeof allFish === "function" ? allFish() || [] : [];
          for (var i = 0; i < list.length; i++) {
            var f = list[i];
            if (!f || f.dead) continue;
            if (f.sick || f.ill) {
              f.sick = Math.max(0, (f.sick || 0) * 0.35);
              if (f.sick < 0.1) {
                f.sick = 0;
                f.ill = 0;
              }
              break;
            }
          }
          if (window.ill && ill.of) {
            var il = ill.of();
            il.tank = Math.max(0, (il.tank || 0) * 0.72);
          }
        } catch (e2) {
        }
        st.hunted = (st.hunted || 0) + 1;
        var line2 = st.n + " took the flashing off the glass.";
        st.last = line2;
        because(line2);
        gold(line2, true);
        egg("strayhunt", line2);
      }
      function leave() {
        var st = state();
        if (!st.alive) return;
        st.alive = false;
        st.left = true;
        var line2 = st.n + " left. The till was quiet.";
        st.last = line2;
        because(line2);
        gold(line2, true);
        egg("straygone", line2);
        try {
          if (window.kin && kin.hurt) kin.hurt("Mae Costa", 0.1, st.n + " left");
        } catch (e) {
        }
        try {
          if (window.feel && feel.play) feel.play("warn");
        } catch (e2) {
        }
      }
      function tickDay() {
        var st = state();
        if (!st.alive) return;
        var d = shopDay();
        if (st._day === d) return;
        st._day = d;
        if (d < 1) return;
        if (d - (st.lastBagDay || 0) >= 1) {
          st.hunger = Math.min(1, (st.hunger || 0) + 0.2);
        }
        if (st.hunger > 0.55 && st.hunger < 0.86) hunt();
        else if (Math.random() < 0.35) hunt();
        if (st.hunger > 0.86) leave();
      }
      function ensureOverlay() {
        if (overlay && overlay.parentNode) return;
        overlay = document.getElementById("strayVeil");
        if (!overlay) {
          overlay = document.createElement("canvas");
          overlay.id = "strayVeil";
          overlay.setAttribute("aria-hidden", "true");
          document.body.appendChild(overlay);
        }
        octx = overlay.getContext("2d");
      }
      function resize() {
        ensureOverlay();
        var dpr = Math.min(2, window.devicePixelRatio || 1);
        var w = window.innerWidth;
        var h = window.innerHeight;
        if (overlay.width !== (w * dpr | 0) || overlay.height !== (h * dpr | 0)) {
          overlay.width = w * dpr | 0;
          overlay.height = h * dpr | 0;
          overlay.style.cssText = "position:fixed;inset:0;width:100%;height:100%;pointer-events:none;z-index:3";
          octx.setTransform(dpr, 0, 0, dpr, 0, 0);
        }
      }
      function drawCat() {
        var sc = sceneName();
        if (sc === "title" || sc === "work" || sc !== "shop" && sc !== "front") {
          if (octx && overlay) octx.clearRect(0, 0, overlay.width, overlay.height);
          return;
        }
        resize();
        var w = window.innerWidth;
        var h = window.innerHeight;
        octx.clearRect(0, 0, w, h);
        var st = state();
        if (!st.alive) return;
        var x = w * 0.68;
        var y = h * 0.26;
        var t = now();
        var hungry = st.hunger > 0.55;
        var a = hungry ? 0.5 : 0.86;
        octx.save();
        octx.translate(x, y);
        octx.fillStyle = "rgba(18,12,8," + a.toFixed(3) + ")";
        octx.beginPath();
        octx.ellipse(0, 8, 14, 9, 0, 0, 7);
        octx.fill();
        octx.beginPath();
        octx.arc(-10, -2, 6.4, 0, 7);
        octx.fill();
        octx.beginPath();
        octx.moveTo(-15.2, -6);
        octx.lineTo(-14.2, -14);
        octx.lineTo(-9.2, -7.4);
        octx.moveTo(-5.2, -7.6);
        octx.lineTo(-4.2, -14.2);
        octx.lineTo(-0.8, -6.4);
        octx.closePath();
        octx.fill();
        octx.strokeStyle = "rgba(18,12,8," + a.toFixed(3) + ")";
        octx.lineWidth = 1.6;
        octx.beginPath();
        octx.moveTo(12, 8);
        octx.quadraticCurveTo(22, 2 + Math.sin(t * 1.6) * 3, 20, 16);
        octx.stroke();
        octx.fillStyle = hungry ? "rgba(196,80,60,.88)" : "rgba(244,196,83,.92)";
        octx.beginPath();
        octx.arc(-12.4, -2.4, 1.15, 0, 7);
        octx.arc(-8, -2.4, 1.15, 0, 7);
        octx.fill();
        octx.restore();
        octx.font = "600 11px Nunito, sans-serif";
        octx.fillStyle = "rgba(232,214,170,.82)";
        octx.textAlign = "center";
        octx.fillText(st.n, x, y + 26);
      }
      function wrapBrowse() {
        if (didBrowse || !window.shopBrowse) return;
        didBrowse = true;
        var orig = window.shopBrowse;
        window.shopBrowse = function(idx, slot, W, floorY, personS, simT) {
          var rec = orig.apply(this, arguments);
          try {
            if (!rec) return rec;
            var st = window.shopLife && shopLife.browse ? shopLife.browse()[idx] : null;
            var keep = typeof keepGuest === "function" ? keepGuest(st) : !!(st && (st._lateMae || st._goingHold || st._lateKid || st._mask));
            var cat = state();
            if (rec.phase === "look" && st && !st._straySaid && !keep) {
              st._straySaid = 1;
              if (!cat.alive) {
                if (rec.kind === "neighbor" || rec.kind === "kid") {
                  rec.line = rec.kind === "kid" ? "Where's " + cat.n + "?" : cat.n + " is gone. I'm not bagging today.";
                  rec.phase = "leave";
                  st.phase = "leave";
                  st.bought = false;
                  st.line = rec.line;
                  st.lineUntil = now() + 3.8;
                  because((rec.name || "Someone") + " walked. " + cat.n + " is gone.");
                }
              } else if (rec.kind === "kid") {
                rec.line = "Can I pet " + cat.n + "?";
                st.line = rec.line;
                st.until = Math.max(st.until || 0, now() + 5.5);
              } else if (rec.kind === "neighbor") {
                rec.line = "That's " + keeperFirst() + "'s. " + cat.n + " still sleeps here.";
                st.line = rec.line;
                st.lineUntil = now() + 3.8;
              } else if (cat.hunger > 0.6 && Math.random() < 0.4) {
                rec.line = cat.n + " looks thin. You feeding that cat?";
                st.line = rec.line;
              }
            }
          } catch (e) {
          }
          return rec;
        };
      }
      function wrapChoir() {
        if (didChoir || !window.choir || !choir.weather) return;
        didChoir = true;
        var orig = choir.weather;
        choir.weather = function() {
          var w = orig.apply(this, arguments);
          try {
            var st = state();
            if (!st.alive) {
              w.sour = Math.min(1, (w.sour || 0) + 0.1);
              if (!w.line) w.line = st.n + " is gone.";
            } else if (st.hunger > 0.6) {
              w.sour = Math.min(1, (w.sour || 0) + 0.05);
            } else {
              w.sweet = Math.min(1, (w.sweet || 0) + 0.04);
            }
          } catch (e) {
          }
          return w;
        };
      }
      function wrapFish() {
        if (didFish || typeof window.drawFishSprite !== "function") return;
        didFish = true;
        var orig = window.drawFishSprite;
        window.drawFishSprite = function(a) {
          try {
            var st = state();
            if (st.alive && a && a.fish && a.fish.nick && sceneName() === "tank" && st.hunger < 0.5) {
              if (((a.fish.fid || 0) + shopDay()) % 3 === 0) a.fish._holdStill = 1;
            }
          } catch (e) {
          }
          return orig.apply(this, arguments);
        };
      }
      function whisper() {
        if (lastGold && now() - lastGoldAt < 12) return lastGold;
        return "";
      }
      function line() {
        var st = state();
        if (!st.alive) return (st.n || "The cat") + " left. The till was quiet.";
        if (st.hunger > 0.6) return st.n + " looks thin. A fish bag feeds the till-cat.";
        if (st.last) return st.last;
        return st.n + " is on the till. " + keeperFirst() + " left them.";
      }
      function seedWiki() {
        try {
          var wiki = typeof WIKI === "function" ? WIKI() : window.WIKI;
          if (!wiki || !wiki.push) return;
          if (wiki.some(function(a) {
            return a && a.id === "k_stray";
          }))
            return;
          wiki.push({
            id: "k_stray",
            sec: "The shop floor",
            t: "The till-cat",
            tags: "cat stray pet till moth nedda vermin hunt bag",
            w: "<p>Aquarium shops have a till-cat. The previous keeper left one. It sits in the window. Regulars ask. It takes the flashing off the glass. Days without a fish bag and it leaves. Mae will not bag if it is gone.</p><p><b>What to do about it:</b> string a fish bag. The cat eats when the till does. Life names them. The gold line will say if they left.</p>"
          });
        } catch (e) {
        }
      }
      function tick() {
        try {
          wrapBrowse();
          wrapChoir();
          wrapFish();
          seedWiki();
          if (sceneName() === "title") return;
          notice();
          if (now() - lastTick > 0.9) {
            lastTick = now();
            watchBags();
            tickDay();
          }
          if (now() - lastUi > 0.12) {
            lastUi = now();
            drawCat();
          }
        } catch (e) {
        }
      }
      window.stray = {
        whisper,
        line,
        of: function() {
          return state();
        },
        seed: function() {
          var st = state();
          st.alive = true;
          st.left = false;
          st.hunger = 0.2;
          st.said = false;
          notice();
          return st;
        },
        go: function() {
          leave();
          return state();
        }
      };
      if (window.__onBeat) window.__onBeat(tick, 160);
      else
        setTimeout(function loop() {
          tick();
          setTimeout(loop, 160);
        }, 200);
    })();
  } catch (e) {
    console.error("Layer stray.js failed to start:", e);
  }

  // src/layers/row.js
  try {
    (function() {
      "use strict";
      var lastTick = 0;
      var lastGold = "";
      var lastGoldAt = 0;
      var didBrowse = false;
      var didChoir = false;
      function now() {
        return typeof performance !== "undefined" ? performance.now() / 1e3 : Date.now() / 1e3;
      }
      function gs() {
        try {
          if (typeof gameState === "function") return gameState();
          if (typeof gameState === "object" && gameState) return gameState;
        } catch (e) {
        }
        return null;
      }
      function year() {
        try {
          if (window.saga && typeof saga.year === "function") return saga.year();
        } catch (e) {
        }
        return 1e3;
      }
      function shopDay() {
        try {
          var g = gs();
          if (g && isFinite(g.t)) return Math.floor((g.t % 1e9 + 1e9) % 1e9 / 2400);
        } catch (e) {
        }
        return 0;
      }
      function sceneName() {
        try {
          if (document.body.classList.contains("titling")) return "title";
          if (typeof sceneNow === "function") return String(sceneNow() || "");
        } catch (e) {
        }
        return "shop";
      }
      function because(text) {
        if (!text) return;
        try {
          if (window.weave && weave.because) weave.because(text);
        } catch (e) {
        }
        try {
          if (window.desk && desk.think) desk.think("row", text);
        } catch (e2) {
        }
      }
      function egg(id, line2) {
        try {
          if (typeof findEgg === "function") findEgg(id, line2);
        } catch (e) {
        }
      }
      function gold(text, force) {
        if (!text) return;
        lastGold = text;
        lastGoldAt = now();
        try {
          var el = document.getElementById("hookWhisper");
          if (el && (force || el.textContent !== text)) {
            el.textContent = text;
            el.classList.add("on", "pop");
          }
        } catch (e) {
        }
      }
      function state() {
        var g = gs();
        if (g && g.row && typeof g.row === "object") return g.row;
        var st = { last: "", a: "", b: "", day: -1, hit: "" };
        try {
          if (g) g.row = st;
        } catch (e) {
        }
        return st;
      }
      function first(n) {
        return String(n || "Someone").split(" ")[0];
      }
      function spill() {
        try {
          if (!window.shopSite || !shopSite.of) return;
          var s = shopSite.of();
          if (!s || !s.cells) return;
          for (var i = 0; i < s.cells.length; i++) {
            var c = s.cells[i];
            if (c.kind === "floor") c.wet = Math.max(c.wet || 0, 0.58);
          }
          s.wetMax = Math.max(s.wetMax || 0, 0.58);
        } catch (e) {
        }
      }
      function bumpFish() {
        try {
          var list = typeof allFish === "function" ? allFish() || [] : [];
          var pick = null;
          for (var i = 0; i < list.length; i++) {
            if (list[i] && !list[i].dead && list[i].nick) {
              pick = list[i];
              break;
            }
          }
          if (!pick) {
            for (var j = 0; j < list.length; j++) {
              if (list[j] && !list[j].dead) {
                pick = list[j];
                break;
              }
            }
          }
          if (!pick) return "";
          pick._holdStill = 1;
          if (pick.brain) pick.brain.stress = Math.min(1, (pick.brain.stress || 0) + 0.18);
          if (pick.mind) pick.mind.stress = Math.min(1, (pick.mind.stress || 0) + 0.18);
          return pick.nick || "A fish";
        } catch (e) {
        }
        return "";
      }
      function start(a, b) {
        if (!a || !b || a === b) return false;
        var st = state();
        if (st.day === shopDay() && shopDay() >= 0 && !arguments[2]) return false;
        st.a = a;
        st.b = b;
        st.day = shopDay();
        st.hit = bumpFish();
        spill();
        var line2 = first(a) + " and " + first(b) + " had a row." + (st.hit ? " " + st.hit + " hit the glass." : " The aisle is wet.");
        st.last = line2;
        because(line2);
        gold(line2, true);
        egg("aislerow", line2);
        try {
          if (window.kin && kin.hurt) {
            kin.hurt(a, 0.08, "a row on the aisle");
            kin.hurt(b, 0.08, "a row on the aisle");
          }
        } catch (e) {
        }
        try {
          if (window.feel && feel.play) feel.play("warn");
        } catch (e2) {
        }
        return true;
      }
      function figOf(name) {
        try {
          if (window.kin && kin.of && name) return kin.of(name);
        } catch (e) {
        }
        return null;
      }
      function keepOf(st) {
        try {
          if (typeof keepGuest === "function") return keepGuest(st);
        } catch (e) {
        }
        return !!(st && (st._lateMae || st._goingHold || st._lateKid || st._mask));
      }
      function maybeRow() {
        if (shopDay() < 1) return;
        var st = state();
        if (st.day === shopDay()) return;
        var br = [];
        try {
          br = window.shopLife && shopLife.browse && shopLife.browse() || [];
        } catch (e) {
        }
        if (br.length < 2) return;
        var open = null;
        try {
          if (window.pinch && pinch.of) open = pinch.of();
        } catch (eP) {
        }
        var looking = [];
        for (var i = 0; i < br.length; i++) {
          var s = br[i];
          if (!s || s.phase === "leave" || keepOf(s)) continue;
          if (s.phase !== "look" && s.phase !== "pay") continue;
          var fig = figOf(s.guestName || s.name);
          looking.push({ st: s, fig, name: fig && fig.n || s.guestName || s.name || "" });
        }
        if (looking.length < 2) return;
        var A = null;
        var B = null;
        if (open && open.who) {
          for (var t = 0; t < looking.length; t++) {
            if (looking[t].name === open.who || looking[t].fig && looking[t].fig.n === open.who)
              A = looking[t];
          }
          for (var k2 = 0; k2 < looking.length; k2++) {
            if (looking[k2] === A) continue;
            if (looking[k2].fig && looking[k2].fig.kind === "neighbor") B = looking[k2];
            else if (!B) B = looking[k2];
          }
        }
        if (!A || !B) {
          for (var x = 0; x < looking.length; x++) {
            for (var y = x + 1; y < looking.length; y++) {
              var fa = looking[x].fig;
              var fb = looking[y].fig;
              var grudge = fa && (fa.trust || 0.4) < 0.28 || fb && (fb.trust || 0.4) < 0.28 || fa && fa.last && /took |row|died|hurt/.test(fa.last) || fb && fb.last && /took |row|died|hurt/.test(fb.last);
              var wet = false;
              try {
                wet = window.shopSite && shopSite.wet && shopSite.wet() > 0.36;
              } catch (eW) {
              }
              if (grudge || wet) {
                A = looking[x];
                B = looking[y];
                break;
              }
            }
            if (A && B) break;
          }
        }
        if (!A || !B || !A.name || !B.name) return;
        var chance = 0.08;
        try {
          if (window.shopSite && shopSite.wet && shopSite.wet() > 0.36) chance += 0.14;
        } catch (eC) {
        }
        if (open) chance += 0.22;
        if (Math.random() > chance) return;
        if (!start(A.name, B.name)) return;
        A.st.phase = "leave";
        A.st.bought = false;
        A.st.line = "I didn't start it.";
        B.st.phase = "leave";
        B.st.bought = false;
        B.st.line = "They shoved me.";
        for (var z = 0; z < br.length; z++) {
          if (!br[z] || keepOf(br[z])) continue;
          if (br[z] === A.st || br[z] === B.st) continue;
          if (br[z].phase === "look" || br[z].phase === "pay") {
            br[z].phase = "leave";
            br[z].bought = false;
          }
        }
      }
      function wrapBrowse() {
        if (didBrowse || !window.shopBrowse) return;
        didBrowse = true;
        var orig = window.shopBrowse;
        window.shopBrowse = function(idx, slot, W, floorY, personS, simT) {
          var rec = orig.apply(this, arguments);
          try {
            if (!rec) return rec;
            var st = window.shopLife && shopLife.browse ? shopLife.browse()[idx] : null;
            maybeRow();
            var row2 = state();
            if (row2.last && row2.day === shopDay() && rec.phase === "look" && st && !st._rowSaid) {
              st._rowSaid = 1;
              var keep = keepOf(st);
              if (!keep) {
                rec.line = rec.kind === "kid" ? "They were yelling." : "I'm not standing in that.";
                rec.phase = "leave";
                st.phase = "leave";
                st.bought = false;
                st.line = rec.line;
                st.lineUntil = now() + 3.6;
              }
            }
          } catch (e) {
          }
          return rec;
        };
      }
      function wrapChoir() {
        if (didChoir || !window.choir || !choir.weather) return;
        didChoir = true;
        var orig = choir.weather;
        choir.weather = function() {
          var w = orig.apply(this, arguments);
          try {
            var st = state();
            if (st.last && st.day === shopDay()) {
              w.sour = Math.min(1, (w.sour || 0) + 0.14);
              if (!w.line) w.line = first(st.a) + " and " + first(st.b) + " had a row.";
            }
          } catch (e) {
          }
          return w;
        };
      }
      function whisper() {
        if (lastGold && now() - lastGoldAt < 12) return lastGold;
        return "";
      }
      function line() {
        var st = state();
        if (st.last && st.day === shopDay()) return st.last;
        if (st.last) return st.last;
        return whisper();
      }
      function seedWiki() {
        try {
          var wiki = typeof WIKI === "function" ? WIKI() : window.WIKI;
          if (!wiki || !wiki.push) return;
          if (wiki.some(function(a) {
            return a && a.id === "k_row";
          }))
            return;
          wiki.push({
            id: "k_row",
            sec: "The shop floor",
            t: "A row on the aisle",
            tags: "brawl fight aisle grudge tavern shove wet row",
            w: "<p>Fin's aisle is the tavern. A thief and a cousin. A wet board and two people who already named a morning. They shove. The puddle spreads. Everyone else walks. A named one hits the glass and holds still.</p><p><b>What to do about it:</b> keep the boards dry. Stay on the aisle when a theft is open. Life names who shoved. The gold line does not cheer you.</p>"
          });
        } catch (e) {
        }
      }
      function tick() {
        try {
          wrapBrowse();
          wrapChoir();
          seedWiki();
          if (sceneName() === "title") return;
          if (now() - lastTick > 1.1) lastTick = now();
        } catch (e) {
        }
      }
      window.row = {
        whisper,
        line,
        of: function() {
          return state();
        },
        seed: function(a, b) {
          var na = a;
          var nb = b;
          try {
            if ((!na || !nb) && window.kin && kin.folk) {
              var all = kin.folk() || [];
              var live = [];
              for (var i = 0; i < all.length; i++) {
                if (all[i] && !all[i].dead && all[i].id !== "keep") live.push(all[i].n);
              }
              if (!na && live[0]) na = live[0];
              if (!nb && live[1]) nb = live[1];
              if (!nb && live[0]) nb = live[0] === na ? "Someone on Salem" : live[0];
            }
          } catch (e) {
          }
          start(na || "Vabal Chen", nb || "Rita Russo", true);
          return state();
        }
      };
      if (window.__onBeat) window.__onBeat(tick, 240);
      else
        setTimeout(function loop() {
          tick();
          setTimeout(loop, 240);
        }, 220);
    })();
  } catch (e) {
    console.error("Layer row.js failed to start:", e);
  }

  // src/layers/mask.js
  try {
    (function() {
      "use strict";
      var lastTick = 0;
      var lastGold = "";
      var lastGoldAt = 0;
      var didBrowse = false;
      var didChoir = false;
      var didFish = false;
      function now() {
        return typeof performance !== "undefined" ? performance.now() / 1e3 : Date.now() / 1e3;
      }
      function gs() {
        try {
          if (typeof gameState === "function") return gameState();
          if (typeof gameState === "object" && gameState) return gameState;
        } catch (e) {
        }
        return null;
      }
      function year() {
        try {
          if (window.saga && typeof saga.year === "function") return saga.year();
        } catch (e) {
        }
        return 1e3;
      }
      function shopDay() {
        try {
          var g = gs();
          if (g && isFinite(g.t)) return Math.floor((g.t % 1e9 + 1e9) % 1e9 / 2400);
        } catch (e) {
        }
        return 0;
      }
      function sceneName() {
        try {
          if (document.body.classList.contains("titling")) return "title";
          if (typeof sceneNow === "function") return String(sceneNow() || "");
        } catch (e) {
        }
        return "shop";
      }
      function because(text) {
        if (!text) return;
        try {
          if (window.weave && weave.because) weave.because(text);
        } catch (e) {
        }
        try {
          if (window.desk && desk.think) desk.think("mask", text);
        } catch (e2) {
        }
      }
      function egg(id, line2) {
        try {
          if (typeof findEgg === "function") findEgg(id, line2);
        } catch (e) {
        }
      }
      function gold(text, force) {
        if (!text) return;
        lastGold = text;
        lastGoldAt = now();
        try {
          var el = document.getElementById("hookWhisper");
          if (el && (force || el.textContent !== text)) {
            el.textContent = text;
            el.classList.add("on", "pop");
          }
        } catch (e) {
        }
      }
      function keeper() {
        try {
          if (window.late && late.keeper) return late.keeper();
        } catch (e) {
        }
        return null;
      }
      function keptFish() {
        try {
          if (window.late && late.kept) return late.kept() || null;
        } catch (e) {
        }
        return null;
      }
      function soldKept() {
        try {
          var g = gs();
          if (g && g.late && g.late.soldKept) return true;
        } catch (e) {
        }
        return false;
      }
      function state() {
        var g = gs();
        if (g && g.mask && typeof g.mask === "object") return g.mask;
        var st = { seen: false, day: -1, last: "", knew: false };
        try {
          if (g) g.mask = st;
        } catch (e) {
        }
        return st;
      }
      function first(n) {
        return String(n || "Someone").split(" ")[0];
      }
      function arrive(rec, st) {
        var k2 = keeper();
        if (!k2 || !k2.n) return false;
        var ms = state();
        if (ms.seen && ms.day === shopDay() && !arguments[2]) return false;
        var kept = keptFish();
        rec.name = k2.n;
        rec.kind = rec.kind === "kid" ? "collector" : rec.kind || "collector";
        rec.phase = "look";
        if (soldKept()) {
          rec.line = "You bagged the one I kept.";
          rec.phase = "leave";
          if (st) {
            st.phase = "leave";
            st.bought = false;
          }
          ms.knew = true;
        } else if (kept) {
          rec.line = "I named that one. " + (kept.nick || "That fish") + ".";
          rec.phase = "look";
          if (st) {
            st.bought = false;
            st.until = Math.max(st.until || 0, now() + 8);
          }
          kept._holdStill = 1;
          kept._gaze = now();
        } else {
          rec.line = "I used to keep this counter.";
          rec.phase = "look";
          if (st) {
            st.bought = false;
            st.until = Math.max(st.until || 0, now() + 6);
          }
        }
        if (st) {
          st._mask = 1;
          st.guestName = k2.n;
          st.name = k2.n;
          st.kind = rec.kind;
          st.line = rec.line;
          st.lineUntil = now() + 5;
          st._goingHold = st._goingHold || 0;
        }
        ms.seen = true;
        ms.day = shopDay();
        var line2 = first(k2.n) + " is on the aisle." + (soldKept() ? " They saw the empty water." : "");
        ms.last = line2;
        because(line2 + " " + rec.line);
        gold(line2, true);
        egg("maskkeep", line2);
        if (soldKept()) {
          try {
            if (window.kin && kin.hurt) kin.hurt("Mae Costa", 0.06, first(k2.n) + " came back. The fish was gone.");
          } catch (eH) {
          }
        }
        return true;
      }
      function wrapBrowse() {
        if (didBrowse || !window.shopBrowse) return;
        didBrowse = true;
        var orig = window.shopBrowse;
        window.shopBrowse = function(idx, slot, W, floorY, personS, simT) {
          var rec = orig.apply(this, arguments);
          try {
            if (!rec) return rec;
            var st = window.shopLife && shopLife.browse ? shopLife.browse()[idx] : null;
            var keep = typeof keepGuest === "function" ? keepGuest(st) : !!(st && (st._lateMae || st._goingHold || st._lateKid || st._mask));
            var ms = state();
            var k2 = keeper();
            if (!keep && rec.phase === "look" && st && !st._mask && shopDay() >= 1 && !ms.seen && k2 && ((idx | 0) === 1 || rec.kind === "collector")) {
              arrive(rec, st);
            }
            if (ms.seen && ms.day === shopDay() && rec.phase === "look" && st && !st._mask && !st._maskSaid && !keep) {
              st._maskSaid = 1;
              if (rec.kind === "kid") {
                rec.line = "That's not a customer.";
                st.line = rec.line;
              } else if (rec.kind === "neighbor" && k2 && k2.kin) {
                rec.line = first(k2.n) + " came back. Don't say you didn't see.";
                st.line = rec.line;
                st.until = Math.max(st.until || 0, now() + 5);
              } else if (rec.kind === "collector") {
                rec.line = "I know that walk. They used to keep this counter.";
                st.line = rec.line;
              }
            }
          } catch (e) {
          }
          return rec;
        };
      }
      function wrapChoir() {
        if (didChoir || !window.choir || !choir.weather) return;
        didChoir = true;
        var orig = choir.weather;
        choir.weather = function() {
          var w = orig.apply(this, arguments);
          try {
            var ms = state();
            if (ms.seen && ms.day === shopDay()) {
              w.sour = Math.min(1, (w.sour || 0) + 0.06);
              if (!w.line) {
                var k2 = keeper();
                w.line = (k2 ? first(k2.n) : "Someone") + " is on the aisle. The water knows.";
              }
            }
          } catch (e) {
          }
          return w;
        };
      }
      function wrapFish() {
        if (didFish || typeof window.drawFishSprite !== "function") return;
        didFish = true;
        var orig = window.drawFishSprite;
        window.drawFishSprite = function(a) {
          try {
            var ms = state();
            if (ms.seen && ms.day === shopDay() && a && a.fish) {
              var kpt = keptFish();
              if (kpt && a.fish === kpt) {
                a.fish._holdStill = 1;
                a.fish._gaze = now();
              }
            }
          } catch (e) {
          }
          return orig.apply(this, arguments);
        };
      }
      function whisper() {
        if (lastGold && now() - lastGoldAt < 12) return lastGold;
        return "";
      }
      function line() {
        var ms = state();
        if (ms.last) return ms.last;
        var k2 = keeper();
        if (ms.seen && k2) return first(k2.n) + " walked the aisle. They did not bag.";
        return whisper();
      }
      function seedWiki() {
        try {
          var wiki = typeof WIKI === "function" ? WIKI() : window.WIKI;
          if (!wiki || !wiki.push) return;
          if (wiki.some(function(a) {
            return a && a.id === "k_mask";
          }))
            return;
          wiki.push({
            id: "k_mask",
            sec: "The chronicle",
            t: "Someone on the aisle is not who they said",
            tags: "keeper identity mask nedda secret return aisle",
            w: "<p>The previous keeper left. Once, they walk in. They look at the one they kept. They do not bag. The fish knows. A kid says that's not a customer. Sell that fish and they walk, and the street hears it from a mouth that used to own the keys.</p><p><b>What to do about it:</b> do not bag the one they kept. Life will name the walk. The gold line does not introduce them.</p>"
          });
        } catch (e) {
        }
      }
      function tick() {
        try {
          wrapBrowse();
          wrapChoir();
          wrapFish();
          seedWiki();
          if (sceneName() === "title") return;
          if (now() - lastTick > 1) lastTick = now();
        } catch (e) {
        }
      }
      window.mask = {
        whisper,
        line,
        of: function() {
          return state();
        },
        seed: function() {
          var rec = { kind: "collector", phase: "look", line: "", name: "" };
          var st = { phase: "look", bought: false };
          arrive(rec, st, true);
          return state();
        }
      };
      if (window.__onBeat) window.__onBeat(tick, 220);
      else
        setTimeout(function loop() {
          tick();
          setTimeout(loop, 220);
        }, 200);
    })();
  } catch (e) {
    console.error("Layer mask.js failed to start:", e);
  }

  // src/layers/age.js
  try {
    (function() {
      "use strict";
      var lastTick = 0;
      var lastGold = "";
      var lastGoldAt = 0;
      var didBrowse = false;
      var didChoir = false;
      function now() {
        return typeof performance !== "undefined" ? performance.now() / 1e3 : Date.now() / 1e3;
      }
      function gs() {
        try {
          if (typeof gameState === "function") return gameState();
          if (typeof gameState === "object" && gameState) return gameState;
        } catch (e) {
        }
        return null;
      }
      function year() {
        try {
          if (window.saga && typeof saga.year === "function") return saga.year();
        } catch (e) {
        }
        return 1e3;
      }
      function shopDay() {
        try {
          var g = gs();
          if (g && isFinite(g.t)) return Math.floor((g.t % 1e9 + 1e9) % 1e9 / 2400);
        } catch (e) {
        }
        return 0;
      }
      function because(text) {
        if (!text) return;
        try {
          if (window.weave && weave.because) weave.because(text);
        } catch (e) {
        }
        try {
          if (window.desk && desk.think) desk.think("age", text);
        } catch (e2) {
        }
      }
      function egg(id, line2) {
        try {
          if (typeof findEgg === "function") findEgg(id, line2);
        } catch (e) {
        }
      }
      function gold(text, force) {
        if (!text) return;
        lastGold = text;
        lastGoldAt = now();
        try {
          var el = document.getElementById("hookWhisper");
          if (el && (force || el.textContent !== text)) {
            el.textContent = text;
            el.classList.add("on", "pop");
          }
        } catch (e) {
        }
      }
      function eraName() {
        try {
          var w = window.realm && realm.world && realm.world();
          if (w && w.age) return String(w.age);
        } catch (e) {
        }
        return "Age of Quiet Water";
      }
      function state() {
        var g = gs();
        if (g && g.age && typeof g.age === "object") return g.age;
        var st = { era: eraName(), mood: "normal", last: "", day: -1 };
        try {
          if (g) g.age = st;
        } catch (e) {
        }
        return st;
      }
      function scoreMood() {
        var n = 0;
        try {
          if (window.guild && guild.standing) n += (guild.standing() - 0.42) * 1.4;
        } catch (e) {
        }
        try {
          if (window.craft && craft.quality) n += (craft.quality() - 0.4) * 0.8;
        } catch (e2) {
        }
        try {
          if (window.realm && realm.embargo && realm.embargo()) n -= 0.55;
        } catch (e3) {
        }
        try {
          if (window.guild && guild.boycott && guild.boycott()) n -= 0.4;
        } catch (e4) {
        }
        try {
          if (window.beast && beast.near) n -= (beast.near() || 0) * 2;
        } catch (e5) {
        }
        if (n > 0.28) return "gold";
        if (n < -0.22) return "dark";
        return "normal";
      }
      function applyMood(force) {
        var st = state();
        st.era = eraName();
        var next = scoreMood();
        if (force) next = force;
        if (next === st.mood) return;
        var d = shopDay();
        st.mood = next;
        st.day = d;
        if (d < 1 && !force) return;
        var line2 = next === "gold" ? "A golden age. The street is staying." : next === "dark" ? "A dark age. They're walking." : st.era + " is quiet again.";
        st.last = line2;
        because(line2);
        gold(line2, true);
        egg("age" + next, line2);
      }
      function edge() {
        var m = state().mood;
        if (m === "gold") return 0.08;
        if (m === "dark") return -0.1;
        return 0;
      }
      function wrapBrowse() {
        if (didBrowse || !window.shopBrowse) return;
        didBrowse = true;
        var orig = window.shopBrowse;
        window.shopBrowse = function(idx, slot, W, floorY, personS, simT) {
          var rec = orig.apply(this, arguments);
          try {
            if (!rec) return rec;
            var st = window.shopLife && shopLife.browse ? shopLife.browse()[idx] : null;
            var keep = typeof keepGuest === "function" ? keepGuest(st) : !!(st && (st._lateMae || st._goingHold || st._lateKid || st._mask));
            if (keep || !st || rec.phase !== "look" || st._ageSaid) return rec;
            st._ageSaid = 1;
            if (shopDay() < 1) return rec;
            var m = state().mood;
            if (m === "dark") {
              rec.line = rec.kind === "kid" ? "Nobody's coming out today." : "Not in this age.";
              rec.phase = "leave";
              st.phase = "leave";
              st.bought = false;
              st.line = rec.line;
            } else if (m === "gold") {
              rec.line = rec.kind === "collector" ? "A good year for a pair. I'll take my time." : "The street is staying. I'll look.";
              st.line = rec.line;
              st.until = Math.max(st.until || 0, now() + 6);
            }
          } catch (e) {
          }
          return rec;
        };
      }
      function wrapChoir() {
        if (didChoir || !window.choir || !choir.weather) return;
        didChoir = true;
        var orig = choir.weather;
        choir.weather = function() {
          var w = orig.apply(this, arguments);
          try {
            var m = state().mood;
            if (m === "gold") {
              w.sweet = Math.min(1, (w.sweet || 0) + 0.12);
              if (!w.line) w.line = "A golden age. The water knows.";
            } else if (m === "dark") {
              w.sour = Math.min(1, (w.sour || 0) + 0.14);
              if (!w.line) w.line = "A dark age. The chord is thin.";
            }
          } catch (e) {
          }
          return w;
        };
      }
      function whisper() {
        if (lastGold && now() - lastGoldAt < 12) return lastGold;
        return "";
      }
      function line() {
        var st = state();
        if (st.last) return st.last;
        return whisper();
      }
      function seedWiki() {
        try {
          var wiki = typeof WIKI === "function" ? WIKI() : window.WIKI;
          if (!wiki || !wiki.push) return;
          if (wiki.some(function(a) {
            return a && a.id === "k_age";
          })) return;
          wiki.push({
            id: "k_age",
            sec: "The chronicle",
            t: "The age lands on the aisle",
            tags: "era golden dark age atlas harbor",
            w: "<p>The age lands on the boards. The atlas already named it. A good hall, a good bag, no war inland: the street stays. A boycott, a beast, a keel: they walk. The choir sweetens or sours with it. The till feels it.</p><p><b>What to do about it:</b> keep the hall. Fill the pair. The gold line will say if the age turned. It will not steal the letter.</p>"
          });
        } catch (e) {
        }
      }
      function tick() {
        try {
          wrapBrowse();
          wrapChoir();
          seedWiki();
          if (now() - lastTick > 1.4) {
            lastTick = now();
            applyMood();
          }
        } catch (e) {
        }
      }
      window.age = {
        whisper,
        line,
        of: state,
        mood: function() {
          return state().mood;
        },
        edge,
        seed: function(m) {
          applyMood(m === "dark" ? "dark" : "gold");
          return state();
        }
      };
      if (window.__onBeat) window.__onBeat(tick, 240);
      else
        setTimeout(function loop() {
          tick();
          setTimeout(loop, 240);
        }, 220);
    })();
  } catch (e) {
    console.error("Layer age.js failed to start:", e);
  }

  // src/layers/hold.js
  try {
    (function() {
      "use strict";
      var lastTick = 0;
      var lastGold = "";
      var lastGoldAt = 0;
      var didBrowse = false;
      var didChoir = false;
      function now() {
        return typeof performance !== "undefined" ? performance.now() / 1e3 : Date.now() / 1e3;
      }
      function gs() {
        try {
          if (typeof gameState === "function") return gameState();
          if (typeof gameState === "object" && gameState) return gameState;
        } catch (e) {
        }
        return null;
      }
      function year() {
        try {
          if (window.saga && typeof saga.year === "function") return saga.year();
        } catch (e) {
        }
        return 1e3;
      }
      function shopDay() {
        try {
          var g = gs();
          if (g && isFinite(g.t)) return Math.floor((g.t % 1e9 + 1e9) % 1e9 / 2400);
        } catch (e) {
        }
        return 0;
      }
      function because(text) {
        if (!text) return;
        try {
          if (window.weave && weave.because) weave.because(text);
        } catch (e) {
        }
        try {
          if (window.desk && desk.think) desk.think("hold", text);
        } catch (e2) {
        }
      }
      function egg(id, line2) {
        try {
          if (typeof findEgg === "function") findEgg(id, line2);
        } catch (e) {
        }
      }
      function gold(text, force) {
        if (!text) return;
        lastGold = text;
        lastGoldAt = now();
        try {
          var el = document.getElementById("hookWhisper");
          if (el && (force || el.textContent !== text)) {
            el.textContent = text;
            el.classList.add("on", "pop");
          }
        } catch (e) {
        }
      }
      function letterFrom() {
        try {
          var L = window.going && going.letter && going.letter();
          if (L && L.from) return String(L.from);
        } catch (e) {
        }
        return "";
      }
      function aSite() {
        try {
          var w = window.realm && realm.world && realm.world();
          var skip = letterFrom();
          if (w && w.sites) {
            for (var i = 1; i < w.sites.length; i++) {
              var s = w.sites[i];
              if (!s || s.ruin || !s.n) continue;
              if (skip && s.n === skip) continue;
              if (s.kind === "city" || s.kind === "town" || s.kind === "hamlet" || s.kind === "port") return s;
            }
            return w.sites[2] || w.sites[1] || w.sites[0];
          }
        } catch (e) {
        }
        return { n: "Pelholm", kind: "town", pop: 80, fish: "river" };
      }
      function kindOf() {
        try {
          if (window.guild && guild.mandate) return String(guild.mandate() || "goldfish");
        } catch (e) {
        }
        return "goldfish";
      }
      function state() {
        var g = gs();
        if (g && g.hold && typeof g.hold === "object") return g.hold;
        var st = { last: "", site: "", want: "", found: false, seen: false, day: -1, loyal: 0.5, settler: "" };
        try {
          if (g) g.hold = st;
        } catch (e) {
        }
        return st;
      }
      function arrive(rec, st, force) {
        var hs = state();
        if (hs.seen && hs.day === shopDay() && !force) return false;
        if (shopDay() < 1 && !force) return false;
        var site = aSite();
        var want = kindOf();
        rec.kind = rec.kind === "kid" ? "collector" : rec.kind || "collector";
        rec.phase = "look";
        rec.want = want;
        rec.line = "Raising a shop in " + site.n + ". A pair of " + want + ".";
        rec.name = rec.name || "Someone of " + site.n;
        if (st) {
          st._hold = 1;
          st.want = want;
          st.line = rec.line;
          st.lineUntil = now() + 6;
          st.guestName = rec.name;
          st.until = Math.max(st.until || 0, now() + 8);
        }
        hs.seen = true;
        hs.day = shopDay();
        hs.site = site.n;
        hs.want = want;
        hs.settler = rec.name;
        var line2 = "They are raising a shop in " + site.n + ".";
        hs.last = line2;
        because(line2 + " A pair of " + want + ".");
        gold(line2, true);
        egg("holdfound", line2);
        return true;
      }
      function noteBag(kind) {
        var hs = state();
        if (!hs.seen || hs.found) return;
        if (hs.want && kind && String(kind).toLowerCase().indexOf(String(hs.want).toLowerCase()) >= 0) {
          hs.found = true;
          hs.loyal = 0.86;
          var line2 = "A hold was founded. " + hs.site + " has a shop.";
          hs.last = line2;
          because(line2);
          gold(line2, true);
          egg("holdborn", line2);
          try {
            var w = window.realm && realm.world && realm.world();
            if (w && w.sites) {
              for (var i = 0; i < w.sites.length; i++) {
                if (w.sites[i] && w.sites[i].n === hs.site) {
                  w.sites[i].pop = (w.sites[i].pop || 40) + 80;
                  w.sites[i].ruin = 0;
                  break;
                }
              }
            }
          } catch (e) {
          }
          try {
            if (window.weave && weave.bumpWord) weave.bumpWord(0.06);
          } catch (e2) {
          }
        }
      }
      function noteMiss() {
        var hs = state();
        if (!hs.seen || hs.found) return;
        if (shopDay() - (hs.day || 0) < 2) return;
        if (hs.missDay === shopDay()) return;
        hs.missDay = shopDay();
        hs.loyal = Math.max(0.08, (hs.loyal || 0.5) - 0.22);
        if (hs.loyal < 0.22 && hs.last && /Haymarket/.test(hs.last)) return;
        if (hs.loyal < 0.22) {
          var line2 = hs.site + " bags at Haymarket now.";
          hs.last = line2;
          because(line2);
          gold(line2, true);
        }
      }
      function edge() {
        var hs = state();
        if (hs.found) return 0.06;
        if (hs.loyal < 0.22) return -0.07;
        return 0;
      }
      function wrapBrowse() {
        if (didBrowse || !window.shopBrowse) return;
        didBrowse = true;
        var orig = window.shopBrowse;
        window.shopBrowse = function(idx, slot, W, floorY, personS, simT) {
          var rec = orig.apply(this, arguments);
          try {
            if (!rec) return rec;
            var st = window.shopLife && shopLife.browse ? shopLife.browse()[idx] : null;
            var keep = typeof keepGuest === "function" ? keepGuest(st) : !!(st && (st._lateMae || st._goingHold || st._lateKid || st._mask));
            var hs = state();
            if (!keep && rec.phase === "look" && st && !st._hold && shopDay() >= 1 && !hs.seen && ((idx | 0) === 2 || rec.kind === "collector")) {
              arrive(rec, st);
            }
            if (hs.seen && rec.phase === "pay" && st && st._hold) {
              noteBag(rec.want || st.want || hs.want);
            }
            if (hs.loyal < 0.22 && rec.phase === "look" && st && !st._holdSaid && !keep) {
              st._holdSaid = 1;
              rec.line = rec.kind === "kid" ? "They went to Haymarket." : hs.site + " bags somewhere else now.";
              rec.phase = "leave";
              st.phase = "leave";
              st.bought = false;
              st.line = rec.line;
            } else if (hs.found && rec.phase === "look" && st && !st._holdSaid && !keep && (rec.kind === "neighbor" || rec.kind === "collector")) {
              st._holdSaid = 1;
              rec.line = "The new shop in " + hs.site + " still sends people here.";
              st.line = rec.line;
            }
          } catch (e) {
          }
          return rec;
        };
      }
      function wrapChoir() {
        if (didChoir || !window.choir || !choir.weather) return;
        didChoir = true;
        var orig = choir.weather;
        choir.weather = function() {
          var w = orig.apply(this, arguments);
          try {
            var hs = state();
            if (hs.found) {
              w.sweet = Math.min(1, (w.sweet || 0) + 0.07);
              if (!w.line) w.line = hs.site + " has a shop. The water heard.";
            } else if (hs.loyal < 0.22) {
              w.sour = Math.min(1, (w.sour || 0) + 0.08);
            }
          } catch (e) {
          }
          return w;
        };
      }
      function whisper() {
        if (lastGold && now() - lastGoldAt < 12) return lastGold;
        return "";
      }
      function line() {
        return state().last || whisper();
      }
      function seedWiki() {
        try {
          var wiki = typeof WIKI === "function" ? WIKI() : window.WIKI;
          if (!wiki || !wiki.push) return;
          if (wiki.some(function(a) {
            return a && a.id === "k_hold";
          })) return;
          wiki.push({
            id: "k_hold",
            sec: "The quarter",
            t: "Someone is raising a shop",
            tags: "settler city founding hold loyalty haymarket",
            w: "<p>Fin's settler walks in off a longer road and wants a pair for a shop that is not this one. Fill it and a town is on the map. Miss it and that street bags at Haymarket. Loyalty is who still comes.</p><p><b>What to do about it:</b> keep two of what they asked for. The letter is a cut road. This is a new street.</p>"
          });
        } catch (e) {
        }
      }
      function tick() {
        try {
          wrapBrowse();
          wrapChoir();
          seedWiki();
          if (now() - lastTick > 1.6) {
            lastTick = now();
            noteMiss();
          }
        } catch (e) {
        }
      }
      window.hold = {
        whisper,
        line,
        of: state,
        edge,
        seed: function() {
          var rec = { kind: "collector", phase: "look", line: "", name: "" };
          var st = { phase: "look", bought: false };
          arrive(rec, st, true);
          return state();
        }
      };
      if (window.__onBeat) window.__onBeat(tick, 240);
      else
        setTimeout(function loop() {
          tick();
          setTimeout(loop, 240);
        }, 220);
    })();
  } catch (e) {
    console.error("Layer hold.js failed to start:", e);
  }

  // src/layers/lord.js
  try {
    (function() {
      "use strict";
      var lastTick = 0;
      var lastGold = "";
      var lastGoldAt = 0;
      var didBrowse = false;
      var didChoir = false;
      var AGENDAS = ["named", "dry", "pair", "mandate", "relic", "kind"];
      function now() {
        return typeof performance !== "undefined" ? performance.now() / 1e3 : Date.now() / 1e3;
      }
      function gs() {
        try {
          if (typeof gameState === "function") return gameState();
          if (typeof gameState === "object" && gameState) return gameState;
        } catch (e) {
        }
        return null;
      }
      function year() {
        try {
          if (window.saga && typeof saga.year === "function") return saga.year();
        } catch (e) {
        }
        return 1e3;
      }
      function shopDay() {
        try {
          var g = gs();
          if (g && isFinite(g.t)) return Math.floor((g.t % 1e9 + 1e9) % 1e9 / 2400);
        } catch (e) {
        }
        return 0;
      }
      function because(text) {
        if (!text) return;
        try {
          if (window.weave && weave.because) weave.because(text);
        } catch (e) {
        }
        try {
          if (window.desk && desk.think) desk.think("lord", text);
        } catch (e2) {
        }
      }
      function egg(id, line2) {
        try {
          if (typeof findEgg === "function") findEgg(id, line2);
        } catch (e) {
        }
      }
      function gold(text, force) {
        if (!text) return;
        lastGold = text;
        lastGoldAt = now();
        try {
          var el = document.getElementById("hookWhisper");
          if (el && (force || el.textContent !== text)) {
            el.textContent = text;
            el.classList.add("on", "pop");
          }
        } catch (e) {
        }
      }
      function aCiv() {
        try {
          var w = window.realm && realm.world && realm.world();
          if (w && w.civs && w.civs.length) {
            for (var i = 0; i < w.civs.length; i++) {
              if (w.civs[i] && w.civs[i].alive !== 0 && w.civs[i].n) return w.civs[i];
            }
            return w.civs[0];
          }
        } catch (e) {
        }
        return { n: "the harbor league", kind: "harbor league", ethic: "trade", faith: "the Salt Mother" };
      }
      function aFig(civ) {
        try {
          var w = window.realm && realm.world && realm.world();
          if (w && w.figs) {
            for (var i = 0; i < w.figs.length; i++) {
              if (w.figs[i] && (civ == null || w.figs[i].civ === civ.i) && w.figs[i].n) return w.figs[i];
            }
            if (w.figs[0]) return w.figs[0];
          }
        } catch (e) {
        }
        return { n: "Calen Maros", job: "captain" };
      }
      function mandate() {
        try {
          if (window.guild && guild.mandate) return String(guild.mandate() || "goldfish");
        } catch (e) {
        }
        return "goldfish";
      }
      function hash32(s) {
        var h = 2166136261;
        s = String(s || "");
        for (var i = 0; i < s.length; i++) {
          h ^= s.charCodeAt(i);
          h = Math.imul(h, 16777619);
        }
        return h >>> 0;
      }
      function state() {
        var g = gs();
        if (g && g.lord && g.lord.n) return g.lord;
        var civ = aCiv();
        var fig = aFig(civ);
        var ag = AGENDAS[hash32(civ.n + "|" + fig.n) % AGENDAS.length];
        var st = {
          n: fig.n,
          civ: civ.n,
          kind: civ.kind,
          ethic: civ.ethic || "trade",
          agenda: ag,
          want: mandate(),
          last: "",
          seen: false,
          day: -1
        };
        try {
          if (g) g.lord = st;
        } catch (e) {
        }
        return st;
      }
      function first(n) {
        return String(n || "Someone").split(" ")[0];
      }
      function agendaLine(st, rec) {
        if (st.agenda === "named") return "The " + st.civ + " want a named line. Not a SKU.";
        if (st.agenda === "dry") return "We don't bag on a wet board. The " + st.civ + " said.";
        if (st.agenda === "pair") return "Two. The " + st.civ + " do not take a unique.";
        if (st.agenda === "mandate") return "The " + st.civ + " asked for " + st.want + ". Same as the hall.";
        if (st.agenda === "relic") return "We came for what is in the window, not the water.";
        return "The " + st.civ + " bag " + st.want + " and nothing else this year.";
      }
      function arrive(rec, st, force) {
        var ls = state();
        if (ls.seen && ls.day === shopDay() && !force) return false;
        if (shopDay() < 1 && !force) return false;
        rec.kind = "collector";
        rec.name = ls.n;
        rec.phase = "look";
        rec.want = rec.want || ls.want;
        rec.line = first(ls.n) + " of " + ls.civ + ". " + agendaLine(ls, rec);
        if (st) {
          st._lord = 1;
          st.name = ls.n;
          st.guestName = ls.n;
          st.want = rec.want;
          st.line = rec.line;
          st.lineUntil = now() + 6;
          st.bought = false;
          st.until = Math.max(st.until || 0, now() + 7);
        }
        ls.seen = true;
        ls.day = shopDay();
        var line2 = first(ls.n) + " of " + ls.civ + " is on the aisle.";
        ls.last = line2;
        because(line2 + " " + rec.line);
        gold(line2, true);
        egg("lordin", line2);
        return true;
      }
      function wet() {
        try {
          return !!(window.shopSite && shopSite.wet && shopSite.wet() > 0.28);
        } catch (e) {
        }
        return false;
      }
      function wrapBrowse() {
        if (didBrowse || !window.shopBrowse) return;
        didBrowse = true;
        var orig = window.shopBrowse;
        window.shopBrowse = function(idx, slot, W, floorY, personS, simT) {
          var rec = orig.apply(this, arguments);
          try {
            if (!rec) return rec;
            var st = window.shopLife && shopLife.browse ? shopLife.browse()[idx] : null;
            var keep = typeof keepGuest === "function" ? keepGuest(st) : !!(st && (st._lateMae || st._goingHold || st._lateKid || st._mask));
            var ls = state();
            if (!keep && rec.phase === "look" && st && !st._lord && shopDay() >= 1 && !ls.seen && ((idx | 0) === 1 || rec.kind === "collector")) {
              arrive(rec, st);
            }
            if (ls.seen && ls.day === shopDay() && rec.phase === "look" && st && !st._lord && !st._lordSaid && !keep) {
              st._lordSaid = 1;
              rec.line = rec.kind === "kid" ? "That's not from Salem." : "The " + ls.civ + " sent someone.";
              st.line = rec.line;
            }
            if (st && st._lord && rec.phase === "look") {
              if (ls.agenda === "dry" && wet()) {
                rec.line = "Wet boards. The " + ls.civ + " walk.";
                rec.phase = "leave";
                st.phase = "leave";
                st.bought = false;
                st.line = rec.line;
              } else if (ls.agenda === "kind" || ls.agenda === "mandate") {
                rec.want = ls.want;
                st.want = ls.want;
              }
            }
          } catch (e) {
          }
          return rec;
        };
      }
      function wrapChoir() {
        if (didChoir || !window.choir || !choir.weather) return;
        didChoir = true;
        var orig = choir.weather;
        choir.weather = function() {
          var w = orig.apply(this, arguments);
          try {
            var ls = state();
            if (ls.seen && ls.day === shopDay()) {
              w.sweet = Math.min(1, (w.sweet || 0) + 0.05);
              if (!w.line) w.line = first(ls.n) + " of " + ls.civ + " is on the aisle.";
            }
          } catch (e) {
          }
          return w;
        };
      }
      function whisper() {
        if (lastGold && now() - lastGoldAt < 12) return lastGold;
        return "";
      }
      function line() {
        var ls = state();
        if (ls.last) return ls.last;
        return whisper();
      }
      function seedWiki() {
        try {
          var wiki = typeof WIKI === "function" ? WIKI() : window.WIKI;
          if (!wiki || !wiki.push) return;
          if (wiki.some(function(a) {
            return a && a.id === "k_lord";
          })) return;
          wiki.push({
            id: "k_lord",
            sec: "The chronicle",
            t: "A civilization has a mouth",
            tags: "leader agenda civ unique inland kingdom",
            w: "<p>Fin's inland kingdom does not send a screen. A person of that civ comes to the glass with a like, a hate, a kind they will not skip. Unique is how they bag, not a card.</p><p><b>What to do about it:</b> listen. A wet aisle will lose the ones who hate wet. A named line will keep the ones who came for names.</p>"
          });
        } catch (e) {
        }
      }
      function tick() {
        try {
          wrapBrowse();
          wrapChoir();
          seedWiki();
          if (now() - lastTick > 1.2) lastTick = now();
        } catch (e) {
        }
      }
      function edge() {
        var ls = state();
        return ls.seen && ls.day === shopDay() ? 0.04 : 0;
      }
      window.lord = {
        whisper,
        line,
        of: state,
        edge,
        seed: function() {
          var rec = { kind: "collector", phase: "look", line: "", name: "" };
          var st = { phase: "look", bought: false };
          arrive(rec, st, true);
          return state();
        }
      };
      if (window.__onBeat) window.__onBeat(tick, 240);
      else
        setTimeout(function loop() {
          tick();
          setTimeout(loop, 240);
        }, 220);
    })();
  } catch (e) {
    console.error("Layer lord.js failed to start:", e);
  }

  // src/layers/great.js
  try {
    (function() {
      "use strict";
      var lastTick = 0;
      var lastGold = "";
      var lastGoldAt = 0;
      var didBrowse = false;
      var didChoir = false;
      var didFish = false;
      var ROLES = ["merchant", "prophet", "admiral", "engineer", "artist", "scientist"];
      function now() {
        return typeof performance !== "undefined" ? performance.now() / 1e3 : Date.now() / 1e3;
      }
      function gs() {
        try {
          if (typeof gameState === "function") return gameState();
          if (typeof gameState === "object" && gameState) return gameState;
        } catch (e) {
        }
        return null;
      }
      function year() {
        try {
          if (window.saga && typeof saga.year === "function") return saga.year();
        } catch (e) {
        }
        return 1e3;
      }
      function shopDay() {
        try {
          var g = gs();
          if (g && isFinite(g.t)) return Math.floor((g.t % 1e9 + 1e9) % 1e9 / 2400);
        } catch (e) {
        }
        return 0;
      }
      function because(text) {
        if (!text) return;
        try {
          if (window.weave && weave.because) weave.because(text);
        } catch (e) {
        }
        try {
          if (window.desk && desk.think) desk.think("great", text);
        } catch (e2) {
        }
      }
      function egg(id, line2) {
        try {
          if (typeof findEgg === "function") findEgg(id, line2);
        } catch (e) {
        }
      }
      function gold(text, force) {
        if (!text) return;
        lastGold = text;
        lastGoldAt = now();
        try {
          var el = document.getElementById("hookWhisper");
          if (el && (force || el.textContent !== text)) {
            el.textContent = text;
            el.classList.add("on", "pop");
          }
        } catch (e) {
        }
      }
      function aFig() {
        try {
          var w = window.realm && realm.world && realm.world();
          if (w && w.figs) {
            for (var i = w.figs.length - 1; i >= 0; i--) {
              if (w.figs[i] && w.figs[i].n && (w.figs[i].fame || 0) > 0.3) return w.figs[i];
            }
            if (w.figs.length) return w.figs[w.figs.length - 1];
          }
        } catch (e) {
        }
        return { n: "Ithen Ravos", job: "merchant", fame: 0.6 };
      }
      function kindOf() {
        try {
          if (window.guild && guild.mandate) return String(guild.mandate() || "goldfish");
        } catch (e) {
        }
        return "goldfish";
      }
      function hash32(s) {
        var h = 2166136261;
        s = String(s || "");
        for (var i = 0; i < s.length; i++) {
          h ^= s.charCodeAt(i);
          h = Math.imul(h, 16777619);
        }
        return h >>> 0;
      }
      function state() {
        var g = gs();
        if (g && g.great && g.great.n) return g.great;
        var fig = aFig();
        var role = ROLES[hash32(fig.n) % ROLES.length];
        var st = { n: fig.n, role, last: "", seen: false, day: -1, did: false, want: kindOf() };
        try {
          if (g) g.great = st;
        } catch (e) {
        }
        return st;
      }
      function first(n) {
        return String(n || "Someone").split(" ")[0];
      }
      function roleLine(st) {
        if (st.role === "merchant") return "I'll take the pair. Double.";
        if (st.role === "prophet") return "This water has a god. Listen.";
        if (st.role === "admiral") return "A pair of " + st.want + " for the flagship.";
        if (st.role === "engineer") return "The filter will hold if you let me look.";
        if (st.role === "artist") return "I came to cut the morning into the glass.";
        return "This water has a name. I can tell you it.";
      }
      function act(st) {
        if (st.did) return;
        st.did = true;
        if (st.role === "merchant") {
          try {
            var g = gs();
            if (g) g.coins = (g.coins || 0) + 18;
          } catch (e) {
          }
        } else if (st.role === "prophet") {
          try {
            if (window.vow && vow.found) vow.found(st.want);
          } catch (e2) {
          }
        } else if (st.role === "engineer") {
          try {
            if (window.craft && craft.noteBag) craft.noteBag(true);
          } catch (e3) {
          }
          try {
            if (window.fever && fever.treat) fever.treat();
          } catch (e4) {
          }
        } else if (st.role === "artist") {
          because(first(st.n) + " cut a morning into the glass.");
        } else if (st.role === "scientist") {
          try {
            if (window.know && know.diagnose) know.diagnose();
          } catch (e5) {
          }
        }
      }
      function arrive(rec, st, force) {
        var gs_ = state();
        if (gs_.seen && gs_.day === shopDay() && !force) return false;
        if (shopDay() < 1 && !force) return false;
        rec.kind = "collector";
        rec.name = gs_.n;
        rec.phase = "look";
        rec.want = gs_.want;
        rec.line = roleLine(gs_);
        if (st) {
          st._great = 1;
          st.name = gs_.n;
          st.guestName = gs_.n;
          st.want = gs_.want;
          st.line = rec.line;
          st.lineUntil = now() + 7;
          st.until = Math.max(st.until || 0, now() + 8);
          if (gs_.role === "merchant" || gs_.role === "admiral") st.bought = false;
        }
        gs_.seen = true;
        gs_.day = shopDay();
        act(gs_);
        var line2 = first(gs_.n) + " the " + gs_.role + " is on the aisle.";
        gs_.last = line2;
        because(line2 + " " + rec.line);
        gold(line2, true);
        egg("great" + gs_.role, line2);
        return true;
      }
      function wrapBrowse() {
        if (didBrowse || !window.shopBrowse) return;
        didBrowse = true;
        var orig = window.shopBrowse;
        window.shopBrowse = function(idx, slot, W, floorY, personS, simT) {
          var rec = orig.apply(this, arguments);
          try {
            if (!rec) return rec;
            var st = window.shopLife && shopLife.browse ? shopLife.browse()[idx] : null;
            var keep = typeof keepGuest === "function" ? keepGuest(st) : !!(st && (st._lateMae || st._goingHold || st._lateKid || st._mask));
            var gs_ = state();
            if (!keep && rec.phase === "look" && st && !st._great && shopDay() >= 1 && !gs_.seen && ((idx | 0) === 1 || rec.kind === "collector")) {
              arrive(rec, st);
            }
            if (gs_.seen && gs_.day === shopDay() && rec.phase === "look" && st && !st._great && !st._greatSaid && !keep) {
              st._greatSaid = 1;
              rec.line = rec.kind === "kid" ? "That's not a regular." : first(gs_.n) + " the " + gs_.role + " is here.";
              st.line = rec.line;
            }
          } catch (e) {
          }
          return rec;
        };
      }
      function wrapChoir() {
        if (didChoir || !window.choir || !choir.weather) return;
        didChoir = true;
        var orig = choir.weather;
        choir.weather = function() {
          var w = orig.apply(this, arguments);
          try {
            var gs_ = state();
            if (gs_.seen && gs_.day === shopDay()) {
              w.sweet = Math.min(1, (w.sweet || 0) + (gs_.role === "engineer" || gs_.role === "prophet" ? 0.12 : 0.06));
              if (!w.line) w.line = first(gs_.n) + " the " + gs_.role + " is on the aisle.";
            }
          } catch (e) {
          }
          return w;
        };
      }
      function wrapFish() {
        if (didFish || typeof window.drawFishSprite !== "function") return;
        didFish = true;
        var orig = window.drawFishSprite;
        window.drawFishSprite = function(a) {
          try {
            var gs_ = state();
            if (gs_.seen && gs_.day === shopDay() && gs_.role === "scientist" && a && a.fish && a.fish.nick) {
              a.fish._gaze = now();
            }
          } catch (e) {
          }
          return orig.apply(this, arguments);
        };
      }
      function whisper() {
        if (lastGold && now() - lastGoldAt < 12) return lastGold;
        return "";
      }
      function line() {
        return state().last || whisper();
      }
      function seedWiki() {
        try {
          var wiki = typeof WIKI === "function" ? WIKI() : window.WIKI;
          if (!wiki || !wiki.push) return;
          if (wiki.some(function(a) {
            return a && a.id === "k_great";
          })) return;
          wiki.push({
            id: "k_great",
            sec: "The chronicle",
            t: "A great person walks in once",
            tags: "great merchant prophet admiral engineer artist scientist",
            w: "<p>A great person walks in once. A merchant pays double. A prophet founds a belief. An admiral wants a pair for a flagship. An engineer sweeten the filter. An artist cuts a morning. A scientist names the water.</p><p><b>What to do about it:</b> be on the aisle. Life will name the walk. The gold line will too.</p>"
          });
        } catch (e) {
        }
      }
      function tick() {
        try {
          wrapBrowse();
          wrapChoir();
          wrapFish();
          seedWiki();
          if (now() - lastTick > 1.1) lastTick = now();
        } catch (e) {
        }
      }
      function edge() {
        var gs_ = state();
        return gs_.seen && gs_.day === shopDay() ? 0.06 : 0;
      }
      window.great = {
        whisper,
        line,
        of: state,
        edge,
        seed: function() {
          var rec = { kind: "collector", phase: "look", line: "", name: "" };
          var st = { phase: "look", bought: false };
          arrive(rec, st, true);
          return state();
        }
      };
      if (window.__onBeat) window.__onBeat(tick, 240);
      else
        setTimeout(function loop() {
          tick();
          setTimeout(loop, 240);
        }, 220);
    })();
  } catch (e) {
    console.error("Layer great.js failed to start:", e);
  }

  // src/layers/wonder.js
  try {
    (function() {
      "use strict";
      var lastTick = 0;
      var lastGold = "";
      var lastGoldAt = 0;
      var didBrowse = false;
      var didChoir = false;
      var overlay = null;
      var octx = null;
      var NAMES = ["First Net", "Quiet Oak", "Salt Spire", "Night Ledger", "Wheel House", "Dry Eye"];
      function now() {
        return typeof performance !== "undefined" ? performance.now() / 1e3 : Date.now() / 1e3;
      }
      function gs() {
        try {
          if (typeof gameState === "function") return gameState();
          if (typeof gameState === "object" && gameState) return gameState;
        } catch (e) {
        }
        return null;
      }
      function year() {
        try {
          if (window.saga && typeof saga.year === "function") return saga.year();
        } catch (e) {
        }
        return 1e3;
      }
      function shopDay() {
        try {
          var g = gs();
          if (g && isFinite(g.t)) return Math.floor((g.t % 1e9 + 1e9) % 1e9 / 2400);
        } catch (e) {
        }
        return 0;
      }
      function because(text) {
        if (!text) return;
        try {
          if (window.weave && weave.because) weave.because(text);
        } catch (e) {
        }
        try {
          if (window.desk && desk.think) desk.think("wonder", text);
        } catch (e2) {
        }
      }
      function egg(id, line2) {
        try {
          if (typeof findEgg === "function") findEgg(id, line2);
        } catch (e) {
        }
      }
      function gold(text, force) {
        if (!text) return;
        lastGold = text;
        lastGoldAt = now();
        try {
          var el = document.getElementById("hookWhisper");
          if (el && (force || el.textContent !== text)) {
            el.textContent = text;
            el.classList.add("on", "pop");
          }
        } catch (e) {
        }
      }
      function anArt() {
        try {
          var w = window.realm && realm.world && realm.world();
          if (w && w.arts && w.arts.length) {
            for (var i = w.arts.length - 1; i >= 0; i--) {
              if (w.arts[i] && w.arts[i].n) return w.arts[i].n;
            }
          }
        } catch (e) {
        }
        try {
          var w2 = window.realm && realm.world && realm.world();
          var site = w2 && w2.sites && (w2.sites[2] || w2.sites[1]);
          var nm = NAMES[(year() + (site ? site.i : 0)) % NAMES.length];
          return "the " + nm + (site ? " of " + site.n : "");
        } catch (e2) {
        }
        return "the First Net";
      }
      function kindOf() {
        try {
          if (window.guild && guild.mandate) return String(guild.mandate() || "goldfish");
        } catch (e) {
        }
        return "goldfish";
      }
      function letterWant() {
        try {
          var L = window.going && going.letter && going.letter();
          if (L && L.open && L.want) return String(L.want);
        } catch (e) {
        }
        return "";
      }
      function state() {
        var g = gs();
        if (g && g.wonder && g.wonder.n) return g.wonder;
        var want = kindOf();
        var skip = letterWant();
        if (skip && want === skip) want = want === "goldfish" ? "tetra" : "goldfish";
        var st = { n: anArt(), want, done: false, last: "", seen: false, day: -1, pilgrims: 0 };
        try {
          if (g) g.wonder = st;
        } catch (e) {
        }
        return st;
      }
      function finish(force) {
        var st = state();
        if (st.done && !force) return;
        st.done = true;
        var line2 = st.n + " is raised. Pilgrims are on the road.";
        st.last = line2;
        because(line2);
        gold(line2, true);
        egg("wonderup", line2);
        try {
          if (window.weave && weave.bumpWord) weave.bumpWord(0.07);
        } catch (e) {
        }
      }
      function noteBag(kind) {
        var st = state();
        if (st.done) return;
        if (st.want && kind && String(kind).toLowerCase().indexOf(String(st.want).toLowerCase()) >= 0) {
          finish();
        }
      }
      function edge() {
        return state().done ? 0.07 : 0;
      }
      function ask(rec, st, force) {
        var ws = state();
        if (ws.done) return false;
        if (ws.seen && ws.day === shopDay() && !force) return false;
        if (shopDay() < 1 && !force) return false;
        rec.kind = rec.kind === "kid" ? "collector" : rec.kind || "collector";
        rec.phase = "look";
        rec.want = ws.want;
        rec.line = "For " + ws.n + ". A pair of " + ws.want + ".";
        if (st) {
          st._wonder = 1;
          st.want = ws.want;
          st.line = rec.line;
          st.lineUntil = now() + 6;
        }
        ws.seen = true;
        ws.day = shopDay();
        var line2 = "They are raising " + ws.n + ".";
        ws.last = line2;
        because(line2);
        gold(line2, true);
        egg("wonderask", line2);
        return true;
      }
      function wrapBrowse() {
        if (didBrowse || !window.shopBrowse) return;
        didBrowse = true;
        var orig = window.shopBrowse;
        window.shopBrowse = function(idx, slot, W, floorY, personS, simT) {
          var rec = orig.apply(this, arguments);
          try {
            if (!rec) return rec;
            var st = window.shopLife && shopLife.browse ? shopLife.browse()[idx] : null;
            var keep = typeof keepGuest === "function" ? keepGuest(st) : !!(st && (st._lateMae || st._goingHold || st._lateKid || st._mask));
            var ws = state();
            if (!keep && !ws.done && rec.phase === "look" && st && !st._wonder && shopDay() >= 1 && !ws.seen && (rec.kind === "collector" || (idx | 0) === 0)) {
              ask(rec, st);
            }
            if (rec.phase === "pay" && st && (st._wonder || ws.want && rec.want === ws.want)) {
              noteBag(rec.want || st.want);
            }
            if (ws.done && rec.phase === "look" && st && !st._wondSaid && !keep) {
              st._wondSaid = 1;
              rec.line = rec.kind === "kid" ? "I came to see it." : "I came for " + ws.n + ".";
              st.line = rec.line;
              st.until = Math.max(st.until || 0, now() + 5);
              ws.pilgrims++;
              if (rec.kind !== "collector" && Math.random() < 0.45) {
                rec.phase = "leave";
                st.phase = "leave";
                st.bought = false;
              }
            }
          } catch (e) {
          }
          return rec;
        };
      }
      function wrapChoir() {
        if (didChoir || !window.choir || !choir.weather) return;
        didChoir = true;
        var orig = choir.weather;
        choir.weather = function() {
          var w = orig.apply(this, arguments);
          try {
            var ws = state();
            if (ws.done) {
              w.sweet = Math.min(1, (w.sweet || 0) + 0.11);
              if (!w.line) w.line = ws.n + " is raised. The water is a site.";
            } else if (ws.seen && !w.line) {
              w.line = "They are raising " + ws.n + ".";
            }
          } catch (e) {
          }
          return w;
        };
      }
      function sceneName() {
        try {
          if (document.body.classList.contains("titling")) return "title";
          if (typeof sceneNow === "function") return String(sceneNow() || "");
        } catch (e) {
        }
        return "shop";
      }
      function ensureOverlay() {
        if (overlay && overlay.parentNode) return;
        overlay = document.getElementById("wonderVeil");
        if (!overlay) {
          overlay = document.createElement("canvas");
          overlay.id = "wonderVeil";
          overlay.setAttribute("aria-hidden", "true");
          document.body.appendChild(overlay);
        }
        octx = overlay.getContext("2d");
      }
      function resize() {
        ensureOverlay();
        var dpr = Math.min(2, window.devicePixelRatio || 1);
        var w = window.innerWidth;
        var h = window.innerHeight;
        if (overlay.width !== (w * dpr | 0) || overlay.height !== (h * dpr | 0)) {
          overlay.width = w * dpr | 0;
          overlay.height = h * dpr | 0;
          overlay.style.cssText = "position:fixed;inset:0;width:100%;height:100%;pointer-events:none;z-index:4";
          octx.setTransform(dpr, 0, 0, dpr, 0, 0);
        }
      }
      function drawWonder() {
        var sc = sceneName();
        if (sc === "title" || sc === "work" || sc !== "shop" && sc !== "front") {
          if (octx && overlay) octx.clearRect(0, 0, overlay.width, overlay.height);
          return;
        }
        if (!state().done) {
          if (octx && overlay) octx.clearRect(0, 0, overlay.width, overlay.height);
          return;
        }
        resize();
        var w = window.innerWidth;
        var h = window.innerHeight;
        octx.clearRect(0, 0, w, h);
        var x = w * 0.28;
        var y = h * 0.2;
        var t = now();
        var glow = 0.16 + 0.08 * Math.sin(t * 1.1);
        octx.save();
        octx.translate(x, y);
        var g = octx.createRadialGradient(0, -8, 2, 0, -8, 34);
        g.addColorStop(0, "rgba(220,211,255," + glow.toFixed(3) + ")");
        g.addColorStop(1, "rgba(220,211,255,0)");
        octx.fillStyle = g;
        octx.beginPath();
        octx.arc(0, -8, 34, 0, 7);
        octx.fill();
        octx.fillStyle = "rgba(24,18,32,.78)";
        octx.beginPath();
        octx.moveTo(0, -22);
        octx.lineTo(12, 10);
        octx.lineTo(-12, 10);
        octx.closePath();
        octx.fill();
        octx.strokeStyle = "rgba(244,196,83,.9)";
        octx.lineWidth = 1.3;
        octx.stroke();
        octx.restore();
        octx.font = "600 11px Nunito, sans-serif";
        octx.fillStyle = "rgba(220,211,255,.86)";
        octx.textAlign = "center";
        octx.fillText(state().n, x, y + 22);
      }
      function whisper() {
        if (lastGold && now() - lastGoldAt < 12) return lastGold;
        return "";
      }
      function line() {
        return state().last || whisper();
      }
      function seedWiki() {
        try {
          var wiki = typeof WIKI === "function" ? WIKI() : window.WIKI;
          if (!wiki || !wiki.push) return;
          if (wiki.some(function(a) {
            return a && a.id === "k_wonder";
          })) return;
          wiki.push({
            id: "k_wonder",
            sec: "The chronicle",
            t: "They are raising a wonder",
            tags: "wonder pilgrimage pair glass site",
            w: "<p>An inland town is raising a work. They need a pair. Fill it and pilgrims come to look. They do not always bag. The object sits in the glass next to the keeper's key. The choir sweetens. The street came because of it.</p><p><b>What to do about it:</b> keep two of what they asked for. Life will say if it is raised.</p>"
          });
        } catch (e) {
        }
      }
      function tick() {
        try {
          wrapBrowse();
          wrapChoir();
          seedWiki();
          drawWonder();
          if (now() - lastTick > 1.1) lastTick = now();
        } catch (e) {
        }
      }
      window.wonder = {
        whisper,
        line,
        of: state,
        edge,
        seed: function() {
          var rec = { kind: "collector", phase: "look", line: "", name: "" };
          var st = { phase: "look", bought: false };
          ask(rec, st, true);
          finish(true);
          return state();
        }
      };
      if (window.__onBeat) window.__onBeat(tick, 200);
      else
        setTimeout(function loop() {
          tick();
          setTimeout(loop, 200);
        }, 200);
    })();
  } catch (e) {
    console.error("Layer wonder.js failed to start:", e);
  }

  // src/layers/envoy.js
  try {
    (function() {
      "use strict";
      var lastTick = 0;
      var lastGold = "";
      var lastGoldAt = 0;
      var didBrowse = false;
      var didChoir = false;
      function now() {
        return typeof performance !== "undefined" ? performance.now() / 1e3 : Date.now() / 1e3;
      }
      function gs() {
        try {
          if (typeof gameState === "function") return gameState();
          if (typeof gameState === "object" && gameState) return gameState;
        } catch (e) {
        }
        return null;
      }
      function year() {
        try {
          if (window.saga && typeof saga.year === "function") return saga.year();
        } catch (e) {
        }
        return 1e3;
      }
      function shopDay() {
        try {
          var g = gs();
          if (g && isFinite(g.t)) return Math.floor((g.t % 1e9 + 1e9) % 1e9 / 2400);
        } catch (e) {
        }
        return 0;
      }
      function because(text) {
        if (!text) return;
        try {
          if (window.weave && weave.because) weave.because(text);
        } catch (e) {
        }
        try {
          if (window.desk && desk.think) desk.think("envoy", text);
        } catch (e2) {
        }
      }
      function egg(id, line2) {
        try {
          if (typeof findEgg === "function") findEgg(id, line2);
        } catch (e) {
        }
      }
      function gold(text, force) {
        if (!text) return;
        lastGold = text;
        lastGoldAt = now();
        try {
          var el = document.getElementById("hookWhisper");
          if (el && (force || el.textContent !== text)) {
            el.textContent = text;
            el.classList.add("on", "pop");
          }
        } catch (e) {
        }
      }
      function letterFrom() {
        try {
          var L = window.going && going.letter && going.letter();
          if (L && L.from) return String(L.from);
        } catch (e) {
        }
        return "";
      }
      function aHamlet() {
        try {
          var w = window.realm && realm.world && realm.world();
          var skip = letterFrom();
          var skipHold = "";
          try {
            if (window.hold && hold.of) skipHold = hold.of().site || "";
          } catch (eH) {
          }
          if (w && w.sites) {
            for (var i = 1; i < w.sites.length; i++) {
              var s = w.sites[i];
              if (!s || s.ruin || !s.n) continue;
              if (skip && s.n === skip) continue;
              if (skipHold && s.n === skipHold) continue;
              if (s.kind === "hamlet" || s.kind === "town" || s.kind === "abbey") return s;
            }
            return w.sites[3] || w.sites[1];
          }
        } catch (e) {
        }
        return { n: "Ithwick", kind: "hamlet", pop: 60, fish: "river" };
      }
      function kindOf() {
        try {
          if (window.wild && wild.of) {
            var p = wild.of().pop || {};
            for (var k2 in p) if (p[k2] > 0.4) return k2 === "gold" ? "goldfish" : k2;
          }
        } catch (e) {
        }
        try {
          if (window.guild && guild.mandate) return String(guild.mandate() || "betta");
        } catch (e2) {
        }
        return "betta";
      }
      function state() {
        var g = gs();
        if (g && g.envoy && g.envoy.site) return g.envoy;
        var s = aHamlet();
        var st = { site: s.n, want: kindOf(), last: "", seen: false, day: -1, suzerain: false, lost: false, filled: false };
        try {
          if (g) g.envoy = st;
        } catch (e) {
        }
        return st;
      }
      function arrive(rec, st, force) {
        var es = state();
        if (es.lost) return false;
        if (es.seen && es.day === shopDay() && !force) return false;
        if (shopDay() < 1 && !force) return false;
        var ham = aHamlet();
        if (!es.site || es.site === ham.n || window.hold && hold.of && hold.of().site === es.site) {
          es.site = ham.n;
          es.want = es.want || kindOf();
        }
        rec.kind = rec.kind === "kid" ? "neighbor" : rec.kind || "neighbor";
        rec.phase = "look";
        rec.want = es.want;
        rec.line = "From " + es.site + ". We only bag here if you keep " + es.want + ".";
        rec.name = rec.name || "Envoy of " + es.site;
        if (st) {
          st._envoy = 1;
          st.want = es.want;
          st.line = rec.line;
          st.guestName = rec.name;
          st.lineUntil = now() + 6;
        }
        es.seen = true;
        es.day = shopDay();
        var line2 = "An envoy from " + es.site + ".";
        es.last = line2;
        because(line2 + " They asked for " + es.want + ".");
        gold(line2, true);
        egg("envoyin", line2);
        return true;
      }
      function noteBag(kind) {
        var es = state();
        if (es.suzerain || es.lost) return;
        if (es.want && kind && String(kind).toLowerCase().indexOf(String(es.want).toLowerCase()) >= 0) {
          es.suzerain = true;
          es.filled = true;
          var line2 = es.site + " bags here. They said so.";
          es.last = line2;
          because(line2);
          gold(line2, true);
          egg("envoysuz", line2);
        }
      }
      function watchLost() {
        var es = state();
        if (!es.seen || es.suzerain || es.lost) return;
        if (shopDay() - (es.day || 0) < 3) return;
        es.lost = true;
        var line2 = es.site + " went to Haymarket.";
        es.last = line2;
        because(line2);
        gold(line2, true);
        egg("envoylost", line2);
      }
      function edge() {
        var es = state();
        if (es.suzerain) return 0.09;
        if (es.lost) return -0.08;
        return 0;
      }
      function wrapBrowse() {
        if (didBrowse || !window.shopBrowse) return;
        didBrowse = true;
        var orig = window.shopBrowse;
        window.shopBrowse = function(idx, slot, W, floorY, personS, simT) {
          var rec = orig.apply(this, arguments);
          try {
            if (!rec) return rec;
            var st = window.shopLife && shopLife.browse ? shopLife.browse()[idx] : null;
            var keep = typeof keepGuest === "function" ? keepGuest(st) : !!(st && (st._lateMae || st._goingHold || st._lateKid || st._mask));
            var es = state();
            if (!keep && rec.phase === "look" && st && !st._envoy && shopDay() >= 1 && !es.seen && !es.lost && ((idx | 0) === 2 || rec.kind === "neighbor")) {
              arrive(rec, st);
            }
            if (rec.phase === "pay" && st && st._envoy) noteBag(rec.want || st.want);
            if (es.lost && rec.phase === "look" && st && !st._envSaid && !keep) {
              st._envSaid = 1;
              rec.line = rec.kind === "kid" ? "They went up the market." : es.site + " bags at Haymarket now.";
              rec.phase = "leave";
              st.phase = "leave";
              st.bought = false;
              st.line = rec.line;
            } else if (es.suzerain && rec.phase === "look" && st && !st._envSaid && !keep && rec.kind !== "kid") {
              st._envSaid = 1;
              rec.line = "We don't go to Haymarket. " + es.site + " said.";
              st.line = rec.line;
            }
          } catch (e) {
          }
          return rec;
        };
      }
      function wrapChoir() {
        if (didChoir || !window.choir || !choir.weather) return;
        didChoir = true;
        var orig = choir.weather;
        choir.weather = function() {
          var w = orig.apply(this, arguments);
          try {
            var es = state();
            if (es.suzerain) {
              w.sweet = Math.min(1, (w.sweet || 0) + 0.08);
              if (!w.line) w.line = es.site + " bags here.";
            } else if (es.lost) {
              w.sour = Math.min(1, (w.sour || 0) + 0.07);
            }
          } catch (e) {
          }
          return w;
        };
      }
      function whisper() {
        if (lastGold && now() - lastGoldAt < 12) return lastGold;
        return "";
      }
      function line() {
        return state().last || whisper();
      }
      function seedWiki() {
        try {
          var wiki = typeof WIKI === "function" ? WIKI() : window.WIKI;
          if (!wiki || !wiki.push) return;
          if (wiki.some(function(a) {
            return a && a.id === "k_envoy";
          })) return;
          wiki.push({
            id: "k_envoy",
            sec: "The quarter",
            t: "A hamlet that only bags here",
            tags: "envoy haymarket hamlet",
            w: "<p>A small hold sends one person. Fill the pair and they stop going to Haymarket. Miss three mornings and they do. Who still bags here is who they still walk for.</p><p><b>What to do about it:</b> keep two of what they asked for. The gold line will name the walk, or the loss.</p>"
          });
        } catch (e) {
        }
      }
      function tick() {
        try {
          wrapBrowse();
          wrapChoir();
          seedWiki();
          if (now() - lastTick > 1.5) {
            lastTick = now();
            watchLost();
          }
        } catch (e) {
        }
      }
      window.envoy = {
        whisper,
        line,
        of: state,
        edge,
        seed: function() {
          var rec = { kind: "neighbor", phase: "look", line: "", name: "" };
          var st = { phase: "look", bought: false };
          arrive(rec, st, true);
          return state();
        }
      };
      if (window.__onBeat) window.__onBeat(tick, 240);
      else
        setTimeout(function loop() {
          tick();
          setTimeout(loop, 240);
        }, 220);
    })();
  } catch (e) {
    console.error("Layer envoy.js failed to start:", e);
  }

  // src/layers/vow.js
  try {
    (function() {
      "use strict";
      var lastTick = 0;
      var lastGold = "";
      var lastGoldAt = 0;
      var didBrowse = false;
      var didChoir = false;
      var didFish = false;
      function now() {
        return typeof performance !== "undefined" ? performance.now() / 1e3 : Date.now() / 1e3;
      }
      function gs() {
        try {
          if (typeof gameState === "function") return gameState();
          if (typeof gameState === "object" && gameState) return gameState;
        } catch (e) {
        }
        return null;
      }
      function year() {
        try {
          if (window.saga && typeof saga.year === "function") return saga.year();
        } catch (e) {
        }
        return 1e3;
      }
      function shopDay() {
        try {
          var g = gs();
          if (g && isFinite(g.t)) return Math.floor((g.t % 1e9 + 1e9) % 1e9 / 2400);
        } catch (e) {
        }
        return 0;
      }
      function because(text) {
        if (!text) return;
        try {
          if (window.weave && weave.because) weave.because(text);
        } catch (e) {
        }
        try {
          if (window.desk && desk.think) desk.think("vow", text);
        } catch (e2) {
        }
      }
      function egg(id, line2) {
        try {
          if (typeof findEgg === "function") findEgg(id, line2);
        } catch (e) {
        }
      }
      function gold(text, force) {
        if (!text) return;
        lastGold = text;
        lastGoldAt = now();
        try {
          var el = document.getElementById("hookWhisper");
          if (el && (force || el.textContent !== text)) {
            el.textContent = text;
            el.classList.add("on", "pop");
          }
        } catch (e) {
        }
      }
      function godName() {
        try {
          if (window.faith && faith.today) {
            var t = faith.today();
            if (t && t.n) return t.n;
          }
        } catch (e) {
        }
        try {
          var w = window.realm && realm.world && realm.world();
          if (w && w.civs && w.civs[0] && w.civs[0].faith) return w.civs[0].faith;
        } catch (e2) {
        }
        return "the Salt Mother";
      }
      function kindOf() {
        try {
          if (window.faith && faith.taboo) {
            var t = faith.taboo();
            if (t) return t;
          }
        } catch (e) {
        }
        try {
          if (window.guild && guild.mandate) return String(guild.mandate() || "tetra");
        } catch (e2) {
        }
        return "tetra";
      }
      function state() {
        var g = gs();
        if (g && g.vow && typeof g.vow === "object") return g.vow;
        var st = { last: "", found: false, god: "", holy: "", day: -1, seen: false };
        try {
          if (g) g.vow = st;
        } catch (e) {
        }
        return st;
      }
      function found(kind, force) {
        var st = state();
        if (st.found && !force) return st;
        st.found = true;
        st.god = godName();
        st.holy = kind || kindOf();
        st.day = shopDay();
        var line2 = st.god + " took the " + st.holy + ". It is holy now.";
        st.last = line2;
        because(line2);
        if (shopDay() >= 1 || force) gold(line2, true);
        egg("vowfound", line2);
        return st;
      }
      function fishKind(f) {
        if (!f) return "";
        try {
          var S = typeof O !== "undefined" ? O : typeof SPECIES !== "undefined" ? SPECIES : null;
          if (S && f.sp != null && S[f.sp]) {
            return String(S[f.sp].gname || S[f.sp].name || S[f.sp].vname || "").toLowerCase();
          }
        } catch (e) {
        }
        return "";
      }
      function hasHoly() {
        var st = state();
        if (!st.found || !st.holy) return false;
        try {
          var list = typeof allFish === "function" ? allFish() || [] : [];
          var q = st.holy.toLowerCase();
          for (var i = 0; i < list.length; i++) {
            var k2 = fishKind(list[i]);
            if (k2 && k2.indexOf(q) >= 0) return true;
          }
        } catch (e) {
        }
        return false;
      }
      function countHoly() {
        var st = state();
        var n = 0;
        if (!st.found || !st.holy) return 0;
        try {
          var list = typeof allFish === "function" ? allFish() || [] : [];
          var q = st.holy.toLowerCase();
          for (var i = 0; i < list.length; i++) {
            var k2 = fishKind(list[i]);
            if (k2 && k2.indexOf(q) >= 0) n++;
          }
        } catch (e) {
        }
        return n;
      }
      function edge() {
        var st = state();
        if (!st.found) return 0;
        if (hasHoly()) return 0.07;
        return -0.06;
      }
      function arrive(rec, st, force) {
        var vs = state();
        if (vs.seen && vs.day === shopDay() && !force) return false;
        if (shopDay() < 1 && !force) return false;
        if (!vs.found) found(kindOf(), force);
        rec.kind = rec.kind === "kid" ? "kid" : "collector";
        rec.phase = "look";
        rec.want = vs.holy;
        rec.line = vs.god + " took the " + vs.holy + ". Keep a pair.";
        if (st) {
          st._vow = 1;
          st.want = vs.holy;
          st.line = rec.line;
          st.lineUntil = now() + 6;
        }
        vs.seen = true;
        vs.day = shopDay();
        if (!vs.last) vs.last = rec.line;
        because(rec.line);
        gold(vs.last, true);
        return true;
      }
      function wrapBrowse() {
        if (didBrowse || !window.shopBrowse) return;
        didBrowse = true;
        var orig = window.shopBrowse;
        window.shopBrowse = function(idx, slot, W, floorY, personS, simT) {
          var rec = orig.apply(this, arguments);
          try {
            if (!rec) return rec;
            var st = window.shopLife && shopLife.browse ? shopLife.browse()[idx] : null;
            var keep = typeof keepGuest === "function" ? keepGuest(st) : !!(st && (st._lateMae || st._goingHold || st._lateKid || st._mask));
            var vs = state();
            if (!keep && rec.phase === "look" && st && !st._vow && shopDay() >= 1 && !vs.seen && (rec.kind === "collector" || rec.kind === "kid" || (idx | 0) === 2)) {
              arrive(rec, st);
            }
            if (vs.found && rec.phase === "look" && st && !st._vowSaid && !keep) {
              st._vowSaid = 1;
              if (rec.kind === "kid") {
                rec.line = "The " + vs.holy + " is holy. " + vs.god + " said.";
                st.line = rec.line;
              } else if (countHoly() < 2 && rec.phase === "pay") {
                rec.line = "Not the last " + vs.holy + ". That's a vow.";
                rec.phase = "leave";
                st.phase = "leave";
                st.bought = false;
                st.line = rec.line;
              } else if (rec.kind === "collector") {
                rec.want = rec.want || vs.holy;
                st.want = st.want || vs.holy;
                rec.line = rec.line || "The blessed " + vs.holy + ". I'll take that pair.";
                st.line = rec.line;
              }
            }
            if (vs.found && rec.phase === "pay" && st && vs.holy) {
              var want = String(rec.want || st.want || "").toLowerCase();
              if (want.indexOf(vs.holy.toLowerCase()) >= 0 && countHoly() <= 2) {
                rec.line = "Not the last " + vs.holy + ". That's a vow.";
                rec.phase = "leave";
                st.phase = "leave";
                st.bought = false;
                st.line = rec.line;
              }
            }
          } catch (e) {
          }
          return rec;
        };
      }
      function wrapChoir() {
        if (didChoir || !window.choir || !choir.weather) return;
        didChoir = true;
        var orig = choir.weather;
        choir.weather = function() {
          var w = orig.apply(this, arguments);
          try {
            var vs = state();
            if (vs.found && hasHoly()) {
              w.sweet = Math.min(1, (w.sweet || 0) + 0.1);
              w.bless = Math.max(w.bless || 0, 0.1);
              if (!w.line) w.line = vs.god + " is in the water. The " + vs.holy + ".";
            } else if (vs.found) {
              w.sour = Math.min(1, (w.sour || 0) + 0.08);
            }
          } catch (e) {
          }
          return w;
        };
      }
      function wrapFish() {
        if (didFish || typeof window.drawFishSprite !== "function") return;
        didFish = true;
        var orig = window.drawFishSprite;
        window.drawFishSprite = function(a) {
          try {
            var vs = state();
            if (vs.found && vs.holy && a && a.fish) {
              var k2 = fishKind(a.fish);
              if (k2 && k2.indexOf(vs.holy.toLowerCase()) >= 0) a.fish._gaze = now();
            }
          } catch (e) {
          }
          return orig.apply(this, arguments);
        };
      }
      function whisper() {
        if (lastGold && now() - lastGoldAt < 12) return lastGold;
        return "";
      }
      function line() {
        return state().last || whisper();
      }
      function seedWiki() {
        try {
          var wiki = typeof WIKI === "function" ? WIKI() : window.WIKI;
          if (!wiki || !wiki.push) return;
          if (wiki.some(function(a) {
            return a && a.id === "k_vow";
          })) return;
          wiki.push({
            id: "k_vow",
            sec: "The chronicle",
            t: "A belief that walks",
            tags: "religion vow pantheon holy taboo prophet faith",
            w: "<p>Faith already has a festival. This is the founding. A prophet, a kind that is holy, a last-of-a-pair that is taboo. Kids repeat it. People will bag the blessed one even when the aisle is a little wet. They will not bag the last. The choir knows which voice is holy.</p><p><b>What to do about it:</b> keep two of the holy kind. The gold line will name the vow.</p>"
          });
        } catch (e) {
        }
      }
      function tick() {
        try {
          wrapBrowse();
          wrapChoir();
          wrapFish();
          seedWiki();
          if (now() - lastTick > 1.2) lastTick = now();
        } catch (e) {
        }
      }
      window.vow = {
        whisper,
        line,
        of: state,
        edge,
        found,
        seed: function() {
          found(kindOf(), true);
          var rec = { kind: "collector", phase: "look", line: "", name: "" };
          var st = { phase: "look", bought: false };
          arrive(rec, st, true);
          return state();
        }
      };
      if (window.__onBeat) window.__onBeat(tick, 240);
      else
        setTimeout(function loop() {
          tick();
          setTimeout(loop, 240);
        }, 220);
    })();
  } catch (e) {
    console.error("Layer vow.js failed to start:", e);
  }

  // src/layers/siege.js
  try {
    (function() {
      "use strict";
      var lastTick = 0;
      var lastGold = "";
      var lastGoldAt = 0;
      var didBrowse = false;
      var didChoir = false;
      var didFish = false;
      var overlay = null;
      var octx = null;
      function now() {
        return typeof performance !== "undefined" ? performance.now() / 1e3 : Date.now() / 1e3;
      }
      function gs() {
        try {
          if (typeof gameState === "function") return gameState();
          if (typeof gameState === "object" && gameState) return gameState;
        } catch (e) {
        }
        return null;
      }
      function year() {
        try {
          if (window.saga && typeof saga.year === "function") return saga.year();
        } catch (e) {
        }
        return 1e3;
      }
      function shopDay() {
        try {
          var g = gs();
          if (g && isFinite(g.t)) return Math.floor((g.t % 1e9 + 1e9) % 1e9 / 2400);
        } catch (e) {
        }
        return 0;
      }
      function hour() {
        try {
          if (typeof jt === "function") return jt() * 24;
          var g = gs();
          if (g && isFinite(g.t)) return (g.t % 2400 + 2400) % 2400 / 100;
        } catch (e) {
        }
        return 12;
      }
      function night() {
        var h = hour();
        return h < 6.2 || h >= 19.2;
      }
      function sceneName() {
        try {
          if (document.body.classList.contains("titling")) return "title";
          if (typeof sceneNow === "function") return String(sceneNow() || "");
        } catch (e) {
        }
        return "shop";
      }
      function because(text) {
        if (!text) return;
        try {
          if (window.weave && weave.because) weave.because(text);
        } catch (e) {
        }
        try {
          if (window.desk && desk.think) desk.think("siege", text);
        } catch (e2) {
        }
      }
      function egg(id, line2) {
        try {
          if (typeof findEgg === "function") findEgg(id, line2);
        } catch (e) {
        }
      }
      function gold(text, force) {
        if (!text) return;
        lastGold = text;
        lastGoldAt = now();
        try {
          var el = document.getElementById("hookWhisper");
          if (el && (force || el.textContent !== text)) {
            el.textContent = text;
            el.classList.add("on", "pop");
          }
        } catch (e) {
        }
      }
      function beastName() {
        try {
          if (window.beast && beast.state) {
            var s = beast.state();
            if (s && s.list) {
              for (var i = 0; i < s.list.length; i++) {
                if (s.list[i] && s.list[i].n && s.list[i].near) return s.list[i].n;
              }
              if (s.list[0] && s.list[0].n) return s.list[0].n;
            }
          }
        } catch (e) {
        }
        return "the thing in the harbor";
      }
      function threat() {
        try {
          if (window.realm && realm.embargo && realm.embargo()) return "war";
        } catch (e) {
        }
        try {
          if (window.beast && beast.near && beast.near() > 0.04) return "beast";
        } catch (e2) {
        }
        try {
          if (window.road && road.cut && road.cut()) return "cut";
        } catch (e3) {
        }
        return "";
      }
      function state() {
        var g = gs();
        if (g && g.siege && typeof g.siege === "object") return g.siege;
        var st = { last: "", on: false, day: -1, who: "", kind: "" };
        try {
          if (g) g.siege = st;
        } catch (e) {
        }
        return st;
      }
      function raise(kind, force) {
        var st = state();
        if (st.on && st.day === shopDay() && !force) return;
        if (shopDay() < 1 && !force) return;
        kind = kind || threat() || "beast";
        st.on = true;
        st.day = shopDay();
        st.kind = kind;
        st.who = kind === "beast" ? beastName() : "the inland war";
        var line2 = kind === "beast" ? st.who + " is on the glass." : "The harbor is shut.";
        st.last = line2;
        because(line2);
        gold(line2, true);
        egg("siege" + kind, line2);
        try {
          if (window.feel && feel.play) feel.play("warn");
        } catch (e) {
        }
        try {
          var list = typeof allFish === "function" ? allFish() || [] : [];
          for (var i = 0; i < list.length; i++) {
            if (list[i] && list[i].nick) list[i]._holdStill = 1;
          }
        } catch (e2) {
        }
      }
      function watch() {
        var t = threat();
        var st = state();
        if (t && shopDay() >= 1 && (!st.on || st.day !== shopDay())) raise(t);
        if (!t && st.on && st.day !== shopDay()) {
          st.on = false;
        }
      }
      function edge() {
        return state().on ? -0.14 : 0;
      }
      function wrapBrowse() {
        if (didBrowse || !window.shopBrowse) return;
        didBrowse = true;
        var orig = window.shopBrowse;
        window.shopBrowse = function(idx, slot, W, floorY, personS, simT) {
          var rec = orig.apply(this, arguments);
          try {
            if (!rec) return rec;
            var st = window.shopLife && shopLife.browse ? shopLife.browse()[idx] : null;
            var keep = typeof keepGuest === "function" ? keepGuest(st) : !!(st && (st._lateMae || st._goingHold || st._lateKid || st._mask));
            var sg = state();
            if (!sg.on || !st) return rec;
            if (rec.phase === "look" && !st._siegeSaid && !keep) {
              st._siegeSaid = 1;
              if (rec.kind === "kid") rec.line = "We're not supposed to be out.";
              else rec.line = sg.kind === "beast" ? "That is in the glass. I'm leaving." : "The inland town is shut.";
              rec.phase = "leave";
              st.phase = "leave";
              st.bought = false;
              st.line = rec.line;
            }
          } catch (e) {
          }
          return rec;
        };
      }
      function wrapChoir() {
        if (didChoir || !window.choir || !choir.weather) return;
        didChoir = true;
        var orig = choir.weather;
        choir.weather = function() {
          var w = orig.apply(this, arguments);
          try {
            var sg = state();
            if (sg.on) {
              w.sour = Math.min(1, (w.sour || 0) + 0.18);
              w.beast = Math.max(w.beast || 0, 0.2);
              if (!w.line) w.line = sg.last || "The harbor is shut.";
            }
          } catch (e) {
          }
          return w;
        };
      }
      function wrapFish() {
        if (didFish || typeof window.drawFishSprite !== "function") return;
        didFish = true;
        var orig = window.drawFishSprite;
        window.drawFishSprite = function(a) {
          try {
            if (state().on && a && a.fish && a.fish.nick) {
              a.fish._holdStill = 1;
              a.fish._gaze = now();
            }
          } catch (e) {
          }
          return orig.apply(this, arguments);
        };
      }
      function ensureOverlay() {
        if (overlay && overlay.parentNode) return;
        overlay = document.getElementById("siegeVeil");
        if (!overlay) {
          overlay = document.createElement("canvas");
          overlay.id = "siegeVeil";
          overlay.setAttribute("aria-hidden", "true");
          document.body.appendChild(overlay);
        }
        octx = overlay.getContext("2d");
      }
      function resize() {
        ensureOverlay();
        var dpr = Math.min(2, window.devicePixelRatio || 1);
        var w = window.innerWidth;
        var h = window.innerHeight;
        if (overlay.width !== (w * dpr | 0) || overlay.height !== (h * dpr | 0)) {
          overlay.width = w * dpr | 0;
          overlay.height = h * dpr | 0;
          overlay.style.cssText = "position:fixed;inset:0;width:100%;height:100%;pointer-events:none;z-index:3";
          octx.setTransform(dpr, 0, 0, dpr, 0, 0);
        }
      }
      function drawKeel() {
        var sc = sceneName();
        if (sc === "title" || sc === "work" || sc !== "shop" && sc !== "front") {
          if (octx && overlay) octx.clearRect(0, 0, overlay.width, overlay.height);
          return;
        }
        var sg = state();
        if (!sg.on) {
          if (octx && overlay) octx.clearRect(0, 0, overlay.width, overlay.height);
          return;
        }
        resize();
        var w = window.innerWidth;
        var h = window.innerHeight;
        octx.clearRect(0, 0, w, h);
        var t = now();
        var a = night() ? 0.42 : 0.16;
        a += 0.06 * Math.sin(t * 0.9);
        octx.fillStyle = "rgba(8,10,18," + a.toFixed(3) + ")";
        octx.fillRect(0, 0, w, h * 0.38);
        octx.save();
        octx.translate(w * 0.5, h * 0.18);
        octx.fillStyle = "rgba(12,16,28," + Math.min(0.86, a + 0.3).toFixed(3) + ")";
        octx.beginPath();
        octx.moveTo(-70, 10);
        octx.lineTo(0, -28);
        octx.lineTo(70, 10);
        octx.lineTo(40, 18);
        octx.lineTo(-40, 18);
        octx.closePath();
        octx.fill();
        octx.strokeStyle = "rgba(244,196,83," + (night() ? 0.35 : 0.12).toFixed(3) + ")";
        octx.lineWidth = 1.2;
        octx.stroke();
        octx.restore();
        octx.font = "600 12px Nunito, sans-serif";
        octx.fillStyle = "rgba(244,196,83,.78)";
        octx.textAlign = "center";
        octx.fillText(sg.who || "the keel", w * 0.5, h * 0.3);
      }
      function whisper() {
        if (lastGold && now() - lastGoldAt < 12) return lastGold;
        return "";
      }
      function line() {
        return state().last || whisper();
      }
      function seedWiki() {
        try {
          var wiki = typeof WIKI === "function" ? WIKI() : window.WIKI;
          if (!wiki || !wiki.push) return;
          if (wiki.some(function(a) {
            return a && a.id === "k_siege";
          })) return;
          wiki.push({
            id: "k_siege",
            sec: "The chronicle",
            t: "The harbor is shut",
            tags: "war siege beast barbarian militia glass embargo",
            w: "<p>Fin's embargo and forgotten beast were numbers. Now militia walk the aisle. Named fish hold still. At night the thing in the harbor is in the shop window. People leave. The till feels a war that is not a combat screen.</p><p><b>What to do about it:</b> wait it out. Keep the pair. The gold line will say if the harbor opened.</p>"
          });
        } catch (e) {
        }
      }
      function tick() {
        try {
          wrapBrowse();
          wrapChoir();
          wrapFish();
          seedWiki();
          watch();
          drawKeel();
          if (now() - lastTick > 1.1) lastTick = now();
        } catch (e) {
        }
      }
      window.siege = {
        whisper,
        line,
        of: state,
        edge,
        on: function() {
          return !!state().on;
        },
        seed: function() {
          raise("beast", true);
          return state();
        }
      };
      if (window.__onBeat) window.__onBeat(tick, 200);
      else
        setTimeout(function loop() {
          tick();
          setTimeout(loop, 200);
        }, 200);
    })();
  } catch (e) {
    console.error("Layer siege.js failed to start:", e);
  }

  // src/layers/pact.js
  try {
    (function() {
      "use strict";
      var lastTick = 0;
      var lastGold = "";
      var lastGoldAt = 0;
      var didBrowse = false;
      var didChoir = false;
      function now() {
        return typeof performance !== "undefined" ? performance.now() / 1e3 : Date.now() / 1e3;
      }
      function gs() {
        try {
          if (typeof gameState === "function") return gameState();
          if (typeof gameState === "object" && gameState) return gameState;
        } catch (e) {
        }
        return null;
      }
      function year() {
        try {
          if (window.saga && typeof saga.year === "function") return saga.year();
        } catch (e) {
        }
        return 1e3;
      }
      function shopDay() {
        try {
          var g = gs();
          if (g && isFinite(g.t)) return Math.floor((g.t % 1e9 + 1e9) % 1e9 / 2400);
        } catch (e) {
        }
        return 0;
      }
      function because(text) {
        if (!text) return;
        try {
          if (window.weave && weave.because) weave.because(text);
        } catch (e) {
        }
        try {
          if (window.desk && desk.think) desk.think("pact", text);
        } catch (e2) {
        }
      }
      function egg(id, line2) {
        try {
          if (typeof findEgg === "function") findEgg(id, line2);
        } catch (e) {
        }
      }
      function gold(text, force) {
        if (!text) return;
        lastGold = text;
        lastGoldAt = now();
        try {
          var el = document.getElementById("hookWhisper");
          if (el && (force || el.textContent !== text)) {
            el.textContent = text;
            el.classList.add("on", "pop");
          }
        } catch (e) {
        }
      }
      function twoCivs() {
        var a = { n: "the harbor league" };
        var b = { n: "the inland kingdom" };
        try {
          var w = window.realm && realm.world && realm.world();
          if (w && w.civs && w.civs.length >= 2) {
            a = w.civs[0];
            b = w.civs[1];
            if (a.n === b.n && w.civs[2]) b = w.civs[2];
          }
        } catch (e) {
        }
        return [a, b];
      }
      function kindOf() {
        try {
          if (window.guild && guild.mandate) return String(guild.mandate() || "guppy");
        } catch (e) {
        }
        return "guppy";
      }
      function state() {
        var g = gs();
        if (g && g.pact && g.pact.a) return g.pact;
        var pair = twoCivs();
        var st = {
          a: pair[0].n,
          b: pair[1].n,
          want: kindOf(),
          last: "",
          mood: "open",
          seen: false,
          day: -1
        };
        try {
          if (g) g.pact = st;
        } catch (e) {
        }
        return st;
      }
      function setMood(mood, force) {
        var st = state();
        if (st.mood === mood && st.seen && !force) return;
        if (shopDay() < 1 && !force) {
          st.mood = mood;
          return;
        }
        st.mood = mood;
        st.day = shopDay();
        st.seen = true;
        var line2 = mood === "pact" ? "A pact. " + st.a + " and " + st.b + " share a road." : mood === "denounce" ? st.b + " denounced the harbor. They will not bag here." : "The road between " + st.a + " and " + st.b + " is quiet.";
        st.last = line2;
        because(line2);
        gold(line2, true);
        egg("pact" + mood, line2);
      }
      function watch() {
        var st = state();
        if (st.seen && st.day === shopDay()) return;
        if (shopDay() < 1) return;
        try {
          if (window.realm && realm.embargo && realm.embargo()) {
            setMood("denounce");
            return;
          }
        } catch (e) {
        }
        try {
          if (window.guild && guild.standing && guild.standing() > 0.62) {
            setMood("pact");
            return;
          }
        } catch (e2) {
        }
        if (!st.seen && shopDay() >= 1) setMood("pact");
      }
      function edge() {
        var m = state().mood;
        if (m === "pact") return 0.08;
        if (m === "denounce") return -0.09;
        return 0;
      }
      function wrapBrowse() {
        if (didBrowse || !window.shopBrowse) return;
        didBrowse = true;
        var orig = window.shopBrowse;
        window.shopBrowse = function(idx, slot, W, floorY, personS, simT) {
          var rec = orig.apply(this, arguments);
          try {
            if (!rec) return rec;
            var st = window.shopLife && shopLife.browse ? shopLife.browse()[idx] : null;
            var keep = typeof keepGuest === "function" ? keepGuest(st) : !!(st && (st._lateMae || st._goingHold || st._lateKid || st._mask));
            var ps = state();
            if (keep || !st || rec.phase !== "look" || st._pactSaid) return rec;
            st._pactSaid = 1;
            if (shopDay() < 1) return rec;
            if (ps.mood === "pact") {
              rec.line = rec.kind === "kid" ? "The caravan brought sweets." : "The pact said a pair of " + ps.want + ". From " + ps.b + ".";
              rec.want = rec.want || ps.want;
              st.want = st.want || ps.want;
              st.line = rec.line;
            } else if (ps.mood === "denounce") {
              rec.line = rec.kind === "kid" ? "They said don't buy here." : ps.b + " said stay away.";
              rec.phase = "leave";
              st.phase = "leave";
              st.bought = false;
              st.line = rec.line;
            }
          } catch (e) {
          }
          return rec;
        };
      }
      function wrapChoir() {
        if (didChoir || !window.choir || !choir.weather) return;
        didChoir = true;
        var orig = choir.weather;
        choir.weather = function() {
          var w = orig.apply(this, arguments);
          try {
            var ps = state();
            if (ps.mood === "pact") {
              w.sweet = Math.min(1, (w.sweet || 0) + 0.07);
              if (!w.line) w.line = "A pact on the water.";
            } else if (ps.mood === "denounce") {
              w.sour = Math.min(1, (w.sour || 0) + 0.09);
              if (!w.line) w.line = ps.b + " denounced the harbor.";
            }
          } catch (e) {
          }
          return w;
        };
      }
      function whisper() {
        if (lastGold && now() - lastGoldAt < 12) return lastGold;
        return "";
      }
      function line() {
        return state().last || whisper();
      }
      function seedWiki() {
        try {
          var wiki = typeof WIKI === "function" ? WIKI() : window.WIKI;
          if (!wiki || !wiki.push) return;
          if (wiki.some(function(a) {
            return a && a.id === "k_pact";
          })) return;
          wiki.push({
            id: "k_pact",
            sec: "The quarter",
            t: "A pact is a caravan",
            tags: "alliance diplomacy trade denounce caravan pact",
            w: "<p>Fin's two civs sign a pact and a person walks in off that road. They denounce and that mouth stops bagging here. Trade is a pair that keeps moving. A war inland is a denunciation you can hear.</p><p><b>What to do about it:</b> keep the hall. Fill the pair the caravan named. The gold line will say if the road soured.</p>"
          });
        } catch (e) {
        }
      }
      function tick() {
        try {
          wrapBrowse();
          wrapChoir();
          seedWiki();
          if (now() - lastTick > 1.5) {
            lastTick = now();
            watch();
          }
        } catch (e) {
        }
      }
      window.pact = {
        whisper,
        line,
        of: state,
        edge,
        seed: function(m) {
          setMood(m === "denounce" ? "denounce" : "pact", true);
          return state();
        }
      };
      if (window.__onBeat) window.__onBeat(tick, 240);
      else
        setTimeout(function loop() {
          tick();
          setTimeout(loop, 240);
        }, 220);
    })();
  } catch (e) {
    console.error("Layer pact.js failed to start:", e);
  }

  // src/layers/lux.js
  try {
    (function() {
      "use strict";
      var lastTick = 0;
      var lastGold = "";
      var lastGoldAt = 0;
      var didBrowse = false;
      var didChoir = false;
      var didFish = false;
      function now() {
        return typeof performance !== "undefined" ? performance.now() / 1e3 : Date.now() / 1e3;
      }
      function gs() {
        try {
          if (typeof gameState === "function") return gameState();
          if (typeof gameState === "object" && gameState) return gameState;
        } catch (e) {
        }
        return null;
      }
      function year() {
        try {
          if (window.saga && typeof saga.year === "function") return saga.year();
        } catch (e) {
        }
        return 1e3;
      }
      function shopDay() {
        try {
          var g = gs();
          if (g && isFinite(g.t)) return Math.floor((g.t % 1e9 + 1e9) % 1e9 / 2400);
        } catch (e) {
        }
        return 0;
      }
      function because(text) {
        if (!text) return;
        try {
          if (window.weave && weave.because) weave.because(text);
        } catch (e) {
        }
        try {
          if (window.desk && desk.think) desk.think("lux", text);
        } catch (e2) {
        }
      }
      function egg(id, line2) {
        try {
          if (typeof findEgg === "function") findEgg(id, line2);
        } catch (e) {
        }
      }
      function gold(text, force) {
        if (!text) return;
        lastGold = text;
        lastGoldAt = now();
        try {
          var el = document.getElementById("hookWhisper");
          if (el && (force || el.textContent !== text)) {
            el.textContent = text;
            el.classList.add("on", "pop");
          }
        } catch (e) {
        }
      }
      function pickKind() {
        try {
          if (window.wild && wild.of) {
            var p = wild.of().pop || {};
            var best = "";
            var low = 1;
            for (var k2 in p) {
              if (p[k2] < low) {
                low = p[k2];
                best = k2;
              }
            }
            if (best) return best === "gold" ? "goldfish" : best;
          }
        } catch (e) {
        }
        try {
          if (window.guild && guild.mandate) return String(guild.mandate() || "tetra");
        } catch (e2) {
        }
        return "tetra";
      }
      function fishKind(f) {
        if (!f) return "";
        try {
          var S = typeof O !== "undefined" ? O : typeof SPECIES !== "undefined" ? SPECIES : null;
          if (S && f.sp != null && S[f.sp]) {
            return String(S[f.sp].gname || S[f.sp].name || S[f.sp].vname || "").toLowerCase();
          }
        } catch (e) {
        }
        return "";
      }
      function have(kind) {
        var n = 0;
        try {
          var list = typeof allFish === "function" ? allFish() || [] : [];
          var q = String(kind || "").toLowerCase();
          for (var i = 0; i < list.length; i++) {
            var k2 = fishKind(list[i]);
            if (k2 && (k2.indexOf(q) >= 0 || q.indexOf(k2) >= 0)) n++;
          }
        } catch (e) {
        }
        return n;
      }
      function state() {
        var g = gs();
        if (g && g.lux && g.lux.kind) return g.lux;
        var st = { kind: pickKind(), last: "", day: -1, missing: false, said: false };
        try {
          if (g) g.lux = st;
        } catch (e) {
        }
        return st;
      }
      function watch() {
        var st = state();
        var d = shopDay();
        if (d !== st.day) {
          st.day = d;
          if (d >= 1) st.kind = pickKind();
        }
        var n = have(st.kind);
        var miss = n < 2;
        if (miss === st.missing) return;
        st.missing = miss;
        if (d < 1) return;
        var line2 = miss ? "The street is without " + st.kind + "." : "The " + st.kind + " is on the rack. The street is quiet.";
        st.last = line2;
        because(line2);
        gold(line2, true);
        egg(miss ? "luxmiss" : "luxhave", line2);
      }
      function edge() {
        var st = state();
        return have(st.kind) >= 2 ? 0.07 : -0.09;
      }
      function wrapBrowse() {
        if (didBrowse || !window.shopBrowse) return;
        didBrowse = true;
        var orig = window.shopBrowse;
        window.shopBrowse = function(idx, slot, W, floorY, personS, simT) {
          var rec = orig.apply(this, arguments);
          try {
            if (!rec) return rec;
            var st = window.shopLife && shopLife.browse ? shopLife.browse()[idx] : null;
            var keep = typeof keepGuest === "function" ? keepGuest(st) : !!(st && (st._lateMae || st._goingHold || st._lateKid || st._mask));
            var ls = state();
            if (keep || !st || rec.phase !== "look" || st._luxSaid) return rec;
            st._luxSaid = 1;
            if (shopDay() < 1) return rec;
            var n = have(ls.kind);
            if (n < 2) {
              rec.line = rec.kind === "kid" ? "Everyone's talking about " + ls.kind + "." : "The street is without " + ls.kind + ". I'm walking.";
              if (rec.kind !== "kid") {
                rec.phase = "leave";
                st.phase = "leave";
                st.bought = false;
              }
              st.line = rec.line;
            } else if (rec.kind === "collector") {
              rec.want = ls.kind;
              st.want = ls.kind;
              rec.line = "The " + ls.kind + " is the one this year. I'll take a pair.";
              st.line = rec.line;
            }
          } catch (e) {
          }
          return rec;
        };
      }
      function wrapChoir() {
        if (didChoir || !window.choir || !choir.weather) return;
        didChoir = true;
        var orig = choir.weather;
        choir.weather = function() {
          var w = orig.apply(this, arguments);
          try {
            var ls = state();
            if (have(ls.kind) >= 2) {
              w.sweet = Math.min(1, (w.sweet || 0) + 0.08);
              if (!w.line) w.line = "The " + ls.kind + " is the luxury. The water has it.";
            } else {
              w.sour = Math.min(1, (w.sour || 0) + 0.1);
              if (!w.line) w.line = "The street is without " + ls.kind + ".";
            }
          } catch (e) {
          }
          return w;
        };
      }
      function wrapFish() {
        if (didFish || typeof window.drawFishSprite !== "function") return;
        didFish = true;
        var orig = window.drawFishSprite;
        window.drawFishSprite = function(a) {
          try {
            var ls = state();
            if (a && a.fish) {
              var k2 = fishKind(a.fish);
              if (k2 && ls.kind && k2.indexOf(ls.kind.toLowerCase()) >= 0) a.fish._gaze = now();
            }
          } catch (e) {
          }
          return orig.apply(this, arguments);
        };
      }
      function whisper() {
        if (lastGold && now() - lastGoldAt < 12) return lastGold;
        return "";
      }
      function line() {
        var st = state();
        if (st.last) return st.last;
        return whisper();
      }
      function seedWiki() {
        try {
          var wiki = typeof WIKI === "function" ? WIKI() : window.WIKI;
          if (!wiki || !wiki.push) return;
          if (wiki.some(function(a) {
            return a && a.id === "k_lux";
          })) return;
          wiki.push({
            id: "k_lux",
            sec: "The shop floor",
            t: "The luxury is a kind",
            tags: "luxury resource strategic scarce tetra",
            w: "<p>Fin's luxury is a fish. This year the inland wants whatever the river made scarce. Keep a pair and collectors stay. Don't, and the street says it is without. Strategic is the same mouth, louder. The till feels a missing kind the way a city feels a missing luxury.</p><p><b>What to do about it:</b> keep two of that fish. The gold line will name the without.</p>"
          });
        } catch (e) {
        }
      }
      function tick() {
        try {
          wrapBrowse();
          wrapChoir();
          wrapFish();
          seedWiki();
          if (now() - lastTick > 1.6) {
            lastTick = now();
            watch();
          }
        } catch (e) {
        }
      }
      window.lux = {
        whisper,
        line,
        of: state,
        edge,
        kind: function() {
          return state().kind;
        },
        seed: function() {
          var st = state();
          st.kind = pickKind();
          st.missing = have(st.kind) < 2;
          st.day = shopDay();
          var line2 = st.missing ? "The street is without " + st.kind + "." : "The " + st.kind + " is on the rack. The street is quiet.";
          st.last = line2;
          because(line2);
          gold(line2, true);
          return st;
        }
      };
      if (window.__onBeat) window.__onBeat(tick, 240);
      else
        setTimeout(function loop() {
          tick();
          setTimeout(loop, 240);
        }, 220);
    })();
  } catch (e) {
    console.error("Layer lux.js failed to start:", e);
  }

  // src/layers/spy.js
  try {
    (function() {
      "use strict";
      var lastTick = 0;
      var lastGold = "";
      var lastGoldAt = 0;
      var didBrowse = false;
      var didChoir = false;
      function now() {
        return typeof performance !== "undefined" ? performance.now() / 1e3 : Date.now() / 1e3;
      }
      function gs() {
        try {
          if (typeof gameState === "function") return gameState();
          if (typeof gameState === "object" && gameState) return gameState;
        } catch (e) {
        }
        return null;
      }
      function year() {
        try {
          if (window.saga && typeof saga.year === "function") return saga.year();
        } catch (e) {
        }
        return 1e3;
      }
      function shopDay() {
        try {
          var g = gs();
          if (g && isFinite(g.t)) return Math.floor((g.t % 1e9 + 1e9) % 1e9 / 2400);
        } catch (e) {
        }
        return 0;
      }
      function because(text) {
        if (!text) return;
        try {
          if (window.weave && weave.because) weave.because(text);
        } catch (e) {
        }
        try {
          if (window.desk && desk.think) desk.think("spy", text);
        } catch (e2) {
        }
      }
      function egg(id, line2) {
        try {
          if (typeof findEgg === "function") findEgg(id, line2);
        } catch (e) {
        }
      }
      function gold(text, force) {
        if (!text) return;
        lastGold = text;
        lastGoldAt = now();
        try {
          var el = document.getElementById("hookWhisper");
          if (el && (force || el.textContent !== text)) {
            el.textContent = text;
            el.classList.add("on", "pop");
          }
        } catch (e) {
        }
      }
      function rival() {
        try {
          var w = window.realm && realm.world && realm.world();
          if (w && w.sites) {
            for (var i = 1; i < w.sites.length; i++) {
              if (w.sites[i] && !w.sites[i].ruin && w.sites[i].kind === "port") return w.sites[i].n;
            }
            if (w.sites[4] && w.sites[4].n) return w.sites[4].n;
          }
        } catch (e) {
        }
        return "Haymarket";
      }
      function state() {
        var g = gs();
        if (g && g.spy && typeof g.spy === "object") return g.spy;
        var st = { last: "", seen: false, day: -1, looks: 0, leaked: false, who: rival() };
        try {
          if (g) g.spy = st;
        } catch (e) {
        }
        return st;
      }
      function arrive(rec, st, force) {
        var ss = state();
        if (ss.seen && ss.day === shopDay() && !force) return false;
        if (shopDay() < 1 && !force) return false;
        rec.kind = "collector";
        rec.phase = "look";
        rec.line = "Just looking at the cards.";
        rec.name = rec.name || "Someone from " + ss.who;
        if (st) {
          st._spy = 1;
          st.bought = false;
          st.line = rec.line;
          st.guestName = rec.name;
          st.until = Math.max(st.until || 0, now() + 9);
          st.lineUntil = now() + 5;
        }
        ss.seen = true;
        ss.day = shopDay();
        ss.looks = (ss.looks || 0) + 1;
        var line2 = "Someone was writing the names down.";
        ss.last = line2;
        because(line2);
        gold(line2, true);
        egg("spyin", line2);
        if (ss.looks >= 2 && !ss.leaked) leak(ss);
        return true;
      }
      function leak(ss) {
        ss.leaked = true;
        var line2 = ss.who + " knows the rack now.";
        ss.last = line2;
        because(line2);
        try {
          if (window.hold && hold.of) {
            var h = hold.of();
            if (h && !h.found) h.loyal = Math.min(h.loyal || 0.5, 0.28);
          }
        } catch (e) {
        }
        try {
          if (window.weave && weave.bumpWord) weave.bumpWord(-0.05);
        } catch (e2) {
        }
      }
      function wrapBrowse() {
        if (didBrowse || !window.shopBrowse) return;
        didBrowse = true;
        var orig = window.shopBrowse;
        window.shopBrowse = function(idx, slot, W, floorY, personS, simT) {
          var rec = orig.apply(this, arguments);
          try {
            if (!rec) return rec;
            var st = window.shopLife && shopLife.browse ? shopLife.browse()[idx] : null;
            var keep = typeof keepGuest === "function" ? keepGuest(st) : !!(st && (st._lateMae || st._goingHold || st._lateKid || st._mask));
            var ss = state();
            if (!keep && rec.phase === "look" && st && !st._spy && shopDay() >= 1 && !ss.seen && ((idx | 0) === 3 || rec.kind === "lunch" || rec.kind === "collector")) {
              arrive(rec, st);
            }
            if (ss.seen && ss.day === shopDay() && rec.phase === "look" && st && !st._spy && !st._spySaid && !keep) {
              st._spySaid = 1;
              if (rec.kind === "kid") {
                rec.line = "They're writing the names down.";
                st.line = rec.line;
              } else if (rec.kind === "neighbor") {
                rec.line = "That's not a customer. That's " + ss.who + ".";
                st.line = rec.line;
              }
            }
            if (st && st._spy && rec.phase === "pay") {
              rec.phase = "leave";
              st.phase = "leave";
              st.bought = false;
              rec.line = rec.line || "Just looking at the cards.";
              st.line = rec.line;
            }
          } catch (e) {
          }
          return rec;
        };
      }
      function wrapChoir() {
        if (didChoir || !window.choir || !choir.weather) return;
        didChoir = true;
        var orig = choir.weather;
        choir.weather = function() {
          var w = orig.apply(this, arguments);
          try {
            var ss = state();
            if (ss.seen && ss.day === shopDay()) {
              w.sour = Math.min(1, (w.sour || 0) + 0.06);
              if (!w.line) w.line = "Someone is writing the names down.";
            }
          } catch (e) {
          }
          return w;
        };
      }
      function whisper() {
        if (lastGold && now() - lastGoldAt < 12) return lastGold;
        return "";
      }
      function line() {
        return state().last || whisper();
      }
      function seedWiki() {
        try {
          var wiki = typeof WIKI === "function" ? WIKI() : window.WIKI;
          if (!wiki || !wiki.push) return;
          if (wiki.some(function(a) {
            return a && a.id === "k_spy";
          })) return;
          wiki.push({
            id: "k_spy",
            sec: "The shop floor",
            t: "Someone is writing the names down",
            tags: "spy espionage cards rival haymarket leak",
            w: "<p>Fin's spy copies the cards, the relic, the pair that is unique. They do not bag. A kid sees them. Two looks and a rival hold knows what you keep. A theft still takes a fish. This takes the list.</p><p><b>What to do about it:</b> you cannot stop the look. You can keep a pair so the list is not a unique. Life will name them.</p>"
          });
        } catch (e) {
        }
      }
      function tick() {
        try {
          wrapBrowse();
          wrapChoir();
          seedWiki();
          if (now() - lastTick > 1.1) lastTick = now();
        } catch (e) {
        }
      }
      function edge() {
        var ss = state();
        if (ss.leaked) return -0.08;
        if (ss.seen && ss.day === shopDay()) return -0.04;
        return 0;
      }
      window.spy = {
        whisper,
        line,
        of: state,
        edge,
        seed: function() {
          var rec = { kind: "collector", phase: "look", line: "", name: "" };
          var st = { phase: "look", bought: false };
          arrive(rec, st, true);
          leak(state());
          return state();
        }
      };
      if (window.__onBeat) window.__onBeat(tick, 240);
      else
        setTimeout(function loop() {
          tick();
          setTimeout(loop, 240);
        }, 220);
    })();
  } catch (e) {
    console.error("Layer spy.js failed to start:", e);
  }

  // src/layers/seat.js
  try {
    (function() {
      "use strict";
      var lastTick = 0;
      var lastGold = "";
      var lastGoldAt = 0;
      var didBrowse = false;
      var didChoir = false;
      var didFish = false;
      function now() {
        return typeof performance !== "undefined" ? performance.now() / 1e3 : Date.now() / 1e3;
      }
      function gs() {
        try {
          if (typeof gameState === "function") return gameState();
          if (typeof gameState === "object" && gameState) return gameState;
        } catch (e) {
        }
        return null;
      }
      function year() {
        try {
          if (window.saga && typeof saga.year === "function") return saga.year();
        } catch (e) {
        }
        return 1e3;
      }
      function shopDay() {
        try {
          var g = gs();
          if (g && isFinite(g.t)) return Math.floor((g.t % 1e9 + 1e9) % 1e9 / 2400);
        } catch (e) {
        }
        return 0;
      }
      function because(text) {
        if (!text) return;
        try {
          if (window.weave && weave.because) weave.because(text);
        } catch (e) {
        }
        try {
          if (window.desk && desk.think) desk.think("seat", text);
        } catch (e2) {
        }
      }
      function egg(id, line2) {
        try {
          if (typeof findEgg === "function") findEgg(id, line2);
        } catch (e) {
        }
      }
      function gold(text, force) {
        if (!text) return;
        lastGold = text;
        lastGoldAt = now();
        try {
          var el = document.getElementById("hookWhisper");
          if (el && (force || el.textContent !== text)) {
            el.textContent = text;
            el.classList.add("on", "pop");
          }
        } catch (e) {
        }
      }
      function aFig() {
        try {
          var w = window.realm && realm.world && realm.world();
          if (w && w.figs) {
            for (var i = 0; i < w.figs.length; i++) {
              if (w.figs[i] && (w.figs[i].job === "judge" || w.figs[i].job === "scribe" || w.figs[i].job === "captain"))
                return w.figs[i];
            }
            if (w.figs[2]) return w.figs[2];
          }
        } catch (e) {
        }
        return { n: "Selum Ithen", job: "judge" };
      }
      function kindOf() {
        try {
          if (window.guild && guild.mandate) return String(guild.mandate() || "cichlid");
        } catch (e) {
        }
        return "cichlid";
      }
      function fishList() {
        try {
          if (typeof allFish === "function") return allFish() || [];
        } catch (e) {
        }
        return [];
      }
      function pickMayor() {
        var list = fishList();
        var best = null;
        var score = -1;
        for (var i = 0; i < list.length; i++) {
          var f = list[i];
          if (!f || !f.nick) continue;
          var s = 0;
          try {
            if (f.mind && typeof f.mind.mood === "number") s += f.mind.mood;
          } catch (e) {
          }
          s += (f.stage || 0) * 0.2;
          s += (f.stress || 0) * -0.1;
          if (f._foe || f.proud) s += 0.3;
          if (s > score) {
            score = s;
            best = f;
          }
        }
        return best;
      }
      function state() {
        var g = gs();
        if (g && g.seat && typeof g.seat === "object") return g.seat;
        var fig = aFig();
        var st = {
          gov: fig.n,
          edict: kindOf(),
          last: "",
          seen: false,
          day: -1,
          mayor: "",
          mayorId: 0
        };
        try {
          if (g) g.seat = st;
        } catch (e) {
        }
        return st;
      }
      function first(n) {
        return String(n || "Someone").split(" ")[0];
      }
      function seatMayor(force) {
        var st = state();
        var m = pickMayor();
        if (!m || !m.nick) return;
        if (st.mayor === m.nick && !force) return;
        var was = st.mayor;
        st.mayor = m.nick;
        st.mayorId = m.fid || 0;
        m._mayor = 1;
        if (shopDay() < 1 && !force) return;
        if (was && was === m.nick) return;
        var line2 = m.nick + " is the one the others follow.";
        st.last = line2;
        because(line2);
        gold(line2, true);
        egg("seatmayor", line2);
      }
      function arrive(rec, st, force) {
        var ss = state();
        if (ss.seen && ss.day === shopDay() && !force) return false;
        if (shopDay() < 1 && !force) return false;
        rec.kind = "collector";
        rec.name = ss.gov;
        rec.phase = "look";
        rec.want = ss.edict;
        rec.line = "The hall voted. A pair of " + ss.edict + ". Not the last of that fish.";
        if (st) {
          st._seat = 1;
          st.name = ss.gov;
          st.guestName = ss.gov;
          st.want = ss.edict;
          st.line = rec.line;
          st.lineUntil = now() + 6;
        }
        ss.seen = true;
        ss.day = shopDay();
        var line2 = first(ss.gov) + " brought an edict.";
        ss.last = line2;
        because(line2 + " " + rec.line);
        gold(line2, true);
        egg("seatedict", line2);
        return true;
      }
      function wrapBrowse() {
        if (didBrowse || !window.shopBrowse) return;
        didBrowse = true;
        var orig = window.shopBrowse;
        window.shopBrowse = function(idx, slot, W, floorY, personS, simT) {
          var rec = orig.apply(this, arguments);
          try {
            if (!rec) return rec;
            var st = window.shopLife && shopLife.browse ? shopLife.browse()[idx] : null;
            var keep = typeof keepGuest === "function" ? keepGuest(st) : !!(st && (st._lateMae || st._goingHold || st._lateKid || st._mask));
            var ss = state();
            if (!keep && rec.phase === "look" && st && !st._seat && shopDay() >= 1 && !ss.seen && ((idx | 0) === 1 || rec.kind === "collector")) {
              arrive(rec, st);
            }
            if (ss.mayor && rec.phase === "look" && st && !st._seatSaid && !keep && (rec.kind === "collector" || rec.kind === "kid")) {
              st._seatSaid = 1;
              rec.line = rec.kind === "kid" ? ss.mayor + " is the boss of the tank." : "That's the one the others follow. " + ss.mayor + ".";
              st.line = rec.line;
            }
            if (ss.seen && rec.phase === "look" && st && st._seat) {
              rec.want = ss.edict;
              st.want = ss.edict;
            }
          } catch (e) {
          }
          return rec;
        };
      }
      function wrapChoir() {
        if (didChoir || !window.choir || !choir.weather) return;
        didChoir = true;
        var orig = choir.weather;
        choir.weather = function() {
          var w = orig.apply(this, arguments);
          try {
            var ss = state();
            if (ss.mayor) {
              w.sweet = Math.min(1, (w.sweet || 0) + 0.05);
              if (!w.line) w.line = ss.mayor + " is the one the others follow.";
            }
            if (ss.seen && ss.day === shopDay() && !w.line) {
              w.line = first(ss.gov) + " brought an edict.";
            }
          } catch (e) {
          }
          return w;
        };
      }
      function wrapFish() {
        if (didFish || typeof window.drawFishSprite !== "function") return;
        didFish = true;
        var orig = window.drawFishSprite;
        window.drawFishSprite = function(a) {
          try {
            var ss = state();
            if (ss.mayor && a && a.fish) {
              if (a.fish.nick === ss.mayor) {
                a.fish._holdStill = 1;
                a.fish._gaze = now();
                a.fish._mayor = 1;
              } else if (a.fish.nick && Math.sin(now() + (a.fish.fid || 0)) > 0.65) {
                a.fish._holdStill = 1;
              }
            }
          } catch (e) {
          }
          return orig.apply(this, arguments);
        };
      }
      function whisper() {
        if (lastGold && now() - lastGoldAt < 12) return lastGold;
        return "";
      }
      function line() {
        return state().last || whisper();
      }
      function seedWiki() {
        try {
          var wiki = typeof WIKI === "function" ? WIKI() : window.WIKI;
          if (!wiki || !wiki.push) return;
          if (wiki.some(function(a) {
            return a && a.id === "k_seat";
          })) return;
          wiki.push({
            id: "k_seat",
            sec: "The chronicle",
            t: "A governor, and a mayor in the water",
            tags: "governor edict mayor tank leader hall",
            w: "<p>Fin's inland sends an edict: a kind, a last-of-a-pair they will not hear of. The tank already had minds. One named fish is the one the others follow. Collectors ask for that one. The others hold still when it does.</p><p><b>What to do about it:</b> do not bag the mayor if you can help it. Fill the edict pair. Life will name both walks.</p>"
          });
        } catch (e) {
        }
      }
      function tick() {
        try {
          wrapBrowse();
          wrapChoir();
          wrapFish();
          seedWiki();
          if (now() - lastTick > 1.4) {
            lastTick = now();
            seatMayor();
          }
        } catch (e) {
        }
      }
      function edge() {
        var ss = state();
        var n = 0;
        if (ss.mayor) n += 0.04;
        if (ss.seen && ss.day === shopDay()) n += 0.03;
        return n;
      }
      window.seat = {
        whisper,
        line,
        of: state,
        edge,
        seed: function() {
          var rec = { kind: "collector", phase: "look", line: "", name: "" };
          var st = { phase: "look", bought: false };
          arrive(rec, st, true);
          seatMayor(true);
          return state();
        }
      };
      if (window.__onBeat) window.__onBeat(tick, 240);
      else
        setTimeout(function loop() {
          tick();
          setTimeout(loop, 240);
        }, 220);
    })();
  } catch (e) {
    console.error("Layer seat.js failed to start:", e);
  }

  // src/layers/house.js
  try {
    (function() {
      "use strict";
      var lastTick = 0;
      var lastGold = "";
      var lastGoldAt = 0;
      var didBrowse = false;
      var didChoir = false;
      var WANTS = ["goldfish", "betta", "guppy", "tetra", "angelfish", "cory", "clown"];
      function now() {
        return typeof performance !== "undefined" ? performance.now() / 1e3 : Date.now() / 1e3;
      }
      function gs() {
        try {
          if (typeof gameState === "function") return gameState();
          if (typeof gameState === "object" && gameState) return gameState;
        } catch (e) {
        }
        return null;
      }
      function shopDay() {
        try {
          var g = gs();
          if (g && isFinite(g.t)) return Math.floor((g.t % 1e9 + 1e9) % 1e9 / 2400);
        } catch (e) {
        }
        return 0;
      }
      function hash32(s) {
        var h = 2166136261;
        s = String(s || "");
        for (var i = 0; i < s.length; i++) {
          h ^= s.charCodeAt(i);
          h = Math.imul(h, 16777619);
        }
        return h >>> 0;
      }
      function because(text) {
        if (!text) return;
        try {
          if (window.weave && weave.because) weave.because(text);
        } catch (e) {
        }
        try {
          if (window.desk && desk.think) desk.think("house", text);
        } catch (e2) {
        }
      }
      function egg(id, line2) {
        try {
          if (typeof findEgg === "function") findEgg(id, line2);
        } catch (e) {
        }
      }
      function gold(text, force) {
        if (!text) return;
        lastGold = text;
        lastGoldAt = now();
        try {
          var el = document.getElementById("hookWhisper");
          if (el && (force || el.textContent !== text)) {
            el.textContent = text;
            el.classList.add("on", "pop");
          }
        } catch (e) {
        }
      }
      function surn(n) {
        var p = String(n || "").trim().split(/\s+/);
        return p.length > 1 ? p[p.length - 1] : p[0] || "Someone";
      }
      function folk() {
        try {
          if (window.kin && kin.folk) return kin.folk() || [];
        } catch (e) {
        }
        return [];
      }
      function living() {
        return folk().filter(function(f) {
          return f && !f.dead && f.id !== "keep";
        });
      }
      function state() {
        var g = gs();
        if (g && g.house && typeof g.house === "object") return g.house;
        var st = { last: "", houses: [], asked: "", day: -1, filled: false };
        try {
          if (g) g.house = st;
        } catch (e) {
        }
        return st;
      }
      function ensure() {
        var st = state();
        var live = living();
        var by = /* @__PURE__ */ Object.create(null);
        for (var i = 0; i < live.length; i++) {
          var s = surn(live[i].n);
          if (!s) continue;
          if (!by[s]) by[s] = [];
          by[s].push(live[i]);
        }
        var names = Object.keys(by);
        if (!st.houses.length) {
          for (var j = 0; j < names.length; j++) {
            var hh = hash32("house:" + names[j]);
            var rival = names[(j + 1) % names.length];
            if (rival === names[j]) rival = "";
            st.houses.push({
              n: names[j],
              want: WANTS[hh % WANTS.length],
              prestige: 0.4 + (hh >>> 8) % 30 / 100,
              rival
            });
          }
        }
        for (var k2 = 0; k2 < st.houses.length; k2++) {
          var hs = st.houses[k2];
          var members = by[hs.n] || [];
          hs.head = members[0] ? members[0].n : "";
          hs.cadet = members[1] ? members[1].n : "";
          hs.nfolk = members.length;
        }
        return st;
      }
      function ofSurn(s) {
        var st = ensure();
        s = String(s || "");
        for (var i = 0; i < st.houses.length; i++) if (st.houses[i].n === s) return st.houses[i];
        return null;
      }
      function ofName(n) {
        return ofSurn(surn(n));
      }
      function bump(s, amt) {
        var h = ofSurn(s);
        if (!h) return;
        h.prestige = Math.max(0.04, Math.min(1, (h.prestige || 0.4) + amt));
      }
      function keepOf(st) {
        try {
          if (typeof keepGuest === "function") return keepGuest(st);
        } catch (e) {
        }
        return !!(st && (st._lateMae || st._goingHold || st._lateKid || st._mask));
      }
      function askHead(rec, st, fig, h, force) {
        var hs = state();
        if (shopDay() < 1 && !force) return false;
        if (hs.asked && hs.day === shopDay() && !force) return false;
        rec.phase = "look";
        rec.want = h.want;
        rec.line = "The " + h.n + "s. A pair of " + h.want + ".";
        rec.kind = rec.kind === "kid" ? "collector" : rec.kind || "collector";
        if (st) {
          st._houseAsk = 1;
          st.want = h.want;
          st.line = rec.line;
          st.lineUntil = now() + 6;
          st.guestName = fig.n;
        }
        hs.asked = fig.n;
        hs.day = shopDay();
        hs.want = h.want;
        hs.which = h.n;
        hs.filled = false;
        var line2 = "The " + h.n + "s asked for a pair of " + h.want + ".";
        hs.last = line2;
        because(line2);
        gold(line2, true);
        egg("houseask", line2);
        return true;
      }
      function noteBag(kind) {
        var hs = state();
        if (!hs.asked || hs.filled) return;
        if (hs.want && kind && String(kind).toLowerCase().indexOf(String(hs.want).toLowerCase()) >= 0) {
          hs.filled = true;
          bump(hs.which, 0.12);
          var line2 = "The " + hs.which + "s took the pair. The house stays.";
          hs.last = line2;
          because(line2);
          gold(line2, true);
          egg("housepair", line2);
        }
      }
      function noteMiss() {
        var hs = state();
        if (!hs.asked || hs.filled) return;
        if (shopDay() - (hs.day || 0) < 2) return;
        if (hs.missDay === shopDay()) return;
        hs.missDay = shopDay();
        bump(hs.which, -0.16);
        var h = ofSurn(hs.which);
        if (h && (h.prestige || 0) < 0.22) {
          var line2 = "The " + hs.which + "s bag at Haymarket now.";
          hs.last = line2;
          because(line2);
          gold(line2, true);
        }
      }
      function wrapBrowse() {
        if (didBrowse || !window.shopBrowse) return;
        didBrowse = true;
        var orig = window.shopBrowse;
        window.shopBrowse = function(idx, slot, W, floorY, personS, simT) {
          var rec = orig.apply(this, arguments);
          try {
            if (!rec) return rec;
            var st = window.shopLife && shopLife.browse ? shopLife.browse()[idx] : null;
            if (keepOf(st)) return rec;
            ensure();
            var fig = null;
            try {
              if (window.kin && kin.of) fig = kin.of(st && (st.guestName || rec.name));
            } catch (e) {
            }
            if (!fig) return rec;
            var h = ofName(fig.n);
            if (!h) return rec;
            if (rec.phase === "look" && st && !st._houseSaid) {
              st._houseSaid = 1;
              if (h.head === fig.n && shopDay() >= 1 && !state().asked && ((idx | 0) === 3 || rec.kind === "collector")) {
                askHead(rec, st, fig, h);
              } else if (h.cadet === fig.n) {
                rec.line = "I'm of the " + h.n + "s. Not the head.";
                st.line = rec.line;
                st.lineUntil = now() + 3.6;
              } else if (h.prestige < 0.2 && rec.kind !== "kid") {
                rec.line = "The " + h.n + "s don't bag here.";
                rec.phase = "leave";
                st.phase = "leave";
                st.bought = false;
                st.line = rec.line;
              } else if (rec.kind !== "kid" && !rec.line) {
                rec.line = "The " + h.n + "s. " + (fig.want || h.want) + ".";
                st.line = rec.line;
              }
            }
            if (rec.phase === "pay" && st && st._houseAsk) noteBag(rec.want || st.want);
            if (rec.phase === "look" && st && h.rival) {
              try {
                var br = window.shopLife && shopLife.browse && shopLife.browse() || [];
                for (var i = 0; i < br.length; i++) {
                  if (!br[i] || br[i] === st) continue;
                  var other = br[i].guestName || br[i].name || "";
                  if (surn(other) === h.rival && (br[i].phase === "look" || br[i].phase === "pay")) {
                    rec.line = "A " + h.rival + " is on the aisle. I'm not standing with them.";
                    rec.phase = "leave";
                    st.phase = "leave";
                    st.bought = false;
                    st.line = rec.line;
                    break;
                  }
                }
              } catch (eR) {
              }
            }
          } catch (e) {
          }
          return rec;
        };
      }
      function wrapChoir() {
        if (didChoir || !window.choir || !choir.weather) return;
        didChoir = true;
        var orig = choir.weather;
        choir.weather = function() {
          var w = orig.apply(this, arguments);
          try {
            var st = ensure();
            var high = 0;
            var low = 0;
            for (var i = 0; i < st.houses.length; i++) {
              if ((st.houses[i].prestige || 0) > 0.7) high++;
              if ((st.houses[i].prestige || 0) < 0.2) low++;
            }
            if (high) w.sweet = Math.min(1, (w.sweet || 0) + Math.min(0.1, high * 0.03));
            if (low) w.sour = Math.min(1, (w.sour || 0) + Math.min(0.12, low * 0.04));
            if (!w.line && st.last && st.day === shopDay()) w.line = st.last;
          } catch (e) {
          }
          return w;
        };
      }
      function whisper() {
        if (lastGold && now() - lastGoldAt < 12) return lastGold;
        return "";
      }
      function line() {
        return state().last || whisper();
      }
      function seedWiki() {
        try {
          var wiki = typeof WIKI === "function" ? WIKI() : window.WIKI;
          if (!wiki || !wiki.push) return;
          if (wiki.some(function(a) {
            return a && a.id === "k_house";
          }))
            return;
          wiki.push({
            id: "k_house",
            sec: "You and your people",
            t: "A surname is a house",
            tags: "dynasty house prestige cadet rival surname head pair",
            w: "<p>Fin's street already had names. A Costa is a house. A head. Prestige. A fish they bag. A rival they will not stand next to. Fill the pair the head asked for and the house stays. Miss it and they bag at Haymarket. Someone of the name, not the head, says they are of the name.</p><p><b>What to do about it:</b> keep two of what the head asked for. Do not put rivals on the aisle together. Life names the house.</p>"
          });
        } catch (e) {
        }
      }
      function tick() {
        try {
          wrapBrowse();
          wrapChoir();
          seedWiki();
          if (now() - lastTick > 1.5) {
            lastTick = now();
            ensure();
            noteMiss();
          }
        } catch (e) {
        }
      }
      function edge() {
        var st = ensure();
        if (st.filled) return 0.05;
        var n = 0, p = 0;
        for (var i = 0; i < st.houses.length; i++) {
          p += st.houses[i].prestige || 0.4;
          n++;
        }
        if (!n) return 0;
        var avg = p / n;
        if (avg < 0.22) return -0.06;
        if (avg > 0.7) return 0.05;
        return 0;
      }
      window.house = {
        whisper,
        line,
        of: state,
        edge,
        ofName,
        bump,
        seed: function(n) {
          ensure();
          var live = living();
          var fig = null;
          if (n && window.kin && kin.of) fig = kin.of(n);
          if (!fig) fig = live[0];
          if (!fig) return state();
          var h = ofName(fig.n) || { n: surn(fig.n), want: "goldfish", head: fig.n };
          var rec = { kind: "collector", phase: "look", line: "", name: fig.n };
          var st = { phase: "look", bought: false, guestName: fig.n };
          askHead(rec, st, fig, h, true);
          return state();
        }
      };
      if (window.__onBeat) window.__onBeat(tick, 240);
      else
        setTimeout(function loop() {
          tick();
          setTimeout(loop, 240);
        }, 220);
    })();
  } catch (e) {
    console.error("Layer house.js failed to start:", e);
  }

  // src/layers/wed.js
  try {
    (function() {
      "use strict";
      var lastTick = 0;
      var lastGold = "";
      var lastGoldAt = 0;
      var didBrowse = false;
      var didChoir = false;
      function now() {
        return typeof performance !== "undefined" ? performance.now() / 1e3 : Date.now() / 1e3;
      }
      function gs() {
        try {
          if (typeof gameState === "function") return gameState();
          if (typeof gameState === "object" && gameState) return gameState;
        } catch (e) {
        }
        return null;
      }
      function shopDay() {
        try {
          var g = gs();
          if (g && isFinite(g.t)) return Math.floor((g.t % 1e9 + 1e9) % 1e9 / 2400);
        } catch (e) {
        }
        return 0;
      }
      function because(text) {
        if (!text) return;
        try {
          if (window.weave && weave.because) weave.because(text);
        } catch (e) {
        }
        try {
          if (window.desk && desk.think) desk.think("wed", text);
        } catch (e2) {
        }
      }
      function egg(id, line2) {
        try {
          if (typeof findEgg === "function") findEgg(id, line2);
        } catch (e) {
        }
      }
      function gold(text, force) {
        if (!text) return;
        lastGold = text;
        lastGoldAt = now();
        try {
          var el = document.getElementById("hookWhisper");
          if (el && (force || el.textContent !== text)) {
            el.textContent = text;
            el.classList.add("on", "pop");
          }
        } catch (e) {
        }
      }
      function first(n) {
        return String(n || "Someone").split(" ")[0];
      }
      function surn(n) {
        var p = String(n || "").trim().split(/\s+/);
        return p.length > 1 ? p[p.length - 1] : p[0] || "";
      }
      function folk() {
        try {
          if (window.kin && kin.folk) return kin.folk() || [];
        } catch (e) {
        }
        return [];
      }
      function living() {
        return folk().filter(function(f) {
          return f && !f.dead && f.id !== "keep" && f.kind !== "kid" && f.job !== "still small";
        });
      }
      function state() {
        var g = gs();
        if (g && g.wed && typeof g.wed === "object") return g.wed;
        var st = { last: "", a: "", b: "", want: "", day: -1, bound: false, seen: false };
        try {
          if (g) g.wed = st;
        } catch (e) {
        }
        return st;
      }
      function kindOf() {
        try {
          if (window.guild && guild.mandate) return String(guild.mandate() || "goldfish");
        } catch (e) {
        }
        return "goldfish";
      }
      function keepOf(st) {
        try {
          if (typeof keepGuest === "function") return keepGuest(st);
        } catch (e) {
        }
        return !!(st && (st._lateMae || st._goingHold || st._lateKid || st._mask));
      }
      function arrive(rec, st, other, force) {
        var ws = state();
        if (ws.seen && !force) return false;
        if (shopDay() < 1 && !force) return false;
        var want = kindOf();
        rec.kind = rec.kind === "kid" ? "collector" : rec.kind || "collector";
        rec.phase = "look";
        rec.want = want;
        rec.line = "We're taking vows. A pair of " + want + ".";
        if (st) {
          st._wed = 1;
          st.want = want;
          st.line = rec.line;
          st.lineUntil = now() + 7;
          st.guestName = rec.name || st.guestName;
          st.until = Math.max(st.until || 0, now() + 9);
        }
        if (other) {
          other._wed = 1;
          other.want = want;
          other.phase = "look";
          other.bought = false;
          other.line = "A pair. For the table.";
          other.lineUntil = now() + 7;
        }
        ws.seen = true;
        ws.day = shopDay();
        ws.a = st && (st.guestName || rec.name) || rec.name || "Someone";
        ws.b = other && (other.guestName || other.name) || "Someone on Salem";
        ws.want = want;
        ws.bound = false;
        var line2 = first(ws.a) + " and " + first(ws.b) + " are here for a pair.";
        ws.last = line2;
        because(line2 + " They are taking vows.");
        gold(line2, true);
        egg("wedpair", line2);
        return true;
      }
      function noteBag(kind) {
        var ws = state();
        if (!ws.seen || ws.bound) return;
        if (ws.want && kind && String(kind).toLowerCase().indexOf(String(ws.want).toLowerCase()) >= 0) {
          ws.bound = true;
          var line2 = first(ws.a) + " and " + first(ws.b) + " took the pair. The houses bind.";
          ws.last = line2;
          because(line2);
          gold(line2, true);
          egg("wedbound", line2);
          try {
            if (window.kin && kin.gift) {
              kin.gift(ws.a, 0.12, "took a wedding pair");
              kin.gift(ws.b, 0.12, "took a wedding pair");
            }
          } catch (e) {
          }
          try {
            if (window.house && house.bump) {
              house.bump(surn(ws.a), 0.1);
              house.bump(surn(ws.b), 0.1);
            }
          } catch (e2) {
          }
        }
      }
      function noteMiss() {
        var ws = state();
        if (!ws.seen || ws.bound) return;
        if (shopDay() - (ws.day || 0) < 2) return;
        var line2 = first(ws.a) + " and " + first(ws.b) + " took their vows without a pair.";
        ws.last = line2;
        because(line2 + " Both families heard.");
        gold(line2, true);
        ws.bound = true;
        ws.missed = true;
        try {
          if (window.kin && kin.hurt) {
            kin.hurt(ws.a, 0.08, "no wedding pair");
            kin.hurt(ws.b, 0.08, "no wedding pair");
          }
        } catch (e) {
        }
      }
      function pickPair() {
        var live = living();
        if (live.length < 2) return null;
        var a = live[0];
        var b = live[1];
        if (surn(a.n) === surn(b.n) && live[2]) b = live[2];
        return { a: a.n, b: b.n };
      }
      function wrapBrowse() {
        if (didBrowse || !window.shopBrowse) return;
        didBrowse = true;
        var orig = window.shopBrowse;
        window.shopBrowse = function(idx, slot, W, floorY, personS, simT) {
          var rec = orig.apply(this, arguments);
          try {
            if (!rec) return rec;
            var st = window.shopLife && shopLife.browse ? shopLife.browse()[idx] : null;
            var ws = state();
            var keep = keepOf(st);
            if (!keep && rec.phase === "look" && st && !st._wed && shopDay() >= 1 && !ws.seen && ((idx | 0) === 2 || rec.kind === "collector")) {
              var other = null;
              try {
                var br = window.shopLife && shopLife.browse && shopLife.browse() || [];
                for (var i = 0; i < br.length; i++) {
                  if (!br[i] || br[i] === st) continue;
                  if (br[i].phase === "leave") continue;
                  if (keepOf(br[i])) continue;
                  other = br[i];
                  break;
                }
              } catch (eB) {
              }
              arrive(rec, st, other);
            }
            if (ws.seen && rec.phase === "pay" && st && st._wed) noteBag(rec.want || st.want || ws.want);
            if (ws.seen && ws.day === shopDay() && rec.phase === "look" && st && !st._wed && !st._wedSaid && !keep) {
              st._wedSaid = 1;
              rec.line = rec.kind === "kid" ? "They're getting married." : first(ws.a) + " and " + first(ws.b) + " are taking vows.";
              st.line = rec.line;
            }
            if (ws.missed && rec.phase === "look" && st && !st._wedMiss && !keep) {
              st._wedMiss = 1;
              if (st.guestName === ws.a || st.guestName === ws.b || rec.name === ws.a || rec.name === ws.b) {
                rec.line = "We took the vows. You didn't have the pair.";
                rec.phase = "leave";
                st.phase = "leave";
                st.bought = false;
                st.line = rec.line;
              }
            }
          } catch (e) {
          }
          return rec;
        };
      }
      function wrapChoir() {
        if (didChoir || !window.choir || !choir.weather) return;
        didChoir = true;
        var orig = choir.weather;
        choir.weather = function() {
          var w = orig.apply(this, arguments);
          try {
            var ws = state();
            if (ws.bound && !ws.missed) {
              w.sweet = Math.min(1, (w.sweet || 0) + 0.1);
              if (!w.line) w.line = first(ws.a) + " and " + first(ws.b) + " took the pair.";
            } else if (ws.missed) {
              w.sour = Math.min(1, (w.sour || 0) + 0.08);
            } else if (ws.seen && ws.day === shopDay()) {
              w.sweet = Math.min(1, (w.sweet || 0) + 0.05);
              if (!w.line) w.line = first(ws.a) + " and " + first(ws.b) + " are here for a pair.";
            }
          } catch (e) {
          }
          return w;
        };
      }
      function whisper() {
        if (lastGold && now() - lastGoldAt < 12) return lastGold;
        return "";
      }
      function line() {
        return state().last || whisper();
      }
      function seedWiki() {
        try {
          var wiki = typeof WIKI === "function" ? WIKI() : window.WIKI;
          if (!wiki || !wiki.push) return;
          if (wiki.some(function(a) {
            return a && a.id === "k_wed";
          }))
            return;
          wiki.push({
            id: "k_wed",
            sec: "You and your people",
            t: "They are taking vows",
            tags: "wedding marriage betrothal alliance pair vows houses bind",
            w: "<p>Alliances, betrothals, a feast that is a wedding. Fin's does not open a character sheet. Two walk-ins, one kind, a pair as the gift. Fill it and the houses bind. Miss it and both families hear. Kids say they are getting married.</p><p><b>What to do about it:</b> keep two of what they asked for. The gold line names the pair. Life writes whether the houses bound.</p>"
          });
        } catch (e) {
        }
      }
      function tick() {
        try {
          wrapBrowse();
          wrapChoir();
          seedWiki();
          if (now() - lastTick > 1.6) {
            lastTick = now();
            noteMiss();
          }
        } catch (e) {
        }
      }
      function edge() {
        var ws = state();
        if (ws.missed) return -0.07;
        if (ws.bound) return 0.06;
        if (ws.seen && ws.day === shopDay()) return 0.03;
        return 0;
      }
      window.wed = {
        whisper,
        line,
        of: state,
        edge,
        seed: function(a, b) {
          var pair = pickPair() || {};
          var rec = { kind: "collector", phase: "look", line: "", name: a || pair.a || "Tomas Russo" };
          var st = { phase: "look", bought: false, guestName: rec.name };
          var other = { phase: "look", bought: false, guestName: b || pair.b || "Nora Chen", name: b || pair.b || "Nora Chen" };
          arrive(rec, st, other, true);
          return state();
        }
      };
      if (window.__onBeat) window.__onBeat(tick, 240);
      else
        setTimeout(function loop() {
          tick();
          setTimeout(loop, 240);
        }, 220);
    })();
  } catch (e) {
    console.error("Layer wed.js failed to start:", e);
  }

  // src/layers/heir.js
  try {
    (function() {
      "use strict";
      var lastTick = 0;
      var lastGold = "";
      var lastGoldAt = 0;
      var didBrowse = false;
      var didChoir = false;
      var didFish = false;
      function now() {
        return typeof performance !== "undefined" ? performance.now() / 1e3 : Date.now() / 1e3;
      }
      function gs() {
        try {
          if (typeof gameState === "function") return gameState();
          if (typeof gameState === "object" && gameState) return gameState;
        } catch (e) {
        }
        return null;
      }
      function shopDay() {
        try {
          var g = gs();
          if (g && isFinite(g.t)) return Math.floor((g.t % 1e9 + 1e9) % 1e9 / 2400);
        } catch (e) {
        }
        return 0;
      }
      function because(text) {
        if (!text) return;
        try {
          if (window.weave && weave.because) weave.because(text);
        } catch (e) {
        }
        try {
          if (window.desk && desk.think) desk.think("heir", text);
        } catch (e2) {
        }
      }
      function egg(id, line2) {
        try {
          if (typeof findEgg === "function") findEgg(id, line2);
        } catch (e) {
        }
      }
      function gold(text, force) {
        if (!text) return;
        lastGold = text;
        lastGoldAt = now();
        try {
          var el = document.getElementById("hookWhisper");
          if (el && (force || el.textContent !== text)) {
            el.textContent = text;
            el.classList.add("on", "pop");
          }
        } catch (e) {
        }
      }
      function first(n) {
        return String(n || "Someone").split(" ")[0];
      }
      function surn(n) {
        var p = String(n || "").trim().split(/\s+/);
        return p.length > 1 ? p[p.length - 1] : p[0] || "";
      }
      function folk() {
        try {
          if (window.kin && kin.folk) return kin.folk() || [];
        } catch (e) {
        }
        return [];
      }
      function state() {
        var g = gs();
        if (g && g.heir && typeof g.heir === "object") return g.heir;
        var st = { last: "", child: "", parent: "", nick: "", day: -1, seen: false, dispute: "" };
        try {
          if (g) g.heir = st;
        } catch (e) {
        }
        return st;
      }
      function keepOf(st) {
        try {
          if (typeof keepGuest === "function") return keepGuest(st);
        } catch (e) {
        }
        return !!(st && (st._lateMae || st._goingHold || st._lateKid || st._mask));
      }
      function passWindows(parent, child) {
        var nick = "";
        try {
          if (!window.going || !going.homes) return "";
          var hs = going.homes() || [];
          for (var i = 0; i < hs.length; i++) {
            var h = hs[i];
            if (!h || h.dead) continue;
            if (h.who === parent.n || h.who === first(parent.n)) {
              h.who = child.n;
              h.heir = 1;
              if (h.nick) nick = h.nick;
            }
          }
        } catch (e) {
        }
        return nick;
      }
      function findPass() {
        var all = folk();
        var dead = [];
        var live = [];
        for (var i = 0; i < all.length; i++) {
          if (!all[i]) continue;
          if (all[i].dead && all[i].id !== "keep") dead.push(all[i]);
          else if (!all[i].dead) live.push(all[i]);
        }
        for (var d = 0; d < dead.length; d++) {
          var parent = dead[d];
          var child = null;
          var other = null;
          for (var k2 = 0; k2 < live.length; k2++) {
            if (live[k2].kinOf === parent.id || live[k2].how === "child") {
              if (!child) child = live[k2];
            } else if (surn(live[k2].n) === surn(parent.n) && live[k2].id !== "mae") {
              other = live[k2];
            }
          }
          if (!child) {
            for (var m = 0; m < live.length; m++) {
              if (surn(live[m].n) === surn(parent.n) && live[m].id !== "mae") {
                child = live[m];
                break;
              }
            }
          }
          if (child) return { parent, child, other: other && other !== child ? other : null };
        }
        return null;
      }
      function inherit(force) {
        var st = state();
        if (st.child && !force) return false;
        var pass = findPass();
        if (!pass && !force) return false;
        if (!pass) {
          if (!force) return false;
          var kid = folk().filter(function(f) {
            return f && !f.dead && f.how === "child";
          })[0];
          var par = kid && kid.kinOf ? folk().filter(function(f) {
            return f && f.id === kid.kinOf;
          })[0] : null;
          if (!kid || !par) return false;
          pass = { parent: par, child: kid, other: null };
        }
        var nick = passWindows(pass.parent, pass.child);
        st.child = pass.child.n;
        st.parent = pass.parent.n;
        st.nick = nick;
        st.dispute = pass.other ? pass.other.n : "";
        st.day = shopDay();
        pass.child.want = pass.parent.want || pass.child.want;
        pass.child.trust = Math.max(pass.child.trust || 0.4, (pass.parent.trust || 0.4) * 0.7);
        pass.child.last = "inherited the window";
        var line2 = first(st.child) + " is " + first(st.parent) + "'s now.";
        if (st.nick) line2 = first(st.child) + " is " + first(st.parent) + "'s now. " + st.nick + " is in the window.";
        st.last = line2;
        because(line2);
        if (shopDay() >= 1 || force) gold(line2, true);
        egg("heirwindow", line2);
        return true;
      }
      function wrapBrowse() {
        if (didBrowse || !window.shopBrowse) return;
        didBrowse = true;
        var orig = window.shopBrowse;
        window.shopBrowse = function(idx, slot, W, floorY, personS, simT) {
          var rec = orig.apply(this, arguments);
          try {
            if (!rec) return rec;
            var st = window.shopLife && shopLife.browse ? shopLife.browse()[idx] : null;
            var hs = state();
            if (!hs.child) inherit();
            var keep = keepOf(st);
            var who = st && (st.guestName || rec.name) || rec.name || "";
            if (hs.child && rec.phase === "look" && st && !st._heir && !keep && who === hs.child && shopDay() >= 1) {
              st._heir = 1;
              rec.line = st.nick ? "I'm " + first(hs.parent) + "'s now. " + hs.nick + " is in the window." : "I'm " + first(hs.parent) + "'s now. The window is mine.";
              rec.want = rec.want || (st.want = rec.want);
              st.line = rec.line;
              st.lineUntil = now() + 6;
              hs.seen = true;
              hs.day = shopDay();
              if (!hs.last || !/window/.test(hs.last)) {
                hs.last = rec.line;
                gold(rec.line, true);
              }
            } else if (hs.dispute && rec.phase === "look" && st && !st._heirSaid && !keep && who === hs.dispute) {
              st._heirSaid = 1;
              rec.line = "That's not theirs. I'm the " + surn(hs.parent) + ".";
              rec.phase = "leave";
              st.phase = "leave";
              st.bought = false;
              st.line = rec.line;
              because(first(hs.dispute) + " disputed the window.");
              gold(first(hs.dispute) + " says the window is not " + first(hs.child) + "'s.", true);
            } else if (hs.child && rec.phase === "look" && st && !st._heirSaid && !keep && rec.kind === "kid") {
              st._heirSaid = 1;
              rec.line = first(hs.child) + " has the window now.";
              st.line = rec.line;
            }
          } catch (e) {
          }
          return rec;
        };
      }
      function wrapChoir() {
        if (didChoir || !window.choir || !choir.weather) return;
        didChoir = true;
        var orig = choir.weather;
        choir.weather = function() {
          var w = orig.apply(this, arguments);
          try {
            var hs = state();
            if (hs.dispute) {
              w.sour = Math.min(1, (w.sour || 0) + 0.07);
              if (!w.line) w.line = first(hs.dispute) + " says the window is not " + first(hs.child) + "'s.";
            } else if (hs.child && hs.nick) {
              w.sweet = Math.min(1, (w.sweet || 0) + 0.04);
            }
          } catch (e) {
          }
          return w;
        };
      }
      function wrapFish() {
        if (didFish || typeof window.drawFishSprite !== "function") return;
        didFish = true;
        var orig = window.drawFishSprite;
        window.drawFishSprite = function(a) {
          try {
            var hs = state();
            if (hs.nick && a && a.fish && a.fish.nick === hs.nick) {
              a.fish._holdStill = 1;
              a.fish._gaze = now();
            }
          } catch (e) {
          }
          return orig.apply(this, arguments);
        };
      }
      function whisper() {
        if (lastGold && now() - lastGoldAt < 12) return lastGold;
        return "";
      }
      function line() {
        return state().last || whisper();
      }
      function seedWiki() {
        try {
          var wiki = typeof WIKI === "function" ? WIKI() : window.WIKI;
          if (!wiki || !wiki.push) return;
          if (wiki.some(function(a) {
            return a && a.id === "k_heir";
          }))
            return;
          wiki.push({
            id: "k_heir",
            sec: "You and your people",
            t: "A child inherits the window",
            tags: "succession heir inherit window disputed claim partition child",
            w: "<p>A disputed claim, a name that used to be someone else's. The windows on Salem already belonged to a person. They die. The child walks in. The fish is still in that glass. Someone of the same house says it is not theirs.</p><p><b>What to do about it:</b> the window changed hands. Life names the child. A disputed house will not bag. Do not sell the one that is still in that glass if the child is coming for the line.</p>"
          });
        } catch (e) {
        }
      }
      function tick() {
        try {
          wrapBrowse();
          wrapChoir();
          wrapFish();
          seedWiki();
          if (now() - lastTick > 1.7) {
            lastTick = now();
            if (!state().child) inherit();
          }
        } catch (e) {
        }
      }
      function edge() {
        return state().dispute ? -0.05 : 0;
      }
      window.heir = {
        whisper,
        line,
        of: state,
        edge,
        seed: function() {
          inherit(true);
          return state();
        }
      };
      if (window.__onBeat) window.__onBeat(tick, 240);
      else
        setTimeout(function loop() {
          tick();
          setTimeout(loop, 240);
        }, 220);
    })();
  } catch (e) {
    console.error("Layer heir.js failed to start:", e);
  }

  // src/layers/bent.js
  try {
    (function() {
      "use strict";
      var lastTick = 0;
      var lastGold = "";
      var lastGoldAt = 0;
      var didBrowse = false;
      var didChoir = false;
      var BENTS = [
        "paranoid",
        "wrathful",
        "kind",
        "greedy",
        "zealous",
        "craven",
        "ambitious",
        "honest",
        "deceitful",
        "gregarious",
        "shy",
        "temperate",
        "vengeful",
        "generous",
        "arrogant",
        "brave"
      ];
      function now() {
        return typeof performance !== "undefined" ? performance.now() / 1e3 : Date.now() / 1e3;
      }
      function gs() {
        try {
          if (typeof gameState === "function") return gameState();
          if (typeof gameState === "object" && gameState) return gameState;
        } catch (e) {
        }
        return null;
      }
      function shopDay() {
        try {
          var g = gs();
          if (g && isFinite(g.t)) return Math.floor((g.t % 1e9 + 1e9) % 1e9 / 2400);
        } catch (e) {
        }
        return 0;
      }
      function hash32(s) {
        var h = 2166136261;
        s = String(s || "");
        for (var i = 0; i < s.length; i++) {
          h ^= s.charCodeAt(i);
          h = Math.imul(h, 16777619);
        }
        return h >>> 0;
      }
      function pick(arr, h) {
        if (!arr || !arr.length) return "";
        return arr[(h >>> 0) % arr.length];
      }
      function because(text) {
        if (!text) return;
        try {
          if (window.weave && weave.because) weave.because(text);
        } catch (e) {
        }
        try {
          if (window.desk && desk.think) desk.think("bent", text);
        } catch (e2) {
        }
      }
      function gold(text, force) {
        if (!text) return;
        lastGold = text;
        lastGoldAt = now();
        try {
          var el = document.getElementById("hookWhisper");
          if (el && (force || el.textContent !== text)) {
            el.textContent = text;
            el.classList.add("on", "pop");
          }
        } catch (e) {
        }
      }
      function first(n) {
        return String(n || "Someone").split(" ")[0];
      }
      function folk() {
        try {
          if (window.kin && kin.folk) return kin.folk() || [];
        } catch (e) {
        }
        return [];
      }
      function state() {
        var g = gs();
        if (g && g.bent && typeof g.bent === "object") return g.bent;
        var st = { last: "", day: -1 };
        try {
          if (g) g.bent = st;
        } catch (e) {
        }
        return st;
      }
      function stamp(fig) {
        if (!fig || fig.bent) return fig && fig.bent;
        var h = hash32("bent:" + (fig.id || fig.n));
        var a = pick(BENTS, h);
        var b = pick(BENTS, h >>> 8);
        if (b === a) b = pick(BENTS, h >>> 4);
        fig.bent = a;
        fig.bent2 = b === a ? "" : b;
        return fig.bent;
      }
      function stampAll() {
        var all = folk();
        for (var i = 0; i < all.length; i++) stamp(all[i]);
      }
      function ofName(n) {
        try {
          if (window.kin && kin.of && n) {
            var fig = kin.of(n);
            if (fig) return stamp(fig);
          }
        } catch (e) {
        }
        return "";
      }
      function keepOf(st) {
        try {
          if (typeof keepGuest === "function") return keepGuest(st);
        } catch (e) {
        }
        return !!(st && (st._lateMae || st._goingHold || st._lateKid || st._mask));
      }
      function lookingCount() {
        var n = 0;
        try {
          var br = window.shopLife && shopLife.browse && shopLife.browse() || [];
          for (var i = 0; i < br.length; i++) {
            if (br[i] && (br[i].phase === "look" || br[i].phase === "pay")) n++;
          }
        } catch (e) {
        }
        return n;
      }
      function wet() {
        try {
          return !!(window.shopSite && shopSite.wet && shopSite.wet() > 0.36);
        } catch (e) {
        }
        return false;
      }
      function night() {
        try {
          if (window.pane && pane.night) return !!pane.night();
        } catch (e) {
        }
        return false;
      }
      function namedOf(want) {
        try {
          var list = typeof allFish === "function" ? allFish() || [] : [];
          for (var i = 0; i < list.length; i++) {
            if (list[i] && list[i].nick) return list[i];
          }
        } catch (e) {
        }
        return null;
      }
      function applyBent(rec, st, fig, bent2) {
        var line2 = "";
        var leave = false;
        if (bent2 === "paranoid" && lookingCount() >= 2) {
          line2 = "Too many people. I'm not staying.";
          leave = true;
        } else if (bent2 === "wrathful" && wet()) {
          line2 = "These boards. I'm not paying for this.";
          leave = true;
          try {
            if (window.kin && kin.hurt) kin.hurt(fig.n, 0.04, "wrath at the wet");
          } catch (e) {
          }
        } else if (bent2 === "kind" && wet()) {
          line2 = "I don't mind the boards. The usual.";
        } else if (bent2 === "greedy") {
          var named = namedOf();
          if (!named) {
            line2 = "I wanted a named one. The line.";
            leave = true;
          } else {
            line2 = "That's " + named.nick + ". Don't round it.";
            st.want = rec.want;
          }
        } else if (bent2 === "zealous") {
          try {
            if (window.vow && vow.of) {
              var v = vow.of();
              if (v && v.kind) {
                rec.want = v.kind;
                st.want = v.kind;
                line2 = "The " + v.kind + " is holy. Not the last of them.";
              }
            }
          } catch (eZ) {
          }
        } else if (bent2 === "craven") {
          var scare = false;
          try {
            if (window.siege && siege.of && siege.of().on) scare = true;
          } catch (eS) {
          }
          try {
            if (window.wane && wane.of && wane.of().on) scare = true;
          } catch (eW) {
          }
          if (scare || night()) {
            line2 = "I'm not staying. Not tonight.";
            leave = true;
          }
        } else if (bent2 === "ambitious" || bent2 === "arrogant") {
          try {
            if (window.seat && seat.of) {
              var ss = seat.of();
              if (ss && ss.mayor) {
                line2 = "I want the one the others follow. " + ss.mayor + ".";
              }
            }
          } catch (eA) {
          }
          if (!line2) line2 = "Not the usual. The one in the window.";
        } else if (bent2 === "honest") {
          try {
            if (window.pinch && pinch.of) {
              var p = pinch.of();
              if (p && p.who) line2 = first(p.who) + " took one. I saw it.";
            }
          } catch (eH) {
          }
        } else if (bent2 === "deceitful") {
          line2 = "Just looking. Names, if you have them.";
          leave = Math.random() < 0.45;
        } else if (bent2 === "gregarious") {
          line2 = fig.known ? "I brought someone. The usual." : "I'm " + first(fig.n) + ". We come together.";
        } else if (bent2 === "shy") {
          line2 = rec.kind === "kid" ? "…" : "The usual.";
        } else if (bent2 === "temperate" && wet()) {
          line2 = "The aisle is a mess. Another day.";
          leave = true;
        } else if (bent2 === "vengeful" && (fig.trust || 0.4) < 0.28) {
          line2 = "You sold us a miss. I'm here about that.";
          leave = true;
        } else if (bent2 === "generous") {
          line2 = "Whatever you have. I'll take it.";
        } else if (bent2 === "brave") {
          line2 = "I'm not walking. The usual.";
        }
        if (!line2) return false;
        rec.line = line2;
        st.line = line2;
        st.lineUntil = now() + 3.6;
        if (leave) {
          rec.phase = "leave";
          st.phase = "leave";
          st.bought = false;
        }
        return true;
      }
      function wrapBrowse() {
        if (didBrowse || !window.shopBrowse) return;
        didBrowse = true;
        var orig = window.shopBrowse;
        window.shopBrowse = function(idx, slot, W, floorY, personS, simT) {
          var rec = orig.apply(this, arguments);
          try {
            if (!rec) return rec;
            var st = window.shopLife && shopLife.browse ? shopLife.browse()[idx] : null;
            if (keepOf(st) || !st) return rec;
            stampAll();
            var fig = null;
            try {
              if (window.kin && kin.of) fig = kin.of(st.guestName || rec.name);
            } catch (e) {
            }
            if (!fig || fig.dead) return rec;
            var bent2 = stamp(fig);
            if (rec.phase === "look" && !st._bentSaid) {
              st._bentSaid = 1;
              if (applyBent(rec, st, fig, bent2) && rec.phase === "leave") {
                var bit = first(fig.n) + " walked. " + bent2 + ".";
                state().last = bit;
                if (shopDay() >= 1 && Math.random() < 0.22) {
                  because(bit);
                }
              }
            }
          } catch (e) {
          }
          return rec;
        };
      }
      function wrapChoir() {
        if (didChoir || !window.choir || !choir.weather) return;
        didChoir = true;
        var orig = choir.weather;
        choir.weather = function() {
          var w = orig.apply(this, arguments);
          try {
            var all = folk();
            var kind = 0;
            var wrath = 0;
            for (var i = 0; i < all.length; i++) {
              if (!all[i] || all[i].dead) continue;
              if (all[i].bent === "kind" || all[i].bent === "generous") kind++;
              if (all[i].bent === "wrathful" || all[i].bent === "vengeful") wrath++;
            }
            if (kind) w.sweet = Math.min(1, (w.sweet || 0) + Math.min(0.06, kind * 0.015));
            if (wrath) w.sour = Math.min(1, (w.sour || 0) + Math.min(0.08, wrath * 0.02));
          } catch (e) {
          }
          return w;
        };
      }
      function whisper() {
        if (lastGold && now() - lastGoldAt < 12) return lastGold;
        return "";
      }
      function line() {
        return state().last || whisper();
      }
      function seedWiki() {
        try {
          var wiki = typeof WIKI === "function" ? WIKI() : window.WIKI;
          if (!wiki || !wiki.push) return;
          if (wiki.some(function(a) {
            return a && a.id === "k_bent";
          }))
            return;
          wiki.push({
            id: "k_bent",
            sec: "You and your people",
            t: "A trait is why they walked",
            tags: "trait personality paranoid kind greedy zealous bent speech",
            w: "<p>Fin's inner lives were a like and a hate. Now the like has a bent: they leave if the aisle is crowded, they bag the wet boards, they want a named line, they tell you who took it. That is not a character sheet. That is why this person walked.</p><p><b>What to do about it:</b> listen. A greedy collector still wants a named one. A kind neighbor will stand in the puddle. Life writes the bent when they walk.</p>"
          });
        } catch (e) {
        }
      }
      function tick() {
        try {
          wrapBrowse();
          wrapChoir();
          seedWiki();
          if (now() - lastTick > 1.4) {
            lastTick = now();
            stampAll();
          }
        } catch (e) {
        }
      }
      window.bent = {
        whisper,
        line,
        of: state,
        ofName,
        seed: function(n, t) {
          stampAll();
          var fig = null;
          try {
            if (window.kin && kin.of) fig = kin.of(n) || (kin.folk() || [])[0];
          } catch (e) {
          }
          if (fig) {
            fig.bent = t || "paranoid";
            state().last = first(fig.n) + " is " + fig.bent + ".";
            gold(state().last, true);
          }
          return state();
        }
      };
      if (window.__onBeat) window.__onBeat(tick, 240);
      else
        setTimeout(function loop() {
          tick();
          setTimeout(loop, 240);
        }, 220);
    })();
  } catch (e) {
    console.error("Layer bent.js failed to start:", e);
  }

  // src/layers/plot.js
  try {
    (function() {
      "use strict";
      var lastTick = 0;
      var lastGold = "";
      var lastGoldAt = 0;
      var didBrowse = false;
      var didChoir = false;
      var KINDS = ["murder", "abduct", "sway"];
      function now() {
        return typeof performance !== "undefined" ? performance.now() / 1e3 : Date.now() / 1e3;
      }
      function gs() {
        try {
          if (typeof gameState === "function") return gameState();
          if (typeof gameState === "object" && gameState) return gameState;
        } catch (e) {
        }
        return null;
      }
      function shopDay() {
        try {
          var g = gs();
          if (g && isFinite(g.t)) return Math.floor((g.t % 1e9 + 1e9) % 1e9 / 2400);
        } catch (e) {
        }
        return 0;
      }
      function hash32(s) {
        var h = 2166136261;
        s = String(s || "");
        for (var i = 0; i < s.length; i++) {
          h ^= s.charCodeAt(i);
          h = Math.imul(h, 16777619);
        }
        return h >>> 0;
      }
      function because(text) {
        if (!text) return;
        try {
          if (window.weave && weave.because) weave.because(text);
        } catch (e) {
        }
        try {
          if (window.desk && desk.think) desk.think("plot", text);
        } catch (e2) {
        }
      }
      function egg(id, line2) {
        try {
          if (typeof findEgg === "function") findEgg(id, line2);
        } catch (e) {
        }
      }
      function gold(text, force) {
        if (!text) return;
        lastGold = text;
        lastGoldAt = now();
        try {
          var el = document.getElementById("hookWhisper");
          if (el && (force || el.textContent !== text)) {
            el.textContent = text;
            el.classList.add("on", "pop");
          }
        } catch (e) {
        }
      }
      function first(n) {
        return String(n || "Someone").split(" ")[0];
      }
      function folk() {
        try {
          if (window.kin && kin.folk) return kin.folk() || [];
        } catch (e) {
        }
        return [];
      }
      function living() {
        return folk().filter(function(f) {
          return f && !f.dead && f.id !== "keep" && f.id !== "mae";
        });
      }
      function state() {
        var g = gs();
        if (g && g.plot && typeof g.plot === "object") return g.plot;
        var st = { last: "", who: "", target: "", kind: "", day: -1, seen: false, done: false, bagged: false };
        try {
          if (g) g.plot = st;
        } catch (e) {
        }
        return st;
      }
      function keepOf(st) {
        try {
          if (typeof keepGuest === "function") return keepGuest(st);
        } catch (e) {
        }
        return !!(st && (st._lateMae || st._goingHold || st._lateKid || st._mask));
      }
      function pickScheme() {
        var live = living();
        if (live.length < 2) return null;
        var h = hash32("plot:" + shopDay());
        var a = live[h % live.length];
        var b = live[(h + 3) % live.length];
        if (a === b) b = live[(h + 1) % live.length];
        if (!a || !b || a === b) return null;
        return { who: a.n, target: b.n, kind: KINDS[h % KINDS.length] };
      }
      function start(rec, st, force) {
        var ps = state();
        if (ps.seen && !force) return false;
        if (shopDay() < 1 && !force) return false;
        var sch = pickScheme();
        if (!sch && !force) return false;
        if (!sch) sch = { who: rec.name || "Tomas Russo", target: "Ev Chen", kind: "murder" };
        rec.kind = "collector";
        rec.phase = "look";
        rec.name = sch.who;
        rec.line = sch.kind === "sway" ? "I'm waiting for " + first(sch.target) + "." : "A fish. For " + first(sch.target) + ".";
        if (st) {
          st._plot = 1;
          st.guestName = sch.who;
          st.line = rec.line;
          st.lineUntil = now() + 6;
          st.until = Math.max(st.until || 0, now() + 8);
        }
        ps.seen = true;
        ps.day = shopDay();
        ps.who = sch.who;
        ps.target = sch.target;
        ps.kind = sch.kind;
        ps.done = false;
        ps.bagged = false;
        var line2 = first(ps.who) + " is waiting for " + first(ps.target) + ".";
        ps.last = line2;
        because(line2);
        gold(line2, true);
        egg("plotwait", line2);
        return true;
      }
      function fire(targetSt) {
        var ps = state();
        if (!ps.seen || ps.done) return;
        ps.done = true;
        var line2 = "";
        if (ps.kind === "murder") {
          line2 = first(ps.who) + " sent a fish bag to " + first(ps.target) + ".";
          try {
            if (window.ill && ill.seed) ill.seed();
          } catch (e) {
          }
          try {
            if (window.going && going.homes) {
              var hs = going.homes() || [];
              for (var i = 0; i < hs.length; i++) {
                if (hs[i] && hs[i].who === ps.target) hs[i].ill = 1;
              }
            }
          } catch (e2) {
          }
        } else if (ps.kind === "abduct") {
          line2 = first(ps.target) + " left with " + first(ps.who) + ". Not a fish bag.";
          if (targetSt) {
            targetSt.phase = "leave";
            targetSt.bought = false;
            targetSt.line = "I have to go with them.";
          }
          try {
            if (window.kin && kin.hurt) kin.hurt(ps.target, 0.12, "taken from the aisle");
          } catch (e3) {
          }
        } else {
          line2 = first(ps.who) + " talked " + first(ps.target) + " around.";
          try {
            if (window.kin && kin.gift) kin.gift(ps.who, 0.06, "a sway on the aisle");
            if (window.kin && kin.hurt) kin.hurt(ps.target, 0.04, "talked around");
          } catch (e4) {
          }
        }
        ps.last = line2;
        because(line2);
        gold(line2, true);
        egg("plotfire", line2);
      }
      function wrapBrowse() {
        if (didBrowse || !window.shopBrowse) return;
        didBrowse = true;
        var orig = window.shopBrowse;
        window.shopBrowse = function(idx, slot, W, floorY, personS, simT) {
          var rec = orig.apply(this, arguments);
          try {
            if (!rec) return rec;
            var st = window.shopLife && shopLife.browse ? shopLife.browse()[idx] : null;
            var ps = state();
            var keep = keepOf(st);
            if (!keep && rec.phase === "look" && st && !st._plot && shopDay() >= 1 && !ps.seen && (idx | 0) === 0) {
              start(rec, st);
            }
            if (ps.seen && rec.phase === "pay" && st && st._plot) {
              ps.bagged = true;
            }
            if (ps.seen && !ps.done && st && !keep) {
              var who = st.guestName || rec.name || "";
              if (who === ps.target && (rec.phase === "look" || rec.phase === "pay")) {
                fire(st);
                rec.line = st.line || rec.line;
                rec.phase = st.phase || rec.phase;
              }
            }
            if (ps.seen && ps.day === shopDay() && rec.phase === "look" && st && !st._plot && !st._plotSaid && !keep) {
              st._plotSaid = 1;
              rec.line = rec.kind === "kid" ? first(ps.who) + " is waiting." : first(ps.who) + " is waiting for " + first(ps.target) + ".";
              st.line = rec.line;
            }
          } catch (e) {
          }
          return rec;
        };
      }
      function wrapChoir() {
        if (didChoir || !window.choir || !choir.weather) return;
        didChoir = true;
        var orig = choir.weather;
        choir.weather = function() {
          var w = orig.apply(this, arguments);
          try {
            var ps = state();
            if (ps.done && ps.kind !== "sway") {
              w.sour = Math.min(1, (w.sour || 0) + 0.12);
              if (!w.line) w.line = ps.last;
            } else if (ps.seen && !ps.done && ps.day === shopDay()) {
              w.sour = Math.min(1, (w.sour || 0) + 0.05);
              if (!w.line) w.line = first(ps.who) + " is waiting for " + first(ps.target) + ".";
            }
          } catch (e) {
          }
          return w;
        };
      }
      function whisper() {
        if (lastGold && now() - lastGoldAt < 12) return lastGold;
        return "";
      }
      function line() {
        return state().last || whisper();
      }
      function seedWiki() {
        try {
          var wiki = typeof WIKI === "function" ? WIKI() : window.WIKI;
          if (!wiki || !wiki.push) return;
          if (wiki.some(function(a) {
            return a && a.id === "k_plot";
          }))
            return;
          wiki.push({
            id: "k_plot",
            sec: "The shop floor",
            t: "Someone is waiting",
            tags: "scheme plot murder abduct sway intrigue wait aisle",
            w: "<p>Fin's shop is the room they wait in. A gift that is a vector. A person who does not bag. If the target walks in, the plot advances. Bag the gift and the window on Salem goes wrong.</p><p><b>What to do about it:</b> listen. A collector who is waiting is not buying. Life names who they waited for. The gold line does not cheer you.</p>"
          });
        } catch (e) {
        }
      }
      function tick() {
        try {
          wrapBrowse();
          wrapChoir();
          seedWiki();
          if (now() - lastTick > 1.5) lastTick = now();
        } catch (e) {
        }
      }
      function edge() {
        var ps = state();
        if (ps.done && ps.kind !== "sway") return -0.08;
        if (ps.seen && !ps.done && ps.day === shopDay()) return -0.03;
        return 0;
      }
      window.plot = {
        whisper,
        line,
        of: state,
        edge,
        seed: function(a, b) {
          var rec = { kind: "collector", phase: "look", line: "", name: a || "" };
          var st = { phase: "look", bought: false };
          start(rec, st, true);
          var ps = state();
          if (b) ps.target = b;
          return ps;
        }
      };
      if (window.__onBeat) window.__onBeat(tick, 240);
      else
        setTimeout(function loop() {
          tick();
          setTimeout(loop, 240);
        }, 220);
    })();
  } catch (e) {
    console.error("Layer plot.js failed to start:", e);
  }

  // src/layers/hush.js
  try {
    (function() {
      "use strict";
      var lastTick = 0;
      var lastGold = "";
      var lastGoldAt = 0;
      var didBrowse = false;
      var didChoir = false;
      function now() {
        return typeof performance !== "undefined" ? performance.now() / 1e3 : Date.now() / 1e3;
      }
      function gs() {
        try {
          if (typeof gameState === "function") return gameState();
          if (typeof gameState === "object" && gameState) return gameState;
        } catch (e) {
        }
        return null;
      }
      function shopDay() {
        try {
          var g = gs();
          if (g && isFinite(g.t)) return Math.floor((g.t % 1e9 + 1e9) % 1e9 / 2400);
        } catch (e) {
        }
        return 0;
      }
      function because(text) {
        if (!text) return;
        try {
          if (window.weave && weave.because) weave.because(text);
        } catch (e) {
        }
        try {
          if (window.desk && desk.think) desk.think("hush", text);
        } catch (e2) {
        }
      }
      function egg(id, line2) {
        try {
          if (typeof findEgg === "function") findEgg(id, line2);
        } catch (e) {
        }
      }
      function gold(text, force) {
        if (!text) return;
        lastGold = text;
        lastGoldAt = now();
        try {
          var el = document.getElementById("hookWhisper");
          if (el && (force || el.textContent !== text)) {
            el.textContent = text;
            el.classList.add("on", "pop");
          }
        } catch (e) {
        }
      }
      function first(n) {
        return String(n || "Someone").split(" ")[0];
      }
      function folk() {
        try {
          if (window.kin && kin.folk) return kin.folk() || [];
        } catch (e) {
        }
        return [];
      }
      function living() {
        return folk().filter(function(f) {
          return f && !f.dead && f.id !== "keep";
        });
      }
      function state() {
        var g = gs();
        if (g && g.hush && typeof g.hush === "object") return g.hush;
        var st = { last: "", who: "", secret: "", want: "", day: -1, seen: false, told: false, hooked: false };
        try {
          if (g) g.hush = st;
        } catch (e) {
        }
        return st;
      }
      function keepOf(st) {
        try {
          if (typeof keepGuest === "function") return keepGuest(st);
        } catch (e) {
        }
        return !!(st && (st._lateMae || st._goingHold || st._lateKid || st._mask));
      }
      function findSecret() {
        try {
          if (window.pinch && pinch.of) {
            var p = pinch.of();
            if (p && p.who) return { secret: first(p.who) + " took one. No fish bag.", want: p.sp || "goldfish" };
          }
        } catch (e) {
        }
        try {
          if (window.heir && heir.of) {
            var h = heir.of();
            if (h && h.dispute) return { secret: first(h.child) + " is not the only " + (h.parent || "heir") + ".", want: "goldfish" };
          }
        } catch (e2) {
        }
        try {
          if (window.late && late.keeper) {
            var k2 = late.keeper();
            if (k2 && k2.n) return { secret: "You're not " + first(k2.n) + ".", want: "goldfish" };
          }
        } catch (e3) {
        }
        try {
          if (window.bond && bond.of) {
            var b = bond.of();
            if (b && b.kind === "lover" && b.a) return { secret: first(b.a) + " and " + first(b.b) + " are not just kin.", want: "guppy" };
          }
        } catch (e4) {
        }
        return { secret: "the one that went missing", want: "goldfish" };
      }
      function start(rec, st, force) {
        var hs = state();
        if (hs.seen && !force) return false;
        if (shopDay() < 1 && !force) return false;
        var live = living();
        var who = st && st.guestName || rec.name || live[1] && live[1].n || "Vabal Chen";
        var sec = findSecret();
        rec.kind = rec.kind === "kid" ? "collector" : rec.kind || "collector";
        rec.phase = "look";
        rec.want = sec.want;
        rec.line = "Bag me that " + sec.want + " or the block hears.";
        rec.name = who;
        if (st) {
          st._hush = 1;
          st.want = sec.want;
          st.guestName = who;
          st.line = rec.line;
          st.lineUntil = now() + 7;
          st.until = Math.max(st.until || 0, now() + 9);
        }
        hs.seen = true;
        hs.day = shopDay();
        hs.who = who;
        hs.secret = sec.secret;
        hs.want = sec.want;
        hs.told = false;
        hs.hooked = false;
        var line2 = first(who) + " has a hook. " + sec.secret;
        hs.last = first(who) + " has a hook at the till.";
        because(line2);
        gold(hs.last, true);
        egg("hushhook", hs.last);
        return true;
      }
      function tell() {
        var hs = state();
        if (!hs.seen || hs.told || hs.hooked) return;
        hs.told = true;
        var line2 = "The block heard. " + hs.secret;
        hs.last = line2;
        because(line2);
        gold(line2, true);
        egg("hushtold", line2);
        try {
          if (window.kin && kin.hurt) kin.hurt(hs.who, 0.04, "told a secret");
        } catch (e) {
        }
      }
      function hookShop() {
        var hs = state();
        if (!hs.seen || hs.hooked) return;
        hs.hooked = true;
        var line2 = first(hs.who) + " has a hook on this counter.";
        hs.last = line2;
        because(line2);
        gold(line2, true);
        egg("hushpaid", line2);
      }
      function wrapBrowse() {
        if (didBrowse || !window.shopBrowse) return;
        didBrowse = true;
        var orig = window.shopBrowse;
        window.shopBrowse = function(idx, slot, W, floorY, personS, simT) {
          var rec = orig.apply(this, arguments);
          try {
            if (!rec) return rec;
            var st = window.shopLife && shopLife.browse ? shopLife.browse()[idx] : null;
            var hs = state();
            var keep = keepOf(st);
            if (!keep && rec.phase === "look" && st && !st._hush && shopDay() >= 1 && !hs.seen && rec.kind !== "kid" && ((idx | 0) === 4 || rec.kind === "lunch")) {
              start(rec, st);
            }
            if (hs.seen && rec.phase === "pay" && st && st._hush) hookShop();
            if (hs.seen && rec.phase === "leave" && st && st._hush && !st.bought && !hs.hooked) tell();
            if (hs.told && rec.phase === "look" && st && !st._hushHeard && !keep) {
              st._hushHeard = 1;
              rec.line = rec.kind === "kid" ? "They said a secret." : hs.secret;
              st.line = rec.line;
            }
          } catch (e) {
          }
          return rec;
        };
      }
      function wrapChoir() {
        if (didChoir || !window.choir || !choir.weather) return;
        didChoir = true;
        var orig = choir.weather;
        choir.weather = function() {
          var w = orig.apply(this, arguments);
          try {
            var hs = state();
            if (hs.told) {
              w.sour = Math.min(1, (w.sour || 0) + 0.1);
              if (!w.line) w.line = "The block heard.";
            } else if (hs.hooked) {
              w.sour = Math.min(1, (w.sour || 0) + 0.05);
            }
          } catch (e) {
          }
          return w;
        };
      }
      function whisper() {
        if (lastGold && now() - lastGoldAt < 12) return lastGold;
        return "";
      }
      function line() {
        return state().last || whisper();
      }
      function seedWiki() {
        try {
          var wiki = typeof WIKI === "function" ? WIKI() : window.WIKI;
          if (!wiki || !wiki.push) return;
          if (wiki.some(function(a) {
            return a && a.id === "k_hush";
          }))
            return;
          wiki.push({
            id: "k_hush",
            sec: "The shop floor",
            t: "A secret is a hook",
            tags: "secret hook blackmail hush intrigue till tell",
            w: "<p>Fin's already had a theft, a keeper who walked back in, a child who inherited a window. Someone knows. They use it. Bag me that one or the block hears. Refuse and they tell. Bag it and they have a hook on the shop.</p><p><b>What to do about it:</b> a collector who talks like that is not browsing. Fill it or the gold line names the secret. Life writes which.</p>"
          });
        } catch (e) {
        }
      }
      function tick() {
        try {
          wrapBrowse();
          wrapChoir();
          seedWiki();
          if (now() - lastTick > 1.5) lastTick = now();
        } catch (e) {
        }
      }
      function edge() {
        var hs = state();
        if (hs.told) return -0.08;
        if (hs.hooked) return -0.04;
        return 0;
      }
      window.hush = {
        whisper,
        line,
        of: state,
        edge,
        seed: function(n) {
          var rec = { kind: "collector", phase: "look", line: "", name: n || "" };
          var st = { phase: "look", bought: false };
          start(rec, st, true);
          return state();
        }
      };
      if (window.__onBeat) window.__onBeat(tick, 240);
      else
        setTimeout(function loop() {
          tick();
          setTimeout(loop, 240);
        }, 220);
    })();
  } catch (e) {
    console.error("Layer hush.js failed to start:", e);
  }

  // src/layers/bond.js
  try {
    (function() {
      "use strict";
      var lastTick = 0;
      var lastGold = "";
      var lastGoldAt = 0;
      var didBrowse = false;
      var didChoir = false;
      var KINDS = ["friend", "rival", "lover", "nemesis", "soulmate"];
      function now() {
        return typeof performance !== "undefined" ? performance.now() / 1e3 : Date.now() / 1e3;
      }
      function gs() {
        try {
          if (typeof gameState === "function") return gameState();
          if (typeof gameState === "object" && gameState) return gameState;
        } catch (e) {
        }
        return null;
      }
      function shopDay() {
        try {
          var g = gs();
          if (g && isFinite(g.t)) return Math.floor((g.t % 1e9 + 1e9) % 1e9 / 2400);
        } catch (e) {
        }
        return 0;
      }
      function hash32(s) {
        var h = 2166136261;
        s = String(s || "");
        for (var i = 0; i < s.length; i++) {
          h ^= s.charCodeAt(i);
          h = Math.imul(h, 16777619);
        }
        return h >>> 0;
      }
      function because(text) {
        if (!text) return;
        try {
          if (window.weave && weave.because) weave.because(text);
        } catch (e) {
        }
        try {
          if (window.desk && desk.think) desk.think("bond", text);
        } catch (e2) {
        }
      }
      function egg(id, line2) {
        try {
          if (typeof findEgg === "function") findEgg(id, line2);
        } catch (e) {
        }
      }
      function gold(text, force) {
        if (!text) return;
        lastGold = text;
        lastGoldAt = now();
        try {
          var el = document.getElementById("hookWhisper");
          if (el && (force || el.textContent !== text)) {
            el.textContent = text;
            el.classList.add("on", "pop");
          }
        } catch (e) {
        }
      }
      function first(n) {
        return String(n || "Someone").split(" ")[0];
      }
      function folk() {
        try {
          if (window.kin && kin.folk) return kin.folk() || [];
        } catch (e) {
        }
        return [];
      }
      function living() {
        return folk().filter(function(f) {
          return f && !f.dead && f.id !== "keep";
        });
      }
      function state() {
        var g = gs();
        if (g && g.bond && typeof g.bond === "object") return g.bond;
        var st = { last: "", a: "", b: "", kind: "", day: -1 };
        try {
          if (g) g.bond = st;
        } catch (e) {
        }
        return st;
      }
      function ensure() {
        var st = state();
        if (st.a) return st;
        var live = living();
        if (live.length < 2) return st;
        var h = hash32("bond:" + (live[0].id || live[0].n) + (live[1].id || live[1].n));
        st.a = live[0].n;
        st.b = live[1].n;
        st.kind = KINDS[h % KINDS.length];
        live[0].bond = st.kind;
        live[0].bondWith = st.b;
        live[1].bond = st.kind;
        live[1].bondWith = st.a;
        return st;
      }
      function keepOf(st) {
        try {
          if (typeof keepGuest === "function") return keepGuest(st);
        } catch (e) {
        }
        return !!(st && (st._lateMae || st._goingHold || st._lateKid || st._mask));
      }
      function otherOnAisle(name) {
        try {
          var br = window.shopLife && shopLife.browse && shopLife.browse() || [];
          for (var i = 0; i < br.length; i++) {
            var n = br[i] && (br[i].guestName || br[i].name);
            if (n === name && br[i].phase !== "leave") return br[i];
          }
        } catch (e) {
        }
        return null;
      }
      function wrapBrowse() {
        if (didBrowse || !window.shopBrowse) return;
        didBrowse = true;
        var orig = window.shopBrowse;
        window.shopBrowse = function(idx, slot, W, floorY, personS, simT) {
          var rec = orig.apply(this, arguments);
          try {
            if (!rec) return rec;
            var st = window.shopLife && shopLife.browse ? shopLife.browse()[idx] : null;
            if (keepOf(st) || !st) return rec;
            var bs = ensure();
            if (!bs.a) return rec;
            var who = st.guestName || rec.name || "";
            var mate = who === bs.a ? bs.b : who === bs.b ? bs.a : "";
            if (!mate || rec.phase !== "look" || st._bondSaid) return rec;
            st._bondSaid = 1;
            var them = otherOnAisle(mate);
            if (bs.kind === "lover" || bs.kind === "soulmate") {
              if (them) {
                rec.want = rec.want || "goldfish";
                st.want = rec.want;
                rec.line = bs.kind === "soulmate" ? "We bag together or we don't." : "We're together. A pair of " + (st.want || "goldfish") + ".";
                st.line = rec.line;
                st.lineUntil = now() + 4;
                if (shopDay() >= 1 && !bs.last) {
                  bs.last = first(bs.a) + " and " + first(bs.b) + " came together.";
                  bs.day = shopDay();
                  because(bs.last);
                  gold(bs.last, true);
                  egg("bondpair", bs.last);
                }
              } else if (bs.kind === "soulmate") {
                rec.line = "Not without " + first(mate) + ".";
                rec.phase = "leave";
                st.phase = "leave";
                st.bought = false;
                st.line = rec.line;
              }
            } else if (bs.kind === "rival" && them) {
              rec.line = first(mate) + " is on the aisle. I'm not staying.";
              rec.phase = "leave";
              st.phase = "leave";
              st.bought = false;
              st.line = rec.line;
              try {
                if (window.kin && kin.hurt) {
                  kin.hurt(who, 0.03, "a rival on the aisle");
                  kin.hurt(mate, 0.03, "a rival on the aisle");
                }
              } catch (eR) {
              }
            } else if (bs.kind === "nemesis") {
              rec.line = "I'm here about " + first(mate) + "'s bag.";
              rec.phase = "leave";
              st.phase = "leave";
              st.bought = false;
              st.line = rec.line;
              try {
                if (window.kin && kin.hurt) kin.hurt(mate, 0.05, "a nemesis on the aisle");
              } catch (eN) {
              }
              if (shopDay() >= 1 && !bs.last) {
                bs.last = first(who) + " came to undo " + first(mate) + "'s bag.";
                gold(bs.last, true);
              }
            } else if (bs.kind === "friend") {
              rec.line = them ? first(mate) + " is here. The usual." : first(mate) + " said this counter was all right.";
              st.line = rec.line;
            }
          } catch (e) {
          }
          return rec;
        };
      }
      function wrapChoir() {
        if (didChoir || !window.choir || !choir.weather) return;
        didChoir = true;
        var orig = choir.weather;
        choir.weather = function() {
          var w = orig.apply(this, arguments);
          try {
            var bs = state();
            if (bs.kind === "lover" || bs.kind === "soulmate" || bs.kind === "friend") {
              w.sweet = Math.min(1, (w.sweet || 0) + 0.04);
            } else if (bs.kind === "rival" || bs.kind === "nemesis") {
              w.sour = Math.min(1, (w.sour || 0) + 0.05);
            }
          } catch (e) {
          }
          return w;
        };
      }
      function whisper() {
        if (lastGold && now() - lastGoldAt < 12) return lastGold;
        return "";
      }
      function line() {
        var st = state();
        if (st.last) return st.last;
        return whisper();
      }
      function seedWiki() {
        try {
          var wiki = typeof WIKI === "function" ? WIKI() : window.WIKI;
          if (!wiki || !wiki.push) return;
          if (wiki.some(function(a) {
            return a && a.id === "k_bond";
          }))
            return;
          wiki.push({
            id: "k_bond",
            sec: "You and your people",
            t: "Friend, rival, lover, nemesis",
            tags: "friend rival lover soulmate nemesis relation bond pair",
            w: "<p>They are a person you walk in with, or will not stand next to, or would undo a fish bag for. Fin's kin had grudges. Now two names have a bond. Lovers want a pair. Rivals walk. A nemesis comes to undo the last fish bag. A soulmate will not bag alone.</p><p><b>What to do about it:</b> listen for two names. Life writes the bond. A pair on the aisle is not always a wedding.</p>"
          });
        } catch (e) {
        }
      }
      function tick() {
        try {
          wrapBrowse();
          wrapChoir();
          seedWiki();
          if (now() - lastTick > 1.5) {
            lastTick = now();
            ensure();
          }
        } catch (e) {
        }
      }
      function edge() {
        var bs = state();
        if (bs.kind === "rival" || bs.kind === "nemesis") return -0.04;
        if ((bs.kind === "lover" || bs.kind === "soulmate") && bs.last) return 0.03;
        return 0;
      }
      window.bond = {
        whisper,
        line,
        of: state,
        edge,
        seed: function(a, b, k2) {
          var st = state();
          var live = living();
          st.a = a || live[0] && live[0].n || "Mae Costa";
          st.b = b || live[1] && live[1].n || "Tomas Russo";
          st.kind = k2 || "lover";
          st.last = first(st.a) + " and " + first(st.b) + " are " + st.kind + "s.";
          st.day = shopDay();
          gold(st.last, true);
          return st;
        }
      };
      if (window.__onBeat) window.__onBeat(tick, 240);
      else
        setTimeout(function loop() {
          tick();
          setTimeout(loop, 240);
        }, 220);
    })();
  } catch (e) {
    console.error("Layer bond.js failed to start:", e);
  }

  // src/layers/claim.js
  try {
    (function() {
      "use strict";
      var lastTick = 0;
      var lastGold = "";
      var lastGoldAt = 0;
      var didBrowse = false;
      var didChoir = false;
      var didFish = false;
      function now() {
        return typeof performance !== "undefined" ? performance.now() / 1e3 : Date.now() / 1e3;
      }
      function gs() {
        try {
          if (typeof gameState === "function") return gameState();
          if (typeof gameState === "object" && gameState) return gameState;
        } catch (e) {
        }
        return null;
      }
      function shopDay() {
        try {
          var g = gs();
          if (g && isFinite(g.t)) return Math.floor((g.t % 1e9 + 1e9) % 1e9 / 2400);
        } catch (e) {
        }
        return 0;
      }
      function because(text) {
        if (!text) return;
        try {
          if (window.weave && weave.because) weave.because(text);
        } catch (e) {
        }
        try {
          if (window.desk && desk.think) desk.think("claim", text);
        } catch (e2) {
        }
      }
      function egg(id, line2) {
        try {
          if (typeof findEgg === "function") findEgg(id, line2);
        } catch (e) {
        }
      }
      function gold(text, force) {
        if (!text) return;
        lastGold = text;
        lastGoldAt = now();
        try {
          var el = document.getElementById("hookWhisper");
          if (el && (force || el.textContent !== text)) {
            el.textContent = text;
            el.classList.add("on", "pop");
          }
        } catch (e) {
        }
      }
      function first(n) {
        return String(n || "Someone").split(" ")[0];
      }
      function surn(n) {
        var p = String(n || "").trim().split(/\s+/);
        return p.length > 1 ? p[p.length - 1] : p[0] || "";
      }
      function namedFish() {
        try {
          var list = typeof allFish === "function" ? allFish() || [] : [];
          for (var i = 0; i < list.length; i++) {
            if (list[i] && list[i].nick && !list[i].dead) return list[i];
          }
        } catch (e) {
        }
        return null;
      }
      function state() {
        var g = gs();
        if (g && g.claim && typeof g.claim === "object") return g.claim;
        var st = { last: "", who: "", nick: "", day: -1, seen: false, given: false, shop: false };
        try {
          if (g) g.claim = st;
        } catch (e) {
        }
        return st;
      }
      function keepOf(st) {
        try {
          if (typeof keepGuest === "function") return keepGuest(st);
        } catch (e) {
        }
        return !!(st && (st._lateMae || st._goingHold || st._lateKid || st._mask));
      }
      function start(rec, st, force) {
        var cs = state();
        if (cs.seen && !force) return false;
        if (shopDay() < 1 && !force) return false;
        var fish = namedFish();
        var who = st && st.guestName || rec.name || "Tomas Russo";
        rec.kind = "collector";
        rec.phase = "look";
        rec.name = who;
        if (fish) {
          rec.line = "That one is of my house. " + fish.nick + ". I'm not paying.";
          cs.nick = fish.nick;
        } else {
          rec.line = "Nedda said this counter would be mine.";
          cs.shop = true;
        }
        if (st) {
          st._claim = 1;
          st.guestName = who;
          st.line = rec.line;
          st.lineUntil = now() + 7;
          st.bought = false;
        }
        cs.seen = true;
        cs.day = shopDay();
        cs.who = who;
        cs.given = false;
        var line2 = fish ? first(who) + " claims " + fish.nick + "." : first(who) + " claims this counter.";
        cs.last = line2;
        because(line2);
        gold(line2, true);
        egg("claimask", line2);
        return true;
      }
      function give() {
        var cs = state();
        if (!cs.seen || cs.given) return;
        cs.given = true;
        var line2 = cs.nick ? first(cs.who) + " took " + cs.nick + ". No coin. A claim." : first(cs.who) + " was told the counter is not theirs.";
        cs.last = line2;
        because(line2);
        gold(line2, true);
        try {
          if (window.house && house.bump) house.bump(surn(cs.who), 0.08);
        } catch (e) {
        }
        try {
          if (window.kin && kin.gift) kin.gift(cs.who, 0.06, "a claim honored");
        } catch (e2) {
        }
      }
      function refuse() {
        var cs = state();
        if (!cs.seen || cs.given || cs.refused) return;
        cs.refused = true;
        var line2 = first(cs.who) + " walked. A slight.";
        cs.last = line2;
        because(line2);
        gold(line2, true);
        try {
          if (window.kin && kin.hurt) kin.hurt(cs.who, 0.1, "a claim refused");
        } catch (e) {
        }
        try {
          if (window.house && house.bump) house.bump(surn(cs.who), -0.1);
        } catch (e2) {
        }
      }
      function wrapBrowse() {
        if (didBrowse || !window.shopBrowse) return;
        didBrowse = true;
        var orig = window.shopBrowse;
        window.shopBrowse = function(idx, slot, W, floorY, personS, simT) {
          var rec = orig.apply(this, arguments);
          try {
            if (!rec) return rec;
            var st = window.shopLife && shopLife.browse ? shopLife.browse()[idx] : null;
            var cs = state();
            var keep = keepOf(st);
            if (!keep && rec.phase === "look" && st && !st._claim && shopDay() >= 1 && !cs.seen && rec.kind === "collector") {
              start(rec, st);
            }
            if (cs.seen && st && st._claim) {
              if (rec.phase === "pay") {
                give();
                rec.phase = "leave";
                st.phase = "leave";
                st.bought = true;
              } else if (rec.phase === "leave" && !st.bought) {
                refuse();
              } else if (rec.phase === "look") {
                rec.line = st.line || rec.line;
                rec.phase = "look";
              }
            }
            if (cs.seen && rec.phase === "look" && st && !st._claim && !st._claimSaid && !keep) {
              st._claimSaid = 1;
              rec.line = rec.kind === "kid" ? "They said that one is theirs." : first(cs.who) + " claims " + (cs.nick || "the counter") + ".";
              st.line = rec.line;
            }
          } catch (e) {
          }
          return rec;
        };
      }
      function wrapChoir() {
        if (didChoir || !window.choir || !choir.weather) return;
        didChoir = true;
        var orig = choir.weather;
        choir.weather = function() {
          var w = orig.apply(this, arguments);
          try {
            var cs = state();
            if (cs.refused) w.sour = Math.min(1, (w.sour || 0) + 0.08);
            else if (cs.given) w.sweet = Math.min(1, (w.sweet || 0) + 0.05);
            if (cs.last && cs.day === shopDay() && !w.line) w.line = cs.last;
          } catch (e) {
          }
          return w;
        };
      }
      function wrapFish() {
        if (didFish || typeof window.drawFishSprite !== "function") return;
        didFish = true;
        var orig = window.drawFishSprite;
        window.drawFishSprite = function(a) {
          try {
            var cs = state();
            if (cs.nick && a && a.fish && a.fish.nick === cs.nick) {
              a.fish._holdStill = 1;
              a.fish._gaze = now();
            }
          } catch (e) {
          }
          return orig.apply(this, arguments);
        };
      }
      function whisper() {
        if (lastGold && now() - lastGoldAt < 12) return lastGold;
        return "";
      }
      function line() {
        return state().last || whisper();
      }
      function seedWiki() {
        try {
          var wiki = typeof WIKI === "function" ? WIKI() : window.WIKI;
          if (!wiki || !wiki.push) return;
          if (wiki.some(function(a) {
            return a && a.id === "k_claim";
          }))
            return;
          wiki.push({
            id: "k_claim",
            sec: "The shop floor",
            t: "That one is of my house",
            tags: "claim pressed title named fish house counter slight",
            w: "<p>The named fish already had a line. Someone walks in and will not pay. They say the one in the water is theirs by blood. Bag it as a gift and the house stays. Refuse and they walk, and the street hears it as a slight. Once, someone claims the counter.</p><p><b>What to do about it:</b> the named one holds still. Life names the claim. A slight is a family that will not bag.</p>"
          });
        } catch (e) {
        }
      }
      function tick() {
        try {
          wrapBrowse();
          wrapChoir();
          wrapFish();
          seedWiki();
          if (now() - lastTick > 1.5) lastTick = now();
        } catch (e) {
        }
      }
      function edge() {
        var cs = state();
        if (cs.refused) return -0.07;
        if (cs.given) return 0.03;
        return 0;
      }
      window.claim = {
        whisper,
        line,
        of: state,
        edge,
        seed: function(n) {
          var rec = { kind: "collector", phase: "look", line: "", name: n || "" };
          var st = { phase: "look", bought: false };
          start(rec, st, true);
          return state();
        }
      };
      if (window.__onBeat) window.__onBeat(tick, 240);
      else
        setTimeout(function loop() {
          tick();
          setTimeout(loop, 240);
        }, 220);
    })();
  } catch (e) {
    console.error("Layer claim.js failed to start:", e);
  }

  // src/layers/fray.js
  try {
    (function() {
      "use strict";
      var lastTick = 0;
      var lastGold = "";
      var lastGoldAt = 0;
      var didBrowse = false;
      var didChoir = false;
      var BREAKS = ["confess", "weep", "isolate", "smash"];
      function now() {
        return typeof performance !== "undefined" ? performance.now() / 1e3 : Date.now() / 1e3;
      }
      function gs() {
        try {
          if (typeof gameState === "function") return gameState();
          if (typeof gameState === "object" && gameState) return gameState;
        } catch (e) {
        }
        return null;
      }
      function shopDay() {
        try {
          var g = gs();
          if (g && isFinite(g.t)) return Math.floor((g.t % 1e9 + 1e9) % 1e9 / 2400);
        } catch (e) {
        }
        return 0;
      }
      function hash32(s) {
        var h = 2166136261;
        s = String(s || "");
        for (var i = 0; i < s.length; i++) {
          h ^= s.charCodeAt(i);
          h = Math.imul(h, 16777619);
        }
        return h >>> 0;
      }
      function because(text) {
        if (!text) return;
        try {
          if (window.weave && weave.because) weave.because(text);
        } catch (e) {
        }
        try {
          if (window.desk && desk.think) desk.think("fray", text);
        } catch (e2) {
        }
      }
      function egg(id, line2) {
        try {
          if (typeof findEgg === "function") findEgg(id, line2);
        } catch (e) {
        }
      }
      function gold(text, force) {
        if (!text) return;
        lastGold = text;
        lastGoldAt = now();
        try {
          var el = document.getElementById("hookWhisper");
          if (el && (force || el.textContent !== text)) {
            el.textContent = text;
            el.classList.add("on", "pop");
          }
        } catch (e) {
        }
      }
      function first(n) {
        return String(n || "Someone").split(" ")[0];
      }
      function folk() {
        try {
          if (window.kin && kin.folk) return kin.folk() || [];
        } catch (e) {
        }
        return [];
      }
      function state() {
        var g = gs();
        if (g && g.fray && typeof g.fray === "object") return g.fray;
        var st = { last: "", who: "", kind: "", day: -1, seen: false };
        try {
          if (g) g.fray = st;
        } catch (e) {
        }
        return st;
      }
      function keepOf(st) {
        try {
          if (typeof keepGuest === "function") return keepGuest(st);
        } catch (e) {
        }
        return !!(st && (st._lateMae || st._goingHold || st._lateKid || st._mask));
      }
      function raise(fig, amt) {
        if (!fig || fig.dead) return;
        fig.fray = Math.min(1, (fig.fray || 0) + amt);
      }
      function tickStress() {
        var all = folk();
        for (var i = 0; i < all.length; i++) {
          var f = all[i];
          if (!f || f.dead) continue;
          if ((f.trust || 0.4) < 0.22) raise(f, 0.04);
          if (f.last && /row|took |died|hurt|walked|slight/.test(f.last)) raise(f, 0.03);
        }
        try {
          if (window.siege && siege.of && siege.of().on) {
            for (var j = 0; j < all.length; j++) if (all[j] && !all[j].dead) raise(all[j], 0.02);
          }
        } catch (e) {
        }
      }
      function spill() {
        try {
          if (!window.shopSite || !shopSite.of) return;
          var s = shopSite.of();
          if (!s || !s.cells) return;
          for (var i = 0; i < s.cells.length; i++) {
            if (s.cells[i].kind === "floor") s.cells[i].wet = Math.max(s.cells[i].wet || 0, 0.5);
          }
          s.wetMax = Math.max(s.wetMax || 0, 0.5);
        } catch (e) {
        }
      }
      function breakOf(fig) {
        var h = hash32("fray:" + (fig.id || fig.n) + shopDay());
        return BREAKS[h % BREAKS.length];
      }
      function fire(rec, st, fig, force) {
        var fs = state();
        if (fs.seen && fs.day === shopDay() && !force) return false;
        if (shopDay() < 1 && !force) return false;
        var kind = breakOf(fig);
        rec.phase = "look";
        rec.name = fig.n;
        if (kind === "confess") {
          rec.line = "I have to say it. " + (fig.last || "Something is wrong.");
        } else if (kind === "weep") {
          rec.line = "Anything. Just a fish. I can't go home empty.";
          rec.want = fig.want || "guppy";
          if (st) st.want = rec.want;
        } else if (kind === "isolate") {
          rec.line = "Don't talk to me. I'm looking.";
          rec.phase = "leave";
          if (st) {
            st.phase = "leave";
            st.bought = false;
          }
        } else {
          rec.line = "I didn't mean to. The boards.";
          rec.phase = "leave";
          if (st) {
            st.phase = "leave";
            st.bought = false;
          }
          spill();
        }
        if (st) {
          st._fray = 1;
          st.guestName = fig.n;
          st.line = rec.line;
          st.lineUntil = now() + 6;
        }
        fig.fray = 0.2;
        fs.seen = true;
        fs.day = shopDay();
        fs.who = fig.n;
        fs.kind = kind;
        var line2 = kind === "smash" ? first(fig.n) + " broke on the aisle. The boards are wet." : first(fig.n) + " broke on the aisle.";
        fs.last = line2;
        because(line2);
        gold(line2, true);
        egg("fray" + kind, line2);
        return true;
      }
      function wrapBrowse() {
        if (didBrowse || !window.shopBrowse) return;
        didBrowse = true;
        var orig = window.shopBrowse;
        window.shopBrowse = function(idx, slot, W, floorY, personS, simT) {
          var rec = orig.apply(this, arguments);
          try {
            if (!rec) return rec;
            var st = window.shopLife && shopLife.browse ? shopLife.browse()[idx] : null;
            if (keepOf(st) || !st) return rec;
            var fig = null;
            try {
              if (window.kin && kin.of) fig = kin.of(st.guestName || rec.name);
            } catch (e) {
            }
            if (!fig) return rec;
            if (rec.phase === "look" && !st._fray && (fig.fray || 0) > 0.72 && shopDay() >= 1) {
              fire(rec, st, fig);
            } else if (rec.phase === "look" && !st._fraySaid && (fig.fray || 0) > 0.45) {
              st._fraySaid = 1;
              rec.line = "I need a fish. Something that stays.";
              st.line = rec.line;
            }
          } catch (e) {
          }
          return rec;
        };
      }
      function wrapChoir() {
        if (didChoir || !window.choir || !choir.weather) return;
        didChoir = true;
        var orig = choir.weather;
        choir.weather = function() {
          var w = orig.apply(this, arguments);
          try {
            var fs = state();
            if (fs.seen && fs.day === shopDay()) {
              w.sour = Math.min(1, (w.sour || 0) + (fs.kind === "smash" ? 0.14 : 0.08));
              if (!w.line) w.line = fs.last;
            }
          } catch (e) {
          }
          return w;
        };
      }
      function whisper() {
        if (lastGold && now() - lastGoldAt < 12) return lastGold;
        return "";
      }
      function line() {
        return state().last || whisper();
      }
      function seedWiki() {
        try {
          var wiki = typeof WIKI === "function" ? WIKI() : window.WIKI;
          if (!wiki || !wiki.push) return;
          if (wiki.some(function(a) {
            return a && a.id === "k_fray";
          }))
            return;
          wiki.push({
            id: "k_fray",
            sec: "The shop floor",
            t: "Someone broke on the aisle",
            tags: "stress mental break confess weep isolate smash coping",
            w: "<p>Fin's people already had a row. This is one person. They confess. They weep and bag the wrong fish. They isolate. They smash and the boards go wet. A comfort fish is what they ask for.</p><p><b>What to do about it:</b> a person who is fraying is still a customer. Keep a small one. Mop if they smash. Life names the break.</p>"
          });
        } catch (e) {
        }
      }
      function tick() {
        try {
          wrapBrowse();
          wrapChoir();
          seedWiki();
          if (now() - lastTick > 1.6) {
            lastTick = now();
            tickStress();
          }
        } catch (e) {
        }
      }
      function edge() {
        var fs = state();
        if (!fs.seen || fs.day !== shopDay()) return 0;
        return fs.kind === "smash" ? -0.08 : -0.04;
      }
      window.fray = {
        whisper,
        line,
        of: state,
        edge,
        seed: function(n) {
          var fig = null;
          try {
            if (window.kin && kin.of) fig = kin.of(n);
            if (!fig && window.kin && kin.folk) fig = (kin.folk() || []).filter(function(f) {
              return f && !f.dead && f.id !== "keep";
            })[0];
          } catch (e) {
          }
          if (!fig) fig = { n: n || "Rita Russo", id: "f1", last: "walked" };
          fig.fray = 1;
          var rec = { kind: "collector", phase: "look", line: "", name: fig.n };
          var st = { phase: "look", bought: false, guestName: fig.n };
          fire(rec, st, fig, true);
          return state();
        }
      };
      if (window.__onBeat) window.__onBeat(tick, 240);
      else
        setTimeout(function loop() {
          tick();
          setTimeout(loop, 240);
        }, 220);
    })();
  } catch (e) {
    console.error("Layer fray.js failed to start:", e);
  }

  // src/layers/feast.js
  try {
    (function() {
      "use strict";
      var lastTick = 0;
      var lastGold = "";
      var lastGoldAt = 0;
      var didBrowse = false;
      var didChoir = false;
      var KINDS = ["feast", "hunt", "funeral", "pilgrimage"];
      function now() {
        return typeof performance !== "undefined" ? performance.now() / 1e3 : Date.now() / 1e3;
      }
      function gs() {
        try {
          if (typeof gameState === "function") return gameState();
          if (typeof gameState === "object" && gameState) return gameState;
        } catch (e) {
        }
        return null;
      }
      function shopDay() {
        try {
          var g = gs();
          if (g && isFinite(g.t)) return Math.floor((g.t % 1e9 + 1e9) % 1e9 / 2400);
        } catch (e) {
        }
        return 0;
      }
      function hash32(s) {
        var h = 2166136261;
        s = String(s || "");
        for (var i = 0; i < s.length; i++) {
          h ^= s.charCodeAt(i);
          h = Math.imul(h, 16777619);
        }
        return h >>> 0;
      }
      function because(text) {
        if (!text) return;
        try {
          if (window.weave && weave.because) weave.because(text);
        } catch (e) {
        }
        try {
          if (window.desk && desk.think) desk.think("feast", text);
        } catch (e2) {
        }
      }
      function egg(id, line2) {
        try {
          if (typeof findEgg === "function") findEgg(id, line2);
        } catch (e) {
        }
      }
      function gold(text, force) {
        if (!text) return;
        lastGold = text;
        lastGoldAt = now();
        try {
          var el = document.getElementById("hookWhisper");
          if (el && (force || el.textContent !== text)) {
            el.textContent = text;
            el.classList.add("on", "pop");
          }
        } catch (e) {
        }
      }
      function surn(n) {
        var p = String(n || "").trim().split(/\s+/);
        return p.length > 1 ? p[p.length - 1] : p[0] || "Someone";
      }
      function folk() {
        try {
          if (window.kin && kin.folk) return kin.folk() || [];
        } catch (e) {
        }
        return [];
      }
      function living() {
        return folk().filter(function(f) {
          return f && !f.dead && f.id !== "keep";
        });
      }
      function state() {
        var g = gs();
        if (g && g.feast && typeof g.feast === "object") return g.feast;
        var st = { last: "", who: "", house: "", kind: "", want: "", day: -1, seen: false, filled: false };
        try {
          if (g) g.feast = st;
        } catch (e) {
        }
        return st;
      }
      function keepOf(st) {
        try {
          if (typeof keepGuest === "function") return keepGuest(st);
        } catch (e) {
        }
        return !!(st && (st._lateMae || st._goingHold || st._lateKid || st._mask));
      }
      function pickWant(kind, who) {
        if (kind === "funeral") {
          var all = folk();
          for (var i = 0; i < all.length; i++) {
            if (all[i] && all[i].dead && all[i].want) return all[i].want;
          }
        }
        try {
          if (window.house && house.ofName) {
            var h = house.ofName(who);
            if (h && h.want) return h.want;
          }
        } catch (e) {
        }
        return "goldfish";
      }
      function askLine(kind, house2, want) {
        if (kind === "hunt") return "The " + house2 + "s are going out. A pair of " + want + " for the boat.";
        if (kind === "funeral") return "The " + house2 + "s are laying someone down. A pair of " + want + ".";
        if (kind === "pilgrimage") return "The " + house2 + "s are walking inland. A pair of " + want + " for the road.";
        return "The " + house2 + "s are laying a table. A pair of " + want + ".";
      }
      function start(rec, st, force) {
        var fs = state();
        if (fs.seen && !force) return false;
        if (shopDay() < 1 && !force) return false;
        var live = living();
        var who = st && st.guestName || rec.name || live[0] && live[0].n || "Mae Costa";
        var h = hash32("feast:" + shopDay() + who);
        var kind = KINDS[h % KINDS.length];
        var houseN = surn(who);
        var want = pickWant(kind, who);
        rec.kind = "collector";
        rec.phase = "look";
        rec.want = want;
        rec.name = who;
        rec.line = askLine(kind, houseN, want);
        if (st) {
          st._feast = 1;
          st.want = want;
          st.guestName = who;
          st.line = rec.line;
          st.lineUntil = now() + 7;
          st.until = Math.max(st.until || 0, now() + 9);
        }
        fs.seen = true;
        fs.day = shopDay();
        fs.who = who;
        fs.house = houseN;
        fs.kind = kind;
        fs.want = want;
        fs.filled = false;
        var line2 = kind === "feast" ? "The " + houseN + "s are laying a table." : kind === "hunt" ? "The " + houseN + "s are going out." : kind === "funeral" ? "The " + houseN + "s are laying someone down." : "The " + houseN + "s are walking inland.";
        fs.last = line2;
        because(line2 + " A pair of " + want + ".");
        gold(line2, true);
        egg("feast" + kind, line2);
        return true;
      }
      function noteBag(kind) {
        var fs = state();
        if (!fs.seen || fs.filled) return;
        if (fs.want && kind && String(kind).toLowerCase().indexOf(String(fs.want).toLowerCase()) >= 0) {
          fs.filled = true;
          var line2 = "The " + fs.house + "s took the pair. The table is laid.";
          if (fs.kind === "funeral") line2 = "The " + fs.house + "s took the pair. They laid someone down.";
          if (fs.kind === "hunt") line2 = "The " + fs.house + "s took the pair. The boat went out.";
          if (fs.kind === "pilgrimage") line2 = "The " + fs.house + "s took the pair. They walked inland.";
          fs.last = line2;
          because(line2);
          gold(line2, true);
          egg("feastfill", line2);
          try {
            if (window.house && house.bump) house.bump(fs.house, 0.1);
          } catch (e) {
          }
          try {
            if (window.kin && kin.gift) kin.gift(fs.who, 0.1, "laid the table");
          } catch (e2) {
          }
        }
      }
      function noteMiss() {
        var fs = state();
        if (!fs.seen || fs.filled) return;
        if (shopDay() - (fs.day || 0) < 2) return;
        fs.filled = true;
        fs.missed = true;
        var line2 = "The " + fs.house + "s laid the table at Haymarket.";
        fs.last = line2;
        because(line2);
        gold(line2, true);
        try {
          if (window.house && house.bump) house.bump(fs.house, -0.12);
        } catch (e) {
        }
      }
      function wrapBrowse() {
        if (didBrowse || !window.shopBrowse) return;
        didBrowse = true;
        var orig = window.shopBrowse;
        window.shopBrowse = function(idx, slot, W, floorY, personS, simT) {
          var rec = orig.apply(this, arguments);
          try {
            if (!rec) return rec;
            var st = window.shopLife && shopLife.browse ? shopLife.browse()[idx] : null;
            var fs = state();
            var keep = keepOf(st);
            if (!keep && rec.phase === "look" && st && !st._feast && shopDay() >= 1 && !fs.seen && ((idx | 0) === 1 || rec.kind === "collector")) {
              start(rec, st);
            }
            if (fs.seen && rec.phase === "pay" && st && st._feast) noteBag(rec.want || st.want || fs.want);
            if (fs.seen && rec.phase === "look" && st && !st._feast && !st._feastSaid && !keep) {
              st._feastSaid = 1;
              rec.line = rec.kind === "kid" ? "They're having a " + (fs.kind === "funeral" ? "funeral" : "table") + "." : "The " + fs.house + "s are " + (fs.kind === "feast" ? "laying a table" : fs.kind === "hunt" ? "going out" : fs.kind === "funeral" ? "laying someone down" : "walking inland") + ".";
              st.line = rec.line;
            }
          } catch (e) {
          }
          return rec;
        };
      }
      function wrapChoir() {
        if (didChoir || !window.choir || !choir.weather) return;
        didChoir = true;
        var orig = choir.weather;
        choir.weather = function() {
          var w = orig.apply(this, arguments);
          try {
            var fs = state();
            if (fs.filled && !fs.missed) {
              w.sweet = Math.min(1, (w.sweet || 0) + (fs.kind === "funeral" ? 0.04 : 0.1));
              if (!w.line) w.line = fs.last;
            } else if (fs.missed) {
              w.sour = Math.min(1, (w.sour || 0) + 0.07);
            } else if (fs.seen && fs.day === shopDay()) {
              w.sweet = Math.min(1, (w.sweet || 0) + 0.05);
              if (!w.line) w.line = fs.last;
            }
          } catch (e) {
          }
          return w;
        };
      }
      function whisper() {
        if (lastGold && now() - lastGoldAt < 12) return lastGold;
        return "";
      }
      function line() {
        return state().last || whisper();
      }
      function seedWiki() {
        try {
          var wiki = typeof WIKI === "function" ? WIKI() : window.WIKI;
          if (!wiki || !wiki.push) return;
          if (wiki.some(function(a) {
            return a && a.id === "k_feast";
          }))
            return;
          wiki.push({
            id: "k_feast",
            sec: "The quarter",
            t: "A house is laying a table",
            tags: "feast hunt funeral pilgrimage activity pair centerpiece house",
            w: "<p>Fin's does not open a calendar. A collector walks in. The Costas are laying a table. A pair as the centerpiece. Fill it and the house stays, the choir sweetens, they pay. Miss it and they go to Haymarket. A funeral wants the kind the dead one liked.</p><p><b>What to do about it:</b> keep two of what they asked for. The gold line names the table. Life writes whether it was laid here.</p>"
          });
        } catch (e) {
        }
      }
      function tick() {
        try {
          wrapBrowse();
          wrapChoir();
          seedWiki();
          if (now() - lastTick > 1.6) {
            lastTick = now();
            noteMiss();
          }
        } catch (e) {
        }
      }
      function edge() {
        var fs = state();
        if (fs.missed) return -0.07;
        if (fs.filled) return 0.06;
        if (fs.seen && fs.day === shopDay()) return 0.03;
        return 0;
      }
      window.feast = {
        whisper,
        line,
        of: state,
        edge,
        seed: function(n) {
          var rec = { kind: "collector", phase: "look", line: "", name: n || "" };
          var st = { phase: "look", bought: false };
          start(rec, st, true);
          return state();
        }
      };
      if (window.__onBeat) window.__onBeat(tick, 240);
      else
        setTimeout(function loop() {
          tick();
          setTimeout(loop, 240);
        }, 220);
    })();
  } catch (e) {
    console.error("Layer feast.js failed to start:", e);
  }

  // src/layers/ward.js
  try {
    (function() {
      "use strict";
      var lastTick = 0;
      var lastGold = "";
      var lastGoldAt = 0;
      var didBrowse = false;
      var didChoir = false;
      function now() {
        return typeof performance !== "undefined" ? performance.now() / 1e3 : Date.now() / 1e3;
      }
      function gs() {
        try {
          if (typeof gameState === "function") return gameState();
          if (typeof gameState === "object" && gameState) return gameState;
        } catch (e) {
        }
        return null;
      }
      function shopDay() {
        try {
          var g = gs();
          if (g && isFinite(g.t)) return Math.floor((g.t % 1e9 + 1e9) % 1e9 / 2400);
        } catch (e) {
        }
        return 0;
      }
      function hash32(s) {
        var h = 2166136261;
        s = String(s || "");
        for (var i = 0; i < s.length; i++) {
          h ^= s.charCodeAt(i);
          h = Math.imul(h, 16777619);
        }
        return h >>> 0;
      }
      function because(text) {
        if (!text) return;
        try {
          if (window.weave && weave.because) weave.because(text);
        } catch (e) {
        }
        try {
          if (window.desk && desk.think) desk.think("ward", text);
        } catch (e2) {
        }
      }
      function egg(id, line2) {
        try {
          if (typeof findEgg === "function") findEgg(id, line2);
        } catch (e) {
        }
      }
      function gold(text, force) {
        if (!text) return;
        lastGold = text;
        lastGoldAt = now();
        try {
          var el = document.getElementById("hookWhisper");
          if (el && (force || el.textContent !== text)) {
            el.textContent = text;
            el.classList.add("on", "pop");
          }
        } catch (e) {
        }
      }
      function first(n) {
        return String(n || "Someone").split(" ")[0];
      }
      function folk() {
        try {
          if (window.kin && kin.folk) return kin.folk() || [];
        } catch (e) {
        }
        return [];
      }
      function byId(id) {
        var all = folk();
        for (var i = 0; i < all.length; i++) if (all[i] && all[i].id === id) return all[i];
        return null;
      }
      function state() {
        var g = gs();
        if (g && g.ward && typeof g.ward === "object") return g.ward;
        var st = { last: "", kid: "", guard: "", day: -1, no: false, grown: "" };
        try {
          if (g) g.ward = st;
        } catch (e) {
        }
        return st;
      }
      function keepOf(st) {
        try {
          if (typeof keepGuest === "function") return keepGuest(st);
        } catch (e) {
        }
        return !!(st && (st._lateMae || st._goingHold || st._lateKid || st._mask));
      }
      function guardianOf(kid) {
        if (!kid) return null;
        if (kid.kinOf) {
          var g = byId(kid.kinOf);
          if (g && !g.dead) return g;
        }
        var all = folk();
        for (var i = 0; i < all.length; i++) {
          if (all[i] && !all[i].dead && all[i].id === "mae") return all[i];
        }
        return null;
      }
      function growUp() {
        var st = state();
        var all = folk();
        var d = shopDay();
        for (var i = 0; i < all.length; i++) {
          var f = all[i];
          if (!f || f.dead) continue;
          if (f.kind === "kid" && f.job === "still in school" && d - (f.bornD || 0) >= 8 && !f._grown) {
            f._grown = 1;
            f.kind = "lunch";
            f.job = "off the boats";
            var g = guardianOf(f);
            if (g && g.bent) f.bent = g.bent;
            else if (window.bent && bent.ofName) f.bent = bent.ofName(f.n) || f.bent;
            f.last = "old enough to bag alone";
            st.grown = f.n;
            var line2 = first(f.n) + " is old enough to bag alone.";
            st.last = line2;
            because(line2 + (g ? " They copied " + first(g.n) + "." : ""));
            if (d >= 1) gold(line2, true);
            egg("wardgrown", line2);
          }
        }
      }
      function wrapBrowse() {
        if (didBrowse || !window.shopBrowse) return;
        didBrowse = true;
        var orig = window.shopBrowse;
        window.shopBrowse = function(idx, slot, W, floorY, personS, simT) {
          var rec = orig.apply(this, arguments);
          try {
            if (!rec) return rec;
            var st = window.shopLife && shopLife.browse ? shopLife.browse()[idx] : null;
            if (keepOf(st) || !st) return rec;
            var fig = null;
            try {
              if (window.kin && kin.of) fig = kin.of(st.guestName || rec.name);
            } catch (e) {
            }
            if (!fig) return rec;
            var ws = state();
            if ((rec.kind === "kid" || fig.kind === "kid") && rec.phase === "look" && !st._wardSaid) {
              st._wardSaid = 1;
              var g = guardianOf(fig);
              var no = false;
              if (g) {
                var h = hash32("ward:" + fig.id + shopDay());
                no = h % 100 < 38;
                rec.line = no ? "The child wanted the " + (fig.want || "guppy") + ". " + first(g.n) + " said no." : first(g.n) + " said the " + (fig.want || "guppy") + " was all right.";
                st.line = rec.line;
                st.lineUntil = now() + 4;
                ws.kid = fig.n;
                ws.guard = g.n;
                ws.day = shopDay();
                ws.no = no;
                if (no) {
                  rec.phase = "leave";
                  st.phase = "leave";
                  st.bought = false;
                  ws.last = first(g.n) + " said no.";
                  if (shopDay() >= 1) {
                    because(ws.last + " " + first(fig.n) + " wanted the " + (fig.want || "guppy") + ".");
                    gold(first(fig.n) + " wanted the " + (fig.want || "guppy") + ". " + first(g.n) + " said no.", true);
                    egg("wardno", ws.last);
                  }
                } else {
                  ws.last = first(g.n) + " said the " + (fig.want || "guppy") + " was all right.";
                }
              }
            } else if (fig._grown && rec.phase === "look" && !st._wardGrown) {
              st._wardGrown = 1;
              rec.line = "I used to need someone. The usual.";
              st.line = rec.line;
            }
          } catch (e) {
          }
          return rec;
        };
      }
      function wrapChoir() {
        if (didChoir || !window.choir || !choir.weather) return;
        didChoir = true;
        var orig = choir.weather;
        choir.weather = function() {
          var w = orig.apply(this, arguments);
          try {
            var ws = state();
            if (ws.no && ws.day === shopDay()) w.sour = Math.min(1, (w.sour || 0) + 0.03);
            if (ws.grown) w.sweet = Math.min(1, (w.sweet || 0) + 0.03);
          } catch (e) {
          }
          return w;
        };
      }
      function whisper() {
        if (lastGold && now() - lastGoldAt < 12) return lastGold;
        return "";
      }
      function line() {
        return state().last || whisper();
      }
      function seedWiki() {
        try {
          var wiki = typeof WIKI === "function" ? WIKI() : window.WIKI;
          if (!wiki || !wiki.push) return;
          if (wiki.some(function(a) {
            return a && a.id === "k_ward";
          }))
            return;
          wiki.push({
            id: "k_ward",
            sec: "You and your people",
            t: "A child, and the aunt who says no",
            tags: "ward guardian education child coming of age aunt guppy",
            w: "<p>Fin's kids already asked for guppies. Now they do not come alone. The guardian decides. The child wanted the guppy. The aunt said no. A child who was still in school becomes someone with a bent. They copy the guardian.</p><p><b>What to do about it:</b> the kid is not the customer. Life names who said no. When they grow, they bag like the person who brought them.</p>"
          });
        } catch (e) {
        }
      }
      function tick() {
        try {
          wrapBrowse();
          wrapChoir();
          seedWiki();
          if (now() - lastTick > 1.7) {
            lastTick = now();
            growUp();
          }
        } catch (e) {
        }
      }
      function edge() {
        var ws = state();
        return ws.no && ws.day === shopDay() ? -0.03 : 0;
      }
      window.ward = {
        whisper,
        line,
        of: state,
        edge,
        seed: function() {
          var kids = folk().filter(function(f) {
            return f && !f.dead && (f.kind === "kid" || f.job === "still in school");
          });
          var kid = kids[0];
          if (!kid) return state();
          var g = guardianOf(kid);
          var st = state();
          st.kid = kid.n;
          st.guard = g ? g.n : "Mae Costa";
          st.no = true;
          st.day = shopDay();
          st.last = first(st.kid) + " wanted the guppy. " + first(st.guard) + " said no.";
          gold(st.last, true);
          because(st.last);
          return st;
        }
      };
      if (window.__onBeat) window.__onBeat(tick, 240);
      else
        setTimeout(function loop() {
          tick();
          setTimeout(loop, 240);
        }, 220);
    })();
  } catch (e) {
    console.error("Layer ward.js failed to start:", e);
  }

  // src/layers/gyve.js
  try {
    (function() {
      "use strict";
      var lastTick = 0;
      var lastGold = "";
      var lastGoldAt = 0;
      var didBrowse = false;
      var didChoir = false;
      function now() {
        return typeof performance !== "undefined" ? performance.now() / 1e3 : Date.now() / 1e3;
      }
      function gs() {
        try {
          if (typeof gameState === "function") return gameState();
          if (typeof gameState === "object" && gameState) return gameState;
        } catch (e) {
        }
        return null;
      }
      function shopDay() {
        try {
          var g = gs();
          if (g && isFinite(g.t)) return Math.floor((g.t % 1e9 + 1e9) % 1e9 / 2400);
        } catch (e) {
        }
        return 0;
      }
      function because(text) {
        if (!text) return;
        try {
          if (window.weave && weave.because) weave.because(text);
        } catch (e) {
        }
        try {
          if (window.desk && desk.think) desk.think("gyve", text);
        } catch (e2) {
        }
      }
      function egg(id, line2) {
        try {
          if (typeof findEgg === "function") findEgg(id, line2);
        } catch (e) {
        }
      }
      function gold(text, force) {
        if (!text) return;
        lastGold = text;
        lastGoldAt = now();
        try {
          var el = document.getElementById("hookWhisper");
          if (el && (force || el.textContent !== text)) {
            el.textContent = text;
            el.classList.add("on", "pop");
          }
        } catch (e) {
        }
      }
      function first(n) {
        return String(n || "Someone").split(" ")[0];
      }
      function folk() {
        try {
          if (window.kin && kin.folk) return kin.folk() || [];
        } catch (e) {
        }
        return [];
      }
      function living() {
        return folk().filter(function(f) {
          return f && !f.dead && f.id !== "keep" && f.id !== "mae";
        });
      }
      function kindOf() {
        try {
          if (window.guild && guild.mandate) return String(guild.mandate() || "goldfish");
        } catch (e) {
        }
        return "goldfish";
      }
      function state() {
        var g = gs();
        if (g && g.gyve && typeof g.gyve === "object") return g.gyve;
        var st = { last: "", who: "", held: "", want: "", day: -1, seen: false, freed: false };
        try {
          if (g) g.gyve = st;
        } catch (e) {
        }
        return st;
      }
      function keepOf(st) {
        try {
          if (typeof keepGuest === "function") return keepGuest(st);
        } catch (e) {
        }
        return !!(st && (st._lateMae || st._goingHold || st._lateKid || st._mask));
      }
      function siegeOn() {
        try {
          if (window.siege && siege.of) {
            var s = siege.of();
            if (s && (s.on || s.last)) return true;
          }
        } catch (e) {
        }
        return false;
      }
      function start(rec, st, force) {
        var gs_ = state();
        if (gs_.seen && !force) return false;
        if (shopDay() < 1 && !force) return false;
        var live = living();
        var who = st && st.guestName || rec.name || live[0] && live[0].n || "Walt Hale";
        var held = live[1] && live[1].n || "Pia Voss";
        if (held === who && live[2]) held = live[2].n;
        var want = kindOf();
        rec.kind = "collector";
        rec.phase = "look";
        rec.want = want;
        rec.name = who;
        rec.line = siegeOn() ? "They held " + first(held) + " under the keel. A pair of " + want + " and they walk." : "They held me. I was promised a " + want + ".";
        if (st) {
          st._gyve = 1;
          st.want = want;
          st.guestName = who;
          st.line = rec.line;
          st.lineUntil = now() + 7;
          st.until = Math.max(st.until || 0, now() + 9);
        }
        gs_.seen = true;
        gs_.day = shopDay();
        gs_.who = who;
        gs_.held = held;
        gs_.want = want;
        gs_.freed = false;
        var line2 = "They held " + first(held) + ". A pair would let them walk.";
        gs_.last = line2;
        because(line2);
        gold(line2, true);
        egg("gyveheld", line2);
        try {
          var fig = null;
          if (window.kin && kin.of) fig = kin.of(held);
          if (fig) fig.last = "held under the keel";
        } catch (e) {
        }
        return true;
      }
      function noteBag(kind) {
        var gs_ = state();
        if (!gs_.seen || gs_.freed) return;
        if (gs_.want && kind && String(kind).toLowerCase().indexOf(String(gs_.want).toLowerCase()) >= 0) {
          gs_.freed = true;
          var line2 = first(gs_.held) + " walks. The pair let them.";
          gs_.last = line2;
          because(line2);
          gold(line2, true);
          egg("gyvefree", line2);
          try {
            if (window.kin && kin.gift) {
              kin.gift(gs_.who, 0.1, "a ransom pair");
              kin.gift(gs_.held, 0.12, "walked free");
            }
          } catch (e) {
          }
        }
      }
      function noteMiss() {
        var gs_ = state();
        if (!gs_.seen || gs_.freed) return;
        if (shopDay() - (gs_.day || 0) < 3) return;
        gs_.freed = true;
        gs_.lost = true;
        var line2 = first(gs_.held) + " did not come back.";
        gs_.last = line2;
        because(line2);
        gold(line2, true);
        try {
          if (window.kin && kin.hurt) kin.hurt(gs_.held, 0.16, "did not come back");
        } catch (e) {
        }
      }
      function wrapBrowse() {
        if (didBrowse || !window.shopBrowse) return;
        didBrowse = true;
        var orig = window.shopBrowse;
        window.shopBrowse = function(idx, slot, W, floorY, personS, simT) {
          var rec = orig.apply(this, arguments);
          try {
            if (!rec) return rec;
            var st = window.shopLife && shopLife.browse ? shopLife.browse()[idx] : null;
            var gs_ = state();
            var keep = keepOf(st);
            var due = siegeOn() || shopDay() >= 3;
            if (!keep && rec.phase === "look" && st && !st._gyve && shopDay() >= 1 && !gs_.seen && due && rec.kind !== "kid") {
              start(rec, st);
            }
            if (gs_.seen && rec.phase === "pay" && st && st._gyve) noteBag(rec.want || st.want || gs_.want);
            if (gs_.seen && rec.phase === "look" && st && !st._gyve && !st._gyveSaid && !keep) {
              st._gyveSaid = 1;
              rec.line = rec.kind === "kid" ? first(gs_.held) + " isn't here." : "They held " + first(gs_.held) + " under the keel.";
              st.line = rec.line;
            }
            if (gs_.lost && rec.phase === "look" && st && (st.guestName === gs_.held || rec.name === gs_.held)) {
              rec.phase = "leave";
              st.phase = "leave";
              st.bought = false;
            }
          } catch (e) {
          }
          return rec;
        };
      }
      function wrapChoir() {
        if (didChoir || !window.choir || !choir.weather) return;
        didChoir = true;
        var orig = choir.weather;
        choir.weather = function() {
          var w = orig.apply(this, arguments);
          try {
            var gs_ = state();
            if (gs_.lost) {
              w.sour = Math.min(1, (w.sour || 0) + 0.12);
              if (!w.line) w.line = first(gs_.held) + " did not come back.";
            } else if (gs_.freed) {
              w.sweet = Math.min(1, (w.sweet || 0) + 0.07);
            } else if (gs_.seen && !gs_.freed) {
              w.sour = Math.min(1, (w.sour || 0) + 0.08);
              if (!w.line) w.line = "They held " + first(gs_.held) + ".";
            }
          } catch (e) {
          }
          return w;
        };
      }
      function whisper() {
        if (lastGold && now() - lastGoldAt < 12) return lastGold;
        return "";
      }
      function line() {
        return state().last || whisper();
      }
      function seedWiki() {
        try {
          var wiki = typeof WIKI === "function" ? WIKI() : window.WIKI;
          if (!wiki || !wiki.push) return;
          if (wiki.some(function(a) {
            return a && a.id === "k_gyve";
          }))
            return;
          wiki.push({
            id: "k_gyve",
            sec: "The quarter",
            t: "They held me. Let them walk.",
            tags: "prisoner ransom hostage keel war pair walk free",
            w: "<p>The war already shut the harbor. Someone walks in with a mark. They were promised a fish. Bag a pair and they let a name walk. Miss it and that name does not come back. A guard may speak for them.</p><p><b>What to do about it:</b> keep two of what they asked for. The gold line names who is held. Life writes whether they walked.</p>"
          });
        } catch (e) {
        }
      }
      function tick() {
        try {
          wrapBrowse();
          wrapChoir();
          seedWiki();
          if (now() - lastTick > 1.6) {
            lastTick = now();
            noteMiss();
          }
        } catch (e) {
        }
      }
      function edge() {
        var gs_ = state();
        if (gs_.lost) return -0.1;
        if (gs_.freed) return 0.05;
        if (gs_.seen && !gs_.freed) return -0.05;
        return 0;
      }
      window.gyve = {
        whisper,
        line,
        of: state,
        edge,
        seed: function(a, b) {
          var rec = { kind: "collector", phase: "look", line: "", name: a || "" };
          var st = { phase: "look", bought: false };
          start(rec, st, true);
          if (b) state().held = b;
          return state();
        }
      };
      if (window.__onBeat) window.__onBeat(tick, 240);
      else
        setTimeout(function loop() {
          tick();
          setTimeout(loop, 240);
        }, 220);
    })();
  } catch (e) {
    console.error("Layer gyve.js failed to start:", e);
  }

  // src/layers/fief.js
  try {
    (function() {
      "use strict";
      var lastTick = 0;
      var lastGold = "";
      var lastGoldAt = 0;
      var didBrowse = false;
      var didChoir = false;
      function now() {
        return typeof performance !== "undefined" ? performance.now() / 1e3 : Date.now() / 1e3;
      }
      function gs() {
        try {
          if (typeof gameState === "function") return gameState();
          if (typeof gameState === "object" && gameState) return gameState;
        } catch (e) {
        }
        return null;
      }
      function shopDay() {
        try {
          var g = gs();
          if (g && isFinite(g.t)) return Math.floor((g.t % 1e9 + 1e9) % 1e9 / 2400);
        } catch (e) {
        }
        return 0;
      }
      function because(text) {
        if (!text) return;
        try {
          if (window.weave && weave.because) weave.because(text);
        } catch (e) {
        }
        try {
          if (window.desk && desk.think) desk.think("fief", text);
        } catch (e2) {
        }
      }
      function egg(id, line2) {
        try {
          if (typeof findEgg === "function") findEgg(id, line2);
        } catch (e) {
        }
      }
      function gold(text, force) {
        if (!text) return;
        lastGold = text;
        lastGoldAt = now();
        try {
          var el = document.getElementById("hookWhisper");
          if (el && (force || el.textContent !== text)) {
            el.textContent = text;
            el.classList.add("on", "pop");
          }
        } catch (e) {
        }
      }
      function state() {
        var g = gs();
        if (g && g.fief && typeof g.fief === "object") return g.fief;
        var st = { last: "", liege: "the harbor", opinion: 0.5, tyrant: false, faction: false, day: -1 };
        try {
          if (g) g.fief = st;
        } catch (e) {
        }
        return st;
      }
      function keepOf(st) {
        try {
          if (typeof keepGuest === "function") return keepGuest(st);
        } catch (e) {
        }
        return !!(st && (st._lateMae || st._goingHold || st._lateKid || st._mask));
      }
      function hallStanding() {
        try {
          if (window.guild && guild.standing) return guild.standing();
        } catch (e) {
        }
        try {
          if (window.guild && guild.of) {
            var g = guild.of();
            if (g && isFinite(g.stand)) return g.stand;
            if (g && isFinite(g.standing)) return g.standing;
          }
        } catch (e2) {
        }
        return 0.5;
      }
      function tickOpinion() {
        var st = state();
        var stand = hallStanding();
        st.opinion = Math.max(0.04, Math.min(1, stand));
        var wasTyrant = st.tyrant;
        st.tyrant = st.opinion < 0.22;
        st.faction = st.opinion < 0.18;
        if (st.tyrant && !wasTyrant && shopDay() >= 1) {
          st.day = shopDay();
          st.last = "The harbor is cruel. They bag here as refuge.";
          because(st.last);
          gold(st.last, true);
          egg("fieftyrant", st.last);
        } else if (!st.tyrant && wasTyrant && shopDay() >= 1) {
          st.last = "The harbor eased. They pay the hall again.";
          because(st.last);
        }
      }
      function wrapBrowse() {
        if (didBrowse || !window.shopBrowse) return;
        didBrowse = true;
        var orig = window.shopBrowse;
        window.shopBrowse = function(idx, slot, W, floorY, personS, simT) {
          var rec = orig.apply(this, arguments);
          try {
            if (!rec) return rec;
            var st = window.shopLife && shopLife.browse ? shopLife.browse()[idx] : null;
            if (keepOf(st) || !st) return rec;
            var fs = state();
            if (rec.phase === "look" && !st._fiefSaid) {
              st._fiefSaid = 1;
              if (fs.faction && rec.kind !== "kid" && rec.kind !== "neighbor") {
                rec.line = "The faction said stay away. They pay the harbor.";
                rec.phase = "leave";
                st.phase = "leave";
                st.bought = false;
                st.line = rec.line;
                if (shopDay() >= 1 && !fs.last) {
                  fs.last = "A faction emptied the aisle.";
                  gold(fs.last, true);
                }
              } else if (fs.tyrant && rec.kind === "neighbor") {
                rec.line = "The harbor is cruel. I still come here.";
                st.line = rec.line;
              } else if (fs.tyrant && rec.kind === "lunch") {
                rec.line = "Dues at the hall. I bag here instead.";
                st.line = rec.line;
              } else if (!fs.tyrant && rec.kind === "collector" && !rec.line) {
                rec.line = "The harbor still has us. The usual.";
                st.line = rec.line;
              }
            }
          } catch (e) {
          }
          return rec;
        };
      }
      function wrapChoir() {
        if (didChoir || !window.choir || !choir.weather) return;
        didChoir = true;
        var orig = choir.weather;
        choir.weather = function() {
          var w = orig.apply(this, arguments);
          try {
            var fs = state();
            if (fs.faction) {
              w.sour = Math.min(1, (w.sour || 0) + 0.1);
              if (!w.line) w.line = "A faction emptied the aisle.";
            } else if (fs.tyrant) {
              w.sour = Math.min(1, (w.sour || 0) + 0.05);
              w.sweet = Math.min(1, (w.sweet || 0) + 0.04);
              if (!w.line) w.line = "The harbor is cruel. They bag here as refuge.";
            }
          } catch (e) {
          }
          return w;
        };
      }
      function whisper() {
        if (lastGold && now() - lastGoldAt < 12) return lastGold;
        return "";
      }
      function line() {
        return state().last || whisper();
      }
      function seedWiki() {
        try {
          var wiki = typeof WIKI === "function" ? WIKI() : window.WIKI;
          if (!wiki || !wiki.push) return;
          if (wiki.some(function(a) {
            return a && a.id === "k_fief";
          }))
            return;
          wiki.push({
            id: "k_fief",
            sec: "The quarter",
            t: "The harbor is the one they pay",
            tags: "vassal liege tyranny faction opinion dues harbor hall",
            w: "<p>The hall already collected dues. Now the harbor master is a mouth. People pay the harbor. If the hall is cruel they bag here as refuge, or they stop because the faction said so. Tyranny is an empty aisle with a reason.</p><p><b>What to do about it:</b> the hall's standing is how the harbor feels about you. A boycott was empty. A faction is empty and they say why. Life names the harbor.</p>"
          });
        } catch (e) {
        }
      }
      function tick() {
        try {
          wrapBrowse();
          wrapChoir();
          seedWiki();
          if (now() - lastTick > 1.5) {
            lastTick = now();
            tickOpinion();
          }
        } catch (e) {
        }
      }
      function edge() {
        var fs = state();
        if (fs.faction) return -0.1;
        if (fs.tyrant) return 0.03;
        return 0;
      }
      window.fief = {
        whisper,
        line,
        of: state,
        edge,
        seed: function() {
          var st = state();
          st.opinion = 0.12;
          st.tyrant = true;
          st.faction = true;
          st.day = shopDay();
          st.last = "The harbor is cruel. They bag here as refuge.";
          gold(st.last, true);
          because(st.last);
          return st;
        }
      };
      if (window.__onBeat) window.__onBeat(tick, 240);
      else
        setTimeout(function loop() {
          tick();
          setTimeout(loop, 240);
        }, 220);
    })();
  } catch (e) {
    console.error("Layer fief.js failed to start:", e);
  }
})();
